# ENGINEERING_NOTES.md — 地雷図鑑+診断プレイブック(v0.25.1350)

## 乱数の枝を足したら、両側の枝を固定するテストを書く(v0.25.3871)
片側しか書かないと、テストは**静かに確率的**になる。実例: 舞妓の近距離に抽選(バックロール30%)を
足した(v0.25.3584)のに、テストは「必ず毬の薙ぎ」のまま。**3回に1回CIが赤くなる**状態が長く放置され、
赤が「たまに落ちるやつ」として無視されるようになっていた(=安全網の劣化)。
作法: `vi.spyOn(Math,'random').mockReturnValue(...)` で**両側の枝をそれぞれ確定**させ、
抽選率そのものも**値でアサート**する(乱数を潰しただけで枝を隠さない)。

## ★0. トラブルシューティング索引(★不具合を見たら、コードより先にここ)

**この索引の下にある知識は、ほとんど「実バグを踏んでから」書かれている。それでも再発する。**
2026-08-22、洋館の横線を**5回外した**。犯人を特定する道具(`?hidelayer=`)は
**v0.25.2785 から在り、しかも当時の設計チャットが同じ失敗を2回してから作った物**だった。
私はそれを**一度も探さずに5回推測した**。同じ日に「絵が薄く残る」の教訓(v0.25.3458)も
**書いてあるのに再発**させた。**足りないのは知識ではなく、手を動かす前に索引を引く手順。**

### ★手順(推測に入る前の5手・順番を守る)

| # | やること | 具体 |
|---|---|---|
| **0** | **索引を引く** | 下の症状表を見る。当たりが無ければ `grep -n "<症状の語>" ENGINEERING_NOTES.md DEVELOPMENT_LOG.md CLAUDE.md` |
| **1** | **道具を探す**(自作する前に) | `grep -rn "tsNum(\|tsBool(\|hidelayer\|HIDE_LAYERS\|CORRIDOR_DEBUG" src/` — **切り分け用のURLツマミは大抵もう在る** |
| **2** | **社長に「3問」を先に聞く** | 下の3問表。**今回の突破口は毎回コードではなく社長の観察だった** |
| **3** | **2回外したら推測をやめる** | CLAUDE.md 7-3。3回目に入らず**消去法(`?hidelayer=all` → 1枚ずつ戻す)**へ切り替える |
| **4** | **直したら索引に1行足す** | 下の症状表に追記。**節を書き足すだけでは次の人は見つけられない**(この索引が生まれた理由) |

**★原因はリポジトリでも手順でもなく「コンテナのディスクが凍結している」ことだった(2026-08-26・究明済み)。**
`2026-08-21 15:39:27 UTC` を最後にこのディスクへの書き込みが完全に止まり、以後の全セッションが
同じ凍結イメージの使い捨てコピーとして起動している(ブート痕跡が 08-22〜25 だけ欠落・reflog も同区間が空)。
**教訓: 「毎回同じ所へ戻る」は乱数ではない=復元元が1つしかない証拠。**症状が完全に決定論的なら、
自分のコード/手順ではなく**土台(環境)を疑って、環境側のログ(`/var/log`・`/tmp/*diag*`)を数える**。
詳細と物証は CLAUDE.md「★★原因が確定した(2026-08-26)」。

**★対策そのものが症状に巻き込まれていないか疑う(2026-08-23)。** 巻き戻り対策の hook を入れたのに
**5回連続で巻き戻った**。原因は「**巻き戻りが hook 自身を消していた**」という循環だった
(リポジトリ内の hook は追加前のコミットへ戻るので消える。リポジトリ外の `~/.claude/` も同じ復元で消える)。
**「対策を入れたのに直らない」時は、対策が症状の射程内に置かれていないかを最初に見る。**

**★自作の切り分けスイッチで消去法をやってはいけない(2026-08-22 の最大の失敗)。**
私は `?nodark/nomesh/noback/nopillar/nobg` を4つ自作して「全部消しても消えない」と報告したが、
**そのスイッチ群は自分が疑っている範囲(`corridorLayer`)しか覆っていなかった**。
つまり**推測の延長を消去法だと誤認した**——最初から答えの入っていない檻を自分で作って探していた。
**消去法は「網羅的に作られた既存の道具」でやる。** 自作するのは、既存を全部試して外した後だけ。

### ★社長に先に聞く3問(症状 → 層の特定)

| 問 | 答えが意味すること |
|---|---|
| **①いつ出る/いつ出ない?** | 「ズームアウトで必ず」=**ズーム依存量**(フェザー/オーバースキャン/スケール)を持つ層 |
| **②画面のどこに出る? 動くと一緒に動く?** | 「どこに居ても同じ高さ」=**画面固定 or 遠景基準**。「世界の物に付いてくる」=**ワールド座標** |
| **③形は一定? 太さ・濃さが変わる?** | 「幅が変わる隙間」=**2枚の接合が動いている**(1枚の描画ミスではない) |

洋館の横線は、この3つの答え(ズームアウト・画面固定の高さ・幅が変わる隙間)だけで
**「遠景と地面の継ぎ目を埋めている層」に一意に落ちる**。実際、社長が先にそう言い当てた。

### ★症状 → 最初の1手 → 過去の実例

| 症状 | 最初の1手 | 過去の実例 | 節 |
|---|---|---|---|
| 線・隙間・切れ目が出る/絵が分断される | **`?hidelayer=all` → 1枚ずつ戻す** | v0.25.3833(gapband が遠景の無い洋館で描かれていた) | 「線・隙間・切れ目」節 |
| 絵が薄く残る/焼き付く | **`resetActorFxDefaults` に既定OFFが在るか** | 3207 / 3324 / 3383 / 3458 / 3838 | 「絵が薄く残る」節 |
| ズームで物が切れる/消える/湧かない | **`?zoomlock=0.4` で固定して実機** | 1241 / 1253 / 1259 / 1324 / 1325(**5層・3層直しても残る**) | §1-2 |
| エフェクトが本体に付いてくる | **`fb.footX` / `fb.footY` / `cx` / `cy` の4語で走査** | 3521 / 3522 / 3523(鍵に `bossStateUntil` を使った) | §3 |
| 上へ動かしたら透明/小さい/追えない | **CLAUDE.md「Y方向の5つの副作用」表** | 2615 / 2616 / 2617(**同じ週に3つとも踏んだ**) | CLAUDE.md |
| 素材を作ったのに絵が出ない/見えない | **発火条件→素材ロード→可視性→表示時間の順に実在確認** | 2411 / 2412 / 2417 / 2426(4回) | CLAUDE.md・実在確認の掟 |
| 起動直後に真っ暗(build/typecheck/testは緑) | **循環importを疑う** | 3390 | 「循環import」節 |
| 練習ラン/ガントレットでボスが出ない・枠が同じ | **`practiceGuard` が localStorage 書き込みを黙って飲む** | 3630 | §2 練習ラン |
| 時計がズレる/技が止まらない | **`gameTime` と実時計の混在** | 3621 | 「時計の混在」節 |
| ベンチの数字が暴れる/結論が出ない | **観測数 `n` を見る(100未満は結論にしない)+ `shift`** | 2690(n=2〜3の壊れた計測器) | 「計測器を疑う」節 |
| CIが赤い(自分の変更と関係が見えない) | **既存flakyテスト台帳を先に見る** | 3793 | 「flakyテスト一覧」節 |
| 自分の変更が消えている/巻き戻っている | **まず `git rev-parse --short HEAD` を見る。`0621fc40` なら巻き戻り確定**(戻り先は定数)→ `git fetch` + `git reset --hard origin/<branch>`。hookは効かない=hook自身が巻き戻って消える | 2026-08-22〜26(**1日で5回**の日あり・戻り先は毎回同じ `0621fc40`=v0.25.3774) | CLAUDE.md ★毎ターンの3点セット/★★原因確定(2026-08-26) |
| ヘッドレスが起動画面で止まる | **`TEST_HANDOFF/HEADLESS.md`(ポップアップは click では消えない)** | 2026-08-22(5回失敗) | TEST_HANDOFF/HEADLESS.md |
| 「前は直っていたのに再発」 | **diffを取る。空なら巻き戻りではなく潜伏バグの露見** | 1324 / 1325 | §1-1 |
| 設計書の記述と実装が食い違う | **設計書のステータス行を信じない。コードをgrep + DEVLOG** | 3757 | 本書 冒頭節 |

## ★設計書の「ステータス行」は腐る——現在地はコード+DEVELOPMENT_LOG先頭で確認(v0.25.3757・社長指摘)

**実バグ化した情報錯綜**: SKILL_BUILD_REDESIGN.mdの冒頭ステータスが「B0実装中・B1発注文」
(2026-08-13頃)のまま放置され、実際は本体が実装済み+多数改修済みだった。設計チャットがこれを
鵜呑みにして「固定2枠→これから6枠化」「特殊装備の撤去はこれから」と**2度連続で古い情報を社長に
報告した**(社長「最近情報が必ず錯綜する。メモが壊れてるか、メモする文化がなくなってるな」)。
- **掟**: 「今どこまで実装済みか」を答える時は、設計書のステータス行を根拠にしない。
  ①該当機能のキーシンボルをコードでgrep(実在配線の確認=実在確認の掟と同じ) ②DEVELOPMENT_LOGを
  そのシンボルで検索して最後の着地エントリを見る。設計書は「設計時の姿」の資料として扱う。
- **書く側の掟(v0.25.3763改訂)**: 状態は**PROJECT_STATUS.mdへ**書く(設計書には状態を書かない=
  v0.25.3762で全設計書から状態記述を撤去済み)。実装バッチは完了報告+DEVLOGエントリの
  「状態変化: …」1行で申告し、STATUSの書き手は設計チャットのみ。

## ★チャットに貼られた画像・素材は「ファイルとして」使える(v0.25.3500・社長指摘)

**社長がチャットへ貼った画像は `/root/.claude/uploads/<セッションID>/` に実ファイルとして落ちている。**
`cp` でそのまま `public/` へ入れられる。**「チャットの画像はバイナリとして書き出せません」は誤り。**

```bash
# 直近に貼られた画像を探す(セッションIDはcwdのハッシュ=環境変数やパスから分かる)
ls -t /root/.claude/uploads/*/ | head
find / -xdev -iname "*.png" -newermt "-6 hours" 2>/dev/null | grep uploads
```

- **この誤解はこのチャットで何度も繰り返された**(社長「このやりとりはこのチャットで何度も繰り返されています」)。
  素材を渡された時は**まず uploads を見る**。社長へ「コミットしてください」と返す前に、必ずここを確認する。
- 併せて、**素材の実寸は必ず measure してから配線する**。例: スキルシートは 5472×136 で
  **1マスが144×136=正方形ではない**。正方形前提で切ると横に潰れる(v0.25.3500で実際に踏みかけた)。
  ```bash
  python3 -c "import struct;d=open('FILE.png','rb').read();print(struct.unpack('>II',d[16:24]))"
  ```


## ボス戦カメラの調整ノブ(v0.25.2969時点・ローカル調整の引き継ぎ)

**社長判断(2026-08-07)**: push→実機のループは重いので**ローカルのdev(HMR)で調整する**。
実機は縦持ち(390×844級)。以下が全ノブ。編集→保存で即反映(vite)。

| ノブ | 場所 | 現在値 | 意味 |
|---|---|---|---|
| `BOSS_FRAME_BAND_H_FRAC` | `src/utils/cameraZoom.ts` | 0.45 | 縦フレーミングの基準=「見える帯」の画面高比。上げる=縦で引きにくく/下げる=引きやすく |
| `BOSS_FRAME_EDGE_MARGIN_PX` | 同上 | 40 | 帯端の内側マージン(画面px) |
| 横の引き床 | 同上 `bossFramingZoom` 内 `Math.max(BOSS_ZOOM_MID, ...)` | 1/1.7 | 横のためにこれ以上引かない |
| 横要求の/2 | 同上 | /2 | 寄せ(下記)が半分負担する前提の減額 |
| `BOSS_DISTANCE_ZOOM_NEAR/MID/FAR_PX` | 同上 | 180/500/1200 | 距離アンカー(gap基準) |
| `BOSS_ZOOM_PROFILES.far` | 同上 | 0.48/0.44/0.40 | 体格別の最深(=床)。**0.40より深くしない**(ドット密度+ZOOM_MIN_ABS監査) |
| 寄せの強さ | `src/pixi/pixiScene.ts` `bossBiasDx * 0.5` / `bossBiasDy * 0.25` | 横0.5/縦0.25 | プレイヤー→ボス中心差の何割をカメラが寄るか。上限=オーバースキャン予算の余り(コード内で自動クランプ・触らない) |
| 寄せの時定数 | 同上 `biasTau` | 入0.5s/戻1.0s | |
| `BOSS_ENGAGE_ENTER/EXIT_PX` | `src/utils/bossEngagement.ts` | 900/1400 | 交戦バブル(画面px基準・farズームでworld換算) |
| `BOSS_LEASH_PX` | 同上 | 1500 | 離脱リーシュ |
| `BOSS_DISENGAGE_GRACE_MS` | 同上 | 1200 | 画面外がこの時間続いたら帰巣開始 |
| `?bosszoom=0.65` 等 | URL | - | 距離プロファイルを固定値で上書き(0.3〜1)。`?zoomlock=` は全ズーム固定 |

**未解決の構造問題(社長スクショ2026-08-07夕・「延々と逃げてもズーム引きのまま」の疑い)**:
離脱判定の `onScreen` は**ズーム後の可視矩形**で測るため、深く引くほど「画面内」が広がり
ボスが離脱扱いにならない→追跡が切れない→引きも戻らない、という**自己維持ループ**になり得る
(useGameLoop の disengage 判定+ bossEngagement の EXIT が両方ズーム連動)。ローカル調整で
引きが戻らない症状が残る場合、ここを**等倍基準の固定距離**(ズーム非連動)へ変える案を検討
(=「逃げたら切れる」を引きの深さと独立させる)。判定変更なので実装前に社長へ一言。


設計チャット(Fable)の調査手法とプロジェクト固有の知見を実装チャットへ継承するための文書。
**バグ調査・原因不明の挙動・描画/音声/集計系を触る前に該当節を読むこと。**
新しい教訓は本書か`src/utils/constitution.test.ts`のどちらかに追記してから完了とする(CLAUDE.md規律6)。

- **gameTime停止シネマティック内で動かす崩壊は実時間、終了後に動かす同行者離脱はgameTimeで分ける**
  (v0.25.2832): attentionはゲームループを早期returnしてgameTimeを止めるが、Pixi描画は続く。
  ボスcorpseは`Date.now()`で崩壊させ、守護霊帰還はgameTimeで止めておくと、同じ停止中に順序を追加管理せず
  「ボス崩壊→同行者帰還」になる。反対に死亡SEのような開始時処理はattention早期returnより前で拾わないと
  演出終了後まで遅延する。

- **固定キャラクターの装備スナップショットも、プレイヤーのロードアウト生成規則を通す**(v0.25.2831):
  手書き配列で完成形を直接持つと、職業固定枠の欠落・他職業枠の混入・上限超過が起きる。固定データには
  選択可能な1枠だけを持たせ、職業枠はプレイヤーと共有する `classSubWeaponFor` から生成する。

- **「弾薬非消費」と「マガジン無し」を同一視しない**(v0.25.2830): 守護霊のリザーブ弾を
  運用系の除外にしても、マガジン容量・1発ごとの装填済み弾消費・リロード時間は戦闘リズムであり、
  プレイヤー同仕様の対象。資源の例外は在庫境界だけに閉じ、Weapon[]と装填/リロード純関数は主語間で共有する。

---

## ★サブエージェントと同じ作業ツリーに居る時、`git add -A` は使わない(v0.25.2749の事故)

**何が起きたか**: 実装サブエージェントが `src/pixi/` を編集している最中に、設計チャット側が
**文書だけのつもりで `git add -A` して commit+push**。結果、**実装途中の `pixiScene.ts` /
`lighting.ts` が巻き込まれ、typecheck 43エラーの状態がブランチに乗った**。

**なぜ気づけなかったか(2つ重なった)**
1. `git add -A` は**作業ツリー全部**を拾う。サブエージェントは**同じツリーで働いている**ので、
   「自分は文書しか触っていない」は**staging の中身を保証しない**。
2. **`npm run typecheck 2>&1 | tail -2 && git add -A && git push` は短絡しない。**
   パイプの終了コードは `tail` のもの(=常に0)なので、**typecheck が落ちても push まで走り抜ける**。
   (同型の事故は v0.25.2690 台でも起きている。)

**掟**
- **サブエージェントを走らせている間は `git add -A` 禁止。触ったファイルを明示的に add する。**
  例: `git add DEVELOPMENT_LOG.md package.json src/data/changelog.ts`
- **`git status --short` を add の前に必ず見る。** 自分が触っていないファイルが出たら、そこで止まる。
- **typecheck/lint の結果でゲートしたい時は `| tail` を挟まない。**
  挟むなら `${PIPESTATUS[0]}` を明示的に見るか、`npm run typecheck > /tmp/tc.log 2>&1 || exit 1` の形にする。
- **バージョン番号の取り合いを避ける**: 実装サブエージェントが push する運用の間は、
  設計側は**自分の push の直前に `git pull --rebase` してから番号を決める**。

## 1. 診断の型(5ステップ・順番を守る)

1. **「いつから」を確定する。** 体感報告を受けたら、まず憶測せずに:
   - 直近のコミットを見る: `git log --oneline <前回正常だった版のコミット>..HEAD`
   - 疑い機能の履歴: `git log -S "キーワード" --oneline` / `git log --grep="日本語キーワード"`
   - **DEVELOPMENT_LOG.mdが正史**(コミットが squash されている時期がある)。
   - 「前は直っていた」と言われたら本当にコードが変わったかをdiffで確認:
     `git diff <正常版> <再発版> -- 該当ファイル`。**diffが空なら巻き戻りではなく
     「条件が変わって潜伏バグが露見した」**(ズーム事件・v0.25.1324/1325の教訓)。
2. **「同じ症状・別レイヤー」を列挙する。** 1つ直して安心しない。同じ見た目の症状を出せる層を
   全部書き出してから対象を絞る。実例(ズーム引きで物が切れる、の5層):
   遠景オーバースキャン(1241)/エフェクトカリング(1253)/世界マスク(1259)/シミュ側回収矩形(1324)/
   フィルタ枠(1325)。**3層直して見えなくなっても残り2層は潜伏していた。**
3. **コメントよりコードを信じる。** コメントと実装値の齟齬は頻出(ジャスト窓±100↔±50等)。
   齟齬を見つけたら修正ついでにコメントも直す。値の出所を確認せず「良かれ」で変えない(CLAUDE.md最重要則)。
4. **修正は安全側+復帰フラグ。** 「消えなくなるだけ」「絞る方向だけ」の修正を選び、
   `?flag=0`で従来挙動へ完全復帰できる形にする(?ladder/?events/?mix/?debt/?setpiece/?beat/?zoomlock が前例)。
5. **直したら教訓を機械化。** 再発しうる不変条件は`constitution.test.ts`へ、
   知識型の教訓は本書へ追記。**チャットにしか書かれていない発見は存在しないのと同じ。**

## 2. プロジェクト固有の地雷(実バグ化したものだけを載せる)

### 練習ラン(ボスモード/ガントレット)
- **★practiceGuard は練習中の localStorage 書き込みを「黙って」全部飲む(v0.25.3630・実バグ)**。
  `beginPracticeRun` の後に localStorage 経由で状態を渡そうとすると、書き込みは失敗もエラーも
  出さずに消える。実際に、練習出撃の `setSelectedStageId(slot.stageId)` が飲まれて
  **全枠が「選択中のステージ」のまま出撃**していた(城ボスの固有技/大技・ボスHP・カットイン名・
  背景・storyBoss湧き=グレン/EXが全て getSelectedStageId 依存 → ガントレットでは新規プロファイル=
  ''のため「どの枠も同じ素のボス」に見え、グレン/EX枠はボスが一生湧かなかった)。
  → **練習中に効かせたい状態は localStorage を経由させない**。実行時の practiceActiveSlot() を
  正とする(getSelected* が練習中は枠を返す・data/progress.ts の★コメント参照)。
  同型の兆候: 「練習/ガントレットでだけ設定が効かない」「メニューからだと直る」。
  機械化: practiceGuard.test.ts「★練習ランの出撃先は関所に飲まれない」。

### 描画(PixiJS v8)
- **「効くはずの変更が無反応」なら、まずその関数が呼ばれているかを疑う(v0.25.2324・実バグ)**。
  レイアウト(リサイズ)時にだけ走る関数へ新しい描画を足すと、**その時点では `currentFarKey` 等の
  ステージ種別が未確定**で早期returnし、以後一度も呼ばれない。洞窟の境目対策は最初これで丸2回
  「無反応」になり、原因を別の場所に探しに行って大きく遠回りした。**毎フレーム走る経路
  (`updatePerspectiveGround` 等)へ移して初めて効いた**。
  → 描画の実験では、結論を出す前に必ず **「画像差分が出ているか」** を確認する。差分ゼロなら
  それは「シロ」ではなく「未実行」。
- **描画の犯人捜しは、推測でノブを増やす前に `window.__pixiScene`(DEVで公開)からシーングラフを
  列挙する(v0.25.2328)**。「問題のY座標に上端 or 下端を持つノード」を機械的に洗えば一発。
  洞窟の黒い横線は263ノード中10件に絞れ、犯人は**最前面 `uiLayer` の `labOutDim`**(可動域外を
  暗くする幕)だった。**背景側をいくら調べても当たらない類のバグがある**。
- **候補を消して確かめる時は `visible=false` ではなく親から切り離す(`parent.removeChild`)(v0.25.2328)**。
  毎フレームの更新に上書きされて「効かない=シロ」と誤判定する。実際これで一度シロ判定して遠回りした。
- **背景の継ぎ目は「輝度差」だけで合否を決めない(v0.25.2327・実バグ)**。洞窟の境目対策は
  ヘッドレスで輝度差 11.85→0.25(98%減)まで追い込んだが、**実機では線が残り「悪化」で撤去**になった。
  人の目が線として拾うのは輝度差だけではなく、**「ぼけた遠景の絵」と「粒立った地面の絵」という
  質感の断絶**も同じくらい効く。数値が良くなっても目で見えていれば直っていない。
  → 前後のスクショを必ず**目視で比較**し、可能なら**実機で確認してから既定値に焼き込む**。
- **明るさの議論は必ず「描画後の画素」で行う(v0.25.2325・実バグ)**。素材の生の輝度と画面の輝度は
  別物。洞窟の岩帯は**素材14.6 → 画面24.9**(照明/ブルームで持ち上がる)で、素材値を根拠に
  「岩は暗いから明るい側ではない」と判断して置き場所を誤った。
- **背景の継ぎ目(横一直線の線)は、まず"2枚の絵の明るさ差"を疑う(v0.25.2324)**。洞窟の
  「岩と地面の境目の黒い横線」は、フィルタでもレイヤー順でも位置でもなく、**遠景の絵(輝度27.8)と
  地面の絵(15.9)が1.75倍違うまま境界で突き合わさっていた**だけだった。被写界深度・ブラー帯・
  岩帯の位置・地面の位置・素材アルファを全部潰してから判明した。対策は境界より上を一律の黒で
  沈めて明るさを揃える(`HORIZON_DARKEN_ALPHA`・`?m0dark=`)。
- **ヘッドレスで背景を測る時は、霧の流れで輝度が±3ぶれる**(v0.25.2324)。差が3未満の比較は
  判定不能として扱うこと。
- **エフェクト色は`rgba(r,g,b,a)`形式で渡す(hexは白化する)**: `pixiScene.glowTint()`は
  `rgba(...)`しかパースせず、**hex(`#rrggbb`)は0xffffff(白)フォールバック**。particle/glow/ring等の
  `color`にhexを渡すと意図の色にならない(v0.25.2029実バグ=血飛沫粒子が白い綿毛になった)。既存の
  hex渡し箇所(spawnBurst死亡飛沫等)も実は白寄りで描かれている=「直したい」場合は見た目が変わるので社長裁定。
- **`Container.filterArea`はローカル座標**として解釈され`worldTransform`で画面へ写像される
  (`FilterSystem._calculateFilterArea`)。ズームで`worldGroup.scale`が変わると静的な枠は
  中央に縮み、**フィルタ枠の外は描画ごと消える**(=敵が切れる)+tilt-shiftの帯もずれる。
  → 対処は`syncWorldFilterArea()`(毎フレーム逆変換)。同種の「画面固定のつもり」を作る時は要注意。
- **マスクもズームに巻き込まれる**: worldGroupの子のマスクはズームで縮む。
  `ZOOM_OVERSCAN(=1/CONTEXT_ZOOM_MIN)`倍で敷く(worldFadeMaskの前例)。
- **ズーム対応はレイヤーごとに漏れて潜伏する**。新しい描画レイヤー/マスク/フィルタ/
  画面境界判定(カリング・回収・湧き)を足したら`?zoomlock=1`(常時最大引き)で1周確認。
- **フィルタのuniform座標は「論理px」ではなく「フィルタ入力テクスチャpx≈CSS px」**(v0.25.1773実バグ=
  SE2でチルトシフトのピント面がプレイヤーの約100px下へずれた)。シェーダは `vTextureCoord×uInputSize`
  と直接比較するため、論理pxで渡すと**ビューポートスケール≠1の端末でだけ**ずれる。常用機(CSS幅390-430)は
  係数≈0.96-1.06で無症状=検証をすり抜ける。縦が短い端末(SE2+Safariバー=375×553→係数0.77)で露出。
  → 座標/距離系uniformは渡す直前に `×stage.scale(ビューポート係数)` でCSS pxへ換算する。
  **端末差の検証は仕組み化済み(v0.25.1775)**: ①数値の不変条件=`src/utils/deviceCoverage.test.ts`
  (常用機13種がスナップ帯内・制限3種が帯外のまま、をCIで毎push機械検査。機種リストの正=
  `deviceCoverage.ts`)②見た目=`node scripts/device-sweep.mjs`(devサーバを起動して実行→
  主要13機種の全画面+プレイヤー周辺を一括スクショ)。画面系を触ったらこの2つを通すこと。
- **時刻スクロールの`tilePosition`にエポックms由来の値を渡さない**(v0.25.1807実バグ=川の筋が
  「描けない・動かない」)。`(Date.now()/1000)*速度`は1e10px級になり、GPUのfloat32ではこの桁の
  ULPが数千px=UVの小数部が全損して模様が消える/フレーム間の1px未満の移動も精度以下で静止する。
  JS(double)上の計算値は正しいためコード読みでは異常が見えない——**シーングラフの実値を覗いて発覚**
  (tilePosition.x=-321億)。→ 霧(`fogT0`)と同じ「開始基準の相対時刻」+テクスチャ周期modulo を使う。
  診断ハンドル: devビルドは `window.__pixiScene` でシーン実値を覗ける(v0.25.1807で常設・prod非搭載)。
- **「画面真っ暗・HUDは生きてる・裏でゲームは進む」=WebGLコンテキストロスト**(v0.25.2160・実機頻発)。
  iOSはメモリ圧でWebGLコンテキストを落とす。DOM(HUD)とrAFシミュは無傷なのでこの症状になる。
  対処は実装済み: PixiStageがcanvasの`webglcontextlost`を検知→0.7s後に親(Game)がkey変更で
  再マウント=フルリビルド(restore待ちはしない: iOSはrestoredが来ないことが多く、RenderTexture/
  ベイク済みプールの復元も信頼できない。boot経路は毎回踏まれていて確実)。シミュ状態は全てstoreに
  あるため続きから描ける。強制再現: `gl.getExtension('WEBGL_lose_context').loseContext()`。
- **勝手リロードはアプリ起因ではない**(v0.25.2160調査): SW無し・location.reload無し・単一バンドル
  (dynamic import無し)を確認済み=チャンク切れ/SW更新の線は消えている。残る説明はiOS Safariの
  メモリ圧kill+自動リロード。更新直後に集中するのはキャッシュ冷え(HTTP+デコード済み画像+
  SFXの`?v=版数`バストが毎更新で全音声を再DL/再デコード)で起動ピークが最大化するため。
  リロード自体を減らす本筋は起動ピーク削減。第一弾=SFXバストの内容ハッシュ化はv0.25.2161で実施済み
  (vite.config.tsが`public/audio/sfx`をビルド時走査→`__SFX_HASHES__`注入→withVersionが参照。
  差し替えたSEだけURLが変わる=毎更新の全音声再DL/再デコードが消えた)。
  **第二弾=ステージ別テクスチャの出撃ステージ限定ロード(v0.25.2166・社長承認)**: 従来は起動時
  `preloadBackgrounds`+出撃時の両方で30枚(デコード後実測~138MB)を全ロード常駐。→ 起動先読みは
  コア4枚のみ・出撃時は `STAGE_TEXTURE_GROUPS`(PixiStage)でそのステージの分だけ。**地雷**:
  近景帯(遠景森2)はミッション個別キー`nearHorizon`でグループを跨ぐ(M7=stage7遠景+'forest'森
  シルエット)ため、farBackdropだけで引かず`NEAR_HORIZON_TEXTURES[s.nearHorizon]`を必ず合算する。
  未知キーは全ロードにフォールバック(安全側)。グループの綴りはSortieTexturePath型でtypecheck検出。
- **音声はジェスチャ保険で拾い直す**(v0.25.2160): iOSの`AudioContext.resume()`はユーザージェスチャ中
  しか効かない場面がある+BGMのplay()拒否は再試行窓(ready系イベント2.2s)を逃すと止まりっぱなし。
  → `attachAudioGestureRecovery`(App起動時に1回)が全pointerdown/keydown(capture・1sスロットル)で
  resume+「鳴っているべきBGMがpausedならapplyBgm」を実行。音欠落系の症状はまずこれで自己回復する。
- **`bezierCurveTo`経路の`fill()`が描画されない**(v0.25.2154実測=フレアの炎マーク)。`getBounds()`は
  正しい矩形を返すのに画素が1つも出ない(スクショの画素スキャンで機械確認)ため、コード読み・バウンズ確認
  では異常が見えない。→ 曲線シェイプは直線`poly([...]).fill()`で近似する(実績あり。ドット絵の画風にも合う)。
- **ポーズ中(`isPaused`)はPixiのtickerが完全停止する**(PixiStage `applyTickerState`・電池対策)。
  キャンバスは直前フレームで凍結=「ポーズで敵/ボットを止めてスクショ検証」は**そのポーズ以降の描画変更が
  一切写らず無効**。ヘッドレス検証は非ポーズのまま、状態を毎フレーム矯正するスクリプト+複数ショットで行う。
- 重い描画の順位: `Text`生成 > 毎フレームGraphics > 大きいαスプライト > 加算ライト ≫ 敵/弾。
  対処は数を減らすことではなく**描画方式を軽くする**(pooled sprite/bitmap font/焼き込み)。CLAUDE.md参照。

### ボス制御(裏ボス/ゲートボス)
- **ミゲル(ゲート2ボス)は専用コントローラで動く**(`useGameLoop.ts`の`enemies.find(e=>e.type==='miguel')`ブロック)。
  汎用の裏ボスコントローラ(`bossRef`/`bs`を使う`else if (boss)`ブロック)を**通らない**。よって「裏ボス全体に効く
  副作用」でボスコントローラが後始末する前提のもの——例: 反射弾ワープが立てる`reaperWarpAlpha`を0→1へフェード復帰
  させる処理(3009付近)——は、**ミゲルでは復帰が走らず値が固定される**(v0.25.1619実バグ「反射弾を当てると絵が消える」=
  `reaperWarpAlpha=0`固定で`container.alpha`が0のまま)。`isHiddenBoss('miguel')===true`なので広域チェックに
  巻き込まれる点に注意。→ 裏ボス広域の副作用を足す時は必ず ①ミゲル専用コントローラが後始末を持つか ②ミゲルを対象外にするか を確認する。

### スプライト素材の変換(社長提供シート→public/sprites)
- **透過PNGに黒キー(color-key)をかけない**(v0.25.1429実バグ)。透過素材はキャラの**輪郭線が
  不透明の黒**(alpha≥200, max(r,g,b)≤22)であることが多く、黒キー/フラッドフィルは縁の透過背景から
  連結してこの枠線まで食う=「シルエットの枠線が消える」。**まず四隅のalphaを見る**:透過(alpha=0)なら
  アルファをそのまま透明源に使い、キー処理は不要。黒キーは「不透明黒背景」の素材専用。
- **プレイヤー立ち絵の焼き規約**: 128×108、図の**右端(銃口)を x=106・足元を y=107**、フレーム間は
  **共通スケール**(最大figH→105px)。既存player-*はこの規約(全部 xmax=106/footY=107)。破ると
  char-select(object-containで50pxに収め足元基準)で色味/大きさ/潰れがズレて見える。
- **素材の `?v=` は「ファイル内容ハッシュ」(v0.25.2277〜)。同名差し替えでも手動バンプは不要**。
  vite.config.ts が `git ls-files -s -- public` の blob SHA-1 から表を作って `__ASSET_HASHES__` に注入し、
  `src/config/assetUrl.ts` の `assetUrl()` / `withAssetVersion()` が引く。**差し替えてコミットすれば
  そのファイルのURLだけが変わる**(他は端末キャッシュが効いたまま=1枚のために164MB再DLしない)。
  - 未コミットの差し替えも拾う(vite.config が working tree の変更分だけ実内容をsha1し直す)。
  - 表に無いファイル(未追跡・git不在ビルド)は `ASSET_VERSION` へフォールバック。
  - `ASSET_VERSION`(config/assetVersion.ts)は**全素材を強制再DLさせたい時の非常ボタン**に降格。
    通常は触らない(上げると全端末で164MB再DLになる)。
  - 旧方式の教訓(残置): 同名差し替えでバンプを忘れると旧絵が配信され続けた(chen v0.25.1424・
    magnum v0.25.1431)。ハッシュ化でこの地雷そのものが消えた。
  **適用範囲に注意(v0.25.1807採録・現在も有効)**: `?v=`が付くのは spriteLoader(sprites/*)・audioManager・
  立ち絵/チュートリアル挿絵など `assetUrl()` を通る経路だけ。**背景類(pixiTexturesの`BACKGROUND_PATHS`=
  遠景/床/地平帯/川筋)はクエリ無しのURL直読み**=表の管轄外(GH PagesのETag+max-age=600で最大10分後に
  自動更新される)。

### 「クリティカルは出ないはず」が破れる経路(v0.25.2295採録・実バグ)
クリティカルは**近接の武器クリ率+`player.critChance`だけではない**。銃にも**対象の型による弱点クリ**があり、
「クリ率0だから出ない」は成り立たない。M0の封印(教わるまでクリを出さない)を入れた時、ここを見落として
**射撃教習でプレイヤーの銃がクリしていた**(社長報告)。
- **銃の弱点クリ**: `WEAKCRIT_ENABLED && weaknessCritBonus(enemy.type, 'gun')`(§5.6 M7)。
  **チャフ(バット/ゾンビ)相手は銃+10%**。射撃教習の相手がゾンビだったのでそのまま踏んでいた。
- 他にも `projectile.crit`(発射時に立つ)/`trapCritBonus`(root中)/`headshot`(PHILL頭部命中)がある。
  **クリを止めたい時は `hitCrit` の合成結果をゲートする**(個別の率を0にして回るのでは漏れる)。
- 併せて: **ダメージ0の弾でもクリ判定・ダメージ数字・ノックバックが走っていた**。演出目的の
  0ダメージ弾を足す時は、この3つを `dmg > 0` で落とすこと(「0」が頭上に並ぶ/敵が押される)。

### 音声(Web/iOS Safari)
- **`HTMLAudio.currentTime`は同期の基準に使えない**: 精度が粗く「デコード位置」であって
  スピーカー出力位置ではない(出力遅延0.1〜0.3s+Bluetooth追加分は見えない)。
  壁時計と音声時計は別の発振器でドリフトする。MP3は先頭に約26〜50msのエンコーダ遅延。
  → リズム同期はB方式(拍の真実をゲーム側に置き、ビート音をSFX用WebAudioの`start(t)`予約で
  鳴らす)。長尺曲との完全同期は約束しない。
- **iOS Safariの自動再生制限**: 操作なしのHTMLAudio play/差し替えはブロックされる。
  動く構成=「単一BGM要素+src差し替え+STARTタップ時の一括解錠」(v0.25.280レシピ)。
  **SFX用WebAudioコンテキストは一度解錠すれば操作なしで鳴らせる**(実証済み・こちらを優先)。
- **低電力モードは計測を汚染する**(30fps固定)。性能判定の前に必ず低電力OFFを確認。
- **【重要・v0.25.1563で確定】この端末(iOS Safari)は「長尺の音声ストリームは同時に1本まで」。2本目の
  同時再生ストリームは、それが**2本目のHTMLAudioElement**でも**Web Audioバッファの連続再生
  (AudioBufferSourceNode)**でも、**描画を引っかからせる**(ダンス~20fps=v0.25.258/274/278 / 紅き夜の
  最初から引っかかる=peak-layer.mp3)。**短いワンショットSFXだけは同時でも無料**(B方式のビート音で実証)。
  - **peak-layerで"2本目HTMLAudio=重い"がクリーンに確定**(`?peaklayer=0`で紅き夜が軽くなる・低電力OFF)。
    これは HANDOFF_DANCE_AUDIO.md §3 の未決(「2要素方式は低電力OFFなら軽い"かも"」)への**答え=重い**。
  - **結論(音を"BGMに重ねたい"時の鉄則)**: 2本目のストリームを開くな。
    ①**短いワンショットSFXをビートグリッドで鳴らす(B方式)** か、②**単一BGMストリームに事前ミックス**する。
    **Web Audioバッファ連続再生への移行は"軽くする対策"にならない**(同じ壁に当たる)——安易に提案しないこと。
  - **正しい"BGM切替"は差し替え(swap=片方をpauseして1本にする)**。深層域の逆再生BGM(`enterDeepReverseBgm`=
    通常BGMをpauseして逆再生版をplay)/ダンス(`setDanceMode`=1要素のsrc差し替え)が実例。**深層域は色+逆再生BGM
    まとめて軽いと実機実証済み(v0.25.1565 `?deepzone=1`完全再現)**。src差し替えを無音にしないための下ごしらえが
    `unlockDanceAudio`(START時に各曲をmute/vol0で一瞬play→pauseして自動再生ロック解除。**これは"重ねる"技術ではなく
    "swapを鳴らせる解錠"**)。
  - **【社長決定 v0.25.1566】PEAK時はBGMを変えない**=`peak-layer`(PEAK/紅き夜の打楽器重ね)を**既定OFF**にした
    (`PEAK_LAYER_DISABLED`既定true・`?peaklayer=1`で試聴のみ可)。紅き夜の「最初から重い」の根治。
- **【音声の同時ストリーム監査 v0.25.1566】現存する音声再生の棚卸し(社長「他にもここ起因を探して」):**
  - **`bgm`**(HTMLAudio・loop): 唯一の常用ストリーム。OK。
  - **`deepBgm`**(深層逆再生・HTMLAudio・loop): **swap**(通常BGMをpause)=同時1本=OK(実証済)。
  - **`peakLayerEl`**(peak-layer・HTMLAudio): **overlay=2本目**=犯人。→ 既定OFF化で解決(上記)。
  - **`heartbeat`**(心音・`setHeartbeatLoop`): **連続ループのWeb Audioバッファ**(`source.loop=true`)=ダンスループと
    **同クラスの地雷候補**。瀕死(ピンチ)中に鳴る。ピンチの残り重さに寄与する疑いあり=**要実機切り分け**。
    診断トグル **`?heartbeat=0`** を追加(v0.25.1566)。※ピンチはビネットのDOM移設(v0.25.1560)で改善済だが、心音は別系統。
  - **`hurricane`**(鞭ハリケーン鳴動・`setHurricaneRumble`): 連続1ループではなく**~2.4秒の短クリップを予約で連発**
    =ワンショット寄り。HANDOFFでも「体感問題なし」。**低リスク**(ハリケーンで重さを感じたらここを疑う)。
  - **SFX/dance-kick**: 短いワンショット=無料。OK。
  - → **結論: 残る要注意は`heartbeat`(連続バッファ)のみ**。`?heartbeat=0`で実機確認 → 重ければ「短いドクンをワンショットで
    刻む(B方式化)」or「素材を短く」で回避(連続ループを避ける)。

### ビルド・型チェック
- **【重要・v0.25.2529発覚】素の`npx tsc --noEmit`はこのリポジトリでは何も検査しない。**
  ルート`tsconfig.json`は`files: []`+`references`のソリューション形式なので、プロジェクト指定なしの
  tscは0ファイルを対象に「エラー0」を返す(=ゲートが空振りする)。**型チェックは必ず
  `npm run typecheck`(= `tsc -p tsconfig.app.json --noEmit`)を使う。**
  過去の「`tsc --noEmit`がTS2304を見逃す」事例も、実はこの空振りだった可能性が高い。
  ビルドはesbuildで型チェックしないため、typecheckが唯一の型ゲート。新規import追加時は目視も併用。

### Tailwind(メニューUI)
- **色透過`/N`は既定スケール(おおむね5刻み)外だと静かに未生成=完全透明になる**
  (v0.25.2146実バグ: `bg-black/92`が透明で、ガチャsuperの暗幕が消え背後のメニューが丸見え)。
  中間値は`/[.92]`のarbitrary記法にするか5刻みへ寄せる。疑ったらライブDOMで
  `getComputedStyle(el).backgroundColor`を実測(未生成なら rgba(0,0,0,0))。
- **transform/filterを持つ祖先の中では`position:fixed`が画面基準にならない**(menu-stagger等)。
  フルスクリーンモーダルは必ず`createPortal(…, document.body)`でbody直下へ
  (v0.25.2146実バグ: 資料室モーダルがスクロール中ページ上部に張り付く)。

### 集計・状態(Zustand/シングルトン)
- **シングルトン集計の「開始時点」保存は必ずディープコピー**(snapshot関数)。
  生参照は差分が常に0になる(killTelemetryStateエイリアシング・v0.25.1343の実バグ)。
- **フェーズ跨ぎの保留状態は遷移でクリアする**(配役pendingCastが緩へ漏れた前例)。
  refに「あとでやる」を持たせたら、そのrefのライフサイクル(いつ消えるか)を必ず書く。
- **「キルが少ない=苦戦」ではない**。出現していない可能性を必ず考慮(出現数とセットで判定)。
- gameTimeは一時停止・スロー・ヒットストップで実時間と伸縮する。実時間(ctx.currentTime/
  performance.now)と混ぜる時は毎フレーム変換し直し、長い先読み予約をしない。
- **`useRef(f())` の引数は毎レンダー評価される**(refが保持するのは初回の戻り値だけで、**式は毎回走る**)。
  よって**ファクトリ関数に副作用を持たせてはいけない**。v0.25.2625の実バグ: `createIdolTickState()` に
  `clearIdolPlayback()`(モジュール変数のリセット)を仕込んだ結果、**パネルが再描画するたびにボスメーカーの
  再生要求が消え、▶を押しても技が1フレームで固まった**。症状が「たまに効く」に見えるのは再描画のタイミング次第。
  → **リセットはリセット地点で明示的に呼ぶ**。ファクトリは純粋に。同型の危険: `useState(f())`・`useMemo` の外の式。
- ★**「配列を丸ごと差し替える更新」と「別の場所からの追記」が同居すると、追記側は必ず消える。**
  v0.25.2629の実バグ: `gameStore.updateEnemies` が当たり判定キュー `pumpkinBlasts` を**空のローカル配列から
  作り直して state を差し替え**る一方、ボス専用コントローラ(useGameLoop のミーミル/トール、
  angelBossTick のラフィ、idolTick のアイドル)は**その前**に `state.pumpkinBlasts` へ追記していた。
  ⇒ **同じフレームのうちに捨てられ、それらの技は一度も当たらなかった**(半径を直しても当たらないのは当然)。
  → **差し替える側は必ず既存 state を引き継いでから積む**(`push(...state.xxx)`)。
  同型の危険: `shieldBlocks` / `skadiIceMarkers` など **updateEnemies が返す他の配列も同じ形**。
  外から追記する場所を作る時は、**差し替え側が引き継いでいるかを必ず確認する**。
  検知: 「コントローラが積んだ判定が updateEnemies を跨いで生き残る」を固定した回帰テストが `sim.test.ts` にある。
- **モジュール変数の「要求箱」で React→ループへ指示を渡す時**は、①誰が消すかを1箇所に決める
  ②消す関数を副作用として他所へ埋め込まない ③「要求が生きている間はループを回す」ゲートを
  **要求箱と同じモジュール**に置く(別モジュールに写すと二重管理になる)。

### スポーン/難易度(この順で効く)
- **経路の合流点**: 全スポーンは`gameStore.addEnemy`を通る(出現計測recordSpawnはここ)。
  通常湧き抽選は`selectEnemyType`(blocked=関所の許可制/mix=チャフ配合/featuredFloor=床)。
  **イベント(囲い/救助/波)は`spawnEnemyAt`直呼び=ゾーン規約の対象外**(意図的な特例)。
- **床(featuredFloor)は問題児の裏口になり得る**。許可リスト(講習/無双/回収)以外に付けない
  (憲法テストが機械検査)。回収系は主役の上限とセットで。
- 値の場所: 配合=`chaffMix.ts`+各シーン定義 / 許可・天井=`gatePressure.ts` /
  台本=`difficultyDirector.ts`(PHASES)+`gateProgram.ts` / 演目=`reliefProgram.ts` /
  イベントゲート=`eventProducer.ts` / 在庫=`boardDebt.ts` / 退屈=`boredomDirector.ts`。

### 2.x 用語+ゲート発火の地雷(v0.25.1848採録・v0.25.1851用語追加・社長決定)
- **用語**: 「ゲート」=境界の囲いゲート1/2(§5.21 M20軸2・5000px/7500px境界の地理トリガー)。
  **城ボス(giantbat)をゲートと呼ばない**(城フィナーレは別システム)。混同すると診断も報告も迷子になる。
- **「NPC8人」(社長決定v0.25.1858)** = 出撃している護衛8人(エドガー/ジョセフ/エリザベス/武蔵/
  ムハンマド/チェン/ローレン/フェイザー・soldierIndex 0..7)。「通常NPC」という言い方はもう使わない。
  グレッグ/ジュン(チュートリアル随行)・商人・二人組・救助NPCは含まない。
- **テキスト表示の用語(社長決定v0.25.1851)**:
  - **通信** = モデル無しのシステム告知。帯バナー(eventBanner=紅き月警告・「変異体が興奮し始めた」・
    「駆除成功!」等)と、ミッション開始の通信ボックス(IntroDialogue・▶通信タグ)がこれ。
  - **会話** = モデル表示ありきのテキスト(NpcDialogue=左上のモデル付き吹き出し。グレッグ/ジュン/護衛/
    グレン/ミラ等)。**会話は必ずモデル入り(大きい場合は半身)**=新話者はNPC_PORTRAIT登録必須。
- **地雷**: ゲート発火を`puzzleActiveNow`でゲートしてはいけない。puzzleActiveNowはフェーズ表が
  boss扱いの時間帯(7:00-7:30)にfalseになるため、その30秒に境界を跨ぐと**ゲートが発火せず素通り**
  できた(実バグ・実測再現→修正=フェーズ条件を外した`gateFireOk`を使用)。ゲートは地理トリガー=
  コマ/フェーズと無関係に発火させること。イベント排他は発火条件側(activeEvent空き待ち)が担う。

## 3. 触る前に見る場所(逆引き)

| 変えたいこと | 触る場所 | 壊しやすいもの(確認先) |
|---|---|---|
| 敵の出現比率 | シーン/台本/演目のmix | 憲法テスト(mix全指定) |
| 問題児の出現条件 | gatePressure(許可/配役)・ゾーン天井 | 憲法第4条テスト・初心者ゾーン |
| 関所の中身 | gateProgram(台本) | featured自己整合テスト |
| 緩の中身 | reliefProgram(演目) | 床の許可リスト・主役上限 |
| イベントのタイミング | eventProducer+useGameLoopの各ゲート | 憲法第5条(山に重ねない・pity猶予) |
| 画面境界の判定 | ?zoomlock=1で確認 | ズーム5層の教訓 |
| リザルト診断 | aiDirectorDebug(DirectorSample) | 0.5sサンプリング・記録のみ厳守 |
| 「エフェクトが本体に付いてくる」 | pixiScene の該当FX描画呼び出しの**座標引数** | ★検索語は `cx/cy` **だけでは足りない**(下記) |

### ★「エフェクトが本体に付いてくる」を探す時の検索語(v0.25.3521→3522・実バグ・2回連続で取り逃した)
- **敵/ボスの描画は本体中心 `cx/cy` ではなく足元 `fb.footX` / `fb.footY` を使う。**
  v0.25.3520 の横断走査は `cx/cy` だけを見ていたため、**斬撃の弧と突進の風圧という主犯を丸ごと
  取り逃し**、社長に「切り離されてないよ。ついてくるよ」と2度目の指摘を受けた。
  走査する時は `fb\.footX|fb\.footY|cx|cy` の**4語**でFX描画呼び出しを洗う。
- 仕分けの物差しは**「その絵は空中に残る物か、本体の状態を示す標識か」**:
  - **残る物(=焼く)**: 斬撃の弧・残像・風圧・破片・血・煙。振りの頭で座標を焼き、以後動かさない。
  - **標識(=live のまま)**: 予告リング・スロウリング・ボスマーカー。本体に付いて回るのが正しい。
  - **武器そのものは標識側**(手に持っている物)。社長の「武器やボスの動きと切り離して」の対象は
    **エフェクト**であって、武器スプライト自体ではない。
- ★**`bossStateUntil` を「識別子」に使ってはいけない(v0.25.3523・実バグ)**。これは**値であって
  同一性ではない**——**ノックバック中の時計の一時停止(v0.25.3497)が毎フレーム delta ぶん足す**ので、
  押されている間じゅう値が変わる。「1回だけ出す」(`trigger*Once` の dedup キー)や
  「立ち上がりで座標を焼く」(錨の key)にこれを使うと、**押されている間だけ毎フレーム発火・
  毎フレーム焼き直し**になり、**エフェクトがボスに付いて行く**。しかも押されている時=
  まさにボスが動く時なので、症状が最大化する。**鍵は時計に依存しないもの**を使う
  (`moveInstanceNo(id, bossState)`=`bossState` が変わるたび +1 の通し番号)。
  その観測は**if分岐の外で毎フレーム無条件**に行う(分岐の中だけだと、同じ技が間に別の技を挟まず
  2回続いた時に「同じ回」と誤認する)。
  **v3497(挙動の修正)の副作用が、3週間後に「絵が付いてくる」という一見無関係な形で出た**——
  時計をずらす変更をしたら、**その時計を読んでいる描画側の"鍵"を必ず洗う**。
- 焼き方の作法は1つに揃っている: `latchFx`(立ち上がりで data を焼く)か、
  `Map<id, {key, x, y}>` の錨(`katanaArcAnchor` / `bountyWhipSmearAnchor` / `acrasielBurstOrigin`)。
  **錨を足したら mark-and-sweep(個体除去)にも delete を足す。**

## 4. 完了前チェック(CLAUDE.mdの規律の実行形)

```
npm run lint && npm run typecheck && npm test && npm run build
```
+ 変更ファイルをReadで再確認 / 負荷スコア / コアループ宣言(①②③のどの輪) /
憲法4条・5条の自己点検 / 教訓の機械化(テスト or 本書) / version bump / DEVELOPMENT_LOG。

## デバッグボット系ツール(M9〜M12)が使い物にならなくなる場面(v0.25.1471採録)
最悪の壊れ方は「動かなくなる」ではなく「**動き続けて古い仕様で合格を出す**」(静かな偽陰性)。
1. **検査中の不変条件そのものを仕様変更した時**(最頻)。CD3秒・コマ順序・上限等を変えたら、
   **同コミットで§5.10の検査リストとテストも更新**(憲法テストと同じ規律)。怠ると誤報か見逃し。
2. **ボットが知らない操作の追加**(新アクション・設置物・盾等)→ その周辺のバグは網にかからない。
   新操作を足したらボットのポリシーにも足す(「playtest緑=安全」の過信に注意)。
3. **ボットの腕で扱えない新メカニクス**(精密照準・タイミングパリィ・資源管理)→ バランス走査が
   「弱い」と誤判定(偽陰性)。この種のメカ追加後の走査結果は割り引いて読む。
4. **ダメージ式・クリ式の変更の取り込み忘れ**(弱点クリ等)→ プロト武器(M12)の計算だけ旧式で乖離。
5. **全体リバランス・スキル大量追加後は過去レポートと比較不能**(基準線=その時点の中央値)。
   走査は生きるが「前回よりz値が上がった」等の経年比較はしない。
6. **tick構造の変更**(固定16.6ms廃止・処理分割)→ M11-aの基準値リセット。実機FPS(M11-b)は
   機種・OS更新をまたぐ絶対比較不可=同一機での前後比較のみ。
7. **前提の違う場**(屋内・新ステージ・特殊モード)はボットの前提(屋外)から外れる。対応させるまで
   その場のバグは網の外。
運用原則: これらのツールは仕様のコピーを内蔵している。**仕様を変えたらツールも同コミットで更新**。
更新しないままの緑は信用しない。

### ヘッドレス実走の実測知見(v0.25.2249・リモートLinux/GPUなしコンテナで採録)
「7. 前提の違う場」の具体例と、環境側の地雷。次にヘッドレスを回す人はここを先に読むこと。
- **GPUなし環境ではPixiJSが実速×0.031しか出ない**(swiftshader=ソフトウェアGL)。ゲーム内8分ぶんに
  約4時間かかり、長時間実走には使えない。実測: pixi 1280×800=×0.031 / pixi 360×640=×0.155 /
  **canvas 1280×800=×0.949 / canvas 360×640=×1.000**。
  → **ロジック検証は `?renderer=canvas` で回す**(描画層だけの差し替えで `useGameLoop`/store は同一)。
  PixiJSは「起動して描画されるか」のスモークだけに使う。**描画品質/FPSはこの環境では測れない**。
  なお `gameTime` はフレーム数に比例(dtクランプ)するので、**待ちは実時間ではなく `gameTime` を見て切る**。
- **M2(ラボ)ではボットが機能しない**(実測: 開始40秒 x=-1189 で停止 → 残り7分間 移動0/キル0/HP変化0)。
  当たり判定を静的に確認したところ**壁にめり込んでおらず左右上下に移動可能**=ゲーム側の詰まりではない。
  原因は**ボットに「ゴールへ向かう」行動が無く、M2の敵は湧いた直後は全員休眠(dormant)で追う相手も
  いない**ため。→ **M2のヘッドレスは湧き/イベント/ピックアップ等のシステム検証には使えるが、
  プレイ可能性・バランスの検証には使えない**。
- **「湧き量」を測る時は上限に当たっていないか必ず疑う**。M2で湧き間隔の差(2.4 vs 1.6)を測ろうとして
  そのまま数えたら 2体 vs 1体で差が出なかった。原因は区画上限(`LAB_ENEMIES_PER_ZONE`=2)/画面上限で
  **棄却される領域を測っていた**こと。**湧いた敵を即座に取り除いて上限に当たらない状態**にしたら
  26体 vs 39体=**×1.50** と理論値どおりに出た。間隔の検証は必ずこの形にする。
- **`__gameStore` は DEV ビルド限定**(`gameStore.ts` 末尾)。`npm run build` + `vite preview` では生えない。
  storeを直読みしたいなら `npm run dev`(既存の `botrun-local.mjs` は本番ビルドなので読めない)。
- **イベントが「出なかった」ことを検証する時は、その窓が本当に開いていたかを別途確かめる**。
  叫喚型ゼロの確認では、`screamerPhaseGateOk`+`phaseAt` を純関数として直接評価し、ラン中に
  ゲートが計50秒開いていたことを示した。これが無いと「元から出ない条件だった」と区別できない。

### ヘッドレス「手本動画」収録の地雷(v0.25.2285採録・19本を30fps mp4で撮り直した実作業から)
チュートリアルの手本(`public/tutorial/*.mp4`)をヘッドレスで撮る時はここを先に読む。**この環境の実速は
原速の約0.075倍・実フレーム間隔503ms**なので、素直に撮ると全部失敗する。
- **30fps収録は素のままでは物理的に不可能**。`useGameLoop.ts` の `Math.min(0.05, rawDelta)` により、
  実フレーム間隔503msは毎フレーム50msへクランプされる=**ゲーム内時間の粒度が20fpsどまり**。
  対策=**rAFのtimestampを33.33ms刻みの合成時計に差し替える**(クランプに掛からなくなり「描画1コマ=
  ゲーム内1/30秒」になる)。`Date.now`/`performance.now` も同じ時計へ載せるとDate.now基準の演出
  (ヒットストップ/スロー/リング)も歩調が合う。
  **注意**: rAFは useGameLoop・PixiJS ticker・React・Playwright のポーリングが同時に回っている。
  「呼ばれた回数」で進めると1実フレームで何度も進んで自滅する。**本物のtimestampが変わった時だけ**
  1コマ進め、同一実フレーム内の全コールバックには同じ値を配ること。
- **収録の重さは「描き直し」ではなくPNGエンコード**(430x932実測)。`captureFrame()`(全画面PNG dataURL)
  =**661ms** / crop→PNG dataURL=**488ms** に対し crop→**JPEG dataURL=2ms**。
  さらに `scene.sync()+renderer.render()` はCPU2msでもGPU描画を1回増やし実フレーム間隔を0.5→1.1秒へ倍化する。
  **最速形=ページ側で自走するrAFループ+明示renderなし(tickerが描いた直後のcanvasを読む)+JPEG**
  =1.17→**0.49秒/コマ**。`page.screenshot()` も既定PNGは1〜4秒、`{type:'jpeg',quality:92}` で0.5秒。
- **DOM収録(page.screenshot)を「1枚撮って1フレーム待つ」で回すと早送りになる**。撮影中に実フレームが
  何枚も流れるため。実証: demo-area-wall が「4秒」で3975px(通常の約9倍)進んだ。対策=**合成時計を
  ゲート化**し、Node側が1コマぶん許可した時だけ進める(許可しない間は dt=0 でゲーム完全停止)。
- **ViteのHMRが収録を殺す**。別セッションがリポジトリを触ると full-reload が飛び
  "Execution context was destroyed" で収録が飛ぶ(実際に38コマ目で被弾)。**public/ にmp4を置くだけでも
  リロードが飛ぶ**。対策=`addInitScript` で `vite-hmr` プロトコルのWebSocketを繋がせない。
- **収録中にプレイヤーが死ぬと canvas ごと消える**(リザルト画面へ遷移)→ `querySelector('canvas')` が
  null になりページ側が例外、Node側は無限待ち。長い待ちを挟むなら**ページ側 setInterval でHPを張る**
  (Node側の毎ポーリング補充では待ち時間中に守れない)+ Node側に「N秒コマが増えなければ打ち切り」の番犬。
- **Vite dev はTSモジュールをURLで配るので、ゲームの敵ファクトリを直接呼べる**:
  `await import('/zombie/src/utils/enemyUtils.ts')` → `spawnEnemyAt(type,x,y,t)`。既存敵のクローン注入より
  正確(型ごとのHP/サイズ/スプライトが本物)。stage-1序盤の敵は bat なのでクローン方式だと絵が寂しくなる。
- 個別の落とし穴: **`HUNTER_START_MS=180000` はヘッドレスでは実40分=事実上到達不能**(`gameTime` を
  190000へ進める) / **拠点解放の10秒滞在は `escorts[].dwellMs`**(`baseSites[].dwellMs` ではない) /
  **`triggerCounter()` はクリティカルで気絶させ2発目がフィニッシャー(即死)**になるので素振りを見せたい時は
  毎ポーリングで `stunUntil` を消す。至近の斬撃FXは画面を白く覆うので**素振りは1回**まで /
  **レベルアップ選択メニューが開いている間は商人の滞在判定が止まる**(`updateMerchantDwell` が
  `showUpgradeMenu` で return)ので**レベルアップは必ず最後に撮る** / `action-*` の切り出しは400px幅=
  プレイヤー中心±200px。敵を+250pxに置くと枠外。
- **成果(参考値)**: 19本を 30fps・コマ数3〜13倍・容量**平均1/10**で撮り直せた(例: demo-base-capture
  3.8fps/1360KB → 30fps/**77KB**)。1本あたり起動73〜85秒+収録0.49秒/コマなので、**1起動で複数本まとめ撮り**が必須。

## M9実装(v0.25.148x)の実際のスコープ(この網に掛からないもの)
`src/utils/playtestDriver.ts`/`playtestInvariants.ts`/`src/store/playtest.test.ts`が実装した
ヘッドレスボットは、M9-A(`src/utils/directorTick.ts`)で切り出したコマ管理/査定/decideNextSpawn
消費/画面外リサイクル+上限カリング/AIディレクター信号**だけ**を回す。以下は現状ボットの網の外
(useGameLoop.ts側に残る~500行超のレガシー経路にあり、今回のヘッドレス化の対象外だったため):
- ハンター・叫喚型ディレクター・レスキュー・紅き月・囲い(ホード/ミニボス)イベント
- 関所ライブ補正(gatePressure)・退屈上振れ(boredomDirector/upswing)・難易度③escalation(spawnEsc)
- 屋内(研究所/ラボ)・雪原ステージ・ボス戦(puzzleActiveNow=false のレガシー査定経路)
- サブウェポン/スキルのアクティブ発動(M10で追加予定の「CDが空くたび使用」ポリシー)
- **プレイヤーの被ダメ経路(接触ダメージ/敵の発砲/敵弾ヒット/地雷/ジャンプ落下攻撃の爆風)**——
  **解消済み(v0.25.1504・M17: combatTick化)**。`src/utils/combatTick.ts`(新規・レンダラ非依存)へ
  切り出し、`useGameLoop.ts`と`playtestDriver.ts`の両方から同じ順序(⑤ジャンプ落下爆風→②敵発砲→
  ③敵弾命中→④地雷→①敵接触)で呼ぶようになった(挙動は移動のみ・不変)。演出(playSfx/spawn*/
  triggerHitImpact/死亡演出)はコールバック注入でヘッドレスはno-op、判定条件自体は実機と完全同一。
  ボットは**構造的にダメージを受けられる**ようになり、`playtest.test.ts`のレポートに`hpLost`/
  `died`(死亡時刻・死因)が追加された。回帰カナリア(棒立ちボットを深部相当の盤面で120秒回すと
  被ダメ>0になる)を`playtest.test.ts`に追加済み。
  **なお対象外(ここに含まれない=引き続き網の外)**: ボス攻撃(ミーミルレーザー/トール各技)・
  ハンター・雪原/ラボ経路。これらは今回もuseGameLoop.ts側のレガシー経路のまま。
- **「深層ラッシュ」経路が未試験・ボットが有能すぎて包囲されない(v0.25.1510で確定した穴)**——
  **rusherペルソナで一部解消(v0.25.1513・M19)**。`playtestBot.ts`に`rusher`ペルソナ(原点から
  外向きへ最大速度で直進・カウンター/カイト/武器切替なし・自動射撃のみ・詰まった時だけ横へ回避)
  を追加し、`playtest.test.ts`の深層ラッシュ・シナリオで**実歩行**によりr≥7500(深層域)へ到達
  できることを確認(実測: 約87秒で到達。社長実機動画の約79秒に近い再現)。
  **ただし残る限界**: (1) M17カナリアで使う「depth teleport-probe」(深部座標へ瞬間配置して被ダメ
  経路だけを見る)の人工性はそのまま残る(rusherは実歩行の経路試験であり、teleport-probeを置き
  換えるものではない・両方併用)。(2) rusherは木/壁の詰まりにだけ対応する簡易回避で、囲まれた時の
  生存判断(逃げる/戦う)は一切しない=「有能すぎる」問題は他ペルソナ(standard/kiter/boar)側では
  未解消のまま。(3) 芯アサーションは「r≥7500へ到達できる」ことだけで、HP/死亡は記録のみ(バランス
  値は今後の守護者ゲート/深さ床次第で変わる想定・固定しない)。
上記に関わるバグはこのボットでは検出できない。壊れた時の網としては上のM9-M12注意点と合わせて読むこと。
また `Date.now()` ゲート系(ノックバック免疫・カウンターCD・ヒットストップ・スロー等)は
`vi.useFakeTimers()`で仮想gameTimeと同期させている(`src/store/playtest.test.ts`)。これを外すと
54,000tickを数秒の実時間で回す都合で近接攻撃CD等が実質機能しなくなり、ランクが全く上がらない
静かな偽陰性になる(実装中に実際に踏んだ教訓)。

## プレイヤー立ち絵の描画・取込み規約(最終確定v0.25.1769・実機検証済み)
**確定レシピ = ①÷Nドット保持焼き(実寸78px幅・nearest) ②頭中心x合わせ ③表示側ピクセルスナップ。**
v0.25.1763〜1768の実機反復で確定した診断ツリー(再発時はここから読む):
- **滲み/モヤ** = 「高解像度素材のままlinear+mipmapでリアルタイム縮小」が原因。縮小率が半端だと
  トリリニア補間が常に隣画素/mip間をブレンドする=**レンダリング解像度を上げても消えない**
  (res3でも大差なし・社長実機確定)。→ 旧「エリオット式=高解像度のまま縮小」案(v1763-1765)は撤回。
  本家HD-2Dが成立するのは描画キャンバスが端末ネイティブ解像度だから(1ドット=3〜6物理px)であり、
  res1運用のうちには前提が無い。
- **潰れ(1px/2px列のまだら)** = 「半端な拡大率×nearest」が原因。1ドット≈1.06〜1.12画面px
  (画面フィット×待機ズーム×遠近)の端数が列の太り/欠けになる。→ **ピクセルスナップ
  (`pixiScene.snapTexelScale`・?psnap=0で復帰)**で1ドット=キャンバス整数pxに丸めて根治
  (HOLD13%/RELEASE19%の線形ブレンド・本体/分身/救援アライ)。副作用=帯内でプレイヤーの見かけ
  サイズ固定(待機ズーム/遠近で変わらない・世界比で最大~13%大きめ)。
- **端末カバレッジの対応方針(v0.25.1774確定・アプリ化=フルスクリーン前提)**: 本作はブラウザ
  (Safariバー付き)でのプレイを基本想定しない。フルスクリーンのビューポート係数は
  iPhone SE2〜16ProMax=0.926〜1.086 / Android 384〜412dp級=0.95〜1.02 /
  **Android 360dp級(Galaxy S標準表示)=0.889(ずれ12.5%)** → HOLD=13%で全常用機カバー。
  既知の制限(帯外・対応しない): iPhone SE1(320dp=0.79)/タブレット(iPad=1.64・本気対応するなら
  「×2スナップ+素材」の別検討)/ブラウザバー付き起動(SE系=0.77・ホームアプリ化で回避)。
- **演出系の変形は知覚的にタダ(v0.25.1770-1771で確定・全ON採用)**: 全7種一括OFFの実機A/Bで
  「動作中の変形はドットの崩れとして気にならない」と社長確認。理由=テンポラルマスキング
  (毎フレーム別の場所に出る崩れは網膜残像で溶けて「動き」として知覚される)。**目に見える潰れを
  作るのは静止/低速(数秒スケール)の半端スケールだけ**(待機ズームの這いが典型)。→ 演出の
  スケール/回転は安心して使ってよい。避けるべきは「ゆっくり変わる連続スケール」を静止絵に掛けること。
  診断用一括OFF=`?pmotion=0`。

### 素材の支給仕様(社長=支給側)
- **形式**: AI生成の「**N倍整数拡大ドット絵**」シート(通常N=4)。シートのまま・コマごとの
  キャンバス不揃いでOK(ツールが吸収)。必須条件は4つ:
  ①透過背景 ②コマ間は完全透過の隙間で区切る ③足元=絵の下端が接地
  ④ポーズ間でキャラの大きさ(ドット密度)が一貫(ツールでは直せない唯一の項目)。
- **対象**: クラスごとに idle(歩きコマ流用可・要指示) / walk 5 / run 5〜6 / melee 2(しゃがみ構え,振り抜き)。
  ※game-0..2 はレンダラ未参照(休眠)のため不要。
- **取込み(実装側)**: `node scripts/import-player-sprites.mjs player-<絵名> walk=..png melee=..png run=..png --dot 4`
  → ÷4 nearest→コマ分割→頭中心x合わせ(median・横揺れ0px=v48規約)→足元下端・**幅78キャンバス**で書き出し。
  同クラス全ポーズを1回の実行でまとめて渡す。idle流用は取込み後に walk-N をコピー。
  **取込み前にグリッド整列を機械検証**(÷N→×N復元の一致率100%を確認)。※取込み後の ASSET_VERSION バンプは
  v0.25.2277で不要になった(`?v=`=内容ハッシュ。コミットすれば自動更新)。
  ツールの再現性はv0.25.1769で検証済み(ストライカー13枚ビット一致)。

### 新規素材の共通規約(ドット密度統一)
新規キャラ系素材はプレイヤーと同じ「N倍拡大ドット絵→÷N焼き」を標準とし、画面内の
“1ドットの見かけサイズ”をキャラ間で揃える(HD-2Dのピクセル比率統一の考え方)。
高密度イラスト系(軍人NPC/裏ボス等の既存)は従来どおり(実寸焼き or linear縮小)。

### 取込みの検収ルール(社長指示v0.25.1766「大きさとか規格が間違えてたらツッコミ入れて。勝手に進めない」)
素材取込みのたびに次を確認し、**1つでも違反・解釈が割れる場合は取り込まずに報告して指示を待つ**(勝手に
補正して進めない):
1. コマ数(歩き5 / 走り5〜6 / 近接2[しゃがみ構え,振り抜き] / idle1 ※idleが歩きコマ流用の場合は指示に従う)
2. 透過背景・コマ間の完全透過区切り(ツールがコマ検出で機械判定)
3. 足元=下端接地
4. ポーズ間・ファイル間のキャラ密度(大きさ)の一貫性(ツールの背丈差警告>12%は目視で正当性を判断し、
   ポーズ由来と断定できなければ報告)
5. 統一キャンバス化後の表示サイズが既存クラスと揃うか(÷N後のキャラ幅が78を超えたらツールが停止する)
6. ドットグリッドの整列(÷N→×N復元一致率が100%でなければ「N倍整数拡大ドット絵」ではない=停止して報告)
※クラスID↔ファイル名の入れ違い(rogue=ストライカー→player-scavenger-* / necromancer=スカベンジャー→
player-striker-*)は既存仕様。取込み先を間違えないこと(campaign.ts/pixiScene.ts L537コメント参照)。

## 地平線フェード帯は「画面高に対する割合」で考える(v0.25.2334・実バグ)
**症状**: 横持ちPC(1280×800)で廃病院が描画されない、と報告された。実際は `visible=true`・テクスチャ
ロード済で、**alpha 0.192 で描かれていた**だけ(=「出ていない」ではなく「見えない」)。

**原因**: 「地平線に近いアクターは薄れて消える」フェード(`pixiScene.horizonActorAlpha`)の帯幅が
**120px固定**だった。この120は**縦持ちの論理画面高(≒876)に対して調整された値**。横持ちでは論理画面高が
**338まで潰れる**ため、120pxの帯が画面のほぼ全体を覆う。結果、足元の建物が0.19、**プレイヤー自身ですら
0.783**まで薄くなっていた。このフェードは敵/木/城/商人/NPC/護衛/プロップが**全部共有**しているので、
影響は病院だけではなかった。

**教訓(逆引き用)**
1. **「描画されない」の報告は、まず alpha を疑う**。`visible`/テクスチャ/カリングより先に
   `sprite.alpha` を実測する。見えない ≠ 出ていない。
2. **画面サイズ由来の定数は px 固定で置かない**。「縦持ちで調整した px」は横持ちで意味が変わる。
   ただし**割合化だけでは足りない**(v0.25.2346で改訂): 初版は `min(120, 高さ×0.14)` としたが、
   「基準の向きでは上限に張り付く」が成立するのは**主力端末の高さ(876)でだけ**で、
   **iPhone SE(論理720)は100.9pxに変わってしまっていた**。割合化する時は
   **「上限に張り付く」を全端末で検算する**か、**そもそも向きで分岐して基準の向きは定数のまま**にする。
   採用したのは後者(`renderSpec.horizonActorFadePx(h, w)`: `h >= w` なら常に120)。
   → 一般則: **「副作用の無い一般化」に見えるものほど、範囲の端(最小/最大端末)で検算する。**
3. **横持ちPCはサポート対象**。`OrientationGuard` は「PC(非タッチ)は横向きで遊ぶため対象外」なので、
   横持ちで崩れるものは実バグ。**タッチ端末だけ縦持ち強制**という非対称を忘れないこと。
4. **同じ座標で縦横を並べて内部値を読む**のが最短の切り分け。今回は
   `__gameStore.setState`でワープ→`__pixiScene.horizonActorAlpha(footY)` と `hospitalSprite.alpha` を
   両方の viewport で読んで10分で確定した(スクショの目視だけでは「出ていない」に見えて誤診する)。

## 「上手さ」のダイヤルは、必ず**撃破数で検算**する(v0.25.2364・実バグ)
**症状**: ボットの腕前段階(`BOT_SKILL_PROFILES`)を足したのに、実測で **novice が master の5倍倒す**という
逆転が2回続けて起きた(v0.25.2356: novice 51 / master 10 → M49実装後も novice 77 / master 27)。

**原因**: **名前と説明は正しいのに、挙動が逆を向いているダイヤルが2つあった。**
| ダイヤル | 私が書いた説明 | 実際の挙動 |
|---|---|---|
| `retreatHpFrac`(master=0.5) | 「危険を察知してHP半分で退避」 | **上手いほど早く逃げる** |
| `surroundCount`(master=2) | 「小さいほど早く危険を察知する」 | **上手いほど早く逃げる**(敵2体で退避=盤面に5〜10体いるので常時逃走) |

1つ目(`retreatHpFrac`→`disengageHp`)は反転させたが、**同じ誤りが隣の行に残っていた**。
「危険察知が早い」という**もっともらしい言い換え**が、退避の増加を隠していた。

**教訓(逆引き用)**
1. **「上手い」を表す新しいダイヤルを足したら、必ず"成果"の実測で検算する。** このゲームなら**撃破数**。
   説明文がどれだけ正しく読めても、**段が上がって成果が下がるなら向きが逆**。
2. **退避・回避・距離維持のダイヤルは、単体では全部「弱くする」方向に効く。** 生存側のダイヤルを
   段階の上位へ寄せると、**攻撃側のダイヤルで相殺しない限り必ず成果が下がる**。
3. **単調性はテストに書く。** ただし**ユニットテストでは断定できない**(挙動の総合結果なので)。
   `src/store/rankProbe.test.ts`(`RANK_PROBE=1`・約50秒)のような**計測プローブ**を用意し、
   ダイヤルを触るたびに回すこと。ユニットは「表の向き」だけを固定できる(実際、旧向きを固定した
   テストが2件あって、反転時に赤くなって気づけた=**向きのテストは書く価値がある**)。

## 自動テストの地雷: デバッグ導線とポーズ(v0.25.2472採録・テストチャット報告v0.25.2468より)
1. **`?idolnow=1` は idol が2体になる**(配置済みの `dormant` 個体を消さずに近傍へ追加する)。
   実プレイに影響は無いが、**idolを対象にした自動テスト/検証では「どちらの個体か」をidで
   区別しないと誤判定する**(実際にテストチャットが「解散しない」と誤認しかけた)。
2. **stage-2 は開始直後にポーズが入る**(チュートリアル等)。ゲームループ全体が `!isPaused` ゲートの
   内側(`useGameLoop.ts`)なので、**ポーズ解除しないとシミュが1フレームも進まず「何も動かない」と
   誤判定する**。チュートリアルのポップアップは `showTutorialPopup` が `isPaused: true` を立てる。
   → 自動テストでは「ポップアップが出たらOKを押す(閉じる)」手順を必ず入れる。恒久策として
   `?autotut=1`(ポップアップを出さず既読処理のみ・ポーズしない)の実装を予定(社長合意v0.25.2472)。
3. ヘッドレスプローブの既知の罠4点(v0.25.2465の守護霊検証で全部踏んだ): ①召喚入口
   `runGhostAndTraitsStep` はボットラン(playtestDriver)に乗っていない=手で毎tick呼ぶ(接近フェーズ中も)
   ②手置きボスは `homeX/homeY` を持たせないとリーシュで待機化して歩き去る ③`runPlaytestTick` の
   dt は**秒**(1/60)——msを渡すとプレイヤーが吹っ飛ぶ ④ボスは至近でも `dormant` のまま=
   aggro半径(380)内へ「歩いて」入って起こす(実績プローブ=bossDistance.probe の型を流用するのが正)。

## 数値を他所から「流用」する時は、**体の大きさを必ず見る**(v0.25.2612・実バグ)

**事故**: 社長報告「ミーミルの牙がミーミルの中心に発動するので当たりっこないです」。

**構造**: ミーミルの「群体の噛みつき」は**本体の中心**に円AoEを出す(城ボスの踏み鳴らしと同じ作法)。
半径は `MIMIR_BITE_RADIUS = GRENADE_BLAST_RADIUS`(=92)を流用していた。ところが:

| ボス | 当たり判定 | 円の半径 | **体の外へ届く距離** |
|---|---|---|---|
| 城ボス(giantbat) | 60×60(半幅30) | 130 | **+100px** ✅ |
| **ミーミル** | **248×138(半幅124)** | 92 | **−32px** ❌ |

プレイヤーは当たり判定にぶつかるので**必ず124px以上外側にいる**。円は体の中に収まっているので、
**左右から挑む限り構造的に一度も当たらない技**だった。**同じ「足元の円」でも符号が逆**になっていた。

**教訓(値そのものより、流用の作法の問題)**:
- **他のボスの値を持ってくる時は、その値が「体の大きさ」に依存していないかを必ず確認する。**
  半径・射程・オフセットは、ほぼ全て体の大きさに依存する。このプロジェクトのボスは
  **60×30 から 519×90 まで約9倍の開きがある**(jormungand 519幅 / mimir 248幅 / skadi 304幅 /
  天使6体は 60×30)ので、流用は事故になりやすい。
- **「絵」ではなく「当たり判定」で考える。** ミーミルは絵が約451×575、当たり判定は248×138。
  判定の話をしている時に絵の寸法を見ると、逆方向に間違える。
- **リーチ(体の外へ何px届くか)で揃える。**「半径をいくつにするか」ではなく
  「**体の外へどれだけ届かせるか**」で決めると、ボスの大きさが違っても意図が揃う。

**機械化**: `src/utils/bodyCenteredAoe.ts` + `bodyCenteredAoe.test.ts`。
足元の円AoEは `BODY_CENTERED_AOES` に登録し、**体の外へ最低40px届く**ことをテストが検算する。
**新しく足元の円AoEを足したら、この表に登録すること**(登録漏れは守られない=そこだけは人が守る)。

**同型が起きうる場所(未検証・気づいたら足す)**: 予告図形の大きさ、近接の届く距離、
「本体の周り」に出すエフェクトの半径、周回半径。いずれも体の大きさに依存する。

## 計測器を疑う: ベンチの観測数が2〜3個しかなかった(v0.25.2690・実バグ)

**事故**: 光の改修の効果を測ろうとして、**同じ負荷・同じ端末で G12 が 35.0 / 35.8 / 37.0 / 39.6 / 58.8**
と暴れた。そこから「塗り面積が主因(約3割)」→「いや同時数が全て」→「重なりが主因」と
**結論を2回ひっくり返した**。犯人はゲーム側ではなく**計測器**だった。

**構造**: ベンチは `useGameLoop` の `fps` を読んでいたが、この値は **1秒に1回しか更新されない**
(`fpsCounterRef` が1000msごとにフレーム数を数えて `setFps`)。それを 500ms 間隔でサンプリングして
いたので:
- 1段=計測2.4秒 → **独立した観測は2〜3個**。しかも同じ値を2回ずつ拾っていた。
- その2〜3個から `avg` / `min` / `drops` を出していた。**min は「2個のうち小さい方」**。

**この解像度では、2つの計測が違うのか同じなのかを判定できない。** 有意差の議論以前の問題。

**教訓**:
- **数字が再現しない時、まず疑うのはゲームではなく計測器。** 「観測が何個あるか」を最初に確認する。
  n=3 なら、その平均は何も言っていない。
- **平均値だけを出す計測器を作らない。** 観測数(n)とばらつき(sd)を必ず一緒に出す。
  出ていれば「n=3」が一目で分かり、2回の結論の反転は起きなかった。
- **計測器は計測対象に触らない。** 旧ベンチは開始と同時に12回の `fetch` を撃っており、
  **一番冷えていて一番大事な最初の2段に被っていた**(通信計測は全段の後ろへ移動)。
- **熱ドリフトは「基準段(canary)」で測る。** 端末は測っているうちに遅くなるので、
  **系統の切れ目ごとに同じ固定負荷を測り直す**。各段の値は基準段との
  **フレーム時間の差(Δms)**で見る。fps は60で頭打ち+非線形なので引き算できないが、
  ms は加算的なので引ける。これが実行順・熱・ビルドをまたいで比較できる唯一の量。
- **基準段には「今調整中のもの」を入れない。** 強glowを基準段に入れると、
  「端末の速さ」と「調整の効果」が混ざってビルドをまたげなくなる。

**機械化**: `src/utils/benchmarkStats.ts` + `benchmarkStats.test.ts`(純関数=フレーム時刻列→統計)。
ベンチ本体(`BenchmarkOverlay.tsx`)は自前の rAF で**全フレームの時刻**を記録する(1段100〜200観測)。

**★重要**: **v0.25.2690 より前のベンチ数値は全部この壊れた計測器で取られている。**
CLAUDE.md の「Empirical render budget」節と `research/LIGHT_REWORK.md` の数値表がこれに当たる。
新しい計測器で取り直すまで、過去の数値は**順位の目安**として扱い、絶対値を根拠にしない。

---

## 循環importはbuild/typecheck/testを素通りして「起動直後に真っ暗」を起こす(v0.25.3390)

**事故**: B2b(v0.25.3384)で levelUpGate.ts が bountyTick.ts から予告寸法をimportした結果、
`gameStore → levelUpGate → bountyTick → gameStore` の循環が成立。bountyTick の**モジュール初期化時**の
`BB_LEAP_RADIUS = PUMPKIN_EXPLOSION_RADIUS`(gameStoreの未初期化const読み)が、本番バンドルの評価順で
**TDZ(`ReferenceError: Cannot access 'X' before initialization`)→起動直後に真っ暗**になった。

**なぜ検知網を全部すり抜けたか**:
- `tsc`は循環importを平気で通す(型の解決に評価順は関係ない)。
- vitest はファイル単位でモジュールを解決するため、テストの入口からだと評価順が変わり顕在化しない。
- `vite build` も成功する(バンドルは作れる。壊れるのは**実行時の評価順**)。
- つまり **「全部緑なのに起動しない」が循環importの典型症状**。起動確認だけが検知手段。

**診断の型**: ①黒画面+consoleに `Cannot access 'X' before initialization` → ほぼ循環import。
② `npx madge --circular --extensions ts,tsx src` で環を列挙(このリポジトリに既知の無害な環が1本ある:
`gameStore > ghostBuild > weaponUtils`。**増えていないか**を見る)。③今回の修正=寸法定数を依存ゼロの葉
`src/utils/bountyDims.ts` へ移動し、gameStore側からの到達枝(levelUpGate→bountyTick)を切った。

**掟**:
- **gameStore(または他の巨大ハブ)がimportするファイルに、gameStoreをimportするファイルをimportさせない。**
  新しいimportを足す時は「この枝でハブに戻る道ができないか」を考える。迷ったら madge を1回回す(8秒)。
- 複数モジュールが共有する定数は、ハブに置かず**依存ゼロの葉モジュール**に置く(bountyDims.tsが手本)。
- 「単一の出どころ」規約(写し定数禁止)と両立する: 葉に定義→全員がそこをimport(re-export可)。

## 時計の混在(v0.25.3621の実バグ・全エージェント共通の地雷)
- このプロジェクトには**時計が3本**ある: ①`gameTime`(シミュ時刻・ラン開始からのms・hitstopで凍る)
  ②`realGameTime`(実時間系のラン時刻) ③`Date.now()`(エポックms。`counterCooldownEnd`/
  `hitstopUntil`/`knockbackUntil`/ズーム包絡などstoreの「〜Until」系の多くがこれ)。
- **異なる時計の値を直接比較しない。** 実例: botのカウンター判定に gameTime と
  Date.now基準の counterCooldownEnd を渡していたため、最初の一振りで CD≈1.7e12 となり
  「CD中」が永久に真=**botのカウンターが実機・ヘッドレスとも一生出ない**(社長報告3回分の真因)。
  ユニットテストは両引数に小さい値を渡すため**この型のバグはテストを素通りする**。
- 直し方の型: 呼び出し側で「残り時間」を取ってから使う時計へ写す
  (`ccEnd - Date.now() + gameTime`)。新しい判定を書く時は引数コメントに**時計の契約**を書く。

## 「除外を1つ外す」は下流の別の消費者へ配り直す行為(v0.25.3785・実バグ)
- 実例: `combatTick.applyContactDamage` の `forEach` は、**1つの敵に対して上から順に**
  「トール専用の除外 → 空中族 → 突進/硬直/溜め → 気絶 → ★ボス接触受け流し → 接触ダメージ」を見る。
  v0.25.3784 で先頭の除外から `thor-dash-move` を1つ外した(接触ダメージをミゲルに揃えるため)結果、
  **同じ個体が下流の「ボス接触受け流し」(`isHiddenBoss` に thor を含む)にも流れ込んだ**。
  受け流しは `rootUntil=900ms` を立て、`useGameLoop` の `frozen` がそれを拾って `bossState='chase'` に
  するので、**同じバッチで直したはずの「突進のカウンター」が実機に一度も出なくなった**
  (Counter!演出・クリ反撃・counter-leap・弾き返し・専用CDが全滅)。
- **型**: 早期 `return` が並ぶ長い `forEach`/`if-else` チェーンで**分岐条件を緩める**時は、
  「この個体が**次に何に拾われるか**」を**下流の全分岐について**読み直す。
  外した除外は「何もしない」ではなく「**下流の誰かに配る**」。
- **競合順序の地雷(併記)**: ボスtickは**フレーム頭の座標**で重なりを見て、そのあと座標を進める。
  `applyContactDamage` は**進めた後の座標**で見る。⇒「突進が到達したフレーム」は tick 側では
  まだ重なっておらず contact 側でだけ重なる=**必ず contact 側が先に取る**。
  「両方に判定がある」時は**フレーム内の順序で勝敗が決まっている**ことを疑う。
  直し方は、順序に依存しない側(=どちらか一方に判定を持たせない)を選ぶ。

## ★既存のflakyテスト一覧(「これは自分が出した赤か?」で迷わないための台帳・v0.25.3793起票)
**`npx vitest run` を回した時に、自分の変更と無関係に赤くなりうるもの。** 新しい赤かどうかの
判断で毎回時間を溶かすので、**見つけた人がここへ1行足す**(直せるなら直す。直さないなら理由を書く)。
- **`src/utils/bountyTick.test.ts` > 舞妓 >「型Bの近距離は毬の薙ぎ・連(mk-naginata1→mk-naginata2)の
  2段になる」= 乱数flaky(実測 6本中2本で赤・約1/3)**。落ち方は
  `expected 'mk-backroll' to be 'mk-naginata1-windup'`——近距離の取り掛かりで
  **`MK_T.backRoll.chance = 0.3` のバックロール台本を先に抽選している**(`bountyTick.ts` の
  `dist <= MK_T.nearMax` 枝・v0.25.3584)ため、乱数次第で薙ぎ・連が選ばれない=**確率が実測と一致**。
  **本バッチ(トール突進)由来ではない既存のもの**。
  直すなら「テスト側で `Math.random` を固定する」か「近距離の期待値を『薙ぎ・連 or バックロール』へ
  緩める」のどちらか(=**抽選の重みは仕様なので触らない**)。
- ~~**`src/store/sim.test.ts` > 「MOVEMENT_REWORK.md 仕様1: 速度ボーナスはランプで立ち上がり…」**~~
  → **★解消済み(v0.25.3863)。この行はもう赤くならない。**
  **原因は実装ではなく「テストが古かった」こと**: `PLAYER_INERTIA_TAU` は**社長指示v0.25.3618
  「プレイヤーの移動に少し慣性入れて」で 0 → 0.06 になった**のに、テストは「完全即応」時代の前提で
  **1フレーム目の `vx` が基礎速度と一致する**ことを期待していた
  (実測 21.147 = 86.13 × alpha 0.2425 = ちょうど慣性1フレームぶん)。
  **直し方**: このテストの仕事は「ランプが `movePlayer` に結線されているか」(タイトルどおり)なので、
  **ランプの状態(`speedRampSustainMs`)で検証する**形へ書き直した。慣性の調整では二度と壊れない。
  慣性の配線自体も1本だけ見るが、**定数(`PLAYER_INERTIA_TAU`)と式(`inertiaAlpha`)を import**して
  写経しない。**★教訓: 数値を写経したテストは、値を変えた時に「一致している」と嘘をつく**
  (v0.25.3855 と同じ型。**赤くなるだけまだ良い方で、通ってしまう方が危ない**)。
  ※以下は解消前の記録(同じ型の赤を次に見た時の参考に残す)。
- **(旧記録)** 上記の赤は **★flakyではない。決定論的に必ず落ちるハードな赤**(v0.25.3798 で訂正)。
  起票時に「実行環境の速度に依存して赤くなることがある(既知)」と書いたのは**誤り**——実測は
  **5本連続(単独4本+フルスイート1本)でビット単位に同じ値**
  (`expected 21.147423904640505 to be greater than 86.13`)。
  落ちているのは**1フレーム目の `player.vx`**(`sim.test.ts:163`)で、**時間計測ではなく
  `movePlayer` の初速そのもの**が期待の約1/4(21.1 / 86.13)。
  **原因未調査・本バッチ(トール突進)由来ではない・担当未定**(誰かがボールを持つまで
  `npx vitest run` は常にこの1件だけ赤い)。
  → **台帳のルール(この節の冒頭)どおり、直す/直さないを決める人が現れたらこの行を更新する。**
  ※教訓: **「環境依存のflaky」と書いた台帳は、本物の回帰を後任に素通りさせる装置になる。**
  台帳へ載せる前に**同じ赤を2〜3回再現し、値が動くか(flaky)動かないか(ハードな赤)を必ず見る。**
- **`src/store/bountyGoldChest.test.ts` >「金箱の中身は赤経験値20+武器抽選3回+スクラップ10倍」
  = 乱数flaky(実測 22本中5本で赤・約1/4)**(v0.25.3808 起票・**トール案件由来ではない**)。
  落ち方は `expected 2 to be greater than 2`(`bountyGoldChest.test.ts:88` の
  `expect(st.player.weapons.length).toBeGreaterThan(weaponsBefore)`)。
  原因はテスト自身のコメントが予告しているとおり——**`grantWeapon` は所持済みキーを引くと本数が増えない**
  ので、武器抽選3回が**3回とも所持済みを引いた乱数の回**だけ「増えていない」で落ちる。
  **同じ赤の中でも `strapTotal` 側(300〜510)は毎回通っている**=箱の中身そのものは出ている。
  直すなら「テスト側で `Math.random` を固定する」か「本数ではなく**抽選が3回走ったこと**を見る」の
  どちらか(=**抽選の重み/所持済みの扱いは仕様なので触らない**)。**担当未定**。
  → **これで既知の赤は3件**(bountyTick=乱数flaky / sim=ハードな赤 / bountyGoldChest=乱数flaky)。
  「落ちるのは2件だけ」と書いてある古い報告は**不正確**(v0.25.3808 の実測で3件目が見つかった)。

## 「載っていない側」だけを固定したテストは、**載っている側が消えても落ちない**(v0.25.3793・実バグの再発防止)
- 実例: `counterReach.test.ts` のトールの節は `expect(...).not.toContain('issen-nihil')`(=紫は
  カウンター不可)を丁寧に固めていた一方、**`HIDDEN_COUNTER_ACTIVE_STATES` に `'thor-dash-move'` が
  載っている**ことは1本も固めていなかった。結果、**その1行を消してもテストは全部緑**——
  実機からは弾き返し・専用CD・走行カウンターが丸ごと消える。
- **型**: 「Xが**入っていない**」の検査は、「Yが**入っている**」の検査の代わりにならない。
  表・許可リスト・州リストに**要素を足して挙動を作った**なら、**足した側**の存在をテストに書く
  (消したら落ちるか=**実際に消して1回落として確認する**のが最短。落ちなければそのテストは効いていない)。
- **優先順位**: 回帰テストは「**実機から消えた挙動**」から書く。打刻の件数・フラグの落とし忘れといった
  周辺の完全性検査は、その後。2巡続けて実機から消えたものにテストが無いのに、軽い項目に長いソース走査
  テストを書いていたら順序を間違えている。

## ★描画の「線・隙間・切れ目」を追う時は、まず `?hidelayer=` を使う(2026-08-22・5回外した反省)

**推測でレイヤーを当てにいかない。現物を1枚ずつ消して特定する。** そのための道具が
`src/pixi/pixiScene.ts` に**既にある**(v0.25.2785 で、当時の設計チャットが同じ失敗を2回して作ったもの)。

```
?hidelayer=all
?hidelayer=far,hz,nh,ff,fog,ground,gapband,grade,vig,air,cloud,strips,dim,veil,mask
```
`far`=遠景パノラマ / `hz`=遠景森1(地平帯) / `nh`=遠景森2 / `ff`=手前森 / `fog`=霧全部 /
`ground`=地面ベース / **`gapband`=隙間埋め帯** / `grade`=全画面グレード / `vig`=周辺減光 /
`air`=寒色グレード / `cloud`=雲影 / `strips`=地面ストリップ / `dim`=帯外の暗幕 /
`veil`=研究所の暗幕 / `mask`=森のフェードマスク。**線が消えた時に指定していた層が犯人。**

**2026-08-22 の失敗**: 洋館の横線を追って**5回外した**(奥壁/遠方フェードの継ぎ目/labdim/黒背景の縦余白/
自作スイッチ4つ)。全部 `corridorLayer` の中を見ていたが、犯人は `pixiScene` の `gapband` だった。
**この道具を最初に使っていれば1往復で終わっていた。**

**症状から場所を絞る型**(社長の観察が毎回の決め手だった):
| 社長の言葉 | 何を意味するか |
|---|---|
| 「どこに居ても同じ高さ」 | プレイヤー位置に依存しない=**画面固定 or 遠景基準**のレイヤー |
| 「ズームアウトで必ず出る」 | **ズーム依存の量**(フェザー/オーバースキャン/スケール)を持つもの |
| 「線ではなく分断・隙間の幅が変わる」 | **2枚のレイヤーの接合**が動いている(1枚の描画ミスではない) |

**遠景の無いステージ(corridorMode / indoorMode / lab)では、遠景系レイヤーを全部止める。**
新しい遠景系レイヤーを足す時は、**この3つの除外を必ず付ける**(gapband はこれを忘れて事故った)。

## ★「絵が薄く残る/焼き付く」の定型(3回踏んだ・2026-08-22 に4回目)

**症状**: 技の後や画面のどこかに、ボスやエフェクトの絵が**半透明のまま残る**。

**原因はほぼ常に「既定OFFの漏れ」**。`pixiScene.resetActorFxDefaults` が
「毎フレーム全部消してから、点ける分岐だけが点け直す」という作法になっており、
**新しいスプライトを足した時に、点ける側だけ書いて消す側を書き忘れる**と残る。

**踏んだ実績**:
| 版 | 何が漏れていたか |
|---|---|
| v0.25.3207 | 既定OFF自体が `drawEnemy`(try/catchの中)にあり、**例外でスキップ**されていた → try の外へ切り出し |
| v0.25.3324 | `hitFlash` が既定OFFの一覧に無かった(白い歪んだシルエットが焼き付く) |
| v0.25.3383/3408 | 賞金首の専用プールが `isGate2AngelBoss` ブロックの中にあり、**賞金首には一度も適用されていなかった** |
| v0.25.3458 | 構え/振りで同じコンテナを使い回すのに、**後から足した子(弧・風圧)を消していなかった** |
| **v0.25.3838** | **`phillWingThrustFx`(v0.25.3740で新設)が既定OFFに無かった** + フィルの本体以外の絵(影/羽/後光/キラキラ)が既定OFFに無かった |

**掟(v0.25.3458 で立てて、v0.25.3838 で再発した)**:
- **同じコンテナを使い回す描画で子を足したら、"消す側"の全経路も同時に直す。**
- **新しいスプライト/プールを足したら、`resetActorFxDefaults` へ必ず1行足す。**
  「点ける分岐が必ず毎フレーム走る」は前提にできない(例外・早期return・カリングで飛ぶ)。
- **`drawEnemy` の後ろの方で点ける絵は特に危ない**——例外が出ると点け直されないのに、
  既定OFFが無いと消えもしない。

**切り分け**: `?hidelayer=` には**敵・ボスの層を落とすキーは無い**(背景・遠景・幕だけ)。
アクターの絵を追う時は DEV ビルドの `window.__pixiScene` からシーングラフを列挙する
(v0.25.2328 で263ノード中10件に絞れた実績)。実機では左下の赤字 `ERR draw:<type>` が
`drawEnemy` の例外を報せる(`errorBeacon.ts`)。

## ★他の系のAIループを「早期return」で乗っ取らない(2026-08-25・噛みつきの踏み込みで2件事故)

`updateEnemies` の敵ループは **1体ぶんのAI本体**であり、そこには移動だけでなく
**技の解決(`pumpkinBlasts` への push=着地爆発・踏み鳴らしの円)** も入っている。
ここで「特別な状態のときは早期returnして位置だけ書く」をやると、**その敵のその技が丸ごと消える**。

- 実例1(v0.25.3924): 噛みつきの踏み込み中に技へ入った敵は、AIを飛ばされて
  **円の判定が push されず**「赤い円の中にいるのに食らわない」になった。
- 実例2(v0.25.3923): 同じ踏み込みが**位置を絶対座標で上書き**していたため、
  ノックバック/リーシュ/ボスの状態機械と殴り合い、相手の書き込み量がそのまま飛距離になった
  (「一発で画面外に出ようとしている」警告が頻発)。

**規則**: ①位置は**差分で足す**(絶対座標で毎フレーム上書きしない) ②**技を出している敵のAIは
乗っ取らない**(乗っ取る前に、その特別な状態のほうを中断する)。
