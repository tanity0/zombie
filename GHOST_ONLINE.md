# 守護霊オンライン化(G-ON)仕様 — オンラインチャット向けハンドオフ

社長との合意 2026-07-30〜31(実装チャット側で協議)。**このファイルがオンライン層の正本。**
霊体(守護霊)本体の仕様は `BOT_AND_GHOST.md` が正本で**現在も活発に変更中**——
オンライン層は霊体仕様に一切触れない(§0の境界原則)。関連節は §2.14(独立メニュー+オンライン構想)/
§2.15(リザルト同行カード+いいね)。**本ファイルの決定は §2.14★未決1・2 と §2.15★未決(いいね)の
社長裁定を含む**(霊体チャットはここを参照して同期する。BOT_AND_GHOST.md への反映は霊体チャットの裁量)。

## 0. 境界原則(最重要 — 霊体仕様の変化から絶縁する)
- **オンライン層はプロファイルを「中身を解釈しない封筒」として運ぶだけ。**
  封筒 = `{ profile: <霊体側の不透明JSON(v付き)>, card: <オンライン層所有の表示用名刺> }` の2区画。
- サーバーはスキーマを知らない(ただのJSON列)。霊体側でノブ/構造が変わってもサーバー・通信コードは無変更。
- 検証・クランプは**受信クライアントの霊体側バリデータ**(その時点の isValidProfile 等)に委ねる。
- **profile の `v`(スキーマ版)が自分の理解と不一致なら使わず捨てて自分フォールバック。**
  霊体仕様が大きく変わったら `v` を上げれば古い公開プロファイルは自然に無効化される。
- `card`(名刺)区画はオンライン層の所有物。公開ボタン押下時にオンライン層が組み立てる。
  名刺の細目(ビルド表示内容)は霊体側の PlayerBuildSnapshot 等が安定してから確定する後決め項目。

## 1. スコープ(社長裁定)
- **ランキングはやらない**(順位で競う構造を作らない)。
- **復讐(宿敵の輸出)は保留**(面白いかも、程度。本テーブル構成で将来流用可=捨て決定にはならない)。
- **本命 = 守護霊の交換 + いいね。**

## 2. 決定事項
1. **基盤**: Supabase + RLS(自分の行しか書けない)。無料枠運用。
   ※初回のみ社長のSupabaseアカウント/プロジェクト作成が必要(コード外作業)。
2. **身元**: 端末ごとの匿名UUID(localStorage)。機種変で消えるのはv1では割り切り。
3. **公開 = 手動ボタン**(自動同期しない)。1人1行のupsert・アルバム丸ごと1体
   (= §2.14★未決2の裁定)。置き場所は §2.14 の独立メニュー(守護霊/討伐記録)想定だが、
   ボタン設置はオンライン基盤と同時(死にボタンは先に置かない=§2.14の方針に同意)。
4. **取得**: ミッション選択画面を開いた時に裏で**3件**取得開始(体感待ちゼロ)。
   召喚の瞬間には通信しない(ランのキャッシュから読む)。
5. **選出 = 出撃時3択**(= §2.14★未決1の裁定は (b))。出撃ボタン押下時に3件揃っていれば
   小モーダル(候補カード3枚+「自分の霊」)。揃わない/通信失敗/オフライン → モーダルを出さず
   無音で自分の霊。**再抽選ボタンは付けない**(強いのが出るまで回す=事実上のランキング化を防ぐ)。
6. **候補カードの表示 = 名前+「このステージに出現するボスを◯秒で撃破」だけ**。
   - ボス名は**召喚者側が未遭遇なら「???」表記**(ネタバレガード。タイムは見せる=
     「???を38秒で倒した人」という選び方自体が面白い)。
   - 撃破記録の無い候補は「記録なし」(選択は可=普段のプレイスタイルで戦う)。
   - 撃破タイムは BossStyleSlot.clearTimeMs(v0.25.2493実装済み)。値の取り出しは
     **霊体側提供のサマリ関数経由**(オンライン層は封筒を直接解釈しない)。
   - **生のノブ数値・いいね数はカードに出さない**(数字を出すと強さ比べ=ランキングに戻る)。
7. **リザルトで開示**: 共闘した守護霊の詳細ステータス+ビルドはリザルトで見せる(§2.15と同方針)。
   選ぶ前は最小情報、働きを見た後に正体を知る、の情報順。
8. **いいね**(§2.15★未決の裁定): 単位=**守護霊の持ち主に1ラン1回**。リザルトの同行カードに
   ボタン設置。サーバーはイベント行insert(送り主UUID+相手UUIDの一意制約で重複・連打を防止)。
   **いいねは選出に影響させない**(持ち主に届く私的な数字のまま)。
   ネガティブボタンは作らない。
9. **持ち主への通知**: 次回起動時に1回だけ「召喚された回数+貰ったいいね」を取得して表示
   (リアルタイム通知はしない)。累計は独立メニューの自分のプロフィールで閲覧(§2.15と同方針)。
10. **名前**: 受信時に文字数制限+フィルタを通してから表示(オンライン層の責務。
    srcName の作り方=霊体側には触れない)。文字種/上限の細目は §2.14★未決3 と合わせて後決め。
11. **安全弁**: `?online=0` で全停止/サーバー不調時は全機能が無音でローカル動作
    (エラーをプレイヤーに見せない)。通信がゲーム進行に影響しないことを構造で保証する
    (4の先読み+キャッシュ方式がその実装)。

## 3. 実装バッチ分割(案)
- **G-ON1**: 匿名UUID+手動公開(封筒組み立て+upsert)+Supabaseプロジェクト/RLS設定
- **G-ON2**: 3件取得+出撃時3択モーダル+ランキャッシュ+フォールバック+持ち主IDの持ち回し
- **G-ON3**: いいね(イベントinsert+リザルトボタン)+起動時通知+名前フィルタ
- 着手時期: **霊体仕様の安定と社長Goを待つ**(§2.14の実装時期未裁定と同じ扱い)。
  独立メニュー(§2.14確定)や§2.13年表カード部品など霊体側UIの後に乗るのが自然。

## 4. コスト(試算 2026-07-31)
- 1プレイ = 3〜5リクエスト・約10KB(公開は手動化でさらに減)。
- 1日1,000プレイでも転送 月450MB ≪ Supabase無料枠5GB。DB 500MB ≒ プロファイル15万人分。
- **現実的な規模(〜数千プレイ/日)まで 0円**。超えたら Supabase Pro $25/月 の崖が1つあるだけ。
- いいねイベント行は集計後に古い行を削除する運用(容量対策・実装1手間)。

## 5. 未決(後決めリスト)
- 名刺(card)区画の内容細目(武器/スキル/レベル等どこまで) — 霊体側 PlayerBuildSnapshot 安定後
- 公開ボタンの正確な置き場所・文言 — §2.14独立メニューの実装時
- 名前の文字種/文字数/NGワードの細目(§2.14★未決3と統合)
- ボス「未遭遇」判定のデータ源 — 実装時に調査(召喚者側ローカルデータの話=霊体仕様外)
- Supabaseプロジェクト作成(社長作業)のタイミング
