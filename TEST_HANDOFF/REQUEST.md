# テスト依頼(追試): 噛みつきが実際に当たっている証拠を取る(2026-08-25 夜・設計チャット)

## 前回(2026-08-25 21:36)の結果と、この追試の理由
前回で **「敵が無害化していない」「固まらない」「consoleエラー0」は確認できた**(A:被弾176/175/130、
B:21枠完走・未接敵0)。**ありがとうございます、目的は達しています。**

ただし**設計側(私)の依頼が1点足りませんでした**。3ランの `deathCause` は
「変異体(獣化型)」「変異体(肥大型)の落下攻撃」「地雷」で、**どれも噛みつき以外の攻撃**でした。
つまり **「噛みつきが実際にプレイヤーへ当たった」直接の証拠がまだ0件**です
(被弾があること自体は他の攻撃で説明できてしまう)。**ここだけを埋める短い追試**をお願いします。

## 目的(これだけ)
**`damageTaken` の中に「噛みつき」による被弾が含まれているかを、証拠として1件でも取る。**

## ★これは「ではない」(労力をかけなくてよい)
- **ボス・ガントレットは今回**不要**。** 前回完走済み。
- バランス・勝率・DPS・見た目の評価は対象外。
- 原因調査・コード修正はしない。
- スクショは下の①を撮れた1枚だけでよい(撮れなければ無しで可)。

## 大前提
- **`git pull`**。版が **v0.25.3929 以降**であることを確認して記録。
- devサーバを使う場合は pull 後に**必ず再起動**。
- **コード・数値は一切変更しない。**

## 回し方(前回詰まった所は直してあります)
`node scripts/botrun-local.mjs` を**そのまま**実行してください。
- `request.config.json` を **`configs` 配列の形**に直しました(前回そのままでは実行できず、
  手作業で回避させてしまいました。すみません)。
- スクリプトが `stage=stage-1` を直書きしていて **stage-2 のランが作れなかった**件も直しました
  (v0.25.3929)。`configs[].stage` で指定できます。設定済みなので触らなくて大丈夫です。
- 3ラン(stage-1 標準 / stage-1 rusher / stage-2 標準)が自動で回ります。**約15〜20分**。

## 記録してほしいこと(これだけ)
1. ★**各ランの `[BOT_REPORT]` 生JSON全文**(前回同様)。
2. ★**画面に出るダメージ表示や死亡画面に「噛みつき」の文字が出た場面**があれば、
   **スクリーンショット1枚**と「何ラン目の何秒ごろか」。
   ※ **rusher ペルソナ**(2本目)は敵に突っ込むので、これが一番出やすいはずです。
3. **`deathCause`** の値(3本ぶん)。
4. **consoleエラー全文**(無ければ「0件」)。
5. 60秒以上進まなくなった場面があれば時刻とスクショ。

**「噛みつき」が一度も観測できなくても、それ自体が有用な結果です。**
その場合は「観測されず」と書いてください(設計チャットが判定を追います)。

## 出力先
- `TEST_HANDOFF/results/<YYYYMMDD-HHMM>-bite-evidence.md` + 同名 `.json`
- コミットメッセージ先頭に `[test-report]`。**bump/changelog/DEVLOG は不要**。
