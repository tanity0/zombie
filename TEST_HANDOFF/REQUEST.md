# テスト依頼: 新仕様ベースライン+対ボスクリ再計測(2026-08-15・設計チャット)

## 目的(前回8/14計測の取り直し。仕様が2つ変わったため)
1. **新仕様ベースライン**: R1固定スタート+時間で上がるランク床(v0.25.3395)+賞金首3:00/7:00・
   紅き夜6:00(v0.25.3420)が入った後の基礎指標——総キル/到達レベル/**ランク推移(床が効くか)**/
   生存時間/スクラップ収支/critStats。
2. **対ボスクリの再計測**: 賞金首がボス扱い(v0.25.3412)になり、クリ率の絞り(×0.5+floor)と
   critStats.vsBoss集計が効くようになった。前回の「交戦したのにvsBoss=0」は解消済みのはず——その検証。
※数字の分析・判断は設計チャット。テストチャットは実行と記録だけでよい。

## 実行(2系統・計5ラン)
**必ず `git pull` して v0.25.3430 以降で回すこと**(1ラン目のBOT_REPORTで版とcritStatsの存在を確認)。

### A. 新仕様ベースライン = 3ラン(new-base-1〜3)
- いつも通り `node scripts/botrun-local.mjs`。stage-1固定・15分cap。
- 3:00/7:00に賞金首が自然出現するはず(ボットが絡むかは運任せ・記録だけでよい)。

### B. ボス戦ラン = 2ラン(crit-boss-1〜2・`?bountynow=1&bountytype=melee`付き)
- 賞金首(馬乗り)を強制出現。ボットが交戦すれば **critStats.vsBoss** に今度こそ乗るはず。
- 乗らなかった場合はその旨を1行(それ自体が「まだ集計が繋がっていない」の証拠になる)。
- **目安: ビルド+5ランで45〜70分**。

## 記録(results/ へ・いつもの形式)
- 各ランの `[BOT_REPORT]` 生JSON全文(critStats・elapsedSec・dda/ランク推移が本体)
- consoleエラー全文(なければ「0件」)/実行環境(headed/headless・OS)/ショップが開いた回数
- 気づき(任意): 賞金首・紅き夜の出現時刻が3:00/7:00/6:00どおりだったか(体感でよい)
