# テスト結果 依頼#9: §6.37 FIELD-WIDE-ZOOM 帯割れ+ボスカメラ復帰+技エフェクト 【2026-08-07 18:57 JST】

- 依頼: `TEST_HANDOFF/REQUEST.md`(依頼#9)
- 環境: **headed 実Chrome(GPU・AMD Radeon)**・`npm run dev`(5173)・**v0.25.2984**(前提の0.25.2983以上を満たす)・
  commit `570a0661`・viewport **390×844 / DPR2**・**src変更なし**(掟)
- スクショ: `results/20260807-1857-fieldwide/`(計25枚超)/ 生データ: 同フォルダの `_A.json` `_BCD.json` `_D-glen.json`
- **判断はしていません**(観測のみ)。

---

## console エラー(全パート共通・**全ランで発生**)

```
[shadow-v9] bakeSilhouette failed (suppressed): TypeError: Cannot read properties of null (reading '0')
    at BindGroup.getResource (http://localhost:5173/zombie/node_modules/.vite/deps/chunk-PHKVVWK...)
```
- **A: 8ラン中8ランで各1件** / B: 3件 / C: 記録あり / D: suriel 1件・acrasiel 2件・glen 1件。
- 文言に `(suppressed)` が入っており握り潰されている形。**それ以外の console エラー・pageerror は0件。**

---

## A. 帯割れチェック(`?zoomlock=0.4`)

実測ズームは全ステージ **0.400〜0.420** で意図どおり掛かっていた。各ステージ「開始地点」「約21秒歩行後」の2枚。

| ステージ | 1.上接合(黒帯) | 2.下接合(黒帯) | 3.左右 | 4.アクター可視 | 実測zoom |
|---|---|---|---|---|---|
| stage-1 | **黒帯なし** | **黒帯なし** | 切れなし | プレイヤー・松明・花が正常表示 | 0.420 |
| **stage-2(重点)** | **黒帯なし** | **黒帯なし** | 切れなし | 敵1体+画面外矢印(左赤/右青)が表示 | 0.420 |
| stage-3 | **黒帯なし** | **黒帯なし** | 切れなし | プレイヤー・戦闘エフェクト・NPC台詞が表示 | 0.419 |
| stage-4 | **黒帯なし** | **黒帯なし** | ※下記 | プレイヤー・敵(赤)・戦車/櫓が表示 | 0.419 |
| stage-5 | **黒帯なし** | **黒帯なし** | 切れなし | プレイヤー・篝火・霧の人影が表示 | 0.419 |
| stage-7 | ※下記 | ※下記 | — | — | 0.400 |

- **stage-3 は以前(v0.25.2970)に私が撮った同条件の絵では「遠景の街と廃墟帯の間に真っ黒な帯」が出ていたが、
  今回は出ていない。**(比較用の旧スクショは `cam/baseline/S1-接近戦-idle-04.png` に残っている)
- **stage-4 のみ**: 黒帯は無いが、遠景帯(画面上部)に**薄い縦の継ぎ目**が2本、地面中央付近に**薄い横線**が1本見える。
- **stage-7**: 歩行後のスクショが**グレンの登場カットインで画面のほぼ全面を覆われており、接合の判定に使えない**。
  開始地点の1枚のみ有効。**カットイン無しの撮り直しが要るなら指示ください。**

### 観点5(M5=stage-5 の戦場の炎・砲火の照明)→ **出ている**
スカイライン上に橙の点光(砲火)、手前に篝火の光溜まりが確認できる。

### 観点6(stage-2 のズーム比較)→ **滑りは見られない**
同一地点 `(0,-214)` で2枚。緑の光条・下部の什器(青いタンク・モニタ)・窓外の位置関係が両者で保たれている。
- `A-stage-2-z0.4-walked.png`(実測 zoom **0.420**)
- `A6-stage-2-z1.0-walked.png`(実測 zoom **0.839**)
- ★**依頼書は「`?zoomlock=1` で等倍」の想定だが、実測は 0.839 だった。**
  `?zoomlock=1` は `CONTEXT_ZOOM_MIN`(=0.8)に固定する仕様のため、**1.0 の比較にはなっていない**。
  等倍との比較が要るなら別のフラグ指定が必要(指示ください)。

### 観点7(M0 の川のきらめき)→ **未確認**
`stage` 指定なしで開始地点・北へ14秒歩行後の2枚を撮ったが、**どちらも川が画面内に入らなかった**
(月・オーロラ・森・花畑・前景霧は正常表示、黒帯なし)。川の所在が分かれば撮り直します。

---

## B. リサイズ固着 → **帯割れの再発なし**

stage-1・`?zoomlock=0.4` で 390×844 → **390×1200** → 390×844。

| 時点 | 実測zoom | 絵 |
|---|---|---|
| 変更前 | 0.43台 | `B-1-before.png` |
| **高さ1200へ変更直後** | **0.433** | `B-2-resized-tall.png` — 月/オーロラ/森/霧/地面/前景が連続。**黒帯なし** |
| **844へ戻した後** | **0.432** | `B-3-restored.png` |

ズーム値が変更前後で 0.433 / 0.432 と一致しており、**固着の兆候は数値でも絵でも出ていない**。

---

## C. ボスカメラ復帰 → **ズームは通常画角へ戻った**

`?smoke=1&stage=stage-3&bossnow=1&class=warrior` / ボス=**jormungand** / 右へ実時間60秒逃走 → 45秒待機。
プレイヤーHPのみ毎秒満タンに戻している(**ハーネス介入**。ボスHP・仕様には未介入)。

| 時点 | zoom | ボスとの中心差 | bossState | 等倍画面内 |
|---|---:|---|---|---|
| flee+0s | 0.894 | (0,-488) | chase | Y |
| flee+5s | 0.704 | (-274,-363) | aim-radial | Y |
| **flee+10s** | 0.599 | (-834,-352) | **return** | n |
| **flee+15s** | **0.998** | (-1463,-497) | return | n |
| flee+20〜35s | 1.000〜0.994 | 〜(-3653,-481) | return | n |
| flee+40s / +45s | 0.851 / 0.706 | 〜(-4592,-493) | return | n |
| flee+50〜55s | 1.000〜1.002 | 〜(-5227,-661) | return | n |
| wait+0s | 1.001 | (-5177,-714) | return | n |
| wait+5〜40s | **1.046〜1.050** | (-4861,-241)で静止 | return | n |

- **ボスが等倍画面外に出た直後(flee+10s)に `bossState='return'`(帰巣)へ入り、その5秒後(flee+15s)にはズームが 0.998 まで戻っている。**
- ★**参考(前回 v0.25.2970 の計測)**: 同種のシナリオを4本+別ステージ2本回して **`return` は一度も観測されなかった**。今回は出ている。
- 気づいた点2つ(判断はしません):
  1. **flee+40〜45s で zoom が一度 0.851 → 0.706 まで下がり、また 1.0 へ戻っている**(逃走中の一時的な引き)。
  2. **待機中の zoom が 1.046〜1.050 と 1.0 をわずかに超えている**(ごく僅かに寄っている)。

---

## D. 技エフェクトの発火

各ボス実時間2分45秒まで粘り、狙いの state に入った瞬間にスクショ。

| ボス | 狙い | 結果 | 観測した全 state |
|---|---|---|---|
| **スリィエル**(stage-6・gateboss=1) | sweep / gaze | **両方出た** | chase, ring-spin-windup, ring-spin, ring-spin-recover, ring-move-windup, ring-beam-windup, ring-active, ring-recover, gaze-windup, **gaze**-recover, sweep-windup, sweep-recover, **sweep** |
| **アクラシエル**(stage-ex1・gateboss=1) | spike / burst / warp | **3つとも出た** | chase, **warp**-out, warp-in, warp-recover, **burst**-windup, burst-recover, burst, spear-windup, spear-recover, **spike**-windup, spike-recover, spike, gaze-windup, gaze-recover |
| **グレン**(stage-7) | nihil(三唱) | **3唱すべて出た** | g-boon/g-sweep/g-dash/g-reach/g-bolt/g-jump/g-talon/g-stomp/g-trijump 各windup・active・recover、**g-nihil-chant1 / chant2 / chant3 / g-nihil-recover** |

- スクショ: `D-suriel-sweep-*.png` / `D-suriel-gaze-*.png` / `D-acrasiel-{spike,burst,warp}-*.png` /
  `D-glen-g-nihil-chant{1,2,3}.png` / `D-glen-g-nihil-recover.png`
- **観点「唱ごとに濃く・大きくなるか」→ なっている。**
  chant1 は赤い外周が薄く、内側の環は**単環で淡い**。chant3 では**外周が太く鮮明になり、内側の環が二重環で明るく大きい**。
- **★グレンの検出に2回失敗した(私の不備・申し送り)**: グレンは実装上 **`type='giantbat'` の `isStoryBoss`** で、
  技の状態は `bossState` ではなく **`aiPhase` の `g-nihil-chant1/2/3`**(`useGameLoop.ts:7493`)。
  1回目は「`bossState` を持つ敵」、2回目は「`type` に 'glen' を含む敵」で絞ったため両方0体だった。
  **3回目に全敵の `aiPhase` を見て取得成功。**「出なかった」ではなく「私が見ていなかった」。
  次回グレンを見る時は **`aiPhase` の `g-*`** を見ること。

---

---

## E.(依頼外・社長のご指摘で追加調査)武器商人がステージ内に出ている件

社長より「武器商人でちゃってるよ」とのご指摘。**スクショに写っていたのは `weaponMerchant`** で確定。事実のみ記す。

| 条件 | 商人の座標 | プレイヤー(湧き位置)からの距離 | スプライト | stageTheme |
|---|---|---:|---|---|
| **stage-2 `?smoke=1`** | **(0, -60)** | **75px** | **visible=true** / alpha 0.30 | **lab** |
| stage-2 smokeなし | (0, -130) | 145px | (未生成) | **forest** |
| stage-1 `?smoke=1` | (0, -130) | 145px | visible=false / alpha 0.15 | forest |
| stage-7 `?smoke=1` | (0, -130) | 145px | visible=true / alpha 0.10 | forest |

- 商人の判定半径は全条件で `radius=58`。
- **stage-2 だけ商人が y=-60(75px)に置かれている**(他ステージは y=-130 / 145px)。
- コード上は `pixiScene.ts:6181` に「**商人は屋内でも(最初の部屋に)出す**」とコメントがあり、
  **出ること自体は意図された配置に見える**(判断はしません)。
- 該当スクショ: `A-stage-2-z0.4-walked.png` / `A6-stage-2-z1.0-walked.png`
  (プレイヤーの下方に、瓶を並べた人物として写っている)

### ★★ 併せて判明: `?smoke=1` の有無で stage-2 の `stageTheme` が変わる
- `?smoke=1&stage=stage-2` → `stageTheme='lab'`(商人 y=-60)
- `?stage=stage-2`(smokeなし)→ `stageTheme='forest'`(商人 y=-130)

**本依頼のA〜Dは全て `?smoke=1` で実施している**ため、**帯割れの検証結果は「`smoke=1` 条件下の絵」**という
前提が付く。smokeなしでの再検証が必要なら指示ください。

---

## 手法メモ
- 全パート headed 実Chrome(掟)。シミュ進行は実時間の約80%(別途実測)。
- チュートリアルのポーズは毎ポーリングで `isPaused:false` を書いて解除(ボットが閉じられないため)。
- C と D ではプレイヤーHPのみ満タンに戻している(でないとランが途中で終わる)。**ボス側には未介入。**
