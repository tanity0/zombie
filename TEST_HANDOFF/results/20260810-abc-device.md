# テスト結果: テスト設計A/B/C(v0.25.3085-3086)の実機検証 【2026-08-10 01:20 JST】

- 依頼: 社長「これを入れた。ランまわして」(テスト設計の型A/B/C)
- 環境: **headed 実Chrome(GPU)**・`npm run dev`(5173)・**v0.25.3086**・390×844 / DPR2・**src変更なし**(掟)
- 生データ: `results/20260810-abc-device/`(スクショ+JSON)

---

## ① ユニット全件 → **3117件 green**

```
Test Files  212 passed | 2 skipped (214)
Tests       3117 passed | 4 skipped (3121)
Duration    165.86s
```
新設分も通過。特に **型Bの本命「2連続ランのスモーク」が87.6秒かけて green**。
```
✓ playtest bot > short smoke run: 2 runs x ~5min-equivalent, mixed personas  87592ms
```

---

## ② 型B(ラン間の持ち越し)の実機確認 → **合格**

1回出撃 → 製品の `resetGame()` → 2回目出撃。演出は **rAFで毎フレーム累積**して数えた
(1秒サンプリングでは短命な演出を落とすため)。

| | gameTime | 撃破 | 演出の最大同時数 | 演出が出ていたフレーム |
|---|---:|---:|---:|---|
| 1本目 | 54秒 | 20 | 262 | 1442/2219 |
| **2本目** | 61秒 | 24 | **245** | **3616/4714** |

**2本目でも演出が普通に出ている**(元の事故は「2回目以降の出撃で1粒も出なかった」)。
console/pageerror ともに **0件**。スクショ: `B2-run1.png` / `B2-run2.png`。

---

## ③ 型A-1(絵の尺=判定の尺)の実機確認 → **数値は一致・絵は未確定**

### 数値(これが本命) → **一致**
滞空フェーズの継続時間を rAF で毎フレーム追い、台帳(`AIR_MOVES`)の値と突き合わせた。

| 技 | 実測 | 台帳(生値→実効) | 判定 |
|---|---|---|---|
| `g-jump-air` | **n=15 / 平均 334ms** | 384 → **320ms** | **一致**(差14ms=フレーム量子化の範囲) |

旧事故は「**絵だけ833ms・判定は320ms**」だった。現在は判定側が台帳値どおりに出ている。

### 絵 → **未確定(私の撮影が届いていない)**
- 1回目の撮影は **ボスが画面外の瞬間**を撮ってしまい、材料にならなかった(赤い着地予告だけが写り、
  ボス本体が居ない)。**撮れたことにはしない。**
- 2回目は「ボスが可視矩形内に居る滞空フレームだけ」に絞って撮り直した(`air-glen-*-INVIEW.png` 4枚)。
  しかし**グレンは巨体で全画面が赤い演出に覆われ、本体の輪郭と接地位置が読み取れない**。
  HPバーと着地予告は写っているが、「**絵が実際に浮いているか**」はこの絵からは断定できない。
- 城ボス側は滞空フェーズを1件も観測できなかった(rAFの計測開始がボス出現より早かった可能性)。

**申し送り**: 絵の側を機械的に確かめるなら、`window.__pixiScene` から**ボスのスプライトYと接地Yの差**を
毎フレーム読み、滞空中に差>0(かつ台帳の `hopPx` に整合)であることを見るのが確実。
スクショの目視では巨体+全画面エフェクトのボスで判定できない。**必要なら次回それで測ります。**

---

## ④ 型C(付け忘れ)について
台帳 `AIR_MOVES` は現在**3件**(`jump` / `g-jump-air` / `g-trijump-air`)。
TEST_DESIGN.md の叩き台にあった `g-glide-active` / `thor jump` / `rafi jump` は**台帳に無い**。
意図的な絞り込みか取りこぼしかは**私には判断できない**(判断は設計チャット)。事実のみ記す。

---

## console
**全パートで pageerror・console error ともに0件。**

## 手法メモ
- 全て headed 実Chrome(掟)。プレイヤーHPのみ満タン維持(A-1の観測窓を確保するため。ボス側は未介入)。
- 滞空フェーズは **rAFで毎フレーム**追った。384ms/336ms の技は秒単位サンプリングでは丸ごと落ちる。
