# 移動速度の再設計(社長裁定 v0.25.2442)— 仕様書(実装バッチM-SPD1)

## 決定の経緯(要約)
- 台帳(research/SKILL_EQUIP_LEDGER.md §B)で判明: 速度倍率はスケーター抜きでも
  runner×mage固有×装備Tier5 = **×2.02** まで積め、直近のボスバランス(回避余裕1.38倍)が
  **2.8倍に崩れる**。減速の経路はコード上に存在しない。
- エルデンリング/ナイトレインの調査: **戦闘中の速度は全ビルド固定**(装備重量が変えるのはロールの質のみ)。
  高速移動はトレント/疾走=**移動側に隔離**。「周辺の余裕は買えるが、反応の芯は買えない」。
- 社長裁定: 値を下げる/廃止するのではなく、**「速度アップに遅延(ランプ)を持たせる」**方式。
  +「スケーターは乗車中攻撃しない」(v0.25.2442確定)。

## 仕様1: 速度ボーナスのランプ+切り返しリセット
**速度ボーナス(基礎速度より上の部分)は「同じ方向へ走り続けた時間」で立ち上がり、
鋭い方向転換または停止でゼロへ戻る。**

- 新設 `src/utils/speedRamp.ts`(純関数+state):
  - `SpeedRampState { sustainMs: number; lastDirX: number; lastDirY: number }`
  - `stepSpeedRamp(state, { dtMs, moving, dirX, dirY })`:
    - `!moving` → sustainMs = 0
    - 移動方向が前回から **RAMP_RESET_ANGLE_DEG=75°** 以上変わったら sustainMs = 0
    - それ以外は sustainMs += dtMs
  - `rampFrac = clamp01(sustainMs / RAMP_FULL_MS)`、**RAMP_FULL_MS = 1500**(予告リード床0.45〜1.8sと噛み合う長さ)
- **適用式**: 対象倍率の積を P として `実効倍率 = 1 + (P - 1) × rampFrac`
  (基礎速度は即応のまま。ボーナス部分だけがランプに乗る)
- **対象(P に入るもの)**: runner(通常+リロード中ボーナス) / warm-up / **マークスマン**(既存の
  「2秒移動で発動」個別条件は廃止してこの共通ランプへ統合=意図的な一本化) / 装備 moveSpeedMult
- **対象外**: スケーターの×3(乗車という自前の条件+慣性が個性。現状維持) / PLAYER_BASE_SPEED /
  MOVE_SPEED_MULT / 敵側の速度(1pxも触らない)
- ランプの状態はシミュレーション層(gameStore の movePlayer 経路)で毎tick更新。レンダラに置かない。
- 復帰フラグ: `?speedramp=0` で旧挙動(ボーナス即時全開)。
- 数値は全て定数化(実機調整前提)。

### 設計意図(コメントに残すこと)
- フィールドの直線移動・緩いカーブのキャイト=ボーナス満額(**速度装備は損しない**)。
- ボスの予告からの回避=急な切り返し=素の速度(**読みのゲームが守られる**)。
- エルデンリングの「戦闘中の速度は定数/ビルド差は回避の質」をうちの形に翻訳したもの。

## 仕様2: スケーター乗車中は攻撃封印(社長確定)
- `skaterRiding === true` の間: **銃の自動発砲・カウンター・サブウェポン発動を全て封印**
  (例外なしの1行ルール。ネタ例外は実機で物足りなければ後日)。
- 降車は既存トグルのまま即時=「降りて即反撃」は成立。
- 見た目: 乗車中は構え武器スプライトを非表示(乗車ポーズは既存)。
- 復帰フラグ: `?skaterlock=0`。
- 慣性・×3倍率は不変。

## 受け入れ条件
1. `stepSpeedRamp` にユニットテスト(立ち上がり1.5s/75°リセット/停止リセット/緩いカーブは維持/
   `?speedramp=0`相当の素通し)
2. 乗車中の封印は「発砲判定の入口」「triggerCounter」「サブ発動の入口」の3箇所で確実に効くこと+テスト
3. typecheck エラー0 / lint エラー0(テスト/ビルドはTesting policyどおり回さない)
4. version bump / changelog(プレイヤー向け表現) / DEVELOPMENT_LOG(JST打刻・★未決があれば明記)
