# テスト設計(社長承認 v0.25.3084・着手順 B → C → A)

> **この文書は設計時の姿です。現行仕様の正はコード+DEVELOPMENT_LOG。案件の状態は PROJECT_STATUS.md を見る。**

開発ログに残っている**実際に壊れた事故**から設計する。テストの本数(191ファイル)は足りていて、
足りていないのは**種類**だった。事故は3つの型しかない。

| 型 | 実例(全て実際に起きた) | 既存テストで捕まらない理由 |
|---|---|---|
| **A** 同じ意味の値が2箇所 | 城ボスの滞空の**絵だけ**別定数(実効833ms)を読み、実際は320msで着地していた(v3077)/ 予兆の刃に向き補正が無かった(v3081)/ 横の画角が**退役した機構**を前提のまま残っていた(v3066) | 片方だけ直しても**両方とも単体では正しい**。突き合わせる主体が居ない |
| **B** ラン間で状態が持ち越される | キラキラの間引き時計がリセットされず、**2回目以降の出撃で1粒も出なかった**(v3070) | テストは毎回まっさらから**1ランしか回さない**ので構造的に踏めない |
| **C** 同じ動作を持つ全員に付け忘れ | グレンの連続ジャンプだけ浮いていなかった(v3077)/ 三連射がキー漏れで**一度も抽選されなかった**(v3046)/ 演出が城ボスにしか付いていなかった(v2426) | 付けた技は動く。**付け忘れた技は誰も見ていない** |

---

## B. ラン間の持ち越し 【着手済み・v0.25.3084】

### B-1 時計の集約(完了)
`src/utils/runClocks.ts` に gameTime基準の時計を**1オブジェクトへ集約**し、`resetGame` は
`resetRunClocks()` を1回呼ぶだけにした。フィールドを足しても**リセット側を直す必要が無い**
(汎用ループで全部0にする)ので、「足したがリセットを忘れた」が**構造的に起きない**。
テスト(`runClocks.test.ts`)は `Object.keys` で総当たりするため、**将来のフィールドも自動で検査対象**。
判定側は `shouldEmitThrottled`(巻き戻り耐性)を併用=**二重の安全弁**。

**掟**: gameTime基準の時計は `runClocks` に足す。ファイル内に `let xxxLastAt = 0` を作らない。

### B-2 2連続ランのスモーク 【完了・v0.25.3086】
土台(B-1)は「時計」しか守れない。**時計以外のモジュール状態**(集計・採番・キャッシュ)は別途必要。
- 内容: 既存のボットスモークを**2周**回す(1ラン → `resetGame` → 2ラン)。
- 合格条件: **1本目と2本目で同じ種類のイベントが出る**(撃破数>0・演出の発火>0・被弾>0 など、
  ラン単位の観測値が2本目で0にならないこと)。値の一致までは求めない(乱数があるため)。
- 期待効果: 型Bを**原理的に全部**引っかける。1本で一族ごと殺せるので投資対効果が最大。
- 実装の勘所: 既存スモークの実行部を関数へ切り出し、2回呼ぶだけにする(新しいハーネスを作らない)。

---

## C. 同じ動作を持つ全員 【C-1/C-2 完了・v0.25.3086】

### C-1 動作の台帳
「跳ぶ技の一覧」「刃を撒く技の一覧」「予告帯を出す技の一覧」を**1つの表**にして、
**描画側も判定側もその表から引く**。今は同じ集合が複数の `if` 連鎖に散っているため、
新しい技を足した時に必ずどこかが漏れる。

対象の表(叩き台):
- `AIR_MOVES` … 跳ぶ技(汎用jump / g-jump-air / g-trijump-air / g-glide-active / thor jump / rafi jump)
- `BLADE_VOLLEY_MOVES` … 刃を撒く技(skadi blade / rafi bone)
- `BAND_TELEGRAPH_MOVES` … 予告帯を出す技

### C-2 網羅テスト
- 表の**全員**が処理を通ること(例: 全ての `AIR_MOVES` が高さ曲線を持つ=高さ0のままの技が無い)。
- 表と実装の**双方向**チェック(表にあるのに実装が無い / 実装があるのに表に無い)。
- 憲法テスト(`constitution.test.ts`)の拡張として置く。

---

## A. 値の二重管理 【A-1 完了・A-2 は手順で運用】

### A-1 まずは跳ぶ技だけ
全技の「絵の尺 = 判定の尺」を突き合わせる台帳は大仕事。**実際に事故ったのは跳ぶ技**なので、
`AIR_MOVES`(C-1の表)に**滞空時間の出どころ**を1つだけ持たせ、store も pixiScene もそこから読む。
テストは「絵の尺と判定の尺が同じ定数から来ている」ことを固定する。

### A-2 退役の取り残し
機構を退役させた時の**チェックリスト**(参照元の洗い出し・前提にしていた値の棚卸し)を
`ENGINEERING_NOTES.md` に置く。テストでの機械化は難しいので手順で守る。

---

## 実施結果(v0.25.3084〜3086・社長「全部入れて」)
| 項目 | 状態 | 実体 |
|---|---|---|
| B-1 時計の集約 | 完了 | `src/utils/runClocks.ts` + `runClocks.test.ts`(総当たりリセット検査) |
| B-2 2連続ラン | 完了 | `src/store/sim.test.ts`「憲法: 2連続ラン」2件(同じ手順を2回回し、2本目が0に落ちないこと) |
| C-1 動作の台帳 | 完了 | `gameStore.AIR_MOVES` / `airMoveFor()`(跳ぶ技の一覧+滞空時間+高さ) |
| C-2 網羅テスト | 完了 | `src/store/airMoveLedger.test.ts`(実装が扱う技が全て載っているか・各行の自己整合) |
| A-1 尺の一本化 | 完了 | pixiScene の滞空描画3経路が**台帳から**滞空時間と高さを引く(独自定数を持たない) |
| A-2 退役の取り残し | 手順 | 機構を退役させる時のチェックリストを ENGINEERING_NOTES.md へ(テストでの機械化は困難) |

## 今後この網に足す時のルール
- **新しい跳ぶ技**を足したら `AIR_MOVES` に1行足す(絵は自動で付き、テストが漏れを検出する)。
- **gameTime基準の時計**を足したら `runClocks` のフィールドにする(`let xxxLastAt = 0` を作らない)。
- 型Cの網は跳ぶ技から始めた。**刃を撒く技/予告帯を出す技**も同じ形で台帳化できる(次の候補)。
