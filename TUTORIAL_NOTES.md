# チュートリアル用・特別仕様メモ

> **この文書は設計時の姿です。現行仕様の正はコード+DEVELOPMENT_LOG。案件の状態は PROJECT_STATUS.md を見る。**

ゲーム内チュートリアル/ヘルプで説明が必要な「分かりにくい/特別な」仕様の覚書。
実装状況の列に注意（spec=社長指示の意図 / impl=現在の実装）。担当エージェントは
チュートリアル整備時にここを参照すること。

## 城フィナーレボス(giantbat)
- 出現条件 = 城へ接近(380px) **かつ** 5分経過。序盤に城へ突っ込んでも出ない。
- `?castlenow=1` でデバッグ即時出現(時間ゲート無視)。
- 実装済み(v0.25.746)。

## 時間による死神(時間制限のデス)
- 15分経過後、**20秒ごとに抽選**。確率 = 10% +(15分以降の経過分×10%)、最大100%(16分20%…24分100%)。
- 抽選ごとに「気配演出」(横切り)。当選で完全出現(追跡死神)。
- 距離(深奥)の死神とは別系統。**時間死神は原点に戻っても去らない**(逃げ場なし・討伐は可能)。
- 出現は**必ず画面外から**(画面の最遠角+余白)。接触ダメージ=**77**。
- 実装済み(v0.25.746、時間開始10分/画面外/ダメ77は v0.25.777)。

## レスキューイベントの敵ターゲット
- 既定は守る対象(survivor)を狙う。
- **プレイヤーが近接ダメージを与えた敵だけ**プレイヤーへターゲット切替(アグロを引き受ける)。
- 銃/爆弾ではアグロは付かない(近接のみ)。
- 実装済み(v0.25.751)。

## スコア/ゴールド
- スコア(ハイスコア)は**青天井**(cap無し)。
- ゴールド換金は各項目を**MAXでクランプ**した合計 ÷2000。
- 死亡しても**クリアボーナス以外は全計上**。speedBonus はラボ勝利のみ。
- 実装済み(v0.25.749)。
