# 実機テストAIの強化 → プレイヤーゴースト助っ人（設計・社長相談 v0.25.2431）

社長の要望（原文の要点）:
1. **実機テスト用AIをかなり強化したい。ちゃんとプレイヤーとして振る舞うように仕上げたい。**
2. 最終的には **要所（ボス戦・ピーク・ゲート）でだけ現れて味方するAI** として使う。
3. そのために **プレイヤーの動きの傾向を「数値として」取って**（機械学習ではない）近い行動をさせ、
   **ゴーストとして助っ人に入る**。ヘイトも取る。カウンター頻度・好きな距離感・近接の傾向など。

---

## 0. 結論（先に）

**この3つは別々の仕事に見えて、実は「1つの層」を作れば全部つながる。**
その層とは **「プレイヤーの実測値を、ボットの既存ノブと同じ形で出す」** こと。

理由は下の §1 のとおりで、**ボット側の受け口はすでに存在している**。足りないのは
「実プレイヤーを測って、その受け口に流し込む配管」だけ。

---

## 1. 現状の棚卸し（既にあるもの）

| ファイル | 行数 | 役割 |
|---|---|---|
| `src/utils/playtestBot.ts` | 639 | 入力合成の本体。**ペルソナ7種**（standard/kiter/stationary/boar/wanderer/rusher/scavenger） |
| `src/utils/botSkill.ts` | 374 | **腕前の段階**（novice/casual/skilled/master）。ペルソナと**直交** |
| `src/utils/botObjective.ts` | 323 | 目的地の選択（POI/回収/前進） |
| `src/utils/botEngagement.ts` | 106 | 交戦の可否 |
| `src/utils/botTelemetry.ts` | 100 | 計測（サブ発動回数 / **ダメージのチャネル別**(gun/melee/other) / フィニッシュ数 / 近接の振り回数・命中数） |

### ★ここが重要
`botSkill.ts` の `BotSkillProfile` は **既に「人間らしさの数値」そのもの**になっている:

- `reactionMs` — 脅威検知から行動までの遅延（人間の反応速度）
- `counterChance` — 検知1回あたりのカウンター試行確率（人間の見逃し）
- `dodge: 'none' | 'projectile' | 'aoe' | 'all'` — どこまで避けるか
- `targeting: 'nearest' | 'weakest' | 'threat' | 'optimal'` — 標的の選び方
- `surroundCount` — 「囲まれた」と判断する敵数

そして `botTelemetry.damageDealt{gun, melee, other}` は **社長の言う「近接の傾向」を既に測っている**。

つまり **「プレイヤーの傾向 → 数値 → ボットの挙動」の受け口はもう空いている。**

---

## 2. 段階計画（各段階が単独で役に立つ形にする）

### 段階1: プレイヤー実測層 `playerTraits.ts`（**最初にやるべき**）

**プレイヤーの1ランを測り、`BotSkillProfile` と同じ形の数値に落とす。**

| プレイヤーから測る | 出力するノブ | 測り方（合流点） |
|---|---|---|
| カウンター成功/試行、成立までの時間 | `reactionMs` / `counterChance` | `lastCounterSuccessTime` / カウンター窓の開閉 |
| 敵との距離のヒストグラム（最頻値） | `preferredDist`（**新設**） | 毎tick、最近接敵との距離をバケットに加算 |
| 近接 vs 銃のダメージ比 | `meleeBias`（**新設**） | **既存 `botTelemetry.damageDealt` をそのまま使う** |
| 被弾数 / 予告に入った回数 | `dodge` | `komaLog` の `hitsTotal` + 予告判定（`isPlayerInAttackTelegraph` が既にある） |
| 倒した敵の選び方（最寄り/瀕死/危険） | `targeting` | キル時の敵の状態を分類して集計 |
| 静止時間の割合・平均移動量 | `mobility`（**新設**） | 入力の有無を積算 |

**掟（`komaLog` と同じ）**: この層は **記録するだけ。ゲームの判定には一切使わない。**
既定は無効で、通常プレイでは1バイトも溜めない。

**段階1だけで得られるもの**:
- **「ボットが人間らしくない」を推測ではなく数字で言える。**
  例: 実プレイヤーの `counterChance` が 0.3 なのにボットの standard が 0.65 なら、
  ボットは**人間の2倍カウンターしている**＝テスト結果が楽観的すぎる、と分かる。
- 今日の実測（v0.25.2409）で `kills 459 / 8.4分 / 被弾33` は取れているので、
  **ボットの同条件ランと直接比較できる**。

### 段階2: テストAIの強化（**段階1の結果で駆動する**）

社長の「ちゃんとプレイヤーとして振る舞う」を、**人力チューニングではなく実測合わせ**で達成する。

- `BotSkill` に **`'mirror'` を追加** — 段階1で測った実プロファイルをそのまま食う段階。
  `?bot=mirror` で「社長の分身」を走らせられる。
- 差分の大きいノブから潰す。現時点で**穴だと分かっているもの**:
  - **【調査で確定】ボットはボスの技を1つも避けない。** 回避の実装
    （`botSkill.ts` の `dodgeVector`）が見ているのは **`aiPhase === 'jump'`（汎用ジャンプ）と
    `aiPhase === 'charge'`（汎用突進）と弾と接触の4種だけ**。つまり:
    - 城ボスの `g-stomp-windup` / `g-jump-air` / `g-dash-charge` / 帯技全部（bite/slam/glide/
      wing/sweepbeam/reach）… **全部見えていない**
    - 連続ジャンプ `g-trijump-*`、グレンの遅延ダメージ `giantDelayedHits` … **見えていない**
    - トール/天使の `bossState` 系の技 … **見えていない**
    - おまけに `DodgeLevel` の `'aoe'` は **どの腕前段階にも割り当てられていない**
      （novice/casual='none' / skilled='projectile' / master='all'）＝型だけあって死んでいる。
    → **これが「ボットが人間らしくない」の最大の穴**。ボス戦のテスト結果は
      「赤を一切避けない人」の数字になっている＝**ボス関連のバランス判断の土台が無い**。
    → 直し方は簡単で、**赤い円/帯の位置は既に store 側に全部ある**
      （`gStompRadius` / `gJumpRadius` / `gTriJumpPts` / `giantDelayedHits[].capsule` /
      `aiFromX/aiTargetX`）。**新規の判定を発明せず**、描画が読んでいるのと同じフィールドを
      見て「赤の中に居たら外へ出る」を足すだけ。
  - リロード・弾切れの管理、サブウェポンの使いどころ。
  - 移動の質（人間は最短直線では動かない）。

### 段階3: ゴースト助っ人（本命）

**同じボットを、味方エンティティとして走らせる。**

- **出現条件**: ボス交戦中（`bossEngagedNow` が**既にある**）/ ピーク（コマ `peak`）/ ゲート
  （`activeGateRef`）。**3つとも既存の判定がそのまま使える。**
- **中身**: 段階1のプロファイルで駆動する `playtestBot`。プレイヤーの分身なので
  「あなたらしい動き」になる（＝ゴーストの意味）。
- **ヘイト**: 既に**召喚ユニット（`summons`）が敵のターゲット候補に入る仕組みがある**
  （`resolveEnemyTarget` / `ALCHEMY_AGGRO_RANGE`）。ゴーストをその枠に乗せれば
  ヘイト機構は**新規実装ゼロ**。
- 保存: 端末に1ラン分のプロファイル（数十バイトの数値）。`tutorialArchive` と同じ localStorage 作法。

---

## 2.5 設計の確定（社長との話し合い v0.25.2433）

### 前提の確定（社長）
- **狙い**: エルデンリングのように「自分の行動パターンを保存でき、それがピンチの人のところへ
  やってきて助けてくれる」快感。オンラインは未来。**まずはオフラインで自分の過去プレイ傾向を
  再現する助っ人スキル**として成立させ、実機テストにも使う。
- 同時1体 / ヘイトは `resolveEnemyTarget` の summons 枠流用 / 呼び出し口は装備スキル枠 /
  ソウル系の「壁の前だけ召喚→撃破/死亡で解散」の区切り。

### Q. ピークをやめて「ボス戦のみ」にすると再現度が上がるか？ → **上がる。推奨**
1. **文脈が一致するから**。ボス戦は1対1の構造化された戦い(予告を読む/カウンター/間合い管理)。
   そこで測った傾向(カウンター頻度・好きな距離・近接率)は、**そのままボス戦で再現される**。
   ピーク(群れ戦)の支配的な行動は範囲処理と群れキャイトで、ボス戦の傾向とは別物。
   混ぜて測ると**プロファイルが薄まる**し、群れ用の傾向をボス戦で再現しても「らしくない」。
2. **測定も切り出せるから**。`bossEngagedNow`(実装済み)でボス交戦区間だけを切り出して測れる
   =データが綺麗。ピークの雑多な行動が混入しない。
3. **ランク査定を汚さないから(技術的に重要)**。昇格は「10秒窓の撃破数」で数えるが、ピークに
   ゴーストが乱入すると**ゴーストのキルがプレイヤーの昇格に混ざる**。ボス戦中は昇格が凍結済み
   (v0.25.2413)なので、**ボス戦のみ=ペーシング系への影響が構造的にゼロ**。
4. ソウル系の召喚サイン(ボス扉の前)とも一致=ファンタジーとして自然。
→ **裁定: 出現はボス戦のみ。ピーク/ゲートは対象から外す**(ゲートはボス戦を含むので実質カバーされる)。

### 未決1: AI本体の作り方 → **薄い専用ドライバ `ghostDriver` を新設し、純関数部品を流用**
- 前提の訂正: `playtestBot` は「プレイヤーを狙う敵」ではなく**「プレイヤーを操縦する層」**
  (InputStateを合成する)。つまり**標的選択・回避・カウンター反応は向きの反転が不要**で、
  そのまま味方に使える。反転が要るのは「何のために動くか」だけ。
- **流用する純関数**: `pickTarget`(標的) / `dodgeVector`+`telegraphDodge`(回避・v0.25.2432で
  ボスの予告も見える) / `COUNTER_REACTION_PROFILES` 相当の反応抽選 / 間合い管理。
- **捨てるもの**: `botObjective`(POI/回収/前進)は丸ごと不要。ゴーストは**戦闘だけ**する。
- **新設するもの**: ①プレイヤー追従(リーシュ: 一定距離を超えたら瞬間的に追いつく=霊体なので
  ワープが世界観的に許される) ②Summon互換のエンティティ(位置/速度/HP)を毎tick駆動する薄い層。
- 置き場所: `src/utils/ghostDriver.ts`(純関数)+ useGameLoop の summons 更新部に1ブロック。

### 未決2: ペルソナの抽出方法 → **連続パラメータ(5〜7個)。分類はしない**
- 「6ペルソナのどれかに分類」は**実は軽くない上に忠実でもない**。既存ペルソナはデバッグ用の
  極端な原型(棒立ち/猪/放浪)で、実プレイヤーの中間的な振る舞いを表現できない。
- 測るのは既に決めた小さなベクトル: `reactionMs / counterChance / preferredDist / meleeBias /
  mobility (+dodge傾向)`。**ヒストグラムと平均を取るだけ**なので実装コストは分類と同等以下。
  ゴースト側は数値をそのまま食う=「近い行動」が最も直接に出る。
- 表示用の「あなたの型: 剣士型」等は**ベクトルから後付けで分類**すれば良い(見せ方だけの問題)。

### 未決3: 発動方式 → **スキル装備で自動発動**
- ボス戦のみ、と決めたので発動条件が一意(`bossEngagedNow` の立ち上がり=召喚、
  ボス撃破 or ゴースト死亡=解散)。**ボタン/ゲージのUIが丸ごと不要**。
- 既存スキル枠の作りにそのまま乗る。オンライン化した時も「ピンチの所へ勝手に来てくれる」
  ファンタジーは自動発動の方が合う(呼ぶのではなく、来る)。

### 未決4: 見た目 → **敵/味方の actor 描画系 + プレイヤー基本テクスチャの青白tint半透明**
- プレイヤー描画パイプラインは入力駆動の特殊系(武器レイヤー/刀/スケボー…)で、流用は重い。
- **ゴースト=青白い霊体**という見せ方にすれば(エルデンリングの霊体もそう)、**装備の見た目
  再現をv1で丸ごと回避できる**——霊体だから装備が見えなくても世界観として自然。
- 実装: summons が既に使っている actor プール描画に「プレイヤーの基本絵+青白tint+α0.7」で乗せる。
  新規描画経路ゼロ。装備見た目の再現は**やるとしてもv2**(オンライン化で他人の装備を見せたくなった時)。

### 未決5: オンライン → **ローカル完結で設計するが、プロファイルを「将来の通信ペイロード」として作る**
- プロファイルは**バージョン番号つきの小さなJSON**(数値だけ・目標200バイト以下)にし、
  localStorage(`tutorialArchive` と同じ作法)へ保存。**このスキーマがそのまま将来のアップロード形式**
  =オンライン化の時にAI設計へ手を入れずに済む。
- 保存は直近Nランの移動平均(1ランの事故で人格が変わらない)。

### 実装順(この設計での段階の再定義)
1. **G1: 計測** `playerTraits.ts` — ボス交戦区間だけを測ってプロファイルJSONを吐く(記録専用・既定無効)。
2. **G2: ゴースト本体** `ghostDriver.ts` + summons統合 + 青白描画 — `?ghost=1` でデバッグ召喚。
   **この時点で実機テストAIとしても使える**(プロファイルを差し替えれば任意の腕前の分身になる)。
3. **G3: スキル化** — 装備スキル枠に追加(ガチャ入手)。ボス戦で自動召喚。
4. (未来) G4: オンライン — プロファイルJSONをBaaSへ。設計変更なし。

## 3. ★未決事項（社長裁定が要る）

(v0.25.2433の話し合いでほぼ解消。残りは以下の2つだけ)
1. **ゴーストの強さ**: プレイヤーの実測そのままか、係数で弱めるか。
   私の推し: **実測そのまま。ただしHPは低め**（すぐ倒れるので「助っ人」の域を出ない）。
2. **G1の計測に使う武器/ダメージの扱い**: ゴーストの攻撃力は「プレイヤーの現在装備」を
   借りるか、固定値か。私の推し: **現在装備を借りる**（強さがラン進行と釣り合う）。

---

## 4. 参照

- 既存のボット関連テスト: `playtestBot.test.ts` / `botSkill.test.ts` / `botObjective.test.ts` /
  `botEngagement.test.ts` / `botTelemetry.test.ts` / `botUpgradePolicy.test.ts` / `store/playtest.test.ts`
- 実プレイヤー計測の前例: `src/utils/komaLog.ts`（記録専用・既定無効・`?komalog=1`）
- ヘイトの前例: `resolveEnemyTarget`（`summons` を敵のターゲット候補に入れる）
