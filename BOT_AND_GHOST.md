# 実機テストAIの強化 → プレイヤーゴースト助っ人（設計・社長相談 v0.25.2431）

社長の要望（原文の要点）:
1. **実機テスト用AIをかなり強化したい。ちゃんとプレイヤーとして振る舞うように仕上げたい。**
2. 最終的には **要所（ボス戦・ピーク・ゲート）でだけ現れて味方するAI** として使う。
3. そのために **プレイヤーの動きの傾向を「数値として」取って**（機械学習ではない）近い行動をさせ、
   **ゴーストとして助っ人に入る**。ヘイトも取る。カウンター頻度・好きな距離感・近接の傾向など。

---

## 0. 結論（先に）

**この3つは別々の仕事に見えて、実は「1つの層」を作れば全部つながる。**
その層とは **「プレイヤーの実測値を、ボットの既存ノブと同じ形で出す」** こと。

理由は下の §1 のとおりで、**ボット側の受け口はすでに存在している**。足りないのは
「実プレイヤーを測って、その受け口に流し込む配管」だけ。

---

## 1. 現状の棚卸し（既にあるもの）

| ファイル | 行数 | 役割 |
|---|---|---|
| `src/utils/playtestBot.ts` | 639 | 入力合成の本体。**ペルソナ7種**（standard/kiter/stationary/boar/wanderer/rusher/scavenger） |
| `src/utils/botSkill.ts` | 374 | **腕前の段階**（novice/casual/skilled/master）。ペルソナと**直交** |
| `src/utils/botObjective.ts` | 323 | 目的地の選択（POI/回収/前進） |
| `src/utils/botEngagement.ts` | 106 | 交戦の可否 |
| `src/utils/botTelemetry.ts` | 100 | 計測（サブ発動回数 / **ダメージのチャネル別**(gun/melee/other) / フィニッシュ数 / 近接の振り回数・命中数） |

### ★ここが重要
`botSkill.ts` の `BotSkillProfile` は **既に「人間らしさの数値」そのもの**になっている:

- `reactionMs` — 脅威検知から行動までの遅延（人間の反応速度）
- `counterChance` — 検知1回あたりのカウンター試行確率（人間の見逃し）
- `dodge: 'none' | 'projectile' | 'aoe' | 'all'` — どこまで避けるか
- `targeting: 'nearest' | 'weakest' | 'threat' | 'optimal'` — 標的の選び方
- `surroundCount` — 「囲まれた」と判断する敵数

そして `botTelemetry.damageDealt{gun, melee, other}` は **社長の言う「近接の傾向」を既に測っている**。

つまり **「プレイヤーの傾向 → 数値 → ボットの挙動」の受け口はもう空いている。**

---

## 2. 段階計画（各段階が単独で役に立つ形にする）

### 段階1: プレイヤー実測層 `playerTraits.ts`（**最初にやるべき**）

**プレイヤーの1ランを測り、`BotSkillProfile` と同じ形の数値に落とす。**

| プレイヤーから測る | 出力するノブ | 測り方（合流点） |
|---|---|---|
| カウンター成功/試行、成立までの時間 | `reactionMs` / `counterChance` | `lastCounterSuccessTime` / カウンター窓の開閉 |
| 敵との距離のヒストグラム（最頻値） | `preferredDist`（**新設**） | 毎tick、最近接敵との距離をバケットに加算 |
| 近接 vs 銃のダメージ比 | `meleeBias`（**新設**） | **既存 `botTelemetry.damageDealt` をそのまま使う** |
| 被弾数 / 予告に入った回数 | `dodge` | `komaLog` の `hitsTotal` + 予告判定（`isPlayerInAttackTelegraph` が既にある） |
| 倒した敵の選び方（最寄り/瀕死/危険） | `targeting` | キル時の敵の状態を分類して集計 |
| 静止時間の割合・平均移動量 | `mobility`（**新設**） | 入力の有無を積算 |

**掟（`komaLog` と同じ）**: この層は **記録するだけ。ゲームの判定には一切使わない。**
既定は無効で、通常プレイでは1バイトも溜めない。

**段階1だけで得られるもの**:
- **「ボットが人間らしくない」を推測ではなく数字で言える。**
  例: 実プレイヤーの `counterChance` が 0.3 なのにボットの standard が 0.65 なら、
  ボットは**人間の2倍カウンターしている**＝テスト結果が楽観的すぎる、と分かる。
- 今日の実測（v0.25.2409）で `kills 459 / 8.4分 / 被弾33` は取れているので、
  **ボットの同条件ランと直接比較できる**。

### 段階2: テストAIの強化（**段階1の結果で駆動する**）

社長の「ちゃんとプレイヤーとして振る舞う」を、**人力チューニングではなく実測合わせ**で達成する。

- `BotSkill` に **`'mirror'` を追加** — 段階1で測った実プロファイルをそのまま食う段階。
  `?bot=mirror` で「社長の分身」を走らせられる。
- 差分の大きいノブから潰す。現時点で**穴だと分かっているもの**:
  - **【調査で確定】ボットはボスの技を1つも避けない。** 回避の実装
    （`botSkill.ts` の `dodgeVector`）が見ているのは **`aiPhase === 'jump'`（汎用ジャンプ）と
    `aiPhase === 'charge'`（汎用突進）と弾と接触の4種だけ**。つまり:
    - 城ボスの `g-stomp-windup` / `g-jump-air` / `g-dash-charge` / 帯技全部（bite/slam/glide/
      wing/sweepbeam/reach）… **全部見えていない**
    - 連続ジャンプ `g-trijump-*`、グレンの遅延ダメージ `giantDelayedHits` … **見えていない**
    - トール/天使の `bossState` 系の技 … **見えていない**
    - おまけに `DodgeLevel` の `'aoe'` は **どの腕前段階にも割り当てられていない**
      （novice/casual='none' / skilled='projectile' / master='all'）＝型だけあって死んでいる。
    → **これが「ボットが人間らしくない」の最大の穴**。ボス戦のテスト結果は
      「赤を一切避けない人」の数字になっている＝**ボス関連のバランス判断の土台が無い**。
    → 直し方は簡単で、**赤い円/帯の位置は既に store 側に全部ある**
      （`gStompRadius` / `gJumpRadius` / `gTriJumpPts` / `giantDelayedHits[].capsule` /
      `aiFromX/aiTargetX`）。**新規の判定を発明せず**、描画が読んでいるのと同じフィールドを
      見て「赤の中に居たら外へ出る」を足すだけ。
  - リロード・弾切れの管理、サブウェポンの使いどころ。
  - 移動の質（人間は最短直線では動かない）。

### 段階3: ゴースト助っ人（本命）

**同じボットを、味方エンティティとして走らせる。**

- **出現条件**: ボス交戦中（`bossEngagedNow` が**既にある**）/ ピーク（コマ `peak`）/ ゲート
  （`activeGateRef`）。**3つとも既存の判定がそのまま使える。**
- **中身**: 段階1のプロファイルで駆動する `playtestBot`。プレイヤーの分身なので
  「あなたらしい動き」になる（＝ゴーストの意味）。
- **ヘイト**: 既に**召喚ユニット（`summons`）が敵のターゲット候補に入る仕組みがある**
  （`resolveEnemyTarget` / `ALCHEMY_AGGRO_RANGE`）。ゴーストをその枠に乗せれば
  ヘイト機構は**新規実装ゼロ**。
- 保存: 端末に1ラン分のプロファイル（数十バイトの数値）。`tutorialArchive` と同じ localStorage 作法。

---

## 2.5 設計の確定（社長との話し合い v0.25.2433）

### 前提の確定（社長）
- **狙い**: エルデンリングのように「自分の行動パターンを保存でき、それがピンチの人のところへ
  やってきて助けてくれる」快感。オンラインは未来。**まずはオフラインで自分の過去プレイ傾向を
  再現する助っ人スキル**として成立させ、実機テストにも使う。
- 同時1体 / ヘイトは `resolveEnemyTarget` の summons 枠流用 / 呼び出し口は装備スキル枠 /
  ソウル系の「壁の前だけ召喚→撃破/死亡で解散」の区切り。

### Q. ピークをやめて「ボス戦のみ」にすると再現度が上がるか？ → **上がる。推奨**
1. **文脈が一致するから**。ボス戦は1対1の構造化された戦い(予告を読む/カウンター/間合い管理)。
   そこで測った傾向(カウンター頻度・好きな距離・近接率)は、**そのままボス戦で再現される**。
   ピーク(群れ戦)の支配的な行動は範囲処理と群れキャイトで、ボス戦の傾向とは別物。
   混ぜて測ると**プロファイルが薄まる**し、群れ用の傾向をボス戦で再現しても「らしくない」。
2. **測定も切り出せるから**。`bossEngagedNow`(実装済み)でボス交戦区間だけを切り出して測れる
   =データが綺麗。ピークの雑多な行動が混入しない。
3. **ランク査定を汚さないから(技術的に重要)**。昇格は「10秒窓の撃破数」で数えるが、ピークに
   ゴーストが乱入すると**ゴーストのキルがプレイヤーの昇格に混ざる**。ボス戦中は昇格が凍結済み
   (v0.25.2413)なので、**ボス戦のみ=ペーシング系への影響が構造的にゼロ**。
4. ソウル系の召喚サイン(ボス扉の前)とも一致=ファンタジーとして自然。
→ **裁定: 出現はボス戦のみ。ピーク/ゲートは対象から外す**(ゲートはボス戦を含むので実質カバーされる)。

### 未決1: AI本体の作り方 → **薄い専用ドライバ `ghostDriver` を新設し、純関数部品を流用**
- 前提の訂正: `playtestBot` は「プレイヤーを狙う敵」ではなく**「プレイヤーを操縦する層」**
  (InputStateを合成する)。つまり**標的選択・回避・カウンター反応は向きの反転が不要**で、
  そのまま味方に使える。反転が要るのは「何のために動くか」だけ。
- **流用する純関数**: `pickTarget`(標的) / `dodgeVector`+`telegraphDodge`(回避・v0.25.2432で
  ボスの予告も見える) / `COUNTER_REACTION_PROFILES` 相当の反応抽選 / 間合い管理。
- **捨てるもの**: `botObjective`(POI/回収/前進)は丸ごと不要。ゴーストは**戦闘だけ**する。
- **新設するもの**: ①プレイヤー追従(リーシュ: 一定距離を超えたら瞬間的に追いつく=霊体なので
  ワープが世界観的に許される) ②Summon互換のエンティティ(位置/速度/HP)を毎tick駆動する薄い層。
- 置き場所: `src/utils/ghostDriver.ts`(純関数)+ useGameLoop の summons 更新部に1ブロック。

### 未決2: ペルソナの抽出方法 → **連続パラメータ(5〜7個)。分類はしない**
- 「6ペルソナのどれかに分類」は**実は軽くない上に忠実でもない**。既存ペルソナはデバッグ用の
  極端な原型(棒立ち/猪/放浪)で、実プレイヤーの中間的な振る舞いを表現できない。
- 測るのは既に決めた小さなベクトル: `reactionMs / counterChance / preferredDist / meleeBias /
  mobility (+dodge傾向)`。**ヒストグラムと平均を取るだけ**なので実装コストは分類と同等以下。
  ゴースト側は数値をそのまま食う=「近い行動」が最も直接に出る。
- 表示用の「あなたの型: 剣士型」等は**ベクトルから後付けで分類**すれば良い(見せ方だけの問題)。

### 未決3: 発動方式 → **スキル装備で自動発動**
- ボス戦のみ、と決めたので発動条件が一意(`bossEngagedNow` の立ち上がり=召喚、
  ボス撃破 or ゴースト死亡=解散)。**ボタン/ゲージのUIが丸ごと不要**。
- 既存スキル枠の作りにそのまま乗る。オンライン化した時も「ピンチの所へ勝手に来てくれる」
  ファンタジーは自動発動の方が合う(呼ぶのではなく、来る)。

### 未決4: 見た目 → **敵/味方の actor 描画系 + プレイヤー基本テクスチャの青白tint半透明**
- プレイヤー描画パイプラインは入力駆動の特殊系(武器レイヤー/刀/スケボー…)で、流用は重い。
- **ゴースト=青白い霊体**という見せ方にすれば(エルデンリングの霊体もそう)、**装備の見た目
  再現をv1で丸ごと回避できる**——霊体だから装備が見えなくても世界観として自然。
- 実装: summons が既に使っている actor プール描画に「プレイヤーの基本絵+青白tint+α0.7」で乗せる。
  新規描画経路ゼロ。装備見た目の再現は**やるとしてもv2**(オンライン化で他人の装備を見せたくなった時)。
- **→ この「はりぼて回避」設計は社長指示v0.25.2475で上書き**(「はりぼてをやめたい。ちゃんとプレイヤーと
  まったく同じ描画に乗せてください」): 歩きコマ(playerTextureName共有)/近接ナイフ振り3コマ+装備近接の実絵/
  発砲反動+青白銃口フラッシュを守護霊にも実装(青白tint+α0.7の霊体の見た目は維持・全て視覚のみ=判定/挙動/
  ダメージ不変。走り/スケボー/武将立ち絵はスコープ外のまま)。

### 未決5: オンライン → **ローカル完結で設計するが、プロファイルを「将来の通信ペイロード」として作る**
- プロファイルは**バージョン番号つきの小さなJSON**(数値だけ・目標200バイト以下)にし、
  localStorage(`tutorialArchive` と同じ作法)へ保存。**このスキーマがそのまま将来のアップロード形式**
  =オンライン化の時にAI設計へ手を入れずに済む。
- 保存は直近Nランの移動平均(1ランの事故で人格が変わらない)。
- v0.25.2477: プロファイルへ `srcName?`(計測時のプレイヤー名・playerName.tsの台帳・欠損可)を追加。
  守護霊の頭上名はこれを表示(オンラインで他人のゴーストが来た時に「その人の名前」が出る構造)。
  自分のプロファイル由来(Summon.ghostIsOwn=true・現状は常時)には「(自分)」を添える。

### 実装順(この設計での段階の再定義)
1. **G1: 計測** `playerTraits.ts` — ボス交戦区間だけを測ってプロファイルJSONを吐く(記録専用・既定無効)。
2. **G2: ゴースト本体** `ghostDriver.ts` + summons統合 + 青白描画 — `?ghost=1` でデバッグ召喚。
   **この時点で実機テストAIとしても使える**(プロファイルを差し替えれば任意の腕前の分身になる)。
3. **G3: スキル化** — 装備スキル枠に追加(ガチャ入手)。ボス戦で自動召喚。
4. (未来) G4: オンライン — プロファイルJSONをBaaSへ。設計変更なし。

## 2.6 抽出の詳細設計（社長リクエスト「2の抽出を詰めたい」v0.25.2434）

### 測る場所と方式
- **ボス交戦区間のみ**(`bossEngagedNow` の立ち上がり〜解除)。
- **スナップショット差分方式**: 交戦開始時に集計のコピーを取り、解除時に差分を確定する。
  CLAUDE.md 実装精度の規律3「**集計のアンカーはディープコピー**」を厳守
  (生きた参照を保存すると差分が常に0になる——v0.25.1343の実バグの型)。

### 各ノブの定義（測り方を1つずつ確定）
| ノブ | 定義 | 測り方(合流点) |
|---|---|---|
| `counterChance` | 機会に対する成立率 | **機会** = ボスがカウンター可能状態(windup/recover)の間にプレイヤーが近接圏(MELEE_RADIUS×2)へ入った回数。**成立** = その窓中に counterHit 系(dashParried/thorCounterHit/各<boss>CounterHit)が発火した回数。成立/機会。 |
| `reactionMs` | 読みの速さ | windup開始(aiPhase/bossState遷移のエッジ)→カウンター成立までの経過msの**中央値**。clamp[100..800]。 |
| `preferredDist` | 好きな間合い | 交戦中毎tick、**ボスの判定帯への最近点距離**(enemyMeleeDistと同式)を50px刻み16バケットに加算→**中央値**。 |
| `meleeBias` | 近接の傾向 | `botTelemetry.damageDealt` の交戦区間差分から melee/(gun+melee)。otherは除外(サブ武器はゴーストが持たない)。 |
| `mobility` | 動き続ける癖 | 交戦中に移動入力があった tick の割合(0..1)。 |
| `hitsPerMin` | 避けの上手さ(逆指標) | 交戦区間の被弾回数を分あたりに正規化(komaLog の被弾検知と同式)。 |

### プロファイル→ゴーストの写像
- `reactionMs`/`counterChance` → カウンター反応抽選へ**そのまま**。
- `preferredDist` → 間合い管理の目標距離(engageDist相当)。
- `meleeBias` → 「近接へ行くか撃つか」の抽選比。
- `mobility` → 停止癖(低いゴーストは足が止まる=下手さも再現される。**上手さだけ写さないのが「らしさ」**)。
- `hitsPerMin` → `dodgeStrength`(0..1)へ逆写像(被弾が多い人のゴーストは避けるのも下手)。
  dodgeレベル自体は 'aoe' 固定で、強弱は dodgeStrength だけで表現(v0.25.2432 の telegraphDodge が土台)。

### 集計と保存
- **EMA(指数移動平均・α=0.3)** を採用。「直近Nランの移動平均」の裁定を満たしつつ、
  **保存が1組で済む**(N個のラン履歴を持たなくてよい)。3〜4ランで新しい癖に馴染む。
- スキーマ(そのまま将来の通信ペイロード・実測~120B):
  `{"v":1,"runs":7,"reactionMs":320,"counterChance":0.42,"preferredDist":180,"meleeBias":0.35,"mobility":0.71,"hitsPerMin":2.1}`
- 保存先: localStorage(`tutorialArchive` と同じ作法)。交戦サンプルが短すぎるラン
  (合計30秒未満)は**EMAに混ぜない**(ノイズで人格が動くのを防ぐ)。
- **保存の保留化(v0.25.2476 社長裁定「リザルトで守護霊化するか選べるように」)**: セッション確定と
  サブ様式のラン集計は即保存せず**ラン内の保留バッファ**に積み、リザルトを閉じる操作(OK/もう一度プレイ/
  メニューに戻る)で **commit(既定・従来と同順のEMA=結果は旧実装とビット一致)** か、リザルトの
  チェック「今回のプレイを守護霊に反映しない」で**全破棄**する。リザルトを経由しない終了(ブラウザ閉じ等)
  は破棄と同じ(安全側)。§2.7 制約1(守護霊装備/`?ghost=1`の計測停止)は従来どおり保留より手前で効く。

## 2.7 ゴースト使用の制約(社長指示 v0.25.2435「このスキルを使ってるプレイでは…」)

### 制約1: ゴーストスキル装備中は、そのプレイヤーのAI(プロファイル)を生成しない
- **社長指示そのまま+理由が測定の純度として完璧**: 計測はボス交戦区間のみ(§2.6)で、ゴーストは
  まさにボス交戦に出てくる。つまり**ゴースト同伴のボス戦は全て「2人での戦い方」に汚染されている**
  (ヘイトが割れる/自分は撃ち専に回る等)。それを記録すると、次のゴーストは「ゴーストが居る前提の
  自分」の複製になり、世代を重ねるほど劣化コピーになる(録音のダビング劣化と同じ構造)。
- **実装ルール**: **スキルを装備しているランでは計測を丸ごと無効**(装備チェック1つ。
  「召喚が起きた区間だけ除外」のような細かい分岐にしない——装備中のボス戦は必ず召喚が起きるので同値、
  かつ単純な方がバグらない)。
- 副次効果: **プロファイルを育てたければソロでボスと戦うしかない**=「自分を鍛える→分身が強くなる」
  というループが自然に生まれる。

### 制約2: スコア半減 【社長確定 v0.25.2440「発動したランだけでいいよ」】
- **対象(確定)**: そのランで**一度でも召喚が発動したら**、ラン全体のスコアを1/2(倍率は叩き台・社長調整)。
  - 「装備しただけで半減」にしない理由: ボスに会わなかったラン(=助けを一切受けていない)まで
    罰すると、外し忘れが理不尽になる。**発動=助けを受けた事実**に課金する。
  - 発動した瞬間にHUDへ明示(例: バナー「ゴースト同伴: スコア1/2」)+リザルトにも明記。
    **黙って半分にしない**(知らずに損した、が一番不信を生む)。
- **狙い(私の理解)**: エルデンリングの遺灰と同じ「楽になる代わりに誇りを差し出す」交換。
  スコアランキング(将来オンライン)を汚染しない意味でも必須の弁。
- ボスHP1.6倍(§3裁定)と合わせて、ゴーストは**「強いが、タダではない」**が二重に成立する:
  戦闘は重くなり(HP1.6倍)、報酬も軽くなる(スコア1/2)。それでも使いたい時に使う保険、という位置。

## 2.8 ゴースト強化第2弾の確定仕様(社長裁定 v0.25.2444)

### G2.5: ヘイト(社長発案「ダメージ蓄積が多い方を狙う」+揺れ対策)
- **ヘイト値 = 直近6秒の与ダメージ**(1秒バケツ×6・rankAssessorのSoftenStateと同じ軽量パターン)。
  累計にしない理由: ゴーストはスキル倍率を持たずDPSで恒常的に負けるため、累計では一生タンクにならない。
- **評価は技の狙いロック時のみ**(全ボスの技は溜め開始で狙いをロックする=掟W4。毎フレーム評価しない
  ので技の途中で狙いが揺れることは構造的に無い)。
- **現ターゲットに×1.3の粘着**(僅差の入れ替わり=パタパタを防ぐ)。両者ダメージゼロ(開幕)は近い方。
- 「殴っている方が狙われる」の1行で説明でき、撃つのを止めて隠れる=ヘイトを渡す、がプレイ選択になる。
- 適用対象: 攻撃対象をプレイヤーに直書きしている全ボス(城ボス/idol/天使6体)の**狙いロック箇所**。
  トール/通常敵は既存の resolveEnemyTarget が既にゴーストを見るので不変。

### G2.6: サブウェポンのオーナー抽象化(社長方針「ホワイトリストは避けたい」)
- 発動の入口に owner(座標・向き・バフ/回復の受け手)を渡す形へリファクタ。
  **既定=プレイヤーで挙動不変**の段階を先に踏み、その後ゴーストが自分を owner として渡す。
- 意味は「効果はオーナーに対して解決する」の1行ルールで全25種が除外なしで定義される
  (自己バフ→ゴースト自身に/世界に出る効果はそのまま世界に)。
- 使う頻度はプロファイル実測(botTelemetry.subUses 由来の subUsesPerMin ノブを追加)。
- **CD正規化を同梱**: 手動再実装3本(dog/sensor-mine/support-sniper)を setSubWeaponCooldown へ寄せる。

### 「1つの財布・1つの薬棚」原則(社長確定 v0.25.2444)
> **プレイヤーとゴーストは1つの財布・1つの薬棚。アイテムはどちらが拾っても、効果は両方に出る。**
- 資源系(弾薬/ゴールド/スクラップ/経験値/武器): インベントリは元々プレイヤーに1つ=誰が拾っても
  プレイヤーに入る(取り合いは構造的に発生しない)。
- 即時効果系(回復/救急鞄/クイックマガジン等): **場に居る両方の体に効く**。
- 動機(社長): 「相手の犬が取りまくってイライラする」を根元から断つ。将来のオンラインでも
  この原則ごと持ち込む=横取り体験を作らない。
- ゴースト側に個別インベントリを持たせない=帳簿は1つのまま(実装も簡単になる)。

## 3. ★未決事項 → **全て解消(社長裁定v0.25.2434)**
1. **ゴーストの強さ = 実測そのまま**(でないと装備構成の意味がない)。
   **ただしボスのHPは1.6倍**(`GHOST_BOSS_HP_MULT = 1.6`)。
   - 適用: **ゴースト召喚が成立した瞬間に1回だけ** health/maxHealth を×1.6(割合は保存)。
   - **ゴーストが死んでも戻さない**(エルデンリング準拠=召喚のコストはボスHPで前払い。
     戻すと「召喚→即死させて損切り」の抜け道ができる)。
2. **攻撃力 = プレイヤーの現在装備を借りる**(ビルドの意味を活かす)。
   - 銃: 装備中の銃の damage/interval で撃つ。**弾薬は消費しない**(霊体。プレイヤーの残弾と分離)。
   - 近接: 装備近接の damage でカウンター/スイング。

---

## 4. 参照

- 既存のボット関連テスト: `playtestBot.test.ts` / `botSkill.test.ts` / `botObjective.test.ts` /
  `botEngagement.test.ts` / `botTelemetry.test.ts` / `botUpgradePolicy.test.ts` / `store/playtest.test.ts`
- 実プレイヤー計測の前例: `src/utils/komaLog.ts`（記録専用・既定無効・`?komalog=1`）
- ヘイトの前例: `resolveEnemyTarget`（`summons` を敵のターゲット候補に入れる）

## 5. 実装結果(G1+G2完了・v0.25.2441・実装サブエージェント)

詳細は DEVELOPMENT_LOG.md の該当エントリ(v0.25.2441)を正本とする。ここは設計チャットが次に
読む時のための要約。

- G1(`src/utils/playerTraits.ts`)/G2(`src/utils/ghostDriver.ts`)とも実装・ユニットテスト済み
  (typecheck/lint エラー0)。`SummonKind` の新値は **`'ghost-ally'`**(`EnemyType` に既存の
  `'ghost'` があったため衝突回避で改名。URLパラメータ `?ghost=1` はユーザー向け名称のまま維持)。
- **実装中に設計前提が崩れていた箇所が2つ見つかった(社長裁定が必要)**:
  1. **ヘイトが「新規実装ゼロ」で全ボスに乗る、は成立しない**。`resolveEnemyTarget` を実際に
     経由するのは thor と通常敵だけ。giantbat・idol・angelBossTick系6体(miguel/jibril/rafi/uri/
     suriel/acrasiel)は攻撃対象をプレイヤーへ直接ハードコードしており、ゴーストへは自動で
     ヘイトが乗らない。→ 主戦場である城ボス(giantbat)戦で「ボスがゴーストを狙わない」ことになる。
  2. **ゴーストの被弾経路は汎用接触ダメージ(`checkEnemySummonCollisions`)のみ**。ボス固有の
     特殊技(突進/薙ぎ払い/ビーム等)の専用当たり判定はプレイヤーだけを見ており、ゴーストには
     当たらない。
  - この2つを本バッチのスコープで直すには11ボスの個別コードを触る必要があり、CLAUDE.md
    「Aを直してと言われたらAだけ直す」に反するため見送った(★未決として実装のみ完了)。
    次の一手は設計チャットの判断が要る: (a) 許容して実機確認へ進む(ヘイトが乗らなくても
    「実測プロファイルでボスを殴る」体験自体は成立する) / (b) 主要ボス(最低でもgiantbat)だけ
    ヘイト対応を追加するバッチを別途発注する、のどちらか。
- その他の★未決(reactionMs判定の概算/ゴースト近接の簡易化/スキル倍率を乗せない設計/
  defaultGhostProfileの叩き台数値)は DEVELOPMENT_LOG.md 参照。
  ※うち「ゴースト近接の簡易化」のカウンター部分は **v0.25.2480で解消**(社長裁定「1」=カウンターは
  per-bossハンドラ合流で本物化。§8参照。通常近接スイングの簡易形は従来どおり)。

## 6. G2.6 実装結果と★未決(v0.25.2449・実装サブエージェント)

詳細は DEVELOPMENT_LOG.md の該当エントリ(v0.25.2449)を正本とする。

### 実装済み
- **オーナー抽象化**: `src/utils/subWeaponOwner.ts`(`SubWeaponOwner`型+`playerAsOwner`/`ghostAsOwner`)。
  発動入口で座標・向き・受け手をオーナーに対して解決する(既定=プレイヤー=挙動完全不変)。
- **ゴーストが実際に使える種(6種)**: heavy-grenade / marksman-trap / decoy / shield / turret / fire-knife
  (=入口を通すだけで自然に動く種)。仕組みは「予約制」: ゴーストは交戦中、subUsesPerMin の間隔で
  「次のサブ発動1回」を予約(`Summon.ghostSubClaim`)し、共有CDが明けた瞬間の自動発動がオーナー=
  ゴーストで出る(CD・資源は共有=「1つの財布」。上記6種は元々資源を使わない)。
- **subUsesPerMin ノブ**: playerTraits の7個目のノブ(botTelemetry.subUses の区間差分/分・EMA)。
  旧保存(6ノブ)は欠損を既定値2で埋める後方互換。ghostDriver 側は `shouldGhostClaimSub` が消費する。
- **CD正規化**: オーバークロック→タイムキーパーの適用を共有純関数 `src/utils/subCooldown.ts` へ寄せた
  (合流点 setSubWeaponCooldown / sensor-mine / support-sniper の3箇所が同じ関数を通る。挙動1ms不変)。
- **1つの薬棚**: 回復(health)の拾得は collectPickup の1箇所へ合流している(プレイヤー接触/ドッグ回収とも)
  ので、そこでゴースト(ghost-ally)にも同割合(自身の最大HPの30%)を回復。ゴースト不在時は完全不変。
  クイックマガジンは「両方の体に効く」の対象としては空振り(ゴーストの銃はマガジン/リロード概念を
  持たない=効かせる中身が無い)ため配線なし。**資源系(弾薬/ゴールド/スクラップ/経験値/武器)は
  コード確認のみ**: collectPickup で全てプレイヤーの帳簿1つへ入り、取り合いは構造的に無い(変更なし)。

### v0.25.2473 実働化+青白化(社長指示「守護霊のサブウェポンは実装されないなら、してほしい。全てプレイヤーと同じく青白くして」)
- **診断結果(ヘッドレスプローブ・使い捨て)**: 予約方式(ghostSubClaim)は**発火していた**(120秒のボス戦で
  ゴースト発動3回/30秒間隔)。実機で観測されなかった原因は「見えない」こと:
  ①絵がプレイヤー発動と完全に同一(tint/マーカーなし) ②「1つの財布」=発動総数は増えず、プレイヤーの
  自動発動の一部(既定プロファイルで約1/7)がゴースト位置に置き換わるだけ ③そのゴーストはプレイヤーの
  近くに居る(プローブ実測で約30px差)——つまり未配線ではなく**無音の再帰属**だった。
- **頻度の床(挙動変更・社長指示の範囲内)**: `GHOST_SUB_USE_MAX_INTERVAL_MS = 25_000`(ghostDriver.ts)。
  実効予約間隔 = min(60000/subUsesPerMin, 25秒)。subUsesPerMin<=0(使わない人)でも交戦中は床が生きる。
  shouldGhostClaimSub は「紐付きボス生存中」しか呼ばれないので床は自然にボス交戦中限定。
- **照準**: ゴースト発動の狙い先=紐付きボス(`pickSubAimTarget`=subWeaponOwner.tsの純関数。
  プレイヤー発動は従来と同一手順の最寄り非リーパー=1bit不変)。対象は狙いを持つ2種(手榴弾/発火ナイフ)。
- **青白化(視覚のみ)**: 発動時に生成物へ `Projectile.ownerGhost: true`(視覚専用マーカー)を付け、
  レンダラが GHOST_ALLY_TINT(0x9fd8ff)/GHOST_ALLY_ALPHA(0.7)で描く。対象=手榴弾(ボマー子含む)/
  トラップ/デコイ/シールド/タレット(弾・ランチャー弾含む)/発火ナイフの本体絵+爆発/捕獲FXの色差し替え。
  プレイヤー起因のサブは1pxも変えない。判定・ダメージ・CD・資源(1つの財布)は不変。

### ★未決1: ゴースト未対応リスト(黙って除外しない、の明記)
入口はオーナー形に抽象化済みだが、以下は**個別の特殊配線が必要**なため現状プレイヤー固定。
対応するかは社長判断(スキル倍率・被弾経路の既存★未決と同様、次バッチ以降の候補)。
- **striker-quick-mag**: マガジンは「プレイヤーが拾いに行く」前提の設計(ドッグにも拾わせない=
  社長指示v0.25.2409)。ゴースト位置から投げると取りに行けない置き去りが起きるため保留。
- **dog**: フェッチ状態機械が帰還先/回収対象の選定でプレイヤー座標を毎フレーム直読みしている。
- **molotov**: 「本人が移動中のみ足元へ置く」設計で、発動条件と設置位置が本人の移動に結合。
- **support-sniper**: 専用タイマーが「プレイヤーの移動中のみ」進む+出現点の画面縁計算がプレイヤー基準。
- **homing**: ロック蓄積=指を付けている間/発射=指離し、というプレイヤーのタッチ入力そのもの。
- **wire-anchor**: 効果=オーナーの体の高速移動。ゴースト側の移動系(ghostDriver)への特殊配線が必要。
- **drone-boomerang / sensor-mine / flare-gun / junk-weapon / shadow-clone**: 発動入口が近接スイング
  (gameStore.triggerCounter)=プレイヤー入力。ゴーストの近接スイングに相乗りさせる配線が必要。
- **katana / murasame / whip**: サブCDではなくカウンター窓経済(counterWindowEnd/counterCooldownEnd)で
  動く=そもそも「CDが明けたら使う」というゴーストの意思決定の形に合わない。
- **alchemy / shijin / sage-stone / striker-hunting / first-aid-kit**: CD概念なし(常駐召喚/リズム/
  パッシブ/静止チャージ/1ラン使い切り)。同上。

### ★未決2: CD正規化で「完全な合流」を見送った3種(理由)
2スキルの適用は共有純関数へ寄せた(=ドリフトは構造的に起きない)が、`subWeaponCooldowns` への
一本化そのものは以下の理由で不可能(挙動を1msも変えない制約下)。
- **sensor-mine**: チャージ制(個別チャージが設置から10秒後に各自再準備)。単一のreadyAtでは
  「チャージN個が別々の時刻に戻る」を表現できない。
- **support-sniper**: 残りms型の専用タイマーで「移動中のみ」進む。gameTime基準のreadyAtに変換すると
  停止中もCDが進む=挙動変更になる。
- **dog**: dog-run倍率の先乗りは「フェッチ完了後の再出動CD」だけに掛かる(空振りリトライ
  DOG_EMPTY_RETRY_MS には掛からない)。倍率を合流点内へ移すと空振りリトライにも掛かって挙動が変わる。

### ★未決3: 「1つの薬棚」の適用範囲の質問(軽微)
ショップの**メドキット購入**(buyShopItem 'medkit'・最大HP30%回復)は「拾うアイテム」ではなく購入品の
ため、今回はゴーストへ効かせていない(仕様の「即時効果系=拾得アイテム」の読みに従い保守側)。
ボス交戦中に武器庫を開ける状況は稀だが、「両方の体に効く」へ含めるかは社長裁定を仰ぐ。

## 7. G3 実装結果と★未決(v0.25.2452・実装サブエージェント)

詳細は DEVELOPMENT_LOG.md の該当エントリ(v0.25.2452)を正本とする。

### 実装済み(社長指示「守護霊スキルは最初から解禁しとこうか」)
- **新スキル `guardian-spirit`(守護霊・super・Lv1固定)**: SKILLS/SKILL_KEYS へ追加。
  ガチャからは出ない(GACHA_EXCLUDED_SKILLS)。**最初から所持**: 新規/既存セーブとも読み込み時に
  ownedSkills へ無ければ追加するマイグレーション(`ensureDefaultOwnedSkills`・campaign.ts。
  §2.5実装順の旧記述「ガチャ入手」はこの社長指示で上書き)。
- **召喚ゲート**: `ghostRunEnabled(ghostDebugEnabled, skills)`(ghostDriver.ts・純関数)=
  `?ghost=1`(開発用・従来どおり) OR 守護霊装備。directorTick の召喚ゲートがこれを通る。
  挙動は既存のまま(1体/ボスHP×1.6/解散条件/ヘイトG2.5/サブ予約G2.6とも不変)。
- **スコア1/2**: ラン中に一度でも召喚が成立したら store の `ghostSummonedThisRun` を打刻
  (resetGameでリセット)。合流点 `calculateResultScore`(resultScoring.ts)の**1箇所だけ**で
  totalScore×`GHOST_SCORE_MULT`(0.5)。リザルト(両レイアウト)に「守護霊が共闘: スコア半分」を表示。
- **計測停止(§2.7 制約1の本則)**: 守護霊装備ラン/`?ghost=1`ランは playerTraits の計測を丸ごと停止
  (`ghostRunActive` 入力・召喚ゲートと同じ判定を共用)。
- ラン途中の装備変化の調査: player.skills をラン中に書き換える経路は poi 専用スキル付与
  (v0.25.2451)のみで、guardian-spirit が途中で増減する経路は無い=「ラン開始時に装備していたか」と
  毎tick判定は同値(★未決にせず記録のみ)。

### ★未決4 → **解決(社長裁定v0.25.2455返信「ゴールドそのままでいいよ」)**: ×0.5はtotalScoreのみ・換金は満額のまま
スコアには2系統ある(resultScoring.ts): **totalScore**(ハイスコア表示/順位)と **goldScore/goldEarned**
(換金)。§2.7の狙いが「誇りを差し出す/スコアランキングを汚染しない」なので、×0.5は
**totalScoreのみ**(=リザルトのSCORE表示とステージ別ハイスコア保存の両方に一貫して掛かる)に適用し、
**ゴールド換金には掛けていない**(経済への波及は指示なしに変えない=保守側)。ゴールドも半減すべきなら
社長裁定で `calculateResultScore` の同じ1箇所に足すだけ。

### ★未決5(軽微): `?ghost=1`(開発用)のランもスコア半分になる
仕様の文言「一度でも実際に召喚されたら」どおり一律適用した(デバッグフラグの免除は仕様に無いため
作っていない)。開発検証でスコアを見たい場面があれば免除の裁定を仰ぐ。

## 2.9 G4: 計測の高解像度化「より本人に近づく物差し」(社長指示 v0.25.2452)

> 社長: 「ai学習に、その技への反応も入れたい。その技に対してどれくらいのカウンター決めてるのか、
> 全て避けてるのか、苦手で食らってるのか」「距離の取り方と接近頻度、技への反応、サブウェポンの
> 使い方、立ち止まる頻度など、より本人に近づく要素をもっておき、物差しにすれば近づく気がする」
> 「シールドをバッシュでのダメージをメインにしてる人とか、アンカーの使い方がうまいとか」

**設計原則: 「行動そのもの」ではなく「コードに既にある結果の分岐」を数える。**
新しい判定を作らず、既存の分岐(カウンター成立箇所/被弾箇所/アンカーのスラム・プラント分岐/
シールドのバッシュイベント)に1行カウンタを置く。プロファイルは小さなJSONのまま保つ。

### (1) 技への反応表(per-move reactions)
- **単位=「暴露」**: ボスの技が1回解決するたび、プレイヤーがその技の狙い対象(ヘイトのロック対象)
  or 危険域内に居たなら1暴露と数える。結果は3分類・優先順位 **counter > hit > dodge**:
  - counter: その技の間にカウンター成立(成立7箇所=counterMaster.tsのリファンド呼び出し箇所と同じ)
  - hit: その技のダメージを食らった(技キーの付与が要る箇所は damageSourceMove タグを1行フック)
  - dodge: 暴露されたが無傷(上2つ以外の残り)
- 技ごとに `{n(暴露回数), counterRate, hitRate}` をEMAで保持(dodgeRate=1-両者)。
  **n<3 の技は既存グローバル6ノブへフォールバック**(初見の技で変な確信を持たせない)。
- 技キーの台帳は **G2.5のボス×技表(DEVELOPMENT_LOG v0.25.2448)を流用**(=「同じ動作を持つ全員に」の
  掟対応を最初から満たす)。
- **段階**: G4a=城ボス系(giantbat/グレン/EX)+トール。G4b=天使6体+idol+裏ボス3体。

### (2) 移動の追加ノブ(2つ)
- `stationaryFrac`: ボス交戦中の静止時間割合(立ち止まる癖)
- `approachPerMin`: 接近エピソード/分(距離が縮む向きに一定px以上詰めた回数)
- 既存の「好みの距離」ノブと合わせ、移動の個性=距離+接近性+静止癖の3軸になる。

### (3) サブウェポンの様式カウンタ(第1弾は2種)
- **wire-anchor**: スラム型(敵ヒット)/プラント型(空振り)の比率(分岐は実装済み=台帳§A-1の2行)
- **shield**: 設置1回あたりのバッシュ回数+バッシュ与ダメージの割合(「置いて守る人」vs「バッシュ主体」)
- 以降のサブは「結果の分岐が既にあるもの」から順次(地雷=起爆率/タレット=キル数/手榴弾=命中率…)。
- **計測と再現は別**: 計測は全対象で可。再現はゴーストが使えるサブ(G2.6対応6種)が前提。
  シールドのバッシュ再現は射程圏内、アンカーは未対応リスト入りのため再現は対応拡張後。

### (4) 再現(G4b・ゴースト側)
ゴーストはボスの溜め(aiPhase/bossState)から技キーを読み、その技の実測率でロール:
counterRate→構え / dodgeRate→離脱 / 残り→逃げずに食らいに行く(「苦手」の再現)。

### ★裁定待ち → **解消(社長裁定・2026-07-30・v0.25.2454返信への回答)**
- **ボスの特殊技をゴーストにも当てるか**(G2★未決2の解消)→ 社長裁定(verbatim):
  **「1:は当然当たらないと意味がない」**=ボスの技はゴーストに当てる。G4bで実装済み(下記実装結果)。
- G4a(計測のみ・記録専用・挙動完全不変)は裁定と独立に進められる。→ 実施済み(v0.25.2454)。

### G4a 実装結果(v0.25.2454・実装サブエージェント)— 計測のみ・挙動完全不変
詳細(技キー×フック箇所の全数表・検証)は DEVELOPMENT_LOG.md v0.25.2454 が正本。要約:
- **(1)技への反応表**: 判定中核(暴露/3分類counter>hit>dodge/EMA)は純関数 `src/utils/moveReaction.ts`
  (テスト18件)。エピソード=aiPhase/bossStateの技ファミリー単位。counter成立の通知はリファンド7箇所
  全部に `notifyMoveCounter()` を1行併設(⑤〜⑦はG4b対象ボス=表キー未定義で現状no-op)。被弾は
  `damagePlayer` の第7引数 `damageSourceMove?`(既定undefined=完全に従来どおり・hateSourceと同じ流儀)。
- **(2)移動2ノブ**: `stationaryFrac` / `approachPerMin` を playerTraits セッション(ボス交戦のみ・
  ghostRunActiveゲート・30秒未満破棄・EMA α=0.3)へ追加。
- **(3)サブ様式**: wire(スラム/プラント)+shield(設置/バッシュ/バッシュ与ダメ)をラン単位で集計し、
  `foldSubStyleTallies()`(resetGameの`resetBotTelemetry()`直前)でEMA混合。ゴーストランは丸ごと破棄。
- **(4)スキーマ**: `zombie-ghost-profile-v1` へ `stationaryFrac`/`approachPerMin`/`moveReactions`/
  `subStyles` を追加。旧保存は欠損を既定値で埋める後方互換(テストで固定)。
- **★未決: なし。** 仕様に明記の無い計測詳細は以下の実装メモで確定した(いずれも記録専用の解釈で、
  ゲーム挙動には影響しない。気に入らなければ数値/定義だけ差し替え可能):
  1. **サブ様式はボス交戦区間に限定しない**(発注書どおりの解釈。平時の使い方が様式のため)。
  2. **stationaryFrac は実座標の変位ベース**(移動速度<12px/sのtick割合)。入力ベースにすると
     既存 mobility の単なる補数(1-mobility)になり情報が増えないため。
  3. **approachPerMin はプレイヤー自身の移動による接近量だけを積む**(ボスの突進/ジャンプ/場外退避
     g-dive で距離が暴れても汚れない)。150px詰めて1エピソード/40px/s超の後退でリセット。
  4. **残響(linger)2000ms**: 技のフェーズ終了後も遅延起爆・飛翔中の咆哮弾の着弾/反射を技へ帰属させる
     窓。残響切れ後(例: グレン血溜まり床の数秒後の踏み直し)は数えない。
  5. **接触ダメージの技帰属は3フェーズのみ**(g-dash-charge/g-quad-charge/g-glide-active=体当たりが
     技のダメージであるもの)。溜め中や技なしの接触は従来どおりタグ無し。
  6. **counterの帰属は時間窓**(§2.9の文言「その技の間にカウンター成立」どおり。開いているエピソード
     全部、無ければ残響中のものへマーク)。
  7. **プロファイル未保存の端末ではサブ様式foldは保存しない**: ここで新規作成すると
     `loadPlayerProfile()!==null` になり、守護霊ランのゴーストが defaultGhostProfile(casual:
     counterChance0.65)ではなく SEED(0.5)で動く=挙動が変わるため(挙動不変を記録より優先)。

### G4b 実装結果(v0.25.2459・実装サブエージェント)— ①ボスの技をゴーストに当てる+②技への反応の再現
詳細(①の適用箇所の全数表・検証)は DEVELOPMENT_LOG.md v0.25.2459 が正本。要約:
- **①当てる(社長裁定「1:は当然当たらないと意味がない」の実装)**: 合流点で一括+個別3系統。
  1. **爆発/カプセルの合流点**(combatTick.applyPumpkinBlastDamage): ゴースト判定を追加し、
     **ボス(isEngageableBoss=BOT_AND_GHOST対象の正本テーブル)の爆発のみ**対象。G4aタグ付きの
     城ボス系全技+thor-jumpに加え、同じ配管のミーミル噛みつき/ヨルムンうねり/スカジ氷/idolパンチも
     自動カバー(発見分=報告表に明記)。パンプキン/lab-zombie-3の着地爆発(非ボス)は従来どおり対象外。
  2. **ボスの敵弾**(combatTick.applyEnemyProjectileHits): ownerTypeがボスの弾はゴーストにも命中
     (g-bolt咆哮弾・裏ボスのバースト/全方位・天使/idolの射撃)。**プレイヤー解決後の残弾のみ**を見る=
     プレイヤーへの判定・ダメージは1bit不変。命中した弾はプレイヤー被弾と同じく消える。
  3. **個別直撃系**: トール一閃/突き/払い+ミーミルレーザー(線分カプセル・共通ヘルパ
     applyGhostAllyCapsuleHit)/グレン血溜まり床(applyGlenFloorDamage内・1フレーム1ヒット)。
  - ダメージはプレイヤーと同じ量・同じ幾何(円/カプセル同式・当たり半径=summonの既存値)。HP減算/
    死亡→解散は既存damageSummon(i-frame=INVULN_MS・health<=0で除去)へ乗せるだけ=新しい死亡処理なし。
    ノックバック/シェイク/被弾音は付けない(既存summon被弾表現=青バースト+lastHitシェイクのみ)。
  - **接触系(g-dash/g-quad/g-glide体当たり)は既存の汎用接触**(checkEnemySummonCollisions)が
    G2から既にゴーストへ当てているため追加なし(二重定義しない)。
  - **未適用(発見済み・報告表に明記)**: 天使の直撃系(ミゲル払い/ウリ薙ぎ/スリィエル環/アクラシエル
    棘・槍/ジブリルのランタン火 等15箇所)=G4c(天使/idolの計測拡張)と同時にまとめるのが安い
    (①の掟では「含めてよい」=任意。合流点経由の技と弾は上記1・2で当たる)。
- **②技への反応の再現**(ghostDriver): 技キー導出は**moveReaction.moveKeyForEnemyをそのまま流用**
  (二重実装なし)。純関数 `rollGhostMoveReaction`(テスト9+統合6件)が技の立ち上がりで1回だけロール:
  r<counterRate→'counter'(既存カウンター試行を優先発動=必ず構える+間合いへ詰める) /
  r<counterRate+dodgeRate→'dodge'(従来の回避) / 残り→'tank'(この技に限り回避を抑制=①で実際に食らう)。
  n<3・キー未定義(天使等)は'fallback'=従来挙動(グローバルノブ・乱数消費順も同一)。ロールは
  技の解決(キー変化/null)かタイムアウト(10s安全弁)でリセット。持ち越しはSummonのフラット3フィールド
  (ghostMoveRollKey/Decision/At)。
- ★未決: なし(実装メモはDEVELOPMENT_LOG参照)。

## 8. ゴーストのカウンター本物化+被弾音+サブSE減衰(v0.25.2480・実装サブエージェント)

詳細(per-bossハンドラ合流の全数表・検証)は DEVELOPMENT_LOG.md v0.25.2480 が正本。要約:

### カウンター本物化(社長裁定「1」= DEVELOPMENT_LOG v0.25.2479 ★未決1の解消)
- **旧★未決「ゴースト近接の簡易化」(§5)のうちカウンター部分はこれで解消**(通常近接スイングの
  簡易化=毎回damageEnemyを通す形は従来どおり)。
- **請求(claim)方式**(新規 `src/utils/ghostCounter.ts`): counterスイング(窓+間合い成立)は請求を
  1件積むだけ。**per-bossハンドラ**(thorCounterHit/hiddenBossCounterHit/idolCounterHit=useGameLoop、
  angelCounterHit=angelBossTick、dashParried相当=combatTick.applyGhostBossParry)が「プレイヤーが
  カウンター可能な状態」の時だけ消費し、**プレイヤー成立と同じ機械的効果**(技の中断/怯み・後退遷移+
  確定クリ crit=true=bumpBossCrit 5クリ紫気絶に蓄積)を与える。判定はper-boss側の既存条件を使う=
  二重実装なし。
- **プレイヤー専用の副作用はghost分岐でスキップ**(計測notifyCounterHit/notifyMoveCounter・コンボ・
  無敵・counterCooldownEnd/counter-masterリファンド・lastCounterSuccessTime・triggerHitImpact・
  markMeleeSwingFx)。プレイヤーのシステム値は1bitも動かない。
- **ダメージ式**: プレイヤーのカウンター反撃と同式の借用装備版=`(借用銃damage ?? 12) × BOSS_CRIT_
  DAMAGE_MULT`(スキル倍率/equipBonusは乗せない=v0.25.2459方針の踏襲)。
- **演出解禁**: 成立時のみ青Counter!層+**金クリ層(リング/バースト/glow34)+headshot SE(距離減衰)**。
  スイング時の先出しCounter!は廃止(不成立の空振りに嘘を出さない)。ズーム/停止/スローは引き続き禁止
  (GHOST_FX_SHAKE_ENABLED は ghostCounter.ts へ移設)。
- **成立判定の差分**(概算=語尾判定 vs プレイヤーの実判定・消費側で全て弾く=広げない):
  giantbatのwindup(W4)/jibril 'warp-recover' /skadi-ice・skadi-bladeのwindup=不成立(請求は流れる)。
  逆にプレイヤーだけ可能な州: 裏3体'dash'・aim-burst/aim-radial・thor/裏ボスの実行中ライン・
  acrasiel 'warp-out'・気絶パリィ=ゴーストは狙わない(狭い側=許容)。

### 被弾音(★未決2の解消・社長裁定「入れてください」)
- **G4bの掟(v0.25.2459)「被弾音は付けない」は本裁定で上書き**。全被弾経路(ボス技/敵弾/汎用接触)の
  合流点= damageSummon の lastHit 打刻をエッジ検知して `player-damage` SE を距離減衰
  (npcSfxDistGain・ゴースト位置)で再生。i-frame(INVULN_MS)+最短200ms保険でスパムなし。

### ゴースト発動サブの発動SE距離減衰(★未決3の解消・「処理重めならいらない」→距離計算のみで軽量=実施)
- 発動SEを持つ3種(shield/turret='shield-deploy'・fire-knife='shot-damage')を、ゴースト発動時のみ
  設置/投擲点で減衰。プレイヤー発動は従来どおり等倍(1bit不変)。heavy-grenade/marksman-trap/decoyは
  発動SE自体が無く対象外。設置後のオブジェクト音(タレット発砲等)は既存のまま(タレット発砲は元々減衰済み)。

## 2.10 G5: 守護霊アルバム=2軸プロファイル(社長発案・裁定 2026-07-30「城ボススロットも分けて」)

### 決定(社長)
- **2軸**: ①**共通プレイスタイル**(現行プロファイル=EMA蓄積・変更なし) ②**ボス別攻略スタイル**
  (撃破ランの写し・ベストプレイ上書き)。**②のデータがあるボスは②でリプレイ、無ければ①で走る。**
- **城ボス(giantbat系)はステージ別スロット**(城ボス/グレン/未確認変異体は同typeだが別の戦いのため)。
- 「上書きしない選択」=**既存のリザルトチェック**(「今回のプレイを守護霊に反映しない」)が両軸に効く。
  新しいUIは足さない。
- 撃破ラン同士の優劣=**被弾/分(hitsPerMin)が少ない方を保持**。

### 仕様(実装バッチG5・確定)
1. **保存フォーマット**: `PlayerProfile`(v:1のまま)へ任意フィールド `bossStyles?: Record<string, BossStyleSlot>`
   を追加(後方互換=欠損可・isValidProfileは任意オブジェクトとして許容)。localStorageキーは既存の1つのまま。
   - **slotKey** = type が `giantbat` なら `giantbat@<stageId>`(`getSelectedStageId()`・例 `giantbat@stage-2`)、
     それ以外は type そのまま(`thor` 等)。対象ボス= `ENGAGEABLE_BOSS_TYPES`(bossEngagement.tsが正本)。
     slotKey計算は純関数 `bossStyleSlotKey(type, stageId)` に切り出す(計測側と消費側で同じ関数を使う)。
   - **BossStyleSlot** = 撃破セッション1回ぶんのサンプル写し(**EMAしない**):
     9ノブ(reactionMs/counterChance/preferredDist/meleeBias/mobility/hitsPerMin/subUsesPerMin/
     stationaryFrac/approachPerMin・各null可=そのセッションで計測できなかったノブ)+
     `subStyles`(下の注)+`snapshot`+`srcClass`+`srcName`+`at`(記録時刻・将来のアルバムUI用)。
   - **moveReactions(技への反応表)は共有のまま**(既にボス×技キーで分かれている=軸2へ複製しない)。
2. **撃破の検知**: `notifyBossClear(bossType, stageId)` を playerTraits に新設し、**ボス死亡の確定点に
   1行ずつ**置く。経路は実装時に**全数列挙**すること(v0.25.2426の教訓「同じ動作を持つ全実装経路に付ける」。
   少なくとも damageEnemy 死亡処理のボス分岐・ストーリーボス撃破(finaleDefeated系)を精査し、
   置いた/置かなかった経路の全数表を実装報告に載せる。爆弾はボスを殺せない=対象外)。
   - セッション(ボス交戦計測)が開いている時だけ session に clearedSlotKey を積む(セッション外の死亡は
     無視=狭い側)。ゴーストランは既存ゲートで session=null のため自動的に記録されない(劣化コピー防止)。
   - endSession で clearedSlotKey ごとに保留レコード `kind:'bossStyle'` を積む(サンプルはそのセッションの
     確定値=既存 PendingSessionRecord と同じ計算)。**30秒フロア(MIN_SESSION_MS)は軸2にも適用**
     (30秒未満の超速攻撃破は録れない=ノイズ防止優先の割り切り・実測で困ったら再訪)。
3. **更新規則(ベスト保持・commit時)**: 既存slotが無ければ採用。有れば **新hitsPerMin ≤ 旧hitsPerMin
   なら上書き**(同値は新しい方=スナップショット/レベルが新鮮)。新サンプルがnullなら上書きしない(保守側)。
4. **subStylesの写し**: commit時、同ランの subStyle レコード(保留バッファ内)が有ればそのラン実測レートを
   直接使い(EMAなし)、無ければ commit 後の軸1 subStyles をコピーする。このため commit は
   **session/subStyle レコードを従来どおり先に順序どおり適用**(=軸1の保存結果は旧実装とビット一致のまま)
   **→ bossStyle レコードを最後に適用**する。
5. **消費(召喚側)**: directorTick の召喚時、bound するボスの slotKey を計算し、
   `profile.bossStyles?.[slotKey]` が**有ればノブ単位で軸2値を優先**(nullノブは軸1へフォールバック)、
   無ければ従来どおり軸1。snapshot/srcClass/srcName も slot 優先(絵と名前=その撃破ランのもの)。
   合成は純関数 `effectiveGhostProfile(profile, slotKey): PlayerProfile` に切り出す
   (ghostDriver 自体は変更しない=受け取るプロファイルの形は不変)。
6. **不変条件(受け入れ条件)**:
   - 撃破が無いラン/ゴーストラン/リザルトのチェックON では bossStyles に1bitも触らない。
   - 軸1の計測・EMA・保存は完全不変(既存 playerTraits テストが無変更で通ること)。
   - 新規純関数(bossStyleSlotKey/ベスト保持判定/effectiveGhostProfile)のユニットテストを同コミットで書く。
   - typecheck+lint エラー0。実装報告に撃破経路の全数表を載せる。
- 設計チャット裁量の叩き台(軽微・社長調整可): 30秒フロアの適用/同値タイは新しい方/`at`記録時刻の追加。

### 実装結果(v0.25.2485・実装サブエージェント・ステータス=実装済み)

詳細(commit順序の設計・実バグ修正の経緯・テスト方針)は DEVELOPMENT_LOG.md v0.25.2485 が正本。
ここは要約。

- 仕様1〜6を全て実装(`bossStyles?`フィールド/`bossStyleSlotKey`/`notifyBossClear`/保留化との合流/
  `isBetterBossStyleSample`(ベスト保持)/`sampleSubStylesFromRecord`/`applyPendingBossStyle`/
  `effectiveGhostProfile`。全て `src/utils/playerTraits.ts`)。`ghostDriver.ts` は無変更。
- **撃破経路の全数表**(死亡分岐を持つ2つの合流点=`gameStore.damageEnemy`と
  `gameStore.grantMeleeKillRewards`だけで、bomb(ボスは対象外=既存仕様)以外の全ボスキルを網羅):
  1. `damageEnemy`(gun/接触/爆発/DoT/カウンター即死等。裏ボスカウンター`combatTick.ts`・
     angelBossTick.tsのカウンター・ghostCounter.tsのゴーストカウンターも全てこの関数を経由)→
     `newHealth===0`分岐で捕獲し`notifyBossClear`。
  2. `grantMeleeKillRewards`(通常近接カウンター/刀/鞭/シールドバッシュ/投擲スケボー/分身の
     melee kill。6呼び出し元すべてがこのヘルパーを経由することをコードで確認)→ for-loop先頭で
     `notifyBossClear`。
  3. 爆弾(VS rosary)はボスを対象外にする既存仕様のまま=対象外。
  4. ストーリーボス(グレン/未確認変異体=giantbat型の別名)固有の死亡処理は無し(1/2の経路で網羅済み)。
  5. デバッグ即死コマンドは存在しない。
- **実装中に見つけて修正した実バグ**: `applyPendingSession`が固定フィールドの新規オブジェクトを
  返しており、既存の`bossStyles`を引き継いでいなかった(2回目以降のセッションcommitでbossStylesが
  消え、ベスト保持判定が常に「新規」扱いになる実バグ)。`base.bossStyles`を明示的に引き継ぐよう修正。
  新規テストの「ベスト保持」ケースで検出。
- テスト: `src/utils/playerTraits.test.ts` に新規15件(既存41件は無修正で通過=軸1のビット一致要件を
  満たす)。`npx vitest run src/utils/playerTraits.test.ts` = 56件全通過。typecheck/lintともエラー0。
- ★未決: なし。

### §2.10 追補(v0.25.2493・社長裁定2026-07-30)
- **「Bは混ぜない。基本的に残るのは撃破だけ。その後死のうが生きようが残る」**: 撃破の無い交戦セッション
  (逃げて切れた負け戦)は**軸1にも軸2にも残さない**(丸ごと破棄)。撃破済みセッションは以後の死亡に
  関係なく保留バッファに残る(従来どおりリザルトでcommit/破棄)。サブ様式(ラン単位)は対象外=従来どおり。
- **撃破タイム採用**(「撃破タイム+カウンター成功率であれば採用」): BossStyleSlotに`clearTimeMs`
  (交戦開始→撃破・撃破の瞬間に確定)を追加。カウンター成功率は既存のcounterChanceスロット値を使う。
- **アルバムUIは作らない**(社長裁定)。代わりに**討伐記録一覧**(ボス別スロットの撃破タイム・
  カウンター成功率・被弾/分等の指標が見れる画面)を後続バッチで作る(置き場所は設計チャットが提案)。

## 2.11 完全パリティ原則(社長指示2026-07-30・最上位ルール)
> 「明確にこれは外して、と言ってない仕様は全部一緒にして。もう抜け漏れない様に完璧に写して!
> 刀とか一閃とかも全部再現して」

- **守護霊はプレイヤーの戦闘仕様の完全な写しである。** 除外は以下の**2群のみ**(社長裁定2026-07-30
  「1と4以外は全部載せろ。完璧に」):
  1. カメラズーム・時間停止・スローモーション演出(出さない)
     **※文字callout(Counter!/Kill!等)は除外1に含めない=出す**(社長裁定2026-07-30「これは出して
     欲しいよ」・v0.25.2528で弾反射のCounter!と処刑のKill!を追加。演出バンドルを丸ごとスキップする時も
     文字だけは個別に出すこと)。
  2. 弾薬非消費・テレメトリ除外・SE距離減衰・スコア×0.5(運用系)
- **【訂正】旧v0.25.2459「スキル倍率・装備ボーナスは乗せない(素の武器性能)」と「守護霊の弾はクリ無し」は
  社長裁定ではなく実装時の保守判断が定着したもの=廃止。** スキル倍率・装備ボーナス・射撃クリティカルも
  **全て再現する**(社長「ステータス保存する意味ないやん」)。※裁定E(独立サブ=タレット等のクリ無し)は
  独立サブの話であり守護霊には適用しない。
- **含意=スナップショットのビルド化**: 完全再現には計測時の**ビルド丸ごと**(武器ロードアウト・スキル+
  レベル・装備・クリ率等)の保存が必要。現行snapshot{maxHealth,speed,level}を拡張する(監査表で不足を列挙)。
- 上記以外の差は**全て漏れ=バグ扱い**(ワイヤー移動・弾反射・ブラストパリィ・気絶パリィ・
  被弾ノックバック・刀モード・鞭・フィニッシュ・コンボ・被弾点滅・全ダメージ/確率補正を含む)。
  新機能をプレイヤーへ足す時は**同コミットでゴーストにも足すか、足せない理由を★未決に書く**こと。
- 監査の正本: `research/GHOST_PARITY_LEDGER.md`(全数監査表)。実装はこの表を消し込む形で進める。
- **§2.11追補=最上位ドクトリン(社長裁定2026-07-31「もう例外はない。同じ仕様にして。将来オンライン
  プレイも考えているので他の考え方は入れないで」「別枠とかもう無いから一生」)**:
  **守護霊は独立した2人目のプレイヤーである。** 共有帳簿・専用枠・ゴースト特有の仕組みを**新設しない(恒久)**。
  状態は主語(オーナー)ごとに持つ——**将来オンラインで実プレイヤーに置き換わっても成立する形だけを作る**。
  - 帰結①: サブCDの「1つの財布」(プレイヤーの subWeaponCooldowns 共有・v0.25.2522〜)は本裁定に反する
    =**ゴースト自前のCD帳簿へ是正**(2人分のサブが独立に回る。ボスHP×1.6が既にその補正)。
  - 帰結②: 分身=主語ごとに1体(ゴーストの分身はゴースト自身の写し=そのビルドのクラス絵+青白)。
  - 帰結③: センサーマイン=主語ごとのチャージ帳簿(設置上限・回復も同じ規則で自前)。
  - 帰結④: 以後、実装・設計の両方で「共有/例外/ゴースト専用の別モデル」の**提案自体を禁止**。
    有効な除外は裁定済みの2群(除外1=演出/除外4=運用系)のみ。迷ったら「実プレイヤーが2人いたら
    どうなるか」で決める。
  - **除外1の試験改定(社長「ためしたい」2026-07-31・v0.25.2583)**: ズーム/スロー/ストップの同梱演出
    (triggerFinishImpact/triggerHitImpact)を**守護霊のキル・フィニッシュ・カウンター成立にも採用**
    (「ズーム自体にCDがあるので、より戦闘が派手になる意味では見てみたい」)。CDは**共有の1本**
    (演出=カメラは世界にひとつの資源=主語ごとに持たない)。サブ起因スロー禁止はプレイヤー同様維持。
    被弾シェイク等その他の除外1項目は従来どおり。`?ghostzoom=0` で従来(除外1)へ完全復帰(A/B用)。
    実機の体感で採否を確定する(採用なら除外1の本文を改定・不採用ならフラグごと撤去)。
- **§2.11追補3: アイテム=「霊体は世界の物に触れない」(社長裁定2026-07-31「そだね」)**。
  ドロップ・スクラップ・武器・宝・弾などの**世界資源に守護霊は干渉しない**(拾得判定なし・財布なし・
  ラン中のビルド成長なし=記録時の写しのまま)。木や岩のすり抜け(§9-7)と同じ「霊体」の整理で、
  ドクトリン(帳簿・状態は各自)とは軸が別: あちらは仕組みの話、こちらは世界への物理干渉の話。
  理由: ①守護霊の本質=記録時の写し(ラン中成長は再現の意味を壊す) ②オンライン時に他人の守護霊が
  自分のドロップを取る不満の構造的回避。
  - 例外(共闘ボーナス): **回復の連動30%**(プレイヤーが救急セットを拾うと守護霊も自分の最大HPの30%
    回復・実装済み)。**ショップ購入メディキットも同じ連動に揃える**(入手経路による非対称の解消・
    RESULT-UI着地後に実装)。経験値=守護霊のキルもプレイヤーへ(助っ人の戦果・現状維持)。

## 2.12 行動品質の裁定(社長2026-07-30「下手さは提案ので良い。それっぽさが出る工夫を。距離の取り方とかね」)
- **原則: 選択=計測値・実行=常に本気。** 移動の下手さの二重再現(hitsPerMin→dodgeStrength逆写像)は廃止。
- **それっぽさの機構(個性はここで出す)**:
  1. **反応遅延**: 回避/カウンターの開始を計測reactionMs(100-800clamp)だけ遅らせる(実行は本気・気づきの早さが個性)。
  2. **距離の取り方**: 平時の間合い=計測preferredDist(近接派/銃派の距離感を維持)。**ボスwindup中のみ
     安全マージンを足して退避**(dodge/tankロールでない場合)。
  3. stationaryFrac/approachPerMinを移動リズムに反映(足を止めがち等。危険時は必ず動く)。
  4. tank率=苦手技は食らう(下手さの主表現・現行維持)。
- 実行側の修正: dodgeVectorは常に全力/カウンター待ちは約1秒(叩き台)で見切って離脱/
  回避対応表(telegraphDodge)を全ボス技へ拡張。
- 実装はパリティ消し込みバッチ(GHOST_PARITY_LEDGER消し込み)に同梱。
- **§2.12追補: ぼーっと立たない=オービット(社長裁定2026-07-30・v0.25.2534実装)**。
  社長「人間なら攻撃/移動/カウンター待ちのどれかを必ずしている。ぼーっとはしない。せめてボスを
  正面に横に歩かせる」→ 旧実装で立ち尽くしていた3場面(帯内の間合い一致・止まり癖tick・
  **tankロール予告中の棒立ち食らい**)を全て**ボス正対の横流れ(オービット)**に置換。
  - 速度倍率(叩き台): 帯内0.55/止まり癖tick0.3/tank予告中0.35(**tank中を速くしすぎると狙い撃ち技が
    偶然外れてhitRate再現=「苦手技は食らう」が壊れる**ため遅め=「避けようとして間に合わない」絵)。
  - 旋回方向は持ち越し+低確率(0.004/tick≒約4秒に1回)で反転。facingは従来どおりボス向き=正対のまま。
  - **例外=カウンター待ちの静止のみ維持**(意味のある静止・窓リング表示つき)。
  - 「足を止めがち」個性は消えない: 止まり癖tickは遅い横流れ+前後に詰めない、で表現が残る。
- **§2.12追補2: 接触(体当たり)回避オン(社長裁定2026-07-31「ではオンにして」・v0.25.2547)**。
  規格はbotSkill既存のまま流用: **接触ダメージが自分の最大HPの20%以上の敵が260px以内なら離れる**
  (弱い接触は避けない=「敵に触れると逃げる」人にはしない)。ゴースト専用モデルなし(ドクトリン準拠)。
  背景: テスト#6で守護霊が38ダメ×3発で死んでおり、コード読みの本命疑いがボス巨体の体当たりだった
  (被弾源の確定ログ=?ghostlog=1・依頼#7と並行)。
  - **改(社長裁定2026-07-31「対処して」・v0.25.2564)**: 実測(依頼#7/#8)で死因がほぼ contact:mimir
    のみと確定し、上の20%閾値だけでは足りないと判明(①高HP記録では閾値が反応しない=ボット用の
    借り物規格、②距離が全て**中心間**で、巨体ボス(mimir幅248)では体内に立たないと近接/カウンターが
    成立しない=プレイヤーのenemyMeleeDist(縁基準)の写し損ね)。対処2点:
    **(a) ボスの体は常時回避対象**(HP閾値なし・縁からGHOST_BOSS_BODY_AVOID_PX=48未満で中心から
    反発・縁に近いほど強い)。48<近接レンジ74なので**踏み込み(近接/カウンター)は殺さない**。
    雑魚は既存の20%規格のまま(=「雑魚に触れると逃げる」人にはしない)。
    **(b) 近接/カウンター射程の物差しを縁基準へ**(gameStore.enemyMeleeDist をdecideGhostへ**注入**
    =katanaAutoの前例。純関数を保つ)。カウンター待ちの静止(意味のある静止)は(a)より優先=構え中は
    反発で動かない。
  - **改2(v0.25.2567・監査GHOST_HIDDEN_RULES 9-2/9-3の是正=明確なバグ修正)**:
    **(c) 間合い管理(preferredDist)も縁基準へ**。計測側(playerTraits.bossBandDist)は最初から縁で
    測っていた(設計書§2.6どおり)のに消費だけ中心間だった**単位取り違え**(2564時点の「計測値は
    中心間」というコメントは誤りだった=このバッチで訂正)。
    **(d) 銃の射程ゲートをプレイヤーと同じ式へ**(aimEnemyDist2=裏ボスのみAABB最近点・他は中心。
    式の正本をweaponUtilsからenemyUtilsへ移設し、プレイヤーと守護霊が同じ1本を使う)。旧実装は
    中心間のままで、ショットガン(120)等は裏ボスの体外から永久に撃てなかった(プレイヤー側だけ
    是正済みの写し損ね)。

### §2.11補足: 実装ドクトリン「写すな、共通化しろ」(社長の指摘2026-07-30「呼び出し口が違うだけじゃないの?」)
- パリティの実装は**コピー実装を禁止**し、**既存プレイヤー関数のオーナー(主語)引数化**を第一手段とする。
  根拠: 共通化した箇所は一発でパリティが出て以後も壊れない(カウンター請求=per-boss判定の共用・
  エフェクト関数共有)。コピーした箇所は必ずズレる(守護霊銃の5差分事故)。
- 既存コードは「プレイヤー=唯一の主語」で状態がplayer直付け(wireDashUntil等)のため、移動系ほど
  掘り直しが重い。それでもコピーせず主語を差し替える——共通関数なら今後プレイヤーへ足す機能が
  自動で守護霊にも効く=恒久の再発防止。

## 2.13 撃破リザルト「年表」仕様(社長裁定2026-07-30「アイコン化してスコアと撃破タイムを載せて、採用するか選ばせよう」)
- 撃破ランのリザルトに**撃破ボスを撃破順にアイコンで並べる**(年表風)。各カード=ボスアイコン+
  **撃破タイム+評価数値(被弾/分・カウンター成功率)**+現在の記録との比較(良化/悪化)。
- **ボスごとに「採用する」チェック**(既定ON=叩き台)。採用したボスのスロットだけ**丸ごと写し**
  (動き+ビルド一体・v0.25.2504議論の帰結=分割不可)で上書き。軸1(共通傾向)は採用1件以上の
  セッションぶんのみ反映。全不採用=従来の「反映しない」と同義。自動ベスト判定(被弾/分)は廃止のまま。
- スコアは保存せず表示時合成(clearTimeMs/hitsPerMin/counterChanceの生値から)。
- 実装: playerTraitsの保留バッファをスロット別決算へ拡張(settlePendingTraitsの選択引数化)+
  GameOverScreenの年表UI(ボスアイコン=既存スプライト流用)。カードUIは討伐記録一覧にそのまま流用する
  (同部品)。**一覧の置き場所は資料室ではなく独立メニューに変更の見込み=§2.14参照(未確定)**。
  発注はパリティ列(BUILD-1→刀/ワイヤー→弾反射等)の後。

## 2.14 討伐記録の独立メニュー化+オンライン構想(社長構想2026-07-30 → **独立メニュー化は確定**)
**社長裁定(2026-07-30)「独立メニュー化しよう」= メニュー新設は確定。**アップロード等オンライン系の
詳細は引き続き下記構想のまま(★未決3件は未裁定)。
社長の発言(原文要旨):「未来にオンライン化するなら、討伐記録をaiのオンライン設定と一緒にメニューを
独立させるのがいいかな。ここで名前の決定と、討伐の保持記録と、アップロードボタンを付けて手動で
アップロードさせようかと薄ら考えている。逆の守護霊ダウンロードは出撃前にランダムか、ある程度
選択肢が出てくるか、などは後で考える必要あり」。
- **構想の骨子**: タイトルから入る**独立メニュー(守護霊/討伐記録)**を新設し、そこに
  ①プレイヤー名の決定 ②討伐の保持記録(=G5アルバムの一覧・§2.13カード部品流用)
  ③手動アップロードボタン(自分の守護霊をオンラインへ公開)をまとめる。
  資料室は操作記録(チュートリアル)専用のまま。
- **設計チャットの整理(未裁定・提案)**:
  - リザルト年表(§2.13)=書き込み口(採用選択)/独立メニュー=保管庫+公開口、の分離は綺麗。
  - アップロードは**手動=正解筋**(公開の意思決定が明示的・自動同期の品質/事故問題を回避・
    実装もスナップショット1個のPOSTで済む)。
  - 名前はアップロードの身分証なので同居が自然。オフライン運用中は未設定でも困らない。
  - **死にボタンは置かない提案**: オンライン基盤ができるまでアップロードボタンは実装しない
    (メニュー+名前+一覧を先行)。採否は社長。
- **★未決 → 裁定済み(2026-07-30〜31・実装チャット側協議。オンライン層の正本=`GHOST_ONLINE.md`)**:
  1. ダウンロード側のUX=**(b)出撃時3択**(ミッション選択で裏3件先読み・揃わなければ無音で自分の霊・
     再抽選なし。候補カードは名前+当ステージボスの撃破タイムのみ・未遭遇ボスは???)。
  2. アップロード単位=**アルバム丸ごと1体**(手動ボタン・1人1行upsert)。
  3. 名前の文字種/上限=オンライン層の受信フィルタ(GHOST_ONLINE.md §2-10)と合わせて後決めのまま。
- **状態**: **独立メニュー新設=確定**(2026-07-30裁定)。§2.13実装の発注時に一覧の置き場所は
  独立メニューとする(資料室流用案は破棄)。オンライン層(Supabase/封筒プロファイル/3択/いいね)の
  実装はオンラインチャット担当=`GHOST_ONLINE.md` 参照。**霊体側の将来提供物**: プロファイルの
  スキーマ版`v`の管理と、封筒を解釈させないためのサマリ関数(撃破タイム等の取り出し口)。

## 2.15 リザルトに同行守護霊のビルド・ステータス表示+いいねボタン(社長指示2026-07-30)
社長の発言:「討伐に付き合ってくれた人のビルドとステータスはリザルトに出したい。参考になるから。
そこに、いいねボタンを置くと思う」。
- **仕様の骨子**: リザルト(§2.13年表)に、そのランに**同行した守護霊のカード**を出す。
  内容=守護霊の持ち主の名前+ビルド(武器/スキル/装備=PlayerBuildSnapshot)+ステータス。
  狙い=**他人のビルドを見て学ぶ導線**(オンライン時は他プレイヤーの守護霊が下りてくるため)。
- **いいねボタン**: 同行守護霊カードに設置。押すと持ち主に「いいね」が届く(オンライン機能)。
  貰った累計いいねは§2.14独立メニュー(自分の守護霊プロフィール)で見える想定が自然。
- **実装の段階分け(設計チャット整理・提案)**:
  1. **表示は先行できる**: 同行守護霊のビルド/ステータス表示はオフライン(自分の守護霊)でも成立
     (GHOST-BUILD-1のPlayerBuildSnapshotをそのまま描くだけ)。§2.13実装バッチに同梱可。
  2. **いいねボタンはオンライン基盤と同時**(§2.14の死にボタン回避方針と同じ扱い=先に置かない)。
     ※「置くと思う」=方向は確定扱い・実装時期のみ後日。
- **★未決 → いいねは裁定済み(2026-07-30〜31・`GHOST_ONLINE.md` §2-8)**: 単位=**守護霊の持ち主に
  1ラン1回**(サーバー側一意制約で連打防止)。**選出に影響させない**(私的な数字のまま)・ネガティブ
  ボタンは作らない。累計は§2.14独立メニューで閲覧+次回起動時に1回通知(同§2-9)。
  残る★未決: オフライン時にカード自体を出すか(自分の守護霊でも参考になるので出す=叩き台)。
- **置き場所の訂正(社長2026-07-30追記)**:「ビルドとかを載せるのはそこ(討伐記録)じゃない。リザルトだな。
  でも後から年表で名前タップしたらポップアップで見れると親切」。
  1. **ビルド+ステータスのフルカード=リザルト(ラン直後)のみ**。討伐記録には常設しない。
  2. **討伐記録(§2.14独立メニューの年表)=同行した持ち主の名前だけ**を各撃破記録に載せる。
  3. **名前タップ→ポップアップ**でその同行守護霊のビルド/ステータスを後から閲覧(親切機能・採用方向。
     §2.13実装と同時でなくてよい)。
  - データ注: ②③のため撃破記録(clearedSlots/BossStyleSlot)に**同行者名+ビルド写し**を保存する必要
    (ポップアップは保存したPlayerBuildSnapshotを描くだけ)。§2.13実装バッチで項目だけ先に持たせる。

## 2.16 実装バッチ GHOST-RESULT-UI 発注仕様(v0.25.2550発注・正本=§2.13/2.14/2.15)

### A. データ層(playerTraits)
- **スロット別決算**: `settlePendingTraits` を「採用選択」引数つきに拡張(採用したボススロットだけ
  丸ごと上書き・軸1(共通傾向)は採用1件以上の時のみ反映・全不採用=従来の「反映しない」と同義)。
  自動ベスト判定(被弾/分)は廃止のまま。撃破セッションのみ記録(clearedSlots)は既存を使う。
- **同行守護霊の保存(§2.15データ注)**: ClearedSlot(clearedSlots要素)に**同行者名+ビルド写し
  (PlayerBuildSnapshot)**を追加保存。現状オフラインでは同行守護霊=自分の記録なので、召喚された
  守護霊のghostName/ghostBuildを撃破時に写す(不在なら未保存=カード非表示)。
- スコアは**保存しない**(表示時に clearTimeMs/hitsPerMin/counterChance の生値から合成)。

### B. リザルト年表UI(GameOverScreen/クリア画面・§2.13)
- 撃破ランのリザルトに**撃破ボスを撃破順にアイコンで並べる年表帯**。各カード=ボスアイコン
  (既存スプライト流用)+撃破タイム+評価数値(被弾/分・カウンター成功率)+**現在の記録との比較**
  (良化/悪化の表示)。
- **ボスごとに「守護霊へ採用」チェック(既定ON=叩き台)**。決定でAの決算を実行。
- **同行守護霊のフルカード(§2.15)**: そのランに同行した守護霊の「持ち主名+ビルド(武器/スキル/装備)+
  ステータス」を表示(オフライン=自分の守護霊でも出す=叩き台)。**いいねボタンは置かない**
  (オンライン基盤と同時=死にボタン回避の裁定)。
- 撃破が無いラン(clearedSlots空)は年表セクション自体を出さない(従来のリザルトのまま)。

### C. 独立メニュー「守護霊」(タイトルから入る・§2.14)
- タイトル画面に入口を1つ追加(名称叩き台:「守護霊」)。中身:
  1. **プレイヤー名の決定**(localStorage保存・叩き台: 最大10文字・空なら「名無し」。文字種フィルタは
     オンライン化時にG-ON側の受信フィルタと合わせて後決め=今は制限しない)。
  2. **討伐の保持記録一覧**: G5アルバム(ボススロット別ベスト)を§2.13カード部品の流用で一覧表示。
     各記録=ボスアイコン+撃破タイム+評価+**同行者の名前のみ**。**名前タップ→ポップアップ**で
     その同行守護霊のビルド/ステータス(保存したPlayerBuildSnapshotを描くだけ)。
  3. **アップロードボタンは置かない**(オンライン基盤と同時)。
- 資料室は不変(操作記録専用のまま)。

### D. 掟
- §2.11追補ドクトリン(共有/例外/専用モデルを作らない)・React再レンダー規律(毎フレーム購読禁止)厳守。
- ゲームプレイ(判定・報酬・計測値)は不変=UI+決算の選択化+保存項目追加のみ。
- UI文言・レイアウト・数値は叩き台=実機調整前提と明記。未決は research/GHOST_PARITY_LEDGER.md ★未決へ。
- BOT_AND_GHOST.md 編集禁止(本仕様の消化状況はDEVELOPMENT_LOGとledgerへ)。git add明示列挙。
- ゲート: `npm run typecheck`(素のnpx tscは無効)0+lintエラー0+related。純関数(決算・名前検証等)は
  ユニットテスト同コミット。
- 版管理: rebase後origin+1・changelog先頭(体験の変化を具体的に)・DEVELOPMENT_LOG(JST打刻)。

## 2.17 二枠年表: ソロ枠と同行枠(社長裁定2026-07-31)+バッチGHOST-DUO-RECORDS発注仕様

**社長裁定**:「年表は同行者の名前と、アイコンと共に別枠で載せる。つまりソロと同行で二枠。
その上で、計測はしない。」= RESULT-UIの★未決1はこれで解決(クローズ)。

### 裁定の整理
- **ソロ枠(従来)**: 守護霊なしのラン=計測あり。撃破記録(BossStyleSlot)+採用チェック(写し)は
  従来どおり**ソロ枠のみ**。守護霊の中身はここからしか生まれない。
- **同行枠(新設)**: 守護霊同行ランでも**撃破の成績は記録する**。ただし**挙動計測はしない**
  (§2.7制約1不変)ので、記録は**撃破タイム+同行者(名前・アイコン・ビルド写し)**のみ。
  **採用チェックは無い**(写し不可)。評価数値(被弾/分・カウンター率)は計測由来のため同行枠には出さない。
- **名前入力は守護霊メニューに一本化**(オプションから撤去=v0.25.2555実施済み)。
- **歴史年表(タイトルのchronicle)への記載は同行撃破でも初回のみ**(社長2026-07-31「もちろん、年表は
  同行でも初のみね」)。ソロと同じ「初回討伐だけ載せる」規則を同行枠にも適用(毎回は載せない)。
  同行初回の行には同行者名を添える(叩き台・GHOST-DUO-RECORDSで実装)。
- **歴史年表の拠点・POI規則(社長裁定2026-07-31・確定版)**: **ゲーム全体で初回のみ**(ステージ不問)。
  - 拠点=**「初めて拠点を開放」の1件のみ**(現行の拠点ごと最大4件/ステージは廃止)。
  - POI=**種別ごとに全体初回1件**(文言=**「初めて警察署を開放」「初めて武器庫を開放」
    「初めて病院を開放」**・最大3件。社長指定「初めて警察署を開放 ね」)。
  実装=種別キーの全体スキャンガード(ステージ不問で同記録の既存有無を見る)。
  実装はSUBS-FINAL着地後(gameStore/useGameLoop競合回避)。

### バッチ GHOST-DUO-RECORDS(SUBS-FINAL着地後に発注・playerTraits競合の直列)
1. **データ**: 同行撃破記録の台帳を新設(localStorage・スロットキー=ソロと同じboss×stage・
   保持=スロット別ベストタイム)。記録経路=守護霊同行ラン中のボス撃破合流点(計測セッション外でも
   打刻できる独立の記録関数。撃破時に同行守護霊のghostName/ghostBuild/クラスを写す=RESULT-UIで
   入れた`ally`の器をこちらの台帳で使う)。**挙動計測(G4a/traits)には一切触れない**。
2. **リザルト**: 同行ランの撃破は**同行枠の年表**として表示(ボスアイコン+撃破タイム+同行者名+
   同行者アイコン(クラス絵)・ベスト更新表示・**採用チェック無し**)。ソロランは従来の年表(採用付き)。
3. **独立メニュー「守護霊」の討伐記録**: 二枠表示(ソロ記録=従来カード/同行記録=新台帳のカード・
   同行者名タップ→ビルドのポップアップ=既存部品流用)。
4. 掟: ドクトリン・再レンダー規律・計測不可侵(§2.7)・BOT_AND_GHOST.md編集禁止・純関数+テスト同コミット。

## 2.18 コマンド方式(次期・守護霊の行動モデル)— 社長裁定2026-07-31「よし、ではそれで行こう。サイコロも提案の案で」

**位置づけ**: §2.12「選択=計測値・実行=常に本気」の後継となる確定設計。相談(2026-07-31)で
台本方式(選択の**順序**を再演)も検討したが**棄却**——ボスの技順はランダムなので順序固定の再演は
むしろ「その人らしさ」を歪める(社長「決まった台本ではない。あくまで選択の率を基準にしてる」)。
採用形は**コマンド方式**: パーツ(コマンド辞書)はゲーム側の共通資産、個人の記録=選択率+味付け、
抽選のブレは袋式で制御。

### プロダクト観(この設計の「なぜ」・社長2026-07-31)
「**ボスの攻略だけしたい人は、自分の癖がついてるAIと討伐に行く。AI育てたい人はソロで行って、
そのボスの攻略コマンドを設計する。設計されたボス戦はその通りに動くだけ。**」
- ソロ出撃=**設計モード**(戦うこと自体がコマンド表を書く行為。採用チェック=どの設計図を清書するか)。
- 同行出撃=**攻略モード**(設計図を持って討伐へ。記録は更新しない=§2.7制約1は既にこの形)。
- 霊は**設計どおりに動くだけ**: 自己学習しない・賢くならない。強くしたければもう一度ソロで設計し直す
  =育成の手段がプレイそのもの。「守護霊が弱い」は設計図の問題としてプレイヤーに返る。
  オンラインは「設計図の交換」という商品になる。

### 確定した骨子(10点)
1. **選択=コマンドの抽選(率基準)**。コマンド辞書(パーツ)=ゲーム側の共通資産として1回だけ設計・実装
   (バッシュを狙う・タレットを敵に向けて置く・この技を避ける…)。**個人の記録=どのコマンドを何%か+
   味付けスカラーだけ**=軽い・読める・オンライン交換のペイロードも極小(ダンスの型: パーツは共通・
   組み合わせと頻度が個性)。設計者が予測しない使い方も「既存パーツの選択率の偏り」として自動で残る
   (開いた語彙)。
2. **技への反応=意図3分類×味付けの2層**(C案): 抽選するのは counter/dodge/tank の3分類だけ
   (%が速く安定する)。その下に**抽選しない味付けスカラー**——dodge=避け方向の癖(横/後ろ/前抜けの
   比率)・動き出しの早さ/counter=待ち位置/tank=撃ち続けるか。
3. **避け方向の癖はタイブレーク(成功が主・癖は従)**: 実行層が攻撃の幾何から「成功する避け方向の集合」を
   出し(予告台帳が既に計算している)、**正解が複数ある時だけ**癖で選ぶ。正解が1つ・癖の方向が壁側なら
   成功優先。成功/失敗そのものは袋の中身(結果の記録)が持つ=「避」を引いた回は成功するよう全力・
   「食」を引いた回は避けない。成功率という別指標は持たない。
4. **サブ=語彙は既存の状態(発明しない)・ツール内はセット%**: 盾のバッシュ/放置/防御/押し込み前進の
   ような**排他モード**は「文脈(平時/技Xの時)×モード内訳%」で持つ(=セットで覚える)。計測は
   **イベント錨点のスナップショット**(技の開始・設置/発動の瞬間・分岐の瞬間にどの状態だったかを見る
   だけ)=パターンの切り分け問題は発生しない。分岐の答え(射線が外れた→型替え%か放置か)も同じ枠。
5. **ツール横断の自由共起テーブル=作らない**(選択できる同時重ね掛けが現状存在しない)。
   **キャリア×ライダー結合**(近接スイング×ジャンク/ブーメラン/フレアガン・逆フリック×スケボー)は
   ゲーム定義の**閉じたリスト**として語彙に昇格し、結合ごとに3点セットで記録:
   **同期癖**(ライダーのCD明け→スイングまでの遅延スカラー)・**敵向き率**(発動瞬間)・**文脈%**。
   CD明け通知が全サブ共通にある以上、スイングのタイミングと向きは「選択」である(社長指摘)。
   将来「選択できる重ね掛け」を追加したら、その結合だけ共起カウンタを疎な辞書へ追加(拡張点・今は作らない)。
6. **分母=狙いに行く(好機待ちしない・社長裁定)**: トリガーはCD明け=原則使いに行く(好機待ちは
   再現性が死ぬ)。**優先度=配分**——ダメージ系サブはその戦闘の**ダメージ配分**(ビルドごと写すので
   火力の混同なし)/非ダメージ系(タレット・デコイ等)は**使用テンポ(回/分)**を同じ配分に正規化。
   いつ出すかの個性は**温存癖**(CD明け→使用までのスカラー=ホーミング溜めと同じ型)。
7. **サイコロ=境界ガード付き袋式**(裁定「サイコロも提案の案で」): 記録の結果をそのまま袋に入れ
   (4回中2回避=[避避食食])、シャッフルして引き切り・空になったら詰め直し。
   - **境界ガード**: 詰め直し時、直前の連続被弾が上限(叩き台=2)に達していて先頭も「食」なら、
     先頭を袋内の「避」と**入れ替え**る(並べ替え=中身は不変なので、引き切れば割合=記録どおりは保たれる
     =「記録の再演」と矛盾しない)。
   - **tank袋(全部「食」)はガード対象外**=「苦手技は食らい続ける」個性を壊さない(仕様)。
   - **袋の寿命=ラン単位**(ラン内は交戦を跨いでも保持・ラン間はリセット=1ラン=記録の1回の再演)。
     引き切り前にランが終わる切り詰め偏りは許容(目的は統計保証ではなく「それっぽさ」)。
   - **n=1は確定行動になる=仕様として許容**。
8. **n統計=実績null用のデフォ値専用**(社長裁定): 記録に無い問い(未遭遇の技・未使用のパーツ)にだけ
   集計デフォを使う。記録がある所をデフォで上書きしない。フォールバック階層=
   記録(袋/セット%) → 本人の集計デフォ → 既定プロファイル(現行casual)。旧プロファイルは記録なし
   =全問デフォで自然に後方互換。
9. **連続量=スカラーのまま**: 間合い(preferredDist)・反応速度(reactionMs)・溜め時間・温存癖・同期癖・
   避け方向比率。位置の軌跡は記録しない(分岐で破綻する=圧縮が正しい場所)。
10. **実行=常にベスト・ゴール逆算**: 記録は手順書ではなく**「その人が成立させたゴール+分岐の答え」**。
    実行層がゴールへ逆算して動く(縁まで詰める・盾のfacingを敵へ合わせる・射線が通る設置位置を選ぶ)。
    近接もサブも同じ「答えが先」の式で統一(ぶれ解消・社長確認済み)。逆算の運動部品は既存
    (詰めて窓を待つ/目標へ歩く/facing)で足り、新規は「射線が通る位置選び」の小関数程度=軽い。

### 実装フェーズ(順に発注。各Phaseの詳細仕様は発注時に設計チャットが確定させる)
- **Phase 1: 技への反応のコマンド化** — moveReactionsのロール(毎回抽選)→**袋式**へ置換+境界ガード+
  味付け(避け方向の癖・動き出し)の計測と適用。記録スキーマは版管理+正規化で欠損補完(後方互換)。
- **Phase 2: 配分とサブのモード%** — 近接ラン(詰めて当てる)・ダメージ配分/使用テンポの計測と優先度・
  ツール内モード%(文脈×セット)・キャリア×ライダー結合の3点セット・温存癖。
- **(Phase 3候補・メモ)設計図ビューア**: 守護霊メニューでボスごとのコマンド表(モード%・配分・袋の中身)を
  読めるUI。データがそのまま人間に読める形なので安く作れる(ソロ=設計、の可視化)。

### 追補: 抜けカードの裁定(社長2026-07-31「おけ、ではそれでお願い」)
- **隙コマンド(バッチCMD-2A・確定)**: 文脈={**完全気絶(紫)**/**技後硬直(リカバー)**/**カウンター成立直後**}
  ×モード{**詰めて叩く**/**撃つ**}を**1つの機構**で計測・再生(1エピソード1票・技キー横断のグローバル癖)。
  **実績nullのデフォ=詰めて叩く**——社長「そのベストは数値がなければいいと思うんだけど、数値があるのに
  決めつけて動かすのはやめたい。紫でも遠くから撃ちまくる人もいる」=数値が付いた瞬間から記録が常に勝つ。
  発端=社長実機報告「紫サークルになったら基本的に叩きに行ったはずなのに、AIは近づいていく気配すらなく
  中距離から撃ってた」(処刑の実行能力=applyGhostMeleeFinisherは実装済み・詰める意思決定が無かった)。
  器(2モード袋)は汎用形で作り、Phase 2のサブモード%へ流用する。
- **雑魚への対応=専用カードを作らず配分へ委任**(社長「雑魚の扱いだけは本当に迷う」→委任案を採用):
  Phase 2の配分に**ボス/雑魚のダメージ配分**を1項目として含め、実機の絵を見てから最終判断する。
  **ボス束縛(雑魚を追いかけて離れない)は構造の安全装置として恒久維持**(裁定v0.25.2469の核は不変。
  データが変えられるのは「絡んできた雑魚に手を出すか」だけ)。
- **低HP時の引き・開幕様子見=見送り**(前者は回復手段が薄く効果が見えにくい・後者は接近リズムが部分的に写す)。
- **フェーズ軸=階層文脈キーとして予約**(社長「フェーズによって行動も変わると思うけど、そこは?」):
  記録は最細粒度(例 `g-stomp@p3`・観測されたマスだけの疎な辞書)で取り、消費は
  **技×フェーズ → 技 → 本人の集計デフォ → 既定** の階層フォールバック。Phase 2仕様は文脈キーを
  **合成文字列**として設計して拡張点を確保する。フェーズタグの実装は隙コマンド・配分の実機確認後の追加バッチ
  (それまでは技全体の記録で動く=害なし)。
