# SKILL_BUILD_REDESIGN — スキルのラン中レベル制ビルド(レア度枠 1/2/3)

作成 2026-08-12(v0.25.3190)・設計チャット。ステータス: **監査8巡目の指摘=全件処理済み(穴+軽微=§16、★4件=裁定済み§16-10:
持ち込み廃止/Lv3覚醒/リロール・バニッシュ+常設スクラップ択/枯渇はB0分布待ち)。
B0(計測器)=実装中(§15)。B1発注文=§17(B0着地後にSonnetへ)**。
矛盾する旧記述は最新節が勝つ。

## 0. ゴール(社長の言葉のまま)
- 「後半に行くほどゲームプレイに派手さや違いが出てくるなら…**またやりたくなる何かが必要**」
- 「今は固定で2枠スキルを装備させているが、これを**ゲームプレイ中のレベル制にシフト**する」
- 「バランスについて、**レア毎に縛れば**? それを踏まえて**レア度も改めて算出**。
  **超レアは1種まで/レアは2種まで/ノーマルは3種まで/合計6個のビルド**」
- 「**スキルビルドはできるだけシンプルに、かつある程度ぶっこわれも許容で　じゃないと面白くない**」
  (社長指示 2026-08-12・v0.25.3198)
  - **解釈(設計原則として全節に優先)**: ①仕組みは足すより削る(迷ったら簡素な方)。
    ②強すぎる組み合わせは**潰す対象ではなく売り**。バランス装置(§5のTTK予算等)は
    「壊れているか監視する物差し」であって「壊れを禁止する締め付け」ではない。
    ③壊れ対策の追加機構(個別ナーフ表・組み合わせ禁止則など)は**作らない**。

## 1. 新しいラン内ビルドの形

### 1-1. 枠(社長指定・確定)
| レア度 | 同時所持の上限 |
|---|---|
| 超レア | **1種** |
| レア | **2種** |
| ノーマル | **3種** |
| 合計 | **6種** |

### 1-2. 取得の流れ
- **出撃前の持ち込みは1枠**(社長裁定v0.25.3192・確定。0枠案は却下=ガチャで当てた超レアを確実に
  使う手段と、引きが外れたランの下限保証を残す)。持ち込みもそのレア度の枠を消費する。
- **スキルの提示は「スキル2枚+装備1枚」の3択**として出す(監査#4/#15対応)。
  - 旧案の「装備3択+スキル1枚=4択」は廃止: スマホ1画面の3択レイアウトを維持し、かつ
    **スキル同士を比べて選ぶ=ビルドを組む体験**を作る(1枚提示では取る/取らないの二択にしかならない)。
  - スキル提示が起きなかったレベルアップは従来の装備3択のまま。
  - 装備1枚が常に並ぶ=「今回は取らない」の逃げ道(監査#13対応。リロール/バニッシュはv1では作らない)。
- 提示条件: ①ガチャで所持 ②このランで未取得 ③そのレア度の枠が空いている
  ④**発動条件を満たせる**(監査#8対応・§2-5)。
- 取得スキルのLvはガチャの所持Lv(ラン中のスキルLv上げはv1ではしない)。
- ボス開始チェストの即時3レベルアップ(`BOSS_START_CHEST_LEVELS`)も提示対象=**ボス前の最終補強**として
  意図に組み込む(監査#1で「事故」と指摘されたが、仕様として明記して裁定対象にする)。

### 1-2b. ★進行曲線(監査#1/#2/#7対応・「後半ほど派手」の心臓)
スキル提示の発生率とレア度重みを**プレイヤーLvで傾斜**させる(叩き台・実機調整前提):

| プレイヤーLv | スキル提示の発生率 | ノーマル / レア / 超レア |
|---|---|---|
| Lv2〜4 | 60% | 85 / 15 / 0 |
| Lv5〜7 | 80% | 60 / 32 / 8 |
| Lv8〜 | 80%(§9-3裁定=③装備専用ターンを2割残す) | 45 / 38 / 17 |

- **超レアの天井(pity)**: Lv8以降、超レア枠が空のままなら**次のスキル提示に超レア候補を1枚確定で混ぜる**。
  ⇒「最も派手な枠が4割のランで空のまま終わる」(監査#2の計算)を潰す。
- 序盤(Lv2〜4)に**抽選の**超レア0%なのは意図: 開幕で最強枠が埋まると**後半に変化が残らない**(ゴールの逆)。
  持ち込み(★#9裁定=例外として許容)はこの縛りの外=配牌であって抽選ではない。
- 該当レア度の候補が空なら隣のレア度へ流す(スキル提示イベント自体は空振りさせない)。

### 1-3. 枠の外に置くもの
- **守護霊系(guardian-spirit / ghost-helper / ghost-slayer)は「同行者」枠へ独立**(社長裁定v0.25.3192・確定)。
  - **配線(監査#10対応)**: `beginGhostOnlineRun` / `selectedGhostMode` は現在 `pendingSkills` から
    helper/slayer モードを決めている。同行者枠の採用時は**同行者枠の選択値**を渡す配線に変更する
    (黙って壊れる箇所として明記。B4の受け入れ条件に含める)。
  - **収支(監査#12対応)**: 旧仕様では霊が2枠の1つを食っていた。同行者枠化は**純増強化**になる。
    社長裁定v0.25.3192: **(a)据え置き**で確定(枠を跨ぐ例外則を作らない)。§5-2のTTK予算で一括監視し、
    予算超過が実測で出たら(b)「同行ランは超レア枠を消費」へ落とす、が既定の後退手順。
- **警察署の報酬3種(poi-*)は6枠の外**(従来どおり直接付与・変更なし)。

### 1-4. ガチャ(開発施設)との関係(監査#11対応=「無改変」の主張を撤回)
- 「所持」の意味は「装備できる」→「**ランの抽選プールに入る+持ち込み候補になる**」へ。
- **レア度表の差し替えはガチャの排出・返金経済に波及する**(排出プール母数/Lv抽選分布/被り返金10/30/50)。
  B3で**再シミュレートを必須化**: 全種MAXまでの期待ラン数(現行基準107ラン・campaign.tsコメント)を
  再計算して DEVELOPMENT_LOG に記録し、乖離が大きければ価格/確率の追従は**別途社長裁定**
  (この設計書では触らない)。
- 確率・価格・pity・Lv機構のコードは触らない(変わるのは表が指すレア度だけ)。

### 1-5. 「またやりたくなる」の装置(監査#4対応・v1の範囲を明示)
- v1で作る: **配牌(進行曲線×抽選)+スキル2枚からの選択+レア度枠の制約+リザルトにビルド6枠を表示**
  (「今回のビルド」がランの顔になる=もう一度回す動機)。
- v1で作らない(意図的スコープ外・実測後に判断): シナジーボーナス/リロール/バニッシュ/ビルド図鑑。
  器が回ってから足す(装置を最初から盛ると調整不能になる)。

## 2. 実装仕様

### 2-1. 抽選と枠の純関数(`src/utils/runSkillDraft.ts`・テスト同コミット)
- `runBuildCapacity(rarity)` = {super:1, rare:2, normal:3}。
- `canAcquireRunSkill(runSkills, key)`: 枠判定の**唯一の出どころ**(UI/抽選/持ち込み検証が全てこれを通る)。
- `draftRunSkillPair(owned, ownedLevels, runSkills, playerLevel, rand)`: §1-2bの曲線で2枚選ぶ。
  超レアpityの状態は store に1フラグ(`superPityArmed`)。
- **ノーマル内の重み**: `flashy` タグ(§2-5)持ちを**×2**(監査#3対応=「絵が出るスキル」を積ませる)。

### 2-2. 取得(gameStore)
- `UpgradeOption` に `type: 'skill'` を追加し、`selectUpgrade` に取得分岐(監査#15)。
  取得= `player.skills` へ追加+`skillLevels[key] = ownedLevels[key]`。既存の `hasSkill`/`skillLevel`
  読み出しは無改変で効く。
- 死亡/勝利でラン内ビルドは破棄(従来の player リセットと同じ)。リザルトに6枠を表示(§1-5)。

### 2-3. 出撃前の持ち込み
- 新定数 `MAX_CARRY_SKILLS = 1`。**`MAX_EQUIPPED_SKILLS`(=2)は削除も変更もしない**(監査#9対応):
  固定守護霊プリセット20人が2スキル構成で、この定数は**霊のビルド検証**(`fixedGuardians.ts:117` throw)が
  使っている。霊は「旧仕様のプレイの写し」なので2スキルのまま正しい。**持ち込み枠と霊の検証を
  別定数に分離する**のがこの設計の正解(共有のまま1へ縮めると起動時throw+テスト赤)。

### 2-4. 移行・互換
- 既存セーブの `pendingSkills` 2件→先頭1件のみ有効(UIで見える形で)。守護霊系が入っていたら
  同行者枠へ読み替え(裁定2で確定)。
- **ボット**: 「スキル枠が提示されたら先頭を取る」**最小ポリシーを入れる**(監査#14対応=計測に必要。
  旧案の「ボットは選ばない」を撤回。装備との優先度は付けない=先頭固定で決定的)。
- 練習ラン/ベンチ/チュートリアルはスキル提示なし(現行どおり)。
- 守護霊のビルド写し(ghostAlly snapshot)は召喚時点の `player.skills` を写す=ラン中取得も写る(無改変)。

### 2-5. スキルのタグ(データ追加・campaign.ts)
| タグ | 意味 | 対象(初期) |
|---|---|---|
| `sortieOnly` | **持ち込み専用**(ラン中提示から除外。出撃時にしか意味が無い) | scrap-builder / warm-up |
| `requires` | 条件を満たす時だけ提示 | dog-run(犬を装備中のみ) |
| `flashy` | ノーマル抽選で重み×2(絵が出る系) | sharpshooter / ricochet / punisher / reflex / slasher / counter-master |

- 監査#8の「ラン中に取ると説明文が嘘になる」への回答: 嘘になるスキルはラン中に**出さない**
  (遡及付与や文面差し替えより単純で、掟「本文に嘘を書かない」に合う)。

## 3. UI
- レベルアップ画面: スキルカードはレア度色+「SKILL」バッジ+残枠表示(「レア枠 残り1」)。3択のまま。
- 装備メニュー: スキル欄→「持ち込み(1)」。
- ビルド確認はポーズ/リザルト(HUD常設はしない。React再レンダー規律)。

## 4. レア度の再算出(社長承認v0.25.3192・確定。crit-up/sniper超昇格・貫通/跳弾/巻き込みノーマル降格を含む)
**原則**: レア度=枠の税。**ボスに効く倍率ほど上位/ボス中立の雑魚特効は下位に下げて積みやすく**。

| スキル | 現 | 新(確定) | 理由(1行) |
|---|---|---|---|
| reaper / berserker / skater / overclock | 超 | **超** | ラン定義級/ボス直撃 |
| crit-up | レア | **超** | ボスクリ×5→5.5+全クリ倍率=ボス直撃の最上位 |
| sniper | レア | **超** | 対ボス実効(巨体・停止/遠距離)最大+100%×2軸 |
| exploder / fire-shooter / bomber / bomb-counter | レア | **レア** | 爆発系(ボスにも乗る・条件付き) |
| knife-master / combo-master / knight / rescue-signal | レア | **レア** | 倍率/生存の軸 |
| sharpshooter / ricochet / punisher | レア | **ノーマル**(flashy) | ボス中立4チャネル=積ませて派手にする側 |
| benkei / reflex | レア | **ノーマル** | 条件付き小強化 |
| attack-shooter / counter-master / slasher / last-magazine / warm-up | ノ | **ノーマル** | 技量/条件付き |
| gold-rush / scrap-builder / magnet / ghost-shooter / time-keeper / runner / seeker / dog-run | ノ | **ノーマル** | 経済/利便 |
| 守護霊3種 | 超/ノ | **枠外(同行者)** | §1-3 |
| poi 3種 | 超 | **枠外(施設報酬)** | §1-3 |

- ガチャの排出にも同じ表を使う(2つのレア度表を持たない)。経済への波及は§1-4の再シミュレートで測る。
- ノーマル枠(3)の中身が経済系で埋まって「見た目に何も起きない」件(監査#3)は、
  `flashy` ×2重み+§1-2bの曲線で対処。それでも埋まりが地味なら経済系の一部を
  `sortieOnly`(持ち込み専用)へ降ろす(実測後の調整点として明記)。

## 5. 受け入れ条件(監査・実装検収用)
### 5-1. 機械的不変条件(ユニットテストで固定)
1. ラン中ビルドがレア度枠(1/2/3)を一度も超えない。
2. 提示は「所持済み・未取得・枠が空き・発動条件を満たす」以外を絶対に出さない。
3. 持ち込みは最大 `MAX_CARRY_SKILLS`(1)で、持ち込みもレア度枠を消費する。
4. `MAX_EQUIPPED_SKILLS` と固定守護霊20人の検証(fixedGuardians)が**無改変で通る**。
5. ガチャの確率・価格・pity・Lvのコードに変更が無い(表の差し替えのみ)+B3で経済再シミュレートを記録。
6. ボット/練習/ベンチ/チュートリアルで意図した挙動(ボット=先頭取得/他=提示なし)。
7. 守護霊のビルド写しがラン中取得スキルを含めて写る。オンライン霊のモード選択(§1-3配線)が生きている。
8. 既存セーブで起動してもクラッシュ/装備消失がない。
### 5-2. ボス予算(監査#6対応・裁定5で確定)
9. **想定最強6枠ビルドの対ボスTTKが、現行の最強2枠ビルド比で−25%以内**(社長承認v0.25.3192)。
   測定はボスメーカー+ボットラン(B5)。超過したらレア度表(§4)を上げ直して再測定。
   目安の意図: 「少し早く溶けるのは6枠化の意図の内。読み合いのフェーズ(形態変化・技ローテ)を
   飛ばすほど溶けたら壊れ」——グレン形態2が見られなくなるようなら締める。
### 5-3. ゴール達成の計測(監査#14対応・B5)
10. ボス突入時の平均枠充足率 **≥4/6**(ボットラン測定)。
11. 超レア枠が埋まったラン比率 **≥80%**(pity込み・同上)。
12. スキルカード提示時の選択率(スキップ=装備選択の率)を記録(数値目標は初回実測後に設定)。

## 6. 裁定記録(全7件 社長承認・v0.25.3192「7件承認」)
1. 持ち込み1枠(0枠案は却下)。
2. 守護霊系は「同行者」枠として独立。
3. 同行者の収支は(a)コスト据え置き(TTK予算で一括監視・超過時は(b)へ後退)。
4. §4レア度表を承認(crit-up/sniperの超レア昇格・sharpshooter/ricochet/punisherのノーマル降格を含む)。
5. ボスTTK予算=現行比−25%以内。
6. 進行曲線+超レアpity(§1-2b)を承認。**「序盤は*抽選の*超レア0%」は骨格**(数字調整で消さない。
   ★#9裁定=持ち込みは例外として許容: 曲線・pityが縛るのは**抽選のみ**)。他の数字は実機調整前提の叩き台。
7. **機構系ノーマル3種(sharpshooter/ricochet/punisher)を初期所持へ追加**(新規プレイヤー対策)。
   実装: `ensureDefaultOwnedSkills` の既定所持(現: 守護霊)へ3種を追加。ガチャで被った場合の
   Lv上昇は既存機構のまま。B3に含める。

## 7. 実装順(バッチ・Sonnet発注単位)
- **B0(新設・監査2巡目#4/#5対応)**: **ベースライン採取**。現行(6枠化前)のうちに
  ①最強2枠ビルドのボスTTK基準値(ボスメーカー・条件固定) ②ステージ別の到達Lv分布(greedyボット30ラン)
  を実測し DEVELOPMENT_LOG に記録する。**B3でレア度表を差し替えた後には二度と取れない**ので必ず先頭。
  「想定最強6枠ビルド」もB0時点で設計チャットが選定・記録して固定する(再現可能に)。
- **B1**: `runSkillDraft.ts`(枠/抽選/曲線/pity純関数)+テスト。store: `MAX_CARRY_SKILLS`・`runBuild`台帳・
  取得アクション・ボットポリシー(§8-6)。
- **B2**: レベルアップUI(スキル2+装備1の3択)+装備メニュー持ち込み1枠化+リザルトのビルド表示。
- **B3**: レア度表差し替え(campaign.ts)+タグ追加+**初期所持3種の追加(裁定7)**+ガチャ経済の再シミュレート記録。
- **B4**: 同行者枠(裁定2/3で確定済み)。ghostOnline のモード配線含む。
- **B5**: 計測(§5-2/5-3)=ボスメーカーTTK+ボットランで充足率/超レア比率。
- B1→B2→B3 は順に依存。B4独立。B5はB3後。

## 8. 監査2巡目(16件)の確定事項(v0.25.3194反映。§1〜§7と矛盾したらこちらが勝つ)

1. **枠の台帳は専用フィールド `runBuild: SkillKey[]`**(store・ラン内)。入るのは
   **ドラフト取得と持ち込みだけ**。警察署報酬(poi-*)と同行者(守護霊系)は `player.skills` には
   入るが **`runBuild` には入れない**=枠・pity・提示に一切影響しない。`canAcquireRunSkill` と
   充足率計測は `runBuild` だけを数える。受け入れ条件追加: **poi(超レア)取得後も超レア提示とpityが
   生きている**(テストで固定)。
2. **スキル提示時に残す装備1枚の選定**: 既存 `generateEquipmentChoices` の生成順の**先頭1枚**
   (=進化があれば進化、無ければ補完/特殊、無ければナイフ/スクラップ)。新しい選定ロジックは書かない。
3. **装備側がカンスト([スクラップ, 回復]の2枚)の時**: スキル2+**先頭1枚(スクラップ)**の3択のまま。
   4択にはしない。
4. **進行曲線のLvしきい値は仮アンカー**。B0で実測したステージ別到達Lv分布に対し、
   **「中央値ランの30%地点/70%地点」で3帯を切り直す**(表のLv2-4/5-7/8+は初期値にすぎないと明記)。
   受け入れ条件10/11の測定条件を固定: **stage-1・所持=初期所持+代表12種(B0で列挙)・greedyボット30ラン**。
5. **B0(§7)でベースライン採取**(TTK基準値・想定最強6枠の選定と記録)。
6. **ボット**: `pickUpgradeByPolicy` の greedy に **skill バケツを「装備進化の次」**として追加
   (先頭固定=決定的)。§5-3の計測は **greedy** で回す。random は従来どおり(skillも等確率で拾う)。
7. **乱数注入**: `draftRunSkillPair(..., rng)` は**必ず注入**。本体は `Math.random`、ヘッドレス計測は
   シード付き(mulberry32)。`Math.random` 直呼びをdraft内に書かない(botUpgradePolicyの掟と同じ)。
8. **`superPityArmed` は resetGame で必ず false へ**(受け入れ条件+テスト。resetRunClocks型の
   「足したがリセット忘れ」の再発防止)。
9. **持ち込み1枠化の改修箇所(全列挙)**: `gameStore.ts` の slice(0,2) 3箇所
   (ロード時4432/セッター12648/runSkills 14044)→ `MAX_CARRY_SKILLS`。`MissionSelect.tsx` の
   506/949/960/971 → 持ち込みUI側=`MAX_CARRY_SKILLS`。**`fixedGuardians.ts:117` だけが
   `MAX_EQUIPPED_SKILLS`(=2・不変)を使い続ける**。受け入れ条件: この対応表どおりであること。
10. **移行順序**: ①`pendingSkills` から守護霊系を**先に**抜いて同行者枠の初期値へ写す
    ②残りの先頭1件を持ち込みへ。例: `[ghost-helper, crit-up]` → 同行者=有志・持ち込み=crit-up。
11. **同行者枠の仕様**: 装備メニューに「同行者」欄。選択肢=**なし/守護霊/守護霊(有志)/守護霊(猛者)**
    (排他1人)。**既定=なし**(現行の「霊スキルを装備しなければ霊は出ない」と同じ挙動)。
    既存セーブは#10の移行で初期化。ボスHP×1.6/G倍率は選択時のみ従来どおり適用。
12. **`requires` の全数洗いはB3の作業項目**(40種を1回洗ってタグ表を確定)。判明分: dog-run(犬装備中)、
    bomber(グレネードランチャー所持——**提示時点の所持で判定**)。knife-master 等の**代償付きスキルは
    提示する**(説明文に代償が書いてある=選択の自由。隠れ地雷ではない)。
13. **候補不足時の構成**: 有効候補2件=スキル2+装備1 / 1件=スキル1+装備2 / 0件=装備3(提示不成立)。
    不変条件②に追記: **同一提示内に同じスキルを2枚並べない**。
14. **ボス開始チェストの3連続レベルアップ**: 各レベルは通常ロール。ただし**pityの確定混入は
    チェスト1回の3連につき最大1回**(3連続で超レア3枚保証、にはしない)。
15. **霊の写しが最大6スキルになる非対称は受容**(霊=実ランの写し。強い霊はランが強かった証)。
    固定守護霊20人は2スキルのまま(旧仕様の写し・§2-3)。霊カードのスキル表示は最大6件を列挙できる
    レイアウトにする(B2)。
16. **`sortieOnly`(scrap-builder/warm-up)は持ち込み1枠の下で死に枠化しうる**。B3実測後の調整点として
    §4末尾に併記: (a)持ち込み専用として数値を強める (b)タグをやめて遡及付与に変える、のどちらかを裁定に上げる。

## 9. 監査3巡目(13件)の確定事項(v0.25.3196反映。§8と同じく、前段と矛盾したらこちらが勝つ)

1. **DDA(difficultyScaler)の再較正**: `playerPower` は `skillCount×1.0` を含み、6枠化で雑魚難易度が
   自動で上がってしまう(2枠時代の較正のまま)。⇒ skillCount項の係数を **1.0→0.4(叩き台)** に改め、
   **B0で新旧の playerPower 分布と escalation 発火率を比較して確定**する。§5-2のTTK予算はボス単体
   なので、この雑魚側の波及はB0/B5のDDA比較が受け持つ(受け入れ条件に追加)。
2. **greedyボットのスキルバケツ位置**: 「装備進化の次」だと毎回装備を取り充足率が測れない。
   ⇒ **「runBuild充足が4未満の間はスキル優先、4以降は装備優先」**(決定的・両方測れる)。
3. **③枠(スクラップ/ナイフ)の保全**: スキル提示率をLv8+で100%にすると装備専用ターンが消え、
   ナイフTier強化(③の25%)とスクラップ収入が停止する。⇒ **Lv8+の提示率は100%→80%**
   (装備専用ターンを2割残す)。B5で**ナイフ到達Tier分布とスクラップ収入の新旧比較**を計測項目に追加。
4. **#8(★裁定済み 2026-08-12「5の未決は推薦で実装」=推薦採用)**: **初期所持にレア1種
   (fire-shooter=爆発弾・絵が出る)を追加する。** 超レアは初期に配らない(ガチャの目標=
   「またやりたくなる」の縦軸として残す)。実装先: 初期所持の台帳(ensureDefaultOwnedSkills)へ
   fire-shooter を追加(B1の受け入れ条件に含める)。
5. **#9(★裁定済み 2026-08-12・同上=(b)採用)**: **超レアの持ち込みは例外として許容**。
   進行曲線・pityは**抽選のみ**を縛る規律とし、裁定6の文言を「**抽選は**序盤超レア0%」へ精密化する
   (裁定1の目的「ガチャで当てた超レアを確実に使う」を守る。持ち込みは計画=配牌の外)。
6. **ドラフト除外の専用定数 `RUN_DRAFT_EXCLUDED_SKILLS` = 守護霊3種+poi3種**。
   `GACHA_EXCLUDED_SKILLS` は流用しない(あちらは reaper を含み、reaperは**ドラフトに出すべき超レア**)。
7. **霊カードのスキル表示は最大9件**(runBuild6+poi3。同行者は player.skills に入らない)。§8-15の「6件」を訂正。
8. **提示発生率のロールも `draftRunSkillPair`(rng注入)の中**で行う。順序: **候補を先に集める→
   0件なら不成立(ロールしない)→候補ありなら発生率ロール**。1関数が「提示の有無+2枚」を返す(null=不成立)。
9. **`pickUpgradeByPolicy` の引数拡張**(runBuild/残枠を渡す)と既存テストの更新を**B1の受け入れ条件に明記**。
10. **dog-run は `requires` をやめ `sortieOnly` へ**(犬の有無は出撃時に確定している。ラン中に犬を失って
    枠が永久に死ぬケースを根から断つ)。`requires` タグ自体はB3の全数洗いのために仕様として残す。
11. **計測ラン(§8-4/§5-3)の持ち込みは「なし」で固定**(超レアを持ち込むだけで条件11が自明達成になるのを防ぐ)。
12. §8-3の訂正: 装備カンスト時に残る1枚は「**③のスクラップ or ナイフ**」(knifeUpgradeOption 25%を含む。
    スクラップ固定ではない)。
13. §1-2条件①の訂正: 「ガチャで所持」→「**`ownedSkills` に在る**(入手経路は問わない。reaper等の
    イベント入手も含む。除外は#6の専用定数だけが決める)」。

### ★未決 → 全件裁定済み(2026-08-12 社長「5の未決は推薦で実装」)
- **#8**: 初期所持へレア1種(fire-shooter)を追加 → **採用**(上記4へ反映)。
- **#9**: 超レアの持ち込み → **(b)例外として許容**(曲線・pityは抽選のみを縛る。上記5へ反映)。
- 次の手順: 監査4巡目 → 通過後 B0(ベースライン計測)から発注。

## 10. 監査4巡目の反映(12件・v0.25.3201)

監査4巡目は不合格(12件)。**設計の穴(実装者が黙って決める/黙って壊れる箇所)は本節で確定**し、
**仕様判断を含む3件(#1/#2/#10係数)だけ★社長裁定へ切り出す**(節末)。

1. **(#1・★社長裁定へ)** 新規プレイヤーの初期候補はノーマル3+レア1のみ=最初の十数ランが毎回同じビルドに
   なる問題。裁定待ち(節末★1)。**§8-4の計測に「初期所持のみ(ガチャ0回)」条件を追加**するのは
   裁定と無関係に確定(最悪ケースを一度も測らない穴の補修)。
2. **(#2・★社長裁定へ)** 「派手さ」の担保と測定。裁定と無関係に確定する部分: **受け入れ条件に
   「ボス突入時点の flashy スキル所持数(中央値≥2)」を代理指標として追加**(§5-3へ)。絵の新造の
   要否は裁定待ち(節末★2)。
3. **(#3)** 受け入れ条件10/11未達時の後退手順を確定:
   ①充足率<4/6 → **帯の境界LvをB0の到達Lv分布(中央値)から切り直す**(発生率・重みは不変)
   → ②なお未達なら発生率を上げる(上限70/85/85)→ ③それでも未達=XPカーブ変更が必要=**社長裁定**。
   超レア率<80% → ①pity発火をLv8+→Lv7+へ前倒し → ②なお未達=社長裁定。
   §1-2bの表のLv8+=80%への訂正漏れも修正済み(§9-3の反映漏れ)。
4. **(#4)** 同行者の矛盾解消: **§9-7が正=同行者は `player.skills` に入らない**(専用の同行者枠フィールド)。
   §8-1の「入る」を撤回。**配線の全列挙**(B4受け入れ条件=全点の切替確認):
   `beginGhostOnlineRun` / `selectedGhostMode` / `directorTick.ts:645 ghostRunEnabled`(召喚ゲート+計測停止
   ゲートの引数源を同行者枠の選択値へ)/ `playerTraits` の計測停止 / `directorTick.ts:773` ボスHP×1.6適用点 /
   霊スナップショット(同行者枠は写さない=霊が霊を呼ばない)。
5. **(#5)** サーバ側スキル上限: `shared/ghostSanitize.mjs:116` の `listOf(…, 8)` を **9へ拡大**
   (runBuild6+poi3。受け側の拡大=後方互換)。B2の受け入れ条件へ追加。
6. **(#6)** `generateEquipmentChoices` 呼び出し元の対応表:
   `gameStore.ts:11991`(ボスドロップ宝箱=boss-startでない側)=**装備3択のまま**(スキル提示は
   レベルアップ+ボス開始チェストの2経路のみ)。`playtestDriver.ts:582` / `BenchmarkOverlay.tsx:843`
   (計測用)=スキル提示なしで固定(計測の再現性)。
7. **(#7)** レア度枯渇時の流し先=**下位方向のみ**(レア空→ノーマル)。上位へは流さない。
   下位も空なら提示不成立=従来の装備3択。⇒「抽選の序盤超レア0%」は構造的に破れない。
8. **(#8)** レア度ロールは**1枚ごとに独立**。pity確定枠が乗ったターンのもう1枚は**通常ロール**
   (超レア2枚並びも許容=§0「ぶっこわれ許容」)。
9. **(#9)** B5計測に「**空きスロット充足時刻**」「**特殊装備(5%/10%)入手率**」の新旧比較を追加。
   ②飢餓が実測されたら後退レバー=スキル提示ターンの装備1枚を「①が進化満了なら②優先」へ
   (新機構なし・既存3択生成の並び替えのみ)。
10. **(#10)** DDA入力の定義を確定: **`skillCount` は runBuild.length を数える**(poi/同行者は含めない=
    §1-3の台帳境界と同一)。**係数(1.0→0.4)の変更は仕様変更として★社長裁定へ**(節末★3。裁定まで
    B0比較は現行係数のまま取る)。
11. **(#11)** B0のTTK測定条件を固定: ボス個体=giantbat@stage-1+ミゲルの2体/武器・装備=初期Tier固定/
    プレイヤーLv=B0の到達Lv分布の中央値時点/ボット=greedy・シード10本固定/各条件10ラン/
    **同行者なし・ありの両方**を測る(裁定3の後退判定に必要)。§8-4と同じ粒度で固定。
12. **(#12)** 小穴3点: ①取得Lvは既存出撃経路(`gameStore.ts:14048`)と同じクランプ
    `Math.max(1, Math.min(skillMaxLevel(k), ownedLevels[k] ?? 1))` を通す ②resetGameで `runBuild` と
    `superPityArmed` をともに初期化(§8-8を訂正) ③スキル提示カードに**所持Lvを表示**(Lvで効果が
    別物のため=情報の欠落の補修。`skillDescForLevel` を使う)。
- **(補足の採用)** `requires` タグ機構は**作らない**(dog-runに続きbomberも `sortieOnly` で処理可能。
  §8-12の全数洗いで本当に必要な種が出た時に初めて導入=§0「仕組みは足すより削る」)。

### ★社長裁定(3件・監査4巡目より) — ★1/★2裁定済み(2026-08-12)・★3待ち
- **★1(体験)=裁定済み「おおむねOK」**: 初期所持を**「開幕から連鎖の絵が出る5種」**にする:
  ノーマル3=シャープシューター(貫通)/跳弾/パニッシャー(裁定7の3種のまま)+
  **レア2=ファイアシューター(爆発弾)+ボムカウンター(カウンター反射弾が爆発)**。
  - レア②の選定理由(発注時の言葉「効果一覧から最も絵が出るもの」): ボムカウンターは**単体で絵が出る**
    (中核動作=カウンターのたび爆発)。銃連鎖(貫通→跳弾→爆発弾)と近接カウンターの2系統が初期から
    揃い「どちらのビルドへ寄せるか」の選択が初ランから生まれる。エクスプローダーは
    fire-shooter依存(単体では絵が出ない)ため初期側には入れない=ガチャ後の重ね掛けの楽しみに残す。
  - 超レアは配らない(ガチャの縦軸)。B1の受け入れ条件の「初期所持」をこの5種に更新(§9-4を上書き)。
  - ※B3レア度表で fire-shooter / bomb-counter が**レア維持**であることを前提にする(変える場合は要再裁定)。
- **★2(派手さ)=裁定済み「2はそれで」**: バーサーカー=**赤い枠光**/オーバークロック=**青い枠光**を
  発動中のプレイヤーに付ける(ピクト+オーラ)。負荷1/10: pooled sprite+小glow・**投影影を落とす
  光源は使わない**(計測台帳「光の絵は無料」)。実装はスキル効果配線のバッチ(B4)に含める。
  §5-3の代理指標(ボス突入時flashy所持数)も併用=検収は指標+この絵の両輪。
- **★3(DDA係数)=裁定済み(社長「×3までで。つまり2枠で1上がる」)**: `skillCount` の寄与は
  **runBuild.length × 0.5(=2枠で+1.0)・上限+3.0(6枠フル)**。推薦の0.4ではなく0.5で確定
  (旧2枠時代の最大+2.0より少し上=スキルを積むほど難度も薄く応える、を残す社長裁定)。
  §10-10の入力定義(runBuild.lengthを数える・poi/同行者は含めない)は不変。B0比較は現行係数で
  取り、B4でこの係数へ切替(切替前後のTTK/被弾をB5で比較)。

## 11. ハイブリッド採用(監査5巡目★A裁定)+監査5巡目21件の反映(v0.25.3210)

### 11-0. ★A裁定(社長「ではハイブリットで」2026-08-12)= ラン中スキルLv上げを採用
- **ガチャの所持Lv=そのランの開始Lv**(従来どおり)。**ラン中は所持済み(runBuild内)スキルの
  「Lv+1カード」が提示に混ざる**。上限は skillMaxLevel(多くはLv3)。未取得スキルのLv上げは無い。
- 提示の形は「スキル2枚+装備1枚」のまま。スキル2枚の候補プール=**新規取得候補 ∪ Lv+1候補**。
  Lv+1カードのレア度=そのスキルのレア度(枠は消費済みなので枠チェックは通らない)。
  混入重み=新規と同格(叩き台。B5で「6種収集完了率/平均最終Lv」を計測して調整)。
- pity(超レア確定混入)は**新規取得候補にのみ**適用(不変)。進行曲線のレア度重みも不変。
- カード表示は「スキル名 Lv2→3」(§10-12③のLv表示機構をそのまま使う)。
- 算定済みの前提(v0.25.3209): 1ランで6枠フルLv3は実質不可(必要提示18回≈到達Lv24〜25)。
  フル完成の唯一の道=ガチャ育成のまま=縦軸は壊れない。
- これにより監査5巡目 **A-1**(初期5種の固定化)と **C-21**(「またやりたくなる」の担い手不在)を解消:
  ガチャ0回でも毎提示に「新規 vs 手持ち強化」の選択が発生し、ラン内成長が担い手になる。
  リザルトは§1-5(6枠+最終Lv表示)の範囲で足りる。

### 11-1. 監査5巡目の残18件の確定
- **A-2**: §10-6の「playtestDriverは提示なし固定」を**撤回**(greedyボット=充足率計測の本体のため)。
  再現性はシード固定で担保。§10-6の「呼び出し元」誤記(playtestDriver/BenchmarkOverlay)も撤回。
  ボスドロップ宝箱(gameStoreのchest経路のboss-startでない側)=装備3択のまま、は維持。
- **A-3/A-4**: bomberは`sortieOnly`に**しない**(発火源はランチャー/ホーミング/タレット/ボムカウンター等
  ラン中に成立する)。**bomberは無条件提示**。`requires`タグ機構は作らない(§9-10の「仕様として残す」
  文言を撤回)。§1-2④の「発動条件」の実対象は dog-run の`sortieOnly`のみ。§5-1-2の不変条件テストは
  sortieOnlyを対象に書く。§2-5のタグ表からrequiresを削除。
- **A-5**: ボス開始チェストの「3連につきpity1回」は、チェストの経験値付与時に**ラン内カウンタ
  `chestOfferBudget`(=このチェスト由来のレベルアップ残数)を立てて識別**する(新設・resetGameでクリア)。
  §10-6の「2経路」記述は「1経路(レベルアップ)+チェスト由来はカウンタで識別」へ訂正。
- **A-6**: スキル提示ターンの装備1枚=「既存3択の先頭」ではなく**「回復カードがあれば回復を優先、
  無ければ先頭」**(装備カンスト後の後半で回復手段を落とさない)。
- **A-7**: **B0に計測器の実装を含める**(2段構成に変更): ①`__BOT_REPORT__`拡張(ボス個体TTK/runBuild/
  レア度枠充足)+ボスメーカーへの6枠ビルド注入口 ②その計測器で基準線測定。
- **A-8**: DDA比較(playerPower分布/escalation発火率)は**実機オートパイロット(`?bot`)で測る**と媒体指定
  (ヘッドレスはspawnEsc:0固定で不可)。
- **B-9**: 初期所持5種は`DEFAULT_OWNED_SKILLS`への**追加**(守護霊3種は残す)。§6-7/§9-4/★1の記述を
  「追加」で統一。
- **B-10**: 同行者枠は**新キー`zombie:companionSlot`**へ永続化。移行=旧LOADOUT_SKILLS_KEYに守護霊系が
  居たら新キーへ書き出し+**旧キーから除去して書き戻す**(一回性の担保)。
- **B-11**: 受け入れ条件9(TTK−25%)の判定対象=**同行者なしのTTK**(同行者ありは参考記録)。
- **B-12**: 有志/猛者で固定20人(2スキル)と実プレイヤー霊(最大6+3)が混ざるばらつきは**v1許容**と明記
  (現状も混在済み。対価差の調整はv2候補)。
- **B-13**: ★2の枠光と★3のDDA係数切替の実装先を**B2(スキル効果・配線)**へ移す(B4は同行者専任)。
- **B-14**: 裁定済み=**バーサーカー金の枠光**/オーバークロック青(色文法の赤は予約のまま)。
- **B-15**: flashy代理指標を再定義: B3のレア度表作業と同時に**全スキルへ「絵が出るか」を再付与**
  (枠光が付いた超レア2種・爆発系レアを含める)。指標=「ボス突入時、絵が出るスキル所持数 中央値≥2」。
  ガチャ0回条件では測らない(初期3種が自動達成のため=計測条件と分離)。
- **B-16**: §10-3の発生率上限を70/85/85→**70/80/80**へ訂正(§9-3の「装備専用ターン2割」の床を守る)。
- **B-17**: 裁定済み=**(a)TTK予算は監視・報告のみ**。§5-2の「上げ直して再測定」を「報告→社長裁定」へ。
- **B-18**: DDA入力をrunBuild.lengthへ変えるとpoi/同行者を数えなくなる**緩み**が出ることを明記。
  B0比較は新旧両定義で取る。★3の上限+3.0は保険条項(0.5×6=3.0で通常は効かない)と明記。
- **B-19**: 「ラン中に取ると説明文が嘘になる」全数洗い(§8-12)を**B1と同時**へ前倒し。
  ナイト型(生成時にしか効かない効果)を洗い出し観点に追加。
- **B-20**: 設計書のコード位置指定は**行番号をやめシンボル名+定数名**で書く(§8-9/§10-12を読み替え)。

### 11-2. 次の手順
監査6巡目(このv0.25.3210反映後の全文) → 通過後 B0(計測器実装+基準線)から発注。

## 12. 装備の行き先(再編・書きかけ=基礎装備の裁定待ち)

2026-08-12の社長裁定・指示の記録(監査6巡目12件の反映と一体で完成させる):
- **ナイフ強化=武器箱へ移設(実装済み・v0.25.3212)**: レベルアップ③枠のナイフ提示(25%)を廃止、
  武器箱がTier5未満なら25%で次Tierナイフ(率は旧値引き継ぎの叩き台)。③枠は常設スクラップ+50。
- **特殊装備=専用POIへ(社長指示「POIを用意してそこに入れちゃおう」「それぞれで3つ設置」)**:
  - ランごとに**特殊装備POIを3つ**設置(装備スロット=体/腕/アクセサリのそれぞれに1つ)。
  - **設置方角はランダム**(既存の迂回POI=detourPoiの角度散らし機構を流用)。
  - **拠点解放でマーカー表示=在処が分かる**(既存の拠点→POI表示動線の延長)。
  - 中身は「そのスロットの特殊装備を選んで取る」(運は場所に限定・中身は選択=ブレ対策)。
  - レベルアップ補完枠への特殊装備混入(空きあり5%/空きなし10%)は撤去=特殊はPOI専任。
  - **POIの絵素材は社長支給待ち**=実装は素材到着後のバッチ。
- **基礎装備(進化・補完)の行き先=★最後の未決**: (a)商人(スクラップ経済で指名買い・レベルアップは
  スキル専業化) / (b)レベルアップに残す。裁定後に§12を完成させ、監査6巡目12件と併せて監査7巡目へ。

### 12-1. 基礎装備=商人(社長裁定「商人にする」2026-08-12)+レベルアップのスキル専業化
- **レベルアップ=スキル専業の3択**(新規∪Lv+1のスキルカード3枚)。装備カードは出さない。
  - 候補が3枚未満しか無い時は有るだけ+不足分はスクラップ+50カードで埋める(空振り無し)。
  - **提示発生率の帯(60/80/80%)は廃止**=毎レベルアップがスキル提示。§1-2bで残るのはレア度重みの
    傾斜(85/15/0→60/32/8→45/38/17)とpityのみ。
- **商人=基礎装備(①進化・②補完)+回復の供給元**。棚は既存 generateEquipmentChoices の
  ①進化(現装備の次Tier)/②補完(空きスロット埋め)ロジックを**棚生成に流用**(選べる性質は完全温存)。
  特殊装備は棚に出さない(POI専任・§12冒頭)。**通貨=スクラップ**(価格表は叩き台をB2で置き、
  B0のスクラップ収入実測で確定)。回復も商人にスクラップ販売枠を追加。棚生成はrng注入(シード可)。
- これにより監査6巡目 **#9(回復優先がナイフ/スクラップ収入を止める)・#3の一部・§8-3/§9-3/§10-9の
  「装備専用ターン」問題群は構造ごと消滅**(レベルアップに装備が居ないため)。

### 12-2. 監査6巡目(12件+軽微)の確定
- **#1**: §5-1-2を2本立てへ書き直し:「**新規取得候補**=所持済み・未取得・枠空き以外を出さない/
  **Lv+1候補**=runBuild内・現Lv<skillMaxLevel以外を出さない」。
- **#2**: 抽選手順を確定: ①カード1枚ごとに**カテゴリロール(新規:Lv+1=50:50。片方枯渇なら他方100%)**
  → ②新規側は§1-2bのレア度ロール(枠の空いたレア度のみ・枯渇は下位方向へ流し、全滅ならLv+1へ) /
  Lv+1側は対象スキルから等確率 → ③3枚は重複なし。**持ち込み超レアのLv+1は曲線の縛り外**
  (裁定6の「縛るのは新規の抽選のみ」を再確認)。カードごと独立(§10-8踏襲)。
- **#3**: §5-3の目標値(≥4/6・≥80%)は**「B0基準線取得後に確定する仮値」**へ格下げ(確定値扱いをやめる)。
- **#4**: 「ガチャ0回」計測条件は**条件11(超レア枠)の対象外**と明記。ガチャ0回の最終ビルドが
  5種同一になる点は**v1許容**(ハイブリッドのLv配分+商人経済の使い方がラン差を担う。
  不足と感じたら初期所持の追加を後日裁定)——設計者判断・社長は拒否可。
- **#5**: greedyボット: **充足<4の間は新規取得カードを最優先**(新規が無ければLv+1、それも無ければ
  スクラップ)。充足後はLv+1優先。決定的(先頭固定)は維持。
- **#6**: B0計測器の出力一覧を**A-7①の受け入れ条件として明記**: ボス突入時スナップショット
  (runBuild/Lv/レア度枠充足)/スキル提示回数・選択内訳counter/ナイフ到達Tier(箱由来)/
  スクラップ収支/商人購入ログ/最終スキルLv・6種収集完了率/ownedSkills・ownedSkillLevelsの
  ヘッドレス上書き口/同行者あり・なし切替口。
- **#7**: **TTKの定義=既存bossClock(ボスごと交戦時計)のbossClockDurationMs を正**とする。
  ボスメーカーに「スキル6枠+各Lv+装備Tier+プレイヤーLv」の注入口をB0で新設
  (XPが入らない部屋なのでLvは注入が前提、と明記)。
- **#8**: 同行者の配線列挙(§10-4)に **bossTestGhostSkill / selectedRunSkills(ボスモードの守護霊注入)**
  を追加(B4受け入れ条件)。
- **#9**: §11-1 A-6(回復優先)は**撤回**(12-1で装備枠ごと消滅。回復=商人)。
- **#10**: 商人棚生成にrng注入(シード可)を明記。レベルアップ側は装備カード自体が無くなるため
  「未シードの装備3枚目」問題は消滅。
- **#11**: 枠光の点灯定義(叩き台): **バーサーカー=HP70%未満で点灯・強度は失HP比例**(金)/
  **オーバークロック=CDリセットproc発生から800ms点灯**(青)。
- **#12**: §7のバッチ表を以下で**上書き**:
  B0=計測器実装+基準線(分布→TTKの順) / B1=スキルドラフト+レベルアップ専業化+B-19全数洗い
  (受け皿=`RUN_DRAFT_EXCLUDED_SKILLS`への追加で提示除外=requires不要) / B2=商人装備棚+回復+
  スクラップ価格表+サーバ上限9 / B3=レア度表+ガチャ再シム+flashy再付与 / B4=同行者枠+移行+
  配線全列挙 / B5=枠光+DDA係数切替 / B6=検収計測(B5相当の全項目)。
- **軽微の確定**: DDAはスキルLvを見ない(v1許容・係数で吸収)/条件12(選択率)はボット測定不可=
  実機プレイの打刻のみ/reaperのラン中入手は**同ラン即ドラフト候補=可**/§1-3の「(b)へ後退」文言は
  B-17裁定により削除扱い/リザルトは6枠+**各スキルの最終Lv**を表示/`UpgradeOption`のスキルLvは
  `level`を流用せず**専用フィールド`skillLv`**(既存equipTierUpgradeの誤判定防止)。

### 12-3. 監査7巡目★裁定の記録(2026-08-13)
- **★1=確定**(社長「1:確定」): 武将装備(特殊装備)は**毎ラン3種確定入手**が仕様
  (POI×3を回れば兜・鎧・小手が揃う。遠征+拠点解放が対価。強さはTTK監視で見張る)。
- **★3=裁定**(社長「練習ランは初期装備」): 練習ラン(ボスモード)は**初期装備で開始**
  (商人不在のままでよい・特別な支給はしない。装備満タン案は不採用)。
- ★2(ガチャ0回の終盤スクラップ化)=説明のうえ裁定待ち。
- **★2=裁定(社長「ヴァンサバみたいに、初期をもう少しビルド増やせば?」=初期所持の拡張で解決)**:
  初期所持を5種→**9種**へ(ノーマル5=貫通/跳弾/パニッシャー/アタックシューター/スラッシャー、
  レア4=ファイアシューター/ボムカウンター/エクスプローダー/ナイト。超レア0=ガチャ専用は不変)。
  どのレア度も所持>枠なので、ガチャ0回でも「9種から6枠を組む」実選択が毎ラン発生
  (=監査5巡目A-1「毎ラン同一ビルド」も同時解消)。枯渇(スクラップ3枚化)は取得6+Lv上げ12=18回
  ≈到達Lv24〜25相当となり通常ランでは実質起きない(v0.25.3209算定)。顔ぶれは設計チャット選定
  (絵が出る優先)・入れ替えは社長裁定でいつでも可。§10-★1/裁定7/§9-4の初期所持記述はこれで上書き。

## 13. 監査7巡目「設計の穴」7件の反映(2026-08-13・裁定3件は§12-3)

### 13-1. #3+#2 商人の装備区画=指名買いカタログ(「引きの棚」を廃止)
- **装備区画は生成しない(ランダム性ゼロ)**: 基礎装備スロットごとに「現在Tierの次の一段」を
  **常時・定価で販売**する固定カタログ。買うとそのスロットが1段進み、商品は自動で次の段に変わる。
  最上段まで到達したスロットは売り切れ表示。
- これにより#2(棚の更新規則)は**規則そのものが不要**になる(生成しない棚に更新はない。
  開閉リロール搾取も構造的に不可能)。§12-1の「棚は既存生成ロジック流用・rng注入」の記述は**削除**。
- 根拠=社長裁定の動機そのもの: 「装備は選べるのがあったから、**ランダムだとかなりブレる**。
  であれば、武器商人で売る?」→商人化の目的はランダム排除。引きの棚を持ち込むのは目的と矛盾していた。
- 雑貨区画(弾薬10/救急50/ワクチン500/サブ武器カード等)は**現行の固定カタログのまま変更しない**
  (現行実装に生成棚は存在しない=軽微指摘「回復は商人に既存」と整合)。

### 13-2. #1 ボット購買ポリシー(B0で実装・決定的)
- 現行playtestDriverは商人圏で`closeShop()`即閉じ=§12-1導入後は経済系の計測が全て無効(監査#1)。
  B0で「購買→閉じる」へ変更する。ポリシーは**乱数なしの優先順位**で決定的に:
  ① HP<50% かつ scrap≥救急価格 → 救急を1個
  ② 装備区画の**最安の次の一段**から順に、scrapが価格以上の間buy(安い順=進行の均し)
  ③ 買えなくなったらclose。
- 購入イベント(品目/価格/残scrap/時刻)をB0計測器のログへ流す。ユニットテスト=同じ店・同じ所持で
  購入列が常に同一であること。
- **B0の基準線はこのポリシー着地後に取る**(買わないボットの経済数値は基準線として無意味)。

### 13-3. #4 スクラップ二重負荷の計測分離(B0)
- **収入は流路別に集計**: キル/箱・POI/レベルアップ③枠(+50)の各流路を分けてログする
  (合計だけだと「どの蛇口を絞るか」の調整ができない)。
- **支出も流路別**: 装備購入/回復購入を分ける。
- 旧規則時代(ナイフ提示・装備レベルアップ混在)の収入実測値は**新経済の基準線に流用しない**。
  B0=計測器+新規則での基準線取り直し、B6=同じ計測器で前後比較(器を変えない)。

### 13-4. #5 B0計測器一覧への追加2点(§12-2の8点に追記→計10点)
- **DDA係数の新旧比較**: 旧算式と新算式(runBuild.length×0.5・cap+3.0)の係数値を毎ラン並記ログし、
  乖離幅を確認する(切替(B5)前に振れ幅を知る)。
- **枠充足タイミング**: 6枠それぞれが埋まった時刻/レベルと、特殊装備3種の入手時刻(POI到達)。
  「後半ほど派手」の進行曲線が実際に立っているかを直接見る指標。

### 13-5. #7 特殊装備POIのセクター設計(detourPoi機構の第2割り当て)
- **4セクター機構は不変**(重複なし・空きなしの既存テストは触らない)。特殊装備POI×3
  (兜=頭…ではなく スロット順: 体=鎧/腕=小手/アクセ=兜)は、裏ボスを除いた**同じ非ボス3セクターへ
  独立にシャッフルで1つずつ**割り当てる(assignDetourSectorsと同型の第2関数・rng注入・テスト同型)。
  結果、各非ボスセクターに寄り道POI 1+特殊POI 1が同居する(距離帯が違うので衝突しない)。
- **距離は未確認汚染帯(5000〜7500)の25/50/75%=5625/6250/6875**(スロットごとに固定。
  乱数は方角だけ=detourと同じ原則)。廃病院の教訓(救済を深く置かない)の逆で、
  **特殊装備は救済ではなく強化なので「深さ=価値」で深くてよい**。角度散らし±30°も同機構を流用。
- 拠点解放→矢印はセクター単位の既存動線を流用(同セクターで矢印が2本になる。区別は絵/色で。
  絵素材=支給済み poi-kabuto/yoroi/kote.png)。
- 受け入れ条件(POIバッチ): 第2割り当ての不変条件テスト(3種が非ボス3セクターへ重複なし)/
  最大引き(?zoomlock=0.4)で描画・カリング破綻なし/中身は「そのスロットの特殊装備を選んで取る」UI。

### 13-6. #10 B2の受け入れ条件(新設)
B2(商人装備区画+価格表+サーバ上限9)の検収条件:
1. 装備区画が各スロットの「次の一段」だけを提示し、購入で1段進む(ユニットテスト)。
2. 価格表がデータ台帳(campaign.ts側)にあり、ハードコード散在しない。
3. 最上段スロットは売り切れ表示になり購入不可。
4. scrap不足時は購入不可(押せない/エラーでなく状態で分かる)。
5. 開閉を繰り返しても提示が変わらない(リロール不可の回帰テスト)。
6. レベルアップ3択にスキル以外が混ざらない(スキル専業化の回帰・§12-1)。
7. 練習ラン(ボスモード)は商人不在のまま=初期装備開始(★3裁定の回帰)。
8. typecheck/lint 0エラー。
- 軽微指摘の反映: 回復は既存の救急(50)で足りる=新規回復商品は追加しない。「条件12(選択率)」は
  ボット測定不可のため実機打刻に置換済み(§12-2)。その他軽微はDEVELOPMENT_LOG v0.25.3216の記載どおり。

## 14. 新スキル台帳(社長承認2026-08-13「全て採用」・実装はB3で台帳組込み+ガチャ再シミュ)
査定済み(既存30種との被り2件=チェインスパーク(跳弾と同構造)/リベンジフレア(反射神経と同一)を
削除し補完済み)。数値は叩き台。方針: 1スキル1軸成長/機能追加は延焼弾Lv2のみ/超レアLv3は遠慮なく強く。

| スキル(仮名) | レア度 | Lv1 / Lv2 / Lv3 |
|---|---|---|
| ビッグバレット | ノ | 弾サイズ×1.3 / ×1.5 / ×1.7(見た目=判定を同時拡大) |
| アイスショット | ノ | 鈍足20%1秒 / 30%1秒 / 40%1.5秒(氷片=キル時3片0.3倍・全Lv共通。ボスは鈍足対象外) |
| 吸血 | ノ | キル20%でHP+2 / +4 / +6(率固定・回復スキル軸の新設) |
| 延焼弾 | レア | 燃焼(秒2×3秒) / +炎床解禁(小・モロトフ資産流用) / 燃焼(秒4×4秒)+炎床大 |
| 処刑の衝撃波 | レア | フィニッシュ時 半径80・近接30% / 100・40% / 120・50%(KB共通。エスカレーション③の前哨) |
| グラビティショット | レア | キル20%で爆縮(引き寄せ・半径100) / 30%・120 / 40%・140(パニッシャーの逆ベクトル) |
| エコーショット | 超 | クリ時複製弾50% / 75% / 100%(crit-up/sniperとの重ねが売り) |
| 弾幕の王 | 超 | 反射弾のダメ・体勢削り ×1.5 / ×1.75 / ×2.0(v0.25.3219反射体勢の建て増し) |
| 血の履帯 | 超 | 軌跡の棘 2秒・秒2 / 3秒・秒3 / 4秒・秒4(スケーターと相性) |

負荷は全て1〜3/10(既存の弾/粒子/リングのプールで描く・強glow/投影影は使わない)。

## 15. B0発注文(計測器実装+基準線・2026-08-13 社長「実装してね」で着手)

### 15-1. スコープ(実装・Sonnetサブエージェント)
1. **`src/utils/runTelemetry.ts`(新設・純関数+モジュール状態・renderer非依存)**: 1ランぶんの計測台帳。
   - 出力(§12-2#6の8点+§13-4の2点=10点): ボス突入時スナップショット(装備スキル/プレイヤーLv/
     装備Tier/straps。**ディープコピーで保存**=実装精度の規律3)/レベルアップ提示回数+選択内訳counter/
     ナイフ到達Tier(箱由来)/**scrap収支の流路別**(収入: キル/箱/POI/レベルアップ③枠/その他、
     支出: 商人品目別)/商人購入ログ(品目・価格・残straps・時刻)/最終(プレイヤーLv・到達エリア・
     ラン時間・結果)/**DDA係数の新旧並記ログ**(現行式の値+新式 `count×0.5(cap+3.0)` の
     would-be値。B0時点のcountは装備スキル数で代用と明記)/枠充足タイミング(B0では空でよい・
     フィールドだけ用意=B1以降で記録)/**ownedSkills・ownedSkillLevelsのヘッドレス上書き口**/
     同行者あり・なし切替口。
   - 集計はkillTelemetryStateの型を踏襲(snapshot関数でコピー・生参照を保存しない)。ユニットテスト同コミット。
2. **ボスメーカー注入口(§12-2#7)**: 「スキル(複数)+各Lv+装備Tier+プレイヤーLv」を注入できる口を
   `bossTest.ts` 系に新設(XPが入らない部屋なのでLvは注入が前提)。TTKは既存bossClockDurationMsが正。
3. **ボット購買ポリシー(§13-2)**: playtestDriverの`closeShop()`即閉じを「購買→閉じる」へ。
   優先順位(乱数なし・決定的): ①HP<50%かつstraps≥救急価格→救急1個 ②(B2以降)装備区画の最安順
   =B0では該当なしとコメントで予約 ③close。購入はrunTelemetryへ記録。決定性のユニットテスト。
4. **ゲーム挙動の変更ゼロ**(計測器=読むだけ。変わってよいのはボットの購買だけ)。

### 15-2. 基準線(実装着地後・設計チャットが実行)
- ①ステージ別到達Lv分布: greedyボット30ラン(分布→TTKの順=§12-2#12)。
- ②現行最強2枠ビルドのボスTTK: **crit-up+sniper**(現行は同時持ち込み可・新表では両方超レアで
  共存不可=6枠化の比較対象として最適)。ボスメーカーで条件固定(ボス/装備Tier/Lv/シードを
  DEVELOPMENT_LOGに記録=再現可能に)。
- 結果はDEVELOPMENT_LOGへ記録。**B3のレア度表差し替え後には二度と取れない**。

### 15-3. 想定最強6枠ビルドの選定・固定(B0の設計チャット担当分)
- **主案(全火力・B5のTTK測定対象)**: 超=crit-up / レア=exploder+fire-shooter /
  ノーマル=sharpshooter+ricochet+punisher。
- **副案(遠距離型・B5で併測)**: 超=sniper / 他は主案と同じ。
- 意図: 現行最強2枠(crit-up+sniper)は新表で共存不可になるため、超1枠をどちらに使うかの
  2通りを測って§5-2のTTK予算(−25%以内)を判定する。

### 15-4. 受け入れ条件
1. runTelemetryの10出力が全てユニットテストで検証される(スナップショットのコピー性含む)。
2. ボスメーカー注入口: 注入した装備/Lv/スキルがランに反映される(テストまたはヘッドレス確認)。
3. ボット購買: 同一の店・所持で購入列が常に同一(テスト)。救急購入がtelemetryに載る。
4. 計測器OFF時(通常プレイ)にフレームコストを足さない(集計はイベント時のみ・per-frame走査なし)。
5. typecheck/lint 0エラー。ゲーム挙動の回帰なし(既存テスト全通過)。

## 16. 監査8巡目の反映(不合格12件+軽微10件・2026-08-13)

### 16-0. ★社長裁定待ち4件(体験に触れるため監査役と2人で決めない=チャットで裁定を仰ぎ中)
- ★A(指摘1): 持ち込み超レアが超レア枠を埋め、抽選・pityが止まる=上級者ほど後半の山が消える。
- ★B(指摘2): 枠が埋まった後半はカードが100% Lv+1になり「後半ほど新しい絵」が構造的に出ない。
- ★C(指摘3): §12-1のスキル専業3択で「取らない自由」(旧・装備1枚=逃げ道)が消えた。
- ★D(指摘4): §12-3★2の「18回≈Lv24〜25」は提示率60/80/80%時代の期待値。提示率廃止後は
  **18回=到達Lv19**で、裁定の根拠数値が崩れている。
★4件は2026-08-13に裁定済み(→§16-10)。

### 16-1. 指摘5: B0計測に基礎装備到達Tierを追加(発注済み)
ボス突入時スナップショットに**スロット別の装備Tier+系統(+特殊装備の有無)**を追加(B0エージェント
へ追補済み)。旧側(レベルアップ配布)の装備到達はB0でしか取れない。計測器出力は計11点。

### 16-2. 指摘6: ボスドロップ宝箱=特殊混入を撤去・基礎3択は維持
- A-2(宝箱=装備3択)は**維持**(ボス報酬の山場・§11-1)。ただし`generateEquipmentChoices`の
  **特殊装備混入(空きあり5%/空きなし10%)は宝箱経路でも撤去**(§12冒頭「特殊はPOI専任」の
  裁定を宝箱にも適用。裁定の動機=特殊の入手経路一本化、に忠実)。B2に含める。
- 宝箱の基礎3択のランダム性は「ボーナス報酬のブレ」であり、レベルアップ動線のブレ(社長裁定の対象)
  とは別物として残す(=採用しなかった指摘として社長へ1行報告)。
- 武器庫POI(100sでTier3確定)は**維持**(確定入手=ランダムではない。§13-1と矛盾しない)。

### 16-3. 指摘7: 武器庫の置換先から特殊装備スロットを除外
`armoryTargetSlot`(equipment.ts)は特殊装備を「Tier0=最弱」とみなし真っ先に置換する。
**特殊装備が入ったスロットは置換候補から除外**する(全スロット特殊なら武器庫は売り切れ表示)。
★1裁定(毎ラン3種確定)を黙って壊さないための機械的修正=B2に含める。

### 16-4. 指摘8: POI/商人が居ないステージの扱い
- **特殊装備POIはforest系屋外ステージ限定の要素**と明記(lab/洋館通路は寄り道POI機構自体が無い。
  これらのステージは特殊装備なしで設計されている前提を仕様化。将来必要になったら別裁定)。
- **M0(訓練)**: スキル提示なし(§2-4)+装備カード廃止で3択が空になるため、
  **M0はレベルアップメニューを出さない**(XPは入るがLvアップ時は自動でスクラップ+50と同等の
  無演出処理。チュートリアルの流れを止めない)。B1に含める。

### 16-5. 指摘9: 新スキル9種の実装バッチと判定定義
- バッチ表に**B7(新スキル実装)**を追加: §14の9種の効果配線+ghostSanitize.mjsのSKILLS
  ホワイトリストへ9キー追加(忘れると霊のスキルが黙って落ちる)+絵はエフェクト2分類に従う。
  B3(台帳・レア度表)→B7(効果配線)の順。B3時点では台帳掲載+抽選対象外フラグで先行可。
- 判定定義(叩き台・実装時にこの値から開始): 処刑の衝撃波の「近接30%」=**発動時の近接攻撃の
  表示ダメージ基準**/血の履帯の棘=幅24px・tick=250ms/グラビティショットの引き寄せ=
  爆縮中心へ120px/s×0.4s/ビッグバレットの拡大は**当たり判定と絵のみ**(貫通数・跳弾回数・
  壁衝突は不変)。
- **血の履帯・延焼弾の炎床は「判定を持つ床」=攻撃ヴィジュアル分類①(判定に揃える・大きくしない)**。

### 16-6. 指摘10: ボット購買の両輪化と保守則(B0追補済み)
- 実機オートパイロット(useGameLoop側のcloseShop即閉じ)にも同じ購買ポリシーを適用(純関数共有)。
- 購買上限の保守則: **購入後もstraps≥100を残す**(武器庫代の温存・叩き台)。ワクチン(500)は
  ボットは買わない(基準線を単純に保つ)。
- `MERCHANT_AVOID_RADIUS`(商人回避)は現状維持=ボットが商人へ寄った時だけ買う。回避解除で
  積極的に商人へ行くかはB6(検収計測)で必要になったら判断。

### 16-7. 指摘11: 商人装備区画の詳細仕様(§13-1の補完)
- **未装備スロット**: 両系統のTier1を**2枚並べる**(初回だけ系統選択がある=指名買いの内)。
- **装備済みスロット**: 現在の系統の「次の一段」1枚のみ。**系統乗り換えは商人では不可**
  (乗り換えは宝箱/武器庫の既存機構に残る)。
- **特殊装備スロット**: 売り切れ表示(商人は武将装備を上書きしない=指摘7と同じ原則)。
- **紅き夜・ボス戦中は商人が開かない(既存仕様のまま)**: 「ボス直前の駆け込み補強は不可」を
  仕様として明記(準備はボス前に済ませる、という緊張を残す)。

### 16-8. 指摘12: 特殊POIの「選んで取る」→「即入手」へ
SPECIAL_DEFSはスロットごとに特殊1種のみ=選択肢が存在しない。POI到達で**そのスロットの特殊装備を
即入手**(選択UIなし)。「運は場所に限定・中身は固定」で社長裁定の動機(ブレ対策)はむしろ強く満たす。
将来スロットに2種目が増えたら選択UIを足す。§13-5の受け入れ条件を読み替え。

### 16-9. 軽微10件の確定
1. 裏ボス無しステージの第2割り当て=**detourの割り当てと同じ3セクターを使う**(裏ボス無しなら
   assignDetourSectorsが実際に使った3つ)。
2. 初期所持9種は**ガチャ除外に入れない**(Lv上げ経路=ガチャ被りを残す。返金増はB3再シムで測って
   from価格側で調整)。※経済に触れるためB3結果は社長報告に含める。
3. §12-3★2の「どのレア度も所持>枠」は**ノーマル/レアに限る**と訂正(超レアは0種=ガチャ専用のまま)。
4. flashy代理指標(中央値≥2)は初期9種でほぼ飽和=**指標としては残すが合否条件から外す**。
5. 価格表の置き場=**gameStore.tsの既存SHOP_*定数群に揃える**(§13-6#2を訂正。台帳を2箇所に
   割らない=既存の場所が正)。
6. スクラップ埋めで同一カードが複数並ぶ場合=**1枚に畳んで残りは空表示**(3枚未満の提示を許容)。
7. §8-9の行番号は**全て削除し関数名参照へ**(B-20の決定を§8-9にも適用)。
8. pityはカテゴリロールに**優先する**(pity発火時はそのカードを新規・超レアで確定=ロールを飛ばす)。
9. 「3枚重複なし」=**引き直しはレア度ロールから**(カテゴリは維持。Lv+1側の重複は対象プールから
   既出を除いて引く)。
10. 持ち帰り装備で特殊を持ち越したスロットの特殊POI=**訪問時にスクラップ200へ変換**(空振りに
    しない・叩き台)。

### 16-10. ★4件の社長裁定(2026-08-13)と波及の確定
- **★A=持ち込み廃止(「持ち込まないでやってみようか」)**: ラン中ビルドは**ラン中取得のみ**。
  裁定記録1(持ち込み1枠)・§2-3(出撃前の持ち込みUI)・§5-1条件3(MAX_CARRY_SKILLS=1)は
  **全て上書き=持ち込み0**。出撃前のスキル選択UIは撤去(装備・同行者の選択は残る)。
  pity・曲線は無改変で全員に働く(枠が事前に埋まる経路が消えたため指摘1は根本解消)。
  「やってみようか」=試行の裁定。実機の手応えで復活も有り得る前提で、撤去は復活可能な形にする。
  ガチャの価値は「超レアの解禁+開始Lv(ハイブリッド§11-0)」に一本化=ガチャ意義は保たれる。
- **★B=B-1**: Lv+1を後半の派手さの担い手にする。**Lv2/Lv3で効果の絵を段階強化・Lv3到達=
  「覚醒」演出**(金オーラ+SE+一瞬のカットイン風。判定は変えない=分類②派手さの絵)。
  絵の実装はB7に含める(B1では覚醒イベントの発火だけ仕込む)。受け入れ条件に
  **「ボス戦中のLv3スキル数(中央値)」をB6の記録指標に追加**(合否条件ではなく観測から)。
- **★C=C-1**: レベルアップは**スキル3択+常設4枚目「スクラップ+50」**(いつでも取らない自由)。
  さらに**リロール**(スクラップ払い・初回50/以後ラン中+50ずつ値上がり・回数無制限)と
  **バニッシュ**(無料・ラン中2回まで・そのスキルをラン中の抽選から除外)を追加(提案①採用)。
  数値は叩き台。リロールで3枚全て引き直し(スクラップ択は残る)。
- **★D=D-1**: ★2(初期9種)の枯渇判断は**B0の到達Lv分布を見てから**。Lv19超が頻出なら再裁定。
  C-1のスクラップ択が受け皿になるため、それまで設計変更なし。
- 提案②(名前つきシナジー)・④⑤は**未裁定のまま**(今回の裁定に含まれない。持ち出さない)。

## 17. B1発注文(スキルドラフト+レベルアップ専業化・裁定4件反映済み)

### 17-1. スコープ
1. **`src/utils/runSkillDraft.ts` 新設**(純関数+テスト同コミット): レア度枠(超1/レア2/ノ3)/
   §1-2bの曲線/pity(§16-9: カテゴリロールに優先・新規超レア確定)/§12-2#2の抽選手順
   (カテゴリロール50:50→レア度ロール→重複なし。引き直しは§16-9の規則)/リロール価格の算出/
   バニッシュ集合の適用。
2. **store(gameStore)**: `runBuild: SkillKey[]`+各スキルのラン中Lv台帳(`skillLv`専用フィールド・
   §12-2軽微)/取得・Lv+1適用アクション/リロール・バニッシュ・スクラップ択のアクション/
   Lv3到達で「覚醒」イベント発火(演出はB7・ここではフラグ+SE用フックのみ)。
3. **レベルアップUI**: スキル3択+常設「スクラップ+50」+リロール/バニッシュボタン。
   React再描画規律(プリミティブ購読)厳守。
4. **持ち込み0化**: 出撃前のスキル持ち込みUI撤去(復活可能な形=データと関数は残しUIと配線だけ落とす)。
   `MAX_CARRY_SKILLS`参照箇所の整理。ボスモードの注入(bossTestGhostSkill/selectedRunSkills)は
   **計測用としてそのまま残す**。
5. **M0(訓練)**: レベルアップメニュー非表示(§16-4。自動スクラップ+50・無演出)。
6. **B-19全数洗い**: 全スキルを「ラン中取得で即機能するか」で洗い、駄目なものは
   `RUN_DRAFT_EXCLUDED_SKILLS`へ(§12-2#12。判断に迷うものは★未決へ書いて止まる)。
7. **DDA係数切替はまだしない**(B5)。ガチャ・レア度表も触らない(B3)。

### 17-2. 受け入れ条件
1. §5-1の機械的不変条件(条件3=持ち込みは「無し」へ読み替え)+§12-2#2の抽選手順がテストで固定。
2. リロール: 価格系列(50,100,150…)・全引き直し・スクラップ択残存がテストで固定。
3. バニッシュ: ラン中2回上限・除外がラン終了でリセット。
4. greedyボット: §12-2#5のポリシー(充足<4は新規優先)+スクラップ択は「取るものが無い時のみ」。
   決定的(テスト)。
5. M0でレベルアップメニューが出ない/通常ステージでは毎レベルアップ提示。
6. 既存セーブ・霊データで起動してもクラッシュなし。typecheck/lint 0エラー。

### 17-3. B1検収記録(設計チャット・2026-08-13)
検収結果=**合格**(typecheck/lint 0・受け入れ条件6点充足・関連テスト89 passed)。実装チャットが
range外として持ち越した下記3件は、検収で以下のとおり確定:
- **初期所持9種の追加=B3が正**(裁定7「B3に含める」+§12-2#12のB3定義どおり。B1の漏れではない)。
- **flashy×2重み=B3が正**(§12-2#12「B3=…flashy再付与」どおり)。
- **タグ機構を作らない=正**(持ち込み0化でsortieOnlyの前提消滅。RUN_DRAFT_EXCLUDED_SKILLS+
  dog-runの動的ゲートで足りている)。
- 実装判断2件を設計として追認: **バニッシュ=除外1枚だけ差し替え**(全引き直しだと無料リロールと
  等価になり経済が壊れる)/**差し替え時にpityは掛け直さない**(pityは1ドラフトイベント1回)。
- scrap-builder/warm-upの除外(取得時点で効果の柱が発火不能)も追認。B3で「ガチャ排出からも
  外すか」を再確認する(ガチャ側に残ると持ち腐れ表示になる)。

#### (原文)★B1実装中の未決(実装チャット記録・2026-08-13)
§17-1の7点だけを実装範囲とし、以下は**判断に迷ったため実装せず**ここに記録する(採否は社長/設計チャット)。

1. **初期所持9種の追加(§12-3★2/§16-9点2)は実装していない。** 該当箇所(§10★1・§12-3★2)は
   「B1の受け入れ条件に含める」「B1が上書き」と書かれているが、**§17-1の7点(発注文そのもの)には
   `DEFAULT_OWNED_SKILLS`/`ensureDefaultOwnedSkills`への言及が一切無い**。「§17-1スコープ7点+
   §17-2受け入れ条件6点が全て」という指示に従い、範囲外として実装しなかった。ノーマル5+レア4種の
   初期解禁(貫通/跳弾/パニッシャー/アタックシューター/スラッシャー、ファイアシューター/
   ボムカウンター/エクスプローダー/ナイト)は`ensureDefaultOwnedSkills`(campaign.ts)1箇所を
   直せば即着手できる状態。
2. **ノーマル抽選の`flashy`×2重み(§2-1末尾/§2-5)は実装していない。** タグ表(§2-5)自体は
   後続の監査で「flashy再付与はB3(レア度表作業と同時)」と明記され、§17-1のB1スコープにも
   `flashy`/`sortieOnly`/`requires`いずれのタグ機構も挙げられていない。§12-2#12のバッチ表上書きが
   `RUN_DRAFT_EXCLUDED_SKILLS`だけを受け皿として名指ししていることとも整合するため、B1では
   レア度カーブ(§1-2b)とpityのみを実装し、flashy重みは持ち越した。
3. **`sortieOnly`/`requires`タグはcampaign.tsに追加していない。** §11-1 A-3/A-4で「requiresは作らない」
   「実対象はdog-runのsortieOnlyのみ」と確定した後、持ち込み0化(★A)で「sortieOnly=持ち込み専用」の
   前提自体が消えたため、B-19の除外は全て`RUN_DRAFT_EXCLUDED_SKILLS`(常時除外)+dog-runの犬装備
   チェック(動的1件のみ・`RunSkillDraftInput.dogEquipped`)に統合した。タグをデータに足す形は取っていない。

### 15-5. 基準線①の結果(2026-08-13・v0.25.3230のクリーンworktreeで計測=B1混入なし)
- greedyボット(persona=standard/skill=master)30ラン×15分相当・4クラス輪番・シード0〜29:
  **到達Lv 中央値10・最大11・最小6(hist: Lv6×1/Lv9×3/Lv10×13/Lv11×13)・死亡0**。
  scrap収入は+0〜+140(中央値+58)・支出0(商人購買条件のHP<50%に達しなかった=死亡0と整合)。
- **★D(§16-10)ここで決着: 枯渇点(スキルカード18回=到達Lv19)は実測分布のはるか上**。
  15分ランの実測Lv10前後に対しLv19は非現実的=初期9種のままで終盤スクラップ化は実質発生しない。
  再裁定不要(D-1の条件を満たした)。
- 留意: ボットは死なず15分張り付き=実機の人間プレイより長め・深めの分布のはず(それでもLv11止まり)。
  ステージは既定(forest系)。lab/洋館は未計測(特殊POI/商人も無いステージ=§16-4)。

### 15-6. 基準線②の結果(2026-08-13・ボスTTK・ヘッドレス5ラン)
- 条件(完全固定・再現可能): crit-up Lv3+sniper Lv3/装備3スロットTier5(firstライン=防護/火力/クリ)/
  プレイヤーLv10(基準線①中央値)/クラスwarrior/ボス=グレン(HP6000×2形態・当たり判定2倍=M7実仕様)/
  skill=master・シード0〜4/新設 b0TtkBaseline.test.ts(B0_TTK=1ゲート・既存コード無変更)。
- 結果: **5/5撃破(両形態)。総経過時間(出現→完全撃破)=中央値128秒・平均147.6秒**(106/60/128/212/232)。
- **★TTK指標の訂正(計測で判明した設計の穴)**: §12-2#7の「bossClockDurationMsが正」は、交戦
  ヒステリシス(離れるとリセット)により同一条件でも16〜182秒とブレて基準線に使えない(既存の
  製品挙動でありバグではない)。**基準線・B5比較の正=「ボス出現→完全撃破の総経過時間」**とする。
  bossClockはゲーム内機構としては無変更(この訂正は計測の物差しの話のみ)。
- B5のTTK予算判定(−25%以内): 6枠ビルド(§15-3の主案/副案)を**同条件・同指標**で測り、
  中央値128秒と比較する(−25%ライン=96秒)。

## 18. B2発注文(商人装備区画+価格表・2026-08-13)

### 18-1. スコープ
1. **商人の装備区画**(§13-1+§16-7が仕様の全て):
   - 装備済みスロット=現在の系統の「次の一段」1枚のみ(買うと1段進み、商品は自動で次段へ)。
   - 未装備スロット=両系統のTier1を2枚並べる(初回だけ系統選択=指名買いの内)。
   - 系統乗り換えは商人では不可。特殊装備スロット=売り切れ表示。最上段到達=売り切れ表示。
   - scrap不足=状態で分かる(押せない見た目)。購入は既存buyShopItem系の正規経路+runTelemetry記録。
2. **価格表(叩き台・B6計測で調整前提)**: `EQUIP_SHOP_COST_BY_TIER = [40, 80, 120, 160, 200]`
   (Tier1→5)。置き場は gameStore.ts の既存 SHOP_* 定数群(§16-9の5=台帳を2箇所に割らない)。
3. **宝箱の特殊混入撤去**(§16-2): generateEquipmentChoicesの特殊5%/10%をボスドロップ宝箱経路でも
   撤去(基礎3択は維持)。レベルアップ経路はB1で装備カード自体が消えているため対象は宝箱のみ。
4. **武器庫の置換除外**(§16-3): armoryTargetSlotは特殊装備が入ったスロットを置換候補から除外。
   全スロット特殊なら売り切れ扱い。
5. **回復=既存の救急(50)のまま**(新商品なし=確認のみ)。
6. **サーバ上限9**(§10-5): ghostSanitize.mjsのownedSkills上限8→9。
7. **ボット購買②の実装**(§13-2): botShopPolicyに「装備区画の最安の次の一段を、straps≥100温存則の
   範囲で安い順に買う」を追加(B0で予約済みのコメント箇所)。決定性テスト更新。

### 18-2. 受け入れ条件(§13-6の8点+)
§13-6の1〜8(次の一段のみ提示/価格台帳/売り切れ/不足時状態/リロール不可回帰/スキル専業回帰/
練習ラン商人不在回帰/typecheck・lint 0)に加え:
9. 宝箱から特殊装備が出ない(回帰テスト)。
10. 武器庫が特殊装備を上書きしない(テスト)。
11. ボット購買が装備を買い、購入がrunTelemetryに載る(テスト)。
12. 商人UIの追加購読はプリミティブ/shallowのみ(React再描画規律)。

### 18-3. B2検収記録(設計チャット・2026-08-13)
検収結果=**合格**(typecheck/lint 0・対象テスト53 passed・受け入れ条件12点中11点充足)。
- **条件10(武器庫の特殊上書きガード)=対象消滅により充足**で確定: 武器庫POIの報酬はv0.25.2533で
  装備→Tier3銃に変更済みで、armoryTargetSlot(装備置換)は死コードだった(監査8指摘7は
  死コードへの指摘だった)。純関数側の特殊除外は字義どおり実装・テスト済み(将来復活時の保険)。
  実挙動の武器庫は装備を触らない=武将装備を壊す経路は存在しない。§16-3はこの記録が上書き。
- ボットの未装備スロット系統選択=index0固定は実装既定として追認(実UIは両系統を提示・バランス影響なし)。

#### (原文)★B2実装中の未決(実装チャット記録・2026-08-13)

- **★1(条件10「武器庫が特殊装備を上書きしない」の対象が実は無い)**: §16-3/§18-1の4は
  「`armoryTargetSlot`(equipment.ts)が特殊装備スロットを置換候補から除外する」という仕様。
  だが実装時に判明: **v0.25.2533(DEVELOPMENT_LOG「§6.24-W実装: 武器庫の報酬を装備→Tier3銃へ変更」)
  で「武器庫」POI(`updateArmory`・`src/store/gameStore.ts`)の付与ロジックは既に
  `rollEquipment`/`armoryTargetSlot`/`equipDefOnPlayer`経路を廃止し、Tier3の**銃**を渡す方式へ
  切り替わっている**。つまり現在の`armoryTargetSlot`はゲームプレイのどこからも呼ばれていない
  (自テスト`equipment.test.ts`からのみ呼ばれる純粋な死にコード)。武器庫は現在も100スクラップを
  要求する(`ARMORY_SCRAP_COST`)が、対価は装備ではなく銃。
  - **実施したこと(仕様変更なし・安全側)**: `armoryTargetSlot`自体は文言どおりに直し、
    特殊装備スロットを除外+全スロット特殊ならnull(売り切れ相当)を返すようにした
    (`src/data/equipment.ts`)。テストも更新(`src/data/equipment.test.ts`)。これにより
    「関数の契約」としては条件10を満たすが、**実際の武器庫POI(gameStore.updateArmory)にこの
    関数を再び繋ぐことはしていない**(それをやると「武器庫の報酬=銃」というv0.25.2533の確定仕様を
    覆す=CLAUDE.md「仕様変更のルール」に抵触するため、実装チャットの裁量では行わない)。
  - **設計チャットへの確認事項**: §16-3/§18-1の4が想定していた「武器庫が特殊装備スロットを
    上書きする」という前提コード経路は現存しない。次のいずれかを裁定してほしい:
    (a) この項目はv0.25.2533の変更で**既に解消済み(装備を扱わないので上書きもしない)**として
        条件10をクローズする / (b) `armoryTargetSlot`を`updateArmory`へ再接続し武器庫の報酬を
        装備へ戻す(仕様変更) / (c) 別の経路(POI/宝箱等)を指していた記述違いなら該当箇所を特定。
    実装チャット推薦=(a)(現状追認・変更コストゼロ)。ただし推薦の採否は設計チャットで。
- **その他の実装上の既定(仕様変更ではないため未決に含めないが、確認歓迎)**:
  - 商人の未装備スロット「初回だけ系統選択」で、**ボットが選ぶ系統**は
    `EQUIP_LINES_BY_SLOT`の先頭(index0=body:protection/arms:firepower/accessory:crit)固定
    (`botShopPolicy.ts`)。プレイヤー向けUIは両系統とも表示するため実プレイには影響しない。
  - 商人購入の`runTelemetry`品目キーは装備定義ID(例: `body-protection-3`)をそのまま使用
    (既存の`buySkillCardFromShop`がスキルキーをそのまま使うのと同じ流儀)。

## 19. B3発注文(レア度表+初期9種+flashy+ガチャ再シム・2026-08-13)

### 19-1. スコープ
1. **レア度表差し替え**(§4の新表・campaign.ts): crit-up/sniper→超、sharpshooter/ricochet/punisher→
   ノーマル(flashyタグ)、benkei/reflex→ノーマル、他は§4のとおり。ガチャ排出も同じ表
   (2つのレア度表を持たない)。ガチャの確率・価格・pityの**コードは変更しない**(表の参照先のみ)。
2. **flashy×2重み**: ノーマル抽選でflashyタグ持ちの重みを2倍(runSkillDraft・§2-1/§4)。
   flashyタグの付与対象は§4の表の(flashy)表記どおり。
3. **初期所持9種**(§12-3★2): ensureDefaultOwnedSkills へ ノーマル5=sharpshooter/ricochet/punisher/
   attack-shooter/slasher+レア4=fire-shooter/bomb-counter/exploder/knight。
   **ガチャ除外には入れない**(§16-9の2=被りLv上げ経路を残す)。
4. **新スキル9種の台帳掲載**(§14・先行掲載のみ): SKILLS台帳へ名前/説明/レア度/Lv1〜3を追加。
   **効果配線はB7**なので、それまで①ドラフト抽選対象外 ②ガチャ排出対象外 のフラグで完全に眠らせる
   (効果の無い当たりを引かせない)。ghostSanitizeホワイトリストへの追加もB7で行う(B3ではしない)。
5. **scrap-builder/warm-upをガチャ排出からも除外**(§17-3の再確認の結論): 持ち込み廃止+ドラフト
   除外により効果への到達経路が消滅=引かせると死に景品になるため。※2種の作り直し(ラン中取得で
   機能する新効果への差し替え)は仕様変更=社長裁定待ちとして★に記録だけする。
6. **ガチャ経済の再シミュレート**(§1-4): 既存のシミュレート機構(無ければ簡易スクリプト)で
   新表+初期9種(被り返金増)の影響を計測し、結果の数字を報告に含める(**価格・確率はいじらない**。
   調整が要りそうなら数字と選択肢を報告するだけ)。

### 19-2. 受け入れ条件
1. §5-1の1〜8(特に4=fixedGuardians無改変で通る/5=ガチャのコード変更なし)。
2. flashy重みのユニットテスト(ノーマル抽選でflashy:非flashy=2:1)。
3. 初期所持9種のテスト(新規プレイヤーで9種所持・超レア0)。
4. 新スキル9種: 台帳に存在するが、ドラフト・ガチャのどちらからも絶対に出ないテスト。
5. 既存セーブ(旧レア度で取得済みのスキル・霊データ)で起動してもクラッシュ/消失なし。
6. typecheck/lint 0エラー。ガチャ再シムの結果数値が報告に含まれる。
