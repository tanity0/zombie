# SKILL_BUILD_REDESIGN — スキルのラン中レベル制ビルド(レア度枠 1/2/3)

作成 2026-08-12(v0.25.3190)・設計チャット。ステータス: **監査6巡目=不合格12件+軽微(要旨は
DEVELOPMENT_LOG v0.25.3211)。並行して社長提案「装備は武器箱へ移す」(レベルアップ=スキル専業/
箱=装備3択)が裁定待ち — 採用なら#3/#9/#12等を構造ごと吸収するため、§12は武器箱裁定と
監査6巡目の反映を一体で書く。★社長裁定残: (i)武器箱案のGO (ii)#4=ガチャ0回の初期ビルド同一問題
(初期所持を枠容量より増やす or ガチャ0回を条件11の対象外と明記)。矛盾する旧記述は最新節が勝つ。
次=§12反映 → 監査7巡目 → 通過後B0発注**。

## 0. ゴール(社長の言葉のまま)
- 「後半に行くほどゲームプレイに派手さや違いが出てくるなら…**またやりたくなる何かが必要**」
- 「今は固定で2枠スキルを装備させているが、これを**ゲームプレイ中のレベル制にシフト**する」
- 「バランスについて、**レア毎に縛れば**? それを踏まえて**レア度も改めて算出**。
  **超レアは1種まで/レアは2種まで/ノーマルは3種まで/合計6個のビルド**」
- 「**スキルビルドはできるだけシンプルに、かつある程度ぶっこわれも許容で　じゃないと面白くない**」
  (社長指示 2026-08-12・v0.25.3198)
  - **解釈(設計原則として全節に優先)**: ①仕組みは足すより削る(迷ったら簡素な方)。
    ②強すぎる組み合わせは**潰す対象ではなく売り**。バランス装置(§5のTTK予算等)は
    「壊れているか監視する物差し」であって「壊れを禁止する締め付け」ではない。
    ③壊れ対策の追加機構(個別ナーフ表・組み合わせ禁止則など)は**作らない**。

## 1. 新しいラン内ビルドの形

### 1-1. 枠(社長指定・確定)
| レア度 | 同時所持の上限 |
|---|---|
| 超レア | **1種** |
| レア | **2種** |
| ノーマル | **3種** |
| 合計 | **6種** |

### 1-2. 取得の流れ
- **出撃前の持ち込みは1枠**(社長裁定v0.25.3192・確定。0枠案は却下=ガチャで当てた超レアを確実に
  使う手段と、引きが外れたランの下限保証を残す)。持ち込みもそのレア度の枠を消費する。
- **スキルの提示は「スキル2枚+装備1枚」の3択**として出す(監査#4/#15対応)。
  - 旧案の「装備3択+スキル1枚=4択」は廃止: スマホ1画面の3択レイアウトを維持し、かつ
    **スキル同士を比べて選ぶ=ビルドを組む体験**を作る(1枚提示では取る/取らないの二択にしかならない)。
  - スキル提示が起きなかったレベルアップは従来の装備3択のまま。
  - 装備1枚が常に並ぶ=「今回は取らない」の逃げ道(監査#13対応。リロール/バニッシュはv1では作らない)。
- 提示条件: ①ガチャで所持 ②このランで未取得 ③そのレア度の枠が空いている
  ④**発動条件を満たせる**(監査#8対応・§2-5)。
- 取得スキルのLvはガチャの所持Lv(ラン中のスキルLv上げはv1ではしない)。
- ボス開始チェストの即時3レベルアップ(`BOSS_START_CHEST_LEVELS`)も提示対象=**ボス前の最終補強**として
  意図に組み込む(監査#1で「事故」と指摘されたが、仕様として明記して裁定対象にする)。

### 1-2b. ★進行曲線(監査#1/#2/#7対応・「後半ほど派手」の心臓)
スキル提示の発生率とレア度重みを**プレイヤーLvで傾斜**させる(叩き台・実機調整前提):

| プレイヤーLv | スキル提示の発生率 | ノーマル / レア / 超レア |
|---|---|---|
| Lv2〜4 | 60% | 85 / 15 / 0 |
| Lv5〜7 | 80% | 60 / 32 / 8 |
| Lv8〜 | 80%(§9-3裁定=③装備専用ターンを2割残す) | 45 / 38 / 17 |

- **超レアの天井(pity)**: Lv8以降、超レア枠が空のままなら**次のスキル提示に超レア候補を1枚確定で混ぜる**。
  ⇒「最も派手な枠が4割のランで空のまま終わる」(監査#2の計算)を潰す。
- 序盤(Lv2〜4)に**抽選の**超レア0%なのは意図: 開幕で最強枠が埋まると**後半に変化が残らない**(ゴールの逆)。
  持ち込み(★#9裁定=例外として許容)はこの縛りの外=配牌であって抽選ではない。
- 該当レア度の候補が空なら隣のレア度へ流す(スキル提示イベント自体は空振りさせない)。

### 1-3. 枠の外に置くもの
- **守護霊系(guardian-spirit / ghost-helper / ghost-slayer)は「同行者」枠へ独立**(社長裁定v0.25.3192・確定)。
  - **配線(監査#10対応)**: `beginGhostOnlineRun` / `selectedGhostMode` は現在 `pendingSkills` から
    helper/slayer モードを決めている。同行者枠の採用時は**同行者枠の選択値**を渡す配線に変更する
    (黙って壊れる箇所として明記。B4の受け入れ条件に含める)。
  - **収支(監査#12対応)**: 旧仕様では霊が2枠の1つを食っていた。同行者枠化は**純増強化**になる。
    社長裁定v0.25.3192: **(a)据え置き**で確定(枠を跨ぐ例外則を作らない)。§5-2のTTK予算で一括監視し、
    予算超過が実測で出たら(b)「同行ランは超レア枠を消費」へ落とす、が既定の後退手順。
- **警察署の報酬3種(poi-*)は6枠の外**(従来どおり直接付与・変更なし)。

### 1-4. ガチャ(開発施設)との関係(監査#11対応=「無改変」の主張を撤回)
- 「所持」の意味は「装備できる」→「**ランの抽選プールに入る+持ち込み候補になる**」へ。
- **レア度表の差し替えはガチャの排出・返金経済に波及する**(排出プール母数/Lv抽選分布/被り返金10/30/50)。
  B3で**再シミュレートを必須化**: 全種MAXまでの期待ラン数(現行基準107ラン・campaign.tsコメント)を
  再計算して DEVELOPMENT_LOG に記録し、乖離が大きければ価格/確率の追従は**別途社長裁定**
  (この設計書では触らない)。
- 確率・価格・pity・Lv機構のコードは触らない(変わるのは表が指すレア度だけ)。

### 1-5. 「またやりたくなる」の装置(監査#4対応・v1の範囲を明示)
- v1で作る: **配牌(進行曲線×抽選)+スキル2枚からの選択+レア度枠の制約+リザルトにビルド6枠を表示**
  (「今回のビルド」がランの顔になる=もう一度回す動機)。
- v1で作らない(意図的スコープ外・実測後に判断): シナジーボーナス/リロール/バニッシュ/ビルド図鑑。
  器が回ってから足す(装置を最初から盛ると調整不能になる)。

## 2. 実装仕様

### 2-1. 抽選と枠の純関数(`src/utils/runSkillDraft.ts`・テスト同コミット)
- `runBuildCapacity(rarity)` = {super:1, rare:2, normal:3}。
- `canAcquireRunSkill(runSkills, key)`: 枠判定の**唯一の出どころ**(UI/抽選/持ち込み検証が全てこれを通る)。
- `draftRunSkillPair(owned, ownedLevels, runSkills, playerLevel, rand)`: §1-2bの曲線で2枚選ぶ。
  超レアpityの状態は store に1フラグ(`superPityArmed`)。
- **ノーマル内の重み**: `flashy` タグ(§2-5)持ちを**×2**(監査#3対応=「絵が出るスキル」を積ませる)。

### 2-2. 取得(gameStore)
- `UpgradeOption` に `type: 'skill'` を追加し、`selectUpgrade` に取得分岐(監査#15)。
  取得= `player.skills` へ追加+`skillLevels[key] = ownedLevels[key]`。既存の `hasSkill`/`skillLevel`
  読み出しは無改変で効く。
- 死亡/勝利でラン内ビルドは破棄(従来の player リセットと同じ)。リザルトに6枠を表示(§1-5)。

### 2-3. 出撃前の持ち込み
- 新定数 `MAX_CARRY_SKILLS = 1`。**`MAX_EQUIPPED_SKILLS`(=2)は削除も変更もしない**(監査#9対応):
  固定守護霊プリセット20人が2スキル構成で、この定数は**霊のビルド検証**(`fixedGuardians.ts:117` throw)が
  使っている。霊は「旧仕様のプレイの写し」なので2スキルのまま正しい。**持ち込み枠と霊の検証を
  別定数に分離する**のがこの設計の正解(共有のまま1へ縮めると起動時throw+テスト赤)。

### 2-4. 移行・互換
- 既存セーブの `pendingSkills` 2件→先頭1件のみ有効(UIで見える形で)。守護霊系が入っていたら
  同行者枠へ読み替え(裁定2で確定)。
- **ボット**: 「スキル枠が提示されたら先頭を取る」**最小ポリシーを入れる**(監査#14対応=計測に必要。
  旧案の「ボットは選ばない」を撤回。装備との優先度は付けない=先頭固定で決定的)。
- 練習ラン/ベンチ/チュートリアルはスキル提示なし(現行どおり)。
- 守護霊のビルド写し(ghostAlly snapshot)は召喚時点の `player.skills` を写す=ラン中取得も写る(無改変)。

### 2-5. スキルのタグ(データ追加・campaign.ts)
| タグ | 意味 | 対象(初期) |
|---|---|---|
| `sortieOnly` | **持ち込み専用**(ラン中提示から除外。出撃時にしか意味が無い) | scrap-builder / warm-up |
| `requires` | 条件を満たす時だけ提示 | dog-run(犬を装備中のみ) |
| `flashy` | ノーマル抽選で重み×2(絵が出る系) | sharpshooter / ricochet / punisher / reflex / slasher / counter-master |

- 監査#8の「ラン中に取ると説明文が嘘になる」への回答: 嘘になるスキルはラン中に**出さない**
  (遡及付与や文面差し替えより単純で、掟「本文に嘘を書かない」に合う)。

## 3. UI
- レベルアップ画面: スキルカードはレア度色+「SKILL」バッジ+残枠表示(「レア枠 残り1」)。3択のまま。
- 装備メニュー: スキル欄→「持ち込み(1)」。
- ビルド確認はポーズ/リザルト(HUD常設はしない。React再レンダー規律)。

## 4. レア度の再算出(社長承認v0.25.3192・確定。crit-up/sniper超昇格・貫通/跳弾/巻き込みノーマル降格を含む)
**原則**: レア度=枠の税。**ボスに効く倍率ほど上位/ボス中立の雑魚特効は下位に下げて積みやすく**。

| スキル | 現 | 新(確定) | 理由(1行) |
|---|---|---|---|
| reaper / berserker / skater / overclock | 超 | **超** | ラン定義級/ボス直撃 |
| crit-up | レア | **超** | ボスクリ×5→5.5+全クリ倍率=ボス直撃の最上位 |
| sniper | レア | **超** | 対ボス実効(巨体・停止/遠距離)最大+100%×2軸 |
| exploder / fire-shooter / bomber / bomb-counter | レア | **レア** | 爆発系(ボスにも乗る・条件付き) |
| knife-master / combo-master / knight / rescue-signal | レア | **レア** | 倍率/生存の軸 |
| sharpshooter / ricochet / punisher | レア | **ノーマル**(flashy) | ボス中立4チャネル=積ませて派手にする側 |
| benkei / reflex | レア | **ノーマル** | 条件付き小強化 |
| attack-shooter / counter-master / slasher / last-magazine / warm-up | ノ | **ノーマル** | 技量/条件付き |
| gold-rush / scrap-builder / magnet / ghost-shooter / time-keeper / runner / seeker / dog-run | ノ | **ノーマル** | 経済/利便 |
| 守護霊3種 | 超/ノ | **枠外(同行者)** | §1-3 |
| poi 3種 | 超 | **枠外(施設報酬)** | §1-3 |

- ガチャの排出にも同じ表を使う(2つのレア度表を持たない)。経済への波及は§1-4の再シミュレートで測る。
- ノーマル枠(3)の中身が経済系で埋まって「見た目に何も起きない」件(監査#3)は、
  `flashy` ×2重み+§1-2bの曲線で対処。それでも埋まりが地味なら経済系の一部を
  `sortieOnly`(持ち込み専用)へ降ろす(実測後の調整点として明記)。

## 5. 受け入れ条件(監査・実装検収用)
### 5-1. 機械的不変条件(ユニットテストで固定)
1. ラン中ビルドがレア度枠(1/2/3)を一度も超えない。
2. 提示は「所持済み・未取得・枠が空き・発動条件を満たす」以外を絶対に出さない。
3. 持ち込みは最大 `MAX_CARRY_SKILLS`(1)で、持ち込みもレア度枠を消費する。
4. `MAX_EQUIPPED_SKILLS` と固定守護霊20人の検証(fixedGuardians)が**無改変で通る**。
5. ガチャの確率・価格・pity・Lvのコードに変更が無い(表の差し替えのみ)+B3で経済再シミュレートを記録。
6. ボット/練習/ベンチ/チュートリアルで意図した挙動(ボット=先頭取得/他=提示なし)。
7. 守護霊のビルド写しがラン中取得スキルを含めて写る。オンライン霊のモード選択(§1-3配線)が生きている。
8. 既存セーブで起動してもクラッシュ/装備消失がない。
### 5-2. ボス予算(監査#6対応・裁定5で確定)
9. **想定最強6枠ビルドの対ボスTTKが、現行の最強2枠ビルド比で−25%以内**(社長承認v0.25.3192)。
   測定はボスメーカー+ボットラン(B5)。超過したらレア度表(§4)を上げ直して再測定。
   目安の意図: 「少し早く溶けるのは6枠化の意図の内。読み合いのフェーズ(形態変化・技ローテ)を
   飛ばすほど溶けたら壊れ」——グレン形態2が見られなくなるようなら締める。
### 5-3. ゴール達成の計測(監査#14対応・B5)
10. ボス突入時の平均枠充足率 **≥4/6**(ボットラン測定)。
11. 超レア枠が埋まったラン比率 **≥80%**(pity込み・同上)。
12. スキルカード提示時の選択率(スキップ=装備選択の率)を記録(数値目標は初回実測後に設定)。

## 6. 裁定記録(全7件 社長承認・v0.25.3192「7件承認」)
1. 持ち込み1枠(0枠案は却下)。
2. 守護霊系は「同行者」枠として独立。
3. 同行者の収支は(a)コスト据え置き(TTK予算で一括監視・超過時は(b)へ後退)。
4. §4レア度表を承認(crit-up/sniperの超レア昇格・sharpshooter/ricochet/punisherのノーマル降格を含む)。
5. ボスTTK予算=現行比−25%以内。
6. 進行曲線+超レアpity(§1-2b)を承認。**「序盤は*抽選の*超レア0%」は骨格**(数字調整で消さない。
   ★#9裁定=持ち込みは例外として許容: 曲線・pityが縛るのは**抽選のみ**)。他の数字は実機調整前提の叩き台。
7. **機構系ノーマル3種(sharpshooter/ricochet/punisher)を初期所持へ追加**(新規プレイヤー対策)。
   実装: `ensureDefaultOwnedSkills` の既定所持(現: 守護霊)へ3種を追加。ガチャで被った場合の
   Lv上昇は既存機構のまま。B3に含める。

## 7. 実装順(バッチ・Sonnet発注単位)
- **B0(新設・監査2巡目#4/#5対応)**: **ベースライン採取**。現行(6枠化前)のうちに
  ①最強2枠ビルドのボスTTK基準値(ボスメーカー・条件固定) ②ステージ別の到達Lv分布(greedyボット30ラン)
  を実測し DEVELOPMENT_LOG に記録する。**B3でレア度表を差し替えた後には二度と取れない**ので必ず先頭。
  「想定最強6枠ビルド」もB0時点で設計チャットが選定・記録して固定する(再現可能に)。
- **B1**: `runSkillDraft.ts`(枠/抽選/曲線/pity純関数)+テスト。store: `MAX_CARRY_SKILLS`・`runBuild`台帳・
  取得アクション・ボットポリシー(§8-6)。
- **B2**: レベルアップUI(スキル2+装備1の3択)+装備メニュー持ち込み1枠化+リザルトのビルド表示。
- **B3**: レア度表差し替え(campaign.ts)+タグ追加+**初期所持3種の追加(裁定7)**+ガチャ経済の再シミュレート記録。
- **B4**: 同行者枠(裁定2/3で確定済み)。ghostOnline のモード配線含む。
- **B5**: 計測(§5-2/5-3)=ボスメーカーTTK+ボットランで充足率/超レア比率。
- B1→B2→B3 は順に依存。B4独立。B5はB3後。

## 8. 監査2巡目(16件)の確定事項(v0.25.3194反映。§1〜§7と矛盾したらこちらが勝つ)

1. **枠の台帳は専用フィールド `runBuild: SkillKey[]`**(store・ラン内)。入るのは
   **ドラフト取得と持ち込みだけ**。警察署報酬(poi-*)と同行者(守護霊系)は `player.skills` には
   入るが **`runBuild` には入れない**=枠・pity・提示に一切影響しない。`canAcquireRunSkill` と
   充足率計測は `runBuild` だけを数える。受け入れ条件追加: **poi(超レア)取得後も超レア提示とpityが
   生きている**(テストで固定)。
2. **スキル提示時に残す装備1枚の選定**: 既存 `generateEquipmentChoices` の生成順の**先頭1枚**
   (=進化があれば進化、無ければ補完/特殊、無ければナイフ/スクラップ)。新しい選定ロジックは書かない。
3. **装備側がカンスト([スクラップ, 回復]の2枚)の時**: スキル2+**先頭1枚(スクラップ)**の3択のまま。
   4択にはしない。
4. **進行曲線のLvしきい値は仮アンカー**。B0で実測したステージ別到達Lv分布に対し、
   **「中央値ランの30%地点/70%地点」で3帯を切り直す**(表のLv2-4/5-7/8+は初期値にすぎないと明記)。
   受け入れ条件10/11の測定条件を固定: **stage-1・所持=初期所持+代表12種(B0で列挙)・greedyボット30ラン**。
5. **B0(§7)でベースライン採取**(TTK基準値・想定最強6枠の選定と記録)。
6. **ボット**: `pickUpgradeByPolicy` の greedy に **skill バケツを「装備進化の次」**として追加
   (先頭固定=決定的)。§5-3の計測は **greedy** で回す。random は従来どおり(skillも等確率で拾う)。
7. **乱数注入**: `draftRunSkillPair(..., rng)` は**必ず注入**。本体は `Math.random`、ヘッドレス計測は
   シード付き(mulberry32)。`Math.random` 直呼びをdraft内に書かない(botUpgradePolicyの掟と同じ)。
8. **`superPityArmed` は resetGame で必ず false へ**(受け入れ条件+テスト。resetRunClocks型の
   「足したがリセット忘れ」の再発防止)。
9. **持ち込み1枠化の改修箇所(全列挙)**: `gameStore.ts` の slice(0,2) 3箇所
   (ロード時4432/セッター12648/runSkills 14044)→ `MAX_CARRY_SKILLS`。`MissionSelect.tsx` の
   506/949/960/971 → 持ち込みUI側=`MAX_CARRY_SKILLS`。**`fixedGuardians.ts:117` だけが
   `MAX_EQUIPPED_SKILLS`(=2・不変)を使い続ける**。受け入れ条件: この対応表どおりであること。
10. **移行順序**: ①`pendingSkills` から守護霊系を**先に**抜いて同行者枠の初期値へ写す
    ②残りの先頭1件を持ち込みへ。例: `[ghost-helper, crit-up]` → 同行者=有志・持ち込み=crit-up。
11. **同行者枠の仕様**: 装備メニューに「同行者」欄。選択肢=**なし/守護霊/守護霊(有志)/守護霊(猛者)**
    (排他1人)。**既定=なし**(現行の「霊スキルを装備しなければ霊は出ない」と同じ挙動)。
    既存セーブは#10の移行で初期化。ボスHP×1.6/G倍率は選択時のみ従来どおり適用。
12. **`requires` の全数洗いはB3の作業項目**(40種を1回洗ってタグ表を確定)。判明分: dog-run(犬装備中)、
    bomber(グレネードランチャー所持——**提示時点の所持で判定**)。knife-master 等の**代償付きスキルは
    提示する**(説明文に代償が書いてある=選択の自由。隠れ地雷ではない)。
13. **候補不足時の構成**: 有効候補2件=スキル2+装備1 / 1件=スキル1+装備2 / 0件=装備3(提示不成立)。
    不変条件②に追記: **同一提示内に同じスキルを2枚並べない**。
14. **ボス開始チェストの3連続レベルアップ**: 各レベルは通常ロール。ただし**pityの確定混入は
    チェスト1回の3連につき最大1回**(3連続で超レア3枚保証、にはしない)。
15. **霊の写しが最大6スキルになる非対称は受容**(霊=実ランの写し。強い霊はランが強かった証)。
    固定守護霊20人は2スキルのまま(旧仕様の写し・§2-3)。霊カードのスキル表示は最大6件を列挙できる
    レイアウトにする(B2)。
16. **`sortieOnly`(scrap-builder/warm-up)は持ち込み1枠の下で死に枠化しうる**。B3実測後の調整点として
    §4末尾に併記: (a)持ち込み専用として数値を強める (b)タグをやめて遡及付与に変える、のどちらかを裁定に上げる。

## 9. 監査3巡目(13件)の確定事項(v0.25.3196反映。§8と同じく、前段と矛盾したらこちらが勝つ)

1. **DDA(difficultyScaler)の再較正**: `playerPower` は `skillCount×1.0` を含み、6枠化で雑魚難易度が
   自動で上がってしまう(2枠時代の較正のまま)。⇒ skillCount項の係数を **1.0→0.4(叩き台)** に改め、
   **B0で新旧の playerPower 分布と escalation 発火率を比較して確定**する。§5-2のTTK予算はボス単体
   なので、この雑魚側の波及はB0/B5のDDA比較が受け持つ(受け入れ条件に追加)。
2. **greedyボットのスキルバケツ位置**: 「装備進化の次」だと毎回装備を取り充足率が測れない。
   ⇒ **「runBuild充足が4未満の間はスキル優先、4以降は装備優先」**(決定的・両方測れる)。
3. **③枠(スクラップ/ナイフ)の保全**: スキル提示率をLv8+で100%にすると装備専用ターンが消え、
   ナイフTier強化(③の25%)とスクラップ収入が停止する。⇒ **Lv8+の提示率は100%→80%**
   (装備専用ターンを2割残す)。B5で**ナイフ到達Tier分布とスクラップ収入の新旧比較**を計測項目に追加。
4. **#8(★裁定済み 2026-08-12「5の未決は推薦で実装」=推薦採用)**: **初期所持にレア1種
   (fire-shooter=爆発弾・絵が出る)を追加する。** 超レアは初期に配らない(ガチャの目標=
   「またやりたくなる」の縦軸として残す)。実装先: 初期所持の台帳(ensureDefaultOwnedSkills)へ
   fire-shooter を追加(B1の受け入れ条件に含める)。
5. **#9(★裁定済み 2026-08-12・同上=(b)採用)**: **超レアの持ち込みは例外として許容**。
   進行曲線・pityは**抽選のみ**を縛る規律とし、裁定6の文言を「**抽選は**序盤超レア0%」へ精密化する
   (裁定1の目的「ガチャで当てた超レアを確実に使う」を守る。持ち込みは計画=配牌の外)。
6. **ドラフト除外の専用定数 `RUN_DRAFT_EXCLUDED_SKILLS` = 守護霊3種+poi3種**。
   `GACHA_EXCLUDED_SKILLS` は流用しない(あちらは reaper を含み、reaperは**ドラフトに出すべき超レア**)。
7. **霊カードのスキル表示は最大9件**(runBuild6+poi3。同行者は player.skills に入らない)。§8-15の「6件」を訂正。
8. **提示発生率のロールも `draftRunSkillPair`(rng注入)の中**で行う。順序: **候補を先に集める→
   0件なら不成立(ロールしない)→候補ありなら発生率ロール**。1関数が「提示の有無+2枚」を返す(null=不成立)。
9. **`pickUpgradeByPolicy` の引数拡張**(runBuild/残枠を渡す)と既存テストの更新を**B1の受け入れ条件に明記**。
10. **dog-run は `requires` をやめ `sortieOnly` へ**(犬の有無は出撃時に確定している。ラン中に犬を失って
    枠が永久に死ぬケースを根から断つ)。`requires` タグ自体はB3の全数洗いのために仕様として残す。
11. **計測ラン(§8-4/§5-3)の持ち込みは「なし」で固定**(超レアを持ち込むだけで条件11が自明達成になるのを防ぐ)。
12. §8-3の訂正: 装備カンスト時に残る1枚は「**③のスクラップ or ナイフ**」(knifeUpgradeOption 25%を含む。
    スクラップ固定ではない)。
13. §1-2条件①の訂正: 「ガチャで所持」→「**`ownedSkills` に在る**(入手経路は問わない。reaper等の
    イベント入手も含む。除外は#6の専用定数だけが決める)」。

### ★未決 → 全件裁定済み(2026-08-12 社長「5の未決は推薦で実装」)
- **#8**: 初期所持へレア1種(fire-shooter)を追加 → **採用**(上記4へ反映)。
- **#9**: 超レアの持ち込み → **(b)例外として許容**(曲線・pityは抽選のみを縛る。上記5へ反映)。
- 次の手順: 監査4巡目 → 通過後 B0(ベースライン計測)から発注。

## 10. 監査4巡目の反映(12件・v0.25.3201)

監査4巡目は不合格(12件)。**設計の穴(実装者が黙って決める/黙って壊れる箇所)は本節で確定**し、
**仕様判断を含む3件(#1/#2/#10係数)だけ★社長裁定へ切り出す**(節末)。

1. **(#1・★社長裁定へ)** 新規プレイヤーの初期候補はノーマル3+レア1のみ=最初の十数ランが毎回同じビルドに
   なる問題。裁定待ち(節末★1)。**§8-4の計測に「初期所持のみ(ガチャ0回)」条件を追加**するのは
   裁定と無関係に確定(最悪ケースを一度も測らない穴の補修)。
2. **(#2・★社長裁定へ)** 「派手さ」の担保と測定。裁定と無関係に確定する部分: **受け入れ条件に
   「ボス突入時点の flashy スキル所持数(中央値≥2)」を代理指標として追加**(§5-3へ)。絵の新造の
   要否は裁定待ち(節末★2)。
3. **(#3)** 受け入れ条件10/11未達時の後退手順を確定:
   ①充足率<4/6 → **帯の境界LvをB0の到達Lv分布(中央値)から切り直す**(発生率・重みは不変)
   → ②なお未達なら発生率を上げる(上限70/85/85)→ ③それでも未達=XPカーブ変更が必要=**社長裁定**。
   超レア率<80% → ①pity発火をLv8+→Lv7+へ前倒し → ②なお未達=社長裁定。
   §1-2bの表のLv8+=80%への訂正漏れも修正済み(§9-3の反映漏れ)。
4. **(#4)** 同行者の矛盾解消: **§9-7が正=同行者は `player.skills` に入らない**(専用の同行者枠フィールド)。
   §8-1の「入る」を撤回。**配線の全列挙**(B4受け入れ条件=全点の切替確認):
   `beginGhostOnlineRun` / `selectedGhostMode` / `directorTick.ts:645 ghostRunEnabled`(召喚ゲート+計測停止
   ゲートの引数源を同行者枠の選択値へ)/ `playerTraits` の計測停止 / `directorTick.ts:773` ボスHP×1.6適用点 /
   霊スナップショット(同行者枠は写さない=霊が霊を呼ばない)。
5. **(#5)** サーバ側スキル上限: `shared/ghostSanitize.mjs:116` の `listOf(…, 8)` を **9へ拡大**
   (runBuild6+poi3。受け側の拡大=後方互換)。B2の受け入れ条件へ追加。
6. **(#6)** `generateEquipmentChoices` 呼び出し元の対応表:
   `gameStore.ts:11991`(ボスドロップ宝箱=boss-startでない側)=**装備3択のまま**(スキル提示は
   レベルアップ+ボス開始チェストの2経路のみ)。`playtestDriver.ts:582` / `BenchmarkOverlay.tsx:843`
   (計測用)=スキル提示なしで固定(計測の再現性)。
7. **(#7)** レア度枯渇時の流し先=**下位方向のみ**(レア空→ノーマル)。上位へは流さない。
   下位も空なら提示不成立=従来の装備3択。⇒「抽選の序盤超レア0%」は構造的に破れない。
8. **(#8)** レア度ロールは**1枚ごとに独立**。pity確定枠が乗ったターンのもう1枚は**通常ロール**
   (超レア2枚並びも許容=§0「ぶっこわれ許容」)。
9. **(#9)** B5計測に「**空きスロット充足時刻**」「**特殊装備(5%/10%)入手率**」の新旧比較を追加。
   ②飢餓が実測されたら後退レバー=スキル提示ターンの装備1枚を「①が進化満了なら②優先」へ
   (新機構なし・既存3択生成の並び替えのみ)。
10. **(#10)** DDA入力の定義を確定: **`skillCount` は runBuild.length を数える**(poi/同行者は含めない=
    §1-3の台帳境界と同一)。**係数(1.0→0.4)の変更は仕様変更として★社長裁定へ**(節末★3。裁定まで
    B0比較は現行係数のまま取る)。
11. **(#11)** B0のTTK測定条件を固定: ボス個体=giantbat@stage-1+ミゲルの2体/武器・装備=初期Tier固定/
    プレイヤーLv=B0の到達Lv分布の中央値時点/ボット=greedy・シード10本固定/各条件10ラン/
    **同行者なし・ありの両方**を測る(裁定3の後退判定に必要)。§8-4と同じ粒度で固定。
12. **(#12)** 小穴3点: ①取得Lvは既存出撃経路(`gameStore.ts:14048`)と同じクランプ
    `Math.max(1, Math.min(skillMaxLevel(k), ownedLevels[k] ?? 1))` を通す ②resetGameで `runBuild` と
    `superPityArmed` をともに初期化(§8-8を訂正) ③スキル提示カードに**所持Lvを表示**(Lvで効果が
    別物のため=情報の欠落の補修。`skillDescForLevel` を使う)。
- **(補足の採用)** `requires` タグ機構は**作らない**(dog-runに続きbomberも `sortieOnly` で処理可能。
  §8-12の全数洗いで本当に必要な種が出た時に初めて導入=§0「仕組みは足すより削る」)。

### ★社長裁定(3件・監査4巡目より) — ★1/★2裁定済み(2026-08-12)・★3待ち
- **★1(体験)=裁定済み「おおむねOK」**: 初期所持を**「開幕から連鎖の絵が出る5種」**にする:
  ノーマル3=シャープシューター(貫通)/跳弾/パニッシャー(裁定7の3種のまま)+
  **レア2=ファイアシューター(爆発弾)+ボムカウンター(カウンター反射弾が爆発)**。
  - レア②の選定理由(発注時の言葉「効果一覧から最も絵が出るもの」): ボムカウンターは**単体で絵が出る**
    (中核動作=カウンターのたび爆発)。銃連鎖(貫通→跳弾→爆発弾)と近接カウンターの2系統が初期から
    揃い「どちらのビルドへ寄せるか」の選択が初ランから生まれる。エクスプローダーは
    fire-shooter依存(単体では絵が出ない)ため初期側には入れない=ガチャ後の重ね掛けの楽しみに残す。
  - 超レアは配らない(ガチャの縦軸)。B1の受け入れ条件の「初期所持」をこの5種に更新(§9-4を上書き)。
  - ※B3レア度表で fire-shooter / bomb-counter が**レア維持**であることを前提にする(変える場合は要再裁定)。
- **★2(派手さ)=裁定済み「2はそれで」**: バーサーカー=**赤い枠光**/オーバークロック=**青い枠光**を
  発動中のプレイヤーに付ける(ピクト+オーラ)。負荷1/10: pooled sprite+小glow・**投影影を落とす
  光源は使わない**(計測台帳「光の絵は無料」)。実装はスキル効果配線のバッチ(B4)に含める。
  §5-3の代理指標(ボス突入時flashy所持数)も併用=検収は指標+この絵の両輪。
- **★3(DDA係数)=裁定済み(社長「×3までで。つまり2枠で1上がる」)**: `skillCount` の寄与は
  **runBuild.length × 0.5(=2枠で+1.0)・上限+3.0(6枠フル)**。推薦の0.4ではなく0.5で確定
  (旧2枠時代の最大+2.0より少し上=スキルを積むほど難度も薄く応える、を残す社長裁定)。
  §10-10の入力定義(runBuild.lengthを数える・poi/同行者は含めない)は不変。B0比較は現行係数で
  取り、B4でこの係数へ切替(切替前後のTTK/被弾をB5で比較)。

## 11. ハイブリッド採用(監査5巡目★A裁定)+監査5巡目21件の反映(v0.25.3210)

### 11-0. ★A裁定(社長「ではハイブリットで」2026-08-12)= ラン中スキルLv上げを採用
- **ガチャの所持Lv=そのランの開始Lv**(従来どおり)。**ラン中は所持済み(runBuild内)スキルの
  「Lv+1カード」が提示に混ざる**。上限は skillMaxLevel(多くはLv3)。未取得スキルのLv上げは無い。
- 提示の形は「スキル2枚+装備1枚」のまま。スキル2枚の候補プール=**新規取得候補 ∪ Lv+1候補**。
  Lv+1カードのレア度=そのスキルのレア度(枠は消費済みなので枠チェックは通らない)。
  混入重み=新規と同格(叩き台。B5で「6種収集完了率/平均最終Lv」を計測して調整)。
- pity(超レア確定混入)は**新規取得候補にのみ**適用(不変)。進行曲線のレア度重みも不変。
- カード表示は「スキル名 Lv2→3」(§10-12③のLv表示機構をそのまま使う)。
- 算定済みの前提(v0.25.3209): 1ランで6枠フルLv3は実質不可(必要提示18回≈到達Lv24〜25)。
  フル完成の唯一の道=ガチャ育成のまま=縦軸は壊れない。
- これにより監査5巡目 **A-1**(初期5種の固定化)と **C-21**(「またやりたくなる」の担い手不在)を解消:
  ガチャ0回でも毎提示に「新規 vs 手持ち強化」の選択が発生し、ラン内成長が担い手になる。
  リザルトは§1-5(6枠+最終Lv表示)の範囲で足りる。

### 11-1. 監査5巡目の残18件の確定
- **A-2**: §10-6の「playtestDriverは提示なし固定」を**撤回**(greedyボット=充足率計測の本体のため)。
  再現性はシード固定で担保。§10-6の「呼び出し元」誤記(playtestDriver/BenchmarkOverlay)も撤回。
  ボスドロップ宝箱(gameStoreのchest経路のboss-startでない側)=装備3択のまま、は維持。
- **A-3/A-4**: bomberは`sortieOnly`に**しない**(発火源はランチャー/ホーミング/タレット/ボムカウンター等
  ラン中に成立する)。**bomberは無条件提示**。`requires`タグ機構は作らない(§9-10の「仕様として残す」
  文言を撤回)。§1-2④の「発動条件」の実対象は dog-run の`sortieOnly`のみ。§5-1-2の不変条件テストは
  sortieOnlyを対象に書く。§2-5のタグ表からrequiresを削除。
- **A-5**: ボス開始チェストの「3連につきpity1回」は、チェストの経験値付与時に**ラン内カウンタ
  `chestOfferBudget`(=このチェスト由来のレベルアップ残数)を立てて識別**する(新設・resetGameでクリア)。
  §10-6の「2経路」記述は「1経路(レベルアップ)+チェスト由来はカウンタで識別」へ訂正。
- **A-6**: スキル提示ターンの装備1枚=「既存3択の先頭」ではなく**「回復カードがあれば回復を優先、
  無ければ先頭」**(装備カンスト後の後半で回復手段を落とさない)。
- **A-7**: **B0に計測器の実装を含める**(2段構成に変更): ①`__BOT_REPORT__`拡張(ボス個体TTK/runBuild/
  レア度枠充足)+ボスメーカーへの6枠ビルド注入口 ②その計測器で基準線測定。
- **A-8**: DDA比較(playerPower分布/escalation発火率)は**実機オートパイロット(`?bot`)で測る**と媒体指定
  (ヘッドレスはspawnEsc:0固定で不可)。
- **B-9**: 初期所持5種は`DEFAULT_OWNED_SKILLS`への**追加**(守護霊3種は残す)。§6-7/§9-4/★1の記述を
  「追加」で統一。
- **B-10**: 同行者枠は**新キー`zombie:companionSlot`**へ永続化。移行=旧LOADOUT_SKILLS_KEYに守護霊系が
  居たら新キーへ書き出し+**旧キーから除去して書き戻す**(一回性の担保)。
- **B-11**: 受け入れ条件9(TTK−25%)の判定対象=**同行者なしのTTK**(同行者ありは参考記録)。
- **B-12**: 有志/猛者で固定20人(2スキル)と実プレイヤー霊(最大6+3)が混ざるばらつきは**v1許容**と明記
  (現状も混在済み。対価差の調整はv2候補)。
- **B-13**: ★2の枠光と★3のDDA係数切替の実装先を**B2(スキル効果・配線)**へ移す(B4は同行者専任)。
- **B-14**: 裁定済み=**バーサーカー金の枠光**/オーバークロック青(色文法の赤は予約のまま)。
- **B-15**: flashy代理指標を再定義: B3のレア度表作業と同時に**全スキルへ「絵が出るか」を再付与**
  (枠光が付いた超レア2種・爆発系レアを含める)。指標=「ボス突入時、絵が出るスキル所持数 中央値≥2」。
  ガチャ0回条件では測らない(初期3種が自動達成のため=計測条件と分離)。
- **B-16**: §10-3の発生率上限を70/85/85→**70/80/80**へ訂正(§9-3の「装備専用ターン2割」の床を守る)。
- **B-17**: 裁定済み=**(a)TTK予算は監視・報告のみ**。§5-2の「上げ直して再測定」を「報告→社長裁定」へ。
- **B-18**: DDA入力をrunBuild.lengthへ変えるとpoi/同行者を数えなくなる**緩み**が出ることを明記。
  B0比較は新旧両定義で取る。★3の上限+3.0は保険条項(0.5×6=3.0で通常は効かない)と明記。
- **B-19**: 「ラン中に取ると説明文が嘘になる」全数洗い(§8-12)を**B1と同時**へ前倒し。
  ナイト型(生成時にしか効かない効果)を洗い出し観点に追加。
- **B-20**: 設計書のコード位置指定は**行番号をやめシンボル名+定数名**で書く(§8-9/§10-12を読み替え)。

### 11-2. 次の手順
監査6巡目(このv0.25.3210反映後の全文) → 通過後 B0(計測器実装+基準線)から発注。

## 12. 装備の行き先(再編・書きかけ=基礎装備の裁定待ち)

2026-08-12の社長裁定・指示の記録(監査6巡目12件の反映と一体で完成させる):
- **ナイフ強化=武器箱へ移設(実装済み・v0.25.3212)**: レベルアップ③枠のナイフ提示(25%)を廃止、
  武器箱がTier5未満なら25%で次Tierナイフ(率は旧値引き継ぎの叩き台)。③枠は常設スクラップ+50。
- **特殊装備=専用POIへ(社長指示「POIを用意してそこに入れちゃおう」「それぞれで3つ設置」)**:
  - ランごとに**特殊装備POIを3つ**設置(装備スロット=体/腕/アクセサリのそれぞれに1つ)。
  - **設置方角はランダム**(既存の迂回POI=detourPoiの角度散らし機構を流用)。
  - **拠点解放でマーカー表示=在処が分かる**(既存の拠点→POI表示動線の延長)。
  - 中身は「そのスロットの特殊装備を選んで取る」(運は場所に限定・中身は選択=ブレ対策)。
  - レベルアップ補完枠への特殊装備混入(空きあり5%/空きなし10%)は撤去=特殊はPOI専任。
  - **POIの絵素材は社長支給待ち**=実装は素材到着後のバッチ。
- **基礎装備(進化・補完)の行き先=★最後の未決**: (a)商人(スクラップ経済で指名買い・レベルアップは
  スキル専業化) / (b)レベルアップに残す。裁定後に§12を完成させ、監査6巡目12件と併せて監査7巡目へ。
