# SKILL_BUILD_REDESIGN — スキルのラン中レベル制ビルド(レア度枠 1/2/3)

作成 2026-08-12(v0.25.3189)・設計チャット。ステータス: **設計中(監査前)**。

## 0. ゴール(社長の言葉のまま)
- 「後半に行くほどゲームプレイに派手さや違いが出てくるなら…**またやりたくなる何かが必要**」
- 「今は固定で2枠スキルを装備させているが、これを**ゲームプレイ中のレベル制にシフト**する」
- 「バランスについて、**レア毎に縛れば**? それを踏まえて**レア度も改めて算出**。
  **超レアは1種まで/レアは2種まで/ノーマルは3種まで/合計6個のビルド**」

## 1. 新しいラン内ビルドの形

### 1-1. 枠(社長指定・確定)
| レア度 | 同時所持の上限 |
|---|---|
| 超レア | **1種** |
| レア | **2種** |
| ノーマル | **3種** |
| 合計 | **6種** |

### 1-2. 取得の流れ
- **出撃前の固定装備は「1枠」に縮小**(旧: 2枠)。所持スキルから1つ選んで持ち込む。
  持ち込んだスキルも**そのレア度の枠を1つ消費**する(枠の外に置かない)。
  ★未決#1: 持ち込み0枠(完全ラン中取得)にする選択肢もある。推薦=1枠
  (「相棒を決めて出る」計画の楽しさを最小限残す。0枠は配牌の純度が上がるが、
  ガチャで当てた超レアを確実に使う手段が消える)。
- **レベルアップの選択肢にスキルカードが混ざる**。従来の装備進化/補完/(スクラップ|ナイフ)の
  3択に、**スキル枠を1つ追加して最大4択**(§2-1)。
- 取得スキルの**Lvはガチャの所持Lv**をそのまま使う(ラン中のスキルLv上げは**しない**・v1)。
- **提示条件**: ①ガチャで所持している ②まだこのランで取得していない
  ③そのレア度の枠が埋まっていない。3条件を満たすスキルからレア度重みで抽選。
- 抽選重み(叩き台・実機調整前提): **ノーマル65% / レア28% / 超レア7%**。
  該当レア度の候補が空なら次のレア度へ流す(空振りでスキル枠自体を出さない、はしない)。

### 1-3. 枠の外に置くもの
- **守護霊系(guardian-spirit / ghost-helper / ghost-slayer)は「スキル」から外し、
  出撃前の独立した「同行者」枠へ移す**(★未決#2・推薦)。理由: ボス戦の看板機能を
  ラン中抽選に沈めると、霊に会えるかが運になる。ゴールド倍率ペナルティは従来どおり。
- **警察署の報酬3種(poi-bombing / poi-guard / poi-thrall)は6枠の外**(従来どおり
  ロードアウト外の直接付与)。施設の価値を枠と競合させない。

### 1-4. ガチャ(開発施設)の意味の更新
- 「所持」の意味が**「装備できる」→「ランの抽選プールに入る」**へ変わる。
  ガチャの価値は下がらない(むしろ引くほど配牌が豊かになる)。確率・価格・pity・
  Lv system は**一切変更しない**。
- 装備メニューのスキル欄は「持ち込み1枠の選択」に変わる(§3-2)。

## 2. 実装仕様

### 2-1. レベルアップ選択肢(upgradeUtils)
- `generateEquipmentChoices` に **スキル候補1枠を追加する純関数** `pickRunSkillOption(owned, ownedLevels,
  runSkills, rand)` を新設(`src/utils/runSkillDraft.ts`・テスト同コミット)。
- 候補が存在しない時(プール枯渇/全枠満杯)はスキル枠を出さず従来の3択のまま。
- 選択時: `player.skills` に追加+`player.skillLevels`(=ownedSkillLevels のコピー値)を設定。
  既存の `hasSkill`/`skillLevel` 読み出しは**無改変で効く**(所持配列に足すだけ)。

### 2-2. 枠の判定(純関数・単一の出どころ)
- `runBuildCapacity(rarity)` = {super:1, rare:2, normal:3} と
  `canAcquireRunSkill(runSkills, key)` を `runSkillDraft.ts` に置く。
  **UI/抽選/持ち込み検証の全てがこれを通る**(枠の判定を2箇所に書かない)。

### 2-3. 出撃前の持ち込み(gameStore)
- `pendingSkills` は**最大1件**へ(既存の保存キーはそのまま・読み出し時に slice(0,1))。
- resetGame: 持ち込みスキルを初期 `player.skills` に入れる(従来と同じ経路・件数だけ減る)。

### 2-4. 移行・互換
- 既存セーブの `pendingSkills` 2件は先頭1件だけ有効(黙って落ちる分は装備メニューで見える)。
- 守護霊系が `pendingSkills` に入っていた場合は同行者枠へ読み替える(★未決#2採用時)。
- ボット(botUpgradePolicy)は当面「スキル枠を選ばない」(装備優先の現行方針のまま)=挙動固定。
  練習ラン/ベンチ/チュートリアルはスキル提示なし(現行どおりロードアウト経路が空)。
- 守護霊のビルド写し(ghostAlly snapshot)は召喚時点の `player.skills` を写す=自動追従・無改変。

## 3. UI

### 3-1. レベルアップ画面
- スキルカードは装備カードと並ぶ1枚。レア度色(既存のガチャ配色)+「SKILL」バッジ。
- 取得後、そのレア度の残枠をカード下部に小さく表示(「レア枠 残り1」)。

### 3-2. 装備メニュー
- スキル欄を「持ち込み(1)」に改称。選択UIは現行の流用(最大数だけ1へ)。
- ビルドの全容(6枠)は**ラン中のHUDではなくポーズ/リザルトで確認**(HUD常設はしない。
  React再レンダー規律: 取得イベント時のみ更新される小コンポーネントで実装)。

## 4. レア度の再算出(叩き台・全件。社長裁定事項)

**原則**: レア度=枠の税。**ボスに効く倍率ほど上位**(超レア1枠が自然にボスバランスの上限になる)。
**雑魚特効(貫通/跳弾/巻き込み等・ボス中立4チャネル)は下位に下げて積みやすくする**
(=派手さの3層計画の②機構層をノーマル帯に配る)。

| スキル | 現 | 新(案) | 理由(1行) |
|---|---|---|---|
| reaper | 超 | **超** | フィニッシュ波及=ラン定義級 |
| berserker | 超 | **超** | 全攻撃倍率=ボス直撃 |
| skater | 超 | **超** | 移動3倍=ラン定義級 |
| overclock | 超 | **超** | サブ連発=山場の頻度が変わる |
| crit-up | レア | **超** | ボスクリ×5→5.5+全クリ倍率=ボス直撃の最上位 |
| sniper | レア | **超** | 対ボス実効(巨体・遠距離維持)最大+100%×2軸 |
| exploder | レア | **レア** | 爆発ダメ+範囲(ボスにも乗るが条件付き) |
| fire-shooter | レア | **レア** | 爆発弾化(同上) |
| bomber | レア | **レア** | 手榴弾派生(同上) |
| bomb-counter | レア | **レア** | カウンター爆発(技量前提) |
| knife-master | レア | **レア** | 近接倍率+クリ(弾ドロップ0の代償付き) |
| combo-master | レア | **レア** | コンボ維持前提の全攻撃倍率 |
| knight | レア | **レア** | 被ダメ-20〜40%=生存の軸 |
| rescue-signal | レア | **レア** | 必中援護 |
| sharpshooter | レア | **ノーマル** | 貫通=ボス中立。積ませて派手にする側 |
| ricochet | レア | **ノーマル** | 跳弾=ボス中立。同上 |
| punisher | レア | **ノーマル** | 巻き込み=ボス中立。同上 |
| benkei | レア | **ノーマル** | 一時クリ率(小・条件付き) |
| reflex | レア | **ノーマル** | 被弾時反撃(CD付き・防御寄り) |
| attack-shooter | ノ | **ノーマル** | 銃+10〜30%(加算小・据え置き) |
| counter-master / slasher / last-magazine / warm-up | ノ | **ノーマル** | 技量/条件付き強化 |
| gold-rush / scrap-builder / magnet / ghost-shooter / time-keeper / runner / seeker / dog-run | ノ | **ノーマル** | 経済/利便 |
| 守護霊3種 | 超/ノ | **枠外(同行者)** | §1-3(★未決#2) |
| poi 3種 | 超 | **枠外(施設報酬)** | §1-3(従来どおり) |

- 再算出は**ガチャの排出レア度にも同じ表を使う**(2つのレア度表を持たない)。
  ⇒ crit-up/sniper の超レア昇格でガチャの排出率が下がる=**入手も適正に渋くなる**(整合)。

## 5. 受け入れ条件(監査・実装検収用)
1. ラン中ビルドが**レア度枠(1/2/3)を一度も超えない**(純関数+ユニットテストで機械化)。
2. レベルアップのスキル候補は「所持済み・未取得・枠が空いている」以外を**絶対に提示しない**。
3. 持ち込みは最大1で、持ち込んだスキルもレア度枠を消費している。
4. ガチャの確率・価格・pity・Lv机構に変更が無い(レア度表の差し替えのみ)。
5. ボット/練習/ベンチ/チュートリアルの挙動が変わらない(スキル提示が出ない)。
6. 守護霊のビルド写しがラン中取得スキルを含めて写る。
7. 既存セーブで起動してもクラッシュ/装備消失がない(2枠→1枠の縮小が黙って壊さない)。

## 6. ★未決事項(社長裁定待ち)
1. 持ち込みは1枠か0枠か(推薦: 1枠)。
2. 守護霊系を「同行者」枠として独立させるか(推薦: 独立)。
3. §4のレア度表の個別承認(特に crit-up / sniper の超レア昇格)。
4. 抽選重み(65/28/7)と、超レア候補が引けた時の演出(ガチャ級の派手さを出すか)。

## 7. 実装順(バッチ・Sonnet発注単位)
- **B1**: `runSkillDraft.ts`(枠判定+抽選の純関数)+テスト。store: pendingSkills 1枠化・取得アクション。
- **B2**: レベルアップUI(スキルカード)+装備メニューの持ち込み1枠化。
- **B3**: レア度表の差し替え(campaign.ts)+ガチャ側の追従確認。
- **B4**: 守護霊系の同行者枠(★未決#2の裁定後)。
- B1〜B3 は順に依存。B4 は独立。
