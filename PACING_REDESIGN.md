# 緩急再設計(心電図化)設計書 — Sonnet実装用ハンドオフ

社長との協議(v0.25.1296〜)で合意した内容の完全版。DISTRIBUTION_REDESIGN.md(実装済み)の次段。
**「ジェットコースター(最初からMAXの固定コース)」から「心電図(試して、上がりすぎたら落とす)」へ。**
実装は本書のバッチ順に1つずつ、各バッチで version bump + DEVELOPMENT_LOG + 検証一式(いつものルール)。

## 運用(2チャット体制・社長指示 v0.25.1301〜)
- **実装チャット(Sonnet)**: 本書のバッチを実装し、結果を DEVELOPMENT_LOG.md と本書の
  ステータス更新でファイルに残す。設計判断はしない(未決事項に当たったら本書に質問を書いて止める)。
- **設計チャット(Fable・別チャット)**: 社長との話し合い。決定事項は本書を直接更新して実装チャットへ渡す。
- 受け渡しはすべてファイル経由(PACING_REDESIGN.md / DEVELOPMENT_LOG.md / AI_DIRECTOR_HANDOFF.md)。
  チャット間でお互いの会話は見えない前提で、決定・未決を必ず本書に書く。

## ★未決事項(Fableチャットで社長と決定するもの)

### ★台本ローテーション化(会議中・社長採否待ち・v0.25.1359叩き台)
統合テスト(v0.25.1357)の判定=「あまりにも単調・ゾンビまでしか出ない」。診断:
①Tank化後、犬/パンプキンは配役(pressure0.50/0.65)のみだが、関所40秒×上げτ8秒では届く前に終わる
②台本選択がrank寄せで、Rankが低いと数/射線ばかり回る ③判断(5)以上はスロット階段で6:40以降のみ適格。
**方針転換の叩き台(社長の指示「1分おきに台本を変えて、一個前の台本は出さない」を仕様化)**:
- 台本選択は「難易度」ではなく**多様性のためのローテーション**にする(難易度はpressure/ゾーン/
  stageAggroが担う)。rank寄せ廃止(Rankはpressure開始値とホード規模に残す)。
- 骨格: 導入60秒(純休憩固定)→以後**関所60秒⇄緩60秒**交互(7:00城ボスまで・7分以降も同じ刻み)。
- 各コマは**未見優先シャッフル+直前の台本は選ばない**。
- 関所メニュー(6): 数(0分〜)/射線(0分〜)/判断(**2分〜**)/襲撃=囲い(**3分〜**)/三択(**4分〜**)/
  スパイク=ミニボス(**4分〜**)——時間解禁の階段(旧スロットmaxRungの置き換え・前倒し)。
- 緩メニュー(4): 純休憩/無双HARVEST/講習(主役1体・未経験型のみ)/回収(出現したのに倒せなかった型)。
- **pressure上げτ 8秒→5秒**(関所60秒内に配役0.50へ確実に届かせる)。
- ゾーン上限(憲法第4条)は**不変**(初心者ゾーンはチャフのみ維持)。
- 社長へ確認したい3点: (1)時間解禁の位置(2分/3分/4分でよいか) (2)緩も60秒固定でよいか
  (現行は20〜90秒の長短ミックス) (3)rank寄せの完全廃止でよいか。

上記以外の未決: ゴールド経済の価格叩き台(CORE_LOOP.md§6)も採否待ち。

### 決定履歴(v0.25.1322・実機確認③→バッチ4を社長決定)
- **HARVEST=雑魚無双モード**(旧「パンプキン2体の狩猟」案は廃止)。
- **講習は主役1体だけ**(同時1・1フェーズ1体・キル後再投入なし)。
- **終盤(7:00以降)の緩はHARVEST無双既定**(だれ対策。純休憩はscore<0.25のみ)。
- **問題児リフラクトリ**: 同型問題児キル後15秒は再投入禁止(「やっと倒したらすぐ沸いた」対応)。

### 決定履歴(v0.25.1318・チャフの役割と盤面在庫を社長決定)
- **チャフ3種の役割**(社長定義): bat=爽快(気持ちよさ・スピード感)/skeleton=刻み(中間)/
  zombie=壁(防衛配置・クリ前提の固さ・経験値稼ぎに不向き)。役割をAIが持ち、狙って配合して湧かす。
  RELAXはバット中心でないとスピード感を失う。緩のゾンビは「群れに1体」までのバランス枠。
- **盤面在庫**: 「捌き切れていないのに次のフェーズ/イベントが来る」問題への対処として、
  盤面の残存重量(boardDebt)を投入判断(イベント/配役/湧きテンポ/関所の登り)に入れる。
- 上記2つを**バッチ3.5**として新設し次の実装とする(仕様は§バッチ3.5)。

### 決定履歴(v0.25.1311・実機確認①の原因分析→次手を社長決定)
- バッチ3最小版後の実機ラン(生存3:56・後半Intensity1.0張り付き・死因パンプキン)を設計チャットで
  分析(詳細=§実機確認①)。**主犯=台本外の固定イベント(囲い2:00・ハンター3:00)、
  従犯=緩フェーズ固定の講習パンプキン。gatePressureは正常動作(エリア1ではゾーン天井0.34で
  問題児解禁・配役とも不発=設計どおり)。**
- 対処として **バッチ2.5(診断計測)を新設**し、その直後に**バッチ7を前倒し実装**することを社長決定。

### 決定履歴(v0.25.1304・Fableチャットで社長決定)
1. **バッチ3の方式 = 連続圧力方式(gatePressure)を採用**(離散段0〜7+最低滞在12秒は不採用)。
   §バッチ3を連続圧力方式で全面書き換え済み。
2. **下げは時定数+被弾インパルス**(普段は滑らかに下降、被弾スパイク時は即−0.15のステップ減)。
3. **関所ごとのpressureは毎回リセット**(0+Rankボーナスから登り直し)。持ち越し式へ変えられる
   ノブ(startPressure)は実装に残す。
※バッチ2が先に完了したため、バッチ3のスタイル配役は「ランダム仮置き」ではなく
  **バッチ2の`styleFromKillCounts`を最初から直結**する(§揺さぶり参照)。

## 0. 全体構造: 3層ループ(これが完成形の絵)
```
台本(骨格) ── いつ山/緩が来るか・その山で許される最大段(時刻で固定・PHASES)
   ↓
演目選択(プロデュース) ── フェーズ切替ごとに「何を上演するか」を選ぶ
   ・山明け → 直前の山の試験結果で RELAX/HARVEST の演目を選ぶ(バッチ4)
   ・緩明け → 総合点(Rank)+スタイルで次の山の台本を選ぶ(バッチ5)
   ↓
アドリブ(gatePressure) ── 選ばれた台本の中で圧を連続的に上げ下げ(バッチ3)
```
既存部品との対応: DirectorRank=総合点の採点係(継続) / 台本PHASES=時刻の骨格(不変) /
反応型AIディレクター(Intensity/Performance)=退屈・苦戦シグナルの計器(信号は既定ON済み)。

## 1. 憲法(全システムが従う・社長承認済み)
1. **画面内は基本10体**。11〜20は「退屈シグナル」が出た時だけの上振れバッファ。Intensityが
   上がり始めたら**即**基本値へ戻す。数はデフォルトの難易度レバーから外す(台本の関所も10)。
2. **問題児の同時数キャップ**: 犬2(済) / 弾plant2(済) / パンプキン2(新) / **毒ghost1(新)** /
   **バフscreamer1**(専用ディレクターが同時1体=準拠済み)。
   ⚠社長注記: **パンプキン2体は出すタイミング+周囲の雑魚数によっては回避不能級**。
   2体目がいる間は雑魚湧きテンポを一段緩める(同時に盛らない)セーフガード必須。
3. **難易度の第一レバーは雑魚の湧きCD**(=刈る物が増える楽しい圧)。問題児の追加はラダー後半
   でのみ・キャップ内。「いやらしいだけの難易度」は作らない。
4. **退屈対応は全ゾーン(初心者ゾーン含む)で有効**。ただし序盤ゾーンで使うのは「いやらしくない
   レバー」(雑魚テンポ・数の上振れ)だけ。問題児レバーはゾーン層に応じて解禁(§バッチ3のゾーン上限)。

## 2. 用語
- **RELAX** = 息を整える時間(まばら・弱い・レア無し)。
- **HARVEST** = 育った自分を確認する時間(気持ちよく倒せる敵がそこそこ+報酬多め)。
- どちらも台本の緩(buildup)フェーズに割り当てる「演目」の違い(バッチ4)。

---

## バッチ1: 憲法の実装

### 1-A. 退屈シグナル→上振れ枠
- **`src/utils/boredomDirector.ts` は作成済み(叩き台・未配線)**。純関数:
  `stepBoredom(prev, {performance, intensity, dtMs})` / `boredomBonus(boredMs)`。
  仕様: Perf≥0.8 かつ Intensity≤0.4 の持続で蓄積(外れたら2倍速減衰)、25秒で発動し
  10秒ごとに+2体、最大+10(=基本10+10=天井20)。**Intensity≥0.6で即ゼロ**(心電図の下り)。
- 配線(`useGameLoop.ts`):
  - `const UPSWING_ENABLED = evParam('upswing') !== '0';`(社長合意: きつければすぐ戻せるように)
  - `upswingRef = useRef(createBoredomState())`。毎フレーム `directorRef.current.state` の
    performance/intensity で step(屋外のみ=labTheme/indoor除外)。新ランのリセットブロックで初期化。
  - `dirCountCap = (lab||indoor) ? MAX_ENEMIES : Math.min(ENEMY_COUNT_CEIL, enemyCountCap(gameTime) + rankAdj.countCapBonus + boredomBonus(upswingRef.current.boredMs))`
  - デバッグ: `DirectorRankDebug` に `upswingBonus: number` を追加し、DirectorOverlay の
    rank行に `up+N` を表示(?director=1時のみ・従来パターン)。
- **テスト**(`boredomDirector.test.ts` 新規): 25s未満は0 / 25sで+2 / 10s刻みで増加 / 最大+10 /
  Intensity0.6で即0 / 条件外で2倍速減衰。

### 1-B. 台本countCapの書き直し(基本10体化)
- `difficultyDirector.ts` の `PHASES`: **buildupは8〜10、gate/bossは全て10**に書き換え
  (具体値: buildup1=8 / 他のbuildup=9、mowdown系buildup=10 / gate・boss=10)。
  関所の圧は「数」ではなく「テンポ(intervalMult)と構成」で出す(憲法第3条)。
- `ENEMY_COUNT_FLOOR` のコメントを「基本上限10(憲法第1条)」に更新。`ENEMY_COUNT_CEIL=20`は
  上振れ専用の天井として残す。
- **既存テストの書き直し必須**(`difficultyDirector.test.ts`): 「gateで天井20に到達」
  「gateはbuildupより数が多い」系のアサートは憲法と矛盾→ 新アサート:
  全フェーズcountCap≤10 / 平均8.5〜10 / 関所はテンポで速い(gate scene intervalMult ≤ 0.8等)。

### 1-C. パンプキン2/ghost1キャップ+2パンプキン緩和
- `useGameLoop.ts` スポーナ: 既存のplant/werewolf個別再抽選ブロック2つを**統合オーバーキャップ
  判定に置換**(相互すり抜け防止):
  ```ts
  const overCap = (t: EnemyType) =>
    (t==='plant' && plantCount>=2) || (t==='werewolf' && wolfCount>=2) ||
    (t==='pumpkin' && pumpkinCount>=2) || (t==='ghost' && ghostCount>=1);
  // overCap(enemy.type) なら最大8回再抽選 → それでもダメなら 'skeleton' 強制
  // 採用後に該当カウントを ++
  ```
- **2パンプキン時の雑魚テンポ緩和**(憲法第2条の社長注記):
  `const PUMPKIN_PAIR_SPAWN_EASE = 1.3;` — 場にパンプキンが2体いる間、湧き間隔判定に乗算
  (`sceneIntervalMult` と同じ掛け所)。
- 対象は通常湧きの抽選のみ。forcedType(台本セットピース/保証出現)は従来どおり対象外。

---

## バッチ1.5: featured床のオプトイン化(序盤の問題児てんこ盛り回帰の修正) — **実装済み v0.25.1302**

**背景(v0.25.1298の実プレイで社長報告)**: 序盤から「パンプキン+犬犬+弾」が出た。原因は
DISTRIBUTION_REDESIGN.md①の「featuredはエリア重み0でも床0.5で出せる」が、無双シーンのチャフ
だけでなく**関所シーンの問題児にも効いてしまった**こと(関所①=pumpkin+werewolf featured が
エリア0/1で空振りしなくなった)。チャフのための床が問題児の裏口になった設計ミス。

### 仕様
- `SpawnScene` に `featuredFloor?: boolean` を追加(省略=false)。
- `selectEnemyType` の床適用(`Math.max(areaW, FEATURED_MIN_AREA_WEIGHT)`)は
  **featuredFloor が true のシーンでのみ**行う。
- true にするのは: `relief-pumpkin` / `relief-wolf`(講習: 社長要望の「序盤で犬だけ・パンプキン
  だけの練習」を守る。緩の中・1種だけ・少数なので安全)/ `mowdown`(深部のチャフ供給=床の本来の目的)。
- **関所シーン(gate-*)は全て false** = エリア規約に従う。序盤ゾーンの関所は雑魚テンポ+自然分布のみ
  (憲法第3条「序盤はCD短縮で試す」どおり)。
- 実装箇所: `difficultyDirector.ts`(型+シーン定義)、`enemyUtils.ts`(selectEnemyTypeに
  floorAllowed 相当の引数を貫通 or featured と floor対象を分けて渡す)、`useGameLoop.ts`(貫通)。
  `sceneSpawn` フラグ(強制回収免除)の仕組みは不変。
- テスト: エリア0で gate-pumpwolf 相当(floor無しfeatured)ではpumpkinが選ばれないこと/
  relief-pumpkin 相当(floor有り)では選ばれること。
- **保証出現(plant1分/犬3分・エリア不問)は撤廃(社長決定)**: `plantGuaranteedRef`/
  `werewolfGuaranteedRef`と発火ブロックを削除。「勉強させる回」の役割はバッチ4の講習演目が継承する。
- セットピース(1:45パンプキン等)は本バッチでは触らず、バッチ7(イベントプロデューサー)統合時に整理。

**実装結果(v0.25.1302)**: 設計どおりに実装。`SpawnScene.featuredFloor`を追加し
relief-pumpkin/relief-wolf/mowdownのみtrue、gate-*は全てfalse(省略)。`selectEnemyType`/
`generateEnemy`に`floorAllowed`引数を貫通(既定false=安全側)。保証出現(plantGuaranteedRef/
werewolfGuaranteedRef含む定数・ref・発火ブロック・新ランのリセット行)を完全削除。
テスト: `enemyUtils.test.ts`(floorAllowed=falseでは関所シーンのpumpkin/werewolfが選ばれない/
floorAllowed=trueでは選ばれる、の対比を追加)、`difficultyDirector.test.ts`(featuredFloorの
シーン別割当を検証)。lint/typecheck/test(158 pass)/build 全通過。
未実装のまま残る保留: セットピース整理(バッチ7で対応予定)。

---

## バッチ2: 計測(試験結果の中身) — **実装済み v0.25.1303**

演目選択(バッチ4/5)とスタイル対応の判断材料。**挙動は一切変えない**(記録と表示のみ)。
- **種別キル数(フェーズ単位)**: `rankRef` のフェーズスナップショットを拡張し、フェーズ中の
  killsByType(少なくとも pumpkin/werewolf/plant/ghost/screamer と「チャフ計」)を差分記録。
  キル計上箇所は2系統のみ: `damageEnemy` のkill分岐(ガン/接触/爆発)と `grantMeleeKillRewards`
  (近接全経路)。軽量シングルトン(directorRankState.tsと同パターン)にカウンタを置き、
  gameStore側から加算・useGameLoopがフェーズ境界でスナップショット。
- **スタイル推定**: ガンキル/近接キルの累積比率(EMA)。上記2系統の計上で自然に取れる。
  `style = melee率>0.6 ? '近接' : melee率<0.3 ? '遠距離' : 'バランス'`(閾値は実機調整前提)。
- **最深到達エリアtelemetry**: ラン中の最大 `areaIndexForPos` を記録し、リザルト(GameOverScreen/
  DirectorResult)に「最深到達: デンジャー」等を表示。ゾーン境界調整(§5)の判断材料。
- デバッグ: ?director=1 のオーバーレイにスタイルと直前フェーズの種別キルを1行追加。

**実装結果(v0.25.1303)**: 設計どおり実装。実装上の1点の解釈: 「累積比率(EMA)」という記述に対し、
具体的な減衰時定数の指定が無かったため、**単純な累積比率(ラン開始からの通算gunKills/meleeKillsの
比)**を採用した(真のEMAは減衰τが必要で、1ラン15分という短さでは通算比とほぼ等価と判断)。
体感とズレるようならバッチ4着手時に減衰式へ差し替え可能な形(`styleFromKillCounts`が純関数)。
- 新規: `src/utils/killTelemetry.ts`(純関数: `bucketForKill`/`styleFromKillCounts`)、
  `src/utils/killTelemetryState.ts`(軽量シングルトン: 累計カウンタ+直前フェーズのデバッグ
  スナップショット)。
- `gameStore.ts`: `damageEnemy`のkill分岐と`grantMeleeKillRewards`のループにそれぞれ
  `recordKill(type, 'gun'|'melee')`を追加。
- `useGameLoop.ts`: `rankPhaseKey`と同じフェーズ境界トリガーで種別キル差分をデバッグ用に記録
  (`killPhaseRef`)。最深到達エリアは`playerAreaIdx`の単調増加を検知した時だけ
  `gameStats.maxAreaReached`へ反映(1ランで最大4回のset()のみ・React再描画コスト無視できる)。
- `GameStats`に`maxAreaReached: number`を追加(2箇所の初期化を更新)。`GameOverScreen.tsx`に
  「最深到達」表示行を追加(`AREA_ZONE_NAMES`をenemyUtils.tsからuseGameLoop.tsと共有するよう整理)。
  DirectorResult.tsxへの表示は今回見送り(GameOverScreen側で十分because AREA_ZONE_NAMESは全ラン共通)。
- テスト: `killTelemetry.test.ts`(6件)、`killTelemetryState.test.ts`(5件)。
- 検証: typecheck / lint(0, full) / test(169 pass, 1 skip) / build 通過。
- 負荷スコア: **1/10**。キル時のスカラーインクリメント+フェーズ境界(15分で最大18回)のオブジェクト
  差分計算のみ。新規描画/per-frame set()なし。

---

## バッチ2.5: 診断計測(リザルトグラフの「原因が読める化」) — **実装済み v0.25.1312**

**背景 = 実機確認①(§バッチ3末尾)**: リザルトのAI DIRECTORグラフは intensity/performance/macro
しか記録しておらず(`aiDirectorDebug.ts`の`DirectorSample`)、「なぜ張り付いたか」——どの区間が
関所か・どのイベントが鳴っていたか・pressureがどう動いたか——がスクショから読めない。今回の
原因特定は設計チャットのコード照合で行ったが、次回から**リザルトのスクショ1枚で自己説明**できる
ようにする。**挙動は一切変えない(記録と表示のみ・バッチ2と同じ性格)**。

### 仕様
- `DirectorSample`(0.5s間隔・cap3000は不変)にフィールド追加:
  - `phaseKind`: 台本フェーズ種別(buildup/gate/boss)
  - `pressure`: gate中のgatePressure値(緩フェーズ中はnull)
  - `areaIdx`: プレイヤーの現在エリア(0-4)
  - `events`: 発火中イベントのビットフラグ(arena/hunter/redNight/screamer/reaper/castleBoss)
  - 1サンプル数値数個の追加なのでメモリ影響は無視できる。
- `DirectorResult.tsx`(リザルト画面のみ・プレイ中の描画は一切増やさない):
  - **関所(gate)区間**を上端の細帯で表示(macro帯とは別レイヤーで区別)。
  - **イベント発火区間**をマーカー帯+凡例1行で表示(囲い/ハンター等が色で分かる)。
  - **gatePressure**を第3の線(細線)で重ねる。
  - **エリア移動**(areaIdx変化)を縦線+ゾーン番号で表示。
- 負荷スコア: **1/10**(既存0.5sサンプリングへのフィールド追加+リザルト画面のみのSVG描画。
  シミュレーション/プレイ中レンダリングへの追加コストなし)。
- テスト: 新フィールドが記録されること(aiDirectorDebugの既存テストパターンに追従)。
- 備考(記録のみ・本バッチでは触らない): GameOverScreenの「最深到達」表示が文字切れしている
  (実機確認①のスクショで「研究対象…」と切れていた)。

### 実装結果(v0.25.1312)
- `aiDirectorDebug.ts`: `DirectorSample`に仕様通り4フィールド追加(`phaseKind`/`pressure`/
  `areaIdx`/`events`)。`events`は`DIRECTOR_EVENT_BIT`(arena/hunter/redNight/screamer/reaper/
  castleBoss)のビットOR。記録元は`useGameLoop.ts`の既存0.5sサンプリング箇所のみ
  (`curPhase.kind`/`pressureOutdoor?gatePressureRef...:null`/`playerAreaIdx`は既に同フレームで
  計算済みの値をそのまま読むだけ・新規計算コストなし)。イベント判定は`ds.activeEvent`/
  `ds.redNight?.phase`/`ds.castleEvent?.bossSpawned`/`ds.enemies`のtype走査(hunter/screamer/
  reaper)を0.5s間隔でのみ行う。
- `DirectorResult.tsx`: 上端に「関所帯(gate=黄/boss=赤)」「イベント発火帯(6色+凡例1行)」の
  2細帯を追加、本体グラフ(intensity/performance)はその分だけ下にオフセット。gatePressureは
  第3の線(黄緑・関所区間だけ途切れる=nullの区間は線を引かない=それ自体が「今どの区間か」の
  情報になる)。エリア移動は縦破線+`Z{idx}`ラベル。すべてリザルト画面内のstatic SVG描画のみ、
  プレイ中のレンダリング/シミュレーションへの追加コストは無し。
- 負荷スコア: **1/10**(申告通り)。理由: 追加計算は0.5s間隔サンプリング内のスカラー比較+
  敵配列1回走査のみ(simulation側)、描画側はリザルト画面表示時の1回限りSVG生成
  (`useMemo`でsamples不変なら再計算なし)。プレイ中のPixi描画・Reactツリーには一切触れていない。
- テスト: `src/utils/aiDirectorDebug.test.ts`新設(5ケース: 初期状態/新フィールド記録/
  gate外でpressureがnull/リングバッファ3000件キャップ/リセット)。既存の`killTelemetryState.test.ts`
  のsingletonテストパターンに追従。
- 確認: `npm run lint && npm run typecheck && npm test && npm run build` 全通過。

---

## バッチ3: 山の連続圧力化 gatePressure(アドリブの本体)最小版 — **実装済み v0.25.1305**

**v0.25.1298の実プレイ後も社長報告「まだ階段を上ってる感じではない・まだ急」**。当然で、バッチ1は
天井を下げただけで、関所の入り方は今も「段差」のまま(緩シーン→関所シーンへフェーズ境界の瞬間に
テンポ1.1→0.55+問題児featured全開の一段ジャンプ)。緩急の本体はこのバッチ。

**方式決定(社長・v0.25.1304)**: 離散段0〜7の状態機械+最低滞在12秒は**不採用**。
「階段を強制的に作るというより、この階段になるように台本の中でディレクターがリアルタイムで
緩急をつけていく」= **連続スカラー `pressure`(0〜1)を毎フレーム動かす方式**で実装する。
段差(二値)が残るのは問題児の解禁しきい値だけ。**数値はすべて実機調整前提**。

最小版(v1)の範囲: 関所中のテンポ/構成をpressureで駆動。スタイル配役は**バッチ2実装済みの
`styleFromKillCounts`を直結**(バッチ2が先に完了したためランダム仮置きは不要になった)。
これだけで「関所に入る→まず雑魚が速くなる→捌けていたら弾が混ざる→さらに犬が来る」の
階段体験になる。

### pressureの動き方(心電図の形)
- `pressure`(0〜1)は**関所(gate)フェーズ中のみ**毎フレーム更新。
  **緩(relief/mowdown)フェーズは対象外**(現行シーンのまま)。
- 目標値 `perf` = 「そつなくこなせているか」の連続スコア(硬い足切り無し):
  `perf = 0.6×無被弾スコア + 0.4×撃破ペーススコア`
  - 無被弾スコア = clamp(直近被弾からの経過秒 / 10, 0, 1)
  - 撃破ペーススコア = clamp(kill/sのEMA / 0.3, 0, 1)(EMA時定数〜5秒)
- pressureはperfへ**指数平滑で追従**(既存のGATE_LIVE_TAU/spawnEscと同じ流儀):
  **上げτ=8秒 / 下げτ=2秒**(上げは遅く、下げは速く)。
- **被弾インパルス(社長決定: 時定数+インパルス)**: 被弾スパイク(2秒以内に2回被弾、または
  1被弾でHP15%以上減)で即 `pressure −= 0.15` のステップ減。時定数だけでは出ない
  「ピンチには即座に圧を抜く」俊敏さの保険。
- Intensity≥0.75 の間は上げを停止(下げのみ)。

### レバーへの変換
- **雑魚テンポ(第一レバー・憲法第3条)**: `intervalMult = 1.0 − 0.45×pressure`
  (1.0→0.55 の連続。段差なし)
- **数**: pressure≥0.55 で countCap+2(数を触るのはここだけ。憲法第1条: 基本10+2)
- **問題児の解禁しきい値**(唯一の段差。点滅防止にヒステリシス±0.05):
  | pressure | 解禁 | 形 |
  |---|---|---|
  | 0.35 | 弾plant(≤2) | 許可(抽選プール解禁) |
  | 0.50 | 犬orパンプキン1種(§揺さぶり) | **配役投入**(forcedType 1体) |
  | 0.65 | 2種目(犬+パンプキン両方=三択入口) | **配役投入** |
  | 0.80 | rareMult×1.35相当 + screamer許可(≤1) | 許可 |
  | 0.95 | ghost許可(≤1)=フルカオス | 許可 |
- **許可**の実装: pressure系が「現在の許可問題児リスト」を持ち、スポーナの抽選で許可外の問題児は
  suppressed扱い(重み0)。既存のシーンfeatured/suppressedの機構をpressure出力で上書きする形。
- **配役投入**: しきい値0.50/0.65を**上に横切った瞬間**にforcedTypeで1体投入するイベント
  (同時数キャップ2は配役側で遵守)。下に割ったら**追加投入を止めるだけ**(場の敵は消さない)。
  投入の瞬間があるので到着演出(バナー/スティングSE)のフックをここに置ける(素材は後日・任意)。

### 台本・ゾーンとの互換(maxRung→天井変換)
- `Phase.maxRung?: number` は離散案のまま使い、**pressureの天井へ変換**:
  | maxRung | 2 | 3 | 4 | 5 | 6 | 7 |
  |---|---|---|---|---|---|---|
  | 天井 | 0.34 | 0.49 | 0.64 | 0.79 | 0.94 | 1.00 |
  序盤の関所=3 / 中盤=4〜5 / 終盤・7分以降の延長関所=6〜7(具体割当はPHASES書き換え時に
  コメント付きで)。
- **ゾーン上限**(憲法第4条)も同じ表でクランプ: エリア0-1(初心者)=0.34(テンポと数のみ) /
  エリア2=0.79 / エリア3=0.94 / エリア4=1.00。
  実効天井 = min(台本maxRungの天井, ゾーン天井)。
- **二重階段の構造(社長確認済み)は不変**: ①ラン全体の階段=台本のmaxRung列(ランの中で
  新しい問題が1つずつ解禁される)②関所内の起伏=pressureの連続な上げ下げ。バッチ6のステージ指数は
  このmaxRung列全体を上下にスライドさせる係数、という三層関係。

### 開始値・持ち越し(社長決定: 毎回リセット)
- 関所開始時 `pressure = 0 + Rank1なら+0.1 / Rank2なら+0.2`(=DirectorRankの
  「次の山を強める」の統合。旧「開始段+1」の置き換え)。
- **関所間の持ち越しは無し**(毎回登り直し=関所ごとに階段を体験)。ただし
  「開始=前関所の終値×0.5」等へ変えられる形(startPressure引数)で実装しておく(調整ノブ)。

### 実装の形
- **新規純関数** `src/utils/gatePressure.ts`(旧案の`pressureLadder.ts`は作らない):
  `stepGatePressure(prev, inputs, dtMs)`。
  - inputs: 直近被弾からの経過ms / kill/s EMA / 被弾スパイクフラグ / intensity / 天井 /
    開始値(startPressure)。
  - 出力: `{pressure, intervalMult, capBonus, allowed(問題児セット), castEvents(配役投入)}`。
- **関所ライブ補正(難易度④gateLiveCorrection)との一本化**: pressure有効時、gate中の④は停止する
  (二重ブレーキ/二重アクセル防止)。`?ladder=0` でpressure系無効=④含む従来挙動に完全復帰
  (フラグ名は`ladder`のまま=切り分け用)。
- デバッグ: オーバーレイに `P0.62/0.79`(現在値/実効天井)+解禁済み問題児の頭文字を表示。
- **テスト**(`gatePressure.test.ts` 新規): 無被弾+撃破維持で単調上昇 / 上げτ>下げτ(同じ乖離
  で下げが速い)/ 被弾インパルスで即−0.15 / しきい値ヒステリシス(0.5を跨いで振動しても
  配役イベントが連発しない)/ 天井クランプ / Intensity0.75で上げ停止 / 開始値のRankボーナス。
- 負荷スコア: **1/10**(毎フレームのスカラー演算のみ。新規描画なし。配役投入は既存の
  forcedTypeスポーン経路を使う)。

### 揺さぶり(スタイル対応・しきい値0.50の1種目選択)
- 遠距離型 → **パンプキン**(ジャンプで距離を詰める=足を動かさせる)
- 近接型 → **犬**(ダッシュが間合い管理を乱す)
- バランス型 → どちらかランダム(ラン内固定でよい)
- ※スタイル推定はバッチ2で**実装済み**(`styleFromKillCounts`)。v1から直結してよい。
  選択はラン内で最初に0.50しきい値を横切った時点のスタイルで決め、以後ラン内固定
  (バランス型のランダムも同じタイミングで1回だけ引く)。

### 配役(casting)方式 — L4DのTankを見習う(社長指示)・**完成版=実装済み v0.25.1354**
- **パンプキンと犬は「台本出現寄り」にする**。L4Dの特殊感染者と同じ発想:
  雑魚(チャフ)だけがランダムプール、**問題児はディレクターが意図した瞬間に配役として落とす**。
  パンプキン=Tank(登場そのものがイベント)、犬=Hunter系(ディレクターの差し込み)。
- 実装: バッチ3完成版の時点で pumpkin/werewolf を**通常湧きプールから撤退**
  (AREA_WEIGHT該当行を0化)。以後の出現経路は:
  ①pressureのしきい値0.50/0.65(**forcedTypeで1体ずつ配役投入**。同時数キャップ2は配役側で遵守)
  ②講習/狩猟の演目(featuredFloor経由・バッチ4) ③セットピース(既存) ④イベントプロデューサー(バッチ7)。
- 3最小版〜完成版の間の暫定: プールは現状のまま(1.5で序盤ゾーンへの漏れは止まっている)。
  撤退は配役が受け皿として機能するのと同時に行う(空白期間を作らない)。

**実装結果(v0.25.1305・最小版)**: 設計どおり実装。プールからのpumpkin/werewolf撤退(Tank化)は
**完成版まで持ち越し**(3最小版のスコープどおり暫定のまま)。
- 新規 `src/utils/gatePressure.ts`(純関数一式: `stepGatePressure`/`intervalMultForPressure`/
  `capBonusForPressure`/`rareBoostActiveForPressure`/`allowedProblemChildren`/`specialCastOrder`/
  `ceilingForMaxRung`/`ceilingForZone`)+`src/utils/gatePressureState.ts`(デバッグ表示用シングルトン)。
- `difficultyDirector.ts`: `Phase.maxRung?`を追加、全gateフェーズに割当(関所①=3→…→関所⑨=7、
  非減少)。buildup/bossはmaxRung無し(pressure対象外)。
- `useGameLoop.ts`: `curPhase.kind==='gate'`中だけ`gatePressureRef`を毎フレームstep。被弾インパルスは
  専用の軽量ref(`pressureHitRef`)でAIディレクター本体のprevHpと独立管理。反映先:
  `sceneIntervalMult`(関所中はpressure由来に置換)/`dirCountCap`(pressure≥0.55で+2)/
  `sceneRareMult`(pressure≥0.80で×1.35)/`sceneBlocked`(未解禁の問題児を全generateEnemy呼び出しで
  重み0に強制)。配役投入(0.50/0.65跨ぎ)は`spawnBounds`確定後にforcedTypeで即時1体、通常湧きの
  CDとは独立、同時数キャップ2は自前チェック。関所ライブ補正(難易度④)は`pressureOutdoor`中だけ停止
  (二重ブレーキ防止)。`?ladder=0`で全体を無効化=④含む従来挙動へ完全復帰。
- テスト: `gatePressure.test.ts`(18件: 単調上昇/上げ下げτ差/被弾インパルス/ヒステリシス2件/天井
  クランプ/Intensity停止/Rank開始値/レバー変換/配役順/許可集合/maxRung・ゾーン変換)、
  `difficultyDirector.test.ts`にmaxRung検証2件を追加。
- 検証: typecheck / lint(0, full) / test(189 pass, 1 skip) / build 通過。
- 負荷スコア: **1/10**(設計どおり。毎フレームはスカラー演算+配列2要素の走査のみ。配役投入も
  既存のforcedTypeスポーン経路を再利用、新規描画なし)。
- 次: バッチ3完成版(Tank化=プール撤退+配役演出フック)、または他バッチへ進む前に実機確認を推奨

**実装結果(v0.25.1354・完成版)**: 設計どおりpumpkin/werewolfを`enemyUtils.ts`の`AREA_WEIGHT`で
**全エリア0化**(通常湧きプールから完全撤退)。
- 副作用の点検: `buildEnemy`(`spawnEnemyAt`/`generateEnemy`共通の内部ヘルパー)は
  `isValidForArea(type, area)`が偽の時に自動で`sceneSpawn: true`を立てる既存機構があるため、
  0.50/0.65の配役投入(`generateEnemy`にforcedType指定)で生成されたpumpkin/werewolfは
  **今回の変更後は常に自動でsceneSpawn保護がかかる**(全エリアでisValidForArea=falseになった
  ため)。距離リサイクル(エリア外に出た敵の強制回収)の除外条件は`!enemy.sceneSpawn`を
  すでに見ているので、追加のコード変更は不要だった。講習/狩猟(featuredFloor経由)・
  セットピース(`spawnEnemyAt`・isWave=trueで元から除外対象)・イベント(`fromEvent`で除外)も
  同様に影響なし。
- 到着演出フック(バナー/スティングSE)は仕様上「素材は後日・任意」と明記されているため、
  今回は見送り(既存の配役投入コード自体がフック済みの状態=`generateEnemy`呼び出し直後に
  差し込めば済む)。
- テスト: `enemyUtils.test.ts`に「pumpkin/werewolfは全エリアでisValidForArea=false」の検証を追加。
- 自己点検: 憲法第4条(初心者ゾーン)に抵触しない(元から初心者ゾーンは0だった。今回は中盤以降の
  ゾーンも0にしただけで、緩める方向ではなく問題児の出現経路を集約する変更)。憲法第5条(緩を
  荒らさない)にも抵触しない(講習/狩猟のfeaturedFloor経路は`Math.max(areaW, 0.5)`の床がそのまま
  効くため無変化)。
- 検証: lint / typecheck / test(291 pass, 1 skip) / build 全通過。実機未確認(統合テストへ)。
  (関所に入った瞬間の体感、配役タイミングの違和感有無、`?ladder=0`との比較)。

### 実機確認①(v0.25.1308前後のビルド・社長スクショ・設計チャットで原因分析済み v0.25.1311)
**結果**: 生存3:56・Lv5・死因=パンプキン落下攻撃・最深到達=研究対象区域(**エリア1=序盤ゾーン**)。
Intensity平均0.65/最大1.00、**1:35から死亡まで約1.0に張り付き**。Perf平均0.52。社長所感
「後半ずっとMAXで息継ぎがない」。

**原因分析(コード照合によるタイムライン復元)**:
| 時刻 | 起きたこと |
|---|---|
| 0:00-1:35 | buildup①(relief-sparse)。穏やか・Perf高(グラフと一致) |
| 1:35 | gate①開始=張り付き開始点と一致 |
| 1:45 | セットピースのパンプキン(台本固定・エリア不問) |
| **2:00** | **囲いイベント発火(ARENA_FIRE_AFTER_MS=2:00固定・以後2分毎): 18体ホード+閉じ込め** |
| 2:15-3:45 | buildup②だが中身は講習パンプキン(relief-pumpkin・featuredFloor=trueでエリア1でも湧く) |
| **3:00** | **ハンター参戦(HUNTER_START_MS=3:00固定)。追跡中dangerBias=1** |
| 3:45 | gate②(gate-chaos)開始 |
| 3:56 | パンプキン落下攻撃で死亡(講習かセットピース由来。エリア1の自然湧きプールにpumpkinは重み0) |

**結論**:
- **主犯=台本外の固定スケジュールイベント**(囲い2:00+ハンター3:00)。Intensityの式
  (近8体でswarm飽和+被弾スパイク下りτ4s+danger源0.5)に対し、閉じ込め18体+追跡+被弾連打で
  張り付くのは**計器として正しい**——実際に息継ぎが存在しなかった。
- **従犯=緩フェーズが固定で講習パンプキンを流す台本**(苦戦中でも休憩にならない→バッチ4の領分)。
- **gatePressureは無実(正常動作)**: エリア1のゾーン天井0.34により問題児解禁(≥0.35)も配役(≥0.50)も
  数+2(≥0.55)も不発、テンポ加速も最大×0.85程度。関所自体は設計どおり抑制されていた。
- **対処(社長決定)**: バッチ2.5(診断計測)→バッチ7(イベントプロデューサー)前倒し。
  憲法第5条「緩を荒らさない・ピンチに撃たない」+囲いの台本編入+ハンターの退屈シグナル統合が
  今回の主犯に直撃する。講習の固定問題はバッチ4で解消。

---

## バッチ3.5: チャフ配合+盤面在庫(社長承認 v0.25.1318) — **実装済み v0.25.1320**

社長との協議(実機確認②後)で決定。2つの欠落を埋める:
①チャフ3種は役割がまったく違うのに、現状は「エリア重み×featured乗算」の成り行き任せで
**狙って配合できない**。②台本・イベント・配役はどれも**盤面に何が残っているかを見ずに**投入
しており、固い敵(ゾンビ/パンプキン)を捌き切れないままピークやイベントが重なる=火に油。

### 3.5-A: チャフ配合(役割ベースのスポーン)

**役割テーブル(社長定義・データとして持つ)**:
| 型 | 役割 | 使いどころ |
|---|---|---|
| bat | **爽快**(気持ちよさの源・スピード感) | RELAX/HARVEST/無双の主成分。経験値稼ぎはここ |
| skeleton | **刻み**(中間・軽い緩急) | どの場面にも混ぜられる調味料 |
| zombie | **壁**(防衛配置・クリティカル前提の固さ) | スピードを落とす/弾と組んで射線を塞ぐ/道を塞ぐ。緩と稼ぎ場は「**群れに1体**」まで(完全0ではない・バランス調整枠=社長指示) |

問題児(plant/犬/パンプキン/ghost/screamer)は既存の配役・許可制のまま(本バッチはチャフ3種のみ)。

**仕様**:
- `SpawnScene.mix?: { bat: number; skeleton: number; zombie: number }`(比率)を追加。
  mix指定シーンでは**チャフの抽選をこの比率で直接引く**(エリア重みの役目は問題児と出現可否に
  残す。深部でbatを出す件は既存featuredFloorと同じ床の仕組みを流用)。
- 叩き台の配合(すべて実機調整前提):
  | シーン/演目 | bat | skeleton | zombie | 意図 |
  |---|---|---|---|---|
  | 純休憩(relief-sparse) | 70 | 25 | 5 | スピード感=バット中心 |
  | 無双(mowdown) | 60 | 35 | 5 | 刈る気持ちよさ |
  | 講習(relief-pumpkin/wolf) | 55 | 40 | 5 | 主役(問題児)を立てる |
  | 関所(gate-pumpwolf等・数系) | 30 | 45 | 25 | テンポは速いが壁で足が止まる |
  | 関所(mass-ranged・射線系) | 25 | 35 | 40 | **壁+弾のコンボ**(社長の言うバリエーション) |
  | boss | 30 | 40 | 30 | — |
- **関所内はpressureで配合を連続シフト**: `mix = lerp(シーンmixの爽快寄り変形, シーンmix, pressure)`
  (入りはバット多めで走らせ、捌けているほど骨→壁へ。テンポに続く第二の連続レバー。
  数は触らない=憲法第3条のまま)。
- **v2(本バッチではやらない・実機確認後)**: ゾンビの湧き位置をプレイヤー進行方向前方にバイアス
  (「道を塞ぐ」の実装)。batは全方位のまま。
- 実装: 新規純関数 `src/utils/chaffMix.ts`(役割テーブル+`pickChaffMix(scene, pressure)`)、
  `selectEnemyType`にmix経路を追加、`useGameLoop.ts`貫通。`?mix=0`で従来抽選に完全復帰。
- テスト: mix指定シーンで比率どおりに偏ること/未指定シーンは従来と完全一致/pressureシフトの単調性。
- 負荷スコア: **1/10**(抽選時の重み計算のみ)。

**実装結果(v0.25.1320)**: 設計どおりに実装。`SpawnScene.mix?`を追加し、表の6シーン
(relief-sparse/relief-pumpkin/relief-wolf/mowdown/gate-pumpwolf/gate-mass-ranged/boss)に
叩き台の比率をそのまま設定。`selectEnemyType`は「チャフ3種(bat/skeleton/zombie)の合計重み
(エリア規約どおりの総量)は保ったまま、内訳だけをmix比率で置き換える」方式(問題児の重み/
出現可否は一切変えない)。**解釈が必要だった点(実機調整前提・透明化)**:
①「シーンmixの爽快寄り変形」の具体的な変形式が仕様に無かったため、bat比率×1.6・zombie比率×0.4
してから正規化、という妥当な値を採用(`EXCITED_BAT_BOOST`/`EXCITED_ZOMBIE_CUT`)。
②配合表に無い`gate-chaos`(全部盛り)はmix未指定のまま=従来のエリア重み任せに自動フォールバック
(社長への確認候補として残す。指定が欲しければ次の会議で決める)。
テスト: `chaffMix.test.ts`(9件・正規化/爽快変形/pressureシフトの単調性)、
`enemyUtils.test.ts`に統計的な偏りテスト3件追加、`difficultyDirector.test.ts`にシーン別mix値の
アサート追加。lint/typecheck/test/build 全通過。

### 3.5-B: 盤面在庫(boardDebt)

**「今盤面に何がいるのか・倒したのか」を知ってから投入する**(社長指示)。

- **新規純関数** `src/utils/boardDebt.ts`: `debtFor(enemies)` = Σ 型の重さ × (残りHP/最大HP)。
  叩き台の重さ: bat 0.5 / skeleton 1 / zombie 2.5 / plant 3 / 犬 4 / screamer 4 / ghost 5 /
  パンプキン 6(ボス系は対象外=別トラック)。残りHP比を掛けるので「半分削ったパンプキン=在庫3」
  として「倒したのか?」が連続値で入る。敵は最大20体なので計算コストは無視できる。
- **在庫を見る場所(4箇所・すべて投入を絞る方向のみ)**:
  1. **イベント発火ゲート**(バッチ7の`eventGateOk`に`boardDebt`入力を追加): debt >
     EVENT_DEBT_MAX(叩き台12)なら囲い/ハンター/叫びを**延期**(タイマー消費なし・掃けたら発火)。
     憲法第5条「ピンチに撃たない」の盤面版。
  2. **配役投入**(gatePressureの0.50/0.65跨ぎ): **直前の配役個体が存命中は次を投入しない**
     (L4DのTank存命中ルール)+debt > CAST_DEBT_MAX(叩き台10)で延期。
  3. **湧きテンポ**: `interval ×(1 + 0.05×max(0, debt−8))`(上限×1.6)。
     PUMPKIN_PAIR_SPAWN_EASEの一般化=固いのが溜まるほど注ぐ量が細る。
  4. **関所の登り**: gatePressureの`risingBlocked`条件に「debt > RISE_DEBT_MAX(叩き台10)」を追加
     (盤面が掃けるまで圧は登らない)。
- **台本PHASESの時刻(骨格)は動かさない**: フェーズは時刻どおり来るが、盤面が汚れていれば中身の
  投入が絞られる、という形(骨格不変の原則と矛盾しない)。
- 診断: `DirectorSample`に`debt`を追加し、リザルト診断グラフに第4の線。イベント延期は既存の
  イベント帯の発火位置で読める。
- `?debt=0`で全無効(従来挙動)。
- テスト: debtFor(重さ×HP比・ボス除外)/各しきい値ゲート/テンポイーズの単調性・上限。
- 負荷スコア: **1/10**(スポーン判定時の敵配列走査のみ・毎フレーム新規計算なし)。

**実装結果(v0.25.1320)**: 設計どおり4箇所に配線。新規純関数 `src/utils/boardDebt.ts`
(`debtFor`/`debtTempoEaseMult`+3つのしきい値定数)。boardDebtは`useGameLoop.ts`のフレーム冒頭
(gatePressureの計算前)で1回だけ計算し(`boardDebtNow`)、後段の3箇所(gatePressure/配役投入/
湧きテンポ)はそのフレーム内の値を直接使用。イベント発火ゲート(4箇所=囲い/紅き月/ハンター/叫び)
はフレーム冒頭より前で判定するため、`pityEventBlockUntilRef`と同じ1フレーム遅延パターン
(`boardDebtRef`)で前フレームの値を読む。
**解釈が必要だった点(社長承認済みの仕様を具体化・透明化)**: 「L4DのTank存命中ルール」は
「配役投入した型が1体でも生存中は次を投入しない」と解釈し実装(旧cap=2から変更)。
既存のパルス方式(castFirstNow/castSecondNow=0.50/0.65跨ぎの一瞬だけtrue)のままだと
「延期」中にパルスを取りこぼすため、`pressureCastRef.current.pendingCast`で保留し、
Tank死亡+debt解消まで毎フレーム再チェックする仕組みを追加(タイマー消費なし=仕様どおり)。
`?debt=0`はTank存命中ルールと湧きテンポイーズの両方を含めて完全に旧挙動(cap=2・延期なし)へ
戻す。診断: `DirectorSample.debt`追加+`DirectorResult.tsx`に第4の線(スレート色・
DEBT_DISPLAY_MAX=20で0-1へ正規化して表示。実際のしきい値超えはイベント帯/関所帯の挙動で読める)。
テスト: `boardDebt.test.ts`(11件)、`gatePressure.test.ts`/`eventProducer.test.ts`/
`aiDirectorDebug.test.ts`に既存挙動の非破壊(debt省略=0と完全一致)を含めて追加。
lint/typecheck/test/build 全通過。実機未確認(4箇所の相互作用・しきい値の体感は実プレイ推奨)。

---

## バッチ4: 緩の演目選択(RELAX/HARVESTのプロデュース)★実機確認③を受け具体化(社長決定 v0.25.1322)・**実装済み v0.25.1326**

フェーズ境界(山→緩)で、直前の山の試験結果(バッチ2)から演目を選ぶ。演目=SpawnSceneと同形の
データ(mix/featured/suppressed/intervalMult/rareMult+XP倍率適用有無)。**台本PHASESの時刻・種別は
不変**で、緩フェーズの「中身(シーン)」だけを演目が差し替える。

**実機確認③での社長決定を反映**:
- **HARVEST=雑魚無双**(旧案「パンプキン2体の狩猟」は廃止)。「育った自分を確認する時間」は
  バット中心の濁流を刈って稼ぐ時間にする。
- **講習は主役1体だけ**: 「初心者エリアで序盤にパンプキン2体が時間差で出る」「やっと倒したら
  すぐ沸いた」の解消。講習の主役(pumpkin/werewolf)は**同時1体・1フェーズ合計1体**
  (キル後は再投入しない=倒したら残り時間はbat中心チャフに切替)。
- **終盤の緩はだれさせない**: 城ボス時刻(7:00)以降の緩は講習・純休憩を選ばず、
  **既定=HARVEST無双(報酬高め)**。例外は成績最悪(score<0.25)時の短い純休憩のみ。

| 演目 | 内容 | 選出条件(叩き台) |
|---|---|---|
| 純休憩(RELAX) | batまばら(mix=70/25/5・interval1.3・レア0・XP等倍) | 山の成績が悪い(score<0.4)。7:00以降はscore<0.25のみ |
| 講習: 犬だけ | werewolf**1体だけ**(同時1・再投入なし)+bat中心チャフ | 序盤(〜4分)かつ犬キル経験が少ない かつ成績普通以上 |
| 講習: パンプキンだけ | pumpkin**1体だけ**(同上) | 序盤(〜4分)かつ同上 |
| 回収 | 前の山の主役1種を弱め少数+チャフ | 山でその敵のキルが少ない=苦戦気味 |
| **HARVEST(雑魚無双)** | **bat中心濁流(mix=70/25/5・interval0.6・rareMult0.5・XP倍率適用)** | 成績普通以上の既定。**7:00以降の緩は常にこれ** |
- 旧「接待(遠距離/近接)」はHARVESTのスタイル微変形(近接=skeleton増し等)としてv2で検討(本バッチではやらない)。
- XP倍率(既存のrankAdj.rewardMult)は HARVEST/回収で適用、純休憩・講習は等倍。
- 実装: `src/utils/reliefProgram.ts`(純関数: {gameTime, 直前gateの成績score, killsByType累計,
  style} → 演目)。useGameLoopは緩フェーズ突入時に1回選び、そのフェーズ中はシーンとして使う。
  講習の「1フェーズ1体」は演目側の状態(投入済みフラグ)で管理。`?program=0` で従来の固定シーンに
  戻せる。デバッグ: ?director=1オーバーレイに演目名を表示。
- テスト: 選出条件の分岐(成績悪→純休憩/序盤+未経験→講習/既定→HARVEST/7:00以降→HARVEST固定)/
  講習の1体制限。
- 負荷スコア: 1/10(フェーズ境界で1回の選択のみ)。

### バッチ4と同時実装: 問題児リフラクトリ(3.5-Bの追補・社長報告「やっと倒したらすぐ沸いた」対応)
- `PROBLEM_REFRACTORY_MS = 15000`(叩き台): **同型の問題児(pumpkin/werewolf/plant/ghost)をキル
  してから15秒間は、同型を再投入しない**。適用経路は通常湧き抽選(featured床含む)+gatePressure
  配役の両方(セットピース復帰時も対象)。ボス系・screamer(専用ディレクター持ち)は対象外。
- 実装: killTelemetry(バッチ2)のキル計上に「型ごとの最終キル時刻」を追加し、スポーナ/配役が参照。
- 「やっと倒した」の余韻の保証=盤面在庫(debt)が下がった直後に同じ重さを即補充しない。
- テスト: キル直後は同型が選ばれない/15秒後は選ばれる。

### 実装結果(v0.25.1326)
- 上記仕様どおり実装。**解釈が必要だった点(社長承認済みの仕様を具体化・透明化)**:
  - 「山の成績score」= 既存の`evaluatePhasePerformance`(DirectorRankが使う連続0-1スコア)をそのまま
    使用。フェーズ境界(rankRefと同じ`rankPhaseKey`トリガー)で`rankRef.current.lastPerf`へ保存し、
    演目選択が読む(rank自体=離散0/1/2の既存システムには触れない・並行運用)。
  - 「苦戦気味(回収の対象)」判定 = 直前フェーズが`gate`だった場合のみ、そのgateの`scene.featured`
    のうちキル数がSTRUGGLE_KILL_MAX(2)未満で最少の型を採用(バッチ2の`getPhaseKillDebug`が
    保持する直前フェーズの種別キル内訳を利用)。buildup→buildupの連続(通常は起きない)や初回
    フェーズは対象データが無いためnull=回収なし→HARVESTへフォールバック。
  - 「1フェーズ合計1体・キル後は再投入しない」= 演目選択時に`reliefProgramRef`へ
    `{phaseKey, program, lessonSpawned:false}`を保存。主役が実際に1体湧いた瞬間(通常湧きループの
    型判定)に`lessonSpawned=true`へ。以後はプログラムの複製(`effectiveProgram`)でfeatured/
    featuredFloorを空にして通常distribution(mix)へ切替。programオブジェクト自体
    (xpBoost/lessonPrimary)は変更しない=xpBoost判定は影響を受けない。
  - `?program=0`は演目選択・XP倍率条件・**問題児リフラクトリ(3.5-B追補)も含めて**一括で旧固定
    シーン挙動へ戻す(リフラクトリは本バッチと同時実装のため同じフラグでスコープ)。
- デバッグ: `src/utils/reliefProgramState.ts`(gatePressureState.tsと同型のシングルトン)経由で
  選定中の演目IDと講習投入済みフラグを`?director=1`オーバーレイに追記表示。
- テスト: `reliefProgram.test.ts`(9件)/`killTelemetry.test.ts`・`killTelemetryState.test.ts`の
  リフラクトリ追加分。lint/typecheck/test(257 pass, 1 skip)/build 全通過。
- 負荷スコア: 1/10(フェーズ境界で1回の演目選択+毎フレームの型判定のみ・描画コストなし)。
- 実機未確認(演目の切り替わり・講習1体制限・回収の弱め感の体感は実プレイ推奨)。

## バッチ5: 山の台本選択・**実装済み v0.25.1340**

緩明けに、次の関所の「台本」をメニューから選ぶ(時刻のmaxRungが常に優先)。
| 台本 | 主題 | maxRung |
|---|---|---|
| 数の関所 | チャフ濁流(CD極短) | 3 |
| 射線の関所 | plant中心 | 4 |
| 判断の関所 | 犬orパンプキン1種+弾 | 5 |
| 三択の関所 | 犬+パンプキン+弾 | 6 |
| 不意打ちの関所 | +叫び/ゴースト | 7 |
- 選択 = min(台本のmaxRung, PHASESのmaxRung, ゾーン上限) を満たすものから、Rank/スタイル/
  直近で見せていない主題を優先(同じ台本の連続を避ける)。
  ※maxRungは§バッチ3の変換表でpressure天井になる(台本の値付けは離散のままでよい)。
- 実装: `src/utils/gateProgram.ts`(純関数)。

### 実装結果(v0.25.1340)
- 上記仕様どおり実装。**解釈が必要だった点(社長承認済みの仕様を具体化・透明化)**:
  - 「選択 = min(台本のmaxRung, PHASESのmaxRung, ゾーン上限)を満たすもの」= 適格フィルタは
    「台本自身のmaxRung ≤ PHASESのそのgateスロットのmaxRung」で判定(ゾーン上限は既存の
    `Math.min(zoneCeiling, rungCeiling)`パイプラインに選定後の台本maxRungをそのまま流すことで
    自動的に反映される=台本選択の適格フィルタ自体には二重で入れていない)。
  - 各台本の具体的な`featured`/`intervalMult`/`rareMult`/`mix`数値は、テーマ名から導出しつつ
    既存シーン定数と極力揃えた: 射線=`SCENE_GATE_MASS_RANGED`と同値/三択=`SCENE_GATE_CHAOS`と
    同値(社長の分類にすでに存在する台本と一致させた)。数の関所/不意打ちの関所は新規導出
    (数=featuredなし・interval0.5の高速チャフ流/不意打ち=三択+screamer・ghostをfeaturedへ追加)。
    実機調整前提(バッチ3.5/4と同じ透明化ポリシー)。
  - 「判断の関所」の犬orパンプキンの選び方 = 既存`specialCastOrder`(近接→犬/遠距離→パンプキン/
    バランスはタイブレーク乱数)と同じスタイル対応ロジックを`gateJudgmentProgram`として再利用実装。
  - 「Rank/スタイル/直近で見せていない主題を優先」= 直近回避を最優先(除外して選択肢がゼロに
    ならない限り必ず除外)、次にRankで難度側へ寄せる(rank2=適格中最難/rank0=適格中最優/
    rank1=タイブレーク乱数で中庸〜バラけさせる)。スタイルは「判断の関所」の主役選び(上記)
    にのみ適用し、5つの台本レベルの選択自体はスタイル非依存(他4台本はスタイルで差別化する
    要素が無いため)。
  - バッチ4の回収判定(`lastGateFeaturedRef`)を、選定済み台本のfeatured(実際に表示されたもの)
    優先で読むよう修正(バッチ5適用前はPHASES固定シーンのfeaturedしか読めなかった)。
- `?gateprogram=0`で本バッチを丸ごと無効化し、PHASES固定シーン/maxRungへ戻せる。
- デバッグ: `src/utils/gateProgramState.ts`(gatePressureState.ts等と同型のシングルトン)経由で
  選定中の台本IDとmaxRungを`?director=1`オーバーレイに追記表示。
- テスト: `gateProgram.test.ts`(11件・適格性フィルタ/rank別優先/直近回避/判断の関所のスタイル
  対応)。lint/typecheck/test(269 pass, 1 skip)/build 全通過。
- 負荷スコア: 1/10(フェーズ境界で1回の選択のみ)。
- 実機未確認(台本ごとの手触り・rank優先の体感は実プレイ推奨)。

## バッチ6: ステージ難易度指数(社長承認済み・各ステージでテスト予定)・**実装済み v0.25.1355**

- **ステージ2(研究所/lab)は例外**=完全対象外(現行どおりDDA/シーン/ディレクター系すべて無効の
  固定難易度を維持)。それ以外のステージに攻撃性スカラー `stageAggro` を1本持たせる:
  | ステージ | stageAggro | 対象層 |
  |---|---|---|
  | 1(森) | 0.0 | 初心者 |
  | 3(廃都) | 0.35 | 初心者+ |
  | 4(雪原) | 0.6 | 初心者〜中級 |
  | 5(軍本部) | 0.8 | 中級 |
  | 6(洋館) | 1.0 | 中級+(ストーリーは中級でクリア可能、が上限の目安) |
- 効くところ(すべて「いやらしくない」側のノブ):
  - pressureの上げ時定数(旧「ラダーの上げ待ち時間」に相当): `上げτ = 10s − 4s×stageAggro`
    (stageAggro=0.5でバッチ3既定の8sに一致。バッチ6実装まではバッチ3の8s固定でよい)
  - 退屈発動: `BORED_START_MS = 30s − 10s×stageAggro`
  - 関所のmaxRungクランプ: `min(scriptMaxRung, 3 + round(4×stageAggro))`
    (ステージ1は最大段4まで=三択・叫び・ゴーストは出ない。上級者はエリア(距離)で遊ぶ)
- ステージkeyの解決は既存のステージ定義(stage.bgm/theme)から。数値はすべて実機調整前提。

### 実装結果(v0.25.1355)
- 新規純関数 `src/utils/stageAggro.ts`: `stageAggroFor(stageId)`(明示表+stage-7/EX継続1.0+
  未知/未選択/lab=中立値0.5)/`riseTauSForAggro`/`boredStartMsForAggro`/`gateMaxRungClampForAggro`。
  ステージkeyの解決は仕様の「stage.bgm/theme」ではなく、既存の`getSelectedStageId()`
  (`src/data/progress.ts`・localStorage直読み。pixiScene.tsが毎フレーム描画で使っているのと
  同じ確立済みパターン)をそのまま流用——`campaign.ts`のid('stage-1'等)を直接引けるため
  bgmキー経由より素直で、既存コードとの重複も避けられる。
- `src/utils/gatePressure.ts`: `stepGatePressure`の入力に`riseTauS?`を追加(省略時UP_TAU_S=8sで
  従来と完全一致)。`src/utils/boredomDirector.ts`: `boredomBonus(boredMs, boredStartMs?)`に第2引数を
  追加(省略時BORED_START_MS=25000msで従来と完全一致)。
- `useGameLoop.ts`: `?stageaggro=0`で中立値0.5固定(バッチ6導入前と完全一致)。`currentStageAggro()`
  ヘルパー(モジュールレベル・localStorage読み取りのみで軽い)を関所pressureのriseTauS/
  退屈発動しきい値(3箇所: upswingBonus本体/バッチ7 eventSizeMult/ハンターのhunterBoredomReady)/
  関所maxRungクランプ(`effectiveMaxRung = min(scriptMaxRung, gateMaxRungClampForAggro(...))`、
  台本自身のmaxRungとゾーン天井のminを取る既存ロジックはそのまま)に配線。
- **式と注記の食い違いを発見**: ★未決事項に記載(ステージ1のmaxRungクランプが式どおりだと3
  ↔ 注記は4)。式を字義どおり実装し、値の変更は行わずに社長裁定待ちとした。
- テスト: `stageAggro.test.ts`(11件: 明示表/終盤継続/中立値フォールバック/各式の境界値・no-op点)。
- 自己点検: 憲法第4条(初心者ゾーン)に抵触しない(初心者ステージ(stage-1)ほどaggroが低く、天井が
  上がる方向には変化しない)。憲法第5条(緩を荒らさない)にも抵触しない(効くのは関所中のpressure/
  退屈しきい値のみで、緩フェーズの演目選択自体は変更していない)。
- 検証: lint / typecheck / test(302 pass, 1 skip) / build 全通過。実機未確認(統合テストへ)。

---

## バッチ7: イベントプロデューサー(社長承認済み)★前倒し決定(v0.25.1311) — **実装済み v0.25.1313**

**前倒し決定(社長・v0.25.1311)**: 実機確認①(§バッチ3末尾)で「後半の張り付き」の主犯と特定された
ため、バッチ2.5(診断計測)の直後に実装する(3完成版/4/5/6より先)。実測の裏付け: 囲い=2:00固定+
以後2分毎、ハンター=3:00固定が、エリア1(序盤ゾーン)の緩フェーズとピンチを踏み荒らした。
依存はバッチ2(実装済み)のみ。

イベントの発生タイミングと規模(撃退数)をAI側へ渡す。現状はイベントが台本の外で
タイマー/ランダムで勝手に鳴っており、緩を荒らす・山に重なる事故が構造的に起きる。

### 大原則(社長指示)
**イベントはノルマではない。** 発生総数は現状を基準にしない。退屈していなさそうなら
無理に差し込まない(プレイヤーが十分エンゲージしている時のイベントは邪魔でしかない)。
イベントは「退屈シグナルが出ている時のスパイス」「緩急に変化をつける切り札」として使う。

### 憲法第5条(追加)
「**大イベントは重ねない・ピンチに撃たない・緩を荒らさない**」
- 大イベント(囲い/紅き月/ハンター/裏ボス)は同時に1つまで(既存の相互抑制を明文化・統一)。
- ピンチ救済(pity)発動中+解除後10秒はイベント発火禁止。
- イベント中は反応型ディレクターをPEAK扱い(紅き月のdangerBias=1方式を全大イベントへ拡張)。

### イベント種別ごとの扱い(全て社長承認済み)
| イベント | 新しい扱い |
|---|---|
| 囲い(ホード/ミニボス) | **「山の台本メニュー」の一枚に昇格**(バッチ5の「イベント関所」)。関所窓の頭で発火=関所そのものがイベントになる。ランダム割り込み廃止 |
| 救助 | **「緩の演目」の一枚に**(バッチ4のメニューへ追加。お使いで稼ぐ=HARVEST系) |
| 紅き月 | **緩フェーズ中にしか開始しない**(既存の山に重ねない)。5〜9分のランダム窓は維持(いつ来るか分からない心理効果)。窓が関所中に開いたら次の緩まで遅延 |
| ハンター | **独自の優勢判定を廃止し、退屈シグナルに統合**。上振れ枠(+数)では足りないほど余裕な相手への「質の緊張」の切り札。CDは維持 |
| 叫び(screamer) | 関所中のみ発火(バッチ3のpressure≥0.80解禁と自然統合。pressure実装まではフェーズkind===gateのゲートだけ先行導入可) |
| 城ボス | 触らない(7分・スタート近辺=骨格) |

### 規模(撃退数)のスケール
- Rank/退屈シグナル高 → ホード頭数・救助攻撃者数を増(例: Rank2で+25%。実機調整前提)
- pityが直近で発動していた → サイズ減 or そもそも選ばれない
- ステージ指数(バッチ6)も規模に乗算

### 実装の形
- `src/utils/eventProducer.ts`(純関数): 入力 {phase kind, boredom, pityLevel, rank,
  activeEvent有無, 各イベントCD} → 出力 {発火可否, 種別の優先度, sizeMult}。
- 既存の各イベントタイマー(nextArenaAt/redNightFireAt/hunterRef/screamerRef)はベース
  スケジュールとして残し、プロデューサーが**ゲート/調整**する(いつものハイブリッド)。
- `?events=0` で従来のランダム発火に完全復帰(切り分け用)。
- 依存: バッチ2(計測)のみ。ラダー(バッチ3)より先に実装しても成立する。

### 実装結果(v0.25.1313)
- `src/utils/eventProducer.ts`新設(純関数のみ): `eventGateOk`(大イベント排他+ピンチ猶予
  `PITY_EVENT_BLOCK_TAIL_MS`=10秒)/ `redNightPhaseGateOk`(緩フェーズのみtrue)/
  `screamerPhaseGateOk`(gateのみtrue)/ `hunterBoredomReady`(上振れ枠が天井
  `BORED_BONUS_MAX`まで使われていたらtrue)/ `eventSizeMult`(rank/退屈/直近pityでの倍率、
  未使用箇所あり=下記「今回やらなかったこと」参照)。テスト`eventProducer.test.ts`(12ケース)。
- `useGameLoop.ts`に`EVENTS_ENABLED`(`?events=0`で全ゲート無効=旧挙動)と
  `pityEventBlockUntilRef`(ピンチ発動中+解除後10秒の猶予。他のフレーム遅延パターンと同じく
  前フレーム値を読む)を追加し、既存4イベントの発火条件へゲートを重ねた:
  - **囲い**: 既存の裏ボス/ハンター排他に**紅き月との排他**を追加+ピンチ猶予。台本メニューへの
    昇格(ランダム割り込み廃止)自体はバッチ5待ち=今回はタイマーはそのまま、ゲートだけ追加。
  - **紅き月**: 既存の裏ボス排他に**囲い/ハンターとの排他**を追加+ピンチ猶予+**緩フェーズ
    (buildup/boss)中にしか開始しない**(`phaseAt(newGameTime).kind`を再取得して判定)。
    抽選(`redNightFiredRef`)は全条件が揃うまで消費しない=関所中に窓が開いても自動で次の緩まで
    遅延する(狙い通りの副作用として実装、追加コードなし)。
  - **ハンター**: 独自の6項目優勢判定(スコア≥4)を**廃止**し、既定は`hunterBoredomReady`
    (退屈シグナルの上振れ枠が天井まで使われている)+ピンチ猶予に統合。CD/出現条件(3分/演出中
    禁止など)は無変更。`?events=0`時のみ旧6項目スコア判定にフォールバック(ロジック自体は
    削除せず残置)。
  - **叫び(screamer)**: `screamerPhaseGateOk`で**関所中のみ**発火するよう追加+ピンチ猶予。
    pressure≥0.80との統合(バッチ3側)は今回未着手=フェーズ種別だけの先行導入(仕様通り)。
  - **城ボス**: 無変更。
  - **救助**: 無変更(バッチ4待ち。憲法第5条の「大イベント」リストにも元々含まれていない)。
  - **反応型ディレクターのPEAK扱い拡張**: `dangerBias=1`の強制を紅き月専用から
    **囲い(rescue以外のactiveEvent)/裏ボス追跡中(bossChasing)にも拡張**。ハンターの索敵/追跡/
    撤退はすでに専用カーブ(chase=1/search=0.6/retreat=0.3)を持つため上書きしない。
- **今回やらなかったこと(透明化)**: 規模(撃退数)スケール`eventSizeMult`は関数として実装・
  テスト済みだが、**呼び出し側への配線は見送った**。囲いのホード数(`ARENA_HORDE_COUNT`)は
  「1秒に3体・6/12/18体目だけパンプキン/ウルフ」という段階スポーンの決め打ちindexに依存しており、
  単純に頭数へsizeMultを掛けるとその決め打ちが崩れる。実機確認①の主犯(固定タイマーで無条件発火
  すること)はゲート追加で解消済みのため、数のスケールは影響範囲が広い割に本バッチの主目的
  (張り付き解消)への寄与が薄いと判断し、別途スケール専用の切り出しが要る作業として保留した。
  同様に「山の台本メニューへの囲いの昇格」「救助のバッチ4メニュー化」もバッチ5/4待ちで未着手
  (今回追加したのはゲートのみ)。
- 負荷スコア: **1/10**(追加コストはすべてスカラー比較+既存refの参照。`phaseAt()`の再呼び出しは
  15件程度の配列走査を1フレームにつき最大2箇所追加しただけ=既存の`outdoorGate`計算(line 980)と
  同種で無視できる)。
- 確認: `npm run lint && npm run typecheck && npm test && npm run build` 全通過。実機での
  「後半の張り付き」再発有無は次回プレイで確認予定(実機確認②)。

---

## 変えないもの(このプロジェクト全体で不変)
- AREA_BASE_DIFFICULTY(距離→強さ倍率)/ COLOR_RATE_BY_AREA基礎値 / エリア境界5層
  (未確認汚染=中〜上級ブリッジとして機能。境界調整はバッチ2のtelemetryを見てから)
- 城ボス=スタート近辺ルール(上級者は遠征がエンドコンテンツ)
- screamer/ハンター/死神/裏ボスの専用ディレクター(別トラック)
- ピンチ救済(pity)・武器箱AI制御・紅き月連携(v0.25.1286-1290)

### 実機確認②(v0.25.1314ビルド・社長報告「最初から弾飛んでくるしゾンビ沸きまくるしでストレスしかない」・原因特定済み v0.25.1315)
**原因(2つ・どちらも今回の改修1312/1313より前から存在。1308→1313のdiff全行確認で同diffは無罪)**:
1. **セットピース固定台本(`src/utils/stageDirector.ts` WAVE_EVENTS)**: 0:35に弾plantをプレイヤー
   周囲260pxへ直接湧き(エリア不問=開始安全圏でも)/1:45パンプキン/2:50plant/3:55七体白兵
   オンスロート(犬入り)/4:55パンプキン2。`spawnEnemyAt`直呼びで**エリア規約・gatePressureの
   問題児ブロック・憲法の数上限を全て素通り**。実機確認①の死亡(3:56)も3:55オンスロートの1秒後
   だった(①の分析の見落とし)。バッチ1.5時の保留「セットピースはバッチ7で整理」を設計チャットが
   バッチ7仕様に書き落としたため未整理で残存(設計チャットの落ち度)。
2. **退屈上振れの開始直後誤認(v0.25.1298から)**: Performance初期値が「開始時=無被弾20秒扱い+
   HP満タン」のため開始約5秒で退屈判定に入り、開始70〜90秒で+10体(基本8→18体)。まばらな
   はずのbuildup①が毎回濁流化+「湧く→押される→静まる→また湧く」のポンプ運動。
   plantエリア1重み1.0(湧きの約8%)が総湧き増で体感倍増。

**応急処置(v0.25.1315・社長指示「はやくしろ」を受け設計チャットが直接実装)**:
- セットピース台本を**既定OFF**(`SETPIECE_ENABLED = ?setpiece=1 の時だけON`)。城ボス(7分)は
  別経路なので不変。見せ場の再設計はバッチ4/5で。
- 上振れに**ラン開始90秒のグレース**(`BORED_RUN_GRACE_MS`、`stepBoredom`にgameTimeMs追加・
  省略時は従来挙動=テスト互換)。`?upswing=0`の全無効フラグは従来どおり。

### 実機確認③(v0.25.1320-21ビルド・診断グラフ初の実戦読解・社長所感3点 → バッチ4具体化 v0.25.1322)
**結果**: 生存11:32・Lv11・撃破320・死因=死神(10分の時間トリガー・仕様どおり)・最深到達=
研究対象区域。RELAX 54%(前回39%)・BUILD 26%・PEAK 20%と、**心電図の谷は大幅に回復**
(社長「だいぶマシになってきた」)。診断帯も機能: 関所帯・囲い(2分毎)・城ボス帯(7:00以降)・
リーパー帯・gatePressure線(関所での登りが見える)・盤面在庫線がリザルトから読めた。

**残る所感3点(社長)と原因**:
1. 「初心者エリアで序盤にパンプキン2体が時間差で出る」「やっと倒したらすぐ沸いた」
   → 原因は**講習シーン(relief-pumpkin・buildup②2:15-3:45)のfeatured床**。キャップ2まで
   時間差で出し、キル後も即座に再抽選で補充される(補充制限が無い)。
2. 「HARVESTがハーベストになってない」→ 現状の緩は講習(pumpkin/wolf練習)とまばらな休憩のみで、
   「刈って稼ぐ」時間が無い。社長決定: **HARVEST=雑魚無双モード**。
3. 「終盤のリラックス/ハーベストがだれてる」→ ボス後(7:30以降)の緩が講習(relief-wolf 60秒等)=
   育ち切ったプレイヤーに練習をさせる時間になっている。社長決定: **終盤の緩はHARVEST無双既定**。
→ 対処はすべて**バッチ4(具体化済み・§バッチ4)+問題児リフラクトリ(同節)**。
※軽微: リザルト「最深到達」の文字切れは未修正のまま(記録済み・任意)。

## バッチ5追補: イベント関所(囲いの台本昇格)+セットピース見せ場の再編入(社長方針 v0.25.1348)・**実装済み v0.25.1353**

**背景(v0.25.1347実機)**: 2:00固定の囲いタイマーが関所①に重なり「関所の圧+ホード18体(犬・
パンプキン入り)」の事故(憲法第5条「山に重ねない」違反)。応急のフェーズゲートではなく、
本命の**台本昇格**で一気に解決する(社長方針「難易度調整を全部入れてから統合テスト」)。

### 仕様
1. **gateProgramメニューに「襲撃の関所(gate-assault)」を追加**(maxRung 4):
   関所開始の頭で囲いホードを発火(既存`beginArenaEvent`+段階スポーン18体をそのまま再利用)。
   ホード全滅/時間切れ後、関所の残り時間は数の関所相当(チャフ濁流・mix{60/35/5})で埋める。
   ※ホードの構成(6体目=パンプキン/12・18体目=犬)はイベント特例として現状維持(ゾーン規約対象外)。
2. **「スパイクの関所(gate-boss-spike)」を追加**(maxRung 5): 関所頭でミニボス版囲い
   (パンプキン+取り巻き=既存boss kind再利用)。旧セットピース(1:45中ボス/4:55ペア)の
   見せ場をここへ再編入。旧3:55オンスロートの役は襲撃の関所(18体段階スポーン)が実質継承=追加実装なし。
3. **囲いのランダム割り込み(2分毎タイマー)は廃止**。`?events=0`で従来のランダム発火へ完全復帰。
   救助(rescue)・卵リングは囲いタイマーから独立させ**緩フェーズ限定**の低頻度イベントとして存続
   (紅き月と同じredNightPhaseGateOk流用)。ハンター・叫び・紅き月は現行ゲートのまま。
4. **選出ルール**: 通常台本と同じ適格性(maxRung≤スロット)に加え、
   (a)最初の2関所(gate①②)ではイベント関所を選ばない(導入を守る)
   (b)直近がイベント関所なら連続で選ばない (c)pity発動中/解除後10秒のスロットでは選ばない。
5. **規模スケール(eventSizeMult)をここで配線**: ホード数18を rank/退屈シグナルで±
   (14〜22・cap20遵守・pity直後は減)。バッチ7の保留を消化。
6. デバッグ: `?gateprogram=0`で台本選択ごと無効(従来復帰)。オーバーレイの台本ID表示は既存を流用。
7. テスト: 選出ルール(a)(b)(c)の純関数テスト+憲法テスト通過(イベント関所はfeatured空=自己整合)。
8. 負荷スコア: 1/10(既存イベントの発火経路を関所頭に移すだけ・新規描画なし)。

### 実装結果(v0.25.1353)
設計どおり実装。`src/utils/gateProgram.ts`に`GATE_ASSAULT`(maxRung4/eventKind:'horde')・
`GATE_BOSS_SPIKE`(maxRung5/eventKind:'boss')を追加。両方`featured: []`+数の関所と同じmix
(自己整合・憲法テスト対象外)。`selectGateProgram`に選出ルール(a)(b)(c)
(`gateIndex>=2`かつ直近非イベントかつ非pity)を追加、`constitution.test.ts`の
`ALL_GATE_PROGRAMS`にも追加して憲法テスト対象化。
`useGameLoop.ts`: 関所選定時(既存のバッチ5選択ブロック)にイベント関所が選ばれたら
`gateEventPendingRef`へ発火予約(`eventSizeMult`でsizeMult算出=バッチ7保留の配線消化)。
既存の囲いトリガーブロック(2分タイマー)の`if (!ae)`直下に予約消化の優先分岐を追加——
予約があり`!bossChasing && !hiddenBossAlive && hunter idle && !redNight`なら即発火、
既存のhorde/boss配置ロジック(段階スポーン/ミニボス配置/リングFX)をそのまま流用。
旧2分タイマーは`EVENTS_ENABLED`時、`redNightPhaseGateOk`で緩フェーズ限定にしたうえでkindを
rescue/egg限定に縮小(horde/bossは関所頭側のみ)。`?events=0`で旧の4種フル抽選+無制限フェーズへ
完全復帰(タイマー分岐のみ・イベント関所側は`?gateprogram=0`で切り分け)。
`hordeSpawnRef`に`total`を追加(既定18=`ARENA_HORDE_COUNT`)。段階スポーンのパンプキン/ウルフ
出現index(旧6/12/18固定)を`round(total/3)`/`round(total*2/3)`/`total`へ一般化——
total=18時は旧実装と完全に同じindex(6/12/18)になることを確認。sizeMult適用時は
`clamp(round(18*mult), 14, 20)`(cap20厳守)。
選出ルール(a)(b)(c)の純関数テスト5件を`gateProgram.test.ts`に追加(既存テストのbaseは
`gateIndex:0`をデフォルトにして影響なし)。
自己点検: 憲法第4条(初心者ゾーン)には抵触しない(gate①②=事実上初心者ゾーン相当の関所では
選出ルール(a)がイベント関所を禁止)。憲法第5条(緩を荒らさない)にも抵触しない(旧ランダム
タイマーは緩フェーズ限定へ縮小、囲いは関所内で完結)。
検証: lint / typecheck / test(290 pass, 1 skip・gateProgram.test.ts 16件+constitution.test.ts
7件を含む) / build 全通過。実機未確認(統合テストへ)。

---

## 実装順とゲート(v0.25.1348改訂: 社長方針「難易度調整を全部入れてから統合テスト」)
1〜5・5追補・3完成版・6・up+N線は実装済み(1298/1302/1303/1305/1312/1313/1315/1320/1326/1340
+穴修正1343/1353/1354/1355/1356)。**実装キューは全項目完了**:
5追補(イベント関所=囲い昇格+セットピース再編入+規模スケール・仕様は§バッチ5追補) →
3完成版(Tank化=pumpkin/werewolfのプール撤退・仕様は§バッチ3配役。到着演出フックは
「素材は後日」につき見送り) →
6(ステージ難易度指数stageAggro・仕様は§バッチ6。maxRungクランプ式と注記の食い違いは
★未決事項へ記載・式どおり実装) →
小物: 診断グラフにup+N(上振れボーナス)線を追加(記録済みのupswingBonusを表示するだけ・挙動不変) →
**残りは実機統合テスト(実機確認④)のみ**: 完成形の心電図をステージ1と5で通しプレイ判定。
(補給管理(AI_DIRECTOR_HANDOFF§3-5)は未仕様のため統合テスト後に設計会議してから)。
「?ladder=0 / ?program=0 / ?gateprogram=0 / ?upswing=0 / ?events=0 / ?setpiece / ?mix=0 /
?debt=0 / ?stageaggro=0 で従来挙動へ即復帰できる」ことは維持している(切り分けと巻き戻しのため)。

### 実装結果(小物: up+N線・v0.25.1356)
`src/utils/aiDirectorDebug.ts`の`DirectorSample`に`upswing`フィールドを追加し、
`useGameLoop.ts`が毎フレーム既に計算していた`upswingBonus`(退屈シグナルの上振れ枠。
0〜`BORED_BONUS_MAX`)をそのまま記録するだけ(新規計算なし)。`DirectorResult.tsx`
(リザルトの心電図)に5本目の線として`BORED_BONUS_MAX`で正規化して重ね描き(ティール色・
盤面在庫と同じ点線スタイル)+凡例に「up+N」を追加。挙動には一切影響しない(表示専用)。
テスト: `aiDirectorDebug.test.ts`に記録の検証1件追加。検証: lint / typecheck /
test(303 pass, 1 skip) / build 全通過。負荷スコア: 1/10(既存の0.5s刻みサンプリングに
数値1個追加+静的SVGの線1本、いずれもリザルト画面のみ・プレイ中の負荷は0)。
