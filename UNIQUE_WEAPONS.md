# ユニーク武器システム(設計書)

**この文書は仕様の正**。案件の状態は書かない(状態=PROJECT_STATUS.md)。

## §0 ゴール(社長の言葉のまま)

> 縦：T1→T2→T3の成長 / 横：同Tier内で好きな武器を選ぶ / 解放：ボス撃破で横の選択肢が増える

**★本節は初出(2026-09-04)の記録。強さの置き方は §11-0(2026-09-05)が上書きする**——
下の「DPSは概ね同等」は、**「ボス撃破報酬なので基本的には+」**(社長指示2026-09-05)へ更新済み。
「単体では完全上位互換にしない」は生きている(担保は §5 不変条件6 の支配テスト)。

社長が挙げた仕様と注意点:
- 武器TierはT1/T2/T3のまま。
- 武器種ごと・Tierごとに使用武器を事前設定する。
- 各Tierには複数の武器候補を用意し、ボス撃破で新武器を恒久解放。
- 解放した武器は装備設定画面で差し替え可能。(ただし出撃時は今まで通りTier1から)
- キャラ別設定にはせず、武器種単位の共通設定にする。
- 同Tier内は上位互換を作らない。DPSは概ね同等で、挙動・得意状況を変える。
- 「ユニーク＝強い」ではなく、**ユニーク＝プレイスタイルが変わる**を基本にする。
- 既存武器・サブウェポンとの役割重複を避ける。
- Tierは縦の成長、武器選択は横の選択として分離する。

## §1 現状(実コードで確認した事実・2026-09-04)

| 事実 | 出どころ |
|---|---|
| 銃は **4カテゴリ × T1〜T3 = 12スロット**(handgun / shotgun / rifle / **glauncher**) | `CATALOG`(`src/utils/weaponUtils.ts:58-99`) |
| 各スロットには**現在1挺だけ**(`handgun-t1` など、キー = `<category>-t<n>`) | 同上 |
| glauncher も**普通に武器箱/敵ドロップから出る**(社長訂正v0.25.3297) | `DROP_CATEGORIES`(`src/utils/weaponDrop.ts:12`) |
| 出撃時の銃は**クラスごとのT1固定**(warrior=shotgun-t1 / rogue=handgun-t1 / mage=rifle-t1 / necromancer=handgun-t1) | `PLAYER_PROFILES`(`src/data/playerProfiles.ts:25-28`) |
| 出撃武器の生成は1箇所 | `getStartingWeapons`(`src/utils/weaponUtils.ts:325`) |
| ランの中の入手(ドロップ/武器箱/宝箱/武器庫)は**全部 `grantWeapon(key)` に集約**されている | `gameStore.ts:15748 / 15814 / 15836 / 15867 / 17045` |
| 銃は**カテゴリごと1挺・高Tier優先**(同Tier以下は弾薬に化ける) | `grantWeapon`(`gameStore.ts:16218-`) |
| 恒久解放の前例あり(localStorage 1キー) | `isKogarasuUnlocked` / `markKogarasuUnlocked`(`src/data/progress.ts:456-467`) |
| 銃の絵は**キーごとのスプライト**。**登録点が2つある**(片方だけだと無言で汎用描画に落ちる) | ①`WEAPON_ICON_KEYS`(`weaponUtils.ts:332`)=HUD/ピックアップの判定 ②`pixiTextures.ts:375-`のマニフェスト=地面の絵 |
| `AmmoType` は **`'phill'` を含む5種**。横の対象は4カテゴリ | `game.ts:1493` / `ARMORY_GUN_CATEGORIES`(`weaponUtils.ts:204`) |
| 「直接銃」の集合はキー列挙。ここに漏れると**着弾クリロール等の戦闘ルールが変わる** | `DIRECT_GUN_WEAPON_KEYS`(`weaponUtils.ts:135`) |
| 装備設定画面の実体 | `renderLoadout`(`src/components/MissionSelect.tsx:1218`・screen `'loadout'`) |
| 近接は **T1〜T5**(銃と別軸)。ドロップは「現装備tier+1」固定 | `nextKnifeKey` / `rollWeaponKey`(`weaponDrop.ts:49-57`) |

**この構造の意味**: 入手経路は既に1点(`grantWeapon`)に集約されているので、**「どのキーを渡すか」を
1箇所で解決すれば、ドロップ・武器箱・宝箱・武器庫の全経路が同時に横対応する**。入手側のコードは触らない。

## §2 決定事項(社長裁定済み)

| # | 名札 | 裁定 |
|---|---|---|
| ①「横の広さ」 | 同Tierに何挺並べるか | **全Tierに横を入れる(T1も含む)**。**各スロット = 既定1挺 + ユニーク1挺 = 2挺**(2026-09-05・§11-0で確定) |
| ②「解放単位」 | 何を倒すと何が増えるか | **ボス1体撃破につき候補1つ解放**(小烏丸と同じ作法) |
| ③「近接の扱い」 | ナイフ系に横を入れるか | **今回は対象外**。近接はT1〜T5で銃と別軸のため、混ぜると縦が壊れる |
| ④「拾った銃」 | ランの中で拾った時どうなるか | **設定に従う**。スロットに設定された挺が出る(入手経路は無改変) |

**①がT1を含むことの効き方**: 出撃時の銃はクラス固定のT1(`PLAYER_PROFILES`)なので、
**T1に横があると「毎ラン最初の1挺」が選択の対象になる**。横の選択が終盤しか無い案(T3だけ)と違い、
**全ランで毎回効く**。これが社長の指定した形。

## §3 データ構造

### 3-1. キーの命名(既存を壊さない)
- 既存キー `<category>-t<n>`(例 `handgun-t1`)は **そのスロットの既定候補**として残す。
- 追加候補は `<category>-t<n>-<slug>`(例 `handgun-t1-revolver`)。
- **既存キーの意味を変えない**ので、セーブ・固定守護霊(`fixedGuardians.ts` の `gunKey`)・
  ボス練習・テストの既存参照は**全て無改変で通る**。

### 3-2. スロットの定義(新規・純データ)
`src/data/weaponSlots.ts`(新規):
```ts
// カテゴリ×Tier の「スロット」に並ぶ候補キー。先頭が既定(未設定時に選ばれる)。
// キーは4カテゴリのみ(AmmoType は 'phill' を含む5種なので、そのまま使わない=監査C3)。
export type SlotCategory = 'handgun' | 'shotgun' | 'rifle' | 'glauncher';
export const SLOT_CANDIDATES: Record<SlotCategory, Record<1|2|3, string[]>> = {
  handgun: { 1: ['handgun-t1', ...], 2: [...], 3: [...] },
  shotgun: { ... }, rifle: { ... }, glauncher: { ... }
};
```

### 3-3. 装備設定(恒久・localStorage 1キー)
```ts
// zombie.loadout.slots
type SlotLoadout = Partial<Record<SlotCategory, Partial<Record<1|2|3, string>>>>;
```
- **キャラ別にしない**(社長指定)。武器種単位の共通設定。
- 未設定 / 未解放 / 存在しないキー → **既定(候補の先頭)へフォールバック**。壊れたセーブで詰まない。

### 3-4. 解放(恒久・小烏丸と同じ作法)
`src/data/progress.ts` に追記:
```ts
// zombie.progress.weaponUnlocks = string[](解放済みキー)
export const isWeaponUnlocked = (key: string): boolean => ...;
export const markWeaponUnlocked = (key: string): boolean => ...; // 新規解放なら true
```
- **既定候補は常に解放済み扱い**(配列に入っていなくても使える)。
- どのボスが何を解放するかは `BOSS_UNLOCK: Record<BossId, string>` の**純データ1表**で持つ。

## §4 解決点(実装の中心)

**純関数を1本足す。**

```ts
// src/utils/weaponSlot.ts(新規・葉モジュール)
export const resolveSlotKey = (key: string, loadout: SlotLoadout, unlocked: ReadonlySet<string>): string
```
- 入力キーの `category` と `tier` を `CATALOG` から引き、そのスロットの設定キーを返す。
- 設定が無い / 未解放 / スロット外 / 近接 → **入力キーをそのまま返す**(恒等)。
- **冪等**(`resolveSlotKey(resolveSlotKey(k)) === resolveSlotKey(k)`)。二重に通っても壊れない。

### 4-1. ★解決は「キーを作る側」で行う(監査A1の是正)
**v1 は「`grantWeapon` の先頭で差し替える」としていたが、これは出荷バグになる。**
敵ドロップは `useGameLoop.ts:12966` が `rollWeaponKey(...)` の結果を pickup の `weaponKey` に焼き、
`pixiScene.ts:25334` が**そのキーのスプライトを地面に描く**。受け取り時にだけ差し替えると
**「地面にハンドガンの絵 → 拾うとリボルバー」**になる。よって解決はキーの**生成点**で行う。

| 生成点 | ファイル |
|---|---|
| 敵ドロップ / 武器箱 | `rollWeaponKey` / `openCrate`(`weaponDrop.ts`) |
| ステージ7開幕宝箱 | `rollTier23Gun`(`weaponDrop.ts`) |
| 武器庫のTier3付与 | `updateArmory` の `grantGunKey`(`gameStore.ts:17045` 手前) |
| 出撃時のT1 | `getStartingWeapons`(`weaponUtils.ts:325`) |

加えて **`grantWeapon` の先頭にも通す**(冪等なので二重適用は無害)。これは
「新しい入手経路を後から足した人が解決を忘れる」ことへの**安全網**であって、主たる解決点ではない。

### 4-2. ★候補キーを登録しなければならない集合(監査A2の是正)
- **`DIRECT_GUN_WEAPON_KEYS`(`weaponUtils.ts:135`)に新候補を必ず含める。**
  現状は `GUN_KEYS_BY_CATEGORY` の平坦化なので、候補キーは漏れる。漏れると
  `useGameLoop.ts:12224`(トラップ/弱点の着弾クリロール)・`12308`(体勢崩れ時のクリ抑制)・
  `critDecay.ts`・`playtestDriver.ts` で**新候補だけ「味方系の弾」扱い**になり、
  「DPS概ね同等」の裏で隠れた性能差が出る。**`SLOT_CANDIDATES` の平坦化から作り直す。**
- **`GUN_KEYS_BY_CATEGORY` には候補を足さない。** `weaponDrop.ts:60/71/86` が `[tier-1]` で
  **添字参照**しているため、配列に足すと Tier とキーの対応が壊れる。ここは**既定候補のみ**のまま。
- **絵の登録は2箇所**(§1): `WEAPON_ICON_KEYS` と `pixiTextures.ts` のマニフェスト。
  **片方だけだと地面の絵が無言で汎用描画に落ちる**(現に `glauncher-t1〜t3` が
  マニフェスト未登録のまま=v0.25.4122で是正)。

## §5 上位互換を作らない(機械化)

社長の注意点「同Tier内は上位互換を作らない。DPSは概ね同等」をテストで固定する
(`src/utils/weaponSlot.test.ts` または `constitution.test.ts` へ追記):

**★適用範囲(2026-09-06に改めた)**: **全21挺に不変条件1を適用する。**
以前は「火炎放射器・レールガン・ロケラン・錬金砲・シグナルは式に乗らないので実測で見る」と
していたが(★未決 #U8)、**社長指摘「その中で数値は決めれないの?」のとおり、決められた**。
やり方は §5-2。**切り離すのが早すぎた**——汎用式で表せないだけで、**帯そのものは同じ**。

- **不変条件1(DPS帯)**: 同スロットのユニーク候補の**実効DPS**が、そのスロットの
  **既定候補の ±10%**に収まり、**狙いはその + 側(0〜+10%)**。
  ★**社長の取り決めは一貫して「±10% の + 寄り」**(2026-09-05「ボス撃破報酬なので、基本的には+」は
  **枠の中で + を狙え**という意味であって、枠を広げる指示ではなかった)。
  ★**v0.25.4125〜4165 の本書は「+0%〜+15%」と書いていたが、これは私が枠を広げた誤り**
  (社長指摘2026-09-06「±10%の+寄りで作っていく みたいな話生きてないの?」)。**v0.25.4166で戻した。**
  硬い枠=**0.90〜1.10**(テストが見る)/ 狙い=**1.00〜1.10**(各武器の値を toBeCloseTo で個別に固定)。
  **実効DPS = `damage * count * magSize / (magSize * cooldown + 実装のリロード) * 1000`**
  (=マガジンを撃ち切ってリロードするまでを1周期とした平均)。
  **★「実装のリロード」= `max(250, reloadMs * 2)`**(`effectiveReloadMs`・`weaponUtils.ts:242`。
  `RELOAD_TIME_MULT = 2`。プレイヤーの育成倍率は掛けない=素の比較)。
  **★生の `reloadMs` で計算してはいけない**(v0.25.4125 の表はこれを誤り、デリンジャーの評価が
  −6.5% ではなく実際は **−36.7%** だった=監査A-3)。
  **★リロードを除いた瞬間DPSでも測ってはいけない**——マガジンとリロードは §7 で「変えてよい軸」に
  置いているので、瞬間DPSで縛ると**「大容量マガジン+同じ瞬間DPS」で実質的に上回る候補**が素通りする。
  **既定の実効DPS(実装値): handgun T1=`15.79` / T2=`28.13` / T3=`37.50`。**
#### §5-2 ★汎用式で表せない武器の測り方(2026-09-06・#U8を置き換えた)
**帯(±10%・狙いは+側)は変えない。変えるのは「1サイクル」の書き方だけ。**

> **実効DPS = 1サイクルで出るダメージ ÷ 1サイクルに掛かる時間**

汎用式は「マガジンを撃ち切ってリロードするまで」を1サイクルとした**その特殊形**でしかない。
⇒ **武器固有の死に時間(溜め・遅延・照射時間)と追加ダメージ(段階成長・多段)を
サイクルに含めれば、どの武器も同じ帯で測れる。**

| 武器 | 1サイクル | 実効DPS | 既定 | 差 |
|---|---|---|---|---|
| **火炎放射器**(S-T3) | 40弾×100ms照射(1tick 5) + リロード | (40×5) ÷ (4000+3000)ms | **28.57** | 26.87 | **+6.3%** |
| **レールガン**(R-T3) | 汎用式(オート射撃)。手動の頭部確定クリは式外の上振れ | 101×4 ÷ (5200+4400)ms | **42.08** | 45.83 | **−8.2%** |
| **アイレーザー**(R-T3) | 溜め700 + 照射3000(1tick 14) + リロード5000 | (30×14) ÷ 8700ms | **48.28** | 45.83 | **+5.3%** |
| **錬金砲**(G-T2) | 破裂4発(30) + 3段まで育てた石の起爆(200) + リロード | (120+200) ÷ 6600ms | **48.48** | 45.83 | **+5.8%** |
| **シグナル**(G-T3) | 空爆3発。**着弾遅延900msは死に時間なので cooldown に足す**(1400+900=2300) | 275×3 ÷ (6900+4000)ms | **75.69** | 70.00 | **+8.1%** |
| **ロケラン**(G-T1) | **溜め500msを cooldown に畳む**。`magSize:1` なので毎射リロード | 140 ÷ (500+4000)ms | **31.11** | 28.65 | **+8.6%** |

- ★**レールガンだけが「−」側**。社長仕様が**「オート時のダメージは低め」と明示している**ため。
  **これが枠を ±10% にしてある理由**——+0〜+15% のような片側の枠では、
  **この武器を仕様どおりに作ると必ず不合格になる**。
- **式外の上振れは帯に足さない**(貫通・多段・爆発範囲・頭部確定クリ)。汎用式の武器で
  射程・減衰・装填の遅さを引かないのと同じ扱い(不変条件7の思想)。
- **★#U8「実測基盤(ボットラン)を受け入れ条件にする」は不要になった。**
  6挺とも上の式で機械化できる=**テストで固定できる**。ボットランは**作らない**
  (作るとしたら「実際に当たるか」の確認であって、強さの物差しではない)。

- **不変条件2(スロット整合)**: 候補キーは全て `CATALOG` に存在し、`category`/`tier` がスロットと一致する。
- **不変条件3(既定の先頭)**: 各スロットの先頭は既存キー `<category>-t<n>`。
- **不変条件4(解放表の健全性)**: `BOSS_UNLOCK` の値は全て候補配列のどれかに含まれ、**既定候補ではない**。
- **不変条件5(フォールバック)**: 未解放キーを設定した `loadout` を渡しても `resolveSlotKey` は
  必ず**解放済みのキー**を返す。

- **不変条件6(支配の禁止・監査A5の是正)**: 候補が既定に対し**全軸で≧**(damage/count/critChance/
  magSize、かつ cooldown/reloadMs が≦、かつ passthrough・pierce が既定以上)になってはいけない。
  **基準DPSだけでは上位互換を止められない**(既定と同じ数値+`passthrough:true` は ±10% を満たしつつ
  厳密上位互換)。この支配テストで落とす。

- **不変条件7(弾薬効率の明示)**: 候補の **1発あたりダメージ = `damage * count`** を既定と並べて**記録する**。
  **`count` が何発でもマガジンの消費は1**(`weaponUtils.ts` の射撃処理は `magazine - 1` 固定)なので、
  二丁ハンドガン(9×2)の弾効率は **18/発** であって 9/発 ではない。
  **★これは「記録する」だけで、実効DPSを下げる根拠にはしない**(社長指摘2026-09-05「+の%がないのはなぜ?」)。
  ユニークは**式に出ない欠点**(止まる時間・減衰・射程)を既に背負っているので、そこへ弾効率ぶんの
  減点を重ねると**同じ欠点で二重に払わせる**ことになる。**ユニークはボス撃破報酬なので上側(+)が基本**(§11-0)。
  **弾薬は発数で拾うので、`damage` が高い候補は同じ弾薬で長く戦える**——これは実効DPSの式に
  現れない強さなので、**数字として並べて見えるようにしておく**(実機調整の材料)。

**±10% は「叩き台の数字」であり社長の裁定対象**(§9 ★未決 #U1)。
**貫通・爆発は基準DPSの式に現れない**ため、同スロットに貫通あり/なしを混ぜると DPS が比較不能になる
(§9 ★未決 #U6)。

## §6 解放とUI

**★本節は第1弾当時(2026-09-04)の記録。解放の流れは §11-6(2026-09-05)が上書きする**——
「撃破=即解放」ではなく **撃破=設計図 → 開発施設で200G購入 → 使える** の4段。本節の
「未解放は灰表示」も **§11-6「未購入は載せない」** へ更新済み。キー形式も **§11-6-3** が正
(`type@stageId` が要るのは城ボスだけ)。

- **ボスの識別は `type@stageId`**(監査A3の是正)。`BossId` という型はコードに無く、ボスは `enemy.type`
  (EnemyType)で識別される。**城ボスは全ステージ `'giantbat'` 1種**(`enemyUtils.ts:356`)なので、
  type をキーにすると城ボス7〜9体で解放が1つしか起きない。既存の識別形式
  (`fixedGuardians.ts` の `'giantbat@stage-3'`、`bossEncounter.ts` の slotKey)に揃える。
- **解放の瞬間**: ボス撃破の確定処理で `markWeaponUnlocked(BOSS_UNLOCK[bossKey])`。
  新規解放なら**既存の取得トースト(`lastWeaponGet`)を流用**して知らせる(新規UIを作らない)。
- **★練習ランでは解放判定を呼ばない**(監査A4の是正)。`practiceGuard.ts` は localStorage の
  **書き込みだけ**を飲み、読みは素通しする。小烏丸の作法(`progress.ts:465`)は
  read→false→write(飲まれる)→**true を返す**ので、**練習でそのボスを倒すたびに偽の解放トーストが出る**。
  ボスモード/ガントレットでは判定自体を通さないこと。
- **設定画面**: 既存の装備設定画面に「銃スロット」欄を足す。**カテゴリ×Tierの12マス**で、
  各マスが解放済み候補のセレクタ。未解放は灰表示。
- **出撃時は今まで通りTier1から**(社長指定)= 設定はキーの差し替えのみで、Tierの初期値は変えない。

## §7 候補武器の設計指針(「強い」ではなく「変わる」)

同スロット内で**変えてよい軸 / 変えてはいけない軸**を先に固定する。

| 変えてよい(挙動・得意状況) | 変えない(縦の成長) |
|---|---|
| `count`(弾数)と `damage` の配分、`cooldown` のリズム | **基準DPS**(§5 不変条件1) |
| `magSize` / `reloadMs`(継戦力と間合いの管理) | そのカテゴリの `RANGE_BY_CATEGORY`(射程はカテゴリの identity) |
| `projectileSpeed` / `projectileSize`(当てやすさ) | 弾の見た目の文法(赤い二重丸=共通・CLAUDE.md) |
| `passthrough` / `pierce`(貫通の有無) | Tier間の強さの段差 |
| `critChance`(`BASE_CRIT_BY_CATEGORY` からの逸脱) | |

**現状の実装では変えられない軸**(変えたいなら別途 `WeaponDef` の拡張が要る):
- **ショットガンの拡散**は `SHOTGUN_SPREAD_CONE_RAD_BY_TIER[tier]`(`weaponUtils.ts:17`)で**Tier固定**。
- **発射音**は `SMG_WEAPON_KEY='handgun-t3'` などの**キー固定**(`useGameLoop.ts:511`)。
  T3ハンドガンの新候補が連射型でも単発音のまま。

**役割重複の回避(社長の注意点)**: 新候補を足す前に、**既存の銃・サブウェポンと同じ役割になっていないか**を
1行で書く(例:「これはグレネードガンと同じ"面制圧"ではないか?」)。書けない候補は採用しない。

## §8 規模と段階導入(★社長判断が要る)

**12スロット × 2〜3挺 = 合計24〜36挺**。既存が12挺なので、**新規は12〜24挺**。
それぞれに ①数値設計 ②役割の1行 ③**専用スプライト**(`WEAPON_ICON_KEYS` に登録しないと絵文字になる)が要る。
**素材が最大の律速**。

段階導入の案(§9 ★未決 #U2 で裁定):
- **第1弾**: 仕組み(§3〜§6)+**1カテゴリぶんの候補3挺**(T1/T2/T3 に1挺ずつ)。仕組みが実機で回ることを確認。
- **第2弾以降**: カテゴリ単位で候補を足す。**仕組みは第1弾で完成しているので、以後は純データの追加だけ**。

## §9 ★未決事項(社長裁定待ち)

- **#U1「DPS帯の幅」**: 同スロット候補の**実効DPS**許容幅。叩き台 **±10%**。
  広げる(±20%)と個性は出しやすいが上位互換に近づく。狭める(±5%)と差が付けにくい。
  **実例(§11-2)**: 提案した3挺は1発が重く**弾持ちが約2倍**なので、不変条件7に従って
  −5.0% / −8.2% / −7.1% と帯の下側へ寄せた。**帯を±5%に狭めると、この釣り合わせができなくなる**。
- ~~**#U2「段階導入の刻み」**~~ → **裁定済み(2026-09-04)= (a) 第1弾は仕組み+1カテゴリ3挺**。
  対象カテゴリは**ハンドガン**(§11)。
- **★城ボス行を表に足す時は `isFinalBossKill` ガードが要る**(検収2巡目B-1・v0.25.4135で実装済み)。
  グレンは**形態1をHP半分で**撃破確定処理へ通す(移行であって討伐ではない)ため、これが無いと
  **形態2に負けても恒久解放される**。「表に1行足すだけで動く」は**城ボス以外の話**。
- ~~**#U14「金環(サブウェポン)の解放・購入の流れ」**~~ → **裁定済み(2026-09-05)= (a) 設計図ゲート付きで
  価格は従来の 20/50/100G のまま**(サブの相場も操作も1つも変えない)。**スリィエル撃破で金環の設計図が出て、
  以後サブの棚に並ぶ**。実装は金環そのものを作る段で行う。旧文: 金環を
  `SubWeaponKey` に足した瞬間、**設計図なしで20Gで棚に並ぶ**(サブの棚は `SUB_WEAPON_KEYS` を全列挙し、
  価格は 20/50/100G の3段。装備条件はLv1以上)。銃の「一律200G」ともサブの3段価格とも噛み合わない。
  (a)**設計図ゲート付きで従来の 20/50/100G**(サブの相場も操作も1つも変えない・**推薦**)/
  (b)200G一括でLv3 /(c)200GでLv1 → 以後 50/100G。
- **#U3「どのボスが何を解放するか」**(★2026-09-05に**第1弾3種+第2弾ぶんが確定**。
  **第1弾: ミゲル→デリンジャー / ジブリル→ハンドキャノン / ラフィ→パイルドライバー**。
  残りは第1弾仕様6種=収束型SG/制圧型SG/火炎放射器/デザートテック/大型狙撃銃/レールガンのみ): 偶像→デュアルレンジピストル /
  ウリ→ガンブレード / ヨルムンガルド→コイルSG / フィル(変異体)→誘導散弾SG / スカジ→氷槍ライフル /
  ミーミル→アイレーザー / スリィエル→金環(サブ)。**残るは第1弾仕様9種の解放元**(デリンジャー・
  ハンドキャノン・パイルドライバー・収束型SG・制圧型SG・火炎放射器・デザートテック・大型狙撃銃・レールガン)。
  下の「ボスの定義」の問題も**第2弾の指定で実質解消**した(指定されたのは全て一意のボス型で、
  パンプキンや賞金首のような何度も倒せる敵は含まれていない)。旧文: `BOSS_UNLOCK` の中身。**先に「ボス」の定義が要る**——
  `isBossType`(`enemyUtils.ts:295`)は pumpkin(毎ラン何度も倒す)・賞金首・hunter・幻影・
  lab-zombie-3・死神族まで含むので、そのまま使うと**1ランで何度も解放が起きる**。
  解放トリガにする集合を列挙して固める必要がある。
- ~~**#U4「glauncher に横を入れるか」**~~ → **裁定済み(2026-09-05・社長の12種仕様)= 入れる**。
  ただし下記の前提工事が要る点は変わらない(§12)。旧文:: 入れる場合、**キー固定の分岐が3系統**あり、そのままだと
  新候補は**爆発しない直進弾**になる —— `isGrenadeGunKey`(`weaponUtils.ts:224`・キー3つ固定 →
  `useGameLoop.ts:12485/12647/12686/12854` の着弾爆発・貫通除外・消滅)/
  `GLAUNCHER_ROLL_DETONATE_PX`(`weaponUtils.ts:364`・キー別)/ `pixiScene.ts:24661`(弾の絵)。
  入れるならこの3系統を**category 判定へ直す**のが前提工事。
- **#U5「解放の見せ方」**: §6 は既存トーストの流用。専用の解放演出を作るか。
- ~~**#U6「貫通・爆発を横の軸に使ってよいか」**~~ → **裁定済み(2026-09-05・社長の12種仕様)= 使う**
  (ロケラン/錬金砲/シグナルランチャーが爆発前提)。**代わりに #U8 が要る。**
- ~~**#U8「式に乗らない武器の物差し」**~~ → **解消(2026-09-06)。ボットラン実測は作らない。**
  社長指摘「その中で数値は決めれないの?」のとおり、**6挺とも「1サイクル」を書けば同じ±10%で
  測れる**(§5-2)。2026-09-05 の裁定は (a)ボットラン実測だったが、**その必要がなくなった**。旧文::旧文:: 火炎放射器(連続照射)・レールガン(手動照準)・ロケラン(溜め)・
  錬金砲(付着→起爆)・シグナルランチャー(時間差空爆)は `damage/cooldown/magSize/reloadMs` で
  実効DPSを計算できない。「単純上位互換にしない」(社長の共通ルール)を何で担保するか。
  (a)**ボットランの実測**(同ステージ・同時間でのキル数/被弾を既定と比較)を受け入れ条件にする(推薦)/
  (b)人手レビューのみ(機械化しない)。
- **#U7「未解放候補を設定画面に出すか」**: §6 は「未解放は灰表示」だが、装備設定画面の現行作法
  (`MissionSelect.tsx` renderLoadout・v0.25.4084)は**「未購入は載せない」**。割れている。
  (a)灰表示にして「何が増えるか」を見せる/(b)現行作法に揃えて載せない。

- ~~**#U9「弾でノックバックさせるか」**~~ / ~~**#U10「体勢削りの比率」**~~ →
  **裁定不要と判明(2026-09-05・社長指摘「体勢削り値で調整すれば?」「無理に重い開発にしようとしてる」)**。
  **どちらも新しい仕組みではなく、既存の道具を1箇所で呼ぶだけ**だった:
  - **ノックバック**: `knockbackEnemy` は既に**10箇所以上**(反射神経/リズムタップ/犬/爆発/シールド/
    発火ナイフ…)から呼ばれている既存API。**弾の命中箇所で同じ1行を呼ぶ**だけ。
    **パイルドライバーの弾だけ**に付ける(全銃には入れない=別案件)。ボスへの効きは
    既存の `canShoveEnemy` の規則に従う(=ボス級は押されない)。
  - **体勢削り**: 比率は既存の **`heavy`(0.10)** を流用し(銃クリ 0.05 の2倍)、強さは
    **`postureMult` の数字で調整**する。**新しい打撃種別は作らない。**
  - **v0.25.4126 で「土台が無い」「体勢システムの仕様変更」と書いたのは過大**だった(監査の見立てを
    そのまま通した)。値で調整できるものを工事として立てない。

- ~~**#U11「ハンドキャノンの帯をどちらで測るか」**~~ → **(a)多体側(撃ち分け)で取る**(2026-09-05)。
  社長仕様が「雑魚を次々狙うほど強く、同一ボスを撃ち続けると弱くなる」と明記しているため。
  同一敵に撃ち切ると約−44%になるのは**仕様どおりの姿**であって、帯の違反ではない。

- **#U12「"連続命中"の解釈」**: 社長仕様は「同じ敵へ**連続して**ダメージを与えるほど低下」。
  (a)**リロードまでの累計**(別の敵に当てても前の敵のカウンタは残る)/(b)**直前の命中対象が同じ時だけ**
  積む(別の敵に当てた時点でリセット)。挙動が大きく変わるので裁定が要る。
- ~~**#U13「第1弾の範囲」**~~ → **決着(2026-09-05)= ハンドガン3種まとめて**。
  #U9・#U10 が裁定不要になり、障害が無くなったため。

## §10 監査の記録(着手前・Fable 5.1・2026-09-04)

- **(A) 6件**: A1(解決点=キー生成側へ・§4-1)/ A2(`DIRECT_GUN_WEAPON_KEYS` の漏れ・§4-2)/
  A3(ボス識別は `type@stageId`・§6)/ A4(練習ランの偽解放・§6)/ A5(支配テスト・§5 不変条件6)/
  A6(glauncher のキー固定3系統・#U4)。**全て本文へ反映済み。**
- **(C) 5件**: C1(§4 の「無改変で乗る」は誤り→§4-1へ書き直し)/ C2(絵の登録点は2つ→§1)/
  C3(`AmmoType` は phill を含む→§1・§3-2)/ C4(設定画面の実体名と作法の割れ→§1・#U7)/
  C5(小烏丸の作法は欠点も継ぐ→§6)。**全て反映済み。**
- **(B) 6件は別案件へ積む**(この案件では直さない): 発射音のキー固定 / ショットガン拡散のTier固定 /
  オンライン守護霊の版ズレ(旧端末で新キーが `handgun-t1` に化ける)/ 解放数とボス数の収支表 /
  解放トーストがボス撃破演出(2.6s)に埋もれないかの実機確認 / 素材受領後の憲法テスト追加。

## §11 ユニーク武器12種(社長仕様 v2・2026-09-05)

**★これが正。** v0.25.4123 で私が提案したハンドガン3挺は撤回済み。**v1(同日の初版)も本節で上書き。**

### 11-0. 共通方針(社長の言葉)
> **単体では通常武器の完全上位互換にしない。**
> **ただし既存スキルとの組み合わせで大きく化けるのは許容。むしろ狙ってよい。**

- ユニーク武器は「ダメージ+○%」のような単純強化ではなく、**射程・射撃方式・弾薬運用・照準方法・
  立ち位置・攻撃タイミング**を変える。
- 通常武器とユニーク武器は**同Tier内で横並びが基本**。ただし
  **制圧SG × アイスショット / 錬金砲 × エクスプローダー / パイルドライバー × パニッシャー /
  レールガン × クリティカル系** のように、**特定のスキル構成が完成した結果として突出して強くなるのは問題なし**。
  むしろ「**この武器を拾った/設定したから、このスキルを取りたい**」というビルド誘導を狙う。
- **★ユニークは「ボス撃破報酬」なので、基本的には「+」**(社長指示2026-09-05)。
  実効DPSは**既定より少し上**に置く(叩き台=**+5〜+10%**)。**±0の横並びではない。**
  ★**枠そのものは ±10%**(2026-09-06に取り決めどおりへ戻した・§5)。
  「+だが完全上位互換ではない」は **不変条件6(支配テスト=全軸で既定以上なら落とす)**が担保する
  ——火力が少し上でも、**装填で止まる・同一敵で減衰する・射程が短い**といった軸で負けていれば
  上位互換ではない。
- **横の広さ**: 各スロット = 既定1挺 + ユニーク1挺 = **2挺**(12スロット×2=24挺)。
- **ナイフT1〜T5は今回の開発範囲外。**
- **★数値は未確定**(社長明言): 細かいDPS・射程・リロード時間・減衰率・最大収束量・錬金石の倍率・
  空爆ディレイなどは**まだ決まっていない**。本書の数値は**叩き台**であり、§13-2 の実測で調整する。

### 11-1. ハンドガン
| Tier | 武器 | 仕様 |
|---|---|---|
| T1 | **デリンジャー系** | 装弾数2発。通常T1より1発の威力が高く、2発を短時間で撃つ。2発ごとにリロードが発生するため継続火力は抑えめ。**瞬間火力＋頻繁なリロード**が特徴 |
| T2 | **高威力ハンドキャノン** | 通常T2より連射性は少し低いが単発威力がかなり高い。**同じ敵へ連続してダメージを与えるほど、その敵への威力が段階的に低下**。リロード完了で減衰リセット。**雑魚を次々狙うほど強く、同一ボスを撃ち続けると弱くなる** |
| T3 | **パイルドライバー** | 極端な短射程。射程内の敵へ**杭を撃ち込む**。強ノックバック＋非常に高い体勢/耐性値削り。遠距離性能を捨て、接近された敵を押し返し崩す。**パニッシャー等との組み合わせで化けてよい** |

### 11-2. ショットガン
| Tier | 武器 | 仕様 |
|---|---|---|
| T1 | **収束型ショットガン** | 初期は通常T1よりかなり広角。**敵に命中した射撃ごとに1段階ずつ集弾率が上昇**し、連続命中するほど散弾が中央へ収束。**一定時間命中しないと初期状態へリセット**。リロードは早め |
| T2 | **制圧型ショットガン** | 通常SGより**長射程**。広範囲・多ペレット・低ダメージ。単体を倒すより**遠距離から多数の敵へ大量にヒット**させることに特化。**延焼弾・アイスショット等と組み合わせると状態効果を広範囲へ撒く武器として大きく化ける** |
| T3 | **火炎放射器** | ショットガン枠だが**射撃方式が完全に変化**。超短射程・連続照射・高DPS。接近リスクと引き換えに高火力。**武器自体に燃焼スキル相当の能力を必須では持たせない**が、延焼系スキルとの強力なシナジーは許容 |

### 11-3. スナイプ／ライフル
| Tier | 武器 | 仕様 |
|---|---|---|
| T1 | **デザートテック** | 通常T1より射程・威力は少し低い。リロードは少し速い。**本来のライフル弾が尽きても他カテゴリの銃弾を代用して射撃可能**。専用弾を優先消費し、弾切れ時に他弾へ移る。**継戦能力特化** |
| T2 | **大型狙撃銃** | **立ち止まっている時間に応じて射程が伸び、射撃間隔も徐々に短くなる**。移動すると蓄積リセット。安全な狙撃位置を確保し留まり続けるほど性能を引き出せる**据銃型** |
| T3 | **レールガン** | 通常のオート射撃も行うが**通常T3よりオート時のダメージは低め**。加えて**PHILL銃同様のターゲットサークルによる手動射撃**が可能。**手動で頭部へ命中すると確定クリティカル**。クリティカル関連スキルとの特化ビルドで化けてよい |

### 11-4. グレネードランチャー
| Tier | 武器 | 仕様 |
|---|---|---|
| T1 | **ロケットランチャー** | 1発ごとに長めのリロード。発射前に短い溜め。**溜め中でも弾頭に敵が接触すればその場で爆発**。発射すると**その時点でターゲティングしていた方向へ一直線**に飛ぶ(**追尾なし**)。**敵か壁に当たった場合のみ爆発し、何にも当たらなければ不発**。高威力・やや広範囲 |
| T2 | **錬金砲** | 錬金弾が命中すると**まず弱い範囲攻撃として破裂**。その範囲内の敵へ**金の石を付着**。石が付いた敵へ再び錬金弾の範囲攻撃を当てると**石が最大3段階まで成長**。**指を離した瞬間、通常の近接/カウンターと同時に付着中の石を起爆**。石の段階に応じて爆発ダメージ・範囲が増加。爆発系スキルとの強シナジーは許容 |
| T3 | **シグナルランチャー(仮)** | PHILL系のターゲットサイトを表示。**指を離した瞬間の指定地点を記録**し、少し遅れてその地点へ空爆。**追尾しないため敵の移動先を予測して置く**必要がある。広範囲・高威力・高い体勢値削り。**高ノックバック特化にはしない** |

### 11-6. 解放の流れ(★社長仕様2026-09-05「撃破で手に入るのは設計図。開発室に商品が並ぶ」)

**ボス撃破 → 設計図を入手 → 開発施設に並ぶ → ゴールドで購入 → 装備設定で選べる**、の4段。
**撃破=即解放ではない**(v0.25.4133 の配線はこの形へ組み替える)。

| 段 | 何が起きるか | 置き場所 |
|---|---|---|
| ①設計図 | ボス撃破で恒久入手(撃破1回で1種) | **新キー `zombie.progress.weaponBlueprints`** |
| ②並ぶ | **設計図があるもの + 店売り**が開発施設のリストに出る(**購入済みは消える**) | `weaponDev` 画面(`MissionSelect.tsx`) |
| ③購入 | **一律 200G**(社長裁定2026-09-05「全部同じ価格でいい。200gにしよう」) | `spendGold` = サブ解放と同じ永続ゴールド(`gameStore.ts:16698`) |
| ④選ぶ | 装備設定 → 銃スロット。**未購入は載せない**(★未決 #U7 は「載せない」で決着) | `renderLoadout` |

#### 11-6-1. ★2状態の置き場所(監査A-1の是正・**ここを外すと4段が3段に潰れる**)
現状 `unlockedWeaponKeys()`(`weaponSlot.ts`)は**「今使ってよい」集合**で、装備UIと生成点4箇所が読む。
**設計図を同じ `weaponUnlocks` に書くと「設計図入手=即使える」に戻る**ので、**台帳を2本に分ける**。

| 台帳 | 意味 | キー |
|---|---|---|
| **`weaponUnlocks`(既存)** | **購入済み**(=使ってよい) | `zombie.progress.weaponUnlocks` |
| **`weaponBlueprints`(新規)** | **設計図あり**(=棚に並ぶ。まだ使えない) | `zombie.progress.weaponBlueprints` |

**読む側の割り当て(これも仕様の一部)**
| 読む場所 | 読む集合 |
|---|---|
| **開発施設の棚** | **(設計図 ∪ 店売り) − 購入済み** |
| **装備設定 / 生成点(`resolveSlotKeyNow`)** | **既定 ∪ 購入済み** |
| **テスト用トグル / `?unlockall=1`** | **両方を全開**(棚にも並び、装備でも選べる) |

- **既存端末の移行は不要**: `BOSS_UNLOCK` が空のまま出荷されているので、`weaponUnlocks` に実データは無い。
- **`resetProgress()` に新キーの削除を1行足す**(`progress.ts`。既存の `weaponUnlocks` 削除の隣)。

#### 11-6-2. ★「店売り」の表(監査A-2の是正)
**「BOSS_UNLOCK に無い=店売り」と実装してはいけない**——`BOSS_UNLOCK` は今**空**なので、
その実装だと**第1弾3種が全部店売りになる**。**明示リスト `STORE_SOLD_KEYS` を純データで持つ。**
- **不変条件(テストで機械化)**: 全ユニーク候補は **`BOSS_UNLOCK` の値** か **`STORE_SOLD_KEYS`** の
  **どちらか一方に属する**(排反かつ網羅)。片方に入れ忘れた武器は**永久に手に入らない**ので、これは必須。

#### 11-6-4. ★設計図が入るのは「ストーリーモードだけ」(社長指示2026-09-05「ストーリーのみだよ」)
**v0.25.4147 で、これは設計図だけの話ではなくなった**——社長指示「**ストーリーモード以外は全て練習なので
何も手に入ってはいけない。年表にも載らない**」により、**進行の書き込みを丸ごと止める関所
(`practiceGuard`)の対象がフリー出撃とボス戦テストへ広がった**。設計図もその中で自然に止まる。
撃破側の判定(`storyRunForBlueprint`)は**二重の安全網として残す**。

| 出撃 | 設計図 |
|---|---|
| **ストーリー(通常の出撃)** | **入る** |
| 練習ラン(ボスモード / ガントレット / `?practice=1`) | 入らない |
| **フリー(周回)出撃** | **入る**(★社長訂正2026-09-05「**ストーリーモードはフリー周回とイコールだよ**」。v0.25.4146〜4147 で一度「入らない」にしたのは私の誤り) |
| **タイトルの「ボス戦テスト」**(`?gateboss=1` / `?bossnow=1` / `?idolnow=1` / `?castlenow=1` / `?bossmaker=1`) | **入らない** |

- `getSelectedFreeMode` は「**会話なし & クリア進行に影響させない出撃**」の旗なので、
  「ストーリーではない出撃」の判定にそのまま使える(新しい旗を作らない)。
- **これで検収2巡目の(B)-2「ボス戦テストで本番の台帳に設計図が書かれる」も塞がった。**
- ★**設計図は既存の永続報酬(小烏丸・年表・城ボスクリアフラグ)とは扱いが違う**——それらは今も
  「練習以外なら入る」。**設計図だけがストーリー限定**(社長指示)。揃えるかは別案件。

#### 11-6-3. ★解放元キーの形式(監査A-4の是正)
**`type@stageId` が要るのは城ボス(`giantbat`)だけ。** 城ボスは全ステージ同じ型なのでステージで割る必要があるが、
**天使系(miguel/jibril/rafi/uri)・裏ボス(mimir/jormungand/skadi/thor)・`idol`・`phillboss`・`suriel` は型が一意**
なので **`type` 単独キーで置く**。
- **★ミゲルを `miguel@stage-1` にしてはいけない**: `GATE2_BOSS_TYPE_BY_STAGE` に無いステージは
  `?? 'miguel'` にフォールバックする(`useGameLoop.ts:3594`)ため、**表に無いステージでもミゲルが出る**。
  ステージ付きキーにすると「倒したのに設計図が出ない」が起きる。
- ルックアップは既存のまま `BOSS_UNLOCK[type@stage] ?? BOSS_UNLOCK[type]` でよい(型単独キーが後段で拾われる)。

**価格の根拠(2026-09-05に実データで取り直した)**: 1ランの獲得ゴールドは **負け≈13G / 普通のクリア≈55G /
上手いクリア≈83G**(`resultScoring.ts`: ゴールド = `floor(換金スコア / 2000)`。クリアボーナス30000点だけで15G)。
既存の相場は **サブウェポン解放 20/50/100G**・**ガチャ 10〜50G/回**・**永続育成 50/100/200/400/800G**
(v0.25.4138で引き下げ)。**200G ≈ 普通のクリア4ラン**で、育成の3段目と同じ帯。
**本体の報酬は設計図(ボスを倒さないと絶対に手に入らない)**で、ゴールドは引き取りの対価という位置づけ。

### 11-7. 第2弾(社長仕様2026-09-05)+ ランチャー3挺(復活)

**★ランチャー(glauncher)は作る。ただし新規は3挺のみ**(社長裁定2026-09-06「ランチャーもいます。
ただし新しく3機のみって意味です」)。2026-09-05 の「撤回」は**この裁定で取り消された**
(過去の裁定は事実として残す)。⇒ **glauncher のスロットは2挺(既定+ユニーク1)**——
**第2弾は無い**のが他3カテゴリとの違い。
- ★未決 **#U4「glauncherに横を入れるか」= 入れる(1挺だけ)で決着**。
- **前提工事「グレネードのキー固定系統を category 判定へ」が復活**(§12・§16-3)。

**★スロットの深さはカテゴリで違う**: handgun / shotgun / rifle は**3挺**(既定+第1弾+第2弾)、
glauncher は**2挺**(既定+ユニーク1)。ユニークは合計 **21種**(第1弾9 + 第2弾9 + ランチャー3)。
**入手の内訳は ボス撃破18 + 店売り3**(§18-1の割当表とちょうど閉じる)。

**★入手が2系統になった**: **店売り**(第2弾のT1=3種)と **設計図(ボス撃破)**(残り)。
**「店売り」の置き場所=開発施設**(社長裁定2026-09-05)。**設計図なしで最初から棚に並び、同じ200Gで買える**。
ボス報酬と同じ棚・同じ通貨・同じ操作で、**仕組みは1つも増えない**(並ぶ条件が「設計図あり」か
「最初から」かの違いだけ)。

#### ハンドガン
| Tier | 武器 | 入手 | 仕様 |
|---|---|---|---|
| T1 | **クロスボウ** | **店売り** | **弾数無限**。**1発撃つごとにリロード**。ダメージ控えめ。弾薬消費を気にしない代わりに火力と手数で劣る**継戦型** |
| T2 | **デュアルレンジピストル** | **偶像**(`idol`) | **オートターゲット中の敵との距離で自動切替**。近距離=高連射・1発低め / 遠距離=低連射・1発高め・精度高め。**境界で頻繁に切り替わらないようヒステリシス**(近距離へ入る距離と遠距離へ戻る距離を別にする)。具体的距離は調整項目 |
| T3 | **ガンブレード** | **ウリ**(`uri`) | 通常距離では銃撃。**敵が至近距離へ入ると近接系の強攻撃へ切替**。近距離攻撃は**高めの体勢削り**。細かい挙動・数値は未確定 |

#### ショットガン
| Tier | 武器 | 入手 | 仕様 |
|---|---|---|---|
| T1 | **切替式ショットガン** | **店売り** | 装弾数3発。**固定サイクル**: 散弾5発 → リロード → スラッグ3発 → リロード → 散弾5発…。散弾=広範囲・対複数 / スラッグ=単発・高威力・高精度・対単体。**距離による自動切替にはしない**——**リロードを境に攻撃モードが変わる**武器 |
| T2 | **コイルショットガン** | **ヨルムンガルド**(`jormungand`) | 散弾が**一度外側へ広がってから、狙った方向/地点へ再収束**する変則軌道。**単純な狭角SGとは別物**にする。詳細軌道・収束距離は調整項目 |
| T3 | **誘導散弾ショットガン** | **フィル(変異体)**(`phillboss`) | ペレット1発のダメージは通常T3より低い。ただし**各ペレットが近くの敵へ誘導し基本的に必ず命中**。複数いれば分散、1体なら**全ペレットが集中**。単体戦は命中ロスゼロ=非常に高いダメージ効率。延焼・凍傷・クリとの強シナジーも許容 |

#### ライフル
| Tier | 武器 | 入手 | 仕様 |
|---|---|---|---|
| T1 | **ボルトアクションライフル** | **店売り** | 高射程・高単発威力・**1発ごとの装填が遅い**。シンプルな一撃型 |
| T2 | **氷槍ライフル** | **スカジ**(`skadi`) | 弾が直線に飛び、**通過した射線上に短時間ダメージ判定の残る軌跡**を作る。直撃だけでなく**敵集団を一直線に制圧**できる。武器自体に凍傷を持たせるかは未確定。既存の凍傷系スキルとのシナジーは許容 |
| T3 | **アイレーザー** | **ミーミル**(`mimir`) | 射撃前に**溜め**。完了後、**一番近い敵**を狙ってレーザー発射。**貫通**するので奥の敵にも当たる。**照射中は対象を追尾**。**照射3秒**・1ヒットではなく**継続ダメージ**。照射後は**長めのリロード**。単体では3秒集中、群れでは追尾する射線で複数体へ |

#### C. サブウェポン(銃の横ではない別枠)
| 武器 | 入手 | 仕様 |
|---|---|---|
| **金環** | **スリィエル**(`suriel`) | **金環を2本同時に射出**。ターゲット本体ではなく**少し離れた位置へ2本を展開**。**金環自体に接触ダメージなし**。所定位置に着くと**それぞれが近くの敵を取得**し、対象方向へ**レーザーを3秒間**発射。**発射後は追尾しない=撃った瞬間の方向へ射線固定**(敵が動けば外れる/別の敵が射線に入れば当たる)。1回のダメージは控えめな**多段継続型**。2本が同じ敵を狙うことも別々を狙うこともある。**要約=敵の周囲へ2基展開し、そこから固定方向の3秒レーザーを撃つ一時砲台型**。ドローンブーメランと移動イメージは近いが、**金環自体は攻撃せず、レーザーだけがダメージ源** |
※ `suriel` の武器は元から**金の環(単眼)**(`src/types/game.ts` の EnemyType 注記)。本人の得物をそのまま報酬にする形。

### 11-9. ★キー台帳(素材と実装で名前をぶらさないための正)

**キーの形は `<category>-t<tier>-<slug>`**(§3-1)。**素材のファイル名もこのキーと同じ**
(`public/sprites/weapons/<key>.png`)。**先に素材が来た武器は、実装前でもこの表のキーで保存する**
——後から名前が変わると絵が出なくなるため。

| スロット | 第1弾 | 第2弾 |
|---|---|---|
| handgun T1 | `handgun-t1-derringer`(デリンジャー)**素材○ 実装○** | `handgun-t1-crossbow`(クロスボウ) |
| handgun T2 | `handgun-t2-handcannon`(ハンドキャノン)**素材○ 実装○** | `handgun-t2-dualrange`(デュアルレンジピストル) |
| handgun T3 | `handgun-t3-piledriver`(パイルドライバー)**素材○ 実装○** | `handgun-t3-gunblade`(ガンブレード) |
| shotgun T1 | `shotgun-t1-focus`(収束型)**素材○** | `shotgun-t1-cycle`(切替式) |
| shotgun T2 | `shotgun-t2-suppress`(制圧型)**素材○** | `shotgun-t2-coil`(コイル) |
| shotgun T3 | `shotgun-t3-flamer`(火炎放射器)**素材○** | `shotgun-t3-homing`(誘導散弾) |
| rifle T1 | `rifle-t1-deserttech`(デザートテック) | `rifle-t1-bolt`(ボルトアクション) |
| rifle T2 | `rifle-t2-heavysniper`(大型狙撃銃) | `rifle-t2-icelance`(氷槍) |
| rifle T3 | `rifle-t3-railgun`(レールガン) | `rifle-t3-eyelaser`(アイレーザー) |
| glauncher T1 | `glauncher-t1-rocket`(ロケットランチャー) | **無し**(ランチャーは新規3挺のみ) |
| glauncher T2 | `glauncher-t2-alchemy`(錬金砲) | **無し** |
| glauncher T3 | `glauncher-t3-signal`(シグナルランチャー) | **無し** |

- **素材○** = 絵を受領し `WEAPON_ICON_KEYS` と `pixiTextures` の**両方**に登録済み。
- **実装○** = `CATALOG` と `SLOT_CANDIDATES` に入っていて、ゲーム中に出る。
- サブウェポン「金環」は銃ではないのでこの表に入れない(サブのキー体系に従う)。

### 11-8. 第2弾の実装見積り(★2026-09-05に数え直した)

**社長指摘「無限弾はむずくないのでは?」は正しい。** 私は「9種のうち8種が新しい挙動」と書いたが、
**実コードを当て直すと本当に新規なのは2種**だった。既存資産の実在を確認した結果が下表。

| 武器 | 区分 | 根拠(実コードで確認) |
|---|---|---|
| **ボルトアクション**(R-T1) | **データのみ** | 数値 + `rangeOverride`(第1弾で追加済み) |
| **クロスボウ**(H-T1) | **データ+小(読み側の棚卸し)** | 「1発ごとにリロード」= `magSize: 1`(既存フィールド)。**「無限弾」= リザーブを `Infinity` として扱うだけ**——`refillWeaponMagazine` は**守護霊のリザーブ非消費を `reserve = Infinity` で表す前例**を持つ(同関数のコメントに明記)。**ただし分岐は1箇所では済まない**(監査C-4): リザーブは生フィールド(`ammoHandgun` 等)で、`ammoPoolFor` 経由11箇所+`AMMO_FIELD[` 直参照7箇所+生フィールド参照が useGameLoop/gameStore/GameHUD/ShopMenu/firstAidKit に散っている。**HUDの残弾表示・弾切れ時の自動切替・救急鞄の払い出し・商人の弾購入まで棚卸しが要る** |
| **切替式SG**(S-T1) | **データ+小1つ** | リロードを境に散弾/スラッグへ切替 = **モード状態1つ + 2セットの数値**。距離判定は不要(社長指定) |
| **デュアルレンジピストル**(H-T2) | **データ+小1つ** | 距離で数値セットを差し替え + ヒステリシス。**対象までの距離は自動射撃の射程判定で既に毎フレーム計算済み**なので、読むだけ |
| **誘導散弾SG**(S-T3) | **一部流用+新規** | 旋回そのものは在る(`gameStore.ts` の `weaponType === 'homing-missile'` ブロック)が**その分岐に閉じている**ので散弾へ開く必要。**「複数なら分散/1体なら集中」の対象割り振りは新規**(ホーミング弾は長押しロックで発射前に対象が決まる別方式) |
| **アイレーザー**(R-T3) | **一部流用+新規** | 流用できるのは**照準追尾の純関数 `stepLaserAim` と位相関数だけ**(`mimirLaserTrack.ts`)。**ダメージ判定は敵→プレイヤー**、**描画はボスの `bossState` を読む pixiScene 側**、状態機械はミーミルのボスブロック内。**プレイヤー側の貫通・継続ダメージ判定/武器の溜め・照射状態/プレイヤーレーザーの描画は新規** |
| **氷槍ライフル**(R-T2) | **一部流用+新規** | **床の持続ダメージという前例は無い**(監査C-2): `tickGroundFires` は v0.25.3280 以降**ダメージを与えず延焼(`burnUntil`)を付ける**だけで、判定は円・寿命はモロトフ定数固定。`floorUntil`/`applyGlenFloorDamage` は**敵→プレイヤー**。流用すると**氷の槍が敵を燃やす**。線形の床判定・配列・描画は新規(小) |
| **ガンブレード**(H-T3) | **★新規** | 至近距離で**近接系の強攻撃へ切替**。銃の射撃経路から近接の攻撃経路を呼ぶ必要がある |
| **コイルSG**(S-T2) | **★新規** | 散弾が**一度外へ広がってから再収束**する軌道。現状の弾は直進のみ |

**⇒ 新規2 / 一部流用+新規3 / データ寄り4。** ★**「流用できる」と書いた3件は、監査で全部
「一部流用+新規」に格下げされた**(2026-09-05)。**楽な方へも間違える**ので、見積りは
**「その資産がプレイヤー側で・同じ向きに・同じ判定形で使えるか」まで確かめてから**書く
(敵→プレイヤーの判定はそのままでは使えない)。刻む時は「新規2種を最後に回す」のが安全。
**前提工事はショットガン拡散のTier固定を解く1つだけ**(切替式SG・コイルSGが要る)。

### 11-5. QA方針(社長指定)
**多段ヒット系(制圧型ショットガン・火炎放射器・錬金砲)は、スキル発動判定が1ヒット単位だと
非常に強くなる可能性がある。** ただし「強くなるから禁止」ではなく、
**意図したシナジーなのか、処理回数の事故なのかだけをQAで確認する**。

**★前例あり(実在確認済み)**: `ice-shot` / `incendiary-round` は**命中のたび**に適用される。
延焼弾Lv2/3 の炎床は「命中のたび」に出る問題が既に起きており、**クールダウン
(`INCENDIARY_FLOOR_CD_MS`・`incendiaryFloorNextAtRef`)を挟んで解決済み**で★未決として記録も残っている。
よって**多段ヒット武器を作る時は、ヒット回数のカウンタを最初から仕込む**(事故かシナジーかを
数字で切り分けられるようにする)。

## §12 実装の見積り(2026-09-05・監査で是正)

**★訂正**: §8 と旧§11 は「仕組みを作れば以後は純データの追加だけ」と書いていたが、**成立しない**。
**12種のうち8種**が武器ごとの新しい挙動コードを要る。
**★ただし「新しい挙動」と「既存APIを1箇所で呼ぶ」を混ぜて数えない**(社長指摘2026-09-05
「無理に重い開発にしようとしてる」)。**パイルドライバーは後者**なので下表で「足りる」に移した。

| 武器 | 既存の仕組みで足りるか | 要るもの |
|---|---|---|
| デリンジャー(H-T1) | **足りる** | データのみ(`magSize:2` + 数値) |
| 制圧型ショットガン(S-T2) | **データ+射程1つ** | 「長射程」は射程がカテゴリ固定(`RANGE_BY_CATEGORY`)なので **`rangeOverride` が要る**(監査C-5) |
| デザートテック(R-T1) | ほぼ足りる | **弾薬代用**の分岐(専用弾を優先消費 → 尽きたら他カテゴリの弾へ) |
| ハンドキャノン(H-T2) | 新規 | **敵ごとの連続命中減衰**(#U12 の解釈裁定が要る) |
| パイルドライバー(H-T3) | **足りる** | 射程の項目1つ(`rangeOverride`)+ 既存APIの呼び出し2行(`knockbackEnemy` / `postureImpact`)。**新しい仕組みは無い** |
| 収束型ショットガン(S-T1) | 新規 | **命中で縮む拡散**(現状 `SHOTGUN_SPREAD_CONE_RAD_BY_TIER` は**Tier固定**=可変にする前提工事) |
| 火炎放射器(S-T3) | 新規 | **連続照射**(弾ではなく持続判定)。弾のプール/描画とは別系統 |
| 大型狙撃銃(R-T2) | 新規 | **静止時間の蓄積**→射程/連射に反映。移動でリセット |
| レールガン(R-T3) | 新規 | **手動ターゲットサークル射撃**(PHILL銃=`phill-revolver` に実在=流用が本命)+ 頭部確定クリ |
| ロケットランチャー(G-T1) | 新規 | **溜め** + 溜め中の接触爆発 + 直進弾 + **不発**(現行 t1/t2 は転がる=`GLAUNCHER_ROLL_DETONATE_PX`) |
| 錬金砲(G-T2) | 新規 | **破裂→付着→3段階成長→指離しで起爆**(近接/カウンターと同時) |
| シグナルランチャー(G-T3) | 新規 | **地点記録** + 時間差空爆(予告の赤い円は既存の窓マスク文法で描ける) |

**前提工事(先に要るもの)**
- **#U9 弾命中のノックバック**(パイルドライバー)。**現行に経路が無い。**
- **#U10 体勢削りの比率**(パイルドライバー)。**仕組みは既にある**(`postureImpact` 引数 + `postureMult`)。
- **グレネードのキー固定を category 判定へ**(2026-09-06にランチャー3挺が復活したため**再び必要**)。
  実在箇所は2つ: **`isGrenadeGunKey`**(`weaponUtils.ts:318`・3キー直書き)と
  **弾の絵**(`pixiScene.ts:24661`・同じ3キー直書き)。**`GLAUNCHER_ROLL_DETONATE_PX` は工事不要**
  ——表に無いキーは「転がらない=t3と同じ」に落ちるので、新3挺の望ましい挙動と一致する(§16-3)。
- **ショットガン拡散のTier固定を解く**(収束型)。
- **`rangeOverride` を射程を読む全箇所へ通す**(監査C-2): 自動射撃ゲートのほか、
  **ボットの射程バンド**(`playtestDriver.ts:482` / `useGameLoop.ts:2395`)・**守護霊**(`useGameLoop.ts:10371`)・
  **幻影**(`phantomTick.ts:427`)・`buildGhostGunShots` が `RANGE_BY_CATEGORY` を直参照している。

## §13 第1弾のバッチ(★#U2=(a)・対象=ハンドガン)

### 13-1. 数値(★叩き台。社長が「数値は未確定」と明言・§13-2 の実測で調整する)

**実効DPS は §5 の式(実装のリロード=`max(250, reloadMs*2)`)で計算。1発ダメージ = `damage * count`。**

| | 既定 | 実効DPS | ユニーク | 実効DPS | 差 | 1発ダメージ |
|---|---|---|---|---|---|---|
| T1 | ハンドガン 9 / cd420 / 1発 / 12発 / R900 | **15.79** | **デリンジャー** 17 / cd300 / 1発 / **2発** / R700 | **17.00** | **+7.7%** | 9 → 17 |
| T2 | 二丁ハンドガン 9 / cd420 / **2発同時** / 10発 / R1100 | **28.13** | **ハンドキャノン** 31 / **cd620** / 1発 / 6発 / R1200 | **30.39** | **+8.1%** | 18 → 31 |
| T3 | マシンピストル 7 / cd100 / 1発 / 30発 / R1300 / crit5% | **37.50** | **パイルドライバー** 36 / cd450 / 1発 / 6発 / R1300 | **40.75** | **+8.7%** | 7 → 36 |

**★3種とも「+」**(社長指示2026-09-05「ボス撃破報酬なので、基本的には+」)。
**上位互換にならないことは不変条件6(支配テスト)で担保する**——3種とも
**マガジンが小さい**(2/6/6 対 12/10/30)ので全軸勝ちにはならない。加えて
デリンジャーは装填で止まり、ハンドキャノンは同一敵で減衰し、パイルドライバーは射程が108(既定170)。

- **デリンジャーの瞬間DPS = 17 ÷ 0.30秒 = 56.7**(既定は 21.4 = **2.6倍**)。2発撃つ0.6秒に対し
  **実装リロード1.4秒**(=時間の70%は装填中)。社長仕様「瞬間火力＋頻繁なリロード」どおり。
- ★**cd600→620 に直した(v0.25.4166)**: 帯を「±10%の+寄り」へ戻したら +10.2% が枠を 0.2pt 超えていた。
  **単発威力31は武器の売りなので保ち、連射だけ僅かに落とした。**
- **ハンドキャノンの +8.1% は「減衰が一度も掛からない」値**(=雑魚を撃ち分けた時)。
  同じ敵に撃ち切ると **18.6 = 既定比 約−34%**(減衰1/0.8/0.6/0.4/0.4/0.4で計算)。社長仕様「雑魚を次々狙うほど強く、同一ボスを撃ち続けると
  弱くなる」そのままなので、**帯は多体側(撃ち分け)で取る**(★未決 #U11 の(a))。
- **パイルドライバーの数値は #U9・#U10 が裁定されるまで意味を持たない**(ノックバックも体勢削りも
  現行に土台が無いため)。

**`CATALOG` に足す定義**
```
'handgun-t1-derringer':  damage 17, cooldown 300, count 1, magSize 2, reloadMs  700, projectileSpeed 540, projectileSize  9
'handgun-t2-handcannon': damage 31, cooldown 620, count 1, magSize 6, reloadMs 1200, projectileSpeed 560, projectileSize 11
'handgun-t3-piledriver': damage 36, cooldown 450, count 1, magSize 6, reloadMs 1300, projectileSpeed 620, projectileSize 12, critChance 0.05
```

**新しく要るフィールド(監査C-2で是正: 「3つ足すだけ」では足りない)**
| 追加先 | 何を足すか |
|---|---|
| `WeaponDef`(`weaponUtils.ts`) | `rangeOverride?` / `knockbackMult?` / `postureMult?` |
| **`Weapon` 型(`game.ts:1452`)と `createWeapon`** | **同3つ**。`createWeapon` は**フィールドを明示コピー**するので、書き足さないと落ちる |
| `Projectile`(`game.ts`) | **`knockbackMult` を新設**。**`postureMult` は既に存在**(弾幕の王が使用)=流用する |
| 射程を読む全箇所 | §12 の一覧(ボット・守護霊・幻影・`buildGhostGunShots`)に `rangeOverride` を通す |

**★パイルドライバーの射程 = 108px**(社長指示2026-09-05「ストライカーの射程伸びた時の近接範囲と
パイルドライバー同じくに」)。実コード: `huntingMeleeRadius`(`gameStore.ts:1229`)は溜め完了時に
`MELEE_RADIUS(74) + HUNTING_MELEE_RADIUS_BONUS_BY_LEVEL[Lv]` を返す(**Lv1=92 / Lv2=98 / Lv3=108**)。
**最大まで伸びた時の 108** を採る(パイルドライバーはT3なので上限に合わせる)。
- **数字を直書きしない**: `MELEE_RADIUS + HUNTING_MELEE_RADIUS_BONUS_BY_LEVEL[3]` から**導出**して
  1本の定数にする(同じ値を2箇所に書かない=このプロジェクトの作法)。ハンティングの値を後で調整したら
  **パイルドライバーの射程も一緒に動く**のが正しい姿。
- **★ズーム補正から外す**(監査C-7の是正): `rangeOverride` を `zoomedGunRange` に通すと
  ボス交戦のズーム0.4で **108→270px** になり、**揃えたはずの近接範囲と食い違う**。
  近接範囲(`MELEE_RADIUS`)はズームで伸びない(CLAUDE.md「Visual vs hitbox」)ので**同じ扱いにする**。
- 弾の飛距離(`duration: 1400`)は射程と無関係なので、**外れた弾は約1300px飛ぶ**。
  「短射程」は**撃ち始める距離**の話である旨をコメントに明記する。

**新しく要る挙動(1つ)**
- **ハンドキャノンの連続命中減衰**: 1命中ごとに **−20%・下限40%**。**リロード完了で全リセット。**
  **敵ごとに独立。** 累積の解釈は **★未決 #U12**(リロードまでの累計 / 直前対象が同じ時だけ)。
  - **★リセットの条件は「装填が発生した時」で統一**(社長裁定2026-09-05。**部分装填=リザーブが足りず
    満タンにならない場合も含む**——`refillWeaponMagazine` は `min(必要数, リザーブ)` を詰めるため。
    通常リロード `tickReload` の従来挙動と同じ条件で、3経路すべて揃えてある)。通常のリロード完了に加え、
    **即時装填(クイックマガジン拾得 / オーバークロック覚醒)でもリセットする**。この2つは `tickReload` を
    通らないので、放っておくと**そこだけ減衰が残る=プレイヤーには気づけない例外**になる。
  - **純関数 `src/utils/handcannonDecay.ts`** に切り出し、`useGameLoop`/`gameStore` に直書きしない。
    ユニットテストを同コミットで書く。
  - 敵の消滅時にカウンタを捨てる(**理由はメモリ掃除**。敵IDは `Date.now()+乱数` で**使い回されない**
    ので誤爆の心配は無い=監査C-3の是正)。消滅経路は複数(キル/画面外リサイクル/上限カリング)なので
    **「生きている敵のIDに無いものを捨てる」掃除**にする(1箇所で拾おうとしない)。

### 13-2. 強さの確認(★2026-09-05・大幅に軽くした)

**第1弾ではボットラン実測を作らない。** ハンドガン3種は**全て §5 の実効DPSの式で計算できる**ので、
**式で確認 → 実機で社長が触る**で足りる(CLAUDE.md「実機で見えているかが最終の真実」)。

- **v0.25.4126 の「決定的アリーナを作る」計画は取り下げ**(社長指摘「無理に重い開発にしようとしてる」)。
  監査A-5〜A-7 が指摘した穴(無シード・ボットが射程70を撃たない・ヘッドレス写しにユニーク要素が
  通らない)は**すべて「実測を作るなら」の話**なので、作らなければ発生しない。
- ~~**★未決 #U8=(a) の実測基盤**~~ → **不要になった(2026-09-06・§5-2)**。5種とも
  「1サイクル」の式で ±10% の帯に入れられたので、**ボットラン実測は作らない**。
- **パイルドライバーの射程108だけは式に出ない**ので、**実機確認の項目**として社長へ渡す
  (「近づかないと撃たない銃として成立しているか」「ハンティングの伸びた間合いと揃って見えるか」)。

### 13-3. 受け入れ条件
1. 既定候補だけを設定した状態で、**現行と完全に同じ挙動**になる(回帰ゼロ)。
2. 設定を候補へ変えると、**出撃時のT1・ドロップ・武器箱・宝箱・武器庫の全経路**でその候補が出る。
3. **地面に落ちている絵と、拾った銃が一致する**(§4-1)。
4. 練習ラン(ボスモード/ガントレット)で**解放が起きない**(§6)。
5. 不変条件2〜6のテストが通る(1・7は §5 の適用範囲に従う)。
6. **実効DPSが §5 の帯に入っている**ことをテストで確認する(§13-2。ボットラン実測は作らない)。
7. (第1弾当時の条件。**次のバッチで `BOSS_UNLOCK` を埋める**=§11-6/§11-7)**`BOSS_UNLOCK` の中身は空のまま**(★未決 #U3 が裁定待ち)。確認は
   **オプション画面「テスト開発用」の「武器解放」トグル**(社長指示2026-09-05・端末に残る)か、
   **`?unlockall=1`** のツマミで行う
   (読み取りは `weaponSlot.ts` 側で `typeof window === 'undefined'` ガード付きの独自パース=監査B-2)。
8. `npm run typecheck` / `npm run lint` がエラー0。

## §15 監査の記録(解放・購入の4段化・2026-09-05)

- **着手前(Fable 5.1)**: (A)4件 / (B)2件 / (C)6件。(A)3件と(C)を本文へ反映、
  **#U14(金環)を社長裁定へ**(→(a)設計図ゲート+従来価格で決着)。
- **検収1巡目(Fable 5.1)**: **(A)なし**。4段の分離(設計図では装備にも生成点にも出ない)・回帰ゼロ・
  撃破の2ガード維持・型単独キー・棚の集合・`spendGold` の戻り値を見てから解放・トグルとリセットの
  両台帳対応・保存の後方互換を、すべて実コードで裏取り済み。
- **(B)2件を積む**:
  1. **装備設定の「選択中」表示が消えることがある**——テストトグルONでユニークを選んでからOFFにすると、
     保存値が未購入キーを指したままになり、どのボタンにもチェックが付かない。
     **実挙動は既定へ正しくフォールバックする**ので出荷バグではないが、表示だけ「未選択」に見える。
  2. **タイトルの「ボス戦テスト」(`?gateboss=1` 等)は `isPracticeRun()` ではない**ので、
     そこでミゲル等を倒すと**本番の台帳に設計図が書かれる**。小烏丸・年表・城ボスフラグが既に同じ扱いなので
     **既存の作法どおり**(今回の増設ではない)だが、開発チャネルで設計図を取れる穴として記録。
- **検収2巡目(Fable 5.1)**: **(A)なし**(2巡連続)。`resetProgress` の消去は1行追加のみで既存は全て健在 /
  装備設定の同居欄(サブウェポン・アバター・スキル一覧)は無変更 / 開発施設の既存リストとガチャも無変更 /
  撃破確定処理の年表・城ボスフラグ・小烏丸も無変更 / **1周の通し(倒す→設計図→200G→装備→出撃)に
  切れる所なし**——を実コードで裏取り。**規約どおり実機へ。**
- **(C)2件目を是正**: changelog の「**ボスを倒すと**設計図が手に入る」は実態より広い
  (落とすのは**ミゲル/ジブリル/ラフィの3体だけ**)。「一部のボス」へ直した。
- **(C)1件を是正**: DEVLOG が `v0.25.4144` を名乗っているのに bump と changelog が入っていなかった
  (共有ファイルは設計チャットの担当。**私の漏れ**)。

## §14 監査の記録(第1弾・着手前・Fable 5.1・2026-09-05)

- **(A) 7件**: A-1(弾ノックバックの経路が無い→**#U9**)/ A-2(クリが出なかったヒットは体勢を削れない→**#U10**。ただし監査の「体勢システムの仕様変更が要る」は
  **過大**で、実際は `postureImpact` 引数が既にあり比率の裁定だけで済む=社長指摘を受けて再調査し是正)/
  A-3(実効DPSの式が生 `reloadMs` を使っていた→**§5 是正・全数値を組み直し**)/
  A-4(ハンドキャノンの帯は減衰なしの上限値→**#U11**)/ A-5(同一シードにできない→**§13-2 作り直し**)/
  A-6(ボットが射程70を撃たない・ヘッドレス写しにユニーク要素が通らない→**§13-2 に工程追加**)/
  A-7(武器の握らせ方とドロップ上書きが未定義→**§13-2 に手順追加**)。
- **(C) 7件**: C-1(新規は8種ではなく**9種**)/ C-2(フィールドは3つでは足りない・`Weapon`型と
  `createWeapon`・`Projectile`・射程参照の全箇所)/ C-3(敵IDは使い回されない=掃除の理由が違う)/
  C-4(「連続」の解釈→**#U12**)/ C-5(制圧SGの長射程は `rangeOverride` が要る)/
  C-6(新規テストはどの既存コマンドでも走らない)/ C-7(`rangeOverride` はズームで伸びる・弾は射程外へ飛ぶ)。
  **全て本文へ反映済み。**
- **(B) 5件は別案件へ積む**: 不変条件6のクリ比較は `weaponBaseCritChance` の解決後の値で行う /
  `?unlockall=1` のパースは独自実装が要る / 守護霊・幻影がユニークを撃つ時の再現有無 /
  合格条件は「下に外れた候補」を素通りさせる(下限も置くか) / `knockbackEnemy` の強さ上限は既定3。
- **★仕様変更に及ぶため社長へ戻した**: **#U9(弾ノックバック)・#U10(非クリ弾の体勢削り)・
  #U11(帯をどちらで測るか)・#U12(連続の解釈)・#U13(第1弾の範囲)**。

## §16 残り18挺のバッチ仕様(社長指示2026-09-06「いったん全武器、実装までもってって。絵は後で渡す」)

**対象 = 第1弾の残り6 + 第2弾の9 + ランチャー3 = 18挺**
(ランチャー3挺は社長裁定2026-09-06で復活・§11-7)。 絵は後で来るので**素材は待たない**
(未受領の12挺は `WEAPON_ICON_KEYS` / `pixiTextures` に**登録しない**=絵文字で出す。
絵が来たら §11-9 のキーで保存して2箇所へ足すだけ)。

### 16-1. 数値(実効DPS帯 = 既定の **±10%・狙いは+側**・§5)
★**v0.25.4166 で帯を「+0〜+15%」から取り決めどおりの「±10%の+寄り」へ戻した。**
**枠を超えていたのは制圧型SG(+14.3%)の1挺だけ**で、cd780→880 に落として +8.8% にした
(他13挺は最初から枠の中=+0.2%〜+8.6%)。
既定の実効DPS: shotgun **17.82 / 21.21 / 26.87** / rifle **23.08 / 28.95 / 45.83** / handgun **15.79 / 28.13 / 37.50**。

| キー | 名前 | damage / cooldown / count / magSize / reloadMs | 実効DPS | 差 |
|---|---|---|---|---|
| `shotgun-t1-focus` | 収束型ショットガン | 6 / 700 / 5 / **2** / 900 | 18.75 | +5.2% |
<!-- ★収束の狭まりは「1トリガー1回」。1トリガー5ペレットなのでペレット単位だと1トリガーで詰まりきる(段数5.22)。バッチB検収で是正・v0.25.4170 -->
| `shotgun-t2-suppress` | 制圧型ショットガン | 4 / **880** / 12 / 3 / 1800 | 23.08 | +8.8% |
| `rifle-t1-deserttech` | デザートテック | 27 / 800 / 1 / 6 / 1000 | 23.82 | +3.2% |
| `rifle-t2-heavysniper` | 大型狙撃銃 | 62 / 1300 / 1 / 5 / 2000 | 29.52 | +2.0% |
| `handgun-t1-crossbow` | クロスボウ | 24 / 300 / 1 / **1** / 600 | 16.00 | +1.3% |
| `handgun-t2-dualrange` | デュアルレンジ(近) | 14 / 260 / 1 / 10 / 1100 | 29.17 | +3.7% |
| 〃 | デュアルレンジ(遠) | 28 / 700 / 1 / 10 / 1100 | 30.43 | +8.2% |
| `handgun-t3-gunblade` | ガンブレード | 8 / 110 / 1 / 26 / 1300 | 38.10 | +1.6% |
| `shotgun-t1-cycle` | 切替式(散弾) | 6 / **1000** / 5 / 3 / 1000 | 18.00 | +1.0% |
| 〃 | 切替式(スラッグ) | 30 / **1000** / 1 / 3 / 1000 | 18.00 | +1.0% |
| `shotgun-t2-coil` | コイルショットガン | 5 / 780 / 9 / 3 / 1800 | 22.73 | +7.1% |
| `shotgun-t3-homing` | 誘導散弾ショットガン | 5 / 430 / 9 / 3 / 1700 | 28.78 | +7.1% |
| `rifle-t1-bolt` | ボルトアクション | 52 / 1500 / 1 / 5 / 1600 | 24.30 | +5.3% |
| `rifle-t2-icelance` | 氷槍ライフル | 58 / 1200 / 1 / 5 / 2000 | 29.00 | +0.2% |
| `glauncher-t1-rocket` | ロケットランチャー | 140 / **500** / 1 / **1** / 2000 | 31.11 | +8.6% |

**★汎用式では表せない3挺(§5-2 の「1サイクル」で測る。★帯は同じ ±10%)**
| キー | 名前 | 数値(叩き台) |
|---|---|---|
| `shotgun-t3-flamer` | 火炎放射器 | 射程 **90px** / 扇 0.5rad / **100msごとに5ダメージ**(照射中50DPS)/ 装填40・リロード1500。100msごとに弾1消費 → **サイクル実効 28.57(+6.3%)** |
| `rifle-t3-railgun` | レールガン | オート: **101** / 1300 / 1発 / 4発 / R2200(=**42.08 / −8.2%**・既定より**低い**=社長仕様「オート時は低め」を守りつつ**±10%の枠の中**へ。旧95は−13.6%で枠外だった)。手動射撃は PHILL 銃と同じターゲットサークル。**手動で頭部命中は確定クリ** |
| `rifle-t3-eyelaser` | アイレーザー | 溜め **700ms** → 照射 **3000ms**(**100msごとに14ダメージ**=照射中140DPS)/ 貫通 / 照射中は対象を追尾 / リロード 2500ms → **サイクル実効 48.28(+5.3%)**。★旧「1tick 8」は**−39.8%**で枠を大きく割っていた |
| `glauncher-t2-alchemy` | 錬金砲 | **破裂 30**(範囲70px)/ 700 / 1発 / **4発** / 1900。**石**: 1段 60 / 2段 120 / 3段 **200**(起爆範囲 110px)→ **サイクル実効 48.48(+5.8%)** |
| `glauncher-t3-signal` | シグナルランチャー | **手動のみ(オート射撃なし・社長裁定2026-09-06)**。空爆 **275** / cd1400 / 1発 / **3発** / 2000。**遅延900ms / 範囲160px / 弾1消費** → **遅延900msを cooldown に足して(実効2300)サイクル実効 75.69(+8.1%)**。★旧300は**+18.0%**で枠外だった |

**ランチャー3挺の帯の見方**(既定の実効DPS: **28.65 / 45.83 / 70.00**)
**3挺とも §5-2 の「1サイクル」で ±10% の中に入れた**(ロケラン +8.6% / 錬金砲 +5.8% / シグナル +8.1%)。
- **ロケラン**: **溜め(500ms)は `cooldown` に含めて表現する**——別時間として実装すると表の数字より
  実効が下がって帯を割る(受け入れ条件7)。`magSize:1` なので毎射リロード。
  **不発(何にも当たらないと爆発しない)**は式外の代償。
- **錬金砲**: 単体相手の1サイクル= 破裂4回(30×4)+ 3段まで育てた石の起爆(200)= **320 を 6.6秒**
  (4×700 + 1900×2)で出す = **48.48**(+5.8%)。**群れに撃つと石の数だけ起爆が増える**が、
  それは既定グレネードのAoEと同じ土俵=式外の上振れ。
- **シグナル**: **オートを持たない**(社長裁定2026-09-06)。**レールガンとは別の形**——レールガンは
  「オート+手動」、**シグナルは PHILL銃と同じ手動専用**。
  ★**着弾遅延900msは撃てない時間なので `cooldown` に足して測る**(ロケランの溜めと同じ扱い)=
  実効cd2300 で **75.69(+8.1%)**。**置き撃ちを外した時の損は式外**(汎用式の武器で射程や装填の
  遅さを引かないのと同じ)。

### 16-2. 挙動(武器ごとに要る新しい処理)

**データだけで済む(新しい処理なし)**
- **制圧型SG**: 上の数値 + `rangeOverride: 250`(ライフル並みの長射程)+ 散り角 **1.30rad**(既定T2は0.70)。
  **弾付与スキル(延焼/凍傷)は武器側で何もしない**——既存のスキルが命中ごとに乗るだけ(社長ルール)。
- **ボルトアクション**: 上の数値 + `rangeOverride: 320`。

**小さい追加**
- **クロスボウ**: `magSize: 1`(1発ごとに装填)+ **無限弾**。無限弾は新フィールド
  `infiniteAmmo?: true` を見て**リザーブを `Infinity` として扱う**。★**読み側の棚卸しが要る**
  (`ammoPoolFor` 経由/`AMMO_FIELD[` 直参照/生フィールド参照。**HUDの残弾表示・弾切れ時の自動切替・
  救急鞄の払い出し・商人の弾購入**まで。監査C-4)。**HUDは「∞」表示**にする。
- **デュアルレンジピストル**: 対象までの距離で**2セットの数値を差し替える**。
  **近へ入る=120px / 遠へ戻る=180px のヒステリシス**(境界で震えない)。距離は自動射撃の射程判定で
  既に毎フレーム出ているので読むだけ。`rangeOverride: 220`。
- **切替式SG**: **リロードを境にモードが入れ替わる**(散弾→スラッグ→散弾…)。武器に `cycleMode` を1つ持ち、
  リロード完了で反転。散り角は散弾 **1.10rad** / スラッグ **0**。**距離では切り替えない**(社長指定)。
- **収束型SG**: **命中した射撃ごとに散り角が1段階狭まる**。初期 **1.30rad** → 命中1回ごとに **−0.18** →
  下限 **0.36**。**2.5秒当てないと初期へリセット**。状態は純関数モジュール
  (`src/utils/focusSpread.ts`)に置き、ユニットテストを同コミットで書く。
- **デザートテック**: **専用弾が尽きたら他カテゴリの弾を代用**。優先順は rifle → handgun → shotgun → glauncher。
  `rangeOverride: 200`。
- **大型狙撃銃**: **静止時間の蓄積**。止まっている間 0→**3秒**で最大まで伸び、射程 **250→400**・
  連射間隔 **×1.0→×0.6**。**移動した瞬間に0へリセット**。帯は**蓄積0の素の値**で測る(上表)。

**新規(重い)**
- **ガンブレード**: 通常は銃。**至近(≤90px)に敵が入ると近接系の強攻撃へ切替**——
  ★**ダメージ ×1.6・間隔400ms(=32DPS)**(**§17-5 で是正済み。旧案の「×3」= 218DPS は
  パイルドライバーの2.7倍で劣る局面が1つも無い完全上位互換だった**)。
  **遠距離の38.10より低い**ので「近づかれると火力は落ちるが、押し返して体勢を崩せる」が代償になる。
  **体勢削りは `heavy`(0.10)**、ノックバック **×1.5**。**近接モード中は銃を撃たない。**
  銃の射撃経路から近接の判定を呼ぶ配線が要る。
- **コイルSG**: 散弾が**一度外へ広がってから狙点へ再収束**する。現行の弾は直進しかしない。
  ★**軌道を確定させた(2026-09-07・C-2検収A-1)。角度を振る形では収束しない**——角度を元へ戻しても
  外へ振れていた間の**横ズレが位置に積み上がる**ので、最終軌道は既定と平行に外へずれた直線=
  「常に広いだけのSG」になる(実際にそう実装されて上がってきた)。
  **⇒ 角度ではなく「中心線からの横ズレ」を直接与える。**
  | 項目 | 値 |
  |---|---|
  | 横ズレの式 | `lateral(t) = A_i × sin(π × min(t / T, 1))` (**t=0 と t=T で必ず 0**) |
  | 収束距離 `COIL_CONVERGE_PX` | **140px**(=ショットガンの射程。**当たる距離で収束が完了する**) |
  | 収束時間 `T` | `COIL_CONVERGE_PX ÷ 弾速` から導く(弾速を変えても収束点が動かない) |
  | 振幅 `A_i` | ペレットiごとに **±44px を等間隔で割り振る**(9発なら −44〜+44) |
  | 基礎の拡散角 | ★**持たせない**(`spreadRadOverride: 0`)。**広がりは全部この横ズレで作る** |
  | 収束後(t>T) | 横ズレ0のまま直進(=収束点から先は1本の線になる) |
  - **これで「単純な狭角SGとは別物」になる**: 手前は**±44pxの膨らみ**、**140pxで1点に絞れる**。
    近いほど面、遠いほど点。**既定T2(角度0.70radの素直な扇)とは形が違う。**
  - **慣性は `sin` が持っている**(0で速度0→中央で最大→Tで速度0)。等速で折れ曲がらない。
  - **受け入れ条件**: **t=T で全ペレットの横ズレが0**になることをユニットテストで固定する。
  - **時計は `gameTime`**(壁時計 `Date.now()` だとスローモーション中に軌道の形が潰れる)。
- **誘導散弾SG**: 各ペレットが**近くの敵へ誘導**。**敵が複数なら分散・1体なら全弾集中**。
  旋回そのものはホーミング弾に在るが**その分岐に閉じている**ので開く必要。**対象の割り振りは新規**。
  ★**2点を確定させた(2026-09-07・C-2検収A-3)。既存のホーミングの旋回速度をそのまま使うと曲がれない**——
  誘導ロケットは弾速200px/sで旋回5rad/s=**半径40px**だが、このペレットは**弾速705px/s**なので
  同じ5rad/sだと**半径141px**。**ショットガンの射程140pxより大きい**ので、別方位の敵へ割り当てられた
  ペレットは曲がり切れずに素通りする=社長仕様「基本的に必ず命中」が複数戦で成立しない。
  | 項目 | 決めた形 |
  |---|---|
  | **旋回速度** | ペレット専用に持つ。**半径40px相当**(=誘導ロケットと同じ曲がり具合)。`弾速 ÷ 40` で導く |
  | **対象の割り振り** | ★**方位で組む**。ペレットを拡散角の順、対象を**プレイヤーから見た方位角の順**に並べ、近い方位どうしを対応させる(round-robin は維持)。**近い順だけで配ると、真横の敵へ端のペレットが飛ばされる** |
  | 死んだ対象 | **`isCorpse` を見て次の生存対象へ移す**(9発集中の武器では死体を旋回するのが目立つ) |
- **氷槍ライフル**: 弾の**射線に短時間の床(線)を残す**。幅 **28px** / **1.2秒** / **200msごとに直撃の20%**。
  ★**既存の炎床は流用できない**(ダメージを与えず延焼を付けるだけ・判定は円・敵→プレイヤー向き)。
  **武器自体に凍傷は持たせない**(社長指定。凍傷スキルとのシナジーは許容)。
- **火炎放射器**: **弾を撃たず前方の扇に持続判定**。100msごとにダメージ。**武器自体に燃焼は持たせない**(社長指定)。
- **レールガン**: **PHILL銃の手動ターゲットサークル射撃を流用**。**頭部命中で確定クリ**。
  オート射撃は通常どおり(数値は上表)。
- **アイレーザー**: **溜め→照射3秒・貫通・追尾**。照準追尾は `stepLaserAim` を流用できるが、
  **プレイヤー側の貫通・継続ダメージ判定/武器の溜め・照射状態/プレイヤーレーザーの描画は新規**。
  ★**ターゲット死亡時は再ターゲットしない=その場で照射を終える**(社長裁定2026-09-06「アイレーザーの
  再ターゲットは無し」)。**残りの照射時間は失われ、そのままリロードへ入る**(=次の敵へ移らないし、
  空へ撃ち続けもしない)。**これが金環との差**——金環は射線固定で撃ち切り、アイレーザーは対象を追う代わりに
  対象が消えたら終わる。
- **ロケットランチャー**: **溜め(500ms)→ 直進弾(追尾なし)→ 敵か壁に当たった時だけ爆発**。
  **何にも当たらなければ不発**(現行 t1/t2 の「転がって一定距離で爆発」とは別物=`GLAUNCHER_ROLL_DETONATE_PX`
  に**入れない**)。**溜め中でも弾頭に敵が接触すればその場で爆発**(=溜め中の自機前方に小さい判定を置く)。
  爆発範囲は既定t1より**やや広い**(既定比 ×1.2)。
- **錬金砲**: **破裂(弱い範囲)→ 範囲内の敵へ金の石を付着 → 石持ちに再命中で最大3段階まで成長 →
  指を離した瞬間に全部起爆**(通常の近接/カウンターと同時)。石は**敵ごとの状態**なので
  純関数モジュール(`src/utils/alchemyStone.ts`)に置きユニットテストを同コミットで書く
  (ハンドキャノンの減衰台帳 `handcannonDecay.ts` と同じ作法)。**爆発系スキルとの強シナジーは許容**(社長)。
- **シグナルランチャー**: **PHILL銃のターゲットサイトを流用**。**指を離した瞬間の地点を記録 →
  900ms 後にその地点へ空爆**。**追尾しない**(敵の移動先を置き撃ちする武器)。
  **高い体勢削り / 高ノックバック特化にはしない**(社長)。予告の赤い円は既存の窓マスク文法で描ける。
  ★**オート射撃は持たない**(社長裁定2026-09-06)。**この武器を装備している間、その銃枠は自動で撃たない**
  ——PHILL銃と同じ形。**⇒ 下の「手動専用銃の掟」が要る**(§16-3b)。

### 16-3. 前提工事
- **ショットガンの散り角がTier固定**(`SHOTGUN_SPREAD_CONE_RAD_BY_TIER`)。**武器ごとに指定できるようにする**
  (収束型・制圧型・切替式・コイルが要る)。**既定3挺の値は1つも変えない**(回帰ゼロ)。
- **グレネード系のキー直書きを category 判定へ**(ランチャー3挺・実在箇所は**2つだけ**):
  | 箇所 | 現状 | 直さないとどうなるか |
  |---|---|---|
  | `weaponUtils.ts:318` `isGrenadeGunKey` | `key === 'glauncher-t1'\|t2\|t3` の3キー直書き | 新3挺が**爆発しない直進弾**になる |
  | `pixiScene.ts:24661` 弾の絵 | 同じ3キー直書き(`fx/grenade-ball`) | 新3挺の弾が**代替表現に化ける** |
  - **`GLAUNCHER_ROLL_DETONATE_PX` は工事不要**: 表に無いキーは「転がらない=既定t3と同じ」に落ちる。
    新3挺はどれも転がらないので、**そのままで正しい**。
  - ★**`weaponUtils.ts:640` のコメント「`rangeOverride` はパイルドライバーのみ・転がりと排他」は
    前提が崩れる**(新ランチャーが `rangeOverride` を持ちうる)。優先順位の記述を実態へ打ち直す。
  - ★**タレット/朱雀/爆撃が `GRENADE_WEAPON_KEY='glauncher-t1'` を名乗って同じ経路に乗っている**
    (`useGameLoop.ts:511`)。category 判定に広げても `glauncher-t1` はカテゴリ内なので**挙動は不変**
    ——**回帰ゼロを受け入れ条件1で確認する**。

### 16-3b. ★手動専用銃の掟(シグナルランチャーで初めて必要になる)
**「自動で撃たない銃」は既存に1挺だけある**——**PHILL銃(`phill-revolver`)**。ただしそれは
**研究所の固定銃**で、`RANGE_BY_CATEGORY.phill` のコメントどおり**自動射程判定の外に置く**という
雑な形で済んでいた(`weaponUtils.ts:455`「手動照準の精密射撃。自動射程判定には使わない」)。
**シグナルは `glauncher` カテゴリの通常の装備枠に入る**ので、その形では足りない。
**述語を1つ作る**(`isManualOnlyGunKey`)。対象は `phill-revolver` と `glauncher-t3-signal` の2つ。

| 経路 | 手動専用銃を持っていたら | 直さないとどうなるか |
|---|---|---|
| `fireWeapon` の自動射撃 | **撃たない**(射程ゲートより前で抜ける) | **シグナルが勝手にオート連射する**(カテゴリ既定の射程250で撃ってしまう) |
| **守護霊の借用銃**(`buildGhostGunShots`) | ★**既定の同カテゴリ銃(`glauncher-t3`)へフォールバックして撃つ** | 守護霊が**何も撃たなくなる**(火力ゼロ) |
| **ボット**(`playtestDriver`)の射程バンド | 同上(既定へフォールバック) | ボットが射程判定で固まる/撃たない=計測が壊れる |
| `autoSwitchIfDry`(弾切れの自動切替) | **通常どおり切り替える** | (変更不要) |
| **幻影(敵対側の写し)** | ★**既定の同カテゴリ銃へフォールバックする**(守護霊・ボットと同じ) | **表に幻影の行が無かった**(設計書の穴・検収B-2)。シグナルを写した幻影は **275ダメージの直進弾を1400msごと**に撃つ=**既定t3の約2倍**の敵弾になる |
| 弾種・リザーブ | **glauncher の弾を普通に消費** | (変更不要) |

★**守護霊/ボットのフォールバックは `resolveSlotKey` の不変条件5と同じ思想**
(未解放/使えないキーを渡されたら必ず既定へ落ちる)。**「撃たない」で済ませない**——
プレイヤーが装備を選んだだけで味方の火力が消えるのは、装備画面から予測できない損失。

### 16-4. 刻み(この順で発注する)
1. **バッチA(データ寄り5挺)**: 制圧型SG / ボルトアクション / デザートテック / クロスボウ / デュアルレンジ
   + **散り角の武器別化(前提工事)**。
2. **バッチB(状態を持つ3挺)**: 収束型SG / 切替式SG / 大型狙撃銃。
3. **バッチC(新規の重い7挺)**: ガンブレード / コイルSG / 誘導散弾SG / 氷槍 / 火炎放射器 / レールガン / アイレーザー。
   **さらに2つに割ってよい**(判定の形が違うため: 弾の軌道系=コイル/誘導/氷槍 と 持続照射系=火炎/レール/アイ、
   近接切替=ガンブレード)。
4. **バッチD(ランチャー3挺)**: ロケットランチャー / 錬金砲 / シグナルランチャー
   + **グレネード系のキー直書きを category 判定へ**(§16-3)+ **手動専用銃の掟**(§16-3b)。
   **Cの後**に置く: シグナルは**レールガンで通した手動照準の経路**をそのまま使うので、
   先にCでその形を確定させた方が安い(手動照準の実装を2回書かない)。
   ★**ただしシグナルはレールガンと違いオートを持たない**ので、**手動照準の経路は
   「オートと共存する形(レールガン)」と「単独で立つ形(シグナル)」の両方**を通す必要がある。
   Cでレールガンを作る時に、**手動照準をオートから独立した部品として切り出しておく**こと。

### 16-5. 受け入れ条件(全バッチ共通)
1. **既定候補だけの状態で現行と完全に同じ挙動**(回帰ゼロ)。**既定3挺の散り角は不変**。
2. 新キーは **`DIRECT_GUN_WEAPON_KEYS` に入れる**/**`GUN_KEYS_BY_CATEGORY` には足さない**(§4-2)。
3. **`rangeOverride` は射程を読む全箇所へ通す**(ボット・守護霊・幻影・`buildGhostGunShots`)。
   **ズーム補正には通さない**。
4. **全ユニークは `BOSS_UNLOCK` か `STORE_SOLD_KEYS` のどちらか一方に属する**(排反かつ網羅・§11-6-2)。
5. **全18挺**の実効DPSが帯(**±10%・狙いは+側**)に入る。汎用式で表せない5挺
   (火炎放射器・レールガン・アイレーザー・錬金砲・シグナル)は **§5-2 の「1サイクル」で測る**
   ——**対象外にしない**。★レールガンだけは社長仕様により **−側(−8.2%)** に置く。
6. `npm run typecheck` / `npm run lint` がエラー0。新しい純関数にはユニットテストを同コミットで。
7. ★**持続線分の「当てる」を土台へ切り出す**(金環の検収監査B-1)。金環のブロックに直書きの
   約20行(倍率・`damageChannel`・`hateSource`・除外・`playEnemyDeath`+`dropEnemyXp`・数字/バースト)を
   **`applyBeamPulse()` として共通化してから**、アイレーザーと氷槍をその上に乗せる。**コピーしない。**
8. **ロケットランチャーの溜め(500ms)は `cooldown` に含めて実装する**。別時間として足すと
   実効DPSが表の 31.11 を割り、帯(+0%)を下回る。

### 16-5c. ★バッチD検収で確定させた数値・挙動(2026-09-07)
監査(A)9件のうち、**仕様に数字が無くて実装者が決めようがなかったもの**をここで確定させる。

| 対象 | 決めた形 | なぜ |
|---|---|---|
| **錬金砲の起爆は「範囲攻撃」** | 石の段階で**半径が増える**: 1段 **70px** / 2段 **90px** / 3段 **110px**。**中心(石持ち本体)は満額、外へ向かって減衰**(既定グレネードの splash と同じ形) | 社長仕様「石の段階に応じて**爆発ダメージ・範囲が増加**」。実装は**石持ち1体に単発**で、110pxは**絵にしか使われていなかった** |
| **シグナルの体勢削り** | `postureImpact='heavy'` + **`postureMult: 1.5`** | 社長仕様「**高い体勢値削り**」。実装は引数を渡しておらず**体勢削りゼロ**(既定グレネードと同じ)だった。パイルドライバーの `'heavy'` を基準に、それより高い意味で 1.5 |
| **金の石の見え方** | **敵の頭上に金の点を段数ぶん(1〜3個)**。pooled Graphics でよい(専用素材は不要) | 「どの敵に石が付いたか・何段か・いま離すと起爆するか」を**知る手段が1つも無かった**。CLAUDE.md「小さくて見えない=演出が存在しないのと同じ」 |
| **シグナルのレティクルは吸い付かない** | **PHILL の頭スナップ(46px)をシグナルには適用しない**(生の照準のまま) | 社長仕様「追尾しないため**敵の移動先を予測して置く**必要がある」。吸い付くと**進行方向の少し先に置けない**=武器の芯が消える |
| **ロケランの方向と位置は「発射(溜め終わり)時点」で確定** | 溜め中は**弾頭が自機に追従**し、**溜め終わりに現在の自機中心と現在の最寄り敵の方向**で撃ち出す | 社長仕様「**発射すると**その時点でターゲティングしていた方向へ一直線」。実装は**引き金時**に固定で、溜め中に動くと**弾頭が自機から離れた地点に残っていた**(§16-2「溜め中の自機前方に小さい判定」とも不一致) |
| **ロケランの寿命は溜め終わりから数える** | 溜め終わりに `createdAt` を打ち直す | 溜めは `gameTime`・寿命は `Date.now()` で、**スロー(最小0.18倍)中は500msの溜めが実時間2.8秒**になり**発射前に弾が消えていた**。カウンター成功→スローは頻出。副次的に「寿命のうち溜めが飛距離を食う」問題も消える |
| **ロケランの発射に加速** | 溜め終わりから **150ms を ease-in** で 0→本来速度 | 0→460px/s の瞬間加速は**慣性MUST違反** |
| **splash キルでも強個体の武器クレートを出す** | シグナル / ロケランの壁爆発 / 錬金の破裂 の3経路に、直撃キルと同じ `isPumpkinTier\|\|giantbat → weapon-crate + 青リング` を足す | **シグナルは全キルが splash** なので、この武器で強個体を倒すと**必ず**クレートが出ない。「同じ動作を持つ全員に付ける」 |
| **手動系はPC入力にも配線する**(#U24を(a)で確定) | `inputActions.ts` の `performTapAction` に **`fireSignalLauncher` / `fireRailgunShot` / `detonateAlchemyStones`** を足す | **PCではシグナルが1発も撃てず(オート無し)、錬金砲の石が永久に起爆しない**。**製品はタッチなので影響は無いが、開発チャネルでの確認が回らない**ので直す |
| **石は `autoSwitchIfDry` の持ち替えでも消す** | クリアを `setActiveWeapon` だけでなく自動切替経路にも通す | 弾切れで自動切替された時だけ石が残っていた(受け入れ条件5の抜け) |

### 16-5b. ★ガンブレードの近接は「弾」ではない(2026-09-07・C-2検収A-2で確定)
**§16-2 の「銃の射撃経路から近接の判定を呼ぶ配線が要る」を、文言どおりに実装する。**
上がってきた実装は**近接モードでも `weaponType:'handgun'` の通常の弾を作っていた**ため、次が起きていた:
| 起きていたこと | なぜ問題か |
|---|---|
| **マガジンを1消費し、リロード中(2600ms)は至近で何も出ない** | 仕様に弾薬の記述は無い。**刃の一振りが弾を食う。**リロード中に至近で完全に無力になるのを社長は知らない |
| **外すと1400ms飛び続け、最大1180px先の別の敵に当たる** | ×1.6+heavy体勢削り+ノックバック×1.5 の「近接攻撃」が**画面の反対側で命中する** |
| **幻影の弾カウンターに打ち返される** | 「斬撃を弾として弾かれる」 |
| SEが `handgun-fire`・絵も通常弾 | **近接に切り替わったことをプレイヤーが知覚する経路が1本も無い**(★実在確認の掟) |
**⇒ 決めた形**: **近接は弾を作らない。**`fireWeapon` から**即時の近接判定**(`MELEE_RADIUS` 系)を直接呼び、
**自前の400ms間隔で発火**する。**弾薬もリロードも消費しない**(弾を作らないので自然にそうなる。
「刃を振るのに弾が要る」は仕様のどこにも無く、弾で実装した副作用でしかない)。
**命中点に斬撃の絵とSEを出す口を用意する**(絵は後日支給。**口が無いと素材が来ても出せない**)。
数値(×1.6・間隔400ms・`heavy` 0.10・ノックバック×1.5)は **§17-5 のまま変えない**。

### 16-6. ★未決(**銃側は全て裁定済み**)
- **★宿題 #U25「爆発系スキルを、錬金砲とシグナルにも乗せるか」**(2026-09-07・バッチD検収B-1)
  **社長の裁定が要ります(仕様に及ぶため)。**
  - **事実**: `skillExplosionMult` / `heavyGunnerExplosionMult` / `registerMultiHit` / ボマー散布は
    **既定グレネードとロケラン(敵ヒット側)にだけ**在り、**錬金の破裂・錬金の起爆・シグナルの空爆・
    ロケランの壁ヒット**には乗っていない。
  - 社長仕様は錬金砲に「**爆発系スキルとの強シナジーは許容**」と書いてあるので、**乗せる方が仕様に近い**。
  - **(a) 4経路すべてに乗せる**(=「爆発は爆発」で揃える)。**推薦**。
  - (b) 錬金砲だけ乗せる(社長が名指ししているのは錬金砲だけ)。
  - (c) 現状のまま。
  - **裁定まで現状維持。**

- **★宿題 #U24「手動照準の銃が、PC入力(キーボード/マウス)では撃てない」**(2026-09-07・レールガン実装で発覚)
  - **事実**: `inputActions.ts` の `performTapAction` は **`key === 'phill-revolver'` のハードゲート**で
    PHILL銃だけが手動発砲する。**シグナルランチャーもレールガンも、PC入力の経路に配線が無い**
    (タッチ操作でしか手動射撃できない。オートは動く)。**シグナルはオート専用が無いので、
    PC入力では実質撃てない武器になる。**
  - これは**レールガンが作った穴ではなく、シグナル実装時からの既存の欠落**
    (レールガンは指示どおり「シグナルと同じ形」に揃えた結果、同じ穴を踏襲した)。
  - ★**(a)で確定した(2026-09-07・§16-5c)**——`performTapAction` に3つとも配線する。
    実機はタッチなので**製品への影響は無い**が、**PCでは錬金砲の石が永久に起爆せず、
    シグナルは1発も撃てない**(オートを持たないため)ので、開発チャネルでの確認が回らない。
    **社長の裁定を待つ性質のものではない**(仕様の変更ではなく、入力経路の配線漏れ)と判断した。
  - ★**配布方針(社長決定v0.25.2549)は「最終形=アプリ配布」なので、製品としては(b)でも成立する。**
    ただし**開発チャネル(GitHub Pages)での確認がPCだとできない**のは段取りの損。

- **★宿題 #U23「ガンブレードの近接一撃に、銃のスキルをどこまで乗せるか」**(2026-09-07・C-2検収2巡目)
  **社長の裁定が要ります(仕様に及ぶため)。**
  - **事実**: 近接は弾を作らなくなった(§16-5b)ので、**銃弾の着弾処理にぶら下がっていた倍率・
    付与スキルがこの一撃に乗らない**。実装は `素ダメージ × クリ倍率 × skillOutgoingDamageMult` の簡略式。
  | 乗らない/違うもの | 影響 |
  |---|---|
  | **ボスへのクリ倍率が `BOSS_CRIT_DAMAGE_MULT=5` ではなく常に 1.5** | **ボスへのクリ近接が銃の1/3.3** |
  | **紫(体勢崩れ)中のクリ抑止が無い** | `applyBrokenGunReward` **と** クリ×1.5 の**二重取り** |
  | クリ率の各項(`softCapCritChance` / マークスマン罠 / 弱点 / `critDecayOnHit` / M0封印) | 銃と別のクリ率になる |
  | **スナイパー倍率 / コンボマスター倍率** | この2スキルがガンブレード近接に無効 |
  | **弾付与スキル全部**(延焼 / 凍傷 / 跳弾 / エコー / ファイアシューター / ラストマガジンLv3の延焼) | 近接には乗らない |
  | `recordCritHit` | クリ計測に載らない |
  - **(a) 近接は「近接」として扱う**(=既存の近接攻撃の倍率経路に合わせる)。**推薦**——
    ガンブレードの至近は**斬撃**であって銃撃ではないので、弾付与スキルが乗らないのはむしろ自然。
    ただし**ボスクリ ×5 と紫の二重取りだけは直す**(この2つは「銃か近接か」ではなく**バグ**)。
  - (b) 銃弾の倍率スタックを丸ごと近接にも通す(実装が重く、「弾付与スキルが近接に乗る」という
    別の不自然さが出る)。
  - (c) 現状のまま(簡略式)を裁定として明記する。
  - **裁定まで現状維持**。★**ただし「ボスクリ ×5→×1.5」と「紫の二重取り」の2つは、
    (a)(b)(c)のどれを選んでも直す対象**なので、次の機会に直す前提で積む。

- **★宿題 #U22「氷槍の床は、弾が飛んでいる間もダメージを出すか」**(2026-09-07・C-1検収2巡目)
  **社長の裁定が要ります(仕様に及ぶため)。**
  - **事実**: 現状の実装は**弾が消えて床が凍結してからパルスを出す**。弾は 1350px/s × 1.4秒 = **1890px**
    飛ぶので、**壁も敵も無ければ床の1発目は発射から1.4秒後**になる。
  - 社長仕様(§16-2)は「幅28px / 1.2秒 / 200msごとに直撃の20%」だけで、**伸長中を無効とは書いていない**。
    「弾より先に当たらない」(検収1巡目A-1)は満たしているが、**弾の後ろの床が飛翔中ずっと無効**なのが
    意図かどうかは決まっていない。
  - **(a) 現状のまま**(凍結してから1.2秒)。**推薦**——「弾の跡に床が残る」という言葉に素直で、
    近距離(すぐ当たる)ほど床が早く効く=間合いの意味が出る。
  - (b) 伸長中も「根元→今の先端」の範囲で200msごとに出す(発射時刻起点で数える)。床の総ダメージが増える。
  - **裁定まで現状維持**(=(a)のまま)。

- **★宿題 #U21「氷槍の床が、直撃した同じ敵にも毎射乗る(完全上位互換の芽)」**(2026-09-07・C-1検収監査で発見)
  **社長の裁定が要ります(仕様に及ぶため)。**
  - **事実**: 貫通は「2体目が居る時だけ」の上振れだが、**床は直撃した同じ敵に毎射乗る**——
    **敵は射線(=プレイヤー方向)へ直進してくるので、床の上を歩く**ため。
    1射あたり **58 → 最大 58 + 12×6 = 130(+125%)**。
  - **⇒ 既定 rifle-t2 に対して劣る軸が1つも無くなる**(=不変条件6「支配の禁止」の趣旨に反する)。
    式(§5-2)では直撃だけを数えて +0.2% としているので、**帯のテストは通ってしまう**。
  - **(a) 直撃した敵は床から除外する**(床は「射線に入った**別の**敵」専用)。**推薦**——
    武器の芯「弾の射線に床を残す」はそのまま残り、上位互換にはならない。
  - (b) 床のぶんをサイクル式に入れて直撃ダメージを下げる(芯は残るが数値が痩せる)。
  - (c) 現状のまま(上位互換を許す)を裁定として明記する。
  - **裁定まで現状維持**(片側へ倒さない)。

- **★宿題 #U20「`rangeOverride` がズーム補正を受けないので、引くと射程が逆転する」**(2026-09-06・バッチB検収で発見)
  **社長の裁定が要ります(仕様に及ぶため)。**
  - **事実**: `gunEffectiveRangePx`(`weaponUtils.ts`)は **`rangeOverride` を持つ武器だけズーム補正を通していない**。
    補正は「画面上の射程を一定に保つ」ためのもの(社長指示v0.25.3170)で、既定武器はカメラが引くと
    ワールド射程が伸びる。**ユニークは伸びない。**
  - **最大引き(`ZOOM_MIN_ABS=0.40`・巨大ボス遠距離)での実際の比較**:
    | 武器 | 射程 | 同カテゴリの既定 | 引いた時 |
    |---|---|---|---|
    | 制圧型SG | 250(固定) | shotgun 140 → **350** | ★**既定より短い** |
    | ボルトアクション | 320(固定) | rifle 250 → **625** | ★**既定の半分** |
    | 大型狙撃銃 | 250〜400(固定) | rifle 250 → **625** | ★**既定より短い** |
    | デザートテック | 200(固定) | rifle 250 → **625** | ★**既定の1/3** |
    | デュアルレンジ | 220(固定) | handgun 170 → **425** | ★**既定より短い** |
    | パイルドライバー | 108(固定) | handgun 170 → 425 | **これは正しい**(極端な短射程が芯) |
  - **⇒ 「長射程」を売りにした4挺が、ボス戦(=一番引く場面)で既定より短くなる。** 売りが反転する。
  - **これは私(設計)の穴**: §16-5 受け入れ条件3の「**ズーム補正には通さない**」は
    **パイルドライバー(短射程が芯)から出た規則**で、長射程の武器に当てると逆を向く。
  - **推薦(案A)**: **`rangeOverride` もズーム補正に通す。ただし「射程が近接判定に紐づく武器」だけ通さない**
    (パイルドライバーの108は `MELEE_RADIUS(74) + ハンティングLv3(34)` の導出値=**ワールドの量**であって
    画面の量ではない。だから補正しないのが正しい)。**判定はキー直書きにせず、
    `rangeFollowsMelee?: true` のような意味のあるフラグで持つ。**
  - (案B) 現状維持=長射程ユニークは引くと不利、という仕様として受け入れる。
  - (案C) 全部の `rangeOverride` を補正する(パイルドライバーが引くと270pxまで伸び、
    「極端な短射程」が壊れるので**私は推さない**)。
  - **裁定が出るまで現状維持**(片側へ倒さない)。**バッチB着地時点の実装は案Bのまま。**

- ~~**#U17「シグナルランチャーのオート射撃」**~~ → **裁定: オート無し**(社長2026-09-06)。
  **PHILL銃と同じ手動専用**。⇒ §16-3b「手動専用銃の掟」が前提工事に加わった。
- ~~**#U15「アイレーザーの再ターゲット」**~~ → **裁定: 再ターゲット無し**(社長2026-09-06)。
  対象が死んだら**その場で照射を終え、残り時間は失われる**(次の敵へ移らない/空へ撃ち続けない)。
- ~~**#U3 の残り6挺の解放元**~~ → **解決**(社長の割当表2026-09-06・§18-1)。**21挺すべての入手経路が
  ボス18 / 店売り3 で確定**したので、受け入れ条件4は各バッチで表に足すだけになった。

## §17 §16への着手前監査の反映(Fable 5.1・2026-09-06)

**(A)9件 / (B)5件 / (C)4件。** 数値そのものは全15行が `CATALOG` から再計算して一致していた。
下は**指摘ごとの結論**。★が付いたものだけ社長の裁定が要る。

### 17-1. 数値を直した(支配テスト・監査A-2)
`weaponSlot.test.ts` の**支配テスト(不変条件6)はハンドガン限定ではなく全候補を自動評価する**ため、
**収束型SGと切替式SGは全軸で既定を上回っていて落ちる**(初期散り角の広さは軸に無い)。
**劣る軸を1つ作って非支配にした**(帯は維持):
- **収束型SG**: `6/700/5/**2**/900` = **18.75(+5.2%)**。**装弾数2**(既定3より少ない)で劣る軸を作る。
- **切替式SG**: `6/**1000**/5/3/1000` = **18.00(+1.0%)**。**連射間隔1000**(既定950より遅い)で劣る軸を作る。
  **装弾数3は社長仕様なので動かさない。** スラッグ側も同じ周期(30/1000/1発/3発)で18.00。

### 17-2. `CATALOG` に書く値を補った(監査A-5)
5つしか書いていなかったので、実装者が既定値で埋めてしまう穴があった。**全15挺に明記する**:
- **critChance**: **ガンブレードは `0.05` を明示**(導出だと 0.16 になる。`handgun-t3` が 0.05 固定なのは
  「連射×大容量で16%はバランスブレイカー」というコメント付きの前例で、**110ms連射で16%は同じ事故の再現**)。
  他は導出のまま。
- **passthrough / pierce**(ライフル6挺): **既定と同じ貫通クラスに揃える**——
  T1(デザートテック/ボルトアクション)= `passthrough: true, pierce: 1`、
  T2/T3(大型狙撃銃/氷槍/レールガン/アイレーザー)= `passthrough: true`。
  **同スロットで貫通あり/なしを混ぜない**(§5・#U6)。
- **projectileSpeed / projectileSize**: 無指定は 520/8 に落ちるので全挺に書く。
  大型狙撃銃 **1000/8**(既定スナイパー同等)/ ボルトアクション **1100/9** / デザートテック **650/8** /
  氷槍 **900/9** / レールガン **1400/8** / クロスボウ **560/8** / デュアルレンジ **540/8**(近)・**700/8**(遠) /
  ガンブレード **560/7** / 収束型・切替式・コイル・誘導散弾・制圧型 **470/7**。
- **アイレーザーの弾薬**: **装填6・1照射で1消費・照射終了時にリロード開始・リロード2500ms**。

### 17-3. 無限弾(クロスボウ)の書き方を確定した(監査A-3)
**リザーブは読むだけでなく「書き戻し」が3箇所ある**(`tickReload` / クイックマガジン / オーバークロック覚醒)。
`ammoPoolFor` にだけ Infinity を仕込むと **`autoSwitchIfDry` がリロードを開始 → 生フィールド0で moved=0 →
永久リロードで1発も撃てない**。逆に3箇所へ Infinity を渡すと **`player.ammoHandgun` に Infinity が書かれる**
(HUD・商人・弾拾いが全部壊れる)。
⇒ **規則: フィールドへ `Infinity` を書かない。** 装填は **`refillWeaponMagazine(w, p, Infinity)` の戻り値のうち
`weapon` だけを使い、`reserve` の書き戻しをスキップする**(守護霊のリザーブ非消費と同じ形)。
**HUDの残弾は「∞」表示。** 棚卸しの対象に `weaponUtils.ts` のゴーストシューター覚醒の書き戻しも入れる。

### 17-4. 装填数アップの扱い(監査A-4)
`effectiveMagSize = magSize + magBonus`(レベルアップ「装填数アップ」は最大5回)。
**クロスボウ(装填1)は1回取るだけで2発になり「1発ごとに装填」という芯が消える**(上限まで取ると帯が+160%)。
⇒ **規則: `magSize <= 2` の武器は `magBonus` を受けない**(`effectiveMagSize` に除外の口を作る)。
**デリンジャー(装填2)も同じ穴を既に踏んでいる**ので、この規則で一緒に塞がる(事実として併記)。

### 17-5. ガンブレードの代償を作った(監査A-6・**ゴール直撃だった**)
旧案は至近で **×3・218DPS**(パイルドライバーの2.7倍)で、**劣る局面が1つも無い=完全上位互換**だった。
⇒ **至近モードは ×1.6・間隔400ms = 32DPS** にする。**遠距離の38.10より低い**ので
「近づかれると火力は落ちるが、押し返して体勢を崩せる」という**明確な代償**になる。
体勢削り `heavy`(0.10)・ノックバック×1.5 は据え置き。**近接モード中は銃を撃たない。**

### 17-6. 弾を撃たない武器の扱い(監査A-8)
**火炎放射器とアイレーザーは弾を作らない**が、守護霊(`buildGhostGunShots`)・幻影・ボットは
**無条件に弾を作る**ので、**火炎放射器を写した守護霊が「5ダメージのショットガン弾」を撃つ**。
⇒ **規則: 非投射武器は3経路とも「撃たない」**(等価実装はしない)。守護霊/幻影/ボットがこの武器を
持っていたら**射撃をスキップする**。

### 17-7. 切替式のモード反転(監査A-9)
「リロード完了」のフックは**3箇所**(ハンドキャノンの減衰リセットと同じ場所)。
⇒ **ハンドキャノンの裁定を踏襲: 「装填が発生した時」に反転**(部分装填・クイックマガジン・
オーバークロック覚醒を含む)。3箇所すべてに置く。

### 17-8. 記述の是正(監査C)
- **C-1**: 「距離は毎フレーム出ている」は誤り。`nearestEnemyDistance` は **`fireWeapon` の中で、
  クールダウン・リロード・弾倉のゲートを通った後にしか計算されない**(=撃てる瞬間だけ)。
  デュアルレンジのヒステリシス判定は**撃つ瞬間に置く**(機能上は問題ない)。
- **C-2**: レールガンを「式に乗らない」に入れたのは誤り。**オート側は式に乗る(39.58 = −13.6%)**。
  除外の理由は **「社長仕様でオート時は既定より低め」= 不変条件1(下振れ不合格)の意図的な例外**。
  帯テストは**レールガンを明示的に除外リストへ入れる**(汎用ループのままだとここだけ落ちる)。
- **C-3**: デザートテックの弾薬代用は**クロスボウ以上の規模**(書き戻し3箇所を別カテゴリへ振り替える+
  HUD残弾・`autoSwitchIfDry`・弾ドロップの型選び)。**「小さい追加」から「新規」へ格上げし、バッチAから外す**。
- **C-4**: 前提工事の口は「値」ではなく **「武器+状態 → rad」を受ける形**にする(収束型の動的散り角が乗るため)。

### 17-9. 刻みを直した(C-3を受けて)
1. **バッチA(データ寄り4挺+前提工事)**: 制圧型SG / ボルトアクション / クロスボウ / デュアルレンジ
   + **散り角を「武器+状態→rad」で受ける形へ**。
2. **バッチB(状態を持つ4挺)**: 収束型SG / 切替式SG / 大型狙撃銃 / **デザートテック**(弾薬代用)。
3. **バッチC(新規7挺)**: ガンブレード / コイルSG / 誘導散弾SG / 氷槍 / 火炎放射器 / レールガン / アイレーザー。

### 17-10. ★社長の裁定が要るもの
- ~~**#U16「レールガンの手動射撃をどうするか」**~~ → **裁定済み(社長2026-09-06)**:
  **「PHILL銃の仕様に、オートの攻撃も単純に追加するだけ。刀と一閃の関係みたいに。(ただし残弾は共存)」**
  - **実装の形**: 指を離した時の処理は既に **PHILL射撃 → ホーミング → 一閃ダッシュ → ワイヤーアンカー** と
    **並んでいて、それぞれ装備で分岐する**(`VirtualJoystick.tsx` の release ハンドラ)。
    ⇒ **`key === 'phill-revolver'` のハードゲートを「手動照準を持つ武器か」の述語へ広げる**だけでよい。
    **監査A-7が言っていた「手動と自動の共存モデルが無い」は、この形なら問題にならない**
    ——**カウンター・ホーミング・一閃と既に同居している**ので、レールガンが1つ増えるだけ。
  - **オート射撃は何も足さない**: レールガンは `category: 'rifle'` なので、
    自動射撃の除外(`category !== 'phill'`)に**引っかからない=最初から自動で撃つ**。
  - **★「残弾は共存」の読み**: レールガンは**ライフル弾**を使い、PHILL銃の phill弾とは**別に持てる**
    (両方の残弾が並立する)。**手動射撃もオート射撃も同じライフル弾を消費する**。
    ※この読みで実装する。違っていれば一言で直せる(弾の口を1つ替えるだけ)。
  - **頭部命中の確定クリ**は PHILL の頭コリジョン(`weaponType === 'phill-bullet'`)に依存しているので、
    **レールガンの弾も同じ頭判定を通す**(弾種の扱いを合わせる)。
- ~~**#U3 の残り6挺の解放元**~~ → **解決**(社長の割当表2026-09-06・§18-1)。
- ~~**#U15「アイレーザーの再ターゲット」**~~ → **裁定: 再ターゲット無し**(社長2026-09-06・§16-6)。

**⇒ §17-10 の「社長の裁定が要るもの」は全て埋まった。銃18挺は発注できる状態。**
残る★未決は **#U19「金環の帰還」**(サブウェポン側)だけ。

### 17-11. (B)5件は積む
守護霊との武器状態の同期(パリティ台帳)/ 大型狙撃銃の可変射程の持たせ方(per-frame書き換えを避ける)/
多段ヒット系のヒットカウンタ(火炎・氷槍床・アイ・制圧型)/ 15挺ぶんの説明文が未記述 /
クロスボウ×既存スキルの常時化(ラストマガジン毎発・クイックマガジン常時・ランナー常時)は**観察点**。

## §18 解放元の表(社長指定2026-09-06)+ 実在確認

社長が19行の割当表を出した。**19行すべて実コードの敵に当たった**(2026-09-06・再調査)。

★**前回「特定できない」と報告した4件は私の探し方の誤り**(社長指摘「まって、ちゃんと調べて。
敵の名前にあるから」)。**搬送体/樹木管理員/衛生兵/軍隊 は「城ボス(`giantbat`)のステージ別の名前」**で、
正本は **`src/data/bossCutin.ts` の `CASTLE_BOSS_NAME_BY_STAGE`**。私は `ENEMY_DEATH_LABELS` と
`EnemyType` の一覧しか当てておらず、**「型が1つで名前がステージで割れる」というこのゲーム特有の形**を
見ていなかった(城ボスは全ステージ同じ `giantbat` 型)。**皮肉なことに、`BOSS_UNLOCK` のキーを
`type@stageId` 形式にしたのはまさにこの形のためで、仕組みは先に作ってあった。**

**教訓(ENGINEERING_NOTES行き)**: 敵の名前を探す時は `EnemyType` の一覧だけを見ない。
**`bossCutin.ts` の2台帳(`CASTLE_BOSS_NAME_BY_STAGE` / `NAMED_BOSS_CUTIN_NAME`)が表示名の正本**で、
片方は**ステージ別**。名前→型の対応はここから引く。

### 18-1. 確定した19行(全て型に当たった)
| 社長の指定 | 型(コード) | 武器 | キー |
|---|---|---|---|
| バス停(変異) | `bounty-ranged` | 大型狙撃銃 T2 | `rifle-t2-heavysniper` |
| 馬乗り(変異) | `bounty-melee` | パイルドライバー T3 | `handgun-t3-piledriver` |
| 鋏(変異) | `bounty-balance` | デリンジャー T1 | `handgun-t1-derringer` |
| 舞妓(変異) | `bounty-maiko` | 錬金砲 T2 | `glauncher-t2-alchemy` |
| **搬送体(変異)** | **`giantbat@stage-1`** | 高威力ハンドキャノン T2 | `handgun-t2-handcannon` |
| **樹木管理員(変異)** | **`giantbat@stage-3`** | 制圧型ショットガン T2 | `shotgun-t2-suppress` |
| **衛生兵(変異)** | **`giantbat@stage-4`** | デザートテック T1 | `rifle-t1-deserttech` |
| **軍隊(変異)** | **`giantbat@stage-5`** | ロケットランチャー T1 | `glauncher-t1-rocket` |
| グレン | `giantbat@stage-7` | シグナルランチャー T3 | `glauncher-t3-signal` |
| ミゲル | `miguel` | レールガン T3 | `rifle-t3-railgun` |
| ジブリル | `jibril` | 火炎放射器 T3 | `shotgun-t3-flamer` |
| ラフィ | `rafi` | 収束型ショットガン T1 | `shotgun-t1-focus` |
| 偶像 | `idol` | デュアルレンジピストル T2 | `handgun-t2-dualrange` |
| ウリ | `uri` | ガンブレード T3 | `handgun-t3-gunblade` |
| ヨルムンガルド | `jormungand` | コイルショットガン T2 | `shotgun-t2-coil` |
| フィル(変異体) | `phillboss` | 誘導散弾ショットガン T3 | `shotgun-t3-homing` |
| スカジ | `skadi` | 氷槍ライフル T2 | `rifle-t2-icelance` |
| ミーミル | `mimir` | アイレーザー T3 | `rifle-t3-eyelaser` |
| スリィエル | `suriel` | 金環 | (サブウェポン・銃スロットではない) |

- **賞金首4種**(バス停/馬乗り/鋏/舞妓)は `getsDramaticDeath` を通る=設計図の確定処理に到達する(確認済み)。
- **城ボス5行**は `isFinalBossKill` ガードの内側(グレン形態1の移行では解放されない)。
- **stage-2 / stage-6 の城ボス**は台帳に無い(stage-2=城ボス不在・永久ロック)ので割当も無い。整合。

### 18-2. `BOSS_UNLOCK` の書き換え(v0.25.4161で実施)
第1弾3挺の解放元を**仮置き(ミゲル/ジブリル/ラフィ)から本来の3体へ差し替えた**。
```
'bounty-balance':   'handgun-t1-derringer',    // 鋏(変異)
'giantbat@stage-1': 'handgun-t2-handcannon',   // 搬送体(変異)
'bounty-melee':     'handgun-t3-piledriver',   // 馬乗り(変異)
```
残り16行は**対応キーがまだ `CATALOG`/`SLOT_CANDIDATES` に無い**ので書かない(書くと不変条件4が落ちる)。
各バッチで武器を実装した回に、その行を足す。

**キー側の不変条件をテストに追加**(`weaponSlot.test.ts`): `@`付きキーは
`giantbat@<城ボスの居るステージ>`、`@`なしキーは `bossCutin` に名前のある型。
打ち間違えると「倒しても何も起きない」形で静かに壊れる(撃破処理は無いキーを黙って無視する)ため。

### 18-3. ランチャー3種は「作る」で確定(社長裁定2026-09-06)
社長: **「ランチャーもいます。ただし新しく3機のみって意味です。」**
⇒ **glauncher に横を入れる。ただし新規は3挺だけ**(T1/T2/T3 に1挺ずつ=**第2弾は無い**)。
2026-09-05 の「撤回」はこの裁定で取り消し(過去の裁定は事実として残す)。

- **キー**: `glauncher-t1-rocket`(ロケットランチャー・軍隊)/ `glauncher-t2-alchemy`(錬金砲・舞妓)/
  `glauncher-t3-signal`(シグナルランチャー・グレン)。台帳は §11-9。
- **数が閉じた**: ユニーク銃 **21挺** = handgun 6 + shotgun 6 + rifle 6 + **glauncher 3**。
  内訳は **ボス撃破18(§18-1の割当表)+ 店売り3**(クロスボウ / 切替式SG / ボルトアクション)。
  **どの武器も入手経路がちょうど1つ**(受け入れ条件4の排反・網羅を満たす)。
- **前提工事が復活**: グレネード系のキー直書き2箇所(`isGrenadeGunKey` / 弾の絵)を category 判定へ。
  詳細と「工事不要と分かった箇所」は **§16-3**。
- **実装は §16 の バッチD**(Cの後。手動照準の経路をレールガンで先に固めるため)。

## §19 金環(スリィエル報酬・サブウェポン)— 社長仕様2026-09-06

**社長仕様(原文の要点)**
- 金環を**2本同時に射出**。**最初のターゲットそのものへぶつけず、ターゲットから少し離れた位置へ2本を展開**。
- **金環本体には接触ダメージなし。**
- 所定位置へ到達すると、**それぞれが近くの敵を取得**し、**対象方向へレーザーを発射**。
- レーザーは**3秒間持続**。**発射後は追尾しない**——発射した瞬間の方向へ**射線固定**。
- 敵が射線から**移動すれば外れる**。**別の敵が射線へ入ればその敵にも当たる**。
- **1回あたりのダメージは控えめ。多段継続ダメージ型。**
- 2本が**同じ敵を狙うことも、別々の敵を狙うこともある**。
- 要約: **敵の周囲へ2基を展開し、そこから固定方向の3秒レーザーを撃つ一時砲台型**。
  移動イメージはドローンブーメランに近いが、**金環自体は攻撃せず、レーザーだけがダメージ源**。

### 19-1. ★これは「線分の持続判定」の1件目——アイレーザー・氷槍と同じ土台
**金環 / アイレーザー(`rifle-t3-eyelaser`) / 氷槍ライフル(`rifle-t2-icelance`) の3つは、
どれも「プレイヤー側の線分に沿った持続ダメージ」**という同じ形をしている。
**先にどれか1つでこの土台を作れば、残り2つはデータと見た目だけ**になる。

| | 線の出どころ | 長さ | 持続 | 追尾 |
|---|---|---|---|---|
| 金環 | 展開した金環2基 | 固定 | 3秒 | **しない**(射線固定) |
| アイレーザー | プレイヤー自身 | 固定 | 3秒 | **する**(§16・対象を追う) |
| 氷槍 | 弾の通った跡 | 弾の射線 | 1.2秒 | しない |

⇒ **土台 = 「始点・終点・半幅・寿命・パルス間隔・1パルスダメージ」を持つ持続線分の配列**と、
**パルスごとに `distToSegment` で敵を拾って当てる1関数**。追尾の有無は「毎パルス終点を更新するか」の差だけ。
**§16 バッチC の着手時に、この土台を最初に切り出す**(3件ぶんの新規コストが1件になる)。

- ★**本物の流用は `distToSegment`(`src/utils/geometry.ts`・依存ゼロの純幾何)だけ**。
  向きに関係なく使えるので「敵→プレイヤーの資産はそのまま使えない」の例外(§11-8の教訓の逆側)。
- ★**ミーミルの `stepLaserAim` は使わない**(金環は追尾しない)。アイレーザーだけが使う。

### 19-2. 数値(叩き台)
| 項目 | 値 | 根拠・注記 |
|---|---|---|
| 本数 | **2本** | 社長仕様 |
| 展開位置 | プレイヤー→ターゲットの向きに対し**直交へ ±70px**(ターゲット中心から) | 「ターゲットから少し離れた位置」。2本が対象を挟む形 |
| 展開時間 | **300ms**(加速→減速の慣性つき) | 慣性MUST。等速で出して瞬間停止しない |
| 対象取得 | 到達した瞬間に**各金環が独立に最寄りの敵**を取る | **同じ敵/別々の敵のどちらにもなる**という仕様が、これで自然に出る(場合分けを書かない) |
| レーザー | 長さ **420px** / 半幅 **7px** / **3000ms** | 射線固定。長いので「別の敵が射線へ入る」が起きる |
| パルス | **200msごと**(=同一敵への再ヒット間隔) | 3秒で**15パルス/本** |
| 1パルスダメージ | Lv1 **6** / Lv2 **8** / Lv3 **10** | 「1回あたり控えめ・多段継続」。理論最大=15×10×2本=**300**だが、射線固定なので実際は敵が抜ける |
| クールダウン | **9000ms**(全Lv共通) | ブーメラン(5000ms・全Lv共通)より長い。CDを全Lv共通にするのはブーメランと同じ作法 |
| 消滅 | レーザー終了後に**慣性つきでフェード**(統一型・PACING_PUZZLE §7-15) | **戻らない**(社長裁定2026-09-06「a」=消える)。展開→停止→照射→消滅の**一方通行** |
| 接触判定 | **無し** | 社長仕様(金環本体はダメージゼロ) |

**Lvで伸びるもの = 1パルスダメージだけ**(`subWeaponUpgradeNotes` の文面は「レーザーの威力が上がる」)。
持続3秒・本数2本・CDは社長仕様の骨格なので**Lvで動かさない**。

### 19-2b. 判定・時計・壁(★監査A2〜A6の是正。実装者に決めさせない)
| 項目 | 決めた形 | 理由 |
|---|---|---|
| **判定式** | `distToSegment(敵中心, a, b) <= 半幅7 + max(w,h)/2` | **敵中心だけに半幅7を当てると当たり帯が14px**になり、40〜60pxの雑魚が横切っても外れる=社長仕様「別の敵が射線へ入れば当たる」が実質起きない。既存の線判定(`angelBossTick.ts:2589`)も `+ pr` で体半径を足している |
| **対象取得の距離** | 同じ距離関数を使う(最寄り=中心距離ではなく上式) | 取得と命中で別の物差しを使わない |
| **時計** | **すべて `gameTime`**(展開300 / 照射3000 / パルス200 / CD9000) | `Date.now()` 基準だと**ボス撃破の時間停止やポーズ中にレーザーが勝手に満了する**(`updateProjectiles` は Date.now だが hitstop/ポーズは gameTime だけを止める) |
| **CDの書き込み** | 必ず `setActorSubWeaponCooldown` を通す | タイムキーパー/オーバークロック/対人トラップ無効化/計測を一手に持つ関数。直書きすると全部素通りする |
| **対象が居ない時** | **発動しない・CDも消費しない** | `pickTarget` が null を返す。CDだけ焼けるのは操作から予測できない損失 |
| **対象の最大距離** | **420px**(=レーザー長と同じ) | 無制限だと1500px先の敵を選んで**画面外へ展開する** |
| **展開点** | 発射時に**固定**(300msの間ターゲットを追わない) | 射線固定という武器の思想と揃える |
| **展開点が壁の中** | `resolveAabb` で押し戻す(壁に触れたらその場で止まる) | 研究所など屋内で ±70px が壁の中に落ちる |
| **レーザーと壁** | **最初の壁で線分そのものを短縮**する | 敵ごとに `segmentBlocked` で弾く形だと**絵は壁を貫いたまま**になる。線を切れば絵と判定が一致する(CLAUDE.md「赤いのに当たらない/赤くないのに当たる」の禁) |
| **除外** | **死神系**(`isReaperFamily && !isTerminalReaper`)**と死体** | `damageEnemy` 自身は死体しか弾かない。プレイヤー側の全パルス系が呼び出し側で除外している(`useGameLoop.ts:11881`) |
| **キル処理** | `playEnemyDeath()` + `dropEnemyXp(...)` を**自分で呼ぶ** | 呼ばないと**キルしても経験値が落ちない**(ブーメラン停止パルスと同型・`useGameLoop.ts:11893〜`) |
| **倍率・チャネル** | ブーメラン停止パルスと**同型**(`skillOutgoingDamageMult`/`growthAtkMult` を乗せる・`damageChannel='other'`・**クリなし**・`hateSource` はオーナー) | 「同じ動作を持つ全員に付ける」。金環だけ別扱いにしない |
| **0ベクトル** | 対象までの距離が閾値未満なら**展開方向をレーザー方向に使う** | 金環と敵がほぼ同位置だと方向が 0 → NaN |

### 19-3. 発動入口(★仕様に書かれていないので、既存に合わせた)
**ドローンブーメランと同じ「近接スイング相乗り」**(`fireDroneBoomerangOnSwing` の並び)にする。
理由: ①社長が「移動イメージはドローンブーメランに近い」と書いている ②サブウェポンの発動入口を
新設すると操作が1つ増える。**別の入口が望みなら1行で変えられる**(この段だけの差し替え)。

- ★**「最初のターゲット」は store に無い**(監査A2)。自動照準の対象を保持するフィールドは存在せず、
  `pickTarget`(`weaponUtils.ts:506`)は**module-local・非export**で発射時にその場で計算しているだけ。
  スイング入口が持っているのは `owner.facing` のみ。⇒ **`pickTarget` を純関数として export し
  (または pcx/pcy/enemies/gameTime を取る形へ切り出し)、スイング時にオーナー中心から呼ぶ。**
  null の時・最大距離は §19-2b のとおり。
- **守護霊(ghost-ally)は撃つ。** 入口は `triggerCounter` と `fireGhostMeleeSwingSubs` の**2箇所から
  同じヘルパを呼ぶ共有経路**なので、**後者の戻り値レコードへ金環を明示配線する**(固定レコード
  `{boomerang,flare,junk,clone,mine}` なので足さないと守護霊だけ撃たない)。
  ゴースト発動時は **`ownerGhost` の青白tint / CDは `ownerGhostId` の帳簿 / `hateSource='ghost'` /
  SEは呼び出し側が距離減衰で鳴らす**(既存サブと同じ4点の差分)。
- **幻影(phantom)は非対応。** `PHANTOM_SUPPORTED_SUBS`(`phantomTick.ts:157`)の白リストに**載せない**
  (このプロジェクトでは「載せた=実装した」の定義)。
- **CD明け通知は全サブ共通のブーメラン型**(CLAUDE.md v0.25.2155・**必須**)。
  `goldRingReadyFxAt` + カチッSE + 頭上マークが ~650ms 出て消える形を**同時に配線する**
  (武器ごとの個別配線なので、書かないと出荷時に抜ける)。マークは金環のテクスチャを使う。
- **刀モードでは発動しない**(共有入口が `subWeaponBlockedByKatana` を通り、`KATANA_ALLOWED_SUBWEAPONS`
  は空)。**それでよい**(他サブと同じ扱い)。
- **金環のキルでスローモーションを出さない**(CLAUDE.md のサブウェポン禁則)。

### 19-3b. 状態の置き場と後始末(★監査A8)
- **`goldRings: GoldRing[]` を store に置く。書き手は `useGameLoop` だけ**(CLAUDE.md「PixiJSは描くだけ」)。
- **`resetGame` で `[]` に戻す**(`projectiles`/`skadiIceMarkers`/`homingLocks` を空にしている塊へ足す)。
  **足さないと前ランの金環が次ランへ残る。**
- **pixi側の view Map は `droneBoomerangViews` と同型**で、消えた金環のスプライトを回収する。
- **プレイヤー死亡・勝利時も消す**(照射中に決着がついた時、動かない線が残らないように)。

### 19-4. 描画の制約(★性能)
- **金環とレーザーに強glowを付けない。** CLAUDE.md の実測: **強glow 1個 ≈ 2ms/フレーム = 予算の約12%**で、
  高いのは**glowが落とす投影影**(`syncLocalEventLighting` が光源1つごとに世界中を走査する)。
  **2基×最大3秒=常時2光源**は「予算の24%を常に払う」ことになる。**発光は焼いたテクスチャ/加算合成で出す**
  (glowの見た目そのものは無料ではなく、**投影影を落とす光源**が高い、が正しい読み)。
- **色は金色。** 敵の予告の色の文法(赤=カウンター可 / 紫=不可)は**敵の攻撃の話**なので、
  プレイヤー側のこの武器には適用しない。
- 線は**pooled sprite**で描く(per-frame `Graphics` の描き直しをしない)。
- ★**レーザーの出現にも慣性を入れる**(監査A10・CLAUDE.md の慣性MUST)。全幅で瞬間に出さず、
  **80〜120msで幅とαを立ち上げる**。**判定は開始と同時でよい**(立ち上がりは「派手さの絵」側)。
- **描画はアクター系の減光を掛けない層に置く**。アクターと同じ層に置くと `horizonActorAlpha` の
  地平線フェードで**画面上部で透ける**(CLAUDE.md「Y方向の5つの副作用」)。
  ★**実装は `effectLayer`(頭上マークと同じ=アクターより前)**。地平線フェードは掛からないので
  **意図は満たす**が、環と線がキャラの絵の上に乗る(検収監査B-5・記述を実態へ合わせた)。
- **HUDにこの配列を購読させない**(React再レンダリング規律)。サブのCD表示は
  `subWeaponCooldowns['gold-ring']` の**数値だけ**を読む。

### 19-5. 負荷スコア
**1/10。** パルスは200msごと=**毎秒5回×2本**。1回の中身は敵配列に `distToSegment` を掛けるだけ
(敵36体でも 360判定/秒)。描画は線2本+金環2基の pooled sprite。
**払う subsystem = simulation(微小)/ rendering(微小)。** 上限は**同時に出る金環2基**で固定
(CDが9秒なので重なって増えない)。**安全弁 = 強glowを使わないこと**(§19-4)。

### 19-6. 解放と価格(★監査A1で書き直した。**前の版は前提が偽だった**)
**スリィエル撃破 → 金環の設計図 → 開発施設に並ぶ → 購入 → 装備。** ★**銃ではないので200Gの棚ではない**
——**サブウェポンの既存価格(20/50/100G)に乗せる**(社長裁定2026-09-05「①a」)。

★**「サブウェポンの解放は別の台帳を使う」と書いたが、その台帳は存在しない。**
開発施設の陳列は **`SUB_WEAPON_KEYS` からキャラ固有・退役を除いた全部を無条件に並べる**
(`MissionSelect.tsx:2540`)。設計図台帳(`markWeaponBlueprint`)も撃破フック
(`gameStore.ts:3534` の `BOSS_UNLOCK`)も**銃キー専用**で、トーストも銃名を引く。
⇒ **`'gold-ring'` を `SUB_WEAPON_KEYS` に足した瞬間、初回起動から20Gで買える**=撃破解放が成立しない。

**作るもの(5つ)**
1. **サブ用の設計図台帳**: `SUB_BLUEPRINTS_KEY` + `markSubBlueprint()`(銃用と同じ形・`resetProgress` で消す)。
2. **`SUB_BOSS_UNLOCK: Partial<Record<EnemyType, SubWeaponKey>>`**(`suriel → 'gold-ring'`)。
   ★**銃の `BOSS_UNLOCK` には絶対に入れない**(あの表の値は `SLOT_CANDIDATES` のどれか=不変条件4。
   入れるとテストが落ちる)。
3. **撃破フックは銃と同じガードの内側**(`!isPracticeRun() && !BOSS_TEST_RUN`。スリィエルは城ボスでは
   ないので `isFinalBossKill` は不要だが、**銃側と同じ1箇所で分岐させる**=経路を2本にしない)。
4. **陳列フィルタ**(`MissionSelect.tsx:2540`)に「**台帳の対象キーは設計図が無ければ載せない**」を追加。
   ★**「設計図が無いサブは載せない」と書いてはいけない**——既存サブは台帳を持たないので**全部消える**。
   **`SUB_BOSS_UNLOCK` の値に含まれるキーだけをゲートする**(互換のため)。
5. **トーストの名前**はサブ名の表(`gameStore.ts:3358` の switch)から引く(銃の `weaponDisplayName` ではない)。

### 19-6b. 追加箇所チェックリスト(★監査B6。1つ落ちると静かに壊れる)
`SubWeaponKey` 共用体(`types/game.ts:1505`)/ `SUB_WEAPON_KEYS`(`campaign.ts:702`)/
`SUB_WEAPON_UPGRADE_NOTES`(Record なので型が強制する)/ **名前 switch(`gameStore.ts:3358`——
`default` があるので落としても型では気づけない)** / `pixiTextures.ts` のテクスチャ登録。

### 19-7. ★未決 → **無し(全て裁定済み)**
- ~~**#U19「金環の帰還」**~~ → **裁定: 消える**(社長2026-09-06)。**戻ってこない。**
  社長の要約「一時砲台型」のとおり。**消滅は慣性つきフェード**(統一型・PACING_PUZZLE §7-15)で、
  **等速で消したり瞬間に消したりしない**(慣性MUST)。
  ⇒ **ブーメランの `boomPhase` に 'return' 相当は要らない**(展開→停止→照射→消滅の一方通行)。
  ⇒ 安全消滅の保険(`DRONE_BOOM_SAFETY_MS` 相当)も**寿命が有限なので不要**
  ——展開300 + 照射3000 + フェードで必ず終わる。

### 19-8b. 監査の記録(検収・Fable 5.1・2026-09-07)
**(A)1件 / (B)7件。(A)は直した。**

**(A-1) 研究所スキン(屋外ラボ)でレーザーが壁を貫通していた。** `beamWalls`(`useGameLoop.ts`)が
`indoor` と「木の幹」の2分岐しか持たず、`labWallsInRegion`/`labPropsInRegion` を見ていなかった。
★**同じステージで弾は壁で止まり**(`grenadeWallsFor`)、**同じ武器の展開点の押し戻しも lab を見ている**
(`meleeWallsAround`)ので、**線だけ貫通すると武器の中で壁の扱いが3通りに割れていた**。
分岐の形と対象を弾側へ揃えて是正(pad だけレーザー長に合わせて広く取る)。
**(B-6) トーチも同時に是正**——弾は止まるのに線は止まらない、という同じ形の食い違いだったため
(1つの式で直る場所なので、巡を増やさずここで入れた)。

**(B) 積んだもの(この巡では直さない)**
1. **★B-1「当てる」が土台に入っていない**——`pickBeamHits`(拾う)までは共通化されたが、倍率・
   `damageChannel`・`hateSource`・除外・`playEnemyDeath`+`dropEnemyXp`・数字/バーストの約20行が
   金環のブロックに直書き。**このままだとアイレーザー/氷槍がコピーする**=「同じ動作を持つ全員に付ける」が
   崩れる入口。⇒ **バッチCの発注文に `applyBeamPulse()` への切り出しを受け入れ条件として入れる。**
2. **B-2 発射SEのトリガが無い**(ブーメランの `boomerangThrowFxAt` に相当するものが未配線)。
   守護霊側の `fireGhostSwingSubs` も金環を読んでいない。**音の素材が来ていないので、絵と一緒に配線する。**
3. **B-3 展開点が壁の反対側へ押し出された時**、そこから壁へ向く線は短縮されない
   (`shortenSegmentAtWalls` は `t0 > 0` のみ採用=「壁の外から当たる場合だけ」)。研究所で起きうる。
4. **B-4 対象取得の距離関数が判定式と別物**(取得=`aimEnemyDist2` / 命中=`distToSegment + max(w,h)/2`)。
   「中心だけを見ない」意図は満たすので実害は薄い。
5. **B-7 設計図トーストにアイコンが出ない**(`hasWeaponIcon` は銃キー専用の表)。素材の話。

**確認して問題無しだった主なもの**: 「別の敵が射線へ入れば当たる」/2本が同じ敵・別々の敵/接触ダメージ無し/
時計=gameTime/CDは `setActorSubWeaponCooldown` 経由/対象無し=発動もCDも無し/最大距離420/展開点固定/
0ベクトル/死神系・死体除外/キル処理/`resetGame`・死亡・勝利で消す/pixiのview Map回収/CD明け通知/
守護霊が撃つ/幻影は白リスト外/銃の `BOSS_UNLOCK` 不変/**陳列ゲートは `SUB_BOSS_UNLOCK` の値だけ=既存サブは消えない**/
練習ラン・ボステストのガードは銃と同じ if の内側/強glowなし/pooled sprite/HUDは `goldRings` を購読しない/
慣性あり/**パルスの単位に「ペレット化」の取り違えは無い**。

### 19-8. 監査の記録(着手前・Fable 5.1・2026-09-06)
**(A)10件 / (B)7件。(A)は全て本文へ反映した**(A1→§19-6 / A2・A7・A9→§19-3 / A3〜A6→§19-2b /
A8→§19-3b / A10→§19-4)。**社長の仕様(挙動)を変える指摘は1件も無かった**ので、社長へ戻す案件は無い。

**★一番重かったのは A1**——§19-6 が「サブウェポンの解放は**別の台帳を使う**」と書いていたが、
**その台帳は存在しなかった**。書いた私は「銃と同じ形が別に在るはず」と思い込んでいて、
**実コードを確かめずに「別の台帳」と書いた**。CLAUDE.md「★実在確認の掟」と
「方向を書く時は実物の関数を読んでから書く」の両方に抵触する。**この設計のまま出していたら、
金環は初回起動から20Gで買えて『スリィエル撃破解放』が成立しなかった。**

**流用の主張は3件とも検証された**: `distToSegment` = 流用可(ただし半径の足し方を決める必要=A3)/
`fireDroneBoomerangOnSwing` の入口 = 流用可(ただしA2・A7の補足が必須)/
`stepLaserAim` を「使わない」判断 = 正しい。

**(B)7件は積む**(この巡では直さない):
1. 土台(持続線分)を金環とバッチCのどちらが先に作るか——1行で決める(金環が先でも成立する)。
2. 展開点の固定/追従 → 本文で「固定」に決めたので**解消済み**。
3. 刀モードの排他 → 本文へ入れたので**解消済み**。
4. サブのスロー禁止 → 本文へ入れたので**解消済み**。
5. React再レンダリング → 本文へ入れたので**解消済み**。
6. 追加箇所チェックリスト → §19-6b として**本文化した**。
7. Y方向の描画層 → 本文へ入れたので**解消済み**。
※ 2〜7 は1行で済むものだったので、積まずにその場で入れた。**残る(B)は 1 だけ。**
