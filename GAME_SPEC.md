# ゲーム仕様書（zombie / ZOMBIE SURVIVAL）

> 最新版スナップショット。`v0.25.1226` 時点。正確なバランス値・カーブは各ソースが一次情報（本書はその索引と全体像）。
> 開発規約・性能予算は `CLAUDE.md`、変更履歴は `DEVELOPMENT_LOG.md` を参照。
> **この文書は設計時の姿です。現行仕様の正はコード+DEVELOPMENT_LOG。案件の状態は PROJECT_STATUS.md を見る。**

---

## 1. 概要 / 技術
- ジャンル: トップダウン HD-2D サバイバルアクション（見下ろし型・ヴァンサバ系の周回×ボス）。
- 構成: **React + Zustand（シミュレーション）＋ PixiJS（描画）**。
  - シミュレーションは `useGameLoop`（唯一のクロック/ライター）と `src/store/gameStore.ts`。描画は `src/pixi/`（読むだけ・状態は書かない）。
  - 当たり判定/世界データは `src/world/`（PixiJS 非依存）。判定は **AABB のみ（ピクセル判定なし）**。
- プラットフォーム: モバイルファースト（タッチ操作）。PC も可。
- フォント: **Orbitron 固定**（英数字・タイトル・Pixi文字・数字bitmapfont すべて）。

## 2. コアループ / 操作
- 周回しながら敵を倒し EXP を集めてレベルアップ→強化選択。ステージのフィナーレボス撃破で帰還（クリア）。
- 操作: 仮想ジョイスティック移動＋自動射撃（最寄り敵へ）。近接は「指離しカウンター」。
- **カウンター（指離し）**: タップ離しのタイミングで成立。敵弾を反射（`Counter!` 表示・青背景／SE／ヒットストップ→スロー）。
  反射弾で一撃死は **プラントのみ**（社長指示）。それ以外は通常反射ダメージ。
- **近接フィニッシュ（KILL）**: 気絶した敵を処刑（`Kill!` 表示・濃いワインレッド背景／kill SE）。スキル「死神」で範囲フィニッシュ。
- **レベルアップ**: まず「LEVEL UP」演出（時間スロー＋キャラ発光）→その後に強化選択メニュー。周辺の敵をノックバック。

## 3. プレイヤークラス（職業）
`src/data/campaign.ts: CHARACTER_CLASSES`。初期銃・専用サブスキル・固有スキル（自動付与）が異なる。
- **ヘビーガンナー**（warrior / ショットガン）
- **マークスマン**（mage / マグマグ＝マグナム・狙撃）
- **ストライカー**（rogue / ファイティングナイフ）
- **スカベンジャー**（necromancer / ハンドガン）

## 4. 武器（`src/utils/weaponUtils.ts: CATALOG`）
3系統×3ティア＋近接＋特殊。自動射撃でカテゴリの弾薬を消費。
- **ハンドガン系**（9mm・速射低威力）: ハンドガン T1 / 二丁ハンドガン T2 / マシンピストル T3（=サブマシンガン・連射）。
- **ショットガン系**（12g・1トリガー=1シェルで複数ペレット）: ショットガン T1(5) / ポンプ式 T2(6) / オートショット T3(7)。
- **ライフル/マグナム系**（.44・重い単発, passthrough/pierce）: マグナム T1(貫通1) / スナイパー T2 / グレネードランチャー T3(着弾爆発)。
- **近接**（弾薬不要・ティアでクリ率上昇）: ナイフ→ダガー→ファイティングナイフ→タクティカルナイフ→対変異体ナイフ。
- **PHILL-銃**（研究所専用リボルバー・手動照準）: 頭部命中=確定ヘッドショット、胴体=2倍ノックバック。

## 5. サブウェポン / スキル（強化訓練=ガチャ）
- **サブウェポン**（`SUB_WEAPON_KEYS`）: heavy-grenade / marksman-trap / striker-hunting / striker-quick-mag / dog / katana /
  decoy / shield / whip / alchemy / turret / shijin（四神）/ fire-knife / drone-boomerang / wire-anchor / homing / shadow-clone。
- **スキル**（`src/data/campaign.ts: SKILLS`・レア度 normal/rare/super）。装備は最大2。
  - 超レア: **死神**（近接フィニッシュ時に範囲フィニッシュ。ボスは即死せず×5）/ バーサーカー / スケーター。
  - レア/ノーマル多数（クリD上昇・ナイト・エクスプローダー・スナイパー・跳弾・ボマー・コンボマスター・ナイフマスター・弁慶・反射神経 …）。
- **ガチャ（開発施設の強化訓練）**: pity（天井）付き。レア度→スキル抽選。重複は返金、Lv反映。
  - **死神はガチャから出ない**。**「死神を倒すと習得」**（撃破でのみ解禁・習得コールアウト）。

## 6. 装備（`src/data/equipment.ts`）
- 部位: **体 / 腕 / アクセ**。各部位2系統×5段階＝通常装備（最大2ステ・デメリットなし）。
  - 体=防護(最大HP/KILL猶予) / 機動(移動/KILL猶予)、腕=火力(D/連射) / 取り回し(リロード/連射)、アクセ=クリ(クリ率/スクラップ) / 弾薬(ドロップ/スクラップ)。
- **特殊装備（武将セット）**: 部位ごと1種・3ステ・レア度非依存・5%出現（鎧/小手/兜）。3点でフル装備=立ち絵差し替え。
- レベルアップで入手。既装備の部位へ**別系統**を選ぶと「現装備→新装備」アイコンで YES/NO 確認。

## 7. 敵 / ボス（`EnemyType`）
- 雑魚: bat / skeleton / zombie / plant(定着・遠距離) / **ghost(=変異体・抱卵型: 周回して1秒ごとに緑卵=mineを撒く。最大20)** / lich / lab-zombie-1〜3。
- 中ボス級: **pumpkin（肥大型・ジャンプ着地攻撃・物資ドロップ）** / **giantbat（飛行型・ステージ終盤フィナーレ・ダッシュ/ジャンプ）**。
- **叫喚型（screamer / 変異体・抱卵型ではなく叫喚型）**: 5分以降・**同時1体**・CDで何度でも出現する**優先処理敵**。距離を保って移動し、出現3秒後→以降10秒間隔で**2秒の溜め→叫喚**。発動で**画面内の通常敵の移動速度・与ダメージを7秒間×1.2**（グローバル窓）。**溜め完了前に倒せば阻止**。直接火力は弱い（接触6・HP60）。
- **ハンター（hunter / 変異体・狩猟型）**: 第8節。**CD長めで何度でも**出現（出現回数の上限なし・再出現CD150〜240秒）。
- **死神（reaper）**: 深奥リスクで出現する「避けるべき存在」。通常個体は不倒、深奥チェイサーはボス級。撃破で死神スキル習得。
- **裏ボス（hidden boss・`isHiddenBoss`）**: **mimir（眼・血色紫）/ jormungand（蛇・藍）/ skadi（氷・蒼白）**。即死耐性・専用ハザード（スカジ氷塊/氷刃 等）。
  - カウンター弾被弾でプレイヤー反対側へワープ（フェードイン）。

## 8. ハンターシステム
`HUNTER_VISION_RANGE=720`（薄紫の視界サークルを表示）。状態機械（idle→spawn→detect→chase…）。
- **視界範囲内に入った時だけ**ジャンプ/ダッシュ攻撃を発動。
- **ジャンプ着地は視界サークル内にクランプ**（溜め中にプレイヤーが円外へ逃げても円の縁までしか飛ばない）。
- 出現アテンション（発見SE）。優勢/被弾履歴で増援等のロジック。

## 9. 進軍NPC（護衛）/ 拠点制圧
`updateSuppression`。屋外で機能。東西南北の拠点（`BASE_SITE_COUNT=4`）。
- 護衛NPCは**毎出撃ランダムな顔ぶれ**（Fisher-Yates・フェイザーはレア枠20%で1枠差し込み）。フェイザー=2丁拳銃（2発・合計2倍）。
- セリフ多数（出撃/囲まれ/救助/後退/拠点視認/並走/制圧/撃破/称賛…）。セリフは上半身バスト立ち絵＋同一の右フェード背景で表示。
- **NPC発砲音＝ハンドガン音流用＋距離減衰**（NPC↔プレイヤー距離、画面外=無音）。**その弾の被弾音も同じ距離減衰**（遠いNPCの攻撃は発砲も被弾も小さく）。

## 10. イベント / ステージ
- ステージ: stage-1〜7＋EX（ex1/ex2）。各ステージにメイン/サブ任務・ボス・テーマ（森/研究所/ラボ等）。
- イベント系: アリーナ（囲い）、救助ホールド、紅き夜（警告→本番・敵実質HP2倍）、武器庫、商人 等。
- ステージ2＝研究所スキン（屋内・暗幕＋可視化円・PHILL銃・カードキー）。

## 11. 経済 / 弾薬
- **スクラップ/ゴールド**: 撃破ドロップ・装備換金（tier1=1g〜tier5=5g・特殊=5g）。開発施設のガチャ/解放に使用。
- **弾薬**: カテゴリ別リザーブ＋装填。撃破時にドロップ率（スライダー＋装備/パッシブ加算）。研究所は固定配置中心。

## 12. オーディオ（`src/audio/audioManager.ts`）
- BGM（ステージ別・ダンス・タイトル）＋ SFX（Web Audio）。SFXは `?v=__APP_VERSION__` でキャッシュバスト（同名差し替えでも確実に反映）。
- 主なSE: 発砲（handgun/smg/shotgun/rifle/grenade-launcher）/ 命中（shot-damage）/ ヘッドショット / 斬撃（slash-damage）/
  カウンター / KILL（melee-finish）/ リロード / 被弾 / 撃破ゾンビ声 / 各サブ武器 等。`playSfx(key, gainMult?)` で距離減衰対応。
- マナーモード/音量（BGM/SE 個別）対応。

## 13. 演出（VFX）
- 被弾フラッシュ=本体スプライトを**白シルエットで加算**（暗い敵もくっきり）。被弾の背中側に**火の破裂（2コマ立ち絵）**（銃系統でサイズ可変）。
- **斬撃エフェクト=ピクセル5コマ**（streak＝線/burst＝中央バースト）。向きで左右反転、流れる演出。
- ダメージ数字=baked BitmapFont（軽量）。コールアウト（Counter!/Kill!/LEVEL UP! 等）。

## 14. UI / UX（FF7リメイク風テーマ・紫）
- 開始前UI（タイトル/ミッション選択/キャラ選択/装備/資料/オプション/開発施設）＋ゲーム内UIを**紫トンマナで統一**。
- ルール: **実線の枠は残さない＝右フェード/両サイドフェード**で表現。パネルは「左不透明→右透明」or「両端→透明」のグラデ。
  ボタンは斜め無し・フェード背景＋紫アクセント。HUDの文字背景も枠なしフェード（中央=両サイド/それ以外=右）。
- HPはオーブ（紫グラデ・上明→下濃）＋EXPリング（細め・右ほど薄い）。武器スロットは紫・細枠。

## 15. 性能予算（要点 / 詳細は CLAUDE.md）
- ボトルネックは**エフェクトの描画方式×同時数**（敵/弾は安い）。重い順: Text > per-frame Graphics(glow/ring/particle/slash) >
  大アルファ画像 > 加算ライト。対策は**プールsprite/bitmap/ベイクtexture**化（敵/弾を減らす・bloomを切るではない）。

## 16. 開発/テスト用ノブ（URL）
- `?renderer=canvas` … 旧Canvas2D（保守外・参照用）。既定はPixiJS。
- `?font=orbitron` … フォント（既定Orbitron固定）。
- `?speed=N` … **ゲームスピード（テンポ）**。既定1.2。プレイヤー/敵の移動＋敵攻撃の溜め/CDをまとめて倍率（0.2〜5）。
  読込時に1回反映・`localStorage` 保存（`?speed=1.2` で既定へ）。
