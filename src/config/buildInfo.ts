// Human-readable build marker shown in the corner of the game screen.
// Bump this string on every change that is pushed for on-device testing, so
// you can confirm at a glance which build the phone/browser actually loaded
// (mobile Safari caches aggressively — a stale bundle looks like "not fixed").
export const BUILD_ID = 'b003 2026-06-05 stun-reticle-arc-fix';
