// 素材(public/ のスプライト/音声)のキャッシュバスト用バージョン。
//
// 重要: アプリ版(`__APP_VERSION__`)は毎 push で上がるため、素材URLのクエリに使うと
// 「コードだけの変更」でもブラウザが別URL扱いし、全スプライト/BGMを毎デプロイ再DLしてしまう
// (社長報告の「都度ダウンロード」)。素材は滅多に変わらないので、ここで専用の固定バージョンを持ち、
// **public/ の既存素材の“内容”を差し替えた時だけ**手で上げる。
// (新規ファイルの追加は別URL=別名なので、ここを上げる必要はない。)
export const ASSET_VERSION = '1';
