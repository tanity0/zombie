import type { JumpSplit } from './enemyJumpSheet';
import type { SweepSplit } from './enemySweepSheet';
import type { IdlePlayback } from './enemyIdleSheet';
// ★敵のアニメーションシートの台帳(**依存ゼロの葉モジュール**)。
//
// ★なぜ葉にするか(ENGINEERING_NOTES「循環importは…」): この表は
// `enemyWalkSheet`(歩き)・`enemyAttackSheet`(攻撃)・`batLanternSwing`(武器)・描画側 の
// **4方向から引かれる**。どれか1つに置くと、その1つを他が import し返して環になる
// (実際 v0.25.4537 で `enemyAttackSheet ⇄ batLanternSwing` の環を作ってしまった)。
// **表と名前だけをここへ置き、計算はそれぞれのモジュールが持つ。**
//
// ★素材を足す時はここへ1行。`pixiTextures` のロード登録も揃える。
// ※`JumpSplit` は **型だけ** を借りる(値を持ち込まないので葉のままでいられる)。
// ★**アスペクトは登録しない**(登録すると `enemyHitStrip`=当たり判定がシートの縦横比で動く)。

/** 歩きシートを持つ立ち絵(立ち絵のテクスチャ名 → コマ数)。シート名は `<立ち絵名>-walk`。 */
export const ENEMY_WALK_SHEETS: Readonly<Record<string, number>> = {
  // 社長支給2026-09-20。8コマ(1コマ 360×520→配信180×260)。
  'bat-female': 8,
  // 社長支給2026-09-21。9コマ(1コマ 97×130)。★**縮小していない**——支給時点で既に小さく、
  // 画面上の描画(高さ60〜90px)に対して1.4〜2.2倍しか余裕が無いため。常駐 0.43MB。
  // ★**足元は支給時点で揃っていた**(全コマ下端y=129・絵の中心が47.5〜48.5=1px以内)。
  'bat-male': 9,
  // 社長支給2026-09-21。9コマ(1コマ 118×130)。常駐 0.53MB。縮小していない。
  // ★**絵の中心は揃っていた**(58.5〜59.0=0.5px以内)。★**下端は 122〜128 と6px振れる**が、
  // これは**四つ足の体が伸び上がる動き**(接地しているのが前手→後足と移り変わる)。
  // 位置合わせはしていない——揃えると、せっかくの上下動を潰すことになる。
  'skeleton-male': 9,
  // 社長支給2026-09-21「skeleton女の子歩き」。9コマ(1コマ 115×130)。常駐 0.51MB。縮小していない。
  // ★**支給時点で揃っていた**(全コマ下端y=129・絵の中心が56.5〜57.0=0.5px以内)。
  // 送りは既定の前方ループ。継ぎ目の実測は**隣の平均の1.20倍**だが、**継ぎ目より大きい隣が在る**
  // (0→1が1.25倍)ので「継ぎ目だけが跳ねている」形ではない。男(0.77倍)と同じ四つ足の歩様で、
  // 折り返すと前進→後退に見えるため、ここは比だけで判断しない。
  'skeleton-female': 9,
  // 社長支給2026-09-21「ゾンビの歩行」。7コマ(1コマ 192×256)。常駐 1.31MB。
  // ★**支給は 2800×520(1コマ400×520)で、絵はきっちり2倍に引き伸ばされていた**
  //   (2×2ブロックの一致率 **100.0%**)。⇒ **最近傍で半分にすると1ビットも失われない**
  //   (色数65のまま。コウモリ女の時のような滑らかさは出ない)。原盤は `art-masters/` へ。
  // ★**透明な余白を切り落とした**(全コマ共通の矩形 x4-195 / y4-259 = 192×256)。
  //   理由: 立ち絵 `zombie-common` は絵が canvas いっぱい(464×640)だが、シートには左右12px・
  //   上12pxの余白があり、`containScale` は**余白ごと**枠に収める。
  //   ★**ゾンビの枠は正方形**(判定が36×36で `enemyFootBox` は boxW=幅×倍率 / boxH=高さ×倍率)。
  //   絵はどれも縦長なので**内接は必ず高さで決まる** ⇒ **描画高さの比 = 中身の高さ ÷ cellの高さ**。
  //   立ち絵=1.000 に対し、歩きの先頭コマは **切る前 0.954 / 切った後 0.969**
  //   (=歩き出しの縮みが **4.6% → 3.1%**)。残差は「シートの中身が cell の高さいっぱいではない」ぶん。
  //   切る矩形は全コマ共通なので**コマ間の位置関係は動かない**。
  //   ★**攻撃シートが来た時も同じ処理をすること**(片方だけ切ると歩き↔攻撃で体格が変わる)。
  // ★足元は支給時点で揃っていた(全コマ下端が cell の底・中心 94.5〜95.5=1px以内)。
  // ★送りは既定の前方ループ(継ぎ目は隣の平均の 0.85倍・継ぎ目より大きい隣が在る)。
  'zombie-common': 7,
  // 社長支給2026-09-21「雲歩き」(=**蜘蛛**歩き。`pumpkin`=蜘蛛の機械足の襤褸)。
  // 12コマ(支給 2100×130 → 透明余白を切って **160×120**)。常駐 0.88MB。
  // ★**切るのがここでは効く**: 立ち絵 `pumpkin-common` は**横長**(1024×768)で、枠が正方形なので
  //   **内接は幅で決まる**。シートは1コマ175px幅の中に絵が144〜160しか無く、そのまま出すと
  //   **最大17%小さくなる**。切ると一番広いコマが立ち絵とぴったり同じ大きさになる。
  // ★切る矩形は全コマ共通(x8-167 / y10-129)=コマ間の位置関係は動かない(和集合の中心 87.5)。
  // ★足元は支給時点で揃っていた(全コマ下端が cell の底・中心 87.0〜88.0=1px以内)。
  // ★送りは既定の前方ループ(継ぎ目は隣の平均の 1.05倍で、継ぎ目より大きい隣が在る。
  //   隣どうしの差も 11.1〜13.7 と**ほぼ一定**=途切れない歩様)。
  'pumpkin-common': 12,
  // 社長支給2026-09-21「自転車の歩き」(=`werewolf`。自転車に跨がる死体)。13コマ
  // (支給 1768×130 → 余白を切って **132×128**)。常駐 0.84MB。切る矩形は全コマ共通(x2-133 / y2-129)。
  // ★**絵は右向き**(ハンドルが右)。型の設定 `faceRight: true` と一致するので、下の表にも登録する。
  // ★送りは**前方ループ**。※継ぎ目の比は 1.44 と高く出たが、**この絵ではこの指標が効かない**
  //   ——**どのコマも先頭コマとの差が18〜23と横並び**(細かい質感で埋まっていて、脚の差が沈む)。
  //   ⇒ 指標ではなく**物理で決めた**: ペダルは一周する。折り返したら**逆回転**になる。
  'werewolf-common': 13,
  // 社長支給2026-09-22「咆哮型の歩き」(=`screamer`。シルクハットに花束のスーツ姿)。16コマ
  // (支給 1600×130 → 余白を切って **96×128**)。常駐 0.75MB。切る矩形は全コマ共通(x2-97 / y2-129)。
  // ★脚は歩いているが**胴と顔はこちらを向く**3/4の絵で、立ち絵と同じ向き。
  //   一度「正面向き=ミラーしない」として登録したが、**社長指示2026-09-22「ミラーは全ての敵で適用します」で
  //   撤回**(v0.25.4566)。素の向きの印(`ENEMY_SHEET_FACES_RIGHT`)は付けない=既定の左向き扱い。
  // ★送りは前方ループ(継ぎ目の比 1.19・継ぎ目18.7より大きい隣が在る 20.2。歩様なので折り返さない)。
  // ★歩幅の倍率は**既定のまま**。実測 1.70回転/秒(コマ27/秒)で、型の設定 strideHz 2.0
  //   (痙攣風の小刻み)とほぼ一致する=自転車のような破綻は起きていない。
  'screamer-common': 16,
  // 社長支給2026-09-23「伐採人の歩き」(=`logger`。旧・死神の立ち絵を流用している型)。16コマ
  // (支給 2080×130 → **全幅で空の下2行だけ落として 130×128**。内容は1画素も削っていない)。
  // 常駐 **1.02MB**。★**縮小していない**——描画は 120×118 で、絵(130×128)に対して余裕が1.1倍しかない。
  // ★立ち絵(130×128・絵は上下いっぱい)と**同じ枠**。歩きの絵は上2行が空=立ち姿より2px低い
  //   (歩く時の沈み込み)。足元は**12/16コマが下端127**、残り4コマが126(踏み出しで浮く足)。
  // ★送りは**前方ループ**。継ぎ目(15→0)の実測は隣の平均の **2.67倍**(既存で最大)だが、
  //   **比だけで判断しない**(この表の掟): ①足元の接地位置に**周期のある歩様**が読める
  //   (重心 60→81→46→77 と8コマ周期=左右2歩で1周)ので、**折り返すと後ろ歩きになる**
  //   ②実寸で並べた画では輪郭が繋がっている(差が大きいのは金糸の高周波な模様)。
  //   ⇒ 実機で継ぎ目が跳ねて見えたら `ENEMY_WALK_PLAYBACK` へ1行足して折り返しへ切り替える。
  // ★歩調は **1.38周/秒(コマ22枚/秒)**(速度75px/s ÷ 歩幅54.4px)。叫喚型の1.70周/秒と同程度。
  'reaper-common': 16,
  // 社長支給2026-09-22「ハンターの歩き」(=棺桶を担ぐ巨人)。7コマ
  // (支給 1680×258 → 余白を切って **228×252**)。常駐 **1.53MB**(今までで一番大きい=ミニボス級の絵)。
  // ★**縮小していない**。2×2の一致率 45%=**引き伸ばしではない**ので、半分にすると絵が濁る。
  //   描画は 106×118 なので 2.1倍の余裕。切る矩形は全コマ共通(x6-233 / y6-257)。
  // ★**立ち絵名が `hunter`**(`-common` が付かない)。変種を持たない敵なので `ENEMY_VARIANT_SETS` に
  //   載っておらず、`enemyTexKey` の最後の `?? type` で解決される。**シート名は `hunter-walk`。**
  // ★立ち絵と並べて同じ向きであることを確認済み。一度「正面向き=ミラーしない」として登録したが、
  //   **社長指示2026-09-22「ミラーは全ての敵で適用します」で撤回**(v0.25.4566)。
  // ★送りは前方ループ(継ぎ目の比 0.90)。歩調は **0.73回転/秒(コマ5.1枚/秒)**——
  //   歩く速さが半分に落とされている型(41px/s・社長裁定v0.25.2429)なので、重い巨人の歩みとして遅い。
  'hunter': 7,
  // 社長支給2026-09-22「リッチの歩き」(=`lich`。鞭状の触腕を巻く亡者・ステージ4/5限定)。13コマ
  // (支給 1807×130 → 余白を切って **135×128**)。常駐 **0.86MB**。切る矩形は全コマ共通(x2-136 / y2-129)。
  // ★**縮小していない**。2×2の一致率 61%=引き伸ばしではない。描画は 90×85 で 1.5倍の余裕。
  // ★足元は支給時点で揃っていた(全コマ下端が cell の底)。中心も 68.5〜69.0(cell の中心 69.0)。
  // ★**左向き**=既定のまま(下の `ENEMY_SHEET_FACES_RIGHT` には載せない)。立ち絵と並べて同じ向きを確認済み。
  // ★送りは**前方ループ**(継ぎ目の比 1.06・継ぎ目より大きい隣が4つ在る。歩様なので折り返さない)。
  // ★★**歩様ではなく「滑走」**(社長指摘2026-09-22「**歩きというかスケート移動**」)。
  //   既定の歩幅で送ると 2.61周期/秒(コマ33.9枚/秒)= **毎秒5.2歩相当の全力疾走の脚**になる。
  //   滑走は1回の蹴りで長く伸びるので、**歩幅の倍率 2.5**(下の表)で **1.04周期/秒(コマ13.6枚/秒)**へ。
  //   ※比較: ハンター5.1 / ゾンビ8.5 / **リッチ13.6** / 蜘蛛18.4 / 自転車19.3 / 咆哮型27。
  'lich-common': 13,
  // 社長支給2026-09-26「ステージ2の研究員女の歩行」(=`lab-zombie-1` の女性の見た目)。16コマ
  // (支給 2112×130 → 上の空き2行だけ落として **132×128**)。常駐 **1.03MB**。
  // ★**縮小していない**(不透明ブロックの2×2一致率 4.2%)。色数32・半透明0%。
  // ★足元は支給時点で揃っていた(全コマ最下行)。背丈も立ち絵と同じ128=倍率合わせは不要
  //   (立ち絵へ重ねた倍率 0.98〜1.09・4コマ平均1.03)。
  // ★**左向き**=既定のまま(研究所ゾンビの素材は左向き基準・立ち絵と同じ向きを確認済み)。
  // ★送りは前方ループ(歩様)。男性の見た目(`lv1-male`)の歩きはまだ無い=男性は従来どおり立ち絵。
  'lab-zombie/lab-zombie-lv1-female': 16,
  // 社長支給2026-09-26「ステージ2の研究員男の歩行」(=`lab-zombie-1` の男性の見た目)。10コマ
  // (支給 1400×130 → 上の空き2行だけ落として **140×128**)。常駐 **0.68MB**(1.0MB未満=起動時のまま)。
  // ★**縮小していない**(不透明ブロックの2×2一致率 7.3%)。色数32・半透明0%。左向き=既定。
  // ★**10コマ目だけ足元が6px浮いていた**(下端123・他は129)。そのままだと継ぎ目で毎周小ジャンプに見える
  //   (社長報告2026-09-24のパンプキンと同じ見え方)ので、公開側のそのコマだけ6px下げて接地させた。原盤は無加工。
  // ★**絵が立ち絵より約1割小さい**——3通りで測って揃った: 全身の重ね 1.14 / 下半身の重ね 1.10 /
  //   頭と背中のランタンの模様合わせ 1.10。枠は立ち絵と同じ128なので、何もしないと歩き出した瞬間に
  //   1割縮んで見える。⇒ 下の `ENEMY_WALK_BODY_H` で 128/1.10 ≈ **116** を渡す。
  'lab-zombie/lab-zombie-lv1-male': 10,
  // 社長支給2026-09-26「研究所のレベル2の歩行と突進用」(=`lab-zombie-2`・男女の区別なし)。16コマ
  // (支給 1696×130 → 上の空き2行だけ落として **106×128**)。常駐 **0.83MB**(1.0MB未満=起動時のまま)。
  // ★縮小なし(2×2一致率 3.7%)・色数32・半透明0%・左向き(既定)。足元は全コマ最下行で揃っていた。
  // ★倍率: 立ち絵へ重ねて 1.03〜1.10(一番確かなコマ12=IoU 0.89 で 1.05)。枠の比の自動補正(131/128)を
  //   引いた残りは約2.6%=「1割を超えたら載せる」に届かないので `ENEMY_WALK_BODY_H` には載せない。
  // ★**突進も同じ絵**(社長「歩行と突進用」)。突進は3倍速なので、下の `ENEMY_WALK_DASH_GEAR` で
  //   自転車と同じ「突時は倍速」(社長指示2026-09-21)にそろえる。
  'lab-zombie/lab-zombie-lv2': 16,
  // 社長支給2026-09-26「レベル3の歩き」(=`lab-zombie-3`)。16コマ(支給 2384×152 → 上の空き2行だけ落として
  // **149×150**)。常駐 **1.43MB**(1.0MB超=下の常駐表に載せる)。縮小なし(2×2一致率 3.5%)・半透明0%・左向き(既定)。
  // ★足元は全コマ最下行で揃っていた。7コマ目は尾の先が枠の上端に触れている(切れてはいない)。
  // ★倍率: 立ち絵(176×200)へ重ねて 0.67〜0.73(中央値0.71)。枠の比は 150/200=0.75 なので、自動の枠合わせだと
  //   約5.6%大きく出る=「1割を超えたら載せる」に届かないので `ENEMY_WALK_BODY_H` には載せない。
  // ★送りは前方ループ(継ぎ目16→1の重なりは隣どうしの範囲内)。
  'lab-zombie/lab-zombie-lv3': 16,
  // ★グレン形態1の浮遊移動。**社長支給2026-09-26「浮遊差し替え」で高さ192版へ差し替え**(初版は256・6.57MB)。
  // 16コマ(支給 4816×194 → 上の空き2行だけ落として **301×192**)。常駐 **3.70MB**=遅延組(下の常駐表)。
  // ★縮小なし: 2×2一致率は33%と高めだが**位相を1pxずらしても変わらない**=引き伸ばしの跡ではなく、
  //   32色で塗った暗い外套の平らな面。半透明0%・左向き(立ち絵と同じ)。足元(外套の裾)は全コマ最下行。
  // ★倍率: 立ち絵(256×194)へ重ねて 0.93〜0.98(4コマ)。枠の比 192/194=0.99 との差は1〜6%=1割未満なので
  //   `ENEMY_WALK_BODY_H` には載せない。継ぎ目16→1の重なり 0.74(隣どうし 0.71〜0.86 の範囲内)=前方ループ。
  // ★**形態2(`glen-boss2`)には掛からない**(`pixiScene` が形態2を「手で描いたコマ無し」として扱う)。
  'glen-boss': 16,
  // 社長支給2026-09-22「**城ボス1 搬送隊の歩き**」(=ステージ1の城ボス。表示名「搬送体(変異)」・型は `giantbat`)。
  // ★**解像度を落として出し直した版**(v0.25.4580)。11コマ(支給 1441×130 → 余白を切って **127×128**)。
  //   常駐 **0.68MB**(初出の254×256=2.73MBの**1/4**)。**ドット/px 1.84 → 0.92**=1ドットも落ちない。
  //   足元(下端129)・中心(64.5〜65.0 / cell中心65.0)は支給時点で揃っていた。11コマ全部の高さが
  //   初出版の半分と一致(±1)。色数32・半透明0%。
  // (初出) 11コマ(支給 2838×258 → 余白を切って **254×256**)。常駐 2.73MB。切る矩形は全コマ共通(x2-255 / y2-257)。
  // ★**立ち絵の名前は `giantbat`** だが、**素材は `public/sprites/atlas-px2/giantbat.png`**
  //   (ステージ1セットのドット絵上書き。別名の台帳=`utils/atlasPxOverrides.ts`)。**シートは `sprites/giantbat-walk.png`**
  //   ——`spritePath` は `sprites/<名前>.png` しか見ないので、シートは `atlas-px2/` に置かない。
  // ★ステージ3/4/5の城ボスは `stage3-enemies/giantbat` 等に解決されるので**このシートは出ない**
  //   (社長支給はステージ1の絵。他ステージは別の絵が届いてから)。ステージ7はグレンの別絵。
  // ★**縮小していない**(2×2の一致率 63%)。描画は 138×140 で 1.8倍の余裕。足元は全コマ下端(257)。
  //   中心も 128.0〜129.0(cell の中心 128.5)。**左向き**=既定のまま(立ち絵と並べて確認済み)。
  // ★送りは**前方ループ**。※継ぎ目の比は **1.53** と高く、継ぎ目より大きい隣が**1つも無い**が、
  //   **この絵ではこの指標を採らない**——這うような歩様なので、折り返すと**後ろ歩き**になる
  //   (自転車のペダル 1.44 で同じ判断をした先例)。**物理で決めた。**
  'giantbat': 11,
  // 社長支給2026-09-24「城3ボス」(=**ステージ3の城ボス**。廃都の絡まった巨躯。型は `giantbat` だが
  //   立ち絵は `stage3-enemies/giantbat` へ解決される=**ステージ1/2の絵とは別物で、シートも別名**)。
  // ★★**差し替え版**(社長支給2026-09-25「城3の歩き」・v0.25.4637)。**脚の踏み替えが入った別の歩様**で、
  //   初出版(2026-09-24)とはコマの絵が全部違う。**初出版の原盤も `art-masters/` に残してある**
  //   (`stage3-giantbat-walk-16f-2944x152.png` / 差し替え版は `-2752x152.png`)。
  // 16コマ(支給 2752×152 → 全コマ共通の矩形 x2-169 / y2-151 で切って **168×150**)。常駐 **1.54MB**。
  // ★**高さ150 = 立ち絵(164×150)と同じ**。**縮小していない**(2×2の一致率 8.7%)。色数33・半透明0%。
  //   ※`public/` の寸法は 2880→2688 と**横だけ縮む**が、**絵の解像度は落ちていない**
  //    (支給の中身の高さはどちらも150。cellが細いのは一番広い姿勢が細いから)。台帳は更新済み。
  // ★足元は支給時点で**全16コマ下端が揃っていた**(中心も 84.5〜86.0 / cell の中心 85.5)。
  // ★**踏み替えが読める**——足元の幅が **131→93→50→49**(2,3で揃う)**→120→117→59→51→51**
  //   (6〜8でまた揃う)**→58→57→57→150**(12=一番開く)**→131→130→127**。左右の脚が交互に運ばれている。
  // ★送りは**前方ループ**。継ぎ目(15→0)は隣の平均の **1.21倍**で、継ぎ目より大きい隣は1組だけ
  //   ——**比だけで判断しない**(この表の掟): 歩様なので**折り返すと後ろ歩きになる**。**物理で決めた。**
  //   ※初出版は継ぎ目0.65倍・15組中13組が上と、閉じ方はそちらの方がきれいだった(事実として記録)。
  // ★★背丈のずれは **-8.9%(初出版) → -2.4%(この版)** へ縮んだが、**どちらも v0.25.4635 で解消済み**
  //   (`utils/sheetFit.ts` が立ち絵とコマの実寸から毎フレーム倍率を出して背丈を立ち絵へ合わせる)。
  'stage3-enemies/giantbat': 16,
  // ★城ボス4(衛生兵・雪原)の歩き(社長支給2026-09-25)。16コマ
  // (支給 2704×152 → 全コマ共通の矩形 x2-166 / y2-151 で切って **165×150**)。常駐 **1.51MB**。
  // ★**縮小していない**(2×2の一致率 5.4%)。色数33・半透明0%。足元は支給時点で揃っていた。
  // ★立ち絵は 197×200(本体184)。**描き込みは立ち絵の 0.73倍**(立ち絵へ重ねて実測)。
  //   枠が 150 なので背丈合わせは自動で 200/150=1.33倍して **0.973倍**=**2.7%だけ小さい**。
  //   歩きの表は `bodyH` を持てない(コマ数だけの表)ので**そのまま**。2.7%は目で追えない
  //   (跳び・攻撃は表に `bodyH` を書けるので 1.000 に合わせてある)。
  // ★送りは**前方ループ**(歩様。折り返すと後ろ歩きになる)。
  'stage4-enemies/giantbat': 16,
  // 城ボス5(社長支給2026-09-25「城5歩き」)。16コマ・1コマ170×150。足元は全コマ最下行で揃っている。
  //   立ち絵へ重ねた倍率は 1.300〜1.385(4コマ測って平均1.34)=枠の比 200/150=1.333 とほぼ同じ
  //   ⇒ 背丈合わせの自動補正だけで足りる(この表は `bodyH` を持てないが、持つ必要もない)。
  'stage5-enemies/giantbat': 16,
  // 社長支給2026-09-24「こちらが歩きです」(=`driller`。削岩機を担いだ死体)。16コマ
  // (支給 2320×130 → 全コマ共通の矩形 x2-142 / y2-129 で切って 141×128 → **左右へ透明を1pxずつ足して 143×128**)。
  // 常駐 **1.12MB**。★**縮小していない**(描画は 120×107 で、絵に対する余裕は1.2倍しかない)。
  // ★★**1px足したのは背丈を立ち絵に合わせるため**: 削岩型の枠は正方形で**内接は幅で決まる**ので、
  //   cellの幅がそのまま背の高さを決める。立ち絵は 143 幅——**切りっぱなしの141だと1.4%高く、
  //   支給のままの145だと1.4%低い**。143へ揃えて**差0%**にした(中心も cell の中心71.0に乗る)。
  // ★足元は支給時点で揃っていた(**全16コマの下端が cell の底**)。
  // ★**絵は右向き**(ドリルが右)。立ち絵も突きも右向きなので、下の `ENEMY_SHEET_FACES_RIGHT` に登録する。
  // ★送りは**前方ループ**。継ぎ目(15→0)は隣の平均の1.29倍だが、**比だけで判断しない**(この表の掟):
  //   足元の重心が 72→77 / 61→67 / 43→60 と**8コマ周期で2回踏み替える**歩様=折り返すと後ろ歩きになる。
  //   継ぎ目より大きい隣も2つ在る(2→3 と 10→11=踏み替えの瞬間)。
  // ★歩幅の倍率は**既定のまま**。実測 1.52周/秒(コマ24枚/秒)で、咆哮型27・伐採人22と同じ並び。
  'driller-common': 16,
  // 社長支給2026-09-24「死神の歩き」(=`reaper`。白い吊り女神「礼賛」。**立ち絵は `reaper2-common`**——
  // `reaper-common` は旧・死神の絵で、v0.25.4004 から**伐採人**が使っている別人)。16コマ
  // (支給 3424×258 → **全幅で空の上2行だけ落として 214×256**。内容は1画素も削っていない)。
  // ★**常駐 3.34MB=今までのシートで最大**(次点はハンターの1.53MB)。★**縮小していない**——
  //   描画は 146×174 で、立ち絵(347×512)が 2.94ドット/px なのに対し**この歩きは 1.47ドット/px**。
  //   半分にすると 0.74=引き伸ばしになるので落とせない。逆に立ち絵と同じ精度にするなら
  //   **高さ512で出し直し(常駐13.4MB)**が要る=社長裁定。
  // ★**背丈は立ち絵と一致する**(どちらも縦長で**内接が高さで決まる**ので、cellの幅は背丈に効かない)。
  //   体の比も実測で一致(横木の幅/高さ=立ち絵0.479 vs 歩き0.480、胴=0.514 vs 0.512)。
  // ★足元は支給時点で揃っていた(**全16コマの下端が cell の底**)。
  // ★**正面向き**(立ち絵と並べて確認済み)。`ENEMY_SHEET_FACES_RIGHT` には**載せない**=既定の左向き扱い。
  // ★★**これは歩様ではなく「浮遊の揺れ」**(社長指示2026-08-28「**歩く感じでは無い。浮いて移動する**」・
  //   型の設定も `kind: 'hover'`)。実測でも足元の重心が **16コマで1周の緩い揺れ**(108→101→109)で、
  //   歩行のような**踏み替えの折り返しが無い**。⇒ 歩幅の倍率を下の表で **2.5**(リッチの滑走と同じ値)。
  //   既定のままだと **1.63周/秒(コマ26枚/秒)**=咆哮型の小刻みと同じ速さになり、終端存在の静けさが出ない。
  //   2.5 で **0.65周/秒(コマ10枚/秒)**=既存で最も遅い。**実機で社長が詰める。**
  // ★送りは**前方ループ**。継ぎ目(15→0)は隣の平均の1.66倍と高いが、**比だけで判断しない**(この表の掟):
  //   **継ぎ目より大きい隣が2つ在り**(4→5 と 10→11=裾と吊り飾りが大きく振れるところ)、
  //   足元の重心の跳びも継ぎ目(+4.1)と隣の最大(-4.5)が同程度=閉じた1周期として読める。
  'reaper2-common': 16,
};

/**
 * ★★**歩幅の倍率**(既定 1 = `ENEMY_WALK_STRIDE_PER_HEIGHT` そのまま)。
 *
 * ★なぜ要るか: 歩幅は「絵の高さ × 0.46」の1本で決めているが、**自転車はこれが合わない**。
 * 実測——自転車の巡航速度 105px/s・枠の高さ 61.5px だと、既定の歩幅では
 * **1秒に3.7回転(コマ48枚/秒)**になり、**60fpsの画面で読めない**(1コマが1.2画面フレーム)。
 * 自転車は**ギアで進む**ので、脚の回転は距離に対して**ずっとゆっくり**でよい。
 * 倍率2.5 = 歩幅 70.7px で **1秒に1.5回転(コマ19枚/秒)**=実車の90rpm相当。
 *
 * ★**リッチも同じ理由**(社長指摘2026-09-22「**歩きというかスケート移動**」)。
 * 歩行なら1歩=1回の接地だが、**滑走は1回の蹴りで長く伸びる**ので、既定の歩幅では脚が回りすぎる
 * (既定だと毎秒2.6周期=**毎秒5.2歩相当**=全力疾走の脚)。**歩様ではなく滑走として送る。**
 */
export const ENEMY_WALK_STRIDE_MUL: Readonly<Record<string, number>> = {
  'werewolf-common': 2.5,
  // リッチ(滑走)。2.5 = 1蹴りで 103.5px 伸びる ⇒ **1秒に1.04周期(コマ13.6枚/秒)**。
  // ★**自転車と同じ値を置いた**——どちらも「脚で地面を刻まず、慣性で伸びる」movement なので、
  //   先例の数字をそのまま叩き台にした(**実機で社長が詰める**。もっと伸びやかにするなら上げる)。
  'lich-common': 2.5,
  // 死神(`reaper`)。**歩様ではなく浮遊の揺れ**(社長指示2026-08-28「歩く感じでは無い。浮いて移動する」)。
  // リッチの滑走と同じ理由——脚で地面を刻まないので、距離に対して絵の周期はずっと長くてよい。
  // 既定だと 1.63周/秒(コマ26枚/秒)。2.5 で **0.65周/秒(コマ10枚/秒)=既存で最も遅い**。
  'reaper2-common': 2.5,
};

/**
 * ★★**突進中のギア**(社長指示2026-09-21「**突時は倍速で**」)。
 *
 * ★**歩幅を長くする=ギアを上げる**。突進は速度が3倍になるので、歩幅をそのままにすると
 * **1秒に4.5回転**まで跳ね上がって目で追えない。歩幅を1.5倍にすると
 * **3倍 ÷ 1.5 = 2倍** ⇒ **巡航1.5回転/秒 → 突進3.0回転/秒 = ちょうど倍速**になる。
 * 実車のスプリントも「ギアを上げて、ケイデンスは倍まで」で、物理と一致する。
 * ★掛かるのは `aiPhase === 'charge'`(自転車の突進そのもの)の間だけ。溜め(windup)は巡航のまま。
 */
export const ENEMY_WALK_DASH_GEAR: Readonly<Record<string, number>> = {
  'werewolf-common': 1.5,
  // 研究所ゾンビLv2(社長支給2026-09-26「歩行と突進用」=突進も歩きの絵で走る)。突進は同じ3倍速
  // (`WEREWOLF_CHARGE_SPEED_MULT`)なので、自転車と同じ 1.5 で「突時は倍速」になる。
  'lab-zombie/lab-zombie-lv2': 1.5,
};

export const walkStrideMul = (idleTexName: string | null | undefined, dashing: boolean): number => {
  const base = (idleTexName && ENEMY_WALK_STRIDE_MUL[idleTexName]) || 1;
  const gear = dashing ? (idleTexName && ENEMY_WALK_DASH_GEAR[idleTexName]) || 1 : 1;
  return base * gear;
};

/**
 * ★★**ミラーの例外表。社長指示2026-09-22「ミラーは全ての敵で適用します」により空になった。**
 *
 * ★★**空のままにしておくこと。例外を作らない。**
 * ここへ登録すると、その個体だけミラーを止める。
 * 設計者は「正面向きに描かれた絵は反転しても得が無く、振り向きの潰し(`ENEMY_TURN_MS`)で
 * 正面の絵が理由もなく捻れる」と考えて**3件登録し(蜘蛛 v0.25.4552 / 咆哮型 v0.25.4560 /
 * ハンター v0.25.4563)、社長指示2026-09-22 で3件とも撤回した**。
 * ⇒ **正面向きに見えるかどうかを設計者が判定しない。全敵ミラーする。**
 * **社長から「この個体はミラーしないで」と明示された時だけ**1行足す。自分の判断で足さない。
 */
export const ENEMY_SHEET_FRONT_ON: Readonly<Record<string, boolean>> = {
};

/** そのシートは正面向きか(=ミラーしない)。**現在はどの絵も false**(上の表は空)。 */
export const sheetFrontOn = (idleTexName: string | null | undefined): boolean =>
  !!(idleTexName && ENEMY_SHEET_FRONT_ON[idleTexName]);

/**
 * ★コマの送り方。既定は**前方ループ**(0→末→0)。`pingpong` は 0→末→0 と折り返す
 * (プレイヤーの5コマ歩きと同じ作法)。
 *
 * ★**測って決める**: 「末コマ→先頭コマ」の絵の差を、隣り合うコマの差の平均と比べる。
 * 前方ループなら継ぎ目は隣と同じかそれ以下(女の実測 **0.68倍**)。**1を超えたら継ぎ目が跳ねる**
 * =前方ループではない(男の実測 **1.34倍**——しかも継ぎ目が全コマ中で最大の差だった)。
 * 男は襤褸で脚がほぼ隠れており、接地点から歩幅を読めないので、この指標で判断した。
 */
export type SheetPlayback = 'loop' | 'pingpong';
export const ENEMY_WALK_PLAYBACK: Readonly<Record<string, SheetPlayback>> = {
  'bat-male': 'pingpong',
};

export const walkPlayback = (idleTexName: string | null | undefined): SheetPlayback =>
  (idleTexName && ENEMY_WALK_PLAYBACK[idleTexName]) || 'loop';

/** 攻撃シートを持つ立ち絵(立ち絵のテクスチャ名 → コマ数)。シート名は `<立ち絵名>-attack`。 */
export const ENEMY_ATTACK_SHEETS: Readonly<Record<string, number>> = {
  // 社長支給2026-09-20「武器を振り下ろす絵」。6コマ(1コマ 552×514→配信276×257)。
  // 足元が最大134pxずれていたので相互相関で揃えた(v0.25.4536)。
  'bat-female': 6,
  // 社長支給2026-09-21。7コマ(1コマ 123×130)。★**縮小していない**(歩きと同じ理由)。
  // ★**足元は支給時点で揃っていた**(全コマ下端y=129・絵の中心が61.0〜61.5=0.5px以内)。
  'bat-male': 7,
  // 社長支給2026-09-21「skeleton男の引っ掻き」(ドット化後)。9コマ(1コマ 107×130)。常駐 0.48MB。
  // ★**揃っていた**(全コマ下端y=129・絵の中心が53.0〜53.5=0.5px以内)。
  // 読み: 0,1=構え / 2,3=引き / **4,5=爪を高く掲げる(溜め切り)** / 6=振り下ろし途中 /
  //       **7=振り切り(当たる)** / 8=戻り。⇒ **当たるコマは既定(末尾から2コマ目=7)のままで合う。**
  'skeleton-male': 9,
  // 社長支給2026-09-21「skeleton女の子引っ掻き」。9コマ(1コマ 142×130)。常駐 0.63MB。縮小していない。
  // ★**揃っていた**(全コマ下端y=129・絵の中心が69.5〜70.5=1px以内)。
  // 読み: 0,1=構え / 2=前へ踏み込む / 3,4=腕を上げる / **5=振り上げ切り** /
  //       **6=振り下ろし(当たる)** / 7,8=戻り。
  // ⇒ ★**当たるコマは 6**(既定の「末尾から2コマ目=7」ではない)。**戻りが2コマある**切り方なので、
  //    既定のままだと**振り切った後の絵で当たる**=掟③(消え切る時刻=当たる時刻)が嘘になる。下の表で指定した。
  'skeleton-female': 9,
  // 社長支給2026-09-21「ゾンビの噛みつき」。11コマ(支給 1232×130 → **透明な余白を切って 108×126**)。
  // 常駐 0.57MB。★切る矩形は全コマ共通(x2-109 / y4-129)=**コマ間の位置関係は動かない**
  //   (和集合の中心 55.5 = cell の中心 55.5)。歩きと同じ処理(v0.25.4548)。
  // 読み: 0=構え(立ち絵と同じ姿) / 1〜5=体を起こして伸び上がる / **6=伸び切り(溜め切り)** /
  //       **7=前へ落として噛みつく(当たる)** / 8,9,10=四つ這いで着地して戻る。
  // ⇒ ★**当たるコマは 7**(既定の「末尾から2コマ目=9」ではない)。**戻りが3コマある**切り方。
  'zombie-common': 11,
  // 社長支給2026-09-22「**リッチのワープ直後の鞭攻撃**」。11コマ(支給 1639×130 → 余白を切って **145×128**)。
  // 常駐 **0.78MB**。切る矩形は全コマ共通(x2-146 / y2-129)=コマ間の位置関係は動かない
  // (和集合の中心 74.0 = cell の中心 74.0)。★縮小していない(2×2の一致率 54%)。足元は全コマ下端。
  // 読み: 0=立ち(鞭を巻いた構え) / 1,2=鞭を引く / **3=大きく振りかぶる(溜め切り)** /
  //       **4=振り抜き(当たる)** / **5=鞭が地を打つ(火花が最大)** / 6〜9=火花が散り、鞭が戻る / 10=巻き直す。
  // ⇒ ★**当たるコマは 4**(既定の「末尾から2コマ目=9」ではない)。**火花のコマ(5)は当たった"後"**なので、
  //    4を当たる瞬間にすると **火花が出た瞬間 = ダメージの瞬間** になる(掟③)。
  //    ※火花の実測: コマ5=374px / 6=180 / 7=158 / 8=64 / 9=1 ⇒ **打点は5、以降は減衰**。
  'lich-common': 11,
  // 社長支給2026-09-26「研究所女性の攻撃モーション」(=`lab-zombie-1` の女性の見た目)。14コマ
  // (支給 1652×130 → 上の空き2行だけ落として **118×128**)。常駐 **0.81MB**。縮小なし(2×2一致率 9.9%)。
  // 左向き(既定)。足元は全コマ最下行で揃っていた。背丈は立ち絵とほぼ同じ(重ねた倍率 0.98〜1.07)。
  // 読み: 0=立ち / 1=身を沈める / 2,3=腕を振りかぶって薙ぐ(速度線) / **4=腕を伸ばし切る(左への伸びが最大65px)** /
  //       5=伸びたまま / 6=引き戻す / 7〜13=立ち直り。⇒ **当たるコマは 4**(下の表)。
  'lab-zombie/lab-zombie-lv1-female': 14,
  // 社長支給2026-09-26(=`lab-zombie-1` の男性の見た目)。10コマ(支給 1360×130 → 上の空き2行だけ落として
  // **136×128**)。常駐 **0.70MB**。縮小なし(2×2一致率 5.2%)。左向き(既定)。足元は全コマ最下行で揃っていた。
  // 背丈は立ち絵と同じ(重ねた倍率 1.00・重なり93%)=歩きのような背丈の補正は要らない。
  // 読み: 0,1=立ち・右腕を後ろへ引く / 2=振りかぶる / **3=鉤爪を左へ突き出し切る** / 4=突き出したまま /
  //       5=振り下ろして引き戻す / 6〜9=立ち直り。⇒ **当たるコマは 3**(下の表)。
  'lab-zombie/lab-zombie-lv1-male': 10,
};

/**
 * ★**当たる瞬間のコマ**(掟③「消え切る時刻 = 当たる時刻」)。
 * 既定は **末尾から2コマ目**——支給された2枚ともこの形だった
 * (最後の1コマは**振り抜き**=当たった後の余韻。無いと「当たって終わり」で慣性が切れる)。
 * 違う切り方のシートが来たらここへ1行足す。
 */
export const ENEMY_ATTACK_IMPACT_FRAME: Readonly<Record<string, number>> = {
  // 骸骨(女)は**戻りが2コマ**(7,8)ある切り方。振り下ろしは6コマ目。
  'skeleton-female': 6,
  // ゾンビは**戻りが3コマ**(8,9,10)ある切り方。噛みつくのは7コマ目。
  'zombie-common': 7,
  // リッチは**火花のコマが「当たった後」**なので、振り抜きの4が当たる瞬間。
  // こうすると **5(火花が地を打つ絵)の頭 = ダメージが解決する瞬間** で一致する。
  'lich-common': 4,
  // 研究員(女)は**伸び切り(4)が当たる瞬間**。2,3の速度線は振っている途中、7〜13は戻り。
  // 既定の「末尾から2コマ目」(=12)だと、立ち直り終わりで噛みつきが解決してしまう。
  'lab-zombie/lab-zombie-lv1-female': 4,
  // 研究員(男)は**鉤爪を突き出し切る3が当たる瞬間**(腕の高さの左端が最も遠い=足元の中心から74px。0〜2は36〜40・4は70・5以降は34〜48)。
  // 既定の「末尾から2コマ目」(=8)だと、立ち直りの途中で噛みつきが解決してしまう。
  'lab-zombie/lab-zombie-lv1-male': 3,
};

/**
 * ★★**そのシートが「武器ごと」描かれているか**(社長報告2026-09-21「コウモリ女の攻撃時に武器が消えてる」)。
 *
 * ★**既定は false = 描かれていない**。true にした個体だけ、別スプライトの武器を出さない
 * (絵とスプライトで**二本持ち**になるため)。
 *
 * ★**既定を false にした理由**: 設計者は v0.25.4537 で「シートがある=武器も描かれている」と
 * **推測で決めつけ**、女の別スプライトのランタンを消した。**実際には女のシートは素手で掴む絵**で、
 * ぶら下がっている小さなランタンは**体の装飾**だった。結果、**女の攻撃から武器が丸ごと消えた**。
 * ⇒ 推測で消さない。**「武器が描かれている」とはっきり見えたシートだけ true にする。**
 * 間違えた時、false なら「二本持ち」(気づける)、true なら「消える」(気づきにくい)。**安全な側は false。**
 */
export const ENEMY_SHEET_HAS_WEAPON: Readonly<Record<string, boolean>> = {
  // ★★**空のまま**にしておくこと。**誰も止めない**=別スプライトの武器は全個体で出る。
  //
  // ここへ登録すると、その個体だけ**別スプライトの武器を止める**。
  // 設計者は「シートに武器が描かれているから二本になる」と考えて**3回登録し、3回とも社長に差し戻された**:
  //   ① コウモリ女のランタン(v0.25.4537 → 4541 で撤回。実際は素手の掴みだった)
  //   ② 骸骨(男/女)の爪  (v0.25.4543/4545 → **4547 で撤回**。社長「別スプライトの爪は消さなくていい」)
  //   ③ コウモリ男の武器  (v0.25.4539 → **4558 で撤回**。社長「バットの男の武器も消えてるよ」)
  // ⇒ **絵とスプライトが重なって困るかどうかは、実機で社長が見て決める。**
  //   (CLAUDE.md「★★元々あるエフェクトを、指示なく消さない」)
  // **社長から「この個体の武器は消して」と明示された時だけ**1行足す。自分の判断で足さない。
};

export const sheetHasWeapon = (idleTexName: string | null | undefined): boolean =>
  !!(idleTexName && ENEMY_SHEET_HAS_WEAPON[idleTexName]);

export const attackImpactFrame = (idleTexName: string | null | undefined): number => {
  const n = attackSheetFrames(idleTexName);
  if (n <= 1) return 0;
  const over = idleTexName ? ENEMY_ATTACK_IMPACT_FRAME[idleTexName] : undefined;
  return over !== undefined ? Math.max(1, Math.min(n - 1, over)) : n - 2;
};

/**
 * ★シートが**右向き**で描かれている立ち絵(既定は左向き)。
 * ミラーの向きが反転する(`enemyMotion.EnemyMotionSpec.faceRight` と同じ意味)。
 * bat-female の2枚はどちらも**左向き**なので、ここには載せない。
 */
export const ENEMY_SHEET_FACES_RIGHT: Readonly<Record<string, boolean>> = {
  // 自転車(werewolf)はハンドルが右=**右向き**。型の `faceRight: true` と同じ。
  'werewolf-common': true,
  // ★削岩型(`driller`)。**立ち絵・歩き・突きの3枚とも右向き**(ドリルが右)。
  // ★**v0.25.4619 の取りこぼしの修正**: 突きのシートを入れた時点で `hasAnimSheet` が真になり、
  //   この個体はミラーの対象へ移っていた。ところがここに登録していなかったため
  //   **既定の「左向き素材」として扱われ、向きが常に逆**になっていた(素材を足すと向きが反転する形の穴)。
  //   ⇒ **シートを足す回は、この表への登録も同時に確かめる。**
  'driller-common': true,
  // ★グレン形態1(`glen-boss`)。**立ち絵も3枚のシートも右向き**(顔が右・右を見ている)。
  //   社長報告2026-09-26「**向きが逆**」: シートを入れた時点でミラーの対象へ移り(`hasAnimSheet`)、
  //   ここに無いので「左向き素材」扱い=**常に逆**を向いていた(v0.25.4619 削岩型と同じ穴)。
  'glen-boss': true,
};

export const walkSheetName = (idleTexName: string): string => `${idleTexName}-walk`;

/**
 * ★**歩きのシートの `bodyH`**(跳び/薙ぎの表と同じ意味=この枠の中で「立ち絵の枠いっぱい」に当たる高さ)。
 * 歩きの表(`ENEMY_WALK_SHEETS`)はコマ数しか持てないので、要る個体だけここに書く。
 * 載せない個体は従来どおり(枠の高さ=立ち絵の高さとみなす)。
 * 前例: 城ボス4の歩きは2.7%小さいが載せていない(目で追えない差)。**1割を超えたら載せる**。
 */
export const ENEMY_WALK_BODY_H: Readonly<Record<string, number>> = {
  'lab-zombie/lab-zombie-lv1-male': 116,   // 研究員(男)。立ち絵より約1割小さく描かれている(上の表の注記)
};

export const walkSheetBodyH = (idleTexName: string | null | undefined): number | null =>
  (idleTexName && ENEMY_WALK_BODY_H[idleTexName]) || null;
export const attackSheetName = (idleTexName: string): string => `${idleTexName}-attack`;

export const walkSheetFrames = (idleTexName: string | null | undefined): number =>
  (idleTexName && ENEMY_WALK_SHEETS[idleTexName]) || 0;

export const attackSheetFrames = (idleTexName: string | null | undefined): number =>
  (idleTexName && ENEMY_ATTACK_SHEETS[idleTexName]) || 0;

/**
 * ★**弾を撃つ絵**(噛みつきとは別の表)。シート名は `<立ち絵名>-shot`。
 *
 * ★**なぜ攻撃シートと分けたか**: 攻撃シートの表は **`hasAnimSheet`=ミラーする個体**の定義も
 * 兼ねている。プラントは**正面向きの花**で `faceMove: false`(左右に向き直らない)なので、
 * 攻撃の表に入れると**撃つたびに左右反転する**。絵の持つ意味が違うので表ごと分ける。
 * ★尺の出どころも違う——噛みつきは `biteAt`+噛み台本、こちらは**次に撃つ時刻**
 * (`utils/plantShot.ts`)。
 */
export const ENEMY_SHOT_SHEETS: Readonly<Record<string, number>> = {
  // 社長支給2026-09-21「プラントの弾攻撃(蕾になるのを早く流して、閉じたら弾が発射するイメージ)」。
  // 8コマ(支給 952×130 → 透明余白を切って **115×128**)。常駐 0.45MB。
  // 並びは **0=開いた花 → 7=閉じた蕾**。0 が立ち絵とほぼ同じ姿(描画比 0.984 / 立ち絵 1.000)。
  'plant-common': 8,
};

export const shotSheetName = (idleTexName: string): string => `${idleTexName}-shot`;

export const shotSheetFrames = (idleTexName: string | null | undefined): number =>
  (idleTexName && ENEMY_SHOT_SHEETS[idleTexName]) || 0;

/** 弾を撃つ絵を持っているか。★**ミラーの判定には使わない**(上のコメント参照)。 */
export const hasShotSheet = (idleTexName: string | null | undefined): boolean =>
  shotSheetFrames(idleTexName) > 1;

/**
 * ★**待機中(呼吸)の絵**。シート名は `<立ち絵名>-idle`。
 *
 * ★これも**ミラーの表には入れない**(`hasAnimSheet` と別)。理由は上の弾の表と同じ。
 * ★**立ち絵の代わりに常時出る**ので、`enemyBreath`(全敵共通の疑似呼吸)と**二重になる**。
 *   ⇒ 描画側で「シートのコマが出ているフレームは呼吸を掛けない」にしてある
 *   (社長指示2026-09-21「絵が入った敵のパターンには歪み入れないで」の適用)。
 */
export const ENEMY_IDLE_SHEETS: Readonly<Record<string, number>> = {
  // 社長支給2026-09-21「プラントの待機中(呼吸)」。6コマ(支給 714×130 → 余白を切って 115×128)。
  // 常駐 0.34MB。★**先頭コマは弾シートの先頭コマと1ビットも同じ**(実測 差0.0)=繋ぎ目が出ない。
  'plant-common': 6,
  // 社長支給2026-09-22「卵体」(=`ghost`。卵を抱いた花嫁)。16コマ(支給 1856×130 → 余白を切って **112×128**)。
  // 常駐 0.88MB。★**歩きではなく待機に置いた**——この敵は `kind: 'hover'`=**歩かず滑る**ので、
  // 歩きシートにすると**止まった瞬間に髪が凍る**。待機シートは常時流れる。
  // ★送りは**前方ループ**(髪と裾が一方向になびく絵。往復させると流れが逆走する)。
  //   継ぎ目の比 1.08・継ぎ目(23.6)より大きい隣が在る(28.5)ので、指標とも矛盾しない。
  'ghost-common': 16,
  // 社長支給2026-09-24「**死神の人形(攻撃武器)**」(=`hangedman`。表示名「使者」。
  //   死神が鐘の音とともに呼び出す、吊るされた人形の耐久武器)。16コマ
  // (支給 544×130 → **全幅で空の上2行だけ落として 34×128**。内容は1画素も削っていない)。常駐 **0.27MB**。
  // ★★**歩きではなく待機に置いた**——卵体と同じ理由。この型は `kind: 'hover'`
  //   (「**吊るされた人形が宙を滑って迫る=歩かず hover**」)なので、歩きシートにすると
  //   **止まった瞬間に揺れが凍る**。吊られた物は止まっていても揺れる。
  // ★**背丈は立ち絵と一致する**(立ち絵31×128・シート34×128。どちらも縦長=**内接は高さで決まる**ので
  //   cellの幅は背丈に効かない。どちらも描画276px)。足元は支給時点で全16コマ揃っていた(下端が cell の底)。
  // ★送りは**前方ループ**。継ぎ目(15→0)は隣の平均の **0.35倍**で、**15組ある隣が全部それより大きい**
  //   ——この表でいちばんきれいに閉じた1周期(比較: 卵体1.08 / 死神1.66)。
  'reaper2-hanged': 16,
};

/**
 * ★送り方の表。**登録は必須**(下のテストが、シートを足して書き忘れると落ちる)。
 * 呼吸のように行って戻る絵は `pingpong`、一方向に流れ続ける絵は `loop`。
 */
export const ENEMY_IDLE_PLAYBACK: Readonly<Record<string, IdlePlayback>> = {
  'plant-common': 'pingpong',   // 花が開いて閉じる=行って戻る
  'ghost-common': 'loop',       // 髪と裾がなびく=流れ続ける
  'reaper2-hanged': 'loop',     // 吊られて一周する揺れ(先頭と末尾がほぼ同じ絵=閉じている)
};

/** 1周期(吸う→吐く→止まる)の長さ。★叩き台——`?idlebreath=` で実機から触れる。 */
export const ENEMY_IDLE_PERIOD_MS: Readonly<Record<string, number>> = {
  // 卵体(ghost)。**叩き台 1800ms**(16コマ=約8.9コマ/秒)。置き換える前の浮遊ゆらぎは
  // strideHz 0.55 × テンポ0.7 = 2.6秒周期だったが、1コマあたりの絵の変化が大きいので少し速めた。
  'ghost-common': 1800,
  // 置き換える前の疑似呼吸は strideHz 0.25 × テンポ0.7 = **5.7秒**とかなり遅かった。
  // 6コマだと1コマ1秒近くなって途切れて見えるので、**3.6秒**を叩き台にする(社長が実機で詰める)。
  'plant-common': 3600,
  // 使者(hangedman)。**2000ms**。**振り子の周期から決めた**——吊り下げの長さ1m前後の振り子は
  // T=2π√(L/g)≒2.0秒。人の背丈ほどの物が縄で揺れる速さがここ。16コマ=8コマ/秒。
  // ※置き換える前の浮遊ゆらぎは strideHz 0.6 × テンポ0.7 = 2.4秒周期だったので、ほぼ据え置き。
  'reaper2-hanged': 2000,
};

export const idleSheetName = (idleTexName: string): string => `${idleTexName}-idle`;

export const idleSheetFrames = (idleTexName: string | null | undefined): number =>
  (idleTexName && ENEMY_IDLE_SHEETS[idleTexName]) || 0;

export const idleSheetPeriodMs = (idleTexName: string | null | undefined): number =>
  (idleTexName && ENEMY_IDLE_PERIOD_MS[idleTexName]) || 3600;

export const idleSheetPlayback = (idleTexName: string | null | undefined): IdlePlayback =>
  (idleTexName && ENEMY_IDLE_PLAYBACK[idleTexName]) || 'pingpong';

/**
 * ★**跳ぶ技の絵**(社長支給2026-09-21「パンプキン(蜘蛛)のジャンプ攻撃時」)。
 * シート名は `<立ち絵名>-jump`。**しゃがみ・滞空・着地の3区間**に割って使う
 * (割り方は `utils/enemyJumpSheet.ts`。尺は判定側=store の時計をそのまま読む)。
 */
export const ENEMY_JUMP_SHEETS: Readonly<Record<string, JumpSplit>> = {
  // 15コマ(支給 2415×130 → 余白を切って **157×128**)。常駐 1.15MB。
  // 読み: **0〜3=しゃがむ**(3で沈み切る) / **4〜9=踏み切り〜頂点〜落下**(7,8が頂点) /
  //       **10〜14=接地〜砂埃〜立ち直り**(11が一番潰れる=着地の瞬間)。
  // ★砂埃は**絵の中にも描かれている**が、**ゲーム側の砂埃エフェクトは消していない**
  //   (社長指示2026-09-21「元々のエフェクトは消さないで」)。
  'pumpkin-common': { crouch: 4, air: 6, land: 5 },
  // ★ハンター(棺桶巨人)16コマ(支給 3344×258 → 余白を切って **205×256**)。常駐 3.20MB。
  // 読み(高さの実測つき): **0〜4=棺桶を担ぎ上げる**(217→218→205→215→245)/
  //       **5〜9=跳ぶ**(**256**=踏み切りで伸び上がる→218→228→207→171=落ちながら振り下ろす)/
  //       **10〜15=叩きつけ〜立ち直り**(**150**=棺桶が地に着く瞬間→133→123=沈み切り→152→176→189)。
  // ★10コマ目(区間の先頭)が**棺桶の接地**=判定の着地と同じ瞬間に出る。
  // ★★**境目を1コマ手前へ直した**(社長指摘2026-09-22「**もう少し手前のコマまでが、ジャンプ前**」・v0.25.4569)。
  //   旧 `crouch:6 / air:4` は**一番伸び上がったコマ(5・高さ256)を溜めの最後**に置いていたため、
  //   「地上で一番背が高く、跳んだ途端に縮む」という逆さまの絵になっていた。
  //   5を滞空の**先頭**にすると、伸び上がり=踏み切り → 以降ずっと縮みながら落ちる、と筋が通る。
  'hunter': { crouch: 5, air: 5, land: 6 },
  // ★城ボス1(搬送体・型は `giantbat`)16コマ(支給 2000×130 → 余白を切って **121×128**)。常駐 **0.95MB**。
  // ★★**解像度を半分にして出し直してもらった版**(v0.25.4577)。初出の版は 243×256×16コマ=3.80MB で、
  //   **画面132pxに対して絵が243ドット=1.84ドット/px**=毎フレーム46%のドットを捨てていた。
  //   いまは **0.92ドット/px=1ドットも落ちない**(むしろ1.09倍に拡大されて出る)。常駐は **1/4**。
  // ★**コマは16で揃っている**(v0.25.4576 の11コマ版は社長が意図して減らしたもの。滞空が粗かったので戻した)。
  //   高さをフル解像度版と1コマずつ照合して一致を確認済み。色数は32(62色版と並べても差は見えない)。
  // 読み: **0〜3=立ちから沈み込む** / **4〜10=踏み切り〜滞空〜落下** / **11〜15=接地〜立ち直り**。
  // ★11コマ目(区間の先頭)が**接地**=判定の着地(爆風)と同じ瞬間に出る。
  // 1コマあたり: 溜め250ms / 滞空45.7ms / 着地140ms。
  // ★★**州の名前が汎用ジャンプと違う**(`g-jump-windup`/`-air`/`-recover`)。時計も城ボス専用
  //   (`GIANT_JUMP_WINDUP_MS` 実効1000ms / `GIANT_JUMP_AIR_MS` 実効320ms / 立ち直りは相と台本で可変)。
  //   描画側(`pixiScene.enemyJumpTexture`)がこの3州を別に写している。
  'giantbat': { crouch: 4, air: 7, land: 5 },
  // ★城ボス3(廃都の絡まった巨躯・型は `giantbat` だが立ち絵は `stage3-enemies/giantbat`)。
  // 社長支給2026-09-24「城3ボスのジャンプ」。16コマ
  // (支給 2192×152 → 全コマ共通の矩形 x2-134 / y2-151 で切って **133×150**)。常駐 **1.22MB**。
  // ★**区切りは城ボス1と同じ 4/7/5**——**絵の高さ**と**足元の幅**が両方その筋で折り返すから(単位px):
  //   **0〜3=沈む**(高さ 122→115→103→**101**=沈み切り。足元の幅は 131→127→119→119 と踏ん張る)/
  //   **4〜10=踏み切り〜滞空〜落下**(高さ 122=足はまだ地に着いたまま伸び上がる → **149→150**=伸び切り
  //    → 129→112→128→114。足元の幅は **116→90→67→51**(7コマ目が最小=絡まった脚を畳み切る)
  //    **→62→79→83** と、**折り返して開き直す**=着地へ向けて脚を伸ばす) /
  //   **11〜15=接地〜立ち直り**(高さ **86**=潰れ始め → **82**=沈み切り → 98→105→108)。
  //   ※7〜10で高さが上下するのは絡まった腕が振れているぶん。**下端は全コマ cell の底**なので、
  //    「浮いている絵」ではない(浮きはゲーム側が付ける=城ボス1と同じ)。
  // ★11コマ目(区間の先頭)が**接地**=判定の着地(爆風)と同じ瞬間に出る。城ボス1と同じ。
  // ★**縮小していない**(2×2の一致率 7.5%)。色数33・半透明0%。
  // ★★**素のままだと背が +9.3% 伸びる**(歩き -8.9% とは**逆の側**)。枠は正方形(当たり判定 60×60)で、
  //   立ち絵 164×150 は**横長だから幅で決まる**のに、跳ぶ絵 133×150 は**縦長なので高さで決まる**
  //   ——決まり手が入れ替わるぶん 164/150=1.093倍だけ高くなる。
  //   ⇒ **v0.25.4635 の背丈合わせ(`utils/sheetFit.ts`)が 0.915倍を掛けて立ち絵へ揃える**
  //   (跳ぶ絵も `atkTex` 経由で `walkTex` に入るので、歩きと同じ経路で掛かる)。
  //   実測(ヘッドレス・遠近スケールで割った値・3本): 歩き 153.3 / 跳ぶ16コマ 152.9〜153.4px=**ばらつき0.3%**。
  // ★足元は支給時点で揃っていた(**全16コマの下端が cell の底**。10コマ目だけ1px上)。
  //   中心も 67.0〜68.5(cell の中心 68.0)。**左向き**=既定のまま(立ち絵・歩きと並べて確認済み)。
  // ★★**絵が小さく描かれていたので `bodyH` で揃える**(社長報告2026-09-25
  //   「**城3のジャンプの絵がやたら小さい**」)。立ち姿のコマ(0)を立ち絵へ重ねると
  //   **0.815倍で一番よく重なる(IoU 0.962=同じ姿勢)**。比較: 叩きつけ1.000(IoU 0.949)・歩きほぼ等倍。
  //   画素数でも裏が取れる(立ち姿の不透明画素 立ち絵15053 / 叩き15262 に対し **跳び10157=67%**
  //   ≒ 面積比の平方根 0.82)。⇒ **跳びだけが 18.5% 小さい**。
  //   `bodyH = 150 × 0.815 ≒ 122`。枠の高さを揃えるだけでは直らない(枠は150で他と同じなので
  //   補正が 1.000 になり、中身が小さいまま出る)。
  //   ★代償: 絵は 1.23倍へ引き伸ばされる(1.0 → 0.81 ドット/px)。**等倍で描き直した版が届けば
  //   この行を消すだけ**で戻る。
  'stage3-enemies/giantbat': { crouch: 4, air: 7, land: 5, bodyH: 122, landBodyH: 95 },
  // ★城ボス4の跳び(社長支給2026-09-25「ジャンプ」)。16コマ
  // (支給 3104×152 → x3-191 / y2-151 で切って **189×150**)。常駐 **1.73MB**。
  // ★**区切りは 5/6/5**。**絵の高さが 5〜10コマ目だけぴったり 132 で一定**
  //   (144,141,145,149,140,**132,132,132,132,132,132**,148,141,139,142,144)
  //   =**滞空の6コマを同じ高さで描いてある**という、はっきりした印。浮きはゲーム側が付ける。
  //   **0〜4=沈み込み**(3コマ目149=一番伸びる)/ **5〜10=滞空**(白い弧が6コマ目で最大=爪の薙ぎ)/
  //   **11〜15=接地〜立ち直り**(11で高さ148・爪を下ろして破片が散る)。
  // ★**縮小していない**(2×2の一致率 6.1%)。色数33・半透明0%。
  // ★**描き込みは立ち絵の 0.720倍**(0コマ目を立ち絵へ重ねて **IoU 0.963**=ほぼ完全一致の測定)。
  //   ⇒ `bodyH = 200 × 0.720 = 144`。**着地だけ別倍率、は無い**(11/13/15コマ目も 0.71〜0.72)。
  // ★★シートの取り違えを是正(社長報告2026-09-25「今は手を前に出すモーションがジャンプ攻撃に入っちゃってる」)。
  // 支給された2枚を**逆に入れていた**。跳びの絵は「しゃがむ→跳び上がる→叩きつけて砂埃」で、
  // 砂埃は**10コマ目で0〜123から278へ跳ねる**=そこが着地。⇒ 3(しゃがみ)/7(滞空)/6(着地)。
  // ★この絵は**薙ぎ表からも同じファイルを引く**(下の `SWEEP_SHEET_SUFFIX` が 'jump')。
  //   飛び掛かりは3区間の跳びとして、それ以外の技は3区間の薙ぎとして、**同じ絵を2通りに読む**。
  'stage4-enemies/giantbat': { crouch: 3, air: 7, land: 6, bodyH: 136 },
  // ★★城ボス5の跳び+叩きつけ(社長支給2026-09-25「ジャンプ及び叩きつけモーション」)。**15コマ**。
  // 絵の読み: 0〜3しゃがみ(足元の幅が119→67へ絞られる) / 4で**腕を真上へ伸ばして踏み切る**(全体高150=最大) /
  // 5〜9滞空(6,7は畳んで最小・8,9で鉤を振りかぶる) / **10で最も潰れる(全体高96=最小)=着地の一撃** /
  // 11〜14で戻る(104→112)。⇒ **4(しゃがみ)/ 6(滞空)/ 5(着地)**。
  // ★`bodyH: 140`。**コマ0と立ち絵は同じ立ち姿**なので照合が効き、**最良スケール1.425(IoU 0.944)**。
  //   枠の比(立ち絵200 ÷ シート150 = 1.333)より**7%大きい**=絵が枠より小さく描かれている。
  //   150 × 1.333 / 1.425 = 140.3。★着地だけ別倍率(`landBodyH`)かは実機で見て決める(城ボス3の前例)。
  'stage5-enemies/giantbat': { crouch: 4, air: 6, land: 5, bodyH: 140 },
  // ★研究所Lv3の跳び(社長支給2026-09-26「レベル3のジャンプ」)。16コマ
  // (支給 2160×152 → 上の空き2行だけ落として **135×150**)。常駐 **1.30MB**。縮小なし(2×2一致率 4.2%)・半透明0%・左向き。
  // 足元は全16コマが最下行(浮きはゲーム側が付ける=他の跳びと同じ)。
  // ★区切りは **3/6/7**。足元の幅(最下10行の横幅・px)と絵の高さで読んだ:
  //   **0〜2=しゃがみ**(0=立ち絵と同じ四つ足 → 2で沈み切る。足元92/84/32)/
  //   **3〜8=跳ぶ**(3=後ろ脚で立ち上がる → 4=つま先で伸び上がる踏み切り・高さ140 → 5〜7=腕を広げて脚を畳む
  //    (高さ88が最小)→ 8=脚を下へ伸ばす。足元91/15/12/53/63/25)/
  //   **9〜15=着地〜立ち直り**(**9で足を広く踏みしめる=足元94**=判定の着地と同じ瞬間 → 13〜15で四つ足へ戻る)。
  // ★★**3コマ目は跳ぶ側**(社長指摘2026-09-26「ジャンプする前のコマ割り間違えてる。ジャンプする時のコマが
  //   入っちゃってる」)。初版は 4/5/7 で、**立ち上がる3コマ目を溜めの最後**に置いていた=地上で立ち上がって
  //   止まってから跳ぶ絵になっていた。足は地に着いていても**跳び上がる動きの始まり**なので滞空の先頭へ移した
  //   (ハンターで「一番伸び上がったコマを溜めの最後に置いていた」のを直した v0.25.4569 と同じ型)。
  // ★倍率: 0コマ目(立ち絵と同じ姿)を立ち絵へ重ねて **0.745倍(IoU 0.949)**。枠の比 150/200=0.75 と 0.7% 差
  //   =`bodyH` は要らない。
  'lab-zombie/lab-zombie-lv3': { crouch: 3, air: 6, land: 7 },
  // ★グレン形態1の跳び(社長支給2026-09-26「ジャンプ」)。11コマ(支給 2882×194 → 上の空き2行だけ落として
  // **262×192**)。常駐 **2.21MB**(遅延組=`glen-boss` の行)。縮小なし(2×2一致率 10.5%)・半透明0%・左向き。
  // 足元は全コマ最下行。倍率: 0コマ目を立ち絵へ重ねて **0.99(IoU 0.84)**=枠の比 192/194 と一致=`bodyH` 不要。
  // ★区切りは **3/4/4**。絵の高さ(px)で読んだ: 0=立ち絵と同じ構え(192)→1(168)→**2で沈み切る(139)** /
  //   **3〜6=跳ぶ**(3,4で伸び上がり 145→152 → **5=腕を広げて一番高い 169** → 6=脚を曲げて降りてくる 133)/
  //   **7〜10=着地〜立ち直り**(**7が一番潰れる 126=着地の一撃**=判定の着地と同じ瞬間 → 8〜10 で 130 のまま構え直す)。
  //   ★沈み切った後の**伸び上がり(3,4)は跳ぶ側**(研究所Lv3・ハンターで直した取り違えと同じ型を先に避けた)。
  // ★州と時計は城ボス共通(`g-jump-windup`/`-air`/`-recover`)。踏み潰し(`g-stomp-`)は別の絵が来るまで従来どおり。
  // ★★**コマごとの大きさ合わせ**(社長指示2026-09-26「全コマ立ち絵と大きさ揃えて。全体的に小さい」)。
  //   顔の大きさで測った人物の倍率: 0.98 / 0.80(※1コマ目は顔が小さく隠れて測りにくく、前後の間を取った)/ 0.70 / 0.70 /
  //   0.68 / 0.68 / 0.66 / 0.68 / 0.66 / 0.66 / 0.70 ⇒ 枠192 × 倍率 = 下の値。
  //   ★代償: 1コマ目以降は **1.25〜1.52倍に引き伸ばされる**(ドットが粗くなる)。人物を枠いっぱいに描き直した版が
  //   届けば、この配列を消すだけで戻る。
  'glen-boss': { crouch: 3, air: 4, land: 4, frameBodyH: [188, 154, 134, 134, 131, 131, 127, 131, 127, 127, 134] },
};

/** 着地の絵を流す長さ(ms)。立ち直り(recover)全体はもっと長いので、その頭だけを使う。 */
export const ENEMY_JUMP_LAND_MS: Readonly<Record<string, number>> = {
  'pumpkin-common': 420,
  // ★ハンターは**立ち直りの全部**を使う(= `PUMPKIN_RECOVER_MS` 1000 ÷ 既定のゲームスピード 1.2)。
  // 蜘蛛と違い、最後のコマが「棺桶を地に置いたまま立つ姿」で**立ち絵(担いだ姿)と繋がらない**ので、
  // 絵を先に終わらせると棺桶が肩へ瞬間移動する。立ち直りが明ける=歩き出す瞬間まで持たせる。
  // ※`?speed=` で既定から変えた時だけ追従しない(蜘蛛の420も同じ性質)。値は `enemyJumpSheet.test.ts` が見張る。
  'hunter': 833,
  // ★城ボス1。立ち直りは**相と台本で変わる**(相3で実効700ms・それ以外は1100ms前後)ので、
  //   **一番短い側(700ms)に合わせて必ず出し切れる長さ**にした。残りは立ち絵で待つ(蜘蛛の420msと同じ扱い)。
  'giantbat': 700,
  // ★城ボス3も同じ(州も時計も城ボス1と共通の `g-jump-*` / `GIANT_JUMP_*`)。
  'stage3-enemies/giantbat': 700,
  // 城ボス4も同じ(州も時計も城ボス1・3と共通)。
  'stage4-enemies/giantbat': 700,
  // 城ボス5も同じ(州も時計も城ボス1・3・4と共通)。
  'stage5-enemies/giantbat': 700,
  // グレン形態1も同じ(州も時計も城ボスと共通)。
  'glen-boss': 700,
  // ★研究所Lv3は**立ち直りの全部**を使う(ハンターと同じ = `PUMPKIN_RECOVER_MS` 1000 ÷ 既定のゲームスピード 1.2)。
  //   着地7コマに「踏みしめる→四つ足へ戻る」まで描いてあるので、立ち直りが明けて歩き出す瞬間まで流し切る。
  'lab-zombie/lab-zombie-lv3': 833,
};

export const jumpSheetName = (idleTexName: string): string => `${idleTexName}-jump`;

/**
 * ★**叫喚の叫びの絵**(社長支給2026-09-23「叫喚の叫びモーション」)。
 * シート名は `<立ち絵名>-scream`。**1つの相(`aiPhase==='scream'`=溜め2秒)に全コマを割る**
 * ——跳びや薙ぎのような区間分けは無い(相が1つしかない技なので)。
 * ★**尺はここに持たない。** 進み具合は判定側の時計(`SCREAMER_WINDUP_MS` と `aiPhaseUntil`)から読む
 *   =「絵は終わっているのに判定はまだ続いている」が起きない。
 * ★支給は 1680×130。**上の空き2行だけ落として 128 に揃えた**(内容は1画素も削っていない)。
 *   立ち絵(90×128)・歩き(96×128)と高さが揃う=叫び始めに背丈が跳ねない。
 */
export const ENEMY_SCREAM_SHEETS: Readonly<Record<string, number>> = {
  'screamer-common': 16,   // 105×128/コマ。常駐 0.82MB。立ち上がり→前かがみへ(絵の高さ 128→110)
};

export const screamSheetName = (idleTexName: string): string => `${idleTexName}-scream`;

export const screamSheetFrames = (idleTexName: string | null | undefined): number =>
  (idleTexName && ENEMY_SCREAM_SHEETS[idleTexName]) || 0;

/**
 * ★**薙ぎ払いの絵**(社長支給2026-09-22「伐採人の薙払いの時のモーション」)。
 * シート名は `<立ち絵名>-sweep`。**溜め/薙ぎ/戻りの3区間**に割って使う
 * (割り方は `utils/enemySweepSheet.ts`。尺は判定側=store の時計をそのまま読む)。
 *
 * ★**なぜ攻撃シートの表と分けたか**: 攻撃シート(`ENEMY_ATTACK_SHEETS`)は**噛みつき台本(`biteAt`)**の
 * 上でコマを送る。伐採人の薙ぎ払いは噛みではなく `aiPhase`(`logger-sweep-*`)の別の時計なので、
 * 攻撃の表へ入れても **`biteAt` が無く1コマも描かれない**(★実在確認の掟)。
 */
export const ENEMY_SWEEP_SHEETS: Readonly<Record<string, SweepSplit>> = {
  // 社長支給2026-09-24「削岩機の突きのアニメーション」(=`driller`)。14コマ
  // (支給 2114×130 → 空の上2行を落とし、**全コマ共通の余白も切って 146×128**。内容は1画素も削っていない)。
  // 常駐 **1.00MB**。★**縮小していない**(描画は 120×105 で、絵に対する余裕が1.2倍しかない)。
  // 足元は**全コマ共通で下端127**。余白を切ったのは背丈のため——枠が正方形なので**内接は幅で決まり**、
  // 151幅のままだと立ち絵(143幅)に対して**背が4.5%縮む**。切って**2.1%**まで戻した(残りは絵の幅そのもの)。
  // ★区間は **8 / 2 / 4**。実測した「隣のコマとの絵の差」で読むと、動きはこう並んでいる:
  //   0〜3=ドリルを引き込む(差25前後・幅 143→88) / 4〜6=**引き切って溜める**(差2.6〜13=ほぼ同じ姿勢) /
  //   7=出はじめ(差17) / **8=突き切る(差33.6=シート中で最大の一歩)** / 9=伸び切り /
  //   10〜12=出したまま(差4.4) / 13=引き戻しにかかる(差14.5)。
  // ★★**当たる瞬間のコマを「出はじめ(7)」から「突き切った(8)」へ変えた**(社長指示2026-09-24
  //   「**次の部分が早く突いてるように見えるように**」)。7を溜めの末尾に置き、**下の重みで36msだけ**出す
  //   ⇒ 目には「引き切った姿 → (一瞬) → **出し切った姿**」と映り、当たる瞬間にはもう突き終わっている。
  // ★掟③は動いていない: **当たるのは溜めが終わる瞬間=突き区間の先頭コマ**(薙ぎと同じ)。
  //   重みは**区間の中**だけを配り直すので、区間の境目=当たる時刻には触れない。
  // ★★重みは**そのコマを出すミリ秒をそのまま書く**(区間ごとに合計が実効尺と一致する=読める表)。
  //   実効 溜め1000ms / 突き183ms / 戻り333ms。比なので、尺が変わっても配分は保たれる。
  //   **動作の意味ごとに配り方を変える**(一本の減速カーブに全部を乗せない):
  //   ① **引き込み 0:115 1:95 2:78 3:62** = 重いドリルを引き始めは重く、**引き切りで勢いよく肩へ収まる**
  //      (だんだん**速く**なる)。合計350ms。
  //   ② **収まり 4:70 5:70** = 肩で受け止める2枚(4と5は同じ姿勢=絵の差2.7)。
  //   ③ **溜め 6:453** = **ピタッと止める1枚**。①②との間に段差を作る(ここが「今から来る」の圧)。
  //      この453msの終盤35%に描画側の震え(`windupTremorPx`)が乗る。
  //   ④ **出はじめ 7:57** = 一番短い1枚。★**30fpsの実機でも必ず1フレームは出る長さ**にしてある
  //      (36msだと端末によって出たり出なかったりする=一番大事な1コマを偶然に任せることになる)。
  //   ⑤ **突き 8:57 9:126** = 突き切った絵が**当たる瞬間**に出て、伸び切りで止まる。
  //   ⑥ **戻り 10:40 11:48 12:58 13:187** = 10〜12はほぼ同じ姿勢(輪郭の差が全体の1%)なので、
  //      **だんだん遅くなる小刻み=打ち込んだドリルの振動が減衰する**として送り、13で引き戻しへ入る。
  'driller-common': {
    windup: 8, active: 2, recover: 4,
    weights: [115, 95, 78, 62, 70, 70, 453, 57, 57, 126, 40, 48, 58, 187],
  },
  // ★伐採人(`logger`)。立ち絵名は **`reaper-common`**——旧・死神の絵が v0.25.4004 で伐採人へ降格した
  //   ため(`ENEMY_VARIANT_SETS.logger = ['reaper-common']`)。**死神本体は `reaper2-common` で別人。**
  // 9コマ(支給 1296×130 → 余白を切って **134×128**)。常駐 0.59MB。切る矩形は全コマ共通(x4-137 / y2-129)。
  // 読み(高さの実測つき): **0=チェーンソーを頭上へ構えて唸らせる(122)** /
  //       **1,2,3=振り下ろして地を噛み、薙ぎ抜ける(110→99→91。2で地面の火花が最大)** /
  //       **4〜8=引き戻して担ぎ直し、構えへ戻る(97→127→128→105→106)**。
  // ★★**0を溜めに、1を薙ぎの先頭に置いた**=**当たる瞬間に刃が走り出す**(掟③)。
  //   溜めは実効1083msを0コマ目で持つ(「武器を構えて一瞬止まる」の型。赤い帯もこの間に出ている)。
  'reaper-common': { windup: 1, active: 3, recover: 5 },
  // ★城ボス3の**叩きつけ**(社長支給2026-09-25「叩きつけ」。州は `g-slam-*`)。16コマ
  // (支給 2656×172 → 全コマ共通の矩形 x2-163 / y2-171 で切って **162×170**)。常駐 **1.68MB**。
  // ★★**この技だけ枠が20px高い**——4コマ目で蔓を頭上まで振り上げるため。**絵の密度は他と同じ**
  //   (立ち姿の本体 135px = 立ち絵135・歩き134と一致)。だから `bodyH: 150` で背丈を揃える(上の定義参照)。
  // 読み(砂埃の画素数を数えて境目を決めた):
  //   **0〜4=蔓を振り上げる**(絵の高さ 149→144→139→145→**170**=振り切り) /
  //   **5〜9=振り下ろして溜め切り、構えたまま静止**(蔓の上端 36→43→44→46→43=ほぼ動かない。
  //    store のコメント「立ち上がって静止」と同じ) /
  //   **10=地を叩く**(砂埃が**ここで初めて出る**。9コマ目までは0、10で126画素) /
  //   **11〜15=砂埃が広がって引く**(338→556→871→**1282**=最大→28=晴れる)。
  // ★★**10コマ目が「叩き」区間の先頭**=**当たる瞬間に砂埃が出る**(掟③)。
  //   当たりは `g-slam-windup` → `-active` の遷移で1回だけ積まれるので、区間の境目がそのまま一致する。
  // ★尺は**判定側の時計そのまま**: 溜め実効1200ms(10コマ=120ms/コマ)/ 叩き260ms(1コマ)/
  //   硬直1300ms(5コマ=260ms/コマ)。**等分**=絵に描かれた加減速をそのまま流す(weights は置かない)。
  // ★**縮小していない**(2×2の一致率 7.4%)。足元は全16コマ下端が cell の底。**左向き**=既定のまま。
  'stage3-enemies/giantbat': { windup: 10, active: 1, recover: 5, bodyH: 150 },
  // ★城ボス1(搬送体)の**攻撃モーション**(社長支給2026-09-25「城1のジャンプと突進以外の攻撃モーション」)。
  // 10コマ(支給 1280×130 → 全コマ共通の矩形 x2-125 / y2-129 で切って **124×128**)。常駐 **0.61MB**。
  // 読み(翼を広げて薙ぐ。絵の高さ/一番広い行):
  //   **0〜5=溜め**(128→117→101→101→110→110。翼をたたんだ姿から**頭上へ開き切る**まで)/
  //   **6=当たり**(幅124=最大。開いた翼を前へ薙ぎ抜く瞬間)/
  //   **7〜9=戻り**(106→101→114。翼をたたみ直して構えへ)。
  // ★**縮小していない**(2×2の一致率 12.3%)。色数33・半透明0%。
  // ★**歩き・跳びと同じ倍率で描かれている**(歩き0に重ねて 1.020倍・IoU 0.905。跳びは1.030)
  //   ⇒ 城ボス3の跳びのような `bodyH` は要らない。
  // ★足元は支給時点で揃っていた(全10コマの下端が cell の底)。中心も 63.0〜63.5(cell の中心 63.5)。
  'giantbat': { windup: 6, active: 1, recover: 3 },
  // ★城ボス4の**攻撃モーション**(社長支給2026-09-25「ジャンプ以外の攻撃」)。16コマ
  // (支給 2832×152 → x2-174 / y2-151 で切って **173×150**)。常駐 **1.58MB**。
  // ★**区切りは 10/1/5**(城ボス3の叩きつけと同じ形)。**砂埃の画素数を数えて境目を決めた**:
  //   9コマ目まで 0〜2画素 → **10コマ目で42** → 11で134 → 13で196 → 15で265(最大)。
  //   本体から離れた小片も 10コマ目から(0〜4 → **20** → 34 → 41)。
  //   **0〜9=溜め**(腕を振りかぶって打ち下ろす)/ **10=当たり**(地を打った瞬間・砂埃が出始める)/
  //   **11〜15=戻り**(砂埃が広がって沈む)。
  // ★**縮小していない**(2×2の一致率 6.1%)。色数33・半透明0%。
  // ★**描き込みは立ち絵の 0.680倍**(0コマ目を立ち絵へ重ねて **IoU 0.963**)。
  //   ⇒ `bodyH = 200 × 0.680 = 136`。これが無いと**9.3%小さく**出る。
  // ★既定は**叩きつけの絵**(`-jump` ファイル)。社長裁定2026-09-25「**絶妙にうまく絵を使ってくれてた
  // から、ちゃんと戻して**」——薙ぎ払い/突進/三連突進/ノヴァはこの読み方のままにする。
  // 腕を前へ薙ぐ絵を使うのは**氷の横薙ぎと通常弾だけ**(下の `GIANT_ALT_SWEEP`)。
  'stage4-enemies/giantbat': { windup: 10, active: 1, recover: 5, bodyH: 136 },
  // ★★城ボス5の攻撃(社長支給2026-09-25「城5ボスの攻撃モーション」。跳び/叩きつけは後日別途)。
  // **8コマ・銃の連射**。閃光の画素数が **0 → 2681 / 1133 / 2483 / 1360 / 2328 / 1295 → 207** で、
  // **1〜6コマ目が撃っている**(大小の波が3つ=3発)。⇒ **1(構え)/ 6(連射)/ 1(撃ち終わり)**。
  // ★この個体は**撃つ相を持つ**(`g-bolt-burst` / `g-trishot-active` / `g-sweepbeam-active`)ので、
  //   連射を**当たりの区間**に置くと、撃っている間だけ連射が流れる=絵と技が一致する。
  // ★`bodyH` は不要。**下半身だけで測った倍率が 1.335** で、立ち絵200 ÷ シート150 = 1.333 と一致
  //   (全身で測ると銃と腕が伸びるぶん 1.38 に出るが、それは構えの差)。
  'stage5-enemies/giantbat': { windup: 1, active: 6, recover: 1 },
  // ★グレン形態1の攻撃。**社長支給2026-09-26「振り回しの差し替え」で7コマ版へ差し替え**
  // (支給 1848×194 → 上の空き2行だけ落として **264×192**)。常駐 **1.30MB**。縮小なし(2×2一致率34%だが位相で変わらない
  // =32色の平らな面)・半透明0%・左向き。★**高さは192で出し直された**(浮遊は256のまま。倍率は枠合わせが立ち絵へ揃える)。
  // 読み: 0=立ち絵と同じ構え / 1〜4=腕を引いて触手を溜める / **5=触手を左へ突き出し切る** / 6=触手を振り回す。
  //   初版(9コマ・256)の 0〜6 と同じ絵で、**触手が周りへ広がって収まる7,8が無くなった**。
  // ⇒ **5(溜め)/ 1(当たり=突き出し5)/ 1(戻り=振り回し6)**。突いて当て、振り回して抜ける。
  //   ※「当たり2・戻り0」は採れない(区間0だと区間ごと消える=`enemySweepSheet.test.ts` の不変条件)。
  //   戻りの相が長い技では 6 で持つ(流し切っても絵を捨てない=`enemySweepSpanFrame`)。
  //   当たりの相を持たない技(爪痕など)は「当たり+戻り」を1続きに流す=戻りの相で 5→6(初版と同じ扱い)。
  // ★城ボス1・5と同じく、城ボス標準技+爪痕系(talon/boon/reach)の3相をこの1枚で描く。
  //   **跳ぶ技(`g-jump-`/`g-trijump-`/…)は `GIANT_JUMP_TECHS` で最初から外れる**=跳び/踏み潰しの絵が来るまで従来どおり。
  // ★倍率: 0コマ目を立ち絵へ重ねて 0.91(IoU 0.76)。枠の比 192/194=0.99 との差は約9%=1割未満なので `bodyH` は付けない。
  // ★★**コマごとの大きさ合わせ**(社長指示2026-09-26「全コマ立ち絵と大きさ揃えて。全体的に小さい」)。
  //   人物の描かれる大きさを**顔の大きさ**で測った(立ち絵の顔をテンプレートにして倍率を振り、一番よく重なる倍率):
  //   0.90 / 0.83 / 0.80 / 0.80 / 0.75 / 0.75 / 0.78 ⇒ 枠192 × 倍率 = 下の値(=そのコマを 1/倍率 へ拡大して立ち絵に揃える)。
  //   ※上の「0.91・約9%」は**小さい側**の誤記だった(大きく出るのではなく、9%小さく出ていた)。
  'glen-boss': { windup: 5, active: 1, recover: 1, frameBodyH: [173, 159, 154, 154, 144, 144, 150] },
};

/**
 * ★3相の技のシート名。既定は `-sweep`。**絵が薙ぎでない型はここで名前を変える**
 * (削岩型は「突き」なので `-thrust`。仕組みは薙ぎと同じものを借りているが、
 *  **素材の名前まで嘘にしない**ための1行の表)。
 */
const SWEEP_SHEET_SUFFIX: Readonly<Record<string, string>> = {
  'driller-common': 'thrust',
  // 城ボス3は「叩きつけ」(`g-slam-*`)。薙ぎでも突きでもないので名前を合わせる。
  'stage3-enemies/giantbat': 'slam',
  // 城ボス1は社長の言葉どおり「攻撃モーション」(翼で薙ぐ。特定の技の名前を付けない)。
  'giantbat': 'attack',
  // 城ボス4も社長の言葉どおり「攻撃」(ジャンプ以外の全技で使う1枚)。
  'stage4-enemies/giantbat': 'jump',   // ★既定は叩きつけの絵。腕を薙ぐ 'attack' は `GIANT_ALT_SWEEP` の2技だけ
  'stage5-enemies/giantbat': 'attack',
  'glen-boss': 'attack',

};

/**
 * ★**この技には配らない**(立ち絵名 → 技の接頭辞)。**跳ぶ技は全個体で常に除外**(専用シートがある)。
 * ここに書くのは**それ以外に外したい技**だけ。
 * - 城ボス1: 社長指示2026-09-25「**ジャンプと突進以外の攻撃モーション**」⇒ 突進も外す。
 * - 城ボス3: 社長指示2026-09-25「**ジャンプ以外の攻撃全て**」⇒ 外すのは跳ぶ技だけ(この表に載せない)。
 */
export const GIANT_MOTION_SKIP: Readonly<Record<string, readonly string[]>> = {
  'giantbat': ['g-dash-'],
};

/** その立ち絵で「配らない技」の接頭辞(無ければ空)。 */
export const giantMotionSkipFor = (idleTexName: string | null | undefined): readonly string[] =>
  (idleTexName && GIANT_MOTION_SKIP[idleTexName]) || [];

/**
 * ★**その立ち絵だけ「当たりの相を持たない技」として読む追加分**(社長報告2026-09-25
 * 「**銃をうつモーションが静止画になってる**」)。
 * 城ボス5の通常弾は**半分が扇撃ち**で、扇は `g-bolt-windup` → `g-bolt-recover` と**当たりの相を通らない**
 * (弾は溜め→立ち直りの境目で出る)。攻撃の絵は 1(構え)/6(連射)/1(撃ち終わり)なので、
 * 溜めも立ち直りも**1コマずつの静止画**になり、**連射の6コマが1度も出ていなかった**。
 * ここに載せた技は、立ち直りを**当たり+戻り**(1〜7コマ目)として流す=**弾が出た瞬間から連射が走る**。
 * ★連射撃ち(`g-bolt-burst`)の3発目も立ち直りの頭で出るので、同じ読み方で合う。
 * ★全ステージ共通の `GIANT_NO_ACTIVE_TECHS` には入れない(城ボス1・3・4の通常弾の絵を動かさないため)。
 */
export const GIANT_NO_ACTIVE_EXTRA: Readonly<Record<string, readonly string[]>> = {
  'stage5-enemies/giantbat': ['g-bolt-'],
};

/**
 * ★**当たりの区間を「一定の速さでループ」させる**(社長報告2026-09-25「その他の同じ絵がでる技が
 * 乱射するモーションが**ぎこちなく流れたりする**」)。
 * 城ボス5の攻撃の絵は**銃の連射**(大小の閃光が交互に3回)=**繰り返す動き**なのに、
 * 従来は当たりの6コマを**相の長さへ1回だけ引き伸ばして**いた。相の長さは技ごとにばらばらで、
 * 薙ぎ払い・三連射は**220msに6コマ詰め込み(速すぎ)**、掃射ビームは**900msに6コマ(遅すぎ)**だった。
 * ⇒ 当たりの間は**`frameMs` ごとに1コマ進めてループ**する。速さは、社長が「合ってる」と言った
 *   通常弾(立ち直り300msで7コマ=約43ms/コマ)に揃えた。
 * `exceptTechs` は従来どおり引き伸ばす技(通常弾は社長裁定で今のままが正)。
 */
export const GIANT_ACTIVE_LOOP: Readonly<Record<string, { readonly frameMs: number; readonly exceptTechs: readonly string[] }>> = {
  'stage5-enemies/giantbat': { frameMs: 45, exceptTechs: ['g-bolt-'] },
};

/** その相で当たり区間をループさせるなら1コマの長さ(ms)、しないなら null。 */
export const giantActiveLoopMsFor = (
  idleTexName: string | null | undefined, aiPhase: string | null | undefined,
): number | null => {
  const cfg = idleTexName ? GIANT_ACTIVE_LOOP[idleTexName] : undefined;
  if (!cfg || !aiPhase) return null;
  if (cfg.exceptTechs.some(t => aiPhase.startsWith(t))) return null;
  return cfg.frameMs;
};

/** その立ち絵で「当たりの相を持たない技」として読む追加分(無ければ空)。 */
export const giantNoActiveExtraFor = (idleTexName: string | null | undefined): readonly string[] =>
  (idleTexName && GIANT_NO_ACTIVE_EXTRA[idleTexName]) || [];

/**
 * ★**技ごとに別の薙ぎシートへ差し替える**(社長指示2026-09-25)。
 * 城ボス4は絵が2枚あり、**技によってどちらを使うかが違う**:
 * - 既定(`SWEEP_SHEET_SUFFIX`/`ENEMY_SWEEP_SHEETS`)= 叩きつけの絵。薙ぎ払い・突進・三連突進・ノヴァ・
 *   踏み鳴らしはこちら(社長裁定「絶妙にうまく絵を使ってくれてたから、ちゃんと戻して」)。
 * - ここに載せた技だけ = **腕を振り上げて前へ薙ぐ絵**。社長指示「**吹雪の薙ぎ、これは手を前に出す
 *   モーションで**」「**通常弾飛ばすとき、これも手を前に出す方のモーション流用で**」。
 * 区切りは**白い弧の画素数**で測った(0〜103 → 842 → **1539** → 917 → 325)=6コマ目が振り抜き。
 */
export interface GiantAltSweep {
  readonly techs: readonly string[];
  readonly suffix: string;
  readonly split: SweepSplit;
  /**
   * ★**シートの何コマ目から読むか**(省略=0=先頭から)。`split` はこのコマから数える。
   * 社長指示2026-09-26「**踏み潰し(ジャンプではなくその場で)もジャンプの後半モーション再利用して**」(グレン)。
   * 跳びの絵の**後半だけ**(頂点→降下→着地→立ち直り)を使うため、前半(しゃがみ・踏み切り)を飛ばす。
   * シートの総コマ数 = `from + split の合計` であること(切り分けがずれる)。
   */
  readonly from?: number;
}
export const GIANT_ALT_SWEEP: Readonly<Record<string, readonly GiantAltSweep[]>> = {
  'stage4-enemies/giantbat': [
    {
      techs: ['g-quad-breath-', 'g-bolt-'],   // 氷の横薙ぎ / 通常弾
      suffix: 'attack',
      split: { windup: 6, active: 1, recover: 9, bodyH: 144 },
    },
  ],
  // ★城ボス5は**既定が銃の連射**(`-attack`)。踏み鳴らしだけ**叩きつけの絵**(`-jump`)へ回す
  // (社長支給2026-09-25「ジャンプ及び叩きつけモーション」=跳びと叩きつけが1枚)。
  // 同じ絵を、飛び掛かりは跳びの3区間(4/6/5)、踏み鳴らしは薙ぎの3区間として読む。
  // ★`g-stomp-` は `GIANT_NO_ACTIVE_TECHS` なので **溜め=0〜9 / 立ち直り=10〜14** になり、
  //   **一撃のコマ(10)が立ち直りの頭=当たる瞬間**に来る(城ボス4と同じ合わせ方)。
  'stage5-enemies/giantbat': [
    {
      // 突進も叩きつけの絵(社長指示2026-09-25「ダッシュはジャンプの着地の絵を使って(城4ボスみたいに)」)。
      // 城ボス4と同じ読み方: 溜め=0〜9 / 走っている間(`g-dash-charge`=当たり)=10(着地の一撃) / 立ち直り=11〜14。
      techs: ['g-stomp-', 'g-dash-'],
      suffix: 'jump',
      split: { windup: 10, active: 1, recover: 4, bodyH: 140 },
    },
  ],
  // ★グレン形態1: 踏み潰し(その場で)は**跳びの絵の後半**(社長指示2026-09-26)。跳び 11コマのうち 5〜10 を使う:
  //   **5=腕を広げて一番高い / 6=脚を曲げて降りてくる**(溜め) → **7=一番潰れる(一撃)** → 8〜10=構え直す。
  //   `g-stomp-` は当たりの相を持たない技(`GIANT_NO_ACTIVE_TECHS`)なので、溜め=5,6 / 立ち直り=7〜10 になり、
  //   **一撃のコマ(7)が立ち直りの頭=当たる瞬間**に来る(城ボス4・5と同じ合わせ方)。その場の技なので浮きは付かない。
  //   大きさは跳びの `frameBodyH`(コマ番号はシートの実番号)がそのまま掛かる。
  'glen-boss': [
    { techs: ['g-stomp-'], suffix: 'jump', from: 5, split: { windup: 2, active: 1, recover: 3 } },
  ],
};

/** その相で使う差し替えシート(無ければ null=既定のシートを使う)。 */
export const giantAltSweepFor = (
  idleTexName: string | null | undefined, aiPhase: string | null | undefined,
): GiantAltSweep | null => {
  const list = idleTexName ? GIANT_ALT_SWEEP[idleTexName] : undefined;
  if (!list || !aiPhase) return null;
  for (const alt of list) if (alt.techs.some(t => aiPhase.startsWith(t))) return alt;
  return null;
};

/** その立ち絵が持つ差し替えシート全部(先読み・原盤台帳・描画倍率の照合に使う)。 */
export const giantAltSweepSheets = (
  idleTexName: string,
): readonly { readonly name: string; readonly bodyH: number | null }[] =>
  (GIANT_ALT_SWEEP[idleTexName] ?? []).map(alt => ({
    name: `${idleTexName}-${alt.suffix}`, bodyH: alt.split.bodyH ?? null,
  }));

export const sweepSheetName = (idleTexName: string): string =>
  `${idleTexName}-${SWEEP_SHEET_SUFFIX[idleTexName] ?? 'sweep'}`;

/**
 * ★**薙ぎシートの「絵の中での振り抜き方向」**(社長報告2026-09-23「伐採人の攻撃モーションが、
 * 武器の進行方向と逆に振ってる」)。ミラーしていない素の状態で、**画面のどちら向きに振るか**。
 * `1` = 左から右 / `-1` = 右から左。
 *
 * ★**薙ぎ中のミラーはこの向きで決める。「体がどちらを向くか」ではない。**
 *   武器スプライトは帯(`aiFrom`→`aiTarget`)の上を進むので、**絵の振りが帯と逆になると
 *   「逆に振っている」**に見える(判定は帯のまま=「見たまんまが当たり判定」が崩れる)。
 * ★実測の仕方: 上半身(腕と得物が在る帯)の重心が、コマを追ってどちらへ動くか。
 */
export const ENEMY_SWEEP_SWING_DIR: Readonly<Record<string, 1 | -1>> = {
  // 伐採人。実測: 上半身の重心が **40.6 → 90.1**(左上に構え→下を通って→右上へ抜ける)=左から右。
  'reaper-common': 1,
};

/** 既定は「左から右」(表に無いシートもそう扱う)。 */
export const sweepSwingDir = (idleTexName: string | null | undefined): 1 | -1 =>
  (idleTexName && ENEMY_SWEEP_SWING_DIR[idleTexName]) || 1;

export const sweepSheetSplit = (idleTexName: string | null | undefined): SweepSplit | null =>
  (idleTexName && ENEMY_SWEEP_SHEETS[idleTexName]) || null;

/**
 * ★そのシートの中で「立ち絵の枠」に当たる高さ(無指定=null=シートの枠の高さで揃える=従来どおり)。
 * 枠より本体が低いシート(=技で枠の外へ伸びる部位がある絵)だけがここに値を持つ。
 */
export const sweepSheetBodyH = (idleTexName: string | null | undefined, frame?: number): number | null => {
  const sp = idleTexName ? ENEMY_SWEEP_SHEETS[idleTexName] : undefined;
  if (!sp) return null;
  const f = frame !== undefined ? sp.frameBodyH?.[frame] : undefined;
  return f ?? sp.bodyH ?? null;
};

export const jumpSheetSplit = (idleTexName: string | null | undefined): JumpSplit | null =>
  (idleTexName && ENEMY_JUMP_SHEETS[idleTexName]) || null;

/**
 * 跳びシートの「立ち絵の枠に当たる高さ」(無指定=null=枠の高さで揃える=従来どおり)。
 * ★`frame` を渡すと**着地の区間だけ別の値**(`landBodyH`)を返す
 * ——城ボス3の跳びは着地の5コマだけさらに小さく描かれていた(社長報告2026-09-25)。
 */
export const jumpSheetBodyH = (
  idleTexName: string | null | undefined, frame?: number,
): number | null => {
  const sp = idleTexName ? ENEMY_JUMP_SHEETS[idleTexName] : undefined;
  if (!sp) return null;
  const f = frame !== undefined ? sp.frameBodyH?.[frame] : undefined;
  if (f !== undefined) return f;
  const land = sp.landBodyH;
  if (land !== undefined && frame !== undefined && frame >= sp.crouch + sp.air) return land;
  return sp.bodyH ?? null;
};

export const jumpLandMs = (idleTexName: string | null | undefined): number =>
  (idleTexName && ENEMY_JUMP_LAND_MS[idleTexName]) || 420;

/**
 * ★その立ち絵が**手で描かれた絵を1枚でも持っているか**(=ミラーの対象か)。
 *
 * 社長裁定2026-09-21「**全敵、アニメーション入れる予定なのでミラーさせます / 少しずつ揃えていくので
 * 個々実装**」+ 社長指示2026-09-22「**ミラーは全ての敵で適用します**」。
 * ミラーしないと脚も振りも進行方向と逆を向く。だが素材は1体ずつ届くので、**型ではなく個体で**
 * 判定する(同じバットでも、シートのある女はミラーし、まだ無い男は従来どおり型の設定のまま)。
 *
 * ★**6つの表のどれか1つでも載っていれば対象**(v0.25.4566 / 薙ぎを追加 v0.25.4575)。旧実装は歩き/攻撃だけを見ており、
 * **待機だけ・弾だけ・跳びだけの絵を持つ個体(プラント/抱卵型)が漏れていた**。
 * 「全ての敵」に例外を作らないため、手で描かれた絵を持つ個体は全部ミラーする。
 */
export const hasAnimSheet = (idleTexName: string | null | undefined): boolean =>
  walkSheetFrames(idleTexName) > 1 || attackSheetFrames(idleTexName) > 1
  || idleSheetFrames(idleTexName) > 1 || shotSheetFrames(idleTexName) > 1
  || jumpSheetSplit(idleTexName) !== null || sweepSheetSplit(idleTexName) !== null;

/** その立ち絵のシートが右向きか(シートが無ければ false=既定の左向き)。 */
export const sheetFacesRight = (idleTexName: string | null | undefined): boolean =>
  !!(idleTexName && ENEMY_SHEET_FACES_RIGHT[idleTexName]);

/**
 * ★★**シートを起動時に読むか、必要になってから読むか**(社長指示2026-09-24「**乗せて**」)。
 *
 * ★なぜ要るか: `pixiTextures` の**起動マニフェストは上の7つの表のキーをそのまま全部展開する**ので、
 * **表に1行足した瞬間、そのシートは全ステージで起動時に常駐する**。ボスは**そのステージにしか出ない**
 * (裏ボスはS1ミーミル/S3ヨルムンガンド/S4スカディ/S5トール・城ボスもステージに1体)のに、
 * ステージ1で遊んでいる間ずっと他ステージのボスの絵を抱えることになる。
 * **ボスの立ち絵は既に遅延組**(`DEFERRED_SPRITE_GROUPS`)なのに、**シートだけがその外に在った。**
 *
 * ★**外れた時に何が起きるか(=遅延にしてよい根拠)**: シートが間に合わないと `sheetSlices()` が
 * `null` を返し、描画は**立ち絵へフォールバックする**。**敵が消えることはなく「一拍だけ動かない」**。
 * 立ち絵の遅延(外すと絵そのものが出ない)より**軽い失敗**なので、判断の敷居はそこまで高くない。
 *
 * ★**決め方は「登場に猶予があるか」**。カットイン/アテンションを挟んで出る個体は遅延でよい
 * (既存の遅延組と同じ理由=寄り360ms+ホールド950ms+カットイン1100msの2.4秒で読み終わる)。
 * **前触れなくその辺に居る敵**(雑魚・強個体)は猶予が無いので**起動時のまま**。
 *
 * ★**書き忘れ・書き間違いはテストが止める**(`sheetResidency.test.ts`):
 * ①**常駐1.0MBを超えるシートを持つ立ち絵は、この表に必ず載っていること**
 * ②**この表のキーは実在するシートを持つこと**(打ち間違えると黙って起動時常駐に戻るため)。
 */
export type SheetResidency = 'deferred' | 'eager';
export const SHEET_RESIDENCY: Readonly<Record<string, SheetResidency>> = {
  // 死神(`reaper`)。歩き3.34MB。気配演出→カットインを挟んで出る。
  'reaper2-common': 'deferred',
  // ▼ハンター変異体(歩き0.82+跳び1.68=2.50MB)は**起動時のまま**。一度 'deferred' にしたが、
  //   品質監査2026-09-24 の指摘で戻した。理由は2つ:
  //   ①**全ステージで出る**ので、遅延にしても「そのステージのぶんだけ」にならない=常駐は減らない。
  //     社長の前提「ボスはステージで出る種類が限られてる」が、この個体にだけ当てはまらない。
  //   ②**M0(訓練)は予告なしで画面内に出る**(`useGameLoop` の `beat.id === 'hunter'`。
  //     `hunterAlerted=true` で即追跡・`triggerAttention` を呼ばない)。**新規プレイヤーが初めて見る
  //     ハンター**で絵が一拍遅れる。通常ステージのイベント側にはカットインが在るが、M0には無い。
  'hunter': 'eager',
  // 城ボス(ステージ1の搬送体)。歩き0.68+跳び0.95+攻撃0.61=**2.24MB**。カットインを挟んで出る。
  // ★このシートはステージ1の絵(`giantbat`)。S3/4/5の城ボスは別名に解決されるので出ない。
  'giantbat': 'deferred',
  // ステージ3の城ボス。歩き1.54+跳び1.22+叩きつけ1.68=**4.44MB**。
  // カットインを挟んで出る+**ステージ3でしか出ない**ので、まるごと遅延。
  'stage3-enemies/giantbat': 'deferred',
  // ステージ4の城ボス。歩き1.51+跳び1.73+攻撃1.58=**4.82MB**。
  // カットインを挟んで出る+**ステージ4でしか出ない**ので、まるごと遅延。
  'stage4-enemies/giantbat': 'deferred',
  // ステージ5の城ボス。歩き1.56+攻撃0.93+跳び/叩きつけ1.24=**3.73MB**。
  // カットインを挟んで出る+**ステージ5でしか出ない**ので、まるごと遅延。
  'stage5-enemies/giantbat': 'deferred',
  // グレン形態1。浮遊3.70+攻撃1.30+跳び2.21=**7.21MB**(全部 高さ192)。ステージ7でしか出ず、出現にカットインを挟むので遅延。
  'glen-boss': 'deferred',
  // ▼ここから下は**起動時のまま**。理由はどれも同じ=**前触れなくその辺に居る**(猶予が無い)。
  'pumpkin-common': 'eager',   // 蜘蛛。跳び1.15MB
  'driller-common': 'eager',   // 削岩型。歩き1.12MB
  'reaper-common': 'eager',    // 伐採人(旧・死神の絵)。歩き1.02MB
  'lab-zombie/lab-zombie-lv1-female': 'eager', // 研究所ゾンビLv1(女)。歩き1.03MB。休眠したまま最初から画面に居る
  'lab-zombie/lab-zombie-lv3': 'eager', // 研究所ゾンビLv3。歩き1.43MB+跳び1.30MB。同じく休眠したまま最初から地図に置かれている
};

/** そのシート群を起動時に読まない(=遅延組へ回す)か。表に無い立ち絵は**起動時**(既定)。 */
export const sheetDeferred = (idleTexName: string): boolean =>
  SHEET_RESIDENCY[idleTexName] === 'deferred';

/** 7つのシート表を「立ち絵名 → シート名」の1本の並びにする(起動側と遅延側が**同じ並びを2つに割る**)。 */
export const allEnemySheets = (): { idle: string; sheet: string }[] => [
  ...Object.keys(ENEMY_WALK_SHEETS).map(idle => ({ idle, sheet: walkSheetName(idle) })),
  ...Object.keys(ENEMY_ATTACK_SHEETS).map(idle => ({ idle, sheet: attackSheetName(idle) })),
  ...Object.keys(ENEMY_SHOT_SHEETS).map(idle => ({ idle, sheet: shotSheetName(idle) })),
  ...Object.keys(ENEMY_IDLE_SHEETS).map(idle => ({ idle, sheet: idleSheetName(idle) })),
  ...Object.keys(ENEMY_JUMP_SHEETS).map(idle => ({ idle, sheet: jumpSheetName(idle) })),
  ...Object.keys(ENEMY_SWEEP_SHEETS).map(idle => ({ idle, sheet: sweepSheetName(idle) })),
  ...Object.keys(ENEMY_SCREAM_SHEETS).map(idle => ({ idle, sheet: screamSheetName(idle) })),
  // ★差し替えシート(技ごとに別の絵を使う個体)。ここに入れないと**先読みも原盤台帳の照合も漏れる**。
  ...Object.keys(GIANT_ALT_SWEEP).flatMap(idle => giantAltSweepSheets(idle).map(a => ({ idle, sheet: a.name }))),
];
