# the ONE ストーリー情報UI・UX仕様

最終更新: 2026-07-15 JST  
状態: 採用済み仕様  
対象: ストーリー選択画面、ステージ内通信、リザルト画面、資料室

## 1. 目的

ストーリー情報を一つの画面へ詰め込まず、プレイヤーが必要な時点で必要な量だけ受け取れる構造にする。

資料室は本筋理解の必須導線ではなく、世界観・科学設定・任務背景へ深みを与える任意の補完機能とする。

## 2. 情報導線

1. 出撃ページ = 任務の目的と特殊条件
2. プレイ中通信 = 現在取るべき行動
3. クリア後リザルト = 今回判明した本筋情報と次任務への接続
4. 資料室 = 発見済み情報の詳細・読み返し

本筋に必須の情報を資料室だけへ置かない。

## 3. 出撃ページ

### 役割

出撃前に理解していないと、任務目的や特殊ルールが分からない情報を表示する。

### 表示項目

- 任務名
- 短い状況説明
- 任務目標
- 特殊条件
- 特殊支給装備
- 必要に応じて短い出撃前会話

### 文字量

- 状況説明: 2〜3短文
- 任務目標: 1文
- 特殊条件: 短いラベル表示
- 長い世界観説明や研究記録は置かない

### M2表示例

**壊滅研究所・資料回収**

変異体発生源となった研究所へ単身潜入する。  
内部個体には通常火器がほぼ通用しない。  
支給されたPHILLガンを使用し、研究資料を回収して帰還せよ。

特殊条件:

- 単身潜入
- PHILLガン固定
- 弾薬有限
- 敵の殲滅は不要

## 4. プレイ中通信

### 役割

設定説明ではなく、現在の行動と緊張感を短く補強する。

### ルール

- 一度に1〜2短文
- 一つの情報だけ伝える
- その時点で通信者が知っている内容だけ話す
- 出撃ページの説明をそのまま繰り返さない
- 重要な核心を通信だけに依存させない
- 通信内容は後からログで確認可能にする

### M2表示例

> 「資料だけ取って戻れ。深追いするな」

M2はプレイヤー単独のため、NPC同行会話は置かない。冒頭または必要箇所の短い通信のみ使用する。

## 5. クリア後リザルト画面

### 役割

今回の任務で判明した本筋情報を、資料室を読まないプレイヤーにも必ず伝える。

既存のスコア・報酬表示に「任務報告」欄を追加する。

### 推奨レイアウト

```text
任務クリア

スコア
報酬

任務報告
・今回判明した情報
・次の任務へつながる情報

［回収資料を見る］
［次へ］
```

### 任務報告のルール

- 2〜4短文
- 今回分かったことを一つ
- 次の疑問または次任務への接続を一つ
- 専門的詳細や長文は置かない
- 初回クリア時は必ず表示
- 再クリア時は再表示してよいが、新規解放処理は重複させない

### M2表示例

**任務報告**

施設は軍の再生医療研究所だった。  
壊滅直前のデータには、通常の治療では説明できない異常値が残されている。  
別地域の共同研究所との通信記録を確認。生存者の捜索を開始する。

### ボタン動作

#### 回収資料を見る

- 今回解放された資料だけを一覧表示する
- 閉じるとリザルト画面へ戻る
- この場で読まなくても進行可能
- 閉じた資料は資料室からいつでも読める

#### 次へ

- 次の任務またはステージ選択画面へ進む
- 未読資料があっても進行を止めない

## 6. 資料室

### 役割

発見済み情報の補完と再読。世界観に深みを出す。

### 推奨カテゴリ

- 変異体
- 武器・特殊装備
- アイテム
- 用語
- 任務記録

### 任務記録

各ステージで回収した詳細資料を保存する。

M2例:

- 軍再生医療計画
- PHILL計画記録
- 異常増殖データ
- リモート共同研究所との通信履歴

### 未読表示

- 新規解放されたカテゴリまたは資料に未読マークを付ける
- 資料を開いた時点で既読にする
- 未読があっても本編進行を妨げない

## 7. データの一元管理

同じ情報を、リザルト画面用と資料室用に別々の文面として重複管理しない。

ステージごとに次のデータを持たせる。

```ts
type StoryStageInfo = {
  stageId: string;
  missionTitle: string;
  preBrief: string[];
  objective: string;
  specialConditions?: string[];
  startComms?: string[];
  clearReport: string[];
  unlockedRecordIds: string[];
  nextStageId?: string;
};
```

資料側は別IDで管理する。

```ts
type ArchiveRecord = {
  id: string;
  category: "mutant" | "weapon" | "item" | "term" | "mission";
  title: string;
  body: string[];
  unlockStageId?: string;
};
```

必要な状態:

```ts
type StoryArchiveState = {
  clearedStageIds: string[];
  unlockedRecordIds: string[];
  readRecordIds: string[];
  latestUnlockedRecordIds: string[];
};
```

## 8. 初回クリア時のUXフロー

1. ステージクリア
2. スコア・報酬計算
3. 任務報告を自動表示
4. 回収資料を解放
5. 「回収資料を見る」または「次へ」を選択
6. 読まなかった資料には未読マーク
7. 次のステージを解放
8. ステージ選択画面へ戻る

## 9. 再プレイ時の扱い

- 任務報告は再表示可能
- 同じ資料を重複解放しない
- 未読・既読状態を維持
- 初回限定の演出がある場合は別フラグで管理
- ストーリー分岐や条件付き資料は、条件成立時のみ追加解放

## 10. PHILL情報の配置方針

現時点ではPHILLの具体的な命名文面は未確定。

配置枠のみ固定する。

- PHILLガンを使う理由: M2出撃ページ
- 操作・弾薬制限: 出撃ページまたは専用チュートリアル
- PHILLが感染前の研究でも使われていた証拠: M2回収資料
- 本筋として最低限必要な発見: M2クリア後の任務報告
- 詳細な研究記録: 資料室
- PHILL＝フィルの回収: M7本編

## 11. 非採用

- 資料室を読まないと本筋が理解できない構造
- クリア後の情報を見るために同じステージ選択ページを再度開かせる構造
- 出撃ページへ任務記録や科学説明を大量に置く
- プレイ中通信で長い研究説明を行う
- リザルト画面と資料室で同一内容の別文章を管理する
- 杦桐説明欄をPHILLガン一つのためだけに新設する
- 未読資料を読むまで次へ進めない設計

## 12. 実装優先順位

1. リザルト画面へ「任務報告」欄を追加
2. ステージ情報データに`clearReport`と`unlockedRecordIds`を追加
3. 「回収資料を見る」導線を追加
4. 資料室へ任務記録カテゴリと未読管理を追加
5. 出撃ページの任務概要・特殊条件表示を統一
6. 通信ログの読み返し対応
7. 各M1〜M7の文面を順次実装

## 13. 受け入れ条件

- 資料室を一度も開かなくても、M1〜M7の本筆を理解できる
- クリア後にステージを再選択せず、今回判明した情報を確認できる
- 詳細資料は任意で読める
- 未読資料は後から判別できる
- 同じ記録の二重管理がない
- M2で単身潜入、PHILLガン、資料回収目的が出撃前に理解できる
- M2クリア後に「異常データとM3への導線が必ず伝わる
