# 二人組(クエストNPC)クエスト設計 — 「特定変異種のサンプルが欲しい」/「とにかくサンプルを集めてきて」

社長指示(v0.25.1684直後)+全裁定(v0.25.1686)を反映した**確定仕様・実装済み**。
**会話内容は後で詰める(社長)=当面は仮テキスト**(EVENT_QUEST_LINES / EVENT_QUEST_LINES_SUB)。
受領/納品のUIはサークル内3秒滞在(白メーター・v0.25.1684の仕組み)。「直前のやり取り以降に一度
サークルを出るまでメーターは進まない」ガード(leftSinceAccept)を受領/納品/次受付の全てに適用。

## 確定仕様(社長裁定済み・実装どおり)

### 1) 強制クエスト「特定変異種のサンプルが欲しい」
- **強制として課すのはステージ1のみ**(社長指示)。ステージ3/4/5はフラグ構造は持つが
  **最初からクリア済み扱い**(config.forced=false)=サブのみ発生。
- 受けると**二人と反対側(原点から方位角+180°)の研究対象区域(距離2000〜2600)にネームド出現**。討伐が条件。
- ネームドの型=**パンプキンか犬(werewolf)**(受領時にランダム1体・裁定#1)。宿敵と同じ個体強化
  (HP×2/与ダメ×2/サイズ×1.5・金tint・神話名プールから名前)だが**別フラグ questTarget**=宿敵システムと分離。
  休眠配置(aggroRange=320・叩き台)=近づくと起動。カリング/距離リサイクル/イベント一掃の対象外。
- **誘導**: 注意誘導(attention)はしない(裁定#7)。**拠点と同じ**「近く(500px以内)+画面外の時だけ縁矢印」
  (金色・pixiScene.syncArrows)。画面内は本体(金tint+名前)がマーク。
- **次ステージ解放 = 城ボスクリアフラグ && 強制クリアフラグ**(裁定#4。城ボスの出現ゲートは無し=7分出現は従来どおり)。
  - 城ボスフラグ: giantbat(イベント産fromEvent除く)討伐の瞬間に永続記録(年表と同じA方式)。
  - 強制フラグ: 納品の瞬間に永続記録。
  - どちらが後でも、揃った瞬間に markStageCleared(=次ステージ出現)。勝利(帰還)そのものでは解放しない。
  - クエスト設定の無いステージ(2/6/7/ex)は従来どおり勝利時に解放。

### 2) サブ「とにかくサンプルを集めてきて」
- **強制クリア後**に受注可(3/4/5は最初から受注可)。**完了は1度きり**(裁定#3=受注のみで死亡したら次runで再受注可)。
- 目標(裁定#2: レア=青、あとは紫・赤の記載どおり):
  | ステージ | 目標 |
  |---|---|
  | ステージ1 | 敵の討伐 10体(全キル) |
  | ステージ3 | レア敵(青個体) 5体 |
  | ステージ4 | 紫敵(紫個体) 5体 |
  | ステージ5 | 赤敵(赤個体) 5体 |
- **進捗表示**(裁定#8): 右上スクラップ下に短く「🧪 変異種討伐 0/1」「🧪 サンプル集め n/N」。達成で緑+「納品」。

### 3) 納品・報酬・完了後
- 目標達成後、二人のサークルで再度3秒滞在=納品。**報酬=ゴールド100**(裁定#5・強制/サブ各)。
- 強制納品後は二人は残り、そのままサブの受付へ(一度離れて再滞在3秒で受注)。
- **サブ納品後は以後そのステージに二人は出現しない**(永続)。そのプレイ中は立ち姿のまま(何も起きない)。
- **クエスト設定の無いステージには二人は出現しない**(裁定#6)。

## 実装マップ(v0.25.1686)
- `src/utils/eventQuest.ts`(+test): 設定テーブル(EVENT_QUEST_CONFIG)・ネームド位置/型抽選・キル進捗判定(純関数)。
- `src/data/progress.ts`: `zombie.progress.eventQuestMeta`({forced,sub}のステージ毎Map)・
  `zombie.progress.castleBoss`(城ボスフラグ)・`syncQuestStageClear`(両フラグ→markStageCleared)。
  旧v1684キー(eventQuestDone)は読まずに廃棄。resetProgressで全て消える。
- `src/store/gameStore.ts`: run内状態(eventQuestActive/Kills/GoalCount/GoalTier)・acceptEventQuest(種別決定+
  ネームド湧き)・completeEventQuest(報酬+永続化+分岐)・キル進捗フック(damageEnemy kill分岐/
  grantMeleeKillRewards)・城ボスフラグ(triggerDramaticDeath)・イベント一掃の除外・resetGameの出現ゲート。
- `src/hooks/useGameLoop.ts`: 滞在の状態機械(受領/納品・目標達成ゲート・台詞出し分け)。
- `src/pixi/pixiScene.ts`: questTargetの金tint+名前・縁矢印マーク。
- `src/components/GameHUD.tsx`: EventQuestPill(右上スクラップ下 n/N)。
- `src/App.tsx`: handleVictory=クエストステージはフラグ同期、他は従来どおり。
- `src/utils/enemyUtils.ts` / `src/utils/directorTick.ts`: questTargetの演出/カリング除外。

## 残タスク(社長)
- **会話テキスト**(受領/納品/サブ受領。現状は仮テキスト)。
- 叩き台の実機調整: ネームド位置(2000-2600)・起動距離(320)・HUD文言(「変異種討伐」「サンプル集め」)。

---


# v2(社長指示 2026-08-21): 二人組クエスト = レスキューイベント化 — 確定仕様

**この章がv2の正本**(発注可能な確定仕様1本)。上のv1章は **v2以前の実装の記録**として残してある——
v1章は「過去にこう作った」の説明であって v2 の仕様ではない。**食い違ったらこの章が正**。
案件の進行状態(どこまで実装されたか・誰のボールか)は **PROJECT_STATUS.md** を見る(設計書に状態は書かない)。

裁定は本文へ溶かしてある(「裁定が出るたび追記」はしない)。残る不明点は **§2-15 ★未決事項**に集約。

**この章では行番号を一切書かない**(実在確認はシンボル名で行う。行番号は数版で必ずずれ、実装者を誤らせるため)。

---

## 2-0. 1枚まとめ(この章で発注するもの)

未クリアランの本編動線を **「拠点確保(S5のみ)→ レスキューイベント → 城ボス → 納品して帰還」** の一本にする。
二人組は「道端に最初から立っている受注NPC」ではなく、**4分に襲われているところを助ける相手**になり、
クエストの中身は **「城ボス(搬送体)を倒してサンプルを持ち帰る」** 1本に統合される。

```
0:00 ──────── 4:00 ──────────────── 5:00 ─────────── 城ボス撃破 ──── 納品 ── リザルト
     二人組不在    レスキュー出現      城ボス出現        帰還サークル1つ    3秒滞在   会話キューが
     城ボスも不在   (制限時間なし)     ※レスキュー未完了 ＋二人がワープ    ＝納品成立  空になったら
                   サークル進入=囲い    ならその分後ろへ  してくる          ＋報酬     gameWon
                   赤い削岩型＋数体     押していく                         ＋クリア記録
                   全滅 → 受注会話
```

### ★★ブロッカーは**全て解消した**(2026-08-30)= **発注可能**

> **★2026-08-30 でブロッカーは全て解消した。** #5「納品成立の契機」は 2026-08-22 に裁定済み
> (「サークル3秒で。」=(A))、#16「囲いの発火6条件のゲート」は 2026-08-30 に裁定済み
> (**6条件を撤去して発火 / 裏ボス中だけ非表示 / 位置で他イベントの範囲を避ける**)。
> **#21「発火枝のゲートの置き場所」は #16 の前提ではなくなった**——ゲートそのものが無くなったため。
> **枝の中に置く**(本文の確定)は**構造の担保として引き続き有効**で、
> 「レスキューの円に入っている間は後続の `else if` へ落ちない=他の囲いが足元で開かない」を守る。

| ブロッカー | 何が決まっていないか | **止まる範囲(ここだけ)** | 決まらないと何が起きるか |
|---|---|---|---|
| ~~★未決 #16「囲いの発火6条件のゲート」~~ | ✅ **裁定済み(社長2026-08-30)= 6条件は撤去して発火。裏ボス中だけ非表示。位置は他イベントの範囲と重ねない** | — | — |
| ~~★未決 #5「納品成立の契機」~~ | ✅ **裁定済み(社長2026-08-22「サークル3秒で。」= (A) サークル内3秒滞在)** | — | — |

> **ブロッカーは無い**(#5=2026-08-22 裁定 / #16=2026-08-30 裁定)。★未決は全部で**21件**=#1「レスキュー囲いの中身」・#2「赤い削岩型をS1/S3に出すか」・#4「レスキュー出現の告知と誘導」〜#22「スケーター乗車中の納品」。#3 は欠番。
> 残る**19件**は「実装を止める」ものではない=本文の現行仕様のまま実装してよく、
> 裁定が出たらその値/見せ方を差し替える)。
> ※ただし **#21「発火枝のゲートの置き場所」(発火枝の書き方)は #16「囲いの発火6条件のゲート」の材料の前提**なので、**#16「囲いの発火6条件のゲート」より先に決める**
> (本文は「枝の中に置く=落とさない」で確定済み=**実装は止まらない**)。

**★この章の完了判定はすべて「イベント側の事実」で決まる。会話には依存しない。**
レスキュー完了=**敵の全滅の瞬間**、納品成立=**3秒滞在が満了した瞬間**。会話はその後キューへ流すだけで、
**原稿が0行でも(=S5)判定は同じように成立する**。

---

## 2-1. 適用範囲(どのランで出るか)

- **対象ステージ = S1 / S3 / S4 / S5**(= `EVENT_QUEST_CONFIG` に設定のあるステージ)。
- **設定の無いステージ(2/6/7/ex)は、そもそも城ボスが出ないステージ**(実在確認):
  stage-2=`theme:'lab'`(labTheme) / stage-6=`corridorMode` / stage-7=`storyBossOnly`(storyBoss) /
  stage-ex=`isExStageRun()` のいずれかで、`useGameLoop` の城ボス出現ブロックが丸ごと除外されている。
  よって **この改修で変わるものが1つも無い**(「従来どおり5:00に出る」ではない=元から出ない)。
- **通常出撃のみ**。次のランでは1つも出さない(実在する除外経路で書く):
  練習ラン(`isPracticeRun`)/ ボスメーカー(`isBossMakerRun`)/ ガントレット(`isGauntletRun`)/
  ベンチ(`benchmarkRun`)/ 洋館通路(`corridorMode`)/ 訓練M0(`tutorialStage`)/ 屋内・ラボ(`indoorMode`・
  `stageTheme==='lab'`)/ EX(`isExStageRun`)/ 再訪(`revisitRun`)/
  **フリー(周回)出撃(`getSelectedFreeMode()`)**。
  - **フリー出撃を外す理由(実在確認)**: `App.tsx` の勝利処理は `if (stageId && !getSelectedFreeMode())` で
    フリーを進行から明示的に外している。フリーでレスキューを出すと、①未納品ステージでは城ボスが
    ゲートされて周回が詰み ②納品すると `delivered` と `markStageCleared` がフリー出撃なのに書き込まれる。
    **「フリー=進行に影響させない」を壊さないため、フリーでは出さない**(既存方針と一致)。
  - ※v2の旧稿にあった「革命ラン」は**現行コードに実体が無い**(呼称のみ)。上の実在する除外で足りる。
- **納品済み(クリア済み)ステージ**: レスキューは出ず、二人も出ない(**消滅は永続**)。城ボスは通常の5:00。

### ★この除外を「どこに書くか」(実在確認・書き足す先を1箇所に決める)

**事実(実在確認)**: 二人組の出現ゲートは `src/store/gameStore.ts` の `resetGame` の中の1式だけで、
中身は **`const qGone = bossMakerRoom || isPracticeRun() || !qCfg || getEventQuestMeta(getSelectedStageId()).sub;`**
(**★v3訂正・監査C16: 4条件**。v2本文は「3条件」「練習を1つも見ていない」と書いていたが、
**練習ランは `isPracticeRun()` で既に除外済み**)
(true なら `{ ...createEventQuestNpc(), status: 'gone' }` を入れる)。
→ **見ているのは「ボスメーカーの部屋」「クエスト設定の有無」「納品済み」の3つだけ**で、
**練習/ガントレット/ベンチ/通路/訓練/屋内/ラボ/EX/再訪/フリーは1つも見ていない**。
このままだと上の除外は**どこにも実装されない**(「§2-1 に書いたから入っている」にならない)。

- **確定: `resetGame` の `qGone` にそのまま条件を足す**(判定を2箇所に分けない)。足す条件は
  **`resetGame` のスコープに実在するもの**で書く(同関数内のローカル/同ファイルの import):
  - `isPracticeRun()`(`src/utils/bossPractice.ts`)/ `isGauntletRun()`(`src/utils/gauntletMode.ts`)/
    `isExStageRun()`(`src/utils/exStage.ts`)/ `getSelectedFreeMode()`(`src/data/progress.ts`)
  - `state.benchmarkRun`(store のフラグ)
  - `resetGame` 内のローカル: **`corridorMode`** / **`indoor`** / **`stageTheme === 'lab'`** /
    **`farBackdrop === 'tutorial'`**(=訓練M0。`useGameLoop` の `tutorialStage` と同じ式)/
    **`state.pendingRevisit`**(=再訪。同関数が `revisitMode` に入れている値)
  - `bossMakerRoom` は**既存のまま残す**。
- **`.sub` は §2-10 のとおり `delivered` へ差し替える**(この行はその差し替え対象の1つでもある)。
- **`isBossMakerRun` / `isPracticeRun` は `resetGame` から既に呼ばれている**(商人・護衛の除外で使用)ので、
  新しい import を増やすのは残りだけ。

### ★★「このランでクエストが有効か」の**唯一の出どころ**(5箇所が同じ1本を読む)

**この述語は少なくとも5箇所が必要とする**(下表)。箇所ごとに別の式を書くと、この設計書が3回警告している
「**同じ判定を2箇所に書くと必ず片方だけ古くなる**」(v0.25.2383/2387/2389 の型)を**新規に5箇所で作る**ことになる。

- **確定: 「このランでクエストが有効か」= `eventQuestNpc.status !== 'gone'` の1本だけ。**
  - **理由(実在確認)**: 上の `qGone` が true の時、`resetGame` は
    **`{ ...createEventQuestNpc(), status: 'gone' as const }`** を入れている。
    つまり **`qGone` の結論はそのまま `status === 'gone'` として store に残っている**ので、
    除外条件(11個)をもう一度書き直す必要はない。**除外を評価するのは `resetGame` の1箇所だけ**で、
    以後は全員がその結果(`status`)を読む。
  - **納品済みステージ**(`delivered`)も同じ経路で `gone` になる=別条件を足さなくてよい。
  - **`getEventQuestConfig(...)` だけで判定してはいけない。** 設定はステージ(S1/S3/S4/S5)にしか
    紐づいていないので、**フリー出撃・練習・ベンチ・ボスメーカーでも true になる**。
    とくに**城ボスのゲート**(§2-6)をこれで書くと、**フリー/練習で城ボスがゲートされて詰む**。

| # | この述語を読む場所 | 読まないと何が壊れるか |
|---|---|---|
| 1 | `resetGame` の `qGone`(=**唯一の書き手**) | — (ここが決める) |
| 2 | 城ボスの出現ゲート(§2-6 `questGateOk`) | フリー/練習/ベンチで城ボスが出ず詰む |
| 3 | HUD の黄色い行(§2-7) | クエストと無関係なランで討伐目標が出る |
| 4 | レスキュー地点の縁矢印(§2-9 `syncArrows` へ渡すか) | 対象外ランで矢印が出る |
| 5 | 素通り防止(§2-8 `requestStoryReturnPrompt` / `updateReturnPhase`) | 対象外ランで「帰還しますか?」が出なくなる=帰れない |

- **書き方**: `useGameStore.getState().eventQuestNpc.status !== 'gone'`(HUD は §2-7 の購読形で読む)。
  **この式以外で「クエストランか」を判定しない。**

**受け入れ条件**
1. 練習/ボスメーカー/ガントレット/ベンチ/通路/訓練/屋内/ラボ/EX/再訪/**フリー**のいずれでも、
   レスキュー地点も二人組も1度も出現しない。
2. **フリー出撃では `delivered` / `markStageCleared` の永続書き込みが1つも起きない**
   (フリーで城ボスを倒して帰っても、次ステージが解放されない=改修前と同じ)。
3. stage-2/6/7/ex では城ボスの出現有無・帰還サークル・解放条件が**この改修の前後で1つも変わらない**。
4. 納品済みステージへ再出撃すると、レスキュー地点が出ず城ボスが5:00に出る。
5. **除外の判定が `resetGame` の `qGone` の1式だけに書かれている**(同じ判定が useGameLoop 等にも
   書かれていない=片方だけ古くなる形になっていない)。
6. **練習ラン/フリー出撃で二人組が出ない**(=`qGone` に条件を足し忘れて、実際には
   `bossMakerRoom || !qCfg || 納品済み` の3条件しか見ていない、が起きない)。
7. **★「このランでクエストが有効か」を判定している式が、コードベースに `status !== 'gone'` の形以外に
   1つも無い。** とくに **城ボスのゲート(§2-6)が `getEventQuestConfig(...)` の有無だけで判定していない**
   (=**フリー出撃/練習ランで城ボスが5:00に出て、従来どおり帰還できる**)。
8. **フリー出撃・練習ラン・ベンチ・ボスメーカーで、城ボス・帰還サークル・「帰還しますか?」の確認・
   縁矢印・HUD が、この改修の前後で1つも変わらない。**

---

## 2-2. タイムライン(未クリアラン・S1/S3/S4)

| 時刻 / 契機 | 起きること |
|---|---|
| 0:00 | 二人組は場に居ない。城ボスも出ない(城は従来どおり最初から在る) |
| **4:00** | **レスキュー地点が出現**(プレイヤー近く・**制限時間なし**=消えない) |
| プレイヤーがサークルに入る | **囲いイベント開始**(赤い削岩型+数体)。全滅で終了 |
| **全滅の瞬間** | **`rescueClearedAt` を打刻 = レスキュー完了**。同じ瞬間に受注会話をキューへ流す(会話は判定に関与しない) |
| **5:00 と `rescueClearedAt` の遅い方** | **城ボス出現**(既存の出現演出・アテンション・カットインのまま) |
| 城ボス討伐 | **帰還サークルが1つ**(従来の場所=城跡付近)+**二人がそこへワープしてくる** |
| サークル内3秒滞在の満了 ※**★未決 #5「納品成立の契機」の裁定待ち** | **納品成立** = 永続記録 + 報酬付与口 + **納品ロック**(入力を無視+被弾を無効。**`isPaused` は使わない**・§2-8)+ 完了会話をキューへ |
| 完了会話が**最後まで流れ切った**時(表示中の1行も無く、キューも空。0行なら即時) | **リザルト(`gameWon`)** — 追加の滞在も入り直しも不要。**判定式は `npcDialogue === null` かつ `npcDialogueQueue.length === 0`**(§2-8。キューの長さだけを見ると**最終行を2.8秒読ませずに飛ぶ**) |

**S5だけ**: 「4:00」が **「4:00 と 拠点2か所確保(ラッチ) の遅い方」** になる(§2-11)。

**受け入れ条件**
1. 4:00 未満ではレスキュー地点も二人組も出ない。
2. レスキューを完了しない限り城ボスは出ない(5:00 を過ぎても出ない)。
3. レスキュー完了が 5:00 より前なら城ボスはちょうど 5:00 に出る。
4. **会話原稿が0行のステージ(S5)でも、全滅すれば城ボスが出る**(会話が無いせいで完了判定が
   止まらない=S5が勝てなくならない)。
5. **納品後に会話が0行でも `gameWon` に到達する**。
5b. **原稿のあるステージでは、完了会話の最終行が最後まで表示されてからリザルトへ行く**
   (最終行が出た瞬間に飛ばない/会話を1行も出さずに飛ばない・§2-8)。
6. **納品後の会話が実時間で進み、最終行の表示が終わった時点でリザルトへ行く。**
   納品ロック中も**シミュレーションは動いたまま**で、「ゲームが止まって会話が1行も進まない/
   リザルトへ行けない」状態にならない(=`isPaused` を使うとこれが起きる・§2-8)。

---

## 2-2B. 二人組(`eventQuestNpc`)の状態遷移 — **v1の滞在受注経路は殺す**

**★v2で最初にやること**: v1の「サークルに3秒滞在すると受注」の状態機械を**生かしたまま v2 を足してはいけない**。
4:00 に出た二人へ近づいた瞬間、囲いが始まる前に **v1の受注が成立**してしまう(ネームド湧き・v1の受注会話・
HUDピルまで発火しうる=二重受注)。

**v2の状態遷移(型の拡張が要る。現行の `EventQuestStatus` は `available | accepted | completed | gone` の4値)**

| 状態 | いつ | 二人の見え方 / 触れるか |
|---|---|---|
| `hidden` | 0:00〜レスキュー出現まで / **裏ボス4体が場に居る間(★社長裁定2026-08-30「裏ボス中だけ非表示で。」)** | 場に居ない(**描画なし+`syncLocalEventLighting` の走査からも外す**・下記)。**円も出ない=入れない**ので「入っても何も起きない」を作らない |
| `rescue` | レスキュー出現〜囲いクリアまで | 立っている。**v1の会話サークル(青・`EVENT_NPC_INTERACT_RADIUS`=64)と滞在アークは描かない。触っても何も起きない。**代わりに**囲いのトリガー円**を描く(§2-3「★待機中のトリガー円は誰が描くか」)——**「何も描かない」ではない** |
| `accepted` | 囲いクリア〜城ボス撃破まで | レスキュー地点に残る。**滞在メーターは進めない**(受注は済んでいる) |
| `warping` | 城ボス撃破 → 帰還サークルへの移動中 | レスキュー地点から**飛び去り**、帰還サークルへ**飛来**(§2-8) |
| `delivering` | 帰還サークル到着〜納品成立まで | 帰還サークルの縁に立つ。**判定・描画は帰還サークル1本**(§2-8・細部18) |
| `completed` / `gone` | 納品後 / 納品済みステージ | 従来どおり(`completed`=このランで退場・`gone`=以後出さない) |

### ★★この6状態を**誰が書くのか**を1箇所に決める(書き手が無いと `rescue` のまま固まる)

**v2の状態は5回遷移する**(★社長裁定2026-08-30 の裏ボス非表示で **`rescue` ⇄ `hidden` の往復2本が加わり計7本**)が、v2 4巡目までの本文は**その書き手を1つも名指ししていなかった**。
書かれないと `status` は `rescue` のまま残り、**§2-7 の黄色い行の条件(`accepted`/`warping`/`delivering`)が
永久に false =討伐目標が一度も出ない**うえ、**§2-9 の縁矢印(`rescue` の間だけ渡す)が城ボス戦のあいだ
ずっと二人組を指し続ける**。

- **確定: 状態遷移を書くのは `useGameLoop` の二人組ブロック(既存の滞在状態機械があった1箇所)だけ。**
  `pixiScene` は書かない(CLAUDE.md「PixiJS only draws」)。`gameStore` のアクション(`acceptEventQuest` /
  `completeEventQuest`)にも**新しい遷移を足さない**(v1経路は §2-2B のとおり殺す。
  例外は `completeEventQuest` が既に持っている `→ completed` の1本=納品成立の瞬間に呼ぶ・下表)。

| # | 遷移 | 契機(この瞬間に書く) | 参照 |
|---|---|---|---|
| 1 | `hidden` → `rescue` | **レスキュー出現**(4:00。S5は「4:00 と 拠点2か所ラッチ の遅い方」)。**飛来を開始するフレーム**で書く(着地の完了ではない)。★裁定2026-08-30: **裏ボス4体が場に居る間は出現そのものを保留**し、**居なくなった最初のフレーム**に(その時点のプレイヤー基準で)出現抽選を行う | §2-3 / §2-11 / §2-4 |
| 2 | `rescue` → `accepted` | **`rescueClearedAt` を打刻するのと同じブロック**(全滅 / 90秒時間切れ / 外から畳まれた後始末 の3契機とも) | §2-5 |
| 3 | `accepted` → `warping` | **帰還サークルが出たフレーム**(`finaleDefeated` → `beginReturnPhase` の直後=`returnCircle` が非nullになった最初のフレーム)。ここで飛び去り(`crouch`→`flyout`)を開始する | §2-8 |
| 4 | `warping` → `delivering` | **飛来(`flyin`)の着地が完了したフレーム**(`movePhase` の最終段が満了した瞬間)。ここで `dwellMs = 0` にして滞在を積み始める | §2-8 |
| **6** | `rescue` → `hidden` | **裏ボス4体のいずれかが場に居る最初のフレーム**、**かつ自分の囲いがまだ発火していない**(`rescueArenaStartedAt === undefined`)。★社長裁定2026-08-30。**出現位置は保持する**(捨てない)。退場は**4:00の飛来の逆再生**=`RESCUE_ALLY_CROUCH_MS` → `RESCUE_ALLY_FLYOUT_MS`(慣性MUST。瞬間消滅させない) | §2-4 |
| **7** | `hidden` → `rescue` | **裏ボス4体が1体も居なくなった最初のフレーム**。**保持していた同じ位置**へ**4:00と同じ飛来**(`flyin`+`spawnRing`2枚)で戻す(出現抽選をやり直さない=同じイベントが2度別の場所に出ない)。**着地が完了するまで円に触れても発火しない**(4:00の出現と同じ規則。復帰と同フレームに発火して二人が居ないまま戦闘が始まるのを防ぐ) | §2-4 |
| 5 | `delivering` → `completed` | **納品成立の瞬間**(§2-8 の手順1〜4と同じ set。`completeEventQuest` が既に `status: 'completed'` を書いている=**その1本を流用する**) | §2-8 / §2-10 |

- **`gone` はこの表に無い**——`gone` を書くのは **`resetGame` の `qGone` の1箇所だけ**(§2-1)。
  ラン中に `gone` へ落とす経路を作らない。
- **各遷移は1フレームに1回だけ**(2と5は「既にその状態なら何もしない」で二重発火を防ぐ。
  §2-5 受け入れ条件1b と同じ趣旨)。

### ★`createEventQuestNpc` の初期値も同じコミットで直す(直さないと 0:00 から二人が立っている)

**事実(実在確認)**: `createEventQuestNpc()` の初期 `status` は **`'available'`**、座標は
**原点基準で方位角ランダム・距離 `EVENT_NPC_MIN_DISTANCE`(460)〜`EVENT_NPC_MAX_DISTANCE`(950)のランダム**。
描画のスイッチは `status` なので、**このままだと 0:00 から二人組が場に描かれる**
(§2-2 受け入れ条件1「4:00 未満では二人組も出ない」が破れる)。

- **確定: `createEventQuestNpc()` の初期 `status` を `'hidden'` にする。**
- **確定: 座標は 4:00 の出現時に §2-3 の規則で上書きする**(=**生成時の乱数配置は使わない**)。
  生成時の `x/y` は「まだ決まっていない値」であって出現位置ではない。
  ※`EVENT_NPC_MIN_DISTANCE`(460)は §2-14 のとおり**飛来の始点**の距離として流用する(別用途)。
- `resetGame` の `qGone` は**従来どおり `status: 'gone'` で上書きする**(§2-1)。

**殺す(または v2 用に作り替える)実体(実在確認)**
- `useGameLoop` の二人組の滞在状態機械(`EVENT_QUEST_DWELL_MS` / `leftSinceAccept` で受領・納品を分岐している
  ブロック) — v2ではレスキュー地点での受注に使わない。
- `gameStore` の `acceptEventQuest`(**ネームド(パンプキン/犬)の湧きを内包している**)/ v1経路の `completeEventQuest`。
- `pixiScene.syncEventQuestNpc` の `status === 'available' | 'accepted'` のときの**会話サークル(青い円)+白い滞在アーク**。
- `GameHUD` の `EventQuestPill`(v1のサブ進捗ピル。v2では §2-7 の行に置き換わる)。

### ★`hidden` は「絵を出さない」だけでは足りない —— 投影影を落とし続ける

**事実(実在確認)**: `pixiScene` の毎フレーム同期は `syncLocalEventLighting(...)` に
**`s.eventQuestNpc` を渡している**(敵/escort/props/trees/cityProps/walls/propObjs/castle/merchant/npc を
走査して強glowの投影影を描く関数)。つまり **`syncEventQuestNpc` 側で描画をスキップしても、
二人組の影だけが地面に落ち続ける**。

**★除外条件は `hidden` だけでは足りない(実在確認)**: `syncLocalEventLighting` の中で二人組を
走査に積んでいる行は **`if (eventNpc.status !== 'completed') { push(eventNpc.x, eventNpc.y, …) }`**
——**見ているのは `completed` の1状態だけ**。よって現行のままでは:
- **`gone`(=納品済みステージ=二人が最初から場に居ないラン)でも影が落ち続ける。**
  これは **v0.25.3054 で「消した建物が投影影を落とし続ける」として直したのと同じ事故**が、
  同じ関数の中に**もう1件残っている**ということ(城/商人は直したが二人組は直していない)。
- v2で足す `hidden` / `warping` も、条件を書き換えないと同じように影だけ残る。

- **確定: 除外条件は「`completed` か」ではなく「★このフレームに絵を描いていない状態か」で書く。**
  `syncEventQuestNpc` が描画をスキップする状態=**`gone` / `hidden` / `completed`(フェード満了後)**を
  **全部**走査から外す(以後 `EventQuestStatus` に状態が増えた時も、**描かない状態を足したら
  ここも足す**——この対応関係を実装時のコメントに1行残す)。
  **さらに `warping` も外す**(絵は描くが、この積み口は高さを表せないため。理由は下の2系統の表)。
  → **`syncLocalEventLighting` に積むのは `rescue` / `accepted` / `delivering` の3状態だけ。**
### ★★二人組の影は**2系統ある**。`warping` の指示を1つにまとめると必ず間違える(実在確認)

**v2 5巡目までの本文は「`warping` は走査から外さず `heightPx` で縮める」と1本で書いていたが、
それは2つの別々の影システムを取り違えている。**

| 系統 | 実体(実在) | 高さを表せるか | v2でどうする |
|---|---|---|---|
| **(a) 強glowの投影影** | `syncLocalEventLighting` が積む候補。積み口は **`push(x, y, w, strength = 1)`** | **表せない**——**高さの引数が無い**(構造体は `{ x, y, w, strength, horizonAlpha }`) | **`warping` も走査から外す**(=飛んでいる間は強glowの投影影を落とさない) |
| **(b) 接地のソフト方向影** | `syncEventQuestNpc` が毎フレーム組む **`npcShadow`**(型は `BuildingShadowReq`)。`syncShadows` がこれを `place({ id: 'npc', … })` で `ShadowCasterReq` へ正規化する | **`ShadowCasterReq.heightPx` が実在**(救援アライ/護衛が `heightPx: v.shadowHeight` で渡している) | **`heightPx` を渡す**(飛んでいる間、影が足元から離れて小さくなる) |

- **★(b) には配線の追加が要る(実在確認)**: **`BuildingShadowReq` には `heightPx` が無く**
  (`x / y / legacyW / rawW / rawH / texture / alpha / shadowFade? / isStatic?` の9つだけ)、
  **`place({ id:'npc', … })` の呼び出しも `heightPx` を渡していない**。
  → **`BuildingShadowReq` に `heightPx?: number` を足し、`npcShadow` の `place(...)` へ素通しする**
  (**護衛/救援アライが既にやっている作法をそのまま持ってくるだけ**=新しい仕組みを作らない)。
  **この `npcShadow` は v2 5巡目までの本文に一度も出てこない**ので、§2-14 の実装マップにも行を立てる。
- **(a)(b) を混ぜないこと**: (a) を `heightPx` で縮めようとすると**存在しない引数を足す**ことになり、
  (b) を「走査から外す」と**接地影が丸ごと消える**(飛んでいるのに影が無い=浮遊感が消える)。

**同型の事故は既に踏んでいる**: `pixiScene` の同じ呼び出し行に
「**v0.25.3054(監査指摘「消した建物が投影影を落とし続ける」): 施設フェード中は城/商人を遠方座標に
差し替えて強glowの投影影スキャンから外す**」というコメントと実装(`{ ...s.castleEvent, x: 1e9, y: 1e9 }`)が
実在する。

- **`hidden` の二人組は `syncLocalEventLighting` の走査から外す。**
  やり方は**城/商人と同じ作法**=渡す時に**遠方座標へ差し替える**(距離カリングで自然に落ちる)か、
  `syncLocalEventLighting` 側で `status === 'hidden'` を**早期 return** する。**どちらでもよいが、必ずどちらかを行う**。

**受け入れ条件**
1. `rescue` 状態の二人に**どれだけ近づいても・中に立ち続けても**、会話サークル・滞在アーク・受注会話・
   HUDピルのいずれも出ない(v1の受注が成立しない)。
2. v2のランで **ネームド(パンプキン/犬)が1体も湧かない**(`acceptEventQuest` のネームド湧きが走らない)。
3. 二人組が**同時に2箇所に居ない**(`warping` 中も含めて、画面上に二人組は常に1組)。
4. **0:00〜4:00(=`hidden`)の間、二人組の投影影が地面に出ない。**
   松明/強glowの近くに二人組の出現予定地が来ても、そこに「誰も居ないのに影だけある」状態にならない。
4b. **納品済みステージ(=`gone`・二人が1度も出ないラン)でも、二人組の投影影が地面に出ない。**
   (現行の `status !== 'completed'` のままだと出る。v0.25.3054 と同型の事故を再発させない。)
4c. **`warping`(空中)の間、接地影(`npcShadow`)が二人の足元に貼り付いたまま等倍で動かない**
   (救援アライと同じく `ShadowCasterReq.heightPx` で高度ぶん縮む)。
4d. **`warping`(空中)の間、強glowの投影影が地面に出ない**(=`syncLocalEventLighting` の走査から
   `warping` も外してある)。松明/爆発の近くを飛び越えても、**空中の二人組から地面へ伸びる影が出ない**。

---

## 2-3. レスキューイベントの出現(4:00)

- **4:00 に、プレイヤーの近くへ「レスキュー地点」が出る**。中身は **二人組(`quest-futari`)+ 囲いのトリガー円**。
- **制限時間なし**。放置しても消えない・失敗しない(=時間切れという概念を作らない)。
- **出現位置**(叩き台):
  - プレイヤーの**進行方向**(直近の移動方向。停止中は向き)へ伸ばした直線を**中心線**とする。
  - 中心線上、プレイヤーから **500〜1000px 先**(既存レスキューの距離帯 `RESCUE_SPAWN_DIST_MIN/MAX` を流用)を基準点にする。
  - 基準点から**中心線と直交する向きへ ±200px**(左右どちらかをランダム)ずらした点を出現位置にする。
  - 障害物と重なる場合は、`beginReturnPhase` と同じ**空き地探索**(角度を振って半径を広げる)の
    **形**を流用して押し出す。**ただし「何と重なったら押し出すか」は下記の集合で判定する**(流用元の集合は使わない)。

### ★「重なる」の判定は**プレイヤーの移動が見るのと同じ集合**で行う(流用元は足りない)

**事実(実在確認)**: 流用元 `beginReturnPhase` の空き地探索が屋外で見ているのは
**木の幹(`treesInRegion`→`trunkRect`)/ 松明(`breakableProps` の `type==='torch'`→`torchRect`)/
城(`castleRect`)の3つだけ**。
一方、**プレイヤーが「そこに立てるか」を決めている唯一の関数は `resolveOutOfSolids`**
(`src/store/gameStore.ts`・`movePlayer` が呼ぶ)で、中身は
**木 → 松明 → 城 → 街プロップ(`resolveCityPropCollision`・S3の廃都)→ 病院 → 武器庫 → 警察署**の連鎖。
→ **流用元をそのまま使うと、S3の街プロップ・病院・武器庫・警察署が見えず、
二人組が「プレイヤーが立てない場所(建物/プロップの中)」に立つ。**
しかも `resolveOutOfSolids` の doc コメントは
「**プレイヤーが立てない場所にはアイテムも落ちない**、という一文で説明できる状態にするのが目的」で、
**同じ判定を2箇所に書くと必ず片方だけ古くなる(v0.25.2383/2387/2389 で3回起きた型)**と明記されている。

- **確定①(必須): 候補点が「立てる場所か」の判定は `resolveOutOfSolids` を通す。**
  候補の矩形を渡し、**押し戻された(座標が変わった)候補は棄却して角度を振り直す**
  (押し戻された座標をそのまま採らない=押し出し先が別の障害物の中になりうるため)。
  **ただし全候補が棄却された最後のフォールバックだけは例外**(下の「★候補が全滅した時に諦める順序」)。
  - **★渡す矩形は `PLAYER_HITBOX`(=28)の正方形**(中心=候補点)。
    **理由**: ①`resolveOutOfSolids` は「**プレイヤーが立てるか**」を決める唯一の関数なので、
    **プレイヤーと同じ大きさで訊く**のが意味どおり ②`EventQuestNpc` には **width/height が実在しない**
    (`radius`=64 は**会話サークル**、`npcShadow` の `legacyW`=84 は**影の絵幅**——どちらも当たり判定ではない)ので、
    それらを矩形寸法に流用すると**判定を持たない数字を判定に使う**ことになる
    ③**大きい矩形(例: 半径240の外接矩形)を渡すと S3の廃都・森で候補がほぼ全滅**して
    毎回フォールバックに倒れ、**小さすぎると壁に埋まる**。
    **新しい寸法定数を1つも作らない。**
  - **★`ctx` は自分で組まない。実在の公開ヘルパ `resolveBountyMove(nx, ny, box)` をそのまま呼ぶ。**
    **事実(実在確認)**: `gameStore` には **`resolveBountyMove(nx, ny, box)`** が既に export されており、
    中身は **store から `labTheme` / `farBackdrop` / `solidProps`(mine と uv-bar を除外)/ `castleEvent` /
    `hospital`+`hospitalTaken` / `armory`+`armoryTaken` / `police`+`policeTaken` / `facilitiesHidden`
    を組んで `resolveOutOfSolids` を呼ぶ**だけの関数(賞金首の移動が使っている)。
    → **ctx の列挙を設計書やコードで書き写さない**(v2 5巡目の本文は
    **`hospitalTaken` / `armoryTaken` / `policeTaken` の3つを書き落としていた**——
    落とすと「**取り返した施設の中に立てる**」等のズレが静かに入る)。
    `resolveBountyMove(cx - PLAYER_HITBOX/2, cy - PLAYER_HITBOX/2, { width: PLAYER_HITBOX, height: PLAYER_HITBOX })`
    の戻り値が**入力と一致すれば採用・ズレたら棄却**。**新しい当たり判定も新しい ctx 組み立ても作らない。**
- **確定②-b(★社長裁定2026-08-30「他のイベントの範囲とは重ならない様に注意」): 候補中心が
  「その時点で立っている他イベントの範囲」の中に入らない候補だけを採る。** 見る範囲は次の3つ
  (いずれも**判定を止める物ではない**ので、②と同じ「距離条件」の形で避ける):
  - **`activeEvent` のサークル**(`x` / `y` / `radius`)——**種別を問わず**(囲い・ゲート・救助ホールド)。
  - **賞金首の索敵圏**(存命個体の中心 + `aggroRange`。既定 `BOUNTY_AGGRO_RANGE_DEFAULT`=380)。
    旧ゲートで唯一"無限"を作っていた重なりなので、**位置の側で避ける**。
  - **(既存)武器商人・拠点サークル**(下の確定②)。
  ※**新しい半径定数を1つも作らない**(すべて実在の値を読む)。
- **確定②: 武器商人・拠点サークルは「立てない場所」ではない**(実在確認: どちらも `resolveOutOfSolids` に
  入っていない=プレイヤーの移動を止めない)。よってこの2つは**衝突ではなく「絵の重なり」**として、
  候補点の**距離条件**で避ける: 候補中心が `weaponMerchant`(半径 `weaponMerchant.radius`)および
  `baseSites` の各サークル(半径 `BASE_CAPTURE_RADIUS`)の**中に入らない**候補だけを採る。
- **確定③: 二人組の着地点は `clampRectToPlayableArea` も通す**(下記のとおり S1/S3/S4/S5 では no-op だが、
  他ステージへ流用された時の保険。**これを安全の根拠にはしない**)。

### ★候補が全滅した時に**諦める順序**(=「出ない」を絶対に選ばないための優先順位)

出現位置には**5つの条件**が乗っている(①原点3000未満 ②商人/拠点サークルに入らない
**②-b 他イベントの範囲に入らない(社長裁定2026-08-30)** ③`resolveOutOfSolids` で
押し戻されない ④`clampRectToPlayableArea`)。**全部を満たす候補が1つも取れない場合に何を諦めるかが
書かれていないと、実装者が勝手に決める**(=「出さない」を選ばれると**城ボスが出ず勝利不能**)。

- **確定: 諦める順序は上から。**(角度を振り、半径を広げる探索を**各段で1周ずつ**行う)
  1. **まず ①原点3000未満 を諦める**(=デンジャーゾーンの中でもよい。難度の扱いは ★未決 #15「デンジャーゾーン内に囲いが出た時の難度」)。
  2. 次に **②商人/拠点サークルの距離条件を諦める**(=絵が重なってもよい。**判定は何も壊れない**——
     実在確認のとおり、この2つは `resolveOutOfSolids` に**入っていない**=プレイヤーを止めないため)。
  2b. 次に **②-b 他イベントの範囲の距離条件を諦める**(社長裁定2026-08-30 の「注意」なので
     **商人/拠点より後まで守る**が、**「出さない」よりは先に諦める**。重なっても発火そのものは
     成立する=勝利不能にはならない)。
  3. **③`resolveOutOfSolids` は最後まで守る。** ただし**それでも1つも取れなかった最終フォールバック**では、
     **押し戻された座標をそのまま採る**。
     - **これは確定①の「押し戻された候補は棄却」の例外**であり、**既存の前例に合わせる側**である。
       **事実(実在確認)**: `collectPickup` の着地点は
       `placed = resolveOutOfSolids({ …, width: PICKUP_HIT_SIZE, height: PICKUP_HIT_SIZE }, ctx)` の
       **戻り値をそのまま採用している**(棄却も振り直しもしない。コメント=「押し出しは一番浅い向きなので、
       木の根元に落ちた弾薬は幹のすぐ横に出る」)。**このゲームの既存の作法は「押し戻して採る」側**なので、
       最終手段でそこへ倒すのは新しい流儀ではない。
  4. **④`clampRectToPlayableArea` は常に最後に通す**(S1/S3/S4/S5 では no-op なので何も起きない)。
- **どの段でも「出さない」は選ばない。**

**受け入れ条件(この段の追加分)**
- 木/建物が密集した場所で 4:00 を迎えても、**二人組が必ず1組出現する**(出ないランが無い)。
- 出た二人組が**壁/木/建物の絵の中に埋まっていない**(最終フォールバックでも `resolveOutOfSolids` を
  通した座標である=素の候補点をそのまま使っていない)。

### ★出現位置の安全化(clampでは守れない。`horizonActorAlpha` も使えない)

**事実(実在確認)①**: `world/playableArea.ts` の `clampRectToPlayableArea` が実際に何かをするのは
**`farBackdrop==='tutorial'` / `labTheme` / `corridorMode` / `exStage` の4条件だけ**で、
**S1/S3/S4/S5 は屋外の通常ステージなので入力をそのまま返す完全な no-op**。
よって「clampを通せば ④可視域外 / ⑤移動可能帯の外 を防げる」という書き方は**この4ステージでは嘘**になる。
(※呼ぶこと自体は害が無いので**呼んでよい**——他ステージへ流用された時の保険になる。ただし
**これを安全の根拠にしてはいけない**。)

**事実(実在確認)②**: `pixiScene.syncEventQuestNpc` は `horizonActorAlpha(npc.y) <= 0` で
**二人組を丸ごと非表示**にする。縁矢印は**画面外の時だけ**出る仕様なので、
**画面内・alpha=0・矢印なし=完全な死角**ができる。

### ★v2初稿の「出現Yを `horizonActorAlpha(出現Y) >= 1.0` まで押し下げる」は**撤回する**

**2つの理由で成立していない(実在確認)**:
1. **呼べない。** `horizonActorAlpha` は `pixiScene` の **private メソッド**で、中身は
   `this.cameraY` / `this.wgZoom()` / `this.wgOffsetY()` を使う**カメラ相対**の値。
   出現位置を決める store / `useGameLoop` からは**そもそも呼べない**。呼べる形にすること自体が
   CLAUDE.md「**判定は world/store 側に置く。pixiScene には置かない**」「world は renderer-agnostic」違反。
2. **仮に呼べても1フレームしかもたない。** 二人組は動かないがカメラは追従するので、
   プレイヤーが少し南へ歩けば二人はフェード帯に入り、**画面内・alpha=0・矢印なしの死角がそのまま再現**する。
   位置で守る方法は、この死角に対して原理的に効かない。

### ★★v2 3〜4巡目の「地平線フェードに floor(0.35)を敷く」は**撤回する**(フェードは1バイトも触らない)

**事実(実在確認)**: `horizonActorAlpha` のしきい値は
**`this.horizonForestFootWorldY = s.camera.y + this.horizonActorHideScreenY()`**、
そして **`horizonActorHideScreenY() = 遠景森レイヤーの下端 − HORIZON_ACTOR_HIDE_OFFSET_PX`**。
つまりこのフェードは「遠景に重なると薄くなる」装置ではなく、
**「遠景森の足元ライン(=地平線)より上には、地面に立つ絵を出さない」装置**。
実在コメントも `// 地平線際は出さない(空に浮かない描画と整合)`(遮蔽物の透過候補)、
`// 地平線で透明化(空に浮かない)`(護衛NPC)、
`// さらに地平線フェード(空に浮かない=他アクターと同じ)`(救助NPC)と**3箇所で同じ言葉で書かれている**。
v2の3〜4巡目はこれを「遠景に重なると薄くなるだけ」と読み違えたまま floor を敷いていた。

**floor(0.35)を敷くと何が起きるか**:
- 二人組・**半径240のトリガー円の上半分**・囲いの敵が、**地平線より上=空/遠景の上に 35% で浮いて描かれる**。
- **足元影も一緒に floor 化される**(敵の足元影は `if (horizonAlpha <= 0) continue;` で落としているため)
  =**影まで空に浮く**。
- フェード帯の幅は**引くほど広がる**(`horizonActorFadePx` は画面px)ので、
  **`?zoomlock=0.4` で悪化する**(CLAUDE.md「ズーム引き考慮」に真っ向から反する)。

- **確定: floor は敷かない。地平線フェード(`horizonActorAlpha` / 敵本体 / 足元影)は1バイトも触らない。**
- **囲いの敵も他の囲いと同じ扱いのまま**(このイベントだけの例外を作らない)。
- **`phillboss` が `1` 固定なのは前例にならない**(専用レイヤーで地平線の外に居るボスであって、
  地面に立つアクターではない)。

### ★では「画面内なのに透明で見失う」をどう閉じるか(=誘導配線側で担保する)

- **確定①: 縁矢印の出す条件を広げる。** 現行の `syncArrows` は
  **`tx >= 0 && tx <= screenW && ty >= 0 && ty <= screenH` なら `continue`(=画面内なら出さない)**。
  レスキュー地点の矢印(§2-9 で新設する配線)だけ、この条件を
  **「画面外の時 **または** 地平線フェードで実質見えない時(`horizonActorAlpha(二人組のy)` が
  ほぼ0の時)」**へ広げる。
  - **これは描画都合の判定なので `syncArrows` の中で完結させる**
    (`horizonActorAlpha` は同じクラスの private メソッド=同一ファイル内で読める)。
    **world / store には1バイトも持ち込まない**(CLAUDE.md「判定は world/store 側に置く」の逆向き=
    *描画都合の値を判定に使わない* を守る。ここで決まるのは「矢印を描くか」だけで、ゲームの挙動は動かない)。
  - しきい値の値そのものは**新設しない**(「0より大きいか」で判定する。実機で「薄いのに矢印が出ない」と
    分かったら社長が絞る)。
- **確定②: 出現位置の側には、地平線を避ける規則を足さない。**
  - 理由: 出現位置の規則は「**プレイヤーの進行方向へ 500〜1000px 先**」(既存の距離帯 `RESCUE_SPAWN_DIST_MIN/MAX`
    を流用)。ここに「プレイヤーより上(−Y側)へは◯◯px までしか置かない」を足すと、
    **プレイヤーが北(−Y)へ走っている時だけ進行方向の候補が全部棄却される**=
    **「進行方向へ出す」という仕様そのものを、向きによって壊す**ことになる(=仕様変更)。
  - **位置では守り切れない**のも事実(出た時に帯の外でも、その後プレイヤーが南へ歩けば帯に入る)。
  - **帯に入っても迷子にならないこと**は確定①(縁矢印)が担保する。加えて、
    **プレイヤーが近づけば二人の画面Yは下がって帯から出る**(カメラがプレイヤーを追う)ので、
    **矢印で向かえば必ず見える**。
  - ※もし実機で「出た瞬間に見えないのが不親切」と分かった場合は、**出現規則を変える前に
    ★未決へ上げる**(進行方向の仕様に触れるため、設計チャットでは決めない)。
- clamp(`clampRectToPlayableArea`)は**通してよいが、④⑤の担保にはならない**ことを前提に置く。
- 出現の**告知と誘導**は §2-9(見せ方の細部だけ ★未決 §2-15 #4「レスキュー出現の告知と誘導」)。

### ★待機中のトリガー円は誰が描くか(**描く実体を1つ名指しする**)

**事実(実在確認)**: 囲いの赤/青の円を描いているのは `pixiScene.syncArena(s.activeEvent, now)` で、
中身は **`if (!ae) return;`**——つまり **`activeEvent` が立っている間しか円を描かない**。
`beginArenaEvent` は進入して**発火した後**に呼ぶので、**4:00〜進入までの待機中は、円を描く実体がどこにも無い**。
一方 `syncEventQuestNpc` が描く円は v1 の会話サークル(半径 `EVENT_NPC_INTERACT_RADIUS`=64・青 `0x60a5fa`)で、
しかも**プレイヤーが近い(`near`)時だけ**描かれる。
→ このままだと **「どこに入れば始まるのか」が画面上に存在しない**(§2-3 が `spawnRing` 2枚と書いたのは
**出現の一瞬**の演出であって、数百msで消える。待機中ずっと出ている絵ではない)。

### ★★v2 3〜4巡目の「待機中の円は `syncEventQuestNpc` が描く」は**撤回する**(構造的に半径が一致しない)

**事実(実在確認)・食い違いが3つある**:
1. **層・合成・座標系が違う。**
   - `syncArena` の円 = **`arenaGfx`**。`L.groundLayer` の子で **`blendMode = 'add'`**、
     **world 座標に直描き**(`g.circle(ae.x, ae.y, ae.radius)`)、**depthScale を掛けない**。
   - `syncEventQuestNpc` の円 = **`eventNpcGfx`**。**`L.actorLayer` の子 `eventNpcView` の中**(=Y-sort に参加・
     `zIndex = npc.y`)、**通常合成**、しかも
     **`g.circle(0, -8 * d, npc.radius * d)`**(=`d = depthScale(npc.y)` 倍+中心を **`-8*d` 上へずらす**)。
   → **同じ「半径240の円」を描いても、`depthScale` の倍率ぶんと `-8*d` のズレぶん、必ず一致しない。**
2. **発火の瞬間に絵が飛ぶ。** 上のとおり待機中(actorLayer・通常合成・depthScale)から
   発火後(groundLayer・加算・等倍)へ**層・合成・半径が同時に入れ替わる**ので、
   進入した瞬間に円が**ワープしたように見える**。
3. **半径の値をそもそも読めない。** `ARENA_EVENT_RADIUS` は **`useGameLoop.ts` のモジュールローカル定数で
   `export` されていない**(`const ARENA_EVENT_RADIUS = 240;`)。`pixiScene` からは import できないので、
   実装者は **240 をベタ書きする**ことになる=**二重管理**(片方だけ動かした瞬間に
   「見えている円と発火する距離が違う」=受け入れ条件9が静かに壊れる)。

### ★確定: 半径は store に持たせ、待機中の円は `arenaGfx` と同じ作法の専用 Graphics が描く

- **半径を store に持たせる(判定と描画の唯一の出どころ)**: `EventQuestNpc` に
  **`triggerRadius: number` を足す**(現行の `radius`=会話サークル 64 とは**別フィールド**。
  会話サークルは v2 では描かないが、型からは消さない=v1経路の巻き添えを避ける)。
  - 値は `useGameLoop` がレスキュー出現時に **`ARENA_EVENT_RADIUS` をそのまま代入する**
    (定数の在処は `useGameLoop` のまま=**export 化も移設もしない**。値の複製をコードに残さない)。
  - **進入判定(§2-4)も描画も、この `triggerRadius` だけを読む。**
    CLAUDE.md「判定は world/store 側に置く。pixiScene には置かない」に沿う形。
- **描くのは `arenaGfx` と同じ作法の専用 Graphics**(例: `questTriggerGfx`)を
  **`L.groundLayer` に `arenaGfx` の隣へ足し、`blendMode = 'add'`・world 座標直描き・depthScale なし**にする。
  **`syncEventQuestNpc`(actorLayer)には描かない。`syncArena` も触らない。**
  - 中心は**二人組の座標**(=囲いの円の中心と同じ・§2-4)。半径は `triggerRadius`。
  - `near` の距離条件を**通さない**(遠くからでも見える。見えないと踏みに行けない)。
  - **絵の作り(塗り+二重リング)は `syncArena` の horde と同じ式をそのまま使う**
    (`radius-4` の淡い塗り / `radius` の太いストローク / `radius-3` の細いストローク)。
- **色は ★未決 §2-15 #10「囲い進入前の警告表示」の裁定に従う**(#10「囲い進入前の警告表示」の案(C)=「円の見た目自体を危険色にする」が選ばれたら危険色)。
  **裁定前の叩き台は、既存の horde 囲いと同じ青 `0x38bdf8`**(`syncArena` の horde 色をそのまま使う=新色を作らない)。
- **★★待機円を止める条件は「`activeEvent` が立ったか」ではない(実在確認・5巡目の穴)**:
  **`activeEvent` はシングルトンで、レスキューと無関係な囲いでも立つ**(ゲート1/ゲート2/退屈補正/
  2分タイマー/救助/緑卵)。「`activeEvent` が立ったら待機円をやめる」と書くと、
  **4:00 に円が出た後、別のイベントの囲いが開いている 40〜60秒のあいだ、トリガー円が画面から消える**。
  **★社長裁定2026-08-30で6条件は撤去された**ので「その間は発火もしない」という旧根拠は消えたが、
  **この確定(自分の囲いが立った時だけ待機円をやめる)はそのまま有効**——他イベントの40〜60秒のあいだ
  トリガー円が画面から消えると、**「円が出た → 消えた → どこへ行けばいいか分からない」**が残るため。
  (発火するようになった今は、消える理由がいっそう無い。)
  - **確定: 待機円を止める条件は `activeEvent?.rescueQuest === true` のときだけ**
    (=**自分の囲いが立った時だけ**やめる。§2-4 の専用フラグをそのまま読む)。
  - **他イベントの囲いの円と、レスキューの待機円が同時に2本描かれることは許容する**
    (どちらも `groundLayer` の加算合成なので重なっても破綻しない。**消えて迷子になる方が桁違いに重い**)。
  - **`activeEvent` が消えた時に待機円を描き直す条件も同じ**——`status === 'rescue'` かつ
    `activeEvent?.rescueQuest !== true` の間はずっと描く(§2-4 の後始末で強制クリアされた場合は
    `status` が `accepted` へ進むので、そこで自然に消える)。
- **発火時の引き継ぎ(絵を1フレームも動かさない)**:
  - **叩き台の色(=horde と同じ青)のままなら、`activeEvent?.rescueQuest === true` になったフレームで
    待機円の描画をやめる。**
    `syncArena` が**同じ層・同じ合成・同じ中心・同じ半径・同じ式**(`pulse = 0.5 + 0.5*sin(now/260)` ほか)で
    描き始めるので、**絵は完全に同一=途切れも飛びも起きない**。クロスフェードは要らない。
  - **#10「囲い進入前の警告表示」の裁定で待機中の円だけ色/濃さを変えた場合**は、その差分ぶんだけ **200ms のクロスフェード**で
    `syncArena` の絵へ寄せる(待機円の alpha を 1→0)。
    ※**`blendMode='add'` なので、重なっている間は加算で少し明るくなる**。だから
    **同じ絵の時はクロスフェードしない**(明るさが動く方が目立つ)。
  - **200ms は新しい体験の数字ではなく、絵の入れ替わりを目に見せないための描画都合の値**(叩き台)。
- 地平線フェードは**掛けない/免除もしない**(=`arenaGfx` と同じ扱い。上の floor 撤回に揃える)。

### ★出現点の距離上限(原点から)

**事実(実在確認)**: 拠点を置く輪は `BASE_SITE_RADIUS`=**3200**、デンジャーゾーンの入口は
`AREA_THRESHOLDS[1]`=**3000**(`src/utils/enemyUtils.ts`)。「進行方向500〜1000px先」だけの規則では、
**拠点付近(3200px)で 4:00 を迎えるとレスキューがデンジャーゾーンの中に出る**。

- **出現点は、原点からの距離が `AREA_THRESHOLDS[1]`(=3000)未満になる候補だけを採る。**
  空き地探索の角度振りの中で、この条件を満たさない候補は**棄却して振り直す**。
- **空き地探索を振り切っても1つも採れなかった場合は、上限を諦めてそのままプレイヤー近傍に出す。**
  —— **「出ない」は絶対に選ばない**(出ないと城ボスが出ず勝利不能)。この場合の難度の扱いは **★未決 §2-15 #15「デンジャーゾーン内に囲いが出た時の難度」**。
  - ※旧稿はここに「(=プレイヤー自身が既に原点から3000px以遠に居る)」と書いていたが、**これは誤り**。
    プレイヤーが3000px以遠に居ても、**進行方向が原点側なら候補は3000未満に取れる**(500〜1000px先+角度振りのため)。
    条件は「プレイヤーの位置」ではなく「**振り切っても候補が1つも無い**」で書く。

- **★S5では、この上限は「例外」ではなく「既定」になる(実在確認)**: S5のレスキューは
  「拠点2か所確保」の後に出る(§2-11)。拠点は `BASE_SITE_RADIUS`=**3200** の輪の上に置かれ、
  確保の在内判定は `BASE_CAPTURE_RADIUS`=**130**。つまり**2か所目を確保した瞬間、プレイヤーは必ず
  原点から 3070〜3330px の間に居る=常にデンジャーゾーン(3000)の外**。
  よって **S5ではこの上限規則が毎回ほぼ機能しない**(進行方向が原点側を向いていた時だけ偶然通る)。
  **S5の出現点の規則そのものをどうするかは ★未決 §2-15 #17「S5の出現点の規則」**(本文の現行仕様=S1/S3/S4と同じ規則、は据え置き)。

### ★出現そのものの慣性(CLAUDE.md MUST・**新しい型は作らない**)

v2初稿はワープ(§2-8)にだけ慣性を指定し、**4:00 の出現そのものの動きを書き忘れていた**。
「パッと出て止まる」は禁止なので、**既存の型をそのまま流用**して書く。

- **二人組** = §2-8 の着地と**同じ飛来ジャンプ**で降ってくる(`RESCUE_ALLY_FLYIN_MS`=300 の水平 ease-out
  + `RESCUE_ALLY_HOP_PX`=48 の sin 1山、着地で減速)。着地後は既存の待機(呼吸)描画へ。
- **囲いのトリガー円** = 既存の囲い発火と同じ **`spawnRing` 2枚の拡大**で現れる
  (内→外へ広がる1枚 + 縁を追う1枚。ゲート1/ゲート2/退屈囲いが全部この2枚組で出している)。
- **★二人の着地が完了するまで、円に触れても囲いは発火しない**(飛来中の進入で始まらない)。

**受け入れ条件**
1. 4:00 ちょうどに1回だけ出現し、以後そのランで再抽選されない。
2. **プレイヤーを上(−Y)へ走らせ続けた状態で 4:00 を迎え、その後さらに上下へ歩き回っても、
   二人組の位置を見失わない**——地平線フェードで二人組が薄くなった/消えた時は、**画面内でも縁矢印が出る**
   (§2-3「★では『画面内なのに透明で見失う』をどう閉じるか」確定①)。
2b. **地平線より上(空/遠景の上)に、二人組・トリガー円・囲いの敵・それらの影が1つも描かれない。**
   `?zoomlock=0.4`(帯が最も広い)でも同じ。**地平線フェードの挙動が、この改修の前後で
   1バイトも変わっていない**(floor を敷いていない=他の敵/木/NPCとまったく同じ薄まり方をする)。
3. 出現点が**プレイヤーが立てない場所**(木/松明/城/街プロップ/病院/武器庫/警察署)と重ならない。
   **S3(廃都)で街プロップの中、S1で病院/武器庫/警察署の中に二人組が立たない**
   (=判定に `resolveOutOfSolids` を通している。流用元の空き地探索の3種だけを見ていない)。
3b. 出現点が**武器商人のサークル・拠点サークルの中に入らない**(絵が重ならない)。
4. 出現点の**原点からの距離が 3000 未満**である(空き地探索を振り切っても候補が1つも無かった場合を除く)。
   ※S5はこの条件を**毎回免除される側に倒れる**(§2-3「★S5では例外ではなく既定」)ので、
   **この条件の検証は S1/S3/S4 で行う**。
4b. **プレイヤーが北(−Y)へ走り続けた状態で 4:00 を迎えても、出現位置の規則(進行方向 500〜1000px 先)が
   そのまま働く**(=地平線を避けるために進行方向の候補を棄却する、という規則を足していない)。
   出た瞬間に地平線フェードで薄い場合は**縁矢印が出ている**(条件2)。
5. `?zoomlock=0.4`(最大引き)で 4:00 を迎え、二人組が画面に入る位置まで歩いた時、
   **二人組の絵と円の中心・半径がズレない**(円の一部だけが描かれる/円と二人組の位置がずれる、が起きない)。
   引きで地平線の帯が広がって二人組が薄くなった場合は、**画面内でも縁矢印が出る**(条件2と同じ担保)。
6. 出現の瞬間、二人組は**飛来ジャンプ(`RESCUE_ALLY_FLYIN_MS` / `RESCUE_ALLY_HOP_PX`)を経由**して降りる
   (=パッと現れて止まらない)。円は `spawnRing` 2枚の拡大で現れる。
7. 二人の着地が完了する前に円へ触れても、囲いが発火しない。
8. **`spawnRing` が消えた後も、トリガー円が画面に描かれ続けている。**
   4:00 の出現から進入までの間、**遠くから見ても「ここに入れば始まる」円が見えている**
   (=`spawnRing` の数百msだけで消えて、以後どこに入ればいいのか分からない状態にならない)。
9. **トリガー円の見た目の半径と、実際に囲いが発火する距離が一致している**
   (円の縁を踏んだ瞬間に発火する。円の内側を歩いても発火しない/円の外で発火する、が起きない)。
   **確認手段: 判定側も描画側も `eventQuestNpc.triggerRadius` だけを読んでいること**
   (コード上に 240 のベタ書きが1つも無い=`ARENA_EVENT_RADIUS` の複製が存在しない)。
9b. **プレイヤーを二人組より下(+Y)/上(−Y)に置いても、円の見た目の大きさと発火距離がズレない**
   (=待機中の円に `depthScale` が掛かっていない。actorLayer ではなく groundLayer に描いている)。
9c. **★別のイベントの囲い(ゲート/退屈補正/2分タイマー/救助/緑卵)が進行中でも、レスキューの
   トリガー円が描かれ続けている。** 検証: 4:00 でレスキューが出た後、他イベントの囲いが開いても
   **円が消えない**(消えるのは自分の囲いが発火した時=`activeEvent.rescueQuest === true` の時だけ)。
   その囲いが終わった後も円はそのまま残っており、入り直せば発火する。
10. **発火した瞬間に、円が飛ばない・同心円が2本にならない・明るさが跳ねない**
    (待機中の円と `syncArena` の円が**同じ層(groundLayer)・同じ合成(add)・同じ中心・同じ半径・同じ式**で
    描かれ、同フレームで引き継ぐ。層/合成/半径が入れ替わって絵が動く/加算で一瞬明るくなる、が起きない)。

---

## 2-4. 囲い戦闘(サークルに入る → 全滅)

- **サークルに入った瞬間に囲いイベントが始まる**。**既存の囲い(`activeEvent`)の型をそのまま使う**。
  - `kind: 'horde'`(=**全滅で終了**する既存の型)。`confinesPlayer` は既定(=拘束する)、`permeable` は既定(=false)。
  - **既存の `kind:'rescue'`(25秒ホールドして守るNPC防衛)は流用しない**。名前が「レスキュー」なだけで
    中身が別物(あちらは `rescueSurvivors` を守る)。混同しないこと。
  - **円の中心 = 二人組の座標**(プレイヤーの進入点ではない)。進入点(=円の縁)を中心にすると
    **二人組が円の外へ出る**。
  - 半径は既存の `ARENA_EVENT_RADIUS`(**値は 240**)を流用(叩き台)。**読む先は store の
    `eventQuestNpc.triggerRadius`**(§2-3。描画と判定で同じ値を読む)。
  - **★「サークルに入った」の判定式(確定・1行)**:
    **プレイヤーの中心点**(`player.x + player.width/2`, `player.y + player.height/2`)と
    **円の中心の距離が `triggerRadius` 以下**なら「入った」。
    - **矩形では判定しない**(足元矩形・当たり判定矩形のどちらも使わない)。
    - **実在の前例と同じ式**: `isInReturnCircle`(`Math.hypot(rc.x - px, rc.y - py) <= rc.radius`・
      px/py はプレイヤー中心)/ 救助イベントのホールド判定 / 拠点確保の在内判定
      (`Math.hypot(x - base.x, y - base.y) <= BASE_CAPTURE_RADIUS`)。
      **このゲームの「円の中に居るか」は全部これ**なので、新しい流儀を作らない。
    - **§2-8 の納品の滞在判定も同じ式**(そちらは既存の `isInReturnCircle` をそのまま使う)。

### ★★発火時の手順は「この順番」でしか成立しない(順序を番号で確定する)

**事実(実在確認)**: `beginArenaEvent` は呼ばれた瞬間に **`radius * 1.5` 以内の非保護敵を一掃する**
(`gameStore` の `state.enemies.filter(...)`)。残すのは `isArenaSweepProtected(e)` が true の個体だけで、
その中身は **`reaper` / `giantbat` / `isPumpkinTier`(=`pumpkin` と **`driller`**) / `isHiddenBoss` /
`isBountyType` / `fixed` / `questTarget`**(`src/utils/enemyUtils.ts`)。

→ **逆順(敵を置いてから `beginArenaEvent`)で書くと、赤い削岩型(`driller`)だけが保護されて残り、
ゾンビ系4体が消える。** 一掃で消えた4体は `cleared` の生存数から居なくなるので、
**囲いは始まった直後から「削岩型1体だけ倒せば終わる」**という別物になる。
エラーも警告も出ない=**「動くけど中身が違う」壊れ方**。

**同じ事故は既に踏んで直してある(実在確認)**: `useGameLoop` のゲート1には
「**重要: beginArenaEvent は呼び出し時点で周辺の非固定敵を一掃するため、必ず「敵を配置する前」に
呼ぶこと(逆順にすると配置直後の台本の敵まで一掃されてしまうバグを実機v0.25.1522で確認)**」、
ゲート2にも「**重要: beginArenaEvent は周辺の非固定敵を一掃するため、必ずボスを配置する前に呼ぶ**」
というコメントと実装が**両方に実在する**。

### ★★発火のコードを置く場所を確定する(同フレーム衝突を「条件」ではなく「構造」で殺す)

**事実(実在確認)**: 既存の囲いの発火は、`useGameLoop` の中で
**`const ae = useGameStore.getState().activeEvent; if (!ae) { … }` の内側にある1本の if/else 連鎖**
になっている:

```
if (!ae) {
  if (gateFireOk && gate1Ready) { …ゲート1… }
  else if (gateFireOk && shouldTriggerGate2({…})) { …ゲート2… }
  else if (puzzleActiveNow) { …退屈補正の囲い(boredomArena)… }
  else { …2分タイマーの囲い/救助/緑卵… }
}
```
→ **既存どうしは「同じフレームに2つ発火する」が構造的に起きない**(if/else + `!ae` の二重)。
だが**レスキューを別の場所(別の if)に書くと、この保証の外に出る**。
とくに危険なのは**ゲート発火待ち**(`gate1PendingRef` / `gate2PendingRef` が立っているフレーム):
同じフレームでレスキューとゲートの両方が `beginArenaEvent` を呼びうる。
**後から呼んだ方が `activeEvent` を上書きし、先に置いた敵は `beginArenaEvent` の一掃で消える。
エラーも警告も出ない。**

- **★★確定(v3・監査A3): 連鎖に入る**前**に「枠を空ける前置き」を1本置く。**
  社長裁定2026-08-30で「**入れば発火する**」が確定した一方、発火枝は `if (!ae)` の**中**に在るので、
  **`activeEvent` が立っている限り枝そのものへ到達しない**。閉じ込め型なら**プレイヤーが円へ来られない**ので
  問題にならないが、**閉じ込めない唯一の型 `kind:'rescue'`(救助ホールド)だけは到達できる**
  (実在: `movePlayer` の円クランプが `kind !== 'rescue'` を除外している。しかも**寿命は2分**=
  `newGameTime + 120000`。`RESCUE_HOLD_NEED_MS`=25000 は**ホールドゲージ**であってイベント寿命ではない)。
  ⇒ **連鎖の直前に**次の1本を置く:
  ```
  // レスキューの円の中に居て、埋まっている枠が「閉じ込めない救助ホールド」なら、正規手順で畳んでから連鎖へ
  if (レスキューの円の中 && ae?.kind === 'rescue') endArenaEvent();
  const ae2 = useGameStore.getState().activeEvent;   // 畳んだ結果を読み直す
  if (!ae2) { …既存の連鎖… }
  ```
  - **上書きしない**(上書きすると救助ホールドの生存者とイベント敵の後始末が走らない)。
  - **待たせない**(同フレームで枠が空くので、そのまま連鎖の枝へ落ちて発火する)。
  - **この前置きが無いと、§2-4 が書いた `kind:'rescue'` の手順は到達不能コードになる**(監査A3)。
- **確定: レスキューの発火は、この if/else 連鎖の中に枝として入れる。** 位置は
  **ゲート2の枝の直後・`else if (puzzleActiveNow)`(退屈補正)の直前**:
  ```
  else if (レスキューの円に入った && §2-4の発火ゲート) { …レスキューの囲い… }
  ```
  - これで **①`activeEvent === null` は連鎖の外側(`!ae`)が保証 ②ゲートより後=
    「ゲート>他イベント」の既存の優先を崩さない ③退屈補正/2分タイマーとは else で排他**、が
    **条件を書かずに構造で**満たされる。**発火ゲートに「ゲート発火待ちが無いこと」を足す必要も無くなる。**
  - **★裁定2026-08-30後: 枝の中に「発火しない」経路は無い**(6条件が消えたため)。
    枝へ落ちた=**必ず発火する**。よって「塞がっているフレームに退屈囲いが割り込む/割り込まない」の
    分岐そのものが消えた。**★未決 #21「発火枝のゲートの置き場所」は前提関係ごと解消**(§2-15)。
    ただし**枝に入ったフレームは後続の `else if` へ落ちない**という性質は残るので、
    **レスキューの円の中で退屈補正の囲いが開くことは構造的に起きない**(=これが「他イベントと重ならない」
    の後半の担保。社長「重ならない様に注意」の、出現位置と対になるもう半分)。
  - **★事実として併記(「既存の抽選を1つも変えない」とは言えない点)**: この枝を
    `else if (puzzleActiveNow)` の**前**に置くので、**`puzzleActiveNow === false` の区間
    (フェーズ表が boss 扱いの区間・ラボ/通路/訓練/ダンス試験など)では、レスキューの枝が
    2分タイマーの枝より先に評価される**。
    → その区間に**プレイヤーがレスキューの円の中に居る**と、**2分タイマーの囲い/救助/緑卵が
    そのフレームは抽選されない**(レスキューの枝で連鎖が止まるため)。
    **頻度は低い**(§2-1 の対象ステージでは、boss 扱いの区間に円の中に立っている必要がある)が、
    **0ではない**ので「既存の発火を1つも変えない」とは書かない。
    ※`puzzleActiveNow === true`(通常プレイの大半)では**2分タイマーの枝は元から到達しない**ので、
    **その区間では評価順の変化そのものが起きない**(上記「★★訂正」と同じ事実)。
- ※この連鎖全体は
  `if (!danceTest && !indoor && !labTheme && !storyBoss && !isExStageRun() && !tutorialStage && !noSpawn)`
  の内側にある(=§2-1 の除外の一部はここでも効く)。**フリー出撃/練習/再訪の除外は §2-1 のゲート側で行う**
  (ここには足さない=判定を2箇所に分けない)。

**レスキューの囲いも、この順番で書く(番号どおり・入れ替え禁止)**:
1. **`beginArenaEvent({ kind:'horde', x: 二人組のx, y: 二人組のy, radius: eventQuestNpc.triggerRadius,
   startedAt: <今の gameTime>, endsAt: <今> + 90秒, rescueQuest: true })` を**先に**呼ぶ。**
   (半径は §2-3 のとおり store の `triggerRadius` を読む=見えている円と1pxも違わない。)
2. **編成の N 体を配置する**(§2-4「編成」・`spawnEnemyAtWithTier` / `addEnemy`)。
   配置した個体の **`id` をこの場でセットに控える**(完了判定のスコープ・下記)。
3. **その直後に `hordeSpawnRef.current = { spawned: N, nextAt: <今の gameTime>, total: N }` を代入する**
   (段階スポーンの停止・下記。ゲート1と同じ作法)。
4. 発火演出(`spawnRing` 2枚+フラッシュ+シェイク+軽いスロー)。
5. **通常囲いの次回発火時刻を押し戻す**(下記「★通常囲いの次回タイマーを押し戻す」)。

### ★`kind:'horde'` を名乗るだけで、指定していない敵が18体追加で湧く(必ず潰す)

**事実(実在確認)**: 囲いの更新ブロックには「horde の段階スポーン」があり、
**`ae.kind === 'horde'` である限り**、`hordeSpawnRef.current`(`{ spawned, nextAt, total }`)を見て
**1秒に1体ずつ `total` 体まで敵を追加し続ける**。`total` の既定は `ARENA_HORDE_COUNT`=**18**で、
種別は通し番号で `pumpkin` / `werewolf` / `zombie`・`skeleton`・`bat` ランダム。
さらに終了判定の `cleared` は
`hordeSpawnDone = ae.kind !== 'horde' || spawned >= total` を**要求する**ので、
**放っておくと「赤い削岩型1体+数体」のはずが18体流入し、全滅させても終わらない**。

**既存の「敵を先に全部置く」囲いはこれを自分で潰している**: ゲート1は配置直後に
`hordeSpawnRef.current = { spawned: gateSpawnedCount, nextAt: newGameTime, total: gateSpawnedCount }` を代入し、
コメントに「**全数即配置済み=段階スポーンは追加しない**」と書いてある。

- **レスキューの囲いも同じ作法を必ず行う**: 編成(§2-4「編成」)を**配置し終えた直後**に
  **`hordeSpawnRef.current = { spawned: N, nextAt: <今の gameTime>, total: N }`**(N=実際に置いた体数)を代入する。
  これを書かないと上の18体が流入する。

### ★このイベントを識別する専用フラグを `ActiveEvent` に足す

時間切れの専用枝(下記)も、完了判定のスコープ(下記)も、**「今の `activeEvent` がレスキューのものか」を
見分けられないと書けない**。v2初稿は何を見て分岐するかを書いていなかった。

- **`ActiveEvent` に `rescueQuest?: boolean` を足す**(型は `src/types/game.ts`)。
  **前例がそのまま実在する**: 同じ型の `policeArena?: boolean` は「警察署アリーナ由来の horde か」を
  1フラグで見分けて `cleared` 分岐の報酬経路を切り替えている(コメントに
  「通常の退屈アリーナと挙動は同じ(kind:'horde'を共用)なので、この1フラグだけで報酬経路を分岐する」)。
- レスキューの囲いを `beginArenaEvent` へ渡す時に `rescueQuest: true` を明示する。
  **以後の分岐は全部このフラグを見る**(時刻や座標で推測しない)。

### ★★立っている自分の囲いを、ゲート囲いが**外から畳む**(=城ボスが永久に出ない)

**事実(実在確認)**: `useGameLoop` には、**既に走っている `activeEvent` を外から強制解除する経路が実在する**。

```
const aePre = useGameStore.getState().activeEvent;
if (gateFireOk && activeGateRef.current == null && aePre && aePre.kind !== 'boss') {
  … gate1WouldFire || gate2WouldFire なら …
  useGameStore.getState().endArenaEvent();
}
```
コメントは「**ゲート発火待ちが立っていて、かつ城ボス以外のイベント(レスキュー/退屈囲い=
kind 'rescue'|'horde')が進行中なら、それを強制解除してゲートを発火可能にする**」
「`kind 'boss'` は強制解除しない(=protect)」。
→ **レスキューの囲いは `kind:'horde'` なので、この保護の外=対象**。
しかも `endArenaEvent()` は **`cleared` / `timedOut` のどちらの枝も通らずに**
`activeEvent = null` + `fromEvent` の敵を全消しするだけ。

**何が起きるか**: 囲いが**音もなく畳まれ、敵も消え、`rescueClearedAt` が立たない**。
→ **城ボスが永久に出ない=勝利不能**。さらに、円はその場に残り発火ゲートも空くので、
**「入る → 発火 → 次フレームで畳まれる」の無限ループ**になり得る(その間ずっと敵が湧いては消える)。

**確定(2段構え。①だけにしない)**

- **①プリエンプションの保護対象にレスキューを足す。**
  上の条件へ **`aePre.rescueQuest !== true`** を1つ足す(`aePre.kind !== 'boss'` と**同じ形の1フラグ分岐**。
  実在の前例=同じ型の `policeArena?: boolean`)。
  → ゲート発火待ちの間はゲートが空くのを待つ(**ゲートは待たされるだけで、詰まない**——
  ゲートの発火予約 `gate1PendingRef` / `gate2PendingRef` は消えず、囲いが終われば発火する)。
- **★②こちらが本命: `activeEvent` が消えたのに `rescueClearedAt` が未打刻なら、強制クリア扱いで打刻する
  後始末を1本置く。**
  - store に **`rescueArenaStartedAt`(数値・0=未発火)**を足し、上の手順1で打刻する(`resetGame` で 0)。
  - `useGameLoop` の二人組ブロックで毎フレーム1回だけ判定する:
    **`rescueArenaStartedAt > 0` かつ `rescueClearedAt === 0` かつ
    `activeEvent?.rescueQuest !== true`** なら → **§2-5 の完了経路へ入れる**
    (`rescueClearedAt` を打刻+受注会話をキューへ。全滅・時間切れとまったく同じ扱い)。
  - **理由**: 外から `endArenaEvent()` を呼ぶ経路は**この先も増える**(現に「ゲート優先」で1本増えた)。
    ①は既知の1経路を塞ぐだけだが、②は**まだ書かれていない経路にも効く**。
    「囲いが消えたのに完了していない」という**状態そのもの**を見ているので、
    誰がどこで消しても勝利不能にならない。
  - **副作用は無い**: 正常系では `cleared` / `timedOut` が先に `rescueClearedAt` を立てるので、
    この後始末は**そもそも成立しない**(打刻済みなら何もしない)。

### ★通常囲いの次回タイマーを押し戻す(レスキュー発火の直後に通常囲いが足元で開かないように)

**事実(実在確認)**: 通常囲いの「次はいつ出せるか」は
**発火した時にだけ**先送りされる(`nextArenaAtRef.current = newGameTime + ARENA_FIRE_INTERVAL_MS`(=2分) /
退屈補正の囲いは `boredomArenaNextEligibleAtRef.current = newGameTime + BOREDOM_ARENA_CD_MS`(=2分))。
**塞がれている間も時刻は進み続ける**ので、条件が空いた瞬間に発火しうる。
しかも**通常囲い/退屈補正の囲いは、どちらも円の中心が「プレイヤーの中心」**
(`const event = { kind, x: pcx, y: pcy, radius: ARENA_EVENT_RADIUS, … }`)。
→ レスキューの囲いが終わった直後の1フレームで、**プレイヤーの足元=さっきまでレスキューの円だった場所に
通常の囲いが開く**ことが普通に起こる(240px の円に連続で閉じ込められる)。

- **確定: レスキューの囲いを発火した時、通常囲いの次回時刻も同じだけ押し戻す**(上の手順5)。
  **`nextArenaAtRef.current = newGameTime + ARENA_FIRE_INTERVAL_MS`** と
  **`boredomArenaNextEligibleAtRef.current = newGameTime + BOREDOM_ARENA_CD_MS`** を代入する
  (=「囲いを1回やった」と同じ扱いにする。**新しい定数を作らない**)。

### ★★訂正: 通常プレイで実際に効くのは**退屈補正の側だけ**(`nextArenaAtRef` は届かない)

**事実(実在確認)**: 上の if/else 連鎖の並びは
**`… else if (puzzleActiveNow) { 退屈補正 } else { 2分タイマー }`**。
つまり **`puzzleActiveNow === true`(=通常プレイの大半)の間、2分タイマーの枝には一度も到達しない**
(`nextArenaAtRef` を読む `arenaReady` はその枝の中にある)。
到達するのは `puzzleActiveNow === false` の時だけ——`labTheme` / `indoor` / `danceTest` / `storyBoss` /
`tutorialStage` / **フェーズ表が boss 扱いの区間**(=`phaseAt(newGameTime).kind === 'boss'`)。

- → **通常プレイでは `nextArenaAtRef` の押し戻しは無意味(no-op)。効いているのは
  `boredomArenaNextEligibleAtRef`(退屈補正の囲い)だけ。**
- **押し戻しは両方とも書く**(**害が無く**、ボスフェーズ区間では2分タイマー枝が動くため保険になる)。
  **ただし「通常囲いが足元で開くのを防いだ」と説明してよいのは退屈補正の側だけ。**
- **★これは ★未決 #16「囲いの発火6条件のゲート」に渡している材料の訂正でもある。**
  #16「囲いの発火6条件のゲート」の材料①は「**空いた瞬間に2分タイマーの囲いがプレイヤーの足元で開く**」と書いていたが、
  **通常プレイではその枝に到達しない**ので**その形では成立しない**。
  **正しい材料**: 塞がっている間もタイマーは進み、**空いた瞬間に開きうるのは
  「退屈補正の囲い」(`boredomArenaNextEligibleAtRef` + 退屈シグナル成立)**——
  こちらも**中心はプレイヤーの中心**(`{ kind:'horde', x: bpcx, y: bpcy, radius: ARENA_EVENT_RADIUS }`)なので、
  **レスキューの円の中で開く**という結論そのものは変わらない(**周期的に再発しうる**のも変わらない)。
  **変わるのは「どちらの囲いが開くか」だけ。**

### ★終了条件:「無期限」にしない(既存の唯一の保険を外さない)

**事実(実在確認)**: 現行の囲いの終了判定は `cleared || timedOut` で、**`timedOut`(=`endsAt` 到達)が最後の保険**。
同じブロックのコメントに「**イベント敵が円外(地平線の上=透明化ゾーン)へ出ると `fromEvent` が0にならず
『誰もいないのに終わらない』状態になる**」と書かれており(引き戻しの安全策が置かれている)、
**この事故は既に起きたことがある**。`endsAt` を実質無期限にすると、1体でもノックバック/ジャンプ/逃走で
引き戻しをすり抜けた瞬間に**囲いが永久に終わらない → レスキュー未完了 → 城ボスが出ない=勝利不能**。
しかもプレイヤーは円内に拘束されたまま(`confinesPlayer` 既定true)で脱出もできない。

### ★「全滅」をこのイベントの敵に限定する(グローバル `fromEvent` を数えない)

**事実(実在確認)**: 現行の終了判定は
`const eventEnemies = useGameStore.getState().enemies.filter(e => e.fromEvent).length;` で、
**`fromEvent` を場全体で数えており、このイベントで湧かせた個体に限定していない**。
`fromEvent: true` を立てる箇所は**囲いの段階スポーン以外にも実在する**:
`beginRescueEvent` の攻撃者、ラボの `idol`、練習/デバッグ経路の各ボス、そして
**`gameStore` の拠点tickの `spawnList`(確保済み拠点への攻撃者)**。

- **事実として併記(思い込みで書かない)**: 拠点tickの攻撃者は**現在 `SUPP_BASE_ATTACKS_ENABLED = false` で
  丸ごとスキップされており、いま実際には湧かない**(コードは残置)。だが **S5は拠点2か所確保が必須**なので、
  この設計は「確保済み拠点の攻撃者が湧く条件」を仕様側から作っている。フラグが戻った瞬間に
  **`cleared` が永久に立たない**構造になる。**そもそも他イベントの敵を自分の終了条件に数えている時点で誤り**。

- **(a) 完了判定は、このイベントで湧かせた個体の id セットに限定する。**
  配置時に `id` を控え、`cleared` の判定は **そのセットの生存数が0か**で行う(`fromEvent` の全体数は数えない)。
  `hordeSpawnDone` 相当のガードは、上の「配置直後に `hordeSpawnRef` を潰す」作法で自動的に満たされる。
  ※`endArenaEvent` が終了時に `fromEvent` を全消しする挙動は**従来どおり**(そこは変えない)。
- **★「生存数0」の定義(実在確認)**: 倒された通常敵は**すぐには `enemies` から消えない**。
  `corpseEligible` な個体は `corpseUntil` を持って死体として配列に残り、
  `isCorpse(e)`(=`e.corpseUntil !== undefined`・`src/utils/enemyUtils.ts`)が true になる。
  **削岩型(`driller`)も `corpseEligible` は true**(`enemyUtils.test.ts` が `pumpkin` と同値で固定している)。
  → **`isCorpse(e)` の個体は生存に数えない。** 書かないと**全滅させても囲いが終わらず、
  90秒の時間切れまで円に閉じ込められる**(しかも「敵は倒したのに終わらない」=原因が見えない)。

### ★上限は 90 秒(既存の帯へ揃える)

- **`endsAt` = 開始 + 90秒**(**有限値**)。`POI_GUARD_DURATION_MS`(実質無期限)のような値にはしない。
  - **事実(実在確認)**: 既存の保険は **horde=`ARENA_HORDE_DURATION_MS`=40秒 / boss=`ARENA_BOSS_DURATION_MS`=60秒**。
    v2初稿の「10分」はこの帯から2桁外れており、**`confinesPlayer` 既定 true の240px円に10分間閉じ込められる**
    =「無期限にしない」という趣旨をまったく満たしていない。
  - ※**90秒という値の是非は ★未決 §2-15 #13「囲いの90秒上限」**(本文の確定値は90秒。社長が別の値を選べばそれに従う)。
- **時間切れは「失敗」ではなく強制クリア扱い**: 全滅と同じ経路(`rescueClearedAt` を打刻 → 受注会話)へ入れる。
- **★時間切れの既存 else 分岐を通さない**: 現行の `timedOut` 側は**全イベント共通で**
  バナー「**ゲート突破失敗 —— 押し戻された**」+ 原点方向への強制ノックバック + シェイクを出す
  (ゲート用の処理)。**このレスキューの囲いはこの分岐を通さない**専用の枝を持つこと。
  **分岐の条件は `ae.rescueQuest === true`**(上の専用フラグ。時刻や座標で推測しない)。
- プレイヤーから見える終わり方は**全滅も時間切れも同じ**(駆除成功→受注会話)。

### ★他イベントとの同時成立(`activeEvent` はシングルトン)

**事実(実在確認)**: 囲いは `activeEvent` の**1枠しかない**。抽選側も
`!bossChasing && !hiddenBossAlive && hunterRef.phase === 'idle' && !redNightActiveNow && !bountyBlocksArena` を
要求している。4:00 前後に通常の囲い/救助/緑卵/ゲート囲い/紅き夜/ハンター追跡/賞金首が動いている可能性は普通にある。
最悪なのは `activeEvent` を上書きした場合で、相手の終了処理が二重に走る
(`endArenaEvent` は `fromEvent` を全消し=**相手のイベント敵まで消える**)。

**★★社長裁定 2026-08-30(★未決 #16「囲いの発火6条件のゲート」= これで確定・ブロッカー解除)**

> 社長の言葉: 「**城ボスとは重なる事ないよね？ 裏ボス中だけ非表示で。／ あとは発火する ／
> ただし他のイベントの範囲とは重ならない様に注意**」

- **発火の6条件ANDは撤去する。** サークルに入ったら**発火する**。
  「入っても何も起きず、合図も出ない」という**原因の見えない勝利不能**は、これで構造ごと消える。
- **裏ボスだけは「阻止」ではなく「非表示」で扱う**(下の §2-2B の `hidden`)。
  **円も二人組も場に出さない**ので、プレイヤーには「まだ出ていない」としか見えない
  =**入れるのに反応しない状態を作らない**(これが旧ゲートの本体的な問題だった)。
- **他イベントとの衝突は「条件」ではなく「位置」と「構造」で避ける**(社長「重ならない様に注意」)。
  §2-3 の出現位置に**他イベントの範囲を避ける条件**を足す(下記)。

**★撤去してよい根拠(実在確認・思い込みで書かない)**

| 旧条件 | 撤去してよい理由(実在確認) |
|---|---|
| 1. `activeEvent === null` | **囲い系イベント中はプレイヤーが円に閉じ込められる**(`gameStore` の移動クランプ: `activeEvent && kind !== 'rescue' && confinesPlayer !== false` の時に円内へクランプ)。→ **閉じ込め型が立っている間、プレイヤーはレスキューの円へ物理的に到達できない**=枠の奪い合いが起きない。**`confinesPlayer: false` を実際に書いている生成箇所は1つも無い**(型と判定側だけに存在)ので、**閉じ込めない `activeEvent` は `kind:'rescue'`(救助ホールド)だけ**。その1件の扱いは下記。 |
| 2. `bossChasing` | 城ボスは **レスキュー完了まで出ない**(§2-2 受け入れ条件2)。裏ボスは上の「非表示」で扱う。残る「ボス追跡中」は賞金首等で、囲いの中で戦えば済む(ペーシング都合であって詰みではない)。 |
| 3. 裏ボス存命 | **撤去ではなく「非表示」へ置き換え**(社長裁定)。 |
| 4. ハンター追跡 | 自己解消する(立ち去りフェード)。重なっても囲いの中で戦えば済む。 |
| 5. 紅き夜 | 20秒で自己解消する。 |
| 6. 賞金首の存命 | **旧ゲート唯一の"無限"の原因**(索敵圏に円が重なるとタイマーが永久に進まない)。ゲートを撤去すれば無限そのものが消える。**位置条件(下記)で索敵圏も避ける**ので、重なりも減る。 |

- **★閉じ込めない唯一の型(`kind:'rescue'`=救助ホールド)と重なった時だけの手順**:
  出現位置の条件(§2-3)で**避けるのが第一**。それでも発火の瞬間に `activeEvent?.kind === 'rescue'` が
  立っていたら、**`endArenaEvent()` で相手を正規終了させてから** `beginArenaEvent()` を呼ぶ。
  **上書きしない**(上書きすると救助ホールドの生存者とイベント敵の後始末が走らず宙に浮く)。
  **待たせない**ので「入っても何も起きない」は作らない。
- **★「ゲート発火待ち」は条件に足さない。** 上の「★★発火のコードを置く場所」のとおり
  **ゲート1/ゲート2の枝の後ろに else if で入れる**ので、ゲート発火待ちのフレームは**構造的に**
  レスキューへ来ない(同フレームで両方が `beginArenaEvent` を呼ぶ経路が存在しない)。
  **条件で守らず構造で守る**——条件は書き漏らすが、else は漏れないため。
- **4:00 のレスキュー出現そのものは、他イベントに関係なく行う**(遅延させない)。
  **唯一の例外が裏ボス**で、それは「遅延」ではなく「非表示」として扱う(§2-2B)。

**★城ボスと重なるか(社長の問いへの答え=重ならない・実在確認)**
1. **時間**: 城ボスの出現は「**5:00 と `rescueClearedAt` の遅い方**」(§2-2 受け入れ条件2/3)。
   **レスキューが終わるまで城ボスは出ない**ので、囲いの最中に城ボスが出てくることはない。
2. **場所**: 出現位置の候補は `resolveOutOfSolids`(→`resolveBountyMove`)を通す。その中に
   **`castleEvent`(城の矩形)が入っている**ので、**城の上に円が出ることはない**。
3. 城ボス撃破後の帰還サークルは城跡付近だが、その時点でレスキューは完了済み(円は無い)。

**★「裏ボス」の述語は4体に限定する(v3・監査A7)**
- **`isHiddenBoss` を使ってはいけない**——実在の `isHiddenBoss` は
  `mimir|jormungand|skadi|thor` に加えて **天使6体(miguel/jibril/rafi/uri/suriel/acrasiel)+idol+phillboss**
  を含む。そのまま使うと **S1/S3 で普通に起きるゲート2ボス戦の間もレスキューが消える**
  =社長の言う「裏ボス」より広い範囲を実装が勝手に決めたことになる。
- **確定: 対象は裏ボス4体だけ**(`mimir` / `jormungand` / `skadi` / `thor`)。
  **専用の述語を1本作って両方(遷移6・7)が同じものを読む**(型名を2箇所に書かない)。

**★★★未決 #23「裏ボス非表示の窓に上限も合図も無い」(v3・監査A4で新規に立てた・社長裁定へ)**
- **①問題**: 裏ボスは「深層域へ近づくと湧き、**深層戦闘域を3秒出た時にだけ**帰巣して消える」型なので、
  **プレイヤーが深層に留まる限り存命=`hidden` に時間上限が無い**。その間は円も二人も**縁矢印も**出ない
  (矢印は `status === 'rescue'` の間だけ渡す)。§2-9 は「レスキューが未完了の間は位置を常に提示し続ける」を
  **【確定・必須要件】**にしているので、**本文どうしが矛盾している**。
- **②選べる案**: (a) **このまま**(裏ボスが居る=プレイヤーが深層に居る、なので浅い所のレスキューへ
  そもそも向かっていない。浅い所へ戻れば裏ボスは帰巣して消える=自己解消する) /
  (b) **非表示の間だけ、理由の分かる合図を出す**(例: 縁矢印の代わりに小さなバナー1回) /
  (c) **非表示をやめて「入っても発火しない」に戻す**(=#16 で潰した失敗形なので非推薦)。
- **③推薦=(a)**。理由: 裏ボスの存命条件が「プレイヤーが深層に居ること」と実質同義なので、
  **プレイヤーから見た「消えている時間」は自分が深層に居る時間**であり、迷子にならない。
  合図を足すと「非表示」という裁定の意図(=存在しないように見せる)と食い違う。
  **ただし §2-9 の必須要件は「表示されている間は提示し続ける」へ限定する必要がある**(この1行は(a)でも要る)。
- ※(a) を採る場合の1行修正は**先に本文へ入れてある**(§2-9 の必須要件の但し書き)。

**★戦闘中は非表示にしない理由(v3・監査A6)**
- 遷移2(`rescue → accepted`)は**全滅の瞬間**なので、**発火〜全滅の最大90秒は `rescue` のまま**。
  遷移6の契機を「裏ボスが居る最初のフレーム」だけにすると、**戦闘の最中に二人が消えうる**
  (=§2-4 受け入れ条件3「囲い中、二人組は必ず円内に居る」が破れる)。
  ⇒ 遷移6に **「自分の囲いがまだ発火していない(`rescueArenaStartedAt === undefined`)」を条件として明記する。**
- **★事実として併記(実在確認)**: 裏ボスの出現条件には `&& !useGameStore.getState().activeEvent`
  (「囲い系イベント中は裏ボスを出さない」)が既にあるので、**現状は偶然守られている**。
  だが**その保護は設計書のどこにも書かれていなかった**ので、既存ガードが動いた瞬間に再発する。
  **条件で守る(上記)+ 既存ガードは事実として記録**、の二重にする。

**受け入れ条件(この裁定の分)**
1. **サークルに入れば必ず囲いが始まる**(他イベント・ハンター・紅き夜・賞金首・ボス追跡の有無に関係なく)。
2. **裏ボスが場に居る間は、円も二人組も画面に出ない**(投影影も出ない=`hidden` の扱い)。
3. **裏ボスが消えたら同じ場所に戻る**(出現抽選をやり直さない=同じイベントが2度別の場所に出ない)。
4. **囲いが始まった後(=`accepted` 以降)は、裏ボスが出ても非表示にしない**(戦闘中/受注済みを消さない)。
5. 発火の瞬間に `activeEvent` が立っていた場合、**相手の終了処理が正規に走ってから**レスキューの囲いが始まる
   (相手のイベント敵/生存者が宙に浮かない)。

### 編成

- **§9 の削岩型(`driller`)の赤個体1体** + 数体の敵(★数と種別は §2-15 #1「レスキュー囲いの中身」)。
  - 赤個体 = `colorTier: 'red'`。`spawnEnemyAtWithTier` で色を強制指定する(抽選に任せない)。
  - 全員 `fromEvent: true` / `dormant: true` / `aggroRange = EVENT_SPAWN_AGGRO_RANGE` (既存の囲いと同じ作法)。
- **色の文法(CLAUDE.md)との整合**: ここでいう「赤」は**個体色(colorTier)**であって、
  「赤=カウンター/回避の対象」の予告色とは別物。削岩型の突きの**赤い予告帯は従来どおり判定と厳密一致**
  (§9-4)で、個体色の変更はこの一致に影響しない。
  - ただし**赤ティアは本体スプライトを `0xff5544` で塗る**(`ENEMY_COLOR_TIER_BODY_TINT`)ため、
    **真っ赤な本体から赤い予告帯が出て「どこまでが判定か」の一発読みが濁る**懸念がある
    (=判定を持たない本体の塗りが画面上に大量に出る)。**扱いは ★未決 §2-15 #2「赤い削岩型をS1/S3に出すか」**。
- **拒否不能・不可逆**: サークルに触れた時点で**戦闘確定。辞退はできない**(円から出られない)。
  瀕死でも同じ。進入前の警告表示を出すかは **★未決 §2-15 #10「囲い進入前の警告表示」**。
  **これを勝利の必須経路に置いてよいか自体は ★未決 §2-15 #12「囲いを拒否不能のまま必須経路にするか」**(本文の現行仕様=拒否不能、は据え置き)。
- **囲いの最中にワクチンで復活した時は、囲いをそのまま継続する**(拘束・敵・進捗をすべて維持。
  復活は他の場面とまったく同じ扱いで、囲いには何の分岐も足さない=**既存挙動を1つも変えない**)。
  ワクチンを持たずに死んだ場合は従来どおりランが終わる(変更なし)。
  ※事実: `player.vaccineRevives` による復活は実在する(病院で入手/ショップで購入。致死ダメージを
  打ち消してHP半分・無敵で復活)。「死亡=ランが終わる」は前提として成り立たない。
- 発火演出は既存の囲いに合わせる(リング+フラッシュ+シェイク+軽いスロー)。**新しい演出は作らない**。

**受け入れ条件**
1. サークルに入るまで囲いは始まらない(近づいただけでは始まらない)。
2. 囲い中は既存の囲いと同じくプレイヤーが円内に拘束される。
3. **囲い中、二人組は必ず円内に居る**(円の中心=二人組の座標)。
4. 敵を全滅させると囲いが終わる。
4b. **★囲い開始の直後(発火から1秒以内)、円内に編成の N 体が全員存在する。**
   赤い削岩型だけが残ってゾンビ系が消えている、が起きない
   (=`beginArenaEvent` を敵の配置より**先**に呼んでいる。逆順だと `isArenaSweepProtected` が
   `driller` だけを守るため削岩型1体しか残らない)。
5. **囲いで湧く敵は設計した編成だけ**(§2-4「編成」の N 体)。**`zombie`/`skeleton`/`bat` が
   1秒ごとに追加で流入しない**(=`hordeSpawnRef` を配置直後に潰してある)。
   **N体を全滅させた時点で必ず終わる**(`hordeSpawnDone` 待ちで止まらない)。
5b. **最後の1体を倒した瞬間に囲いが終わる**(死体(`corpseUntil` 付き)が円内に残っていても、
   それを生存に数えて待たない=「倒したのに終わらない」が起きない)。
6. **このイベントの敵を全滅させれば、場に他イベント由来の `fromEvent` 敵が居ても囲いは終わる**
   (完了判定がグローバルな `fromEvent` 数を見ていない)。
7. **イベント敵が1体でも取り残されても、90秒で必ず終わる**(=永久に終わらない状態にならない)。
   その時**「ゲート突破失敗 —— 押し戻された」バナーも原点方向へのノックバックも出ない**。
8. **240pxの円に閉じ込められる時間は、どんな場合でも90秒を超えない。**
9. **★(社長裁定2026-08-30で書き換え)他イベント(通常の囲い/ゲート囲い/救助/紅き夜/ハンター追跡/
   賞金首)の最中にサークルへ入っても、①**囲いは必ず発火し**、②**既存イベントが壊れない**
   (相手のイベント敵が消えない・相手の終了処理が二重に走らない)。
   - 閉じ込め型の `activeEvent` が立っている間は**プレイヤーが円へ到達できない**ので、この状況自体が起きない。
   - 到達できる唯一の型 `kind:'rescue'`(救助ホールド)の時は、**相手を `endArenaEvent()` で正規終了させてから**
     レスキューの囲いを開く(下の「★★発火のコードを置く場所」の**前置き**)。
   - **裏ボスだけは別扱い**(発火を止めるのではなく、そもそも**円が出ていない**=§2-2B の `hidden`)。
10. 4:00 に他イベントが動いていても、**レスキュー地点の出現自体は遅れない**。
11. 赤い削岩型が**必ず1体**出る(色抽選のブレで色なしになることが無い)。
12. 削岩型の赤い突き予告帯と実際の判定が一致している
    (確認手段: **`src/utils/drillerAi.test.ts`(`§9-4 間合い3分岐` ほか)が緑のまま**であること)。
13. 囲いの最中にワクチンで復活しても、囲い・拘束・敵の進捗が維持される(囲いが勝手に畳まれない)。
14. **囲いの敵の見え方が、通常の囲いの敵と1つも変わらない**(地平線フェード・足元影ともに
    この改修で例外を作っていない=floor を敷いていない・§2-3)。
15. **★ゲート発火待ちの状態(未確認境界を踏破した直後)でレスキューの囲いに入っても、
    囲いが外から畳まれない。** 敵が突然消えない/円に入り直しても即座に畳まれる無限ループにならない
    (=プリエンプションが `aePre.rescueQuest !== true` で保護されている)。
15b. **★どんな理由であれ `activeEvent` が消えた時、`rescueClearedAt` が必ず立つ。**
    検証: 囲いの最中に `endArenaEvent()` が外から呼ばれた状況を作っても(ゲート優先の強制解除など)、
    **その後 5:00 以降に城ボスが出る**(=勝利不能ランにならない)。
16. **レスキューの囲いが終わった直後の数秒に、プレイヤーの足元で通常の囲い/退屈補正の囲いが開かない**
    (次回タイマーを押し戻してある)。
17. **同じフレームに `beginArenaEvent` が2回呼ばれない**(レスキューの発火が既存の if/else 連鎖の中にある)。
    検証: レスキューの円の中でゲート境界を踏んでも、**片方だけが発火し、もう片方は待つ**。

---

## 2-5. レスキュー完了と受注会話

- **レスキュー完了 = 囲いの敵が全滅した瞬間**。**完了として扱う契機は次の3つで、どれも同じ経路へ入れる**:
  1. **全滅**(`cleared`)
  2. **90秒の時間切れ**(`timedOut`・§2-4。失敗ではなく強制クリア扱い)
  3. **★囲いが外から畳まれた**(`activeEvent` が消えたのに未打刻・§2-4「★★立っている自分の囲いを、
     ゲート囲いが外から畳む」の後始末)
  その瞬間に **`rescueClearedAt` を打刻する**。これが城ボスの押し出し条件の基準時刻(§2-6)。
  **3つとも、プレイヤーから見える終わり方は同じ**(駆除成功→受注会話)。
- **★`rescueClearedAt` は store のフィールド(数値・0=未)。`resetGame` で 0 に戻す。**(§2-14 に行あり)
  - **`useRef` にしない。** 理由①**HUD(§2-7)がこのランでクエストが進行中かの判定に読む**ので、
    `useGameLoop` 内の `useRef` では**読めない**(`castleAttnRef` を否定したのと**まったく同じ理由**)。
    理由②`useRef` は `resetGame` で消えないので、**前のランの値が残ったまま次のランが始まり、
    レスキューを1度もやっていないのに 5:00 で城ボスが出て §2-7 の黄色い行まで出る**
    (しかも納品が起こらないので**その行は永久に消えない**)。
  - **打刻するのは1箇所だけ**(この二人組ブロック)。既に `> 0` なら**上書きしない**(受け入れ条件1b)。
  - **同じ set で `status` を `rescue` → `accepted` へ進める**(§2-2B の遷移2)。
- 同じ瞬間に **「受注時」の通信**(§2-13 の①〜③)を左上の会話(`enqueueNpcDialogue`)へ**流すだけ**。
  **会話は完了判定に一切関与しない**(会話の終了を待たない。原稿が0行のステージ=S5でも完了する)。
- 会話の本文は §2-13 を**一言一句そのまま**。話者名は `グレン`/`ミラ`(`NpcDialogue` のバストアップ対応)。

**受け入れ条件**
1. 全滅の直後に `rescueClearedAt` が立ち、受注時の会話が1回だけ流れる(2回流れない)。
1b. **上の3契機のどれで終わっても、`rescueClearedAt` が立つのは1回だけ**(全滅と後始末が同じフレームに
   重なっても、会話が2回流れない/打刻が上書きされない)。
2. **原稿が0行のステージでも `rescueClearedAt` が立つ**(会話が無いせいで完了しない、が起きない)。
2b. **★レスキューを1度もやらずにランを終えた次の出撃で、`rescueClearedAt` が 0 から始まる。**
   検証: ①レスキューに触らずに死亡/撤退 → 同じステージへ再出撃 → **5:00 を過ぎても城ボスが出ない**
   (前ランの打刻が残っていない) ②その再出撃で **§2-7 の黄色い行が1度も出ない**。
   (=`rescueClearedAt` が store にあり `resetGame` で 0 に戻っている。`useRef` で実装されていない。)
3. 会話の本文・話者・順序が §2-13 と1文字も違わない。
4. **受注会話の開始と同じフレームで城ボスが湧かない**(城ボスの出現は最短でも
   `rescueClearedAt + 3.0秒`・§2-6)。
   - ★注意: **「会話が終わるまで城ボスが出ない」ではない。** 受注会話は3〜4行=8.9〜13.7秒あるので、
     固定ディレイ 3.0秒の後に出現・その 950ms 後にアテンション(カメラ寄せ+カットイン)が入る=
     **会話の途中にアテンションが重なる**。**それでよい**(v2初稿の「会話中に割り込まない」は
     「会話に依存しない」という §2-0 の中核原則と相互に排他だったので撤回する。
     会話終了待ちにすると、原稿0行のS5と原稿ありのS1/S3/S4で城ボスの出現時刻が変わってしまう)。

---

## 2-6. 城ボス(5:00 / レスキュー未完了ならその分後ろへ)

- **出現時刻 = max(5:00, `rescueClearedAt` + `RESCUE_TO_CASTLE_DELAY_MS`)**。**ディレイは固定 3.0秒**。
  - 5:00 = 既存 `CASTLE_BOSS_MIN_TIME_MS`(実在確認: **現行は5分**。v1章の「7分」は古い記述)。
  - レスキュー未完了の間は**何分経っても出ない**。完了した時点で(5:00を過ぎていれば)3.0秒後に出る。
  - **★なぜディレイが要るか(実在確認)**: ディレイ 0 だと、5:00 以降に全滅した瞬間の**同じフレーム**で
    受注会話(3〜4行)が始まり、城ボスが湧き、その **950ms 後**にアテンション
    (`castleAttnRef.current = { at: newGameTime + 950, ... }` → カメラ寄せ+カットイン+時間停止)が入る。
    全滅の余韻・駆除成功バナー・会話の1行目に**大技の演出が被って全部潰れる**。
  - **★ディレイは会話の長さに依存させない**(=「会話が終わるまで待つ」にしない)。
    待つ形にすると原稿0行のS5と原稿ありのS1/S3/S4で城ボスの出現時刻が変わり、
    §2-0 の中核原則「**完了判定はイベント側の事実だけで決まる。会話には依存しない**」を壊す。
  - 3.0秒は**全滅からアテンションまで合計 3.95秒**を作る値(3.0 + 既存の 950ms)。**受注会話の途中に
    アテンションが重なるのは仕様どおり**(§2-5 受け入れ条件4)。
- **既存のゲートは1つも外さない**: ボス交戦中の先送り(`castleSpawnLocked` / `facilitiesLocked`)、
  通路/EX/ラボ/訓練/再訪の除外は従来どおり。今回足すのは「クエストステージ未クリアなら
  **レスキュー完了済み** を AND する」の1条件だけ。
- **★`questGateOk` の中身(確定・1行)**: **`eventQuestNpc.status === 'gone' || rescueClearedAt > 0`**。
  - 前半が §2-1 の述語(=このランでクエストが有効でない)の裏返し。**`getEventQuestConfig(...)` で
    判定しない**——設定はステージにしか紐づかないので、**フリー出撃・練習・ベンチ・ボスメーカーでも
    true になり、城ボスがゲートされて詰む**(§2-1「★★唯一の出どころ」)。
  - 後半が「レスキュー完了済み」。**`rescueClearedAt` は store のフィールド**(§2-14)。
- **★ANDを掛ける先(実在確認)**: 現行は
  `castleBossReady = FORCE_CASTLE_BOSS || practiceForces('castlenow') || newGameTime >= CASTLE_BOSS_MIN_TIME_MS`。
  **`castleBossReady` 全体に AND すると、開発用の強制出現(`?castlenow=1` / `FORCE_CASTLE_BOSS`)まで塞がり、
  S1/S3/S4/S5 で城ボスの実機確認・ボス演出の検証ができなくなる。**
  よって **AND は「時間条件の項」にだけ掛ける**:
  `FORCE_CASTLE_BOSS || practiceForces('castlenow') || (newGameTime >= CASTLE_BOSS_MIN_TIME_MS && questGateOk)`。
- 出現演出(魔法陣・バナー「危険変異体出現」・アテンション・カットイン)は**従来どおり不変**。

**受け入れ条件**
1. レスキュー未完了のまま 5:00・6:00・7:00 を過ぎても城ボスが出ない。
2. レスキュー完了が 5:00 以降なら、**完了(全滅)から 3.0秒後**に城ボスが出る
   (**同じフレームでは出ない**)。完了が 5:00 より前なら、ちょうど 5:00 に出る。
2b. **原稿0行のS5でも、原稿3〜4行のS1/S3/S4でも、全滅から城ボス出現までの時間が同じ**
   (会話の行数で出現時刻が変わらない)。
3. **`?castlenow=1` / `FORCE_CASTLE_BOSS` は S1/S3/S4/S5 でも従来どおり効く**(レスキュー未完了でも出る)。
3b. **★★その確認ランが「帰れないラン」にならない(v3・監査A8)。**
   **穴の形**: 強制出現 → 撃破 → `beginReturnPhase` で帰還サークルが出る → §2-2B 遷移3
   (`accepted → warping`)は「`returnCircle` が非nullになった**最初のフレーム**」の**エッジ**で書くが、
   status は `hidden`/`rescue` なので**遷移しない** → `delivering` に入らない → 3秒滞在が始まらない →
   **納品が成立しない**。一方 §2-8(A)は `requestStoryReturnPrompt` を「`deliveryLocked` **または**
   納品未成立」の間ずっと塞ぐので**確認ダイアログも出ない**。`updateReturnPhase` もクエスト中は
   `gameWon` を立てない。⇒ **帰る手段が1つも無い。**
   - **確定①(素通り防止の条件を直す)**: `requestStoryReturnPrompt` を塞ぐのは
     **「二人組が `warping` / `delivering` のいずれかである(=納品の経路に乗っている)」時だけ**にする。
     **`accepted` へ到達していないランでは従来どおり確認ダイアログを出す**(=帰れる)。
     ⇒ 本来の目的(「サンプルを渡さずリザルトへ行ける」を塞ぐ)は**納品経路に乗っている時だけ**守れば足りる。
   - **確定②(エッジ駆動の穴を塞ぐ)**: 遷移3の契機を**エッジではなく状態で書く**——
     「**`returnCircle` が非null かつ `status === 'accepted'`**」なら `warping` を開始する
     (円が出た**後**に `accepted` へ進んだ場合も拾える)。
   - 受け入れ条件: **`?castlenow=1` で城ボスを出して倒したランが、必ずリザルトまで到達できる**
     (S1/S3/S4/S5 の全部で)。
4. 対象外ステージ/クリア済みランでは 5:00 ちょうどに出る(変化なし)。
5. 別ボスと交戦中の先送りが従来どおり効く。

---

## 2-7. サブクエスト欄の討伐目標表示

- **城ボスの出現アテンションの後**、サブクエスト欄(右上・スクラップ下の縦積み)の**一番上**に
  **`[搬送体(変異)の討伐 0/1]`** を出す。

### ★「アテンションの後」を何で判定するか(読む先を名指しする)

**★v2 2巡目の「`castleEvent.bossSpawned === true` かつ `attention === null`」は撤回する。**

**事実(実在確認)**: 城ボスの出現ブロックは、湧かせたその場で `markCastleBossSpawned()` を呼び
(=`castleEvent.bossSpawned` が**即 true**)、アテンションは**その 950ms 後**に別のディスパッチャが
`castleAttnRef.current = { at: newGameTime + 950, ... }` を消化して `triggerAttention` する。
→ **出現〜950ms の間は `bossSpawned === true` かつ `attention === null` が成立する**ので、
上の条件だと **黄色の討伐行がアテンションより先に出る**=自分の受け入れ条件1(「城ボス出現アテンションより
前には表示されない」)に違反する。

**逆側(「一度 `attention` が非nullになったのを見てから」)も採らない。** その形は
「アテンションが必ず一度は張られる」ことに依存する。ディスパッチャは
`castleAttnRef.current.at > 0 && newGameTime >= at && !attention` の3条件で待つ設計なので
**現行では捨てられずに待ち続ける**が、**HUDの表示可否をこの内部タイミングの読みに賭ける形にはしない**
(表示が出ない=「倒したのに何をすればいいか分からない」へ倒れるため)。

**→ store に「アテンションが実際に張られた瞬間」を1フィールドとして持たせる(確定)**:
- **`castleAttnDoneAt`(仮名・数値・0=未)** を store に足す。
  `useGameLoop` の城ボス遅延アテンションのディスパッチャが **`triggerAttention(...)` を呼んだのと
  同じ行で** `castleAttnDoneAt = newGameTime` を打刻する(=「実際に張った」という**事実**を記録する)。
  `resetGame` で 0 に戻す。
### ★★「`castleAttnDoneAt > 0` の1本だけ」は**撤回する**(クエストと無関係な全ランで黄色の行が出る)

**事実(実在確認)**: `castleAttnDoneAt` を打刻することになるディスパッチャ
(`castleAttnRef.current.at > 0 && newGameTime >= at && !attention` の中で `triggerAttention` を呼ぶ箇所)は、
**クエストの有無を一切見ていない**。城ボスのアテンションは
**対象外ステージでも・納品済みステージでも・`?castlenow=1` でも・練習でも張られる**。
→ 表示条件を「`castleAttnDoneAt > 0` の1本だけ」にすると、
**レスキューを1度もやっていないラン全部で `[搬送体(変異)の討伐 0/1]` が出る。**
しかも城ボスを倒すと `1/1` → `[サンプルを届ける]` に変わり、
**納品という契機が起こらないので永久に消えない**(消す契機が納品しかないため)。

- **確定: 表示条件は次の AND にする。**
  1. **このランでクエストが進行中** = **レスキュー完了済み(`rescueClearedAt > 0`)かつ 納品が未成立**
     (=`eventQuestNpc.status` が `accepted` / `warping` / `delivering` のいずれか。
     `completed` / `gone` / `hidden` / `rescue` では出ない)
  2. **`castleAttnDoneAt > 0`**(アテンションが実際に張られた)
- **`castleEvent.bossSpawned` も `attention` も見ない**(2つを組み合わせた瞬間に上の 950ms 窓が復活する)。
  **950ms 窓を潰したいのは `bossSpawned`/`attention` の組み合わせの話**であって、
  **クエスト在否の AND とは無関係**(=1に足しても窓は復活しない)。
- **「1本だけ」という言い切りは撤回する。** 条件は2つ。**増やしてよいのはここまで**
  (3つ目を足したくなったら ★未決へ)。
- **`castleAttnRef` は `useGameLoop` 内の `useRef` で store に無い=HUD からは読めない**ので使わない。
  **判定を useRef や描画都合の値から作らない**(CLAUDE.md「判定は world/store 側に置く」)。
- **メインクエストらしく色を変える**: 黄色系(叩き台 `#facc15`)。通常サブクエ(白/達成で緑)と差別化する。
- **`1/1` を立てるソースは `finaleDefeated`(store の単一フラグ)だけ**にする。
  ※城ボス撃破の検知経路は複数ある(`triggerDramaticDeath` 経由 / 各キル経路の `isFinalBossKill` /
  `markCastleBossCleared`)が、**それらは全部 `finaleDefeated` に集約されている**。表示側は
  `finaleDefeated` 1本だけを読み、他の経路を直接見ない(二重カウント・取りこぼしの防止)。
- **討伐後もこの行は消さない**。`1/1` になったら行を **`[サンプルを届ける]`** に差し替え、
  **消すのは納品が成立した瞬間**。理由=「倒したのに終わらない」の迷子を防ぐのがこの行の役目のため。

### ★どこに置くか(実在確認・一意に決める)

- **`state.subquests` 配列には触れない。** そこへ混ぜると `SUBQUEST_SAVE_KEY`(`zombie.progress.subquests`)の
  `active[]` 保存/補充ロジックまで汚れる。
- **`SubquestHud` とは別コンポーネント**を、同じ縦積みの**先頭**(=現行 `EventQuestPill` が置かれている位置)に
  置く。縦積みの列は `GameHUD.tsx` の「クエスト列」(`EventQuestPill` と `SubquestHud` を並べている div)。
- `SubquestHud.tsx` は色を `r.done` だけで決めており**行データに色を持っていない**ので、
  黄色の行を `SubquestHud` に流し込む改造はしない(別コンポーネントにする理由でもある)。

### ★購読の形を確定する(書かないと 60fps 再描画になる)

**事実**: `eventQuestNpc` は `warping` 中に**毎フレーム座標を書き換える**(§2-14)ので、
**`useGameStore(s => s.eventQuestNpc)` と書かれた瞬間に毎フレーム新参照=HUDが60回/秒 再描画される**
(CLAUDE.md「React re-render discipline」違反)。**オブジェクトを購読しない。**

- **確定: 使うフィールドを `boolean` / 文字列の**プリミティブ**で個別に購読する。**
  ```
  const attnDone   = useGameStore(s => s.castleAttnDoneAt > 0);      // boolean
  const rescued    = useGameStore(s => s.rescueClearedAt > 0);       // boolean
  const qStatus    = useGameStore(s => s.eventQuestNpc.status);      // 文字列(遷移時にしか変わらない)
  const bossKilled = useGameStore(s => s.finaleDefeated);            // boolean
  ```
  → 書き換わるのは**アテンション成立/レスキュー完了/状態遷移/城ボス撃破**の**4回だけ**。
  `s.eventQuestNpc.status` は**オブジェクトではなく文字列を返す選択子**なので、
  親オブジェクトの参照が毎フレーム変わっても**再描画されない**。
- **手本は同じファイルに実在する**: 現行の `EventQuestPill` は
  `s.eventQuestActive` / `s.eventQuestKills` / `s.eventQuestGoalCount` の**プリミティブ3つ**を
  個別に購読しており、コメントにも「購読はプリミティブ3つだけ(…=React再描画規律)」と書かれている。
  **同じ形にする。**
- **`shallow` の小袋も使わない**(4つとも別々の契機で動くプリミティブなので、個別購読で足りる)。

**受け入れ条件**
1. 城ボス出現アテンションより前には表示されない。**城ボスが湧いてからアテンションが始まるまでの
   950ms の間も出ない**(=`bossSpawned` を見ていない)。
1b. アテンションが終わった直後には**必ず出ている**(「出ないまま」に倒れない)。
1c. **★クエストと無関係なランでは1度も出ない。** 具体的に、次のどれでも黄色の行が出ないこと:
   ①stage-2/6/7/ex ②**納品済み(クリア済み)ステージへの再出撃** ③フリー出撃 ④練習/ガントレット/ベンチ
   ⑤**`?castlenow=1` / `FORCE_CASTLE_BOSS` で城ボスを出した確認ラン**(レスキュー未完了)。
   **とくに⑤で出ると、`1/1` に変わったあと納品が起きないので画面から永久に消えない。**
2. 常にサブクエスト欄の最上段に出る(自動サブクエより上)。
3. 通常サブクエと色が明確に違う。
4. この行の追加で**自動サブクエの枠数(2)が減らない**。`SUBQUEST_SAVE_KEY` の保存内容が
   **この行のせいで1バイトも変わらない**。
5. 城ボス撃破で `1/1` になり、行が `[サンプルを届ける]` へ変わり、**納品の瞬間に消える**。
6. 表示は**進捗が動いた時にだけ**書き換わる(毎フレーム再描画しない=React再描画規律)。
6b. **★二人組が `warping`(毎フレーム座標が動く)の間も、この行のコンポーネントが再描画されない。**
   検証: React DevTools の Highlight updates(または `?perf` の描画計測)で、
   **城ボス撃破〜着地までの飛行中にこの行が1回も再描画されない**こと
   (=`useGameStore(s => s.eventQuestNpc)` のようなオブジェクト購読になっていない)。

---

## 2-8. 帰還サークル = 納品サークル(統合)・二人のワープ・納品→リザルト

社長の言葉(2026-08-21): 「**このサークルを二人組にしちゃえばいいか?**」「**つまりボス倒すとワープしてくるイメージだよ?**」

- 城ボス討伐 → **帰還サークルは従来どおりの場所(城跡付近)に1つだけ出る**(`beginReturnPhase` の出し方は変えない)。
- **そのサークルへ二人組がワープしてくる**(サンプルを受け取りに来る)。**プレイヤーが二人の元へ戻るのではない**。
- 二人組は**同時に2箇所に居てはいけない**。レスキュー地点の二人は**飛び去り**、サークルの二人に**1体化**する。

### 納品の成立と、その後の7〜10秒

**事実(実在確認)**: 会話は `NPC_DIALOGUE_MS`(2800ms)×行数 + `NPC_DIALOGUE_GAP_MS`(500ms)の**時間駆動**で、
**ゲームは止まらない**。2〜3行で 6.6〜9.9秒あり、その間プレイヤーは操作でき、敵に殴られ、**死ねる**。
既存の `answerStoryReturnPrompt` は `isPaused: true` を伴うのに、納品側には何の指定も無かった。

### ★★`isPaused` は使わない(v2初稿の「`isPaused` 相当」は撤回)

**事実(実在確認)**: `useGameLoop` の `updateNpcDialogue(newGameTime)` の呼び出しは
**`if (!isPaused && !backgrounded && (rendererReady))` ブロックの内側**にある。そして
`updateNpcDialogue` は `NPC_DIALOGUE_MS`(2800)の**時間駆動**で行を送る。

→ **納品成立 → `isPaused: true` → ループが止まる → 会話が1行も進まない → キューが永遠に空にならない
→ `gameWon` に到達せず、入力も効かない=完全なフリーズ。** 原稿のある S1/S3/S4 は**100%停止**する。

既存の `answerStoryReturnPrompt` が `isPaused: true` を使えているのは、それが
**「タップ待ちのモーダル(帰還しますか?)」で、解除がプレイヤーの入力だから**。
**時間駆動の待ちに転用した瞬間にデッドロックになる。**

### ★★この節は**確定**(社長裁定 2026-08-22「サークル3秒で。」=(A))

**2巡目までの本文は「納品成立=3秒滞在の満了」を確定仕様として書いていたが、その滞在を数える実体が無い。**

**★★確定(v3・監査A10): 滞在を積むのも、満了で手順1〜5を実行するのも `useGameLoop` の二人組ブロック1箇所。**
`updateReturnPhase`(store の `set()` リデューサ)には**滞在を積ませない**。理由: 手順1〜5は
**永続書き込み・`enqueueNpcDialogue`・`completeEventQuest`・`gameWon`** を含み、リデューサから素直に呼べない。
2箇所に分けると「満了の検知」がループとstoreに生え、**この設計書が3回警告している「同じ判定を2箇所に書く」型**を
案件の中心で作る。**§2-2B の「状態遷移を書くのは `useGameLoop` の二人組ブロックだけ」と同じ1箇所に揃える。**

**事実(実在確認)**: `updateReturnPhase` は、**通常ストーリー(=城ボス撃破後)の分岐で滞在を積まない**。
`if (state.finaleDefeated && !state.corridorMode)` の枝は
`const dwellMs = inside ? 1 : 0;`——**円内なら 1、円外なら 0 を入れるだけ**の在内フラグで、
`deltaTime` を足していない。3秒(`RETURN_CIRCLE_HOLD_MS`)を積むのは**その下の屋内/イベント帰還の枝**だけ。

**しかも逆側がもっと深刻**: 通常ストーリーの帰還は**離指で確定する**。
`VirtualJoystick` / `utils/inputActions.ts` が指を離した時に `requestStoryReturnPrompt()` を呼び、
円内なら「帰還しますか?」が開く。**「はい」で `answerStoryReturnPrompt(true)` → その場で `gameWon: true`。**
→ **3秒立ち止まる動作そのものが「指を離す」動作**なので、
**サンプルを渡さずにリザルトへ行ける**(納品記録なし・次ステージ解放なし・完了会話なし)。
**城ボスを倒したのに何一つ進まないランが成立する。**

**→ この節の本文(下記)は ★未決 §2-15 #5「納品成立の契機」が (A) に裁定された場合の仕様である。**
**★#5「納品成立の契機」の裁定が出るまで実装しないのは、この節のうち「納品成立の契機」と「素通り防止
(`requestStoryReturnPrompt` / `updateReturnPhase`)」の2点だけ。**
納品ロック(移動/攻撃/被弾/割り込みポーズ)・ワープの2段・`delivering` の円の扱い・報酬付与口・
会話キューが空になったら `gameWon`、は **(A)(B) どちらでも同じ**なので**先に実装してよい**
(§2-0 / §2-15 冒頭の表と同じ)。

**(A)「クエスト進行中は確認ダイアログを出さない」が選ばれた場合に、追加で必要な指定**:
- **クエスト進行中は `requestStoryReturnPrompt` を発火させない。**
  同関数の冒頭の早期 return 条件(`storyReturnPromptVisible || gameWon || !finaleDefeated ||
  corridorMode || !isInReturnCircle(...)`)へ**1つ足す**。
  → 指を離しても確認ダイアログが出ない=**素通りでリザルトへ行く経路が塞がる**。
- **★★足す条件は「納品が未成立の間」ではない(5巡目の穴・ガードの向きが逆)。**
  「納品**未成立**の間だけ塞ぐ」形にすると、**納品が成立した瞬間にガードが外れる**。
  **事実(実在確認)**: `VirtualJoystick` の `release` は
  **`requestStoryReturnPrompt()` を `isAttackLocked()` の判定より前に、無条件で呼んでいる**
  (`const returnPromptOpened = pointerId !== null && fireCounter ? …requestStoryReturnPrompt() : false;`
  → その**後**で `!returnPromptOpened && !isAttackLocked()` を見る)。
  同ファイルのコメントにも「**押下は isInputLocked で弾かれても、押しっぱなしからの指離しは素通りする**」と
  書かれている。→ **`deliveryLocked` を `isInputLocked()` に足しても、この呼び出しは塞げない。**
  - 何が起きるか: **ロックが掛かった瞬間にスティックを握っていたプレイヤーが指を離すと**、
    納品ロックの最中に **`isPaused: true` が立って完了会話が凍る**。そこで「はい」を押せば
    **`gameWon` の2つ目の書き手が生きたまま残り**、会話を飛ばしてリザルトへ行く
    (=「`gameWon` を書くのは1箇所だけ」という確定が破れる)。
  - **確定: 足す条件は OR にする —— 「`deliveryLocked` が立っている **または** このランでクエストの
    納品が未成立」の間はどちらも発火させない。**
    = **クエストランで納品フェーズに入ったら、以後ラン終了まで `requestStoryReturnPrompt` は
    1度も開かない**(納品前は「素通り防止」、納品後は「会話の凍結防止」——**両方を同じ1条件で塞ぐ**)。
  - **対象外ステージ・クリア済みランは1バイトも変えない**(§2-1 の述語が false なので、
    `deliveryLocked` も立たず条件も成立しない)。
- **`updateReturnPhase` はクエスト進行中に `gameWon` を立てない。**
  クエスト中は**納品の滞在を積む専用の枝**を通す(`deltaTime * 1000` を足して
  `EVENT_QUEST_DWELL_MS`=3000 で満了)。満了時にやるのは**納品成立の手順(下記1〜4)だけ**で、
  **`gameWon` はここでは立てない**。
- **`gameWon` を立てる場所は「会話キューが空になった時」の1箇所だけにする**(下記手順5)。
  クエスト進行中に `gameWon` を書く経路を**2つ以上作らない**(どちらが先に立つかで結果が変わるため)。
- **対象外ステージ・クリア済みランは従来どおり**(離指→確認ダイアログ→はい→`gameWon`)。**1バイトも変えない。**

**(B)「従来どおり確認ダイアログを出し、『はい』で納品を始める」が選ばれた場合の流れ**:
- 離指→確認ダイアログ(`isPaused: true` のまま=タップ待ちなので**デッドロックしない**)→
  「はい」で **`gameWon` を立てず**、`isPaused: false` に戻して**納品ロック(下記3)へ入る**→
  完了会話→キューが空になったら `gameWon`。
- この場合 **3秒滞在は使わない**(帰る意思の確認は確認ダイアログが担う)。
  §2-8 の受け入れ条件4(「着地してから3秒で納品」)は**滞在ではなく「はいを押してから」**へ読み替える。

---

**以下は (A) を前提とした本文。**

**納品成立 = サークル内3秒滞在(`EVENT_QUEST_DWELL_MS`)が満了した瞬間。** その瞬間に、この順で行う:
1. **クリア記録**(§2-10・永続)
2. **報酬付与**(中身は後日裁定。**付与口だけ用意する**=当面は何も付与しない。
   ※現行の `EVENT_QUEST_REWARD_GOLD`=100 を撤去するか据え置くかは **★未決 §2-15 #7「報酬の100Gを撤去するか」**)
3. **納品ロックを掛ける**(**`isPaused` は使わない**)。中身は次の4点に分解する:
   - **①シミュレーションは動かしたまま。** `isPaused` / `backgrounded` / `rendererReady` のどれにも触らない。
     会話は実時間で進み、7〜10秒後にちゃんと空になる。
   - **②入力だけ無視する。** 専用フラグ **`deliveryLocked`(store・boolean)** を持つ。
     **★2巡目の「毎フレーム `touchActive: false` / `swipeDirection: null` を落とす」は撤回する。**
     **事実(実在確認)**: 流用元の `requestStoryReturnPrompt` は `touchActive: false` /
     `swipeDirection: null` を落とすと**同時に `isPaused: true` も立てている**ので、それで足りている。
     移動の実体は**もう1本ある**——`useGameControls.ts` の keydown/keyup が
     **`useGameStore.setState({ inputState })` で `inputState.up/down/left/right` を store へ書き**、
     `useGameLoop` はそれを `movePlayer(inputState, ...)` へそのまま渡している。
     → **`touchActive`/`swipeDirection` を落としても、WASD/矢印キーでは普通に歩ける**
     (=納品ロック中にサークルから歩いて出られる)。
     **★★3巡目の「`movePlayer` の冒頭で `deliveryLocked` なら即 return する」は撤回する。**
     **事実(実在確認)**: `movePlayer` は移動だけの関数ではない。同じ1回の `set()` の中で
     **`vx` / `vy` / `direction` / `lastDirection` / `isMoving` / `aimX` / `aimY` /
     速度ランプ(`speedRampSustainMs` ほか)/ `marksmanMovingSince` / PHILL のレティクル**まで
     **毎フレーム書いている**。入口で return すると**それら全部が最後の値のまま7〜10秒フリーズする**:
     ①走りながら滞在が満了すると、会話中ずっと**その場で走行アニメが回り続ける**(`isMoving` が true のまま)
     ②**カメラが移動時の引きズームで固定**される ③`isMoving` 駆動の効果が出続ける
     ④**速度が1フレームで 0**(座標だけ止まる)=**加減速の無い停止**=CLAUDE.md「★動きの絶対ルール: 慣性」違反。
     **→ 確定: 早期 return はしない。`movePlayer` の中で「入力が無いもの」として通す。**
     `deliveryLocked` の時は **`tx = ty = 0`** に落とし、**`swipeDirection` / `dashOverride`(ワイヤー/一閃/
     ホップ)/ スライド(`shijinSlideUntil`)も無視する**(=強制移動の上書きも掛けない)。
     - **事実(実在確認)**: 速度は
       `vx = player.vx + (tx * moveSpeed * speedScale - player.vx) * alpha` で目標へ寄せる形。
       `alpha = inertiaAlpha(deltaTime, PLAYER_INERTIA_TAU)`、**`PLAYER_INERTIA_TAU = 0.06`(60ms)**
       (社長指示v0.25.3618「プレイヤーの移動に少し慣性入れて」で 0 から入った値)。
       → **目標を 0 にするだけで 60ms かけて自然に減速し、`isMoving` も自動で false に落ちる**
       (慣性MUSTを満たす。新しい減速処理を1つも書かない)。
     - **被弾ノックバック(`knockbackUntil` の枝)は従来どおり通す**——③で被弾自体を弾くので新規には
       発生しないが、ロック直前に食らった吹き飛びが残っている場合は**最後まで減衰させる**
       (途中で瞬間停止させない)。
     - 「入口1点で閉じる」という趣旨は保てる(**書き換えるのは `tx/ty` を決める箇所1つだけ**で、
       個別の入力フィールドを毎フレーム落として回らない=React再描画規律にも優しい)。
   - **③被ダメージを `damagePlayer` の入口で無条件に弾く。**
     **プレイヤー被弾の入口は `damagePlayer`**(実在確認: 冒頭が `if (player.invulnerable) return false;`)。
     ここに **`deliveryLocked` なら即 `return false`** の1行を足す。
     ※`takeDamage` という関数は**このコードベースに存在しない**。
   - **④攻撃を止める(入口5つ)**: 下記「★攻撃の入口を全部名指しする」。
4. **完了時の会話**(§2-13 の「完了時」)をキューへ流す(`enqueueNpcDialogue`)
5. **会話が最後まで流れ切った時(0行なら即時)に `gameWon`**(★判定式は下記)

### ★★「会話キューが空」を `npcDialogueQueue.length === 0` で書いてはいけない(最終行が2.8秒読まれない)

**事実(実在確認)**: `updateNpcDialogue(gameTime)` は
**キューの先頭を `npcDialogue` へ移してから `NPC_DIALOGUE_MS`(2800ms)表示する**
(`cur = { ...head, until: gameTime + NPC_DIALOGUE_MS }; queue = queue.slice(1);`)。
→ **`npcDialogueQueue.length === 0` が真になるのは「最終行が表示され“始めた”瞬間」**。
字義どおり実装すると、**完了会話の最終行が読まれる前(2.8秒早く)にリザルトへ飛ぶ**。

**逆側の事故もある**: 判定ブロックを**手順4の enqueue より前**に置くと、
**納品が成立したフレームはまだキューが空**なので、**会話を1行も出さないまま `gameWon`** になる
(原稿のある S1/S3/S4 で完了会話が丸ごと消える)。

- **確定: 判定式は `npcDialogue === null` かつ `npcDialogueQueue.length === 0` の AND。**
  (`npcDialogue` は表示中の1行。`until` を過ぎると `updateNpcDialogue` が `null` に落とす。)
- **確定: この判定は「手順4の `enqueueNpcDialogue` と同じブロックの、その後」に置く。**
  (=納品が成立したフレームでは、enqueue 済みのキューを見て判定する。**別ブロック/前フレームで見ない**)。
- **原稿0行(S5)のとき**: enqueue しても両方が空のまま=**同じフレームで `gameWon`** になる(仕様どおり)。
- **`gameWon` を書くのはこの1箇所だけ**(§2-8 の (A) の確定と同じ。2つ目の書き手を作らない)。

### ★★`deliveryLocked` を**落とす場所**(立てる側だけ書くと、入力の死んだランが起動する)

`deliveryLocked` は `isInputLocked()` に足す(下記)ので、**true のまま残ると
移動・攻撃・マウス・ジョイスティック・サブウェポンが全部死んだランが始まる**。
**立てる側(手順3)と同じ強さで、落とす側を2箇所とも書く。**

- **① `resetGame` で `false`**(=ラン跨ぎで残さない。§2-14 の「ラン内の状態フィールド」行)。
- **② `gameWon: true` を立てるのと同じ `set()` で `false`**(手順5)。
  → リザルト画面へ遷移した後にロックが残らない。**別フレームで落とさない**(1フレームでも
  「`gameWon` かつ `deliveryLocked`」が成立する窓を作らない)。
- **これ以外に落とす場所を作らない**(納品ロックは「成立 → リザルト」の間だけ立っている)。

### ★攻撃の入口を全部名指しする(★銃は「入力」ではない=入力側だけ塞ぐと撃ち続ける)

**事実(実在確認)**: **銃の自動射撃は入力を1つも通らない。** `useGameLoop` が毎フレーム
`fireWeapon(activeGun, postReloadPlayer, enemies)` を呼び、`fireWeapon` の中身は
`if (now - weapon.lastFired < effCooldown) return [];` の**クールダウン駆動**。
→ **タップ/フリック側だけ塞いでも、納品ロック中ずっと敵を撃ち続ける**(=「操作でも敵の攻撃でも
結果が変わらない」を満たさない)。

**確定: 止める(撃たせない)。** 納品は「渡して終わる」場面で、戦闘の続きではないため。
入口は次の**5つ**で、それぞれ実在の判定へ `deliveryLocked` を足す:

| # | 入口(実在) | いま見ているゲート | どうする |
|---|---|---|---|
| 1 | **銃の自動射撃**(`useGameLoop` の `fireWeapon(...)` 呼び出し) | `isAttackLocked()` ほか | **`isAttackLocked()` に `deliveryLocked` を足す**(下記) |
| 2 | **タップ/離指の近接・カウンター・PHILL**(`VirtualJoystick` の `release` → `triggerCounter()`) | `isAttackLocked()` | 同上(1と同時に閉じる) |
| 3 | **マウス左右クリック**(`MouseControls` → `performTapAction` / `performFlickAction`) | **`isInputLocked()` のみ** | **`isInputLocked()` に `deliveryLocked` を足す**(下記) |
| 4 | **キーボードのフリック/カウンターキー**(`useGameControls` の keydown) | **`isGameTimeStopped()` のみ** | **その2つの枝に `deliveryLocked` を1条件足す**(既存の `isGameTimeStopped()` は残す=他の挙動を変えない) |
| 5 | **サブウェポンの自動発動**(`useGameLoop` の「サブ発動入口」= `inReturnCircle` 変数を全サブが通る1箇所) | `SKATER_LOCK_ENABLED && skaterRiding` | **その変数の式に `deliveryLocked` を足す**(全サブが1箇所で閉じる) |

- **`isAttackLocked()` / `isInputLocked()` に足す(確定)**:
  実在の定義は **`isInputLocked() = isPaused || player.health <= 0 || corridorRunInActive || farBackdrop === 'ending' || isGameTimeStopped()`**(★v3訂正・監査C17: **5項**。v2は `farBackdrop === 'ending'` を落として4項で引用していた=`deliveryLocked` を足す先の式そのもの)、
  **`isAttackLocked() = isInputLocked() || attention !== null`**。
  **`isInputLocked()` に `deliveryLocked` を足せば `isAttackLocked()` も同時に閉じる**(=1〜3が1行で片付く)。
  `isPaused` を使わずに「入力を無視する」を実現する、という §2-8 の趣旨とも整合する。
  ※`isInputLocked()` の実在の参照元は `MouseControls`(2箇所)・`VirtualJoystick`(2箇所)・
  `DebugOverlay`(表示のみ)・`isAttackLocked` だけ=**メニューを閉じられなくなる等の巻き添えは無い**。
- **キーボードの移動(4以外)**は `movePlayer` 側(上記②)で止まる。**判定を2重に書かない。**

### ★「無敵化」を既存の被弾無敵で作らない(1秒で切れる)

**事実(実在確認)**: `player.invulnerable` / `invulnerableTime` は
**`INVULN_MS`(=1000)で自動失効する被弾無敵(i-frame)**で、`useGameLoop` が
`if (player.invulnerable && Date.now() - player.invulnerableTime > INVULN_MS) { invulnerable: false }` と
毎フレーム解除している。**納品後の待ちは7〜10秒あるので、1秒で切れて残り6〜9秒の会話中に死ぬ。**

- **`invulnerable` / `invulnerableTime` は使わない。** 上の③(`damagePlayer` 入口の無条件棄却)で作る。

- **納品成立後は、プレイヤーの操作でも敵の攻撃でも結果が変わらない**(死なない・サークルを出られない)。
- **追加の滞在も、離れて入り直す操作も要らない。**
  - 理由(社長): 帰還サークルに入るかどうかは元々プレイヤーの任意なので、**入った時点で「帰る」と決めている**。
    拾い物や整理は入る前に済ませられる。
- **二重に作らない**: サークルは1つ、ゴール表示も1つ。納品用の別サークル・別マーカーを増やさない。
  ※社長指示「城ボス倒した時のゴール表示は2人クエストの時は表示しない」との関係は **★未決 §2-15 #8「城ボス撃破時のゴール表示」**。
- **納品段階では二人組側の円を描かない**(細部18): 帰還サークル(半径 `RETURN_CIRCLE_RADIUS`=95)と
  二人組の会話サークル(`EVENT_NPC_INTERACT_RADIUS`=64)が同じ場所に重なって**円が2つ**見える。
  `delivering` 状態では **`syncEventQuestNpc` の会話サークル・滞在アークを描かない**。
  **判定も帰還サークル1本**(二人組の円では判定しない)。

### ワープの出現・退場演出(慣性は必須・CLAUDE.md)

- 「パッと出て止まる」も「瞬間消滅」も禁止。**既存の型を流用し、新しい型を発明しない。**
- **消える側(レスキュー地点)= 飛び去り**: しゃがみ(溜め)→上方向へ ease-in で抜ける。
  実在の型 = 救難信号アライの離脱(`RESCUE_ALLY_CROUCH_MS` → `RESCUE_ALLY_FLYOUT_MS` のバックジャンプ)。
- **現れる側(帰還サークル)= 飛来ジャンプ**(水平は ease-out、垂直は sin 1山のホップ、着地で減速)。
  実在の定数: `RESCUE_ALLY_FLYIN_MS`(300) / `RESCUE_ALLY_HOP_PX`(48)(`gameStore.ts`)、
  描画側の実装は `pixiScene.ts` の救援アライ描画(飛来=放物線+`jumpStretch`)。
- **★「消滅 → 飛来」の2段であることを必ず守る**。データ上 `eventQuestNpc` は**1件しかない**ので、
  実装者が**座標を書き換えるだけ**で済ませると、プレイヤーがレスキュー地点の近くに居る時に
  **二人が瞬間消滅する**(慣性の禁則違反)。
- 着地点は**サークルの縁寄り**で、プレイヤーと重ならない位置。着地後は既存の待機(呼吸)描画へ戻る。
- **★二人の着地が完了するまで、納品の3秒滞在は進めない**(`warping` の間は `dwellMs` を加算しない)。
  プレイヤーは城ボスを倒したその場に居るので、**飛来中に既にサークルの中に立っている**のが普通。
  ここを書かないと「二人がまだ空中に居るのに納品が成立する」ことになる。
  滞在が進み始めるのは `delivering` に入ってから。

### ★`leftSinceAccept` ガードの扱い(書かないと納品メーターが1ミリも動かない)

**事実(実在確認)**: v1の滞在状態機械は `leftSinceAccept`(「直前のやり取り以降に一度サークルの外へ出たか」)が
**false の間、滞在メーターを進めない**(`useGameLoop`: `} else if (q.leftSinceAccept) {` の内側でしか `dwellMs` を足さない)。
`createEventQuestNpc` の初期値は **`true`**(生成直後は「外に居た」扱い)で、`false` にするのは
`acceptEventQuest` / `completeEventQuest` の2箇所だけ。
→ v2は §2-2B でその2つを走らせないので**現状は true のまま残る**が、
**v1の状態機械をそのまま流用して書き直すと、この分岐だけ残って納品が永久に成立しない**。

**★3巡目の根拠「プレイヤーは倒したその場=最初から円の中に居る」は誤りなので書き直す(実在確認)**:
帰還サークルは **`beginReturnPhase(castleEvent.x, castleEvent.y)`**——第3引数 `avoidPlayer` が
**既定 true** で呼ばれており、中身は
**「プレイヤーとの距離が `RETURN_CIRCLE_AVOID_DIST`(=240)未満なら、プレイヤーから 240px 離れた点へ動かす」**。
円の半径は **`RETURN_CIRCLE_RADIUS`=95**。
→ **出現の瞬間、プレイヤーは必ず円の中心から 240px 以上離れている=円の外に居る。**
「倒したその場で既に円の中」は**構造的に起きない**(この前提の上に載っていた検証手順も成り立たなかった)。

### ★クエスト専用枝でも「入った瞬間の設置物の撤去」は引き継ぐ(確定)

**事実(実在確認)**: `updateReturnPhase` の**通常ストーリー枝でも**、円へ入った瞬間
(`justEntered = inside && rc.dwellMs === 0`)に
**`projectiles` から `RETURN_CLEAR_WEAPON_TYPES`(=`grenade` / `trap` / `turret` / `decoy`)を撤去している**
(下のイベント帰還枝にも同じ1行がある。コメント=「**入った瞬間(外→内)に設置中のトラップ/手榴弾/
タレット/デコイを撤去(出入りハメ防止)**」)。

- **確定: §2-8 のクエスト専用枝でも、この撤去をそのまま引き継ぐ**(=既存挙動を1つも変えない)。
  撤去の契機も同じ「**円の外→内になった最初のフレーム**」。
- **捨てない理由**: 撤去は「出入りハメ防止」の安全弁で、v2の納品では**円の中で7〜10秒立ち止まる**ので
  むしろ効く場面が増える。落とすと**この改修だけが例外的にハメを許す**ことになる。
- 実装メモ: 専用枝は滞在(`deltaTime * 1000`)を積む形になるので、`justEntered` の判定は
  **`inside && rc.dwellMs === 0`** の既存式をそのまま使う(新しい入場フラグを作らない)。

- **確定(結論は変えない): 納品には「一度外へ出るまで待つ」ガードを掛けない。**
  `delivering` へ入る瞬間に `leftSinceAccept` 相当を **`true`** にし、`dwellMs = 0` にする。以後は無条件で積む。
- **正しい理由**: ①v1のガードは「受領→その場で即納品」の**連鎖**を防ぐためのもので、v2の納品は
  その前に城ボス1体を倒している=連鎖の心配が無い ②二人組は**帰還サークルへ後から飛来する**ので、
  「二人が来る前にプレイヤーが円の中に立っていた」場合でも滞在が始まらないのは**理由の無い足止め**になる。
- ※上の事実(プレイヤーは円の外から始まる)は**ガードを外す理由にはならないが、外しても害が無いことの裏付け**
  にはなる(=どちらにしても普通は一度外に居る)。

### ★納品ロック中の「割り込みポーズ」の扱い(`isPaused` を立てる実在6箇所を全部裁く)

**事実(実在確認)**: `deliveryLocked` は `isPaused` を使わない(上記)が、**`isPaused` は他所からも立つ**。
`isPaused` が立つと `updateNpcDialogue` が呼ばれない(§2-8 冒頭)=**完了会話が止まる**。
**`isPaused: true` を書いている実在の箇所は次の6つ**(1つも落とさずに扱いを決める):

| # | 実在の経路 | 入力が要るか | 納品ロック中に起こるか | 扱い(確定) |
|---|---|---|---|---|
| 1 | **レベルアップの選択メニュー**(`useGameLoop`: `if (introUntil > 0 && Date.now() >= introUntil) { showUpgradeMenu: true, isPaused: true, levelUpIntroUntil: 0 }`) | **不要(自動)** | **普通に起こる**(城ボス撃破で経験値が大量に入る) | **開かない=保留**(下記) |
| 2 | **武器商人サークルの滞在**(`updateMerchantDwell`: 円内に `MERCHANT_TALK_DWELL_MS` 居ると `showShopMenu: true, isPaused: true`) | **不要(自動)** | **起こりうる**(帰還サークルと商人サークルが近いと、立っているだけで開く) | **滞在を積まない=開かない**(下記) |
| 3 | **宝箱ピックアップ**(`collectPickup` の `case 'chest'`: `showUpgradeMenu: true, isPaused: true`) | **不要(磁力で拾えば起きる)** | **起こりうる**(城ボスのドロップが磁力圏内にあると拾う) | **開かない**(下記 ＋ ★未決 #19「納品ロック中に宝箱の1回を失うか」) |
| 4 | **チュートリアルポップ**(`showTutorialPopup`: `tutorialPopup, tutorialPopupShown: true, isPaused: true`) | 不要(条件成立で自動) | 起こりうる | **出さない=既読も付けない**(次のランで出る) |
| 5 | **確保済み拠点の武器庫サークル**(`triggerCounter` の中: `showShopMenu: true, isPaused: true`) | **要る(離指=攻撃入力)** | **起きない**(攻撃入口を全部塞いだので `triggerCounter` が走らない) | **追加対応なし** |
| 6 | **帰還確認ダイアログ**(`requestStoryReturnPrompt`: `storyReturnPromptVisible, isPaused: true`) | **要らない**(★離指は `isAttackLocked()` の**前**に無条件で呼ぶ=押しっぱなしからの離指が素通りする) | **★起こる**(ロックが掛かった時にスティックを握っていて、その後 指を離した場合) | **★`deliveryLocked` の間も発火させない**(上の (A) の OR 条件。「納品未成立の間だけ」では塞がらない) |
| — | ポーズ操作 / `backgrounded` | 要る/端末都合 | 起こりうる | **塞がない**(下記) |

- **①確定: 納品ロック中はレベルアップの選択メニューを開かない(保留する)。**
  上のメニュー開き条件へ **`&& !deliveryLocked`** を足す。`levelUpIntroUntil` は**この分岐の中でしか 0 にされない**ので、
  **保留してもフラグは消えず、ロックが解けた瞬間に開く**(取りこぼさない)。
  ※ただしリザルトへ遷移した後に開く必要は無い——`gameWon` 後にメニューが開かないことは既存の
  リザルト遷移側の挙動に従う(**この設計で新しい分岐を足さない**)。
  - **★抑えるのは「選択メニュー」だけ。LEVEL UP の演出はそのまま走らせる(確定)。**
    **事実(実在確認)**: レベルアップは `gainExperience` の中で
    **`triggerTimeSlow(0.25, LEVELUP_INTRO_MS)`(=850ms のスロー)+ グロー2枚 + SE** を出し、
    **`levelUpIntroUntil` の経過後に `useGameLoop` が別ブロックでメニューを開く**——
    **演出とメニューは別の場所**なので、`&& !deliveryLocked` を足しても**演出は1バイトも変わらない**。
    **確定: 演出は抑えない**(既存挙動を変えない側。CLAUDE.md「Aだけ直す」)。
    ※事実として併記: 会話は `newGameTime`(=`timeScale` が掛かった時計)で進むので、
    **850ms のスローの間だけ会話の進みが遅くなる**(止まりはしない。伸びは最大でも0.6秒程度)。
    `updateNpcDialogue` は `isPaused` でしか止まらないので、**デッドロックにはならない**。
- **②確定: 納品ロック中は商人サークルの滞在を積まない。**
  `updateMerchantDwell` の早期 return の条件(`!inCircle || showShopMenu || showUpgradeMenu || gameTime < shopReopenAt`)へ
  **`deliveryLocked`** を足す(=`merchantDwellMs` が 0 に戻るだけ)。**何も失われない**
  (ロック解除の直後はリザルトへ行くので、そもそも買い物の場面ではない)。
- **③確定: 納品ロック中は宝箱の選択メニューを開かない。** `collectPickup` の `case 'chest'` で
  `deliveryLocked` の間は `showUpgradeMenu` / `isPaused` を立てない。
  ※**この1回の選択は失われる**(レベルアップと違って保留するフラグが無い)。
  **失ってよいかは ★未決 §2-15 #19「納品ロック中に宝箱の1回を失うか」**(本文の現行仕様=開かない、は据え置き。**ブロッカーではない**)。
- **④確定: 納品ロック中はチュートリアルポップを出さない。**
  `showTutorialPopup` の冒頭で `deliveryLocked` なら**何もせずに返す**
  (`tutorialPopupShown` も既読も付けない=次のランで出る。**失われない**)。
- **ポーズ操作・`backgrounded` は塞がない**(端末の都合で止まるのは従来どおり)。
  止まっている間は会話も進まないが、**復帰すれば続きから進む**(時間駆動なので巻き戻らない)。
  ※事実として併記: これらまで塞ぐと「ゲームを一時中断できない7〜10秒」を作ることになるため、塞がない側を推薦した。

**実装上の注意**: 上の「★★この節は『★未決 #5「納品成立の契機」の裁定待ち』」を読むこと。
**現行の城ボス帰還は「3秒滞在」ではない**(通常ストーリー枝は滞在を積まず、離指→確認ダイアログ→
「はい」で即 `gameWon`)。**#5「納品成立の契機」の裁定が出るまで実装しないのは「納品成立の契機」と「素通り防止」の2点だけ**
(この節の残りは (A)(B) どちらでも同じ=先に実装してよい)。

**受け入れ条件**
1. 城ボス討伐後、帰還サークルは**1つだけ**出る(納品用の2つ目のサークルが出ない)。
   **納品段階で同じ場所に円が2つ見えない**(会話サークルを描かない)。
2. その直後に二人組がサークルへワープしてくる。**レスキュー地点の二人は同時に消えている**
   (画面のどこにも二人組が2組居ない)。
3. **レスキュー地点をプレイヤーが見ている状態で城ボスを倒しても、二人が瞬間消滅しない**:
   飛び去りは **`RESCUE_ALLY_CROUCH_MS`(しゃがみ溜め)→ `RESCUE_ALLY_FLYOUT_MS`(バックジャンプ)の2段を
   経由する**。現れる側は **`RESCUE_ALLY_FLYIN_MS` の水平 ease-out + `RESCUE_ALLY_HOP_PX` の山**を経由する
   (=座標の書き換え1回で済ませていない)。
4. **二人が空中に居る(`warping`)間は滞在メーターが進まない。**
   着地(`delivering`)してから3秒で納品が成立する。
5. 納品成立後、**敵の攻撃を連続で受けてもプレイヤーのHPが1も減らない**
   (`damagePlayer` が入口で弾いている。被弾無敵タイマーに頼っていないので**10秒経っても切れない**)。
4b. **`delivering` に入った瞬間から滞在メーターが進む。**
   検証手順(実機): 城ボス撃破 → 帰還サークルは**プレイヤーから 240px 以上離れた所に出る**ので、
   ①**そこへ歩いて入り、そのまま立ち続ける** → 3秒で納品が成立する。
   ②**二人の飛来より先に円の中に入って立っていた**場合でも、`delivering` に入った時点から3秒で成立する
   (「一度円の外へ出ないと進まない」が起きない)。
4c. **★納品が成立する前に、指を離してもリザルトへ行けない**(「帰還しますか?」の確認ダイアログが
   出ない=サンプルを渡さずにクリアできない)。**対象外ステージ・クリア済みランでは従来どおり出る。**
6. **納品成立後にサークルを出ようとしても、攻撃しようとしても何も起きない。**
   **★PC(キーボード)で WASD / 矢印キーを押し続けてもプレイヤーが移動しない**
   (=入力フィールドを落とす形ではなく、`movePlayer` の中で入力を 0 として扱っている)。
6a-1. **★走りながら納品が成立した時、その場で走行アニメが回り続けない。**
   `isMoving` が false に落ち、**慣性の時定数ぶんかけて減速して止まる**
   (1フレームで速度が 0 になる=加減速の無い停止、が起きない)。
   カメラの移動時ズームも待機側へ戻る。
   ※**「60ms で止まる」とは書かない**——時定数は乗り物で変わる。徒歩は `PLAYER_INERTIA_TAU`=0.06 だが、
   **スケーター乗車中は Lv別に 1.2 / 0.8 / 0.5 秒**(実在確認)。
   **乗車中に納品が成立した時の扱いは ★未決 §2-15 #22「スケーター乗車中の納品」**(下の 4c/6 が破れうる)。
6a-2. **★納品ロック中、銃が1発も撃たれない**(自動射撃はクールダウン駆動で入力を通らないため、
   入力側だけ塞いだ実装だと撃ち続ける)。**サブウェポンも1つも発動しない。**
   マウス左右クリック・キーボードのフリック/カウンターキーでも何も起きない。
6b. **納品ロック中にレベルアップしても、選択メニューが会話を止めない**
   (メニューはロック解除後に開く。**レベルアップそのものが消えない**=保留されている)。
6c. **★納品ロック中に、勝手にメニューが開いて会話が止まらない。** 具体的に:
   ①帰還サークルが**商人サークルと重なっていても**ショップが開かない
   ②**宝箱を磁力で拾っても**装備選択が開かない ③チュートリアルポップが出ない。
   **どの場合も `isPaused` が true にならない**(=会話が最後まで実時間で進み、リザルトへ到達する)。
7. **納品成立後も会話が実時間で進む**(1行あたり `NPC_DIALOGUE_MS` で送られる)。**画面が固まらない。**
   会話が終わると(0行なら即時)リザルトへ遷移する(再滞在・入り直し・追加操作を一切要求しない)。
   **`isPaused` が納品によって `true` になっていない。**
8. 納品の瞬間に報酬付与口が呼ばれる(中身が空でも呼ばれていることをログ/テストで確認できる)。
9. 対象外ステージ・クリア済みランの帰還サークルの挙動が**1つも変わらない**。
10. **★納品ロック中にスティックを握ったまま指を離しても「帰還しますか?」が出ない。**
   検証手順(実機): 帰還サークルへ**スティックを倒したまま進入**し、**指を離さずに**3秒待って納品を成立させ、
   **その後で指を離す** → **確認ダイアログが開かない・`isPaused` が true にならない・会話が止まらない**。
   (=`requestStoryReturnPrompt` のガードが「納品未成立の間」ではなく `deliveryLocked` を含む OR になっている。
   離指は `isAttackLocked()` の**前**に無条件で呼ばれるので、`isInputLocked()` に足しただけでは塞がらない。)
11. **★完了会話の最終行が最後まで表示されてからリザルトへ行く。**
   S1(完了2行)で、**2行目が表示され始めた瞬間ではなく、表示され終わってから**遷移する
   (=判定が `npcDialogue === null` かつ `npcDialogueQueue.length === 0` の AND になっている)。
   **逆に、納品したのに会話が1行も出ないまま遷移することもない**(判定が enqueue の後にある)。
12. **★納品してリザルトへ行った次の出撃で、操作が普通にできる。**
   検証: 納品クリア → タイトル → 同じ/別のステージへ再出撃 → **移動・攻撃・マウス・サブウェポンが全部効く**
   (=`deliveryLocked` が `resetGame` で false に戻っている)。
   **`gameWon` が立った時点でもロックは既に落ちている**(リザルト表示中に入力が死んでいる状態を作らない)。
13. **★納品の3秒滞在で円へ入った瞬間、設置中のトラップ/手榴弾/タレット/デコイが撤去される**
   (=既存の `RETURN_CLEAR_WEAPON_TYPES` の撤去をクエスト専用枝でも引き継いでいる)。

---

## 2-9. 誘導マーク(縁矢印)

社長指示: 「**納品の時、2人への誘導マークは表示してあげてね(城ボスとか商人と同じやつね)**」

- **納品先の誘導は、既存の帰還サークルの縁矢印がそのまま担う**(実在確認: `pixiScene.syncArrows` 内で
  `returnCircle` を読み、**画面外の時だけ**エメラルド `0x34d399` の縁矢印を出している)。
  §2-8 で納品先=帰還サークルに統合したので、**新しい矢印を足す必要がない**。
- **二人組本体へ別の矢印を足さない**(同じ場所に2本出る=二重マーク)。
- 城ボス(`castle.bossSpawned` 時の赤)・武器商人(`0xfbbf24`)と同じ系統の縁矢印。
- **★ただし「画面外の時だけ」なので実質ほとんど出ない。** 帰還サークルは城ボスを倒したその場に出るため、
  撃破直後はほぼ確実に画面内=矢印は出ない。社長指示が画面上で満たされない場面が多い。
  **扱いは ★未決 §2-15 #9「納品先の誘導マーク」**(本文の現行仕様=既存の縁矢印のまま、は据え置き)。
### ★レスキュー地点(4:00)側の誘導 —— 「必ず出す」は**本文で確定**(未決は見せ方だけ)

レスキューは**制限時間なし=見失うと城ボスが出ず勝利不能**になる。よって:

- **【確定・必須要件】レスキューが未完了で、かつ**レスキュー地点が表示されている間**(`status === 'rescue'`)は、
  その位置を常にプレイヤーへ提示し続ける。**
  ※**★v3の限定(監査A4)**: 裏ボス4体が居る間は `hidden`(円も二人も出ない)ので、**その間は提示しない**
  (提示する対象そのものが場に無い)。この窓の扱いは **★未決 #23「裏ボス非表示の窓に上限も合図も無い」**
  (§2-4・推薦=(a) このまま)。
  **「誘導を足さない」という選択肢は無い**(選ぶと勝利不能な設計が確定してしまうため)。
  §2-3 の「未完了の間は地平線フェードの対象外」と合わせて、**「画面内なのに透明」も「画面外で無印」も
  同時に起きない**状態を作る。
- **未決なのは見せ方だけ**(バナー文言/アテンションの有無/縁矢印か画面内マーカーか)= **★未決 §2-15 #4「レスキュー出現の告知と誘導」**。
  どの案を選んでも上の必須要件は満たすこと。

### ★★この誘導には**配線が1本も無い**(足さないと「実装済み」と報告されて矢印が出ない)

**事実(実在確認)**: `pixiScene.syncArrows(...)` の引数は
`pickups / castle / merchant / _camera / castleVisible / event / pois / baseSites / escorts /
playerCenter / hunters / screamers / questTargets / bossMark` で、
**`eventQuestNpc` を1つも受け取っていない**(毎フレームの呼び出し側も渡していない)。
`questTargets` は**v1のネームド(討伐対象の敵)**の配列で、v2では1体も湧かない(§2-2B)ので**常に空**。
§2-9 の「既存の帰還サークルの縁矢印がそのまま担う」は**納品先(城ボス撃破後)の話だけ**で、
**4:00〜レスキュー完了までの誘導には、既存で流用できる配線が存在しない。**

- **確定: `syncArrows` に「レスキュー未完了の待機中のトリガー円(=二人組の座標)」を渡す引数を1本足す**
  (`baseSites` / `hunters` / `screamers` と同じ作法=**配列で渡して既存の縁矢印の描き方を流用する**)。
  渡すのは `status === 'rescue'`(未完了)の間だけ。完了(`accepted` 以降)になったら**空を渡す**。
- **★確定: この矢印だけ「出す条件」を広げる(§2-3 の floor 撤回とセット)。**
  **事実(実在確認)**: `syncArrows` の各対象は
  **`if (tx >= 0 && tx <= this.screenW && ty >= 0 && ty <= this.screenH) continue;`**
  (=post-zoom の実画面座標で画面内なら描かない)で統一されている。
  レスキュー地点の枝だけ、この条件を
  **「画面外 **または** `horizonActorAlpha(二人組のy)` がほぼ0(=地平線フェードで実質見えない)」**へ広げる。
  - **`horizonActorAlpha` は同じクラスの private メソッド**なので、**`syncArrows` の中だけで完結する**
    (store / world に描画都合の値を1バイトも持ち込まない)。
  - **他の対象(拠点/城/商人/帰還サークル/ハンター等)の条件は1つも変えない。**
  - これで「**画面内なのに透明で、矢印も出ない死角**」が閉じる(地平線フェード自体は不変のまま)。
- **色は城ボス(赤)・商人(`0xfbbf24`)・帰還サークル(`0x34d399`)と重複しない既存系統から選ぶ**
  (どれを使うかは ★未決 §2-15 #4「レスキュー出現の告知と誘導」の見せ方と一緒に裁定する)。
- **これを §2-14 の実装マップにも行として載せる**(載せないと「縁矢印は既存で足りる」と読まれて
  **1本も出ないまま完了報告される**)。

**受け入れ条件**
1. 納品前、帰還サークルが画面外にある時だけ縁矢印が出る(画面内では出ない=既存の作法)。
2. 二人組へ向く矢印が2本出ない。
3. 対象外ステージ・クリア済みランの矢印挙動が変わらない。
4. **レスキュー未完了の間、二人組の位置が常に分かる。**
   4:00 で二人組を一度も見ずに反対方向へ 3000px 走り続けても、**位置の手がかりが画面から消えない**
   (=見失って勝てなくなる状態が作れない)。
5. **★二人組が地平線フェードで薄くなった/消えた時、画面内でも縁矢印が出る**
   (=「画面内なのに透明で、矢印も出ない」死角が無い)。`?zoomlock=0.4` でも同じ。
6. **この条件変更が他の縁矢印に波及していない**(拠点/城/商人/帰還サークル/ハンター/叫喚は
   従来どおり**画面外の時だけ**出る)。

---

## 2-10. 永続記録・次ステージ解放・EX解放の鎖

### 記録

- **クリア記録 = 納品成立の瞬間**(城ボス討伐の瞬間ではない)。討伐だけして帰らなかったランは記録しない。
- **完了フラグ**: `eventQuestMeta`(`zombie.progress.eventQuestMeta`)を **`delivered` 基準へ作り替える**。
- **★型・空値・検証を同じコミットで直す(実在確認)**: 現行の `isValidEventQuestMeta` は
  **`forced` と `sub` の両方が boolean であること**を検証条件にしており、外れると
  `emptyEventQuestMeta()` を返して**丸ごと捨てる**。`{delivered:true}` だけ書くと**次回起動で消える**
  (=納品したのに次の出撃でまたレスキューが出る/次ステージが解放されない)。
  よって **`EventQuestMeta` の型 / `emptyEventQuestMeta` / `isValidEventQuestMeta` を同じコミットで
  `delivered` 基準へ書き換える**(旧 `forced` / `sub` は optional にするか完全撤去。既存セーブの移行は
  考慮不要=社長裁定)。
- **城ボスフラグ**(`zombie.progress.castleBoss`)は従来どおり討伐の瞬間に記録する(不変)。
- **次ステージ解放**: `syncQuestStageClear` の判定を `delivered` ベースにする。新フローでは納品が必ず
  城ボス討伐より後なので、**納品の瞬間に揃って `markStageCleared` が走る**。

#### ★★`syncQuestStageClear` は「`cfg.forced` を見ない」形に書き換える(機械的な置換では S3/S4/S5 が抜ける)

**事実(実在確認)**: 現行は
**`const forcedOk = !cfg.forced || getEventQuestMeta(stageId).forced;`** →
`if (forcedOk && getCastleBossCleared(stageId)) markStageCleared(stageId);` の形。
そして **`EVENT_QUEST_CONFIG` の `forced` は stage-1 だけ `true`、stage-3 / stage-4 / stage-5 は `false`**。
→ **`!cfg.forced` の項が S3/S4/S5 では常に true** なので、`.forced` を `.delivered` へ置き換えるだけ
(=`!cfg.forced || meta.delivered`)では、**S3/S4/S5 は納品しなくても解放される**
=**城ボスを倒して帰るだけで次ステージが開く**(**勝利を配る側の事故**)。
しかも **S1 だけで試すと通ってしまう**(S1は `forced: true` なので `meta.delivered` が効く)。

- **★★確定(v3・監査A11): `syncQuestStageClear` の**呼び出し口**を消さないこと。**
  **実在確認**: 呼び出しは3箇所——①`gameStore` の城ボス撃破時(`isFinalBossKill` の中)
  ②**`completeEventQuest` の `forced` 分岐の中だけ** ③`App.tsx` の勝利処理。
  §2-14 は `completeEventQuest` の「`forced`/`sub` の**分岐ごと**を `delivered` の1本へ」と指示しているので、
  **素直に分岐を消すと②=納品の瞬間に走る唯一の同期まで消える**。
  ⇒ **`delivered` の1本へまとめた**後**も、その中で `syncQuestStageClear` を必ず呼ぶ。**
  受け入れ条件: §2-10 の「**納品した瞬間に次ステージが解放される(リザルトを見る前でも記録されている)**」が
  実際に満たされること(リザルト前に落ちたランでも解放されている)。
- **確定: `syncQuestStageClear` の判定は
  「**`getEventQuestMeta(stageId).delivered` かつ `getCastleBossCleared(stageId)`**」の**2条件だけ**にする。**
  **`cfg.forced` を読む項(`!cfg.forced ||`)は丸ごと落とす。**
- v2では **S1/S3/S4/S5 が同じ1本のクエスト**(レスキュー→城ボス→納品)を回すので、
  **ステージによって条件が変わる理由がもう無い**(`forced` はv1の「強制/サブの2段」の名残)。
- `EVENT_QUEST_CONFIG` の `forced` フィールド自体は**この節では消さない**(消すと `storyCanon.test.ts` 系の
  固定に波及しうる)。**読む場所を無くす**のがこの確定の中身
  (v2で `cfg.forced` を読む実在の箇所は `syncQuestStageClear` と `acceptEventQuest` の `forcedPending` の
  2つだけで、後者は §2-2B で経路ごと撤去する)。
- **二人組の消滅は永続**: `delivered` のステージには以後レスキューが出ず、二人も出ない。

### ★★`forced` / `sub` に触っている場所は**8つ**(読み手6+**書き手2**)。全部直す

**事実(実在確認)**: `getEventQuestMeta(...)` を呼んでいる実在の箇所は(テストを除いて)**8つ**。
**うち2つは読み手ではなく書き手**で、3巡目の表は**書き手を1行も載せていなかった**。
表をチェックリストに使うと**読み手だけ `delivered` になり、書き手は `sub` を書き続ける**
=**納品しても永久に未クリア・次ステージが解放されない・次のランでまたレスキューが出る**。

| # | 触っている場所(実在・シンボル) | 読/書 | 今 何をしている | v2でどうする |
|---|---|---|---|---|
| 1 | `src/utils/storyProgress.ts` `subsAllCompletedFromMeta()` | 読 | `getEventQuestMeta(id).sub` | **`delivered` へ**(EX解放の鎖・下記) |
| 2 | **`src/components/MissionSelect.tsx`(サブ進捗 n/N)** | 読 | `const subQuestDone = stage.subs.length > 0 && getEventQuestMeta(stage.id).sub;`(その下の `subTotal` / `subCleared` が使う) | **`delivered` へ** |
| 3 | **`src/components/MissionSelect.tsx`(サブ任務カードの CLEAR バッジ)** | 読 | `getEventQuestMeta(stage.id).sub && <span>CLEAR</span>` | **`delivered` へ** |
| 4 | `src/store/gameStore.ts` `resetGame` の二人組の出現ゲート | 読 | `qGone = bossMakerRoom \|\| !qCfg \|\| getEventQuestMeta(...).sub` | **`delivered` へ**(+§2-1 の除外条件もここへ足す) |
| 5 | `src/store/gameStore.ts` `acceptEventQuest` の `forcedPending` | 読 | `cfg.forced && !getEventQuestMeta(stageId).forced` | **v1経路ごと撤去**(§2-2B) |
| 6 | `src/data/progress.ts` `syncQuestStageClear` の `forcedOk` | 読 | `!cfg.forced \|\| getEventQuestMeta(stageId).forced` | **★`getEventQuestMeta(stageId).delivered` の1項へ**。**`!cfg.forced \|\|` を落とす**(残すと S3/S4/S5 は `forced:false` なので常に true=**納品せずに解放される**)(§2-10) |
| **7** | **`src/store/gameStore.ts` `completeEventQuest`** | **★書き** | `const meta = getEventQuestMeta(stageId);` → `setEventQuestMeta(stageId, { ...meta, forced: true })`(強制納品)/ `{ ...meta, sub: true }`(サブ納品)の**2分岐** | **★`setEventQuestMeta(stageId, { ...meta, delivered: true })` の1本にする**(`forced`/`sub` の分岐ごと廃止)。**これが v2 唯一の書き手** |
| **8** | **`src/store/gameStore.ts` `completeEventEncounter`**(M5の「遭遇のみ」) | **★書き** | `setEventQuestMeta(stageId, { ...meta, sub: true })` | **★撤去**(§2-14 の `encounterOnly` 撤去とセット。v2ではS5も納品する=遭遇のみが無くなる) |

→ **書き手(7・8)を落とすと、読み手だけ直っていても何も達成にならない。**
→ **MissionSelect の2箇所(2・3)を落とすと、納品してもミッション一覧のサブ任務が永久に未達成のまま**になり、
「次ステージは解放されたのに、そのステージのサブは未達成」というちぐはぐが残る。
→ **`delivered` を書く場所は 7 の1箇所だけ**(納品成立の瞬間・§2-8)。**2箇所以上作らない。**

### ★S5にサブ任務カードが無い(v2ではS5も同じクエストを回すのに、S5だけ表示が出ない)

**事実(実在確認)**: `src/data/campaign.ts` の `SUB_RESCUE_MISSION`(「身元不明民間人の救助」)は
**stage-1 / stage-3 / stage-4 の `subs` にしか入っていない**。stage-5 は `subs: []` で、
`src/data/storyCanon.test.ts` が **`expect(st('stage-5').subs).toEqual([])`** で固定している。
MissionSelect の進捗は `stage.subs.length > 0` を前提にしているので、
**S5は納品しても一覧に何も出ない**(カードも n/N も CLEAR バッジも無い)。

- **カードを足すか出さないかは ★未決 §2-15 #18「S5にサブ任務カードを出すか」**(**憲法テストの書き換えを伴う**ので #14「storyCanonテストの書き換え」と連動する)。
  本文の現行仕様=**S5は従来どおりカード無し**、は据え置く。

### EX解放の鎖(**1つも変えない**)

社長裁定(2026-08-21): 「こいつ倒してサンプル集めてきて。って城ボスの話にすれば、設定変えなくていいのでは?」→「おけ」

- 二人組クエストそのものを **「城ボス(搬送体)を倒してサンプルを持ち帰る」話として読み替える**ことで、
  現行の鎖(**サンプル3本 → グレンの薬 → 洋館再訪で使用 → EX解放**)は**構造を1つも変えなくてよい**。
- **薬の付与条件 = S1・S3・S4 の3本を納品**(通信原稿①血液試料/②特殊個体/③さらに希少な個体 と数が一致。
  **S5は4本目=解放条件には数えない**)。
- 実装は**読む先の差し替えだけ**: `src/utils/storyProgress.ts` の `subsAllCompletedFromMeta()` が
  `getEventQuestMeta(id).sub` を読んでいる箇所を `delivered` へ変える。
  `SUB_QUEST_STAGE_IDS = ['stage-1','stage-3','stage-4']` は**そのまま**。
  `medicineOwned` → `medicineUsed` → `canShowEx` の判定も**そのまま**。

**受け入れ条件**
1. 城ボスを倒しただけで死亡/撤退したランでは、次ステージが解放されない。
1b. **★★S3・S4・S5 で城ボスを倒して帰還しても、納品していなければ次ステージが解放されない。**
   検証手順: **S1だけで確かめない**——S3/S4/S5 は `EVENT_QUEST_CONFIG.forced` が **false** なので、
   `!cfg.forced || …` の項を残したままだと**この3ステージだけ素通りする**(S1は通ってしまう)。
   S3・S4・S5 の**3つとも**、「城ボス撃破 → 納品せずに帰還/死亡」で**次ステージが開かない**ことを見る。
1c. **`syncQuestStageClear` のコードに `cfg.forced` を読む行が1つも残っていない**
   (条件は `delivered` と 城ボス撃破済み の2つだけ)。
2. 納品した瞬間に次ステージが解放される(リザルトを見る前でも記録されている)。
2b. **★納品後にミッション選択画面を開くと、そのステージのサブ任務が「達成」になっている**
   (カードに CLEAR バッジが付き、サブ進捗の n/N が増えている)。
   **「解放されたのにサブは未達成」というちぐはぐが残らない**(S1/S3/S4 で確認する。S5は #18「S5にサブ任務カードを出すか」の裁定次第)。
3. **納品 → タイトルへ戻る → リロード → 同じステージへ再出撃すると、レスキューが出ない**
   (=保存が読めなくなっていない)。次ステージも解放されたままである。
   **同じ確認を、リロード後のミッション選択画面の CLEAR 表示についても行う**(保存が読めていることの確認)。
4. S1・S3・S4 の3本を納品した状態でM7クリア→ED後に**グレンの薬**が付与される。
5. S5を納品しても薬の付与条件のカウントは増えない(3本の判定に影響しない)。
6. 練習ラン中は上記の永続書き込みが1つも起きない(`practiceGuard` の関所で塞がれている)。
7. **★`delivered` を `true` にする実在のコードが1箇所だけ**(`completeEventQuest`)。
   **`sub` / `forced` を書く行がコードベースに1つも残っていない**
   (`completeEventQuest` の2分岐・`completeEventEncounter` の両方を直してある)。
   検証: 納品 → **ミッション選択で CLEAR が付く**(=読み手だけ直して書き手が古い、が起きない)。

---

## 2-11. ステージ5のみ: 拠点2か所確保が先行条件

- **第1段階「拠点を2か所以上確保」**: 既存の拠点システム(`baseSites` の `status === 'captured'` を数える)。
  **確保数 < 2 の間はレスキューが出ない = 城ボスも出ない**。
- **★一度2に達したらランの終わりまでラッチする**(`basesEverCaptured >= 2`)。
  **事実(実在確認)**: 拠点は `status: 'open' | 'captured'` の2値で、**HPが0になると `captured` → `open` へ戻る**
  (失陥しうる)。現在値の比較のままにすると、**2か所目確保でレスキューが出た直後に1つ失うと条件が false へ戻り、
  出たレスキューが消える/城ボスのゲートが閉じ直す**——§2-2 の「制限時間なし=消えない」と矛盾する。
  よって**「これまでに2か所以上確保したことがあるか」の単調なラッチ**で判定する(ラン内のみ・永続しない)。
- **レスキュー出現 = 「4:00」と「2か所目を確保した瞬間」の遅い方**
  (4:00 時点で拠点2未満なら、2か所目を確保した瞬間に出る)。
- 掲示: 第1段階を**サブクエスト欄の最上段**に `[拠点確保 n/2]` として出す(§2-7 と同じ「メイン扱い」の色・
  同じ別コンポーネント。`state.subquests` には触れない)。表示の `n` はラッチ済みなら 2 のまま下がらない。
  レスキュー出現とともにこの行は消え、以後は §2-7 の行に置き換わる。
- **S5の通信原稿は後日支給**(§2-13)。原稿が入るまで **S5は受注/完了とも会話0行**で進む。
  **会話が0行でも、レスキュー完了(§2-5)も納品成立(§2-8)もイベント側で成立する**
  (=S5が「会話が無いから完了できない」で詰まらない)。原稿が届いたら差し込むだけ。

**受け入れ条件**
1. S5で拠点1か所以下のまま 4:00 を過ぎても、レスキューが出ない。
2. 2か所目を確保した瞬間(4:00 以降なら即座に)レスキューが出る。
3. **2か所目を確保した後で拠点を失っても、レスキューは消えず、城ボスのゲートも閉じ直さない**
   (`[拠点確保 2/2]` の表示も 1/2 へ戻らない)。
4. `[拠点確保 n/2]` が拠点を取るたびに更新され、レスキュー出現で消える。
5. **S5で会話が1行も流れなくても、囲いを全滅させれば城ボスが出て、納品すればリザルトへ行ける。**
6. S1/S3/S4 には拠点の先行条件が付かない(4:00 で出る)。

---

## 2-12. v1 / v2旧稿から**廃止・撤回**した記述(残さないもの)

| 撤回した記述 | 現行(この章) |
|---|---|
| 「5分に二人組が出現→サークル3秒滞在で受注→通信→城ボス」 | **4:00 レスキュー出現→囲い全滅→受注会話**(§2-2〜2-5) |
| 「納品の再滞在は不要(完了通信で完了)」 | **撤回**。納品(3秒滞在)を復活し、帰還サークルと統合(§2-8) |
| 「納品場所=レスキュー地点に残る二人。プレイヤーが戻る」 | **撤回**。**二人がサークルへワープしてくる**(§2-8) |
| 「納品後に一度離れて再滞在で帰還」(設計チャットの解釈) | **撤回**。完了会話キューが空になったら**そのままリザルト**(§2-8) |
| 「レスキュー完了=受注会話の終了」(v2初稿) | **撤回**。完了=**全滅の瞬間**(会話に依存しない・§2-5) |
| 「囲いの `endsAt` は実質無期限」(v2初稿) | **撤回**。**有限**+時間切れは強制クリア扱い(§2-4) |
| 「囲いの `endsAt` は10分」(v2 1巡目の直し) | **撤回**。既存の保険は40秒/60秒。**90秒**へ(★未決 #13「囲いの90秒上限」)。10分は「240px円に10分閉じ込め」で趣旨を満たしていなかった(§2-4) |
| 「出現点は `clampRectToPlayableArea` で④⑤を防ぐ」(v2初稿) | **撤回**。S1/S3/S4/S5 では clamp は **no-op**(§2-3) |
| 「出現Yを `horizonActorAlpha(出現Y) >= 1.0` まで押し下げる」(v2 1巡目の直し) | **撤回**。`horizonActorAlpha` は **pixiScene の private・カメラ相対**で store から呼べず、呼べても1フレームしか効かない。**未完了の間は地平線フェードの対象外にする**(描画側の1条件)へ(§2-3) |
| 「納品後は `isPaused` 相当で入力を止める」(v2 1巡目の直し) | **撤回**。`updateNpcDialogue` が `!isPaused` の内側=**会話が進まず永久フリーズ**。**入力遮断+`damagePlayer` 入口での被弾棄却**の2点で作る(§2-8) |
| 「納品後はプレイヤーを無敵化」(v2 1巡目の直し) | **撤回**。`invulnerable` は `INVULN_MS`=1000 で自動失効し、残り6〜9秒の会話中に死ぬ(§2-8) |
| 「完了判定は `fromEvent` が0になったら」(v2 1巡目の直し) | **撤回**。`fromEvent` は**グローバル**で他イベント由来も数える。**このイベントの id セット**で数える(§2-4) |
| 「サークル進入の発火条件は `activeEvent === null` だけ」(v2 1巡目の直し) | **★2026-08-30の社長裁定で決着**。1巡目→「既存抽選と同じ6条件」へ変更→**その6条件ごと撤去**。現在の確定は「**円に入ったら発火する**」+「**裏ボス中は円を出さない(`hidden`)**」+「**出現位置で他イベントの範囲を避ける**」(§2-4 / §2-3 / §2-2B) |
| 「城ボス出現 = max(5:00, `rescueClearedAt`)」(v2 1巡目の直し) | **撤回**。同フレームで受注会話と城ボス出現+950msアテンションが衝突する。**+固定3.0秒**(会話の長さに依存しない)へ(§2-6) |
| 「会話中に城ボスの出現演出が割り込まない」(v2 1巡目の受け入れ条件) | **撤回**。会話終了待ちは「会話に依存しない」という中核原則と排他。**同フレームで湧かない**(最短3.0秒後)へ差し替え(§2-5) |
| 「`rescue` 状態では会話サークルも滞在アークも描かない」(v2 2巡目) | **撤回**。`syncArena` は `if (!ae) return;` で **`activeEvent` が立つまで円を描かない**ので、そのままだと**待機中に「どこに入れば始まるか」の絵が画面に無い**。`syncEventQuestNpc` に**トリガー円専用の枝**を足す(半径240・`near` を通さない)(§2-3) |
| 「出現の円は `spawnRing` 2枚で現れる」だけの記述(v2 2巡目) | **不足**。`spawnRing` は数百msの一過性の演出。**待機中ずっと出ている円の描画担当を別に名指しする**(§2-3) |
| 「未完了の間は地平線フェードの**対象外**にする」(v2 3巡目) | **撤回**(→ 一度 floor 方式へ直したが、その floor 自体も**5巡目で撤回**。下の該当行を見る) |
| 「フェードの免除対象は**二人組**」(v2 3巡目) | **撤回**(→ 一度「見せ物一式」へ広げたが、**免除そのものを5巡目で撤回**。下の該当行を見る) |
| 「出現点の上限が破れるのは**プレイヤー自身が3000px以遠に居る時**」(v2 3巡目) | **誤り**。プレイヤーが3000以遠でも、進行方向が原点側なら候補は3000未満に取れる。条件は「**振り切っても候補が1つも無い**」で書く。**S5は2か所目確保の瞬間にプレイヤーが必ず3070〜3330pxに居る**(`BASE_SITE_RADIUS`=3200 ± `BASE_CAPTURE_RADIUS`=130)=**例外ではなく既定**(§2-3・★未決 #17「S5の出現点の規則」) |
| 「敵を配置してから `beginArenaEvent`」/ 順序の未記載(v2 3巡目) | **確定**。①`beginArenaEvent` ②配置 ③`hordeSpawnRef` を潰す。逆順だと `isArenaSweepProtected` が**削岩型だけ**を守り、ゾンビ系4体が消える(§2-4) |
| 「完了判定は id セットの**生存数**が0か」だけの記述(v2 3巡目) | **不足**。死体(`corpseUntil` 付き)が配列に残る。**`isCorpse(e)` は生存に数えない**(§2-4) |
| 「納品ロックの入力遮断=`touchActive`/`swipeDirection` を毎フレーム落とす」(v2 3巡目) | **撤回**。流用元の `requestStoryReturnPrompt` は**同時に `isPaused: true` を立てているから足りている**。キーボードは `useGameControls` が `inputState.up/down/left/right` を store へ書く別経路で、**WASD/矢印で普通に歩ける**。**`movePlayer` の入口で `deliveryLocked` を見て早期 return** へ(§2-8) |
| 「§2-7 の表示条件 = `castleEvent.bossSpawned === true` かつ `attention === null`」(v2 3巡目) | **撤回**。城ボスは湧いた瞬間に `bossSpawned` が立ち、アテンションは**その950ms後**。この条件は**出現〜950msの間に真**になり、黄色の行がアテンションより先に出る。**store に `castleAttnDoneAt` を持たせ、HUDはそれ1本を読む**(§2-7) |
| 「納品成立=3秒滞在の満了」を**確定仕様**として書く(v2 3巡目) | **修正**。滞在を数える実体が無い(`updateReturnPhase` の通常ストーリー枝は `dwellMs = inside ? 1 : 0`)。しかも**離指→「帰還しますか?」→はいで即 `gameWon`** =納品せずクリアできる。**本文は ★未決 #5「納品成立の契機」の(A)を前提とした条件付き**として書き直した(§2-8) |
| 「レスキュー地点の誘導は既存の縁矢印で足りる」(v2 3巡目) | **不足**。`syncArrows` は **`eventQuestNpc` を受け取っていない**し、`questTargets` はv2では常に空。**引数を1本足して配線する**(§2-9) |
| 「`delivered` への差し替えは `subsAllCompletedFromMeta` だけ」(v2 3巡目) | **不足**。`MissionSelect.tsx` に**2箇所**(サブ進捗 n/N と CLEAR バッジ)、`resetGame` の `qGone`、`syncQuestStageClear` の `forcedOk` がある。落とすと**納品しても一覧が永久に未達成**(§2-10) |
| 「未完了の間は地平線フェードに **floor(0.35)** を敷く」(v2 3〜4巡目) | **★撤回**。`horizonActorAlpha` のしきい値は**遠景森の足元ライン(=地平線)**由来で、目的は「地平線より上=空に絵を出さない」(実在コメント「空に浮かない」×3箇所)。floor を敷くと**二人組・円の上半分・囲いの敵・その影が空の上に35%で浮く**。帯は引くほど広がるので `?zoomlock=0.4` で悪化。**フェードは1バイトも触らず、誘導配線側(縁矢印の条件を「画面外 or 地平線で実質見えない」へ広げる)で担保する**(§2-3 / §2-9) |
| 「囲いの敵にも floor を掛ける(免除は見せ物一式)」(v2 4巡目) | **撤回**(上と同根)。**囲いの敵は他の囲いと同じ扱いのまま=例外を作らない**(§2-3) |
| 「待機中のトリガー円は `syncEventQuestNpc` が描く」(v2 4巡目) | **★撤回**。`eventNpcGfx` は **actorLayer(Y-sort)・通常合成・`circle(0, -8*d, radius*d)`(depthScale倍+中心ズレ)**、`arenaGfx` は **groundLayer・`blendMode='add'`・world座標・等倍**。**半径1px一致が構造的に不可能**で、発火時に層/合成/半径が同時に入れ替わって絵が飛ぶ。さらに `ARENA_EVENT_RADIUS` は **`useGameLoop` の非export定数**=実装者が240をベタ書きする(二重管理)。→ **半径を store の `eventQuestNpc.triggerRadius` に持たせ、待機円は groundLayer の専用 Graphics(add・等倍)で描き、発火時は同じ見た目のまま200msでクロスフェード**(§2-3) |
| 「§2-7 の表示条件は `castleAttnDoneAt > 0` の**1本だけ**」(v2 4巡目) | **★撤回**。打刻するディスパッチャは**クエストの有無を一切見ていない**ので、`?castlenow=1`・フリー・練習・**納品済みステージ**など**レスキューを1度もやっていないラン全部**で黄色の行が出る(しかも `1/1` に変わった後、納品が起きないので**永久に消えない**)。→ **「クエスト進行中(レスキュー完了済み・未納品)」との AND**(950ms窓の話とは無関係)(§2-7) |
| 「納品ロックは `movePlayer` の冒頭で早期 return」(v2 4巡目) | **★撤回**。`movePlayer` は `vx/vy`・`direction`・`isMoving`・`lastDirection`・`aim`・速度ランプまで毎フレーム書いており、return すると**7〜10秒フリーズ**(その場で走行アニメが回り続ける/カメラが移動ズームのまま/**速度が1フレームで0=慣性MUST違反**)。→ **早期 return はせず、`deliveryLocked` の時は `tx=ty=0` として通す**(`PLAYER_INERTIA_TAU`=0.06 で自然に減速し `isMoving` も落ちる)(§2-8) |
| 「攻撃・サブ・フリックも**それぞれの入口**で弾く」だけの記述(v2 4巡目) | **不足**。**銃の自動射撃は入力ではなくクールダウン駆動**(`fireWeapon` の `now - lastFired < effCooldown`)なので、入力側だけ塞ぐと**ロック中ずっと撃ち続ける**。→ **入口5つを実在名で列挙**(`isInputLocked`/`isAttackLocked`/`MouseControls`/`useGameControls` の2枝/サブ発動入口の `inReturnCircle`)(§2-8) |
| 「投影影の除外は `hidden` だけ」(v2 4巡目) | **不足**。実在の条件は **`if (eventNpc.status !== 'completed')`** なので、**`gone`(納品済み=二人が出ないラン)でも影が落ち続ける**(v0.25.3054 で直したのと同型の残り1件)。→ **描画していない全状態(`gone`/`hidden`/フェード満了後の`completed`)を走査から外す**+`warping` は `heightPx` で高度ぶん縮める(§2-2B) |
| 「出現位置は `beginReturnPhase` の空き地探索を流用」(v2 3〜4巡目) | **不足**。流用元が屋外で見るのは**木の幹・松明・城の3つだけ**で、**街プロップ(S3)・病院・武器庫・警察署を1つも見ていない**(プレイヤーの `resolveOutOfSolids` は全部見る)。→ **「立てるか」の判定は `resolveOutOfSolids` を通す**+商人/拠点サークルは距離条件で避ける(§2-3) |
| 「プレイヤーは倒したその場=最初から円の中に居る」(v2 4巡目の根拠) | **誤り**。帰還サークルは `avoidPlayer` 既定 true で **`RETURN_CIRCLE_AVOID_DIST`=240** だけプレイヤーから離して置かれ、半径は **95**。→ **出現の瞬間、プレイヤーは必ず円の外**。`leftSinceAccept` ガードを外す**結論は妥当なので変えず、根拠だけ書き直した**(§2-8) |
| 「発火6条件が満たされない間は待てば解ける」(v2 4巡目の含み) | **不足**。通常囲い/退屈補正の囲いの**次回時刻は発火した時にだけ先送りされる**ので、塞がれている間もタイマーは進み、**空いた瞬間にプレイヤーの足元(=レスキューの円の中)で開く**=**周期的に再発する**。→ この事実を #16「囲いの発火6条件のゲート」の材料へ併記し、**レスキュー発火時に通常囲いの次回タイマーを押し戻す**を本文で確定(§2-4) |
| 「同フレーム衝突は発火条件で防ぐ」(v2 4巡目の含み) | **不足**。既存の抽選は**1本の if/else 連鎖**で構造的に排他しているが、レスキューを別の if に書くとその外に出る(ゲート発火待ちのフレームで**両方が `beginArenaEvent` を呼ぶ**)。→ **レスキューの発火を既存の連鎖の中(ゲート2の後・退屈補正の前)に入れる**(§2-4) |
| 「`activeEvent` はシングルトンなので**発火**さえ守ればよい」(v2 4巡目) | **★致命的な不足**。**既に走っている囲いを外から強制解除する経路が実在する**(ゲート発火待ちのプリエンプション: `aePre.kind !== 'boss'` なら `endArenaEvent()`)。レスキューは `kind:'horde'` なので**対象**で、`cleared`/`timedOut` のどちらも通らずに畳まれる=**`rescueClearedAt` が立たず城ボスが永久に出ない(勝利不能)**+入る→畳まれるの**無限ループ**。→ **①プリエンプションに `aePre.rescueQuest !== true` の保護を足す ②`activeEvent` が消えたのに未打刻なら強制クリア扱いで打刻する後始末を置く(本命)**(§2-4) |
| 「`rescueClearedAt` の在処を書かない」(v2 5巡目まで) | **★不足**。この章の中心変数なのに**宣言先も `resetGame` でのリセットも1行も無かった**(`rescueArenaStartedAt`/`castleAttnDoneAt` にはある)。`useRef` で実装されると ①§2-7 のHUDから**読めない**(`castleAttnRef` を否定した理由と同型)②`resetGame` で消えないので**前ランの値が残り、レスキュー未実施の次ランで5:00に城ボスが出て黄色の行も出る**。→ **store に置き `resetGame` で 0**(§2-5 / §2-14) |
| 「`syncQuestStageClear` の判定を `delivered` ベースにする」だけの記述(v2 5巡目まで) | **★致命的な不足**。実在は **`!cfg.forced \|\| getEventQuestMeta(stageId).forced`** で、**stage-3/4/5 は `forced: false`**。機械的な置換(`!cfg.forced \|\| meta.delivered`)だと**S3/S4/S5 は常に true=納品せずに解放される**(**S1だけ試すと通ってしまう**)。→ **`delivered && 城ボス撃破済み` の2条件だけにし、`cfg.forced` を読む項を落とす**(§2-10) |
| 「`deliveryLocked` を立てる」だけの記述(v2 5巡目まで) | **★不足**。**落とす場所が1箇所も書かれていなかった**。`isInputLocked()` に足す設計なので、true のまま残ると**移動・攻撃・マウス・ジョイスティックが全部死んだラン**が起動する。→ **`resetGame` で false**+**`gameWon: true` と同じ `set()` で false**(§2-8 / §2-14) |
| 「素通り防止のガード=『クエストの納品が**未成立**』を早期returnに足す」(v2 5巡目) | **★撤回(向きが逆)**。**`VirtualJoystick.release` は `requestStoryReturnPrompt()` を `isAttackLocked()` の判定より前に無条件で呼ぶ**(「押しっぱなしからの指離しは素通りする」)。納品**成立**の瞬間にガードが外れるので、**ロック中の離指で `isPaused:true` が立って完了会話が凍り**、「はい」で**`gameWon` の2つ目の書き手**が生きる。→ **`deliveryLocked` **または** 納品未成立」の OR**(=納品フェーズに入ったら以後ずっと出さない)(§2-8) |
| 「待機円は `activeEvent` が立ったフレームで描画をやめる」(v2 5巡目) | **★撤回**。`activeEvent` は**シングルトンでレスキュー以外の囲いでも立つ**ので、**他イベントの40〜60秒のあいだトリガー円が消える**(その間は6条件で発火もしないので「円が出た→消えた→城ボスが出ない」だけが見える)。§2-4 本文の「欠けている間は円はそのまま残る」とも矛盾。→ **止めるのは `activeEvent?.rescueQuest === true` の時だけ**(2本同時描画は許容)(§2-3) |
| 「`EventQuestStatus` を6状態へ拡張する」だけの記述(v2 5巡目まで) | **★不足**。**5遷移の書き手が本文にも実装マップにも無かった**。`rescue` のまま固まると**§2-7 の黄色い行が永久に出ず**、**§2-9 の矢印が城ボス戦の間ずっと二人組を指す**。→ **書き手=`useGameLoop` の二人組ブロック1箇所**+5遷移の契機を明記(§2-2B / §2-14) |
| 「このランでクエストが有効か」を各所で書く(v2 5巡目まで暗黙) | **★不足**。少なくとも5箇所(`qGone`/城ボスのゲート/HUD/矢印/素通り防止)が要る述語の**出どころが名指しされていなかった**。とくに城ボスのゲートを `getEventQuestConfig` だけで書くと**フリー出撃/練習でも城ボスがゲートされて詰む**。→ **`eventQuestNpc.status !== 'gone'` の1本**(§2-1) |
| 「`createEventQuestNpc` の初期値はそのまま」(v2 5巡目まで暗黙) | **★不足**。実在の初期値は **`status: 'available'`**・座標は**原点基準460〜950のランダム**。直さないと**0:00から二人を描く**(描画スイッチは status)。→ **初期 `status='hidden'`。座標は4:00の出現時に上書き(生成時の乱数配置は使わない)**(§2-2B) |
| 「`warping` は走査から外さず `heightPx` で縮める」(v2 5巡目) | **★撤回(2つの影システムの取り違え)**。`syncLocalEventLighting` の積み口は **`push(x, y, w, strength)`=高さの引数が無い**。`heightPx` は **`ShadowCasterReq`**(別経路)のフィールドで、二人組ではそれに当たるのが `syncEventQuestNpc` の **`npcShadow`**(**5巡目までの本文に一度も出てこない**)。→ **(a) 強glowの投影影=`warping` も走査から外す (b) `npcShadow`=`BuildingShadowReq` に `heightPx?` を足して `place(...)` へ渡す**(§2-2B / §2-14) |
| 「会話キューが空=`npcDialogueQueue.length === 0`」(v2 5巡目まで暗黙) | **★不足**。`updateNpcDialogue` は**キューの先頭を `npcDialogue` へ移してから2800ms表示する**ので、この式は**最終行が表示され“始めた”瞬間**に真=**2.8秒早くリザルトへ飛ぶ**。判定を enqueue より前に置くと**会話0行で `gameWon`**。→ **`npcDialogue === null` かつ `queue.length === 0`** を**手順4の enqueue と同じブロックのその後**で見る(§2-8) |
| 「候補は `resolveOutOfSolids` を通す」だけの記述(v2 5巡目) | **★不足**。**渡す矩形の寸法が未定義**(`EventQuestNpc` に width/height は無い)。ctx の列挙から **`hospitalTaken`/`armoryTaken`/`policeTaken`** が抜けていた。同じ ctx を組む**公開ヘルパ `resolveBountyMove(nx,ny,box)` が既に実在**する。→ **矩形=`PLAYER_HITBOX`(28)の正方形**・**ctx は `resolveBountyMove` を流用**(§2-3) |
| 「候補が全滅したら上限を諦める」だけの記述(v2 5巡目) | **★不足**。**諦める優先順位が未定義**だった(4条件のうち何から捨てるのか)。→ **①原点3000 → ②商人/拠点サークル → ③最後は `resolveOutOfSolids` の押し戻し座標を採る**(既存前例=`collectPickup` は押し戻し座標をそのまま採用している)(§2-3) |
| 「レスキュー発火時に `nextArenaAtRef` を押し戻せば通常囲いが足元で開かない」(v2 5巡目) | **★訂正**。連鎖は `… else if (puzzleActiveNow) { 退屈補正 } else { 2分タイマー }` なので、**通常プレイ(`puzzleActiveNow=true`)では2分タイマー枝に到達しない**=`nextArenaAtRef` の押し戻しは**no-op**。効くのは **`boredomArenaNextEligibleAtRef`(退屈補正)だけ**。押し戻しは両方書く(害なし・ボスフェーズの保険)。**#16「囲いの発火6条件のゲート」の材料もこの形へ訂正**(§2-4) |
| 「レスキューの枝を足しても既存の抽選を1つも変えない」(v2 5巡目の含み) | **★訂正**。`puzzleActiveNow === false` の区間では、**レスキューの枝が2分タイマーの枝より先に評価される**(円の中に居ると そのフレームの抽選が止まる)。頻度は低いが0ではないので**事実として書く**(§2-4) |
| 「納品ロック中はレベルアップを保留する」だけの記述(v2 5巡目) | **不足**。**演出まで抑えるのかが未指定**だった。実在ではレベルアップの**スロー(0.25×850ms)+グロー+SEは `gainExperience` 側**、メニュー開きは `useGameLoop` の別ブロック=**別の場所**。→ **抑えるのはメニューだけ。演出はそのまま走らせる**(既存挙動を変えない)(§2-8) |
| 「クエスト専用の滞在枝を通す」だけの記述(v2 5巡目) | **不足**。通常ストーリー枝が持つ**設置物の撤去**(`justEntered` に `grenade`/`trap`/`turret`/`decoy` を撤去=出入りハメ防止)を引き継ぐか捨てるかが未指定だった。→ **引き継ぐ**(§2-8) |
| v1「強制クエスト=ネームド(パンプキン/犬)討伐」 | 廃止。討伐対象は**城ボス(搬送体)**(§2-6)。ネームド湧きは走らせない(§2-2B) |
| v1「サブ『とにかくサンプルを集めてきて』」 | 廃止(サンプル系は右上の自動サブクエが担う)。二人組=城ボスクエスト専任 |
| v1「二人組は開始時から場に配置」 | 廃止。4:00 まで場に居ない(§2-2) |
| v1「サークル3秒滞在で受注」 | 廃止。**v1の滞在受注経路は殺す**(§2-2B) |
| v1「次ステージ解放 = 城ボスフラグ && 強制フラグ」 | **納品成立の瞬間**に一本化(§2-10) |
| v1「城ボスは7分出現」 | **事実として現行は 5:00**(`CASTLE_BOSS_MIN_TIME_MS`)。v1章の記述が古い |
| 「革命ランでは出さない」 | 「革命ラン」は現行コードに実体なし。**実在する除外経路**で書く(§2-1) |
| 「2/6/7/ex は城ボスが従来どおり5:00」(v2初稿) | **事実誤認**。2/6/7/ex は**元から城ボスが出ない**(§2-1) |

> 事実として併記(判断材料・制約ではない): 移行の考慮は不要(社長「いまは開発中なので移行のこととか
> 考えなくていいです」)、S5の既存「遭遇のみ」イベント(M5・確定台詞)は新仕様で置き換えてよい(社長「はい」)。

---

## 2-13. 通信原稿(社長支給 2026-08-21・**一言一句そのまま**使う)

**割り当て(社長裁定 2026-08-21): S1=① 血液試料回収 / S3=② 特殊個体 / S4=③ さらに希少な個体 / S5=後日支給。**
> ★**S1だけ未確定**: ①の1行目は「またお前か。」だが、v2では**そこが初対面**になる。
> v1の初遭遇原稿(`EVENT_QUEST_LINES_FORCED`・確定稿・テストで全文固定)の扱いを含めて **★未決 §2-15 #11「S1初対面の台詞が消える」**。
> **この節の原稿そのものは1文字も変えない**(変わりうるのは「S1で初遭遇5行を前に付けるか」だけ)。
「受注時」= レスキュー完了の瞬間(§2-5)。「完了時」= 納品成立の瞬間(§2-8)。
**どちらも会話は「流すだけ」で、完了判定には関与しない。**

### ① 血液試料回収(S1)
受注時
- グレン「またお前か。頼みがある。極秘のミッションでな。理由は聞くな」
- ミラ「そう！　血液サンプルを持ち帰って、独自開発した製剤の有用性を極秘で調べるんだよ！」
- グレン「……」

完了時
- ミラ「ありがと！　これ、報酬ね！」
- グレン「それ俺のだろ」

### ② 特殊個体の試料回収(S3)
受注時
- グレン「また会ったな。実は頼みがある。理由は聞くなよ」
- ミラ「秘密だからね！　通常個体とは違う血液が、こっちの試料にどう反応するか――」
- グレン「……あっ、後ろに変異体だ、ミラ！」
- ミラ「わっ！　どこ！？」

完了時
- グレン「報酬はそうだなぁ……」
- ミラ「もう渡したよ！」
- グレン「また俺の……」

### ③ さらに希少な個体の試料回収(S4)
受注時
- グレン「よく会うな。追っかけか？　まあいい、頼みがある」
- ミラ「うぐ……めっちゃ怒られるから、理由は言えないの……」
- グレン「頼んだぞ」

完了時
- グレン「今日は俺の物を渡すなよ」
- ミラ「もう渡したよ！」
- グレン「……」

> **実在確認(2026-08-22)**: この3本×2場面は **すでに `src/utils/eventQuest.ts` に実装済み**
> (`EVENT_QUEST_SUB_ACCEPT_LINES` / `EVENT_QUEST_SUB_COMPLETE_LINES`・stage-1/3/4)で、原稿と**完全に一致**する。
> v2実装では**本文を書き直す必要はなく、呼び出す場面(受注=レスキュー完了の瞬間 / 完了=納品成立の瞬間)を
> 差し替えるだけ**。

---

## 2-14. 実装マップ(**実在確認済み・2026-08-22 時点のコード。シンボル名のみ・行番号は書かない**)

### 触る場所

| 何を | どこ(実在・シンボル) |
|---|---|
| **★★このランの状態フィールド(store に追加・全部 `resetGame` でリセット)** | `src/store/gameStore.ts`。**この4つは全部 store に置く。`useRef` にしない**(HUD/描画が読む・ラン跨ぎで消える必要がある): ①**`rescueClearedAt`(数値・0=未)**——レスキュー完了の打刻。§2-5 が書き、§2-6(城ボスのゲート)・§2-7(HUD)・§2-4(後始末)が読む。**`useRef` にすると HUD から読めず(`castleAttnRef` を否定したのと同じ理由)、`resetGame` でも消えないので前ランの値で城ボスが出る** ②**`rescueArenaStartedAt`(数値・0=未)**——囲いの発火打刻(後始末用) ③**`deliveryLocked`(boolean・既定 false)**——納品ロック ④**`castleAttnDoneAt`(数値・0=未)**——アテンション成立の打刻。**`resetGame` で ①②④=0 / ③=false**(§2-5 / §2-7 / §2-8) |
| **★`deliveryLocked` を落とす2箇所(立てる側だけ書かない)** | ①**`resetGame` で `false`** ②**`gameWon: true` を立てるのと同じ `set()` で `false`**(§2-8 手順5)。**`isInputLocked()` に足す**フラグなので、true のまま残ると**移動・攻撃・マウス・ジョイスティック・サブが全部死んだランが起動する**。**この2箇所以外に落とす場所を作らない**(§2-8) |
| **★`EventQuestStatus` の遷移の書き手(1箇所)** | **`src/hooks/useGameLoop.ts` の二人組ブロックだけ**が5遷移を書く: ①`hidden`→`rescue`=レスキュー出現(飛来開始)のフレーム ②`rescue`→`accepted`=`rescueClearedAt` を打刻するのと同じブロック(全滅/時間切れ/後始末の3契機) ③`accepted`→`warping`=`returnCircle` が非nullになった最初のフレーム ④`warping`→`delivering`=飛来の着地が完了したフレーム(ここで `dwellMs=0`) ⑤`delivering`→`completed`=納品成立(**`completeEventQuest` が既に書いている `status:'completed'` を流用**)。`gone` を書くのは `resetGame` の `qGone` だけ。**書き手が無いと `rescue` のまま固まり、§2-7 の黄色い行が永久に出ず、§2-9 の矢印が城ボス戦の間ずっと二人組を指す**(§2-2B) |
| **★`createEventQuestNpc` の初期値** | `src/store/gameStore.ts` `createEventQuestNpc`。現行は **`status: 'available'`** で、座標は**原点基準・方位角ランダム・距離 `EVENT_NPC_MIN_DISTANCE`(460)〜`EVENT_NPC_MAX_DISTANCE`(950)のランダム**。→ **初期 `status` を `'hidden'` にする**(描画スイッチは status なので、直さないと **0:00 から二人が立っている**)。**座標は 4:00 の出現時に §2-3 の規則で上書きする=生成時の乱数配置は使わない**(§2-2B) |
| **★二人組の接地影(`npcShadow`)に高度を渡す** | `src/pixi/pixiScene.ts`。`syncEventQuestNpc` が毎フレーム **`this.npcShadow = { x, y, legacyW, rawW, rawH, texture, alpha, shadowFade }`** を組み、`syncShadows` が **`place({ id:'npc', … })`** で `ShadowCasterReq` へ正規化している。**`BuildingShadowReq` には `heightPx` が無く、`place` 呼び出しでも渡していない**ので、`warping` の高度を表すには **`BuildingShadowReq` に `heightPx?: number` を足して `place(...)` へ素通しする**(護衛/救援アライは既に `heightPx: v.shadowHeight` で渡している=同じ作法)。**強glowの投影影(`syncLocalEventLighting`)とは別系統**——あちらの積み口は `push(x, y, w, strength)` で**高さの引数が無い**ので、`warping` は**走査から外す**(§2-2B) |
| レスキュー出現(4:00)・囲い開始・全滅判定・時間切れ分岐 | `src/hooks/useGameLoop.ts` の囲い系イベントのブロック(`arenaReady` の抽選/発火 → `cleared` / `timedOut` の終了判定 → 時間切れ側のゲート失敗処理)。半径定数は `ARENA_EVENT_RADIUS`(**値は240**) |
| **★発火時の順序(必須・入れ替え禁止)** | ①`beginArenaEvent({…, rescueQuest: true })` → ②編成N体を配置(+`id` をセットに控える) → ③直後に `hordeSpawnRef` を潰す → ④発火演出。**`beginArenaEvent` は `radius*1.5` 内の非保護敵を一掃する**ので逆順だと **`isArenaSweepProtected`(=`reaper`/`giantbat`/`isPumpkinTier`=pumpkin と **driller**/`isHiddenBoss`/`isBountyType`/`fixed`/`questTarget`)に守られた削岩型だけが残り、ゾンビ系が消える**。ゲート1/ゲート2の「重要: … 必ず『敵を配置する前』に」コメントと同じ作法(§2-4) |
| **段階スポーンの停止(必須)** | `src/hooks/useGameLoop.ts` の `hordeSpawnRef`。**編成の配置直後に `{ spawned: N, nextAt: <今>, total: N }` を代入する**(ゲート1と同じ作法)。書かないと `ARENA_HORDE_COUNT`=**18** 体が1秒ごとに流入し、`hordeSpawnDone` が満たされず終わらない(§2-4) |
| **「生存数0」の定義** | `src/utils/enemyUtils.ts` `isCorpse(e)`(=`e.corpseUntil !== undefined`)。**死体は生存に数えない**。削岩型も `corpseEligible` は true(`enemyUtils.test.ts` が pumpkin と同値で固定)なので、数えると**全滅させても90秒の時間切れまで終わらない**(§2-4) |
| **イベント識別フラグ** | `src/types/game.ts` `ActiveEvent` に **`rescueQuest?: boolean` を追加**(前例=同じ型の `policeArena?: boolean`)。時間切れの専用枝・完了判定のスコープはこのフラグで分岐する(§2-4) |
| **完了判定のスコープ** | `useGameLoop` の `const eventEnemies = ...enemies.filter(e => e.fromEvent).length` は**グローバル**。レスキューでは**このイベントで湧かせた個体の id セット**で数える(§2-4) |
| **発火ゲート** | **★社長裁定2026-08-30で6条件ANDは撤去した。ゲートは書かない**——**円に入ったら発火する**。実装で要るのは2つだけ: ①発火枝は既存の `if (!ae) { gate1 → gate2 → [ここ] → 退屈補正 → … }` 連鎖の**中**に置く ②その連鎖に入る**前**に「円の中に居て `ae?.kind === 'rescue'` なら `endArenaEvent()`」の**前置き1本**(§2-4)。**裏ボスは発火ゲートではなく `hidden`(§2-2B)で扱う=そもそも円が無い** |
| **★発火のコードを置く場所(同フレーム衝突の構造的排他)** | `src/hooks/useGameLoop.ts` の `const ae = ...activeEvent; if (!ae) { if (gateFireOk && gate1Ready) {…} else if (gateFireOk && shouldTriggerGate2({…})) {…} else if (puzzleActiveNow) {…退屈補正…} else {…2分タイマー…} }` の**連鎖の中**へ、**ゲート2の後・`else if (puzzleActiveNow)` の前**に枝を1本足す。別の if に書くと**ゲート発火待ちのフレームで両方が `beginArenaEvent` を呼ぶ**(後勝ちで上書き＋先に置いた敵が一掃で消える。エラーは出ない)(§2-4) |
| **★★プリエンプションからの保護(勝利不能の本体)** | `src/hooks/useGameLoop.ts` の **`const aePre = useGameStore.getState().activeEvent; if (gateFireOk && activeGateRef.current == null && aePre && aePre.kind !== 'boss') { … endArenaEvent(); }`**(コメント=「ゲート発火待ち…城ボス以外のイベント(レスキュー/退屈囲い)が進行中なら強制解除」)。**レスキューは `kind:'horde'` なので対象**で、`endArenaEvent()` は `cleared`/`timedOut` を通らない=**`rescueClearedAt` が立たないまま囲いが畳まれ、城ボスが永久に出ない**。→ **①条件へ `aePre.rescueQuest !== true` を足す**(`kind !== 'boss'` と同じ形)(§2-4) |
| **★★後始末(本命の防御)** | store に **`rescueArenaStartedAt`(数値・0=未)**を追加(発火時に打刻・`resetGame` で 0)。`useGameLoop` の二人組ブロックで毎フレーム **`rescueArenaStartedAt > 0 && rescueClearedAt === 0 && activeEvent?.rescueQuest !== true`** なら**強制クリア扱いで §2-5 の完了経路へ入れる**。`endArenaEvent()` を外から呼ぶ経路は今後も増えるので、**経路ごとに塞ぐのではなく状態で救う**(§2-4 / §2-5) |
| **★通常囲いの次回タイマーの押し戻し** | レスキュー発火時に **`nextArenaAtRef.current = newGameTime + ARENA_FIRE_INTERVAL_MS`(=120000)** と **`boredomArenaNextEligibleAtRef.current = newGameTime + BOREDOM_ARENA_CD_MS`(=120000・`src/utils/boredomArena.ts`)** を代入する。**どちらも発火時にしか先送りされない**ので、書かないとレスキュー直後にプレイヤーの足元(通常囲い/退屈囲いはどちらも中心=プレイヤー中心)で次の囲いが開く(§2-4) |
| **★出現位置の「立てるか」判定** | **`src/store/gameStore.ts` の公開ヘルパ `resolveBountyMove(nx, ny, box)` をそのまま呼ぶ**(中身は store から ctx を組んで `resolveOutOfSolids` を呼ぶだけ=賞金首の移動が使っている実在の1本)。**ctx を自分で組まない**——列挙すると **`hospitalTaken` / `armoryTaken` / `policeTaken` を書き落とす**(5巡目の設計書が実際に落としていた)。**渡す矩形は `PLAYER_HITBOX`(=28)の正方形**(`EventQuestNpc` に width/height は無い。`radius`=64 は会話サークル、`npcShadow.legacyW`=84 は影の絵幅=**どちらも当たり判定ではない**。大きい矩形を渡すと S3の廃都/森で候補がほぼ全滅する)。**戻り値が入力と一致すれば採用・ズレたら棄却して振り直す**。**`beginReturnPhase` の空き地探索は「角度を振って半径を広げる」形だけを流用**——その中の障害物集合(`trunkRect` / `torchRect` / `castleRect` の3つだけ)は**足りない**。武器商人・拠点サークルは `resolveOutOfSolids` に**入っていない**(=プレイヤーを止めない)ので、距離条件で別に避ける(§2-3) |
| **★候補が全滅した時に諦める順序** | ①原点3000未満 → ②商人/拠点サークルの距離条件 → の順に諦め、**③`resolveOutOfSolids` は最後まで守る**。**それでも取れない最終フォールバックでだけ、押し戻された座標をそのまま採る**(既存の前例=`collectPickup` の着地点は `resolveOutOfSolids` の戻り値をそのまま採用している)。④`clampRectToPlayableArea` は常に最後に通す。**どの段でも「出さない」は選ばない**(出ないと城ボスが出ず勝利不能)(§2-3) |
| **納品ロック: 移動(`isPaused` を使わない)** | **`src/store/gameStore.ts` `movePlayer` の中で `deliveryLocked` なら `tx = ty = 0`**(+`swipeDirection`/`dashOverride`/スライドも無視)。**★冒頭の早期 return は禁止**——同じ `set()` が `vx/vy`・`direction`・`lastDirection`・`isMoving`・`aimX/aimY`・速度ランプ・`marksmanMovingSince`・PHILLレティクルまで書いているため、return すると全部が固まる(走行アニメが回り続ける/移動ズームのまま/**1フレームで速度0=慣性MUST違反**)。`alpha = inertiaAlpha(dt, PLAYER_INERTIA_TAU)`、**`PLAYER_INERTIA_TAU = 0.06`** なので目標0で60msかけて自然に止まる。**`touchActive`/`swipeDirection` を落とす形も撤回**——キーボードは `src/hooks/useGameControls.ts` が `setState({ inputState })` で `inputState.up/down/left/right` を書き、`useGameLoop` が `movePlayer(inputState, …)` へ渡すので素通りする(§2-8) |
| **納品ロック: 攻撃(入口5つ)** | ①**銃の自動射撃**=`useGameLoop` の `fireWeapon(activeGun, …)`(**入力を通らない**。`fireWeapon` は `now - weapon.lastFired < effCooldown` の**クールダウン駆動**) ②離指の近接/カウンター/PHILL=`VirtualJoystick` の `release` → `triggerCounter()` ③マウス=`MouseControls` → `performTapAction` / `performFlickAction` ④キーボード=`useGameControls` の keydown(`isFlickKey` / `isCounterKey` の2枝) ⑤サブウェポン=`useGameLoop` の**サブ発動入口**(全サブが通る `inReturnCircle` 変数)。**実装: `isInputLocked()` に `deliveryLocked` を足す**(実在=`isPaused \|\| player.health <= 0 \|\| corridorRunInActive \|\| isGameTimeStopped()`)と ①②③ が同時に閉じる(`isAttackLocked() = isInputLocked() \|\| attention !== null` なので自動で伝播)。④は2枝が **`isGameTimeStopped()` しか見ていない**ので個別に1条件足す。⑤は変数の式に足す(§2-8) |
| **納品ロック: 被弾** | `gameStore.ts` **`damagePlayer` の入口**(冒頭が `if (player.invulnerable) return false;`)に `deliveryLocked` なら `return false` を足す。**`takeDamage` という関数は存在しない**。`invulnerable` / `INVULN_MS`(=1000)は使わない |
| **納品ロック中の割り込みポーズ(`isPaused` を立てる実在6箇所)** | ①**レベルアップ**=`useGameLoop` の `if (introUntil > 0 && Date.now() >= introUntil) { showUpgradeMenu: true, isPaused: true, levelUpIntroUntil: 0 }` に **`&& !deliveryLocked`**(`levelUpIntroUntil` はこの分岐でしか 0 にされない=**保留しても消えず解除後に開く**) ②**商人サークルの滞在**=`gameStore.updateMerchantDwell` の早期return条件に `deliveryLocked` を足す(**入力不要で自動的に開く**) ③**宝箱**=`collectPickup` の `case 'chest'` の `showUpgradeMenu: true, isPaused: true` を立てない(**磁力で拾えば起きる**・★未決 #19「納品ロック中に宝箱の1回を失うか」) ④**チュートリアルポップ**=`showTutorialPopup` 冒頭で `deliveryLocked` なら既読も付けずに返す ⑤確保済み拠点の武器庫(`triggerCounter` 内)・⑥`requestStoryReturnPrompt` は**入力経由なので上の攻撃ロックで閉じる**=追加対応なし。ポーズ操作/`backgrounded` は塞がない(§2-8) |
| **素通り防止(#5「納品成立の契機」が(A)の場合)** | `src/store/gameStore.ts` `requestStoryReturnPrompt` の早期 return 条件へ **`deliveryLocked` **または** クエストの納品が未成立」の OR** を1つ足す。**★「納品未成立の間だけ」では塞がらない**——実在確認: `VirtualJoystick.release` は **`requestStoryReturnPrompt()` を `isAttackLocked()` の判定より前に無条件で呼ぶ**(同ファイルのコメント「押しっぱなしからの指離しは素通りする」)。よって `isInputLocked()` に `deliveryLocked` を足しても**この呼び出しは通る**=**納品ロック中に離指すると `isPaused:true` が立って完了会話が凍り、「はい」で `gameWon` の2つ目の書き手が生きる**。足さないと**離指→「帰還しますか?」→はい→即 `gameWon`** でサンプルを渡さずクリアできる。`updateReturnPhase` はクエスト中に `gameWon` を立てない=**`gameWon` を書くのは会話が流れ切った1箇所だけ**(§2-8) |
| **★`gameWon` の判定式(会話の終わり)** | **`npcDialogue === null` かつ `npcDialogueQueue.length === 0`** の AND を、**納品成立後は毎フレーム評価する**(★v3・監査A9: 「手順4の enqueue と同じブロックのその後で見る」だけだと、原稿のある S1/S3/S4 は**納品フレームでは `npcDialogue !== null` なので false → 二度と評価されず永久待機**する。しかも `deliveryLocked` で入力が死んでいる)。**確定: 納品成立で `deliveryLocked` を立てたら、以後この AND を毎フレーム見て、真になったフレームで `gameWon`。** 評価の起点は**手順4の `enqueueNpcDialogue` より後**(前に置くと納品フレームでキューが空=会話0行で即 `gameWon`)。**実在確認**: `updateNpcDialogue` は**キューの先頭を `npcDialogue` へ移してから `NPC_DIALOGUE_MS`(2800)表示する**ので、**`queue.length === 0` は「最終行が表示され始めた瞬間」に真**=字義どおり書くと**最終行を2.8秒読ませずに飛ぶ**。逆に判定を enqueue の前に置くと**納品フレームでキューが空=会話0行で `gameWon`**(§2-8) |
| **納品の滞在を積む枝** | `src/store/gameStore.ts` `updateReturnPhase` の `if (state.finaleDefeated && !state.corridorMode)` の枝は **`dwellMs = inside ? 1 : 0`(在内フラグ)で滞在を積まない**。クエスト中は `deltaTime*1000` を足す専用の枝を通す。**`delivering` へ入る瞬間に `leftSinceAccept` 相当を true・`dwellMs=0` にして以後は無条件で積む**。**★`justEntered`(=`inside && rc.dwellMs === 0`)の `RETURN_CLEAR_WEAPON_TYPES`(`grenade`/`trap`/`turret`/`decoy`)撤去は専用枝でも引き継ぐ**(既存の「出入りハメ防止」を落とさない)(§2-8) |
| **★アテンション完了の打刻(HUDの読む先)** | `src/store/gameStore.ts` に **`castleAttnDoneAt`(数値・0=未)** を追加し、`src/hooks/useGameLoop.ts` の城ボス遅延アテンションのディスパッチャ(`castleAttnRef.current.at > 0 && newGameTime >= at && !attention` の中)で **`triggerAttention(...)` を呼ぶのと同じ行で打刻**する。`resetGame` で 0 に戻す。**★HUD の表示条件は `castleAttnDoneAt > 0` **かつ** 「このランでクエストが進行中(`rescueClearedAt > 0` かつ納品未成立=`eventQuestNpc.status` が `accepted`/`warping`/`delivering`)」の AND**。ディスパッチャは**クエストの有無を一切見ていない**ので、`castleAttnDoneAt` 単独だと `?castlenow=1`・フリー・練習・**納品済みステージ**でも黄色の行が出て、`1/1` に変わった後**永久に消えない**。`castleEvent.bossSpawned` と `attention` の組み合わせは**出現〜950msの間に真になる**ため使わない。**★HUDの購読はプリミティブを個別に引く**(`s.castleAttnDoneAt > 0` / `s.rescueClearedAt > 0` / `s.eventQuestNpc.status` / `s.finaleDefeated`)。**`useGameStore(s => s.eventQuestNpc)` と書くと `warping` 中に毎フレーム新参照=60fps再描画**になる。手本は同ファイルの現行 `EventQuestPill`(プリミティブ3つを個別購読)(§2-7) |
| **★待機中のトリガー円の描画** | `src/pixi/pixiScene.ts` に **`arenaGfx` と同じ作法の専用 Graphics を1つ新設**し、**`L.groundLayer` へ `arenaGfx` の隣に addChild + `blendMode = 'add'`**(world座標直描き・depthScaleなし)。半径は **store の `eventQuestNpc.triggerRadius`** を読む。**`syncEventQuestNpc`(=`eventNpcGfx`・actorLayer・通常合成・`circle(0, -8*d, radius*d)`)には描かない**——層/合成/depthScale が違い**半径が構造的に一致しない**。**`syncArena` も触らない**(`if (!ae) return;` なので待機中は何も描かない)。**★待機円をやめる条件は `activeEvent?.rescueQuest === true` のときだけ**——`activeEvent` はシングルトンで**レスキューと無関係な囲い(ゲート/退屈補正/救助/緑卵)でも立つ**ので、「`activeEvent` が立ったら」で書くと**他イベントの40〜60秒のあいだトリガー円が画面から消える**(その間は§2-4の6条件で発火もしないので「円が出た→消えた→城ボスが出ない」だけが見える)。他イベントの円と2本同時に描かれるのは**許容する**。発火時は**同フレームで `syncArena` の円へ引き継ぐ**(同じ式で描くので絵は完全に同一。色を変えた場合だけ200msでクロスフェード)(§2-3) |
| **★半径の唯一の出どころ** | `src/types/game.ts` `EventQuestNpc` に **`triggerRadius: number` を追加**(既存の `radius`=会話サークル64 とは別)。値は `useGameLoop` が出現時に `ARENA_EVENT_RADIUS` を代入する(**この定数は `useGameLoop` のモジュールローカルで `export` されていない**=pixi から import できない。**export化も移設もせず、値を store 経由で渡す**)。進入判定も描画もこの1つを読む(§2-3) |
| **★レスキュー地点の縁矢印の配線** | `src/pixi/pixiScene.ts` `syncArrows` は **`eventQuestNpc` を受け取っていない**(引数は pickups/castle/merchant/_camera/castleVisible/event/pois/baseSites/escorts/playerCenter/hunters/screamers/questTargets/bossMark)。`baseSites`/`hunters` と同じ作法で**配列引数を1本足し、呼び出し側で `status === 'rescue'` の間だけ渡す**。`questTargets` はv1のネームド用=v2では常に空なので流用できない。**★この枝だけ「画面内なら `continue`」(`tx>=0 && tx<=screenW && ty>=0 && ty<=screenH`)を「画面外 or `horizonActorAlpha(npc.y)` がほぼ0」へ広げる**(同クラスの private メソッドなので `syncArrows` 内で完結。他の対象の条件は変えない)(§2-9) |
| **★地平線フェードは触らない(floor 撤回)** | `src/pixi/pixiScene.ts` の `horizonActorAlpha` / 敵本体の `const horizonAlpha = e.type === 'phillboss' ? 1 : this.horizonActorAlpha(fb.footY);` / 足元影の `if (horizonAlpha <= 0) continue;` / `syncEventQuestNpc` の `horizonAlpha <= 0` 早期return——**この4箇所を1バイトも変えない**。しきい値は `horizonForestFootWorldY = camera.y + horizonActorHideScreenY()`(=**遠景森レイヤーの下端 − `HORIZON_ACTOR_HIDE_OFFSET_PX`**)で、目的は「**地平線より上=空に絵を出さない**」。floor を敷くと空の上に絵と影が浮く(§2-3) |
| **★`delivered` に触る先(8箇所=読み6+書き2・全部直す)** | **読み**: `src/utils/storyProgress.ts` `subsAllCompletedFromMeta` / **`src/components/MissionSelect.tsx` の2箇所**(`const subQuestDone = stage.subs.length > 0 && getEventQuestMeta(stage.id).sub;` と CLEAR バッジ) / `src/store/gameStore.ts` `resetGame` の `qGone` / **`src/data/progress.ts` `syncQuestStageClear` の `forcedOk`**(**★`!cfg.forced \|\|` の項ごと落として `delivered` の1項にする**。残すと **`forced:false` の S3/S4/S5 は常に true=納品せずに解放される**) / `acceptEventQuest` の `forcedPending`(v1経路ごと撤去)。**★書き(3巡目の表に無かった)**: **`gameStore.completeEventQuest`**(`setEventQuestMeta(stageId, {...meta, forced:true})` と `{...meta, sub:true}` の2分岐 → **`{...meta, delivered:true}` の1本へ**=v2唯一の書き手)/ **`gameStore.completeEventEncounter`**(`{...meta, sub:true}` → **撤去**)。**書き手を直さないと、読み手だけ `delivered` になって永久に未クリア**(§2-10) |
| **★二人組の投影影(除外条件を書き換える)** | `src/pixi/pixiScene.ts` の `syncLocalEventLighting(...)` 呼び出しは **`s.eventQuestNpc` を渡している**。関数の中で積んでいる行は **`if (eventNpc.status !== 'completed') { push(eventNpc.x, eventNpc.y, …) }`**=**`completed` しか見ていない**ので、**`gone`(納品済み=二人が出ないラン)でも影が落ち続ける**(v0.25.3054 で城/商人を直した時の**残り1件**)。→ **「描画していない全状態(`gone` / `hidden` / フェード満了後の `completed`)」を走査から外す**。**★v3訂正(監査A12): `warping` も走査から外す**——この表の別行と正反対の指示になっていた。**`syncLocalEventLighting` の積み口は `push(x, y, w, strength)` で高さの引数を持たない**(=`heightPx` を渡す先が無い)ので、空中の二人から地面へ影が伸びるのを止める唯一の手段が「走査から外す」。**§2-2B の確定・受け入れ条件4d と同じ側**(`ShadowCasterReq.heightPx` を使うのは**接地影(`npcShadow`)側だけ**) |
| 城ボスのゲート | `src/hooks/useGameLoop.ts` の `castleBossReady`(**AND は時間条件の項だけに掛ける**・§2-6)。出現ブロックと出現アテンション(`castleAttnRef`)は**演出不変** |
| 帰還サークル出現 | `src/hooks/useGameLoop.ts`(`finaleDefeated` → `beginReturnPhase(castleEvent.x, y)`) / 実体は `src/store/gameStore.ts` の `beginReturnPhase`(半径 `RETURN_CIRCLE_RADIUS`=95) |
| 納品の滞在・会話・報酬 | `src/hooks/useGameLoop.ts` の二人組の滞在状態機械(`EVENT_QUEST_DWELL_MS`=3000・`leftSinceAccept` ガード)。**v1の受注分岐は殺す**(§2-2B) |
| 帰還の完了判定 | `src/store/gameStore.ts` `updateReturnPhase`(★通常ストーリーは滞在ではなく**離指→確認ダイアログ**: `requestStoryReturnPrompt` / `answerStoryReturnPrompt`。後者は `isPaused: true` を伴う) |
| 二人組の状態・生成・出現ゲート | `src/store/gameStore.ts`: `createEventQuestNpc`、距離定数 `EVENT_NPC_MIN_DISTANCE`(460)/`EVENT_NPC_MAX_DISTANCE`(950)、`EVENT_NPC_INTERACT_RADIUS`(64)、`resetGame` の出現ゲート。型は `src/types/game.ts` の `EventQuestStatus` / `EventQuestNpc`(**v2で状態を拡張**・§2-2B) |
| **★§2-1 の除外を書く先(1箇所)** | `src/store/gameStore.ts` `resetGame` の **`const qGone = bossMakerRoom \|\| !qCfg \|\| getEventQuestMeta(getSelectedStageId()).sub;`**——**今は3条件しか見ていない**(練習/ガントレット/ベンチ/通路/訓練/屋内/ラボ/EX/再訪/フリーを1つも見ていない)。ここへ `isPracticeRun()` / `isGauntletRun()` / `isExStageRun()` / `getSelectedFreeMode()` / `state.benchmarkRun` / 同関数内のローカル `corridorMode` / `indoor` / `stageTheme === 'lab'` / `farBackdrop === 'tutorial'` / `state.pendingRevisit` を足す(`.sub` は `delivered` へ)。**判定を2箇所に分けない**(§2-1) |
| **★`EventQuestNpc` に足すフィールド(現行は8つしか無い)** | 現行は `x / y / radius / status / questIndex / fadeStartedAt / dwellMs / leftSinceAccept` **だけ**で、**飛び去り・飛来を表せるフィールドが1つも無い**。足すもの: **`moveStartedAt`(段の開始 gameTime)/ `moveFromX` `moveFromY`(始点)/ `moveToX` `moveToY`(終点)/ `movePhase`(`'crouch' \| 'flyout' \| 'flyin'`)**。`createEventQuestNpc` の初期値と `resetGame` も同じコミットで直す(§2-8) |
| **★ワープの段を進める場所** | **`src/hooks/useGameLoop.ts` の二人組ブロック(1箇所)が毎フレーム段を進め、座標を store へ書く。** `pixiScene` は**書かない**(CLAUDE.md「PixiJS only draws」「判定は world/store 側」)。**描画側で始点↔終点を補間して二人を動かす実装にしない**(store の `x/y` が動かないと縁矢印・投影影・`zIndex` が全部ズレる)。**4:00 の飛来の始点** = **着地点から見てプレイヤーと反対側へ `EVENT_NPC_MIN_DISTANCE`(460)離れた点**(実在定数を流用=新しい距離定数を作らない。プレイヤーの手前を横切らず「遠くから降りてきた」に読める。**カメラを参照しない**ので store 側で計算できる)。**着地点(=終点)だけが以後の位置なので、`clampRectToPlayableArea` を通すのは着地点**(飛行中の途中座標は通さない)(§2-3 / §2-8) |
| 受注/完了のアクション | `src/store/gameStore.ts`: `acceptEventQuest`(**ネームド湧きを内包=v2では走らせない**) / `completeEventQuest`(**報酬付与口はここ**。`EVENT_QUEST_REWARD_GOLD`=100 の扱いは ★未決 #7「報酬の100Gを撤去するか」) |
| 囲いの開始/終了 | `src/store/gameStore.ts`: `beginArenaEvent` / `endArenaEvent`(**`fromEvent` を全消しするので他イベントと重ねない**・§2-4) |
| 設定テーブル・会話原稿 | `src/utils/eventQuest.ts`: `EVENT_QUEST_CONFIG`(S5に `basesRequired: 2` を追加+**`encounterOnly` を撤去**・下記)、`EVENT_QUEST_SUB_ACCEPT_LINES` / `EVENT_QUEST_SUB_COMPLETE_LINES` |
| **★`encounterOnly` の撤去(S5が納品できなくなる)** | **フラグとその全参照を消す**: ①`src/utils/eventQuest.ts` の `EventQuestConfig` の `encounterOnly?: boolean` 宣言 ②同 `EVENT_QUEST_CONFIG['stage-5']` の `encounterOnly: true` ③`src/store/gameStore.ts` `acceptEventQuest` 冒頭の `if (cfg.encounterOnly) return;` ④`src/hooks/useGameLoop.ts` の滞在分岐 `if (getEventQuestConfig(qStageId)?.encounterOnly) { completeEventEncounter(); enqueueNpcDialogue(EVENT_QUEST_ENCOUNTER_LINES); ... }` ⑤`src/store/gameStore.ts` の `completeEventEncounter`(v2では呼ばれない)。**★CIが赤くなる**: `src/data/storyCanon.test.ts` が `EVENT_QUEST_CONFIG['stage-5'].encounterOnly` を `true` に固定している(**テストの書き換えは ★未決 §2-15 #14「storyCanonテストの書き換え」**) |
| 永続フラグ | `src/data/progress.ts`: `EventQuestMeta`(型)・`emptyEventQuestMeta`・`isValidEventQuestMeta`(**3つとも `delivered` 基準へ**・§2-10)、`getEventQuestMeta` / `setEventQuestMeta`、`markCastleBossCleared`、`syncQuestStageClear` |
| EX解放の鎖 | `src/utils/storyProgress.ts`: `SUB_QUEST_STAGE_IDS`、`subsAllCompletedFromMeta`(**ここの読み先だけ差し替え**) |
| 二人組の描画 | `src/pixi/pixiScene.ts`: `syncEventQuestNpc`(`horizonActorAlpha<=0` で丸ごと非表示・会話サークル/滞在アークもここ・`EVENT_NPC_FADE_MS`=1100) |
| 縁矢印 | `src/pixi/pixiScene.ts`: `syncArrows`(帰還サークルの矢印は同関数内・`returnCircle` を直接読む=**追加配線不要**。**画面外の時だけ**) |
| 帰還サークルの描画 | `src/pixi/pixiScene.ts`: `syncReturnCircle` |
| サブクエ欄 | 黄色の行は **`SubquestHud` とは別コンポーネント**を `src/components/GameHUD.tsx` の「クエスト列」先頭へ(現行 `EventQuestPill` の位置)。`SubquestHud.tsx` / `src/utils/subquests.ts`(`SubquestRunEntry` / `SUBQUEST_SAVE_KEY`)には**触れない** |
| 二人組の進捗ピル(v1) | `src/components/GameHUD.tsx` の `EventQuestPill`(v1のサブ用。**v2では §2-7 の表示に置き換わるので撤去**) |
| フリー出撃の除外 | `src/data/progress.ts` `getSelectedFreeMode` / `src/App.tsx` の勝利処理(`if (stageId && !getSelectedFreeMode())`) |

### 流用する既存部品(新規に作らない)

| 用途 | 流用先(実在) |
|---|---|
| 囲い(全滅で終わる) | `ActiveEvent { kind: 'horde' }`(`src/types/game.ts`)。半径は `ARENA_EVENT_RADIUS`=240 |
| 色を強制した敵の湧き | `src/utils/enemyUtils.ts` `spawnEnemyAtWithTier`(第5引数で `colorTier` を強制) |
| 削岩型 | `src/utils/drillerAi.ts`(AI純関数)。型は `'driller'`(`src/types/game.ts`) |
| ワープの飛来 | 救難信号アライ/守護霊の飛来ジャンプ: `RESCUE_ALLY_FLYIN_MS`=300 / `RESCUE_ALLY_HOP_PX`=48(`gameStore.ts`)、描画は `pixiScene.ts` の救援アライ(放物線+ease-out+`jumpStretch`) |
| ワープの退場(飛び去り) | 同じ救難信号アライの離脱: `RESCUE_ALLY_CROUCH_MS`(しゃがみ溜め)→ `RESCUE_ALLY_FLYOUT_MS`(バックジャンプ) |
| 出現位置の空き地探索 | `src/store/gameStore.ts` `beginReturnPhase` の空き地探索(角度を振って半径を広げる) |
| 拠点の確保数(S5) | `state.baseSites.filter(b => b.status === 'captured').length`(**ラッチして使う**・§2-11) |
| 通常プレイ判定 | `isPracticeRun` / `isBossMakerRun` / `isGauntletRun` / `benchmarkRun` / `corridorMode` / `isExStageRun` / `getSelectedFreeMode` |

### 実在確認で判明した「旧マップの誤り・今の事実」

1. **`ARENA_EVENT_RADIUS` の値は 240**。旧マップが「615」と書いていたのは**行番号**であって値ではない
   (実装者が半径615で作る事故が起きうる誤記だった)。**この章では行番号を一切書かない**。
   同型の誤記だったもの: `EVENT_NPC_MIN/MAX_DISTANCE`(実値は 460/950)、`EVENT_NPC_INTERACT_RADIUS`(実値は 64)。
2. **城ボスは5:00**(`src/config/castleBoss.ts` `CASTLE_BOSS_MIN_TIME_MS = 5*60*1000`)。v1章の「7分出現」は**古い**。
3. **`castleBossReady` には開発用の強制出現が同居している**(`FORCE_CASTLE_BOSS || practiceForces('castlenow') || 時間`)。
   全体に AND すると `?castlenow=1` が死ぬ(§2-6)。
4. **`clampRectToPlayableArea` は S1/S3/S4/S5 では no-op**(tutorial / labTheme / corridorMode / exStage の
   4条件しか見ていない)。屋外の通常ステージで「clampが守ってくれる」は成立しない(§2-3)。
5. **`syncEventQuestNpc` は `horizonActorAlpha <= 0` で二人組を丸ごと非表示にする**。
   縁矢印は画面外の時だけなので、**画面内・透明・矢印なしの死角**が作れてしまう(§2-3)。
6. **囲いの `timedOut` 側はゲート専用の失敗処理**(バナー「ゲート突破失敗 —— 押し戻された」+原点方向へ
   ノックバック+シェイク)を**全イベント共通で**通る。レスキューの囲いはこの枝を通してはいけない(§2-4)。
7. **`endArenaEvent` は `fromEvent` の敵を全消しする**。他イベント中に `activeEvent` を上書きすると
   相手のイベント敵まで消える(§2-4)。
8. **`isValidEventQuestMeta` は `forced`/`sub` が boolean であることを要求**しており、
   `{delivered:true}` だけ保存すると次回起動で丸ごと捨てられる(§2-10)。
9. **拠点は失陥する**(`status: 'open' | 'captured'`。HP0で `captured` → `open`)。現在値比較のままだと
   S5のゲートが閉じ直す(§2-11)。
10. **プレイヤーには `player.vaccineRevives` による復活が実在する**(病院で入手/ショップで購入。
    致死ダメージを打ち消してHP半分で復活)。「死亡=ランが終わる」は前提として成り立たない
    (扱いは §2-4 の本文で確定=**囲いをそのまま継続**する)。
11. **会話はゲームを止めない**(`NPC_DIALOGUE_MS`=2800 × 行数 + `NPC_DIALOGUE_GAP_MS`=500 の時間駆動)。
    納品後の7〜10秒は無防備になる(§2-8)。
12. **通信原稿3本は既に実装済み**で原稿と完全一致(§2-13 の注)。新規に書き起こす作業は無い。
13. **城ボス撃破後の帰還は「3秒滞在」ではない**。`finaleDefeated` 経路は**離指→「帰還しますか?」確認**
    (§2-8 の注)。3秒(`RETURN_CIRCLE_HOLD_MS`)は屋内/イベント帰還の経路。
14. **帰還サークルの縁矢印は既にある**が**画面外の時だけ**(`syncArrows` が `returnCircle` を直接読む・
    エメラルド `0x34d399`)。**城ボスのマーカー(`castle.bossSpawned` 時の赤)も同じく画面外の時だけ**なので、
    「常時表示の画面内マーカー」は**このゲームにまだ存在しない**(★未決 #9「納品先の誘導マーク」)。
15. **`kind: 'rescue'` は既に別物として存在する**(防衛イベント・`src/world/rescue.ts`)。
    **★v3訂正(監査C18)**: `RESCUE_HOLD_NEED_MS`=25000 は**ホールドゲージ**であって寿命ではない。
    **イベントの `endsAt` は `newGameTime + 120000`(2分)**。しかも**プレイヤーを閉じ込めない**
    (`movePlayer` の円クランプが `kind !== 'rescue'` を除外)ので、**この2分の間にレスキュー円へ歩いて行ける**
    =§2-4 の「前置き1本(`endArenaEvent()` してから発火)」が実際に効く唯一の相手。
    名前が同じでも流用しない(§2-4)。
16. **削岩型は現在 stage-4〜7 限定**(`DRILLER_ALLOWED_STAGES`・`allowDrillerForRun`)。
    ただしこのゲートが掛かるのは**通常湧きの pumpkin 枠の差し替え**だけで、`spawnEnemyAtWithTier` の
    直接湧きは通らない。**S1/S3 で赤い削岩型を出すと「削岩型の初出がS1」になる**(★未決 #2「赤い削岩型をS1/S3に出すか」)。
17. **赤ティアは本体スプライトを `0xff5544` で塗る**(`ENEMY_COLOR_TIER_BODY_TINT`)。
    赤い本体+赤い予告帯の可読性は要検討(★未決 #2「赤い削岩型をS1/S3に出すか」)。
18. **`SubquestHud` は行ごとの色を持っていない**(`r.done` で緑/白のみ)。黄色の行は別コンポーネントにする(§2-7)。
19. **練習ランの永続書き込みは `practiceGuard` が localStorage ごと塞ぐ**ので、新フラグを足しても既定で安全。
    **ただしフリー出撃(`getSelectedFreeMode`)は practiceGuard の対象ではない**ので、§2-1 の除外が要る。
20. **`kind:'horde'` を名乗るだけで敵が18体追加で湧く。** 段階スポーンは `ae.kind === 'horde'` の間ずっと
    `hordeSpawnRef` を見て1秒に1体ずつ `ARENA_HORDE_COUNT`(=18)体まで足し続け、`cleared` は
    `spawned >= total` を要求する。ゲート1は配置直後に `hordeSpawnRef` を自前で潰している(§2-4)。
21. **完了判定の `fromEvent` はグローバル。** `enemies.filter(e => e.fromEvent)` は場全体を数えており、
    このイベントの敵に限定していない。`fromEvent: true` の生産者は囲いの段階スポーン以外にも実在する
    (`beginRescueEvent` の攻撃者 / ラボの `idol` / 練習・デバッグ経路 / **拠点tickの `spawnList`**)。
    ※拠点tickは現在 `SUPP_BASE_ATTACKS_ENABLED = false` で無効(コードは残置)(§2-4)。
22. **既存の囲いの保険は 40秒(horde)/ 60秒(boss)。** v2初稿の「10分」はこの帯から2桁外れていた(§2-4)。
23. **囲いの抽選側は6条件をANDしている**(`activeEvent` / `bossChasing` / `isHiddenBoss` /
    `hunterRef.phase==='idle'` / `redNight?.phase!=='active'` / `isBountyType` 不在)。
    **`activeEvent === null` だけでは足りない**(§2-4)。裏ボス側にも
    「囲い系イベント中は裏ボスを出さない(重なると逃走で詰み=終わらない・社長報告)」という
    既往事故のコメントが実在する。
24. **`horizonActorAlpha` は `pixiScene` の private メソッドで、`this.cameraY` / `this.wgZoom()` を使う
    カメラ相対**。store / `useGameLoop` からは呼べないし、呼べても出現1フレームしか成立しない(§2-3)。
25. **`updateNpcDialogue(newGameTime)` は `if (!isPaused && !backgrounded && rendererReady)` の内側**にあり、
    会話は時間駆動。**納品後に `isPaused: true` を立てると会話が1行も進まず永久フリーズ**する。
    `answerStoryReturnPrompt` が `isPaused` を使えるのは**タップ待ちのモーダル**だから(§2-8)。
26. **プレイヤー被弾の入口は `damagePlayer`**(冒頭 `if (player.invulnerable) return false;`)。
    **`takeDamage` という関数はこのコードベースに存在しない**。`invulnerable` は `INVULN_MS`(=1000)で
    毎フレーム自動解除される被弾無敵で、7〜10秒の待ちには使えない(§2-8)。
27. **城ボスの出現アテンションは出現の 950ms 後**(`castleAttnRef.current = { at: newGameTime + 950 }`)。
    ディレイ0だと受注会話の1行目に被る(§2-6)。`castleAttnRef` は `useRef` で **store に無い**ので
    HUD の表示条件には使えない(→ store に `castleAttnDoneAt` を足して打刻し、**クエスト進行中との AND**で
    読む・§2-7 / 事実61)。
28. **`EVENT_QUEST_CONFIG['stage-5']` は `encounterOnly: true`** で、`acceptEventQuest` の冒頭に
    `if (cfg.encounterOnly) return;` がある。**撤去しないとS5だけ永久に納品できない**
    (原稿0行なので画面上は「何も起きない」だけで原因が見つけにくい)。
    `src/data/storyCanon.test.ts` が `encounterOnly === true` を固定しているのでCIが赤くなる(§2-14 / ★未決 #14「storyCanonテストの書き換え」)。
29. **`EVENT_QUEST_LINES_FORCED` は M1初遭遇の5行**(ミラ「この国の人？　助かったよ！」/グレン「俺はグレン。
    こいつはミラだ。礼を言う。では……」ほか)で、`storyCanon.test.ts` が全文を固定している。
    v2がS1へ割り当てた受注原稿①は「またお前か」で始まる(★未決 #11「S1初対面の台詞が消える」)。
30. **デンジャーゾーンの入口は `AREA_THRESHOLDS[1]`=3000、拠点の輪は `BASE_SITE_RADIUS`=3200**。
    「進行方向500〜1000px先」だけでは拠点で 4:00 を迎えるとデンジャーゾーン内に出る(§2-3)。
31. **`isArenaSweepProtected` は `driller` を守る**(`isPumpkinTier` = `pumpkin` + `driller`)。
    一方 `zombie`/`skeleton`/`bat` は守られない。**`beginArenaEvent` を敵の配置より後に呼ぶと
    赤い削岩型1体だけが残る**(§2-4)。ゲート1/2には「必ず敵を配置する**前**に呼ぶ」コメントが実在する。
32. **`syncArena` は `if (!ae) return;`**。`activeEvent` が立つまで**円を1本も描かない**=
    待機中のトリガー円を描く実体がコード上に存在しない(§2-3)。
33. **`syncEventQuestNpc` の会話サークルは半径 `EVENT_NPC_INTERACT_RADIUS`(=64)・青 `0x60a5fa`で、
    しかも `near`(半径+72)に入った時だけ描く**。囲いのトリガー円(240)とは**別物**(§2-3)。
34. **敵にも地平線フェードが掛かる**(`e.type === 'phillboss' ? 1 : this.horizonActorAlpha(fb.footY)`)。
    足元影は `if (horizonAlpha <= 0) continue;` で丸ごと落ちる。
    **※これは「地平線より上に絵を出さない」ための仕様であって不具合ではない**(→ 事実51)。
    **囲いの敵にも例外を作らない**(=フェードは触らない)。見失い対策は縁矢印側(§2-9)(§2-3)。
35. **キーボードの移動は `touchActive`/`swipeDirection` を通らない。**
    `useGameControls.ts` が `useGameStore.setState({ inputState })` で `inputState.up/down/left/right` を
    書き、`useGameLoop` が `movePlayer(inputState, …)` へ渡す。
    `requestStoryReturnPrompt` が入力フィールドを落とすだけで足りているのは
    **同時に `isPaused: true` を立てているから**(§2-8)。
36. **城ボスは湧いた瞬間に `markCastleBossSpawned()` を呼び、アテンションはその 950ms 後**。
    `bossSpawned && attention === null` は**出現〜950msの間 真**になる(§2-7)。
    ※ディスパッチャは `!attention` を待ち続けるので**アテンションが捨てられる経路は無い**が、
    HUDの表示可否をこの内部タイミングの読みに賭けない。
37. **`updateReturnPhase` の通常ストーリー枝は滞在を積まない**(`const dwellMs = inside ? 1 : 0;`)。
    3秒(`RETURN_CIRCLE_HOLD_MS`)を積むのは屋内/イベント帰還の枝だけ。
    さらに **`requestStoryReturnPrompt`(離指)→`answerStoryReturnPrompt(true)`→即 `gameWon: true`** で
    **納品せずリザルトへ行ける**(§2-8)。呼び出し元は `VirtualJoystick.tsx` と `utils/inputActions.ts`。
38. **`leftSinceAccept` の初期値は `true`**(`createEventQuestNpc`)。`false` にするのは
    `acceptEventQuest` / `completeEventQuest` の2箇所だけ。v2ではその2つを走らせないので実害は無いが、
    **v1の状態機械を流用して書き直すと納品メーターが永久に進まない**(§2-8)。
39. **レベルアップのメニューは `useGameLoop` の1箇所で開く**
    (`if (introUntil > 0 && Date.now() >= introUntil) { showUpgradeMenu: true, isPaused: true, levelUpIntroUntil: 0 }`)。
    `levelUpIntroUntil` は**この分岐でしか 0 にされない**ので、条件を足して保留しても**取りこぼさない**(§2-8)。
40. **`syncArrows` は `eventQuestNpc` を受け取っていない**(§2-9)。`questTargets` はv1のネームド用で
    v2では常に空。**レスキュー地点の縁矢印は配線を1本足さないと1本も出ない。**
41. **`.forced`/`.sub` に触るのは8箇所**——**読み手6**(`subsAllCompletedFromMeta` / `MissionSelect.tsx` ×2 /
    `resetGame` の `qGone` / `syncQuestStageClear` の `forcedOk` / `acceptEventQuest` の `forcedPending`)
    **+ 書き手2**(`completeEventQuest` / `completeEventEncounter`)。→ 事実60(§2-10)。
42. **`SUB_RESCUE_MISSION` は stage-1/3/4 の `subs` にしか入っていない**。stage-5 は `subs: []` で
    `storyCanon.test.ts` が `expect(st('stage-5').subs).toEqual([])` を固定(★未決 #18「S5にサブ任務カードを出すか」)。
43. **囲いの発火6条件の自己解消性**(★未決 #16「囲いの発火6条件のゲート」の材料): `activeEvent`=有限 / `bossChasing`=帰巣で解ける /
    裏ボス=`despawn` で `enemies` から除去される / ハンター=立ち去りフェード /
    紅き夜=**20秒**(`beginRedNightWarning` が `endAt = activeAt + 20000`) /
    賞金首=**dormant のまま60秒**(`BOUNTY_LINGER_MS`=60000)+退場フェードで消える。
    **ただし賞金首の滞在タイマーが進むのは `dormant` の間だけ**で、`dist <= aggroRange`(既定380)で起き、
    交戦中は `bountyLastEngagedAt` が毎フレームリセットされる=**円が賞金首の索敵圏に重なると、
    円の中で待つ限り永久に発火しない**。
44. **`isCorpse(e)` = `e.corpseUntil !== undefined`**。`driller` も `corpseEligible` は true
    (`enemyUtils.test.ts` が pumpkin と同値で固定)(§2-4)。
45. **拠点確保の在内半径は `BASE_CAPTURE_RADIUS`=130、拠点の輪は 3200**。
    → **2か所目を確保した瞬間、プレイヤーは必ず原点から 3070〜3330px に居る**=常に3000の外(★未決 #17「S5の出現点の規則」)。
46. **★走っている `activeEvent` を外から強制解除する経路が実在する。** `useGameLoop` の
    `if (gateFireOk && activeGateRef.current == null && aePre && aePre.kind !== 'boss') { … endArenaEvent(); }`
    (ゲート発火待ちのプリエンプション・社長決定v0.25.1555)。**保護されているのは `kind:'boss'` だけ**なので
    `kind:'horde'` のレスキューは対象。`endArenaEvent()` は `cleared`/`timedOut` を通らない(§2-4)。
47. **既存の囲いの発火は1本の if/else 連鎖**(`if (!ae) { gate1 → gate2 → 退屈補正 → 2分タイマー }`)で
    構造的に排他している。**別の if に書くとこの保証の外へ出る**(§2-4)。
48. **通常囲いの次回時刻は「発火した時にだけ」先送りされる**(`nextArenaAtRef.current = newGameTime +
    ARENA_FIRE_INTERVAL_MS`(=120000)/ `boredomArenaNextEligibleAtRef.current = newGameTime +
    BOREDOM_ARENA_CD_MS`(=120000))。**塞がれている間も時刻は進む**=空いた瞬間に発火しうる。
    しかも**通常囲い/退屈補正の囲いはどちらも中心=プレイヤー中心**(`{ kind, x: pcx, y: pcy, … }`)(§2-4)。
49. **`ARENA_EVENT_RADIUS` は `useGameLoop.ts` のモジュールローカル定数で `export` されていない**
    (`const ARENA_EVENT_RADIUS = 240;`)。`pixiScene` からは読めない(§2-3)。
50. **`syncArena` の円と `syncEventQuestNpc` の円は、層も合成も座標系も違う。**
    `arenaGfx` = `L.groundLayer` の子・`blendMode='add'`・**world座標に直描き**(`g.circle(ae.x, ae.y, ae.radius)`)。
    `eventNpcGfx` = `L.actorLayer` の子 `eventNpcView`(`zIndex = npc.y`)の中・通常合成・
    **`g.circle(0, -8 * d, npc.radius * d)`**(`d = depthScale(npc.y)`)。**半径1px一致は構造的に不可能**(§2-3)。
51. **地平線フェードのしきい値は遠景森の下端**: `horizonForestFootWorldY = camera.y +
    horizonActorHideScreenY()`、`horizonActorHideScreenY() = L.horizonForest.y + L.horizonForest.height −
    HORIZON_ACTOR_HIDE_OFFSET_PX`。実在コメントは「**地平線際は出さない(空に浮かない描画と整合)**」
    「地平線で透明化(**空に浮かない**)」「さらに地平線フェード(**空に浮かない**=他アクターと同じ)」の**3箇所**。
    →**「遠景に重なると薄くなる」装置ではない**。floor を敷くと空の上に絵と影が浮く(§2-3)。
52. **`movePlayer` は移動だけの関数ではない。** 同じ `set()` で `vx`/`vy`/`direction`/`lastDirection`/
    `isMoving`(`speedNow > moveSpeed * 0.15`)/`aimX`/`aimY`/速度ランプ/`marksmanMovingSince`/
    PHILLレティクルを毎フレーム書く。**冒頭の早期returnは全部を凍らせる**(§2-8)。
53. **`PLAYER_INERTIA_TAU = 0.06`(60ms)**(社長指示v0.25.3618で 0 から変更)。
    `vx = player.vx + (tx*moveSpeed*speedScale − player.vx) * inertiaAlpha(dt, tau)` なので、
    **目標を0にすれば60msで自然に減速し `isMoving` も落ちる**(§2-8)。
54. **銃の自動射撃は入力を1つも通らない。** `useGameLoop` が毎フレーム `fireWeapon(activeGun, …)` を呼び、
    中身は `if (now - weapon.lastFired < effCooldown) return [];` の**クールダウン駆動**。
    **入力側だけ塞ぐと納品ロック中も撃ち続ける**(§2-8)。
55. **共通ゲートは2本**(★v3訂正・監査C17: `isInputLocked` は **`farBackdrop === 'ending'` を含む5項**): `isInputLocked() = isPaused || player.health <= 0 || corridorRunInActive || farBackdrop === 'ending' ||
    isGameTimeStopped()` / `isAttackLocked() = isInputLocked() || attention !== null`。
    **`MouseControls` は `isInputLocked()` しか見ておらず、`useGameControls` の flick/counter 枝は
    `isGameTimeStopped()` しか見ていない**(§2-8)。
56. **`isPaused: true` を書く実在の箇所は6つ**: レベルアップのメニュー / **`updateMerchantDwell`**(円内滞在で
    **入力不要で自動的に**ショップが開く)/ **`collectPickup` の `case 'chest'`**(磁力で拾えば装備選択が開く)/
    `showTutorialPopup` / `triggerCounter` 内の確保済み拠点の武器庫 / `requestStoryReturnPrompt`(§2-8)。
57. **`syncLocalEventLighting` の二人組の除外条件は `status !== 'completed'` の1つだけ**
    (`if (eventNpc.status !== 'completed') { push(eventNpc.x, eventNpc.y, …) }`)。
    → **`gone` でも投影影が落ち続ける**(v0.25.3054 と同型の残り1件)。影の高度は
    `ShadowCasterReq.heightPx` で表す(§2-2B)。
58. **`resolveOutOfSolids`(プレイヤーが立てるかの唯一の関数)が見るのは
    木→松明→城→街プロップ→病院→武器庫→警察署**(labThemeなら壁/プロップ)。
    一方 **`beginReturnPhase` の空き地探索が屋外で見るのは木の幹・松明・城の3つだけ**。
    **武器商人と拠点サークルはどちらにも入っていない**(=プレイヤーを止めない)(§2-3)。
59. **帰還サークルは `beginReturnPhase(castleEvent.x, castleEvent.y)`= `avoidPlayer` 既定 true** で呼ばれ、
    **`RETURN_CIRCLE_AVOID_DIST`=240** 未満ならプレイヤーから240px離す。半径は **`RETURN_CIRCLE_RADIUS`=95**。
    → **出現の瞬間、プレイヤーは必ず円の外**(「倒したその場が円の中」は起きない)(§2-8)。
60. **`getEventQuestMeta` の呼び出しは(テストを除き)8箇所**で、**うち2つは書き手**
    (`completeEventQuest` の `forced:true` / `sub:true` の2分岐、`completeEventEncounter` の `sub:true`)。
    3巡目の表は読み手6つしか載せていなかった(§2-10)。
61. **城ボスの遅延アテンションのディスパッチャはクエストの有無を見ていない**
    (`castleAttnRef.current.at > 0 && newGameTime >= at && !attention` の3条件だけ)。
    **`?castlenow=1` でも納品済みステージでもフリーでも張られる**(§2-7)。
62. **二人組の出現ゲート(`resetGame` の `qGone`)は3条件しか見ていない**
    (`bossMakerRoom || !qCfg || meta.sub`)。練習/ガントレット/ベンチ/通路/訓練/屋内/ラボ/EX/再訪/
    フリーは1つも見ていない(§2-1)。
63. **`isInReturnCircle` は「プレイヤー中心 vs 円中心の距離 <= 半径」**
    (`Math.hypot(rc.x - px, rc.y - py) <= rc.radius`)。救助のホールド判定・拠点確保の在内判定も同じ形=
    **このゲームの「円の中に居るか」は全部これ**(§2-4)。
64. **`EVENT_QUEST_CONFIG` の `forced` は stage-1 だけ true**(stage-3 / stage-4 / stage-5 は **false**)。
    `syncQuestStageClear` の実在の式は **`const forcedOk = !cfg.forced || getEventQuestMeta(stageId).forced;`**
    なので、**`.forced` を `.delivered` へ置き換えるだけでは S3/S4/S5 が常に true のまま**
    =城ボスを倒して帰るだけで解放される。**`!cfg.forced ||` の項ごと落とす**(§2-10)。
65. **`VirtualJoystick.release` は `requestStoryReturnPrompt()` を `isAttackLocked()` の判定より前に、
    無条件で呼ぶ**(`const returnPromptOpened = pointerId !== null && fireCounter ? …() : false;` →
    その後で `!returnPromptOpened && !isAttackLocked()`)。同ファイルのコメント
    「**押下は isInputLocked で弾かれても、押しっぱなしからの指離しは素通りする**」。
    → **`isInputLocked()` に `deliveryLocked` を足しても、この経路は塞げない**(§2-8)。
66. **`createEventQuestNpc()` の初期 `status` は `'available'`**、座標は**原点基準・方位角ランダム・
    距離 460〜950 のランダム**。描画スイッチは status なので**直さないと 0:00 から二人が立っている**(§2-2B)。
67. **`resetGame` の `qGone` が true の時、入るのは `{ ...createEventQuestNpc(), status: 'gone' as const }`**。
    → **「このランでクエストが有効か」は `eventQuestNpc.status !== 'gone'` の1本で読める**
    (除外条件を2度書かない)(§2-1)。
68. **二人組の影は2系統ある。** (a) `syncLocalEventLighting` の積み口は **`push(x, y, w, strength = 1)`**
    =**高さの引数が無い**(構造体は `{x,y,w,strength,horizonAlpha}`)。
    (b) `syncEventQuestNpc` が組む **`npcShadow`**(型 `BuildingShadowReq` = `x/y/legacyW/rawW/rawH/
    texture/alpha/shadowFade?/isStatic?` の9つ)を `syncShadows` が `place({id:'npc',…})` で
    `ShadowCasterReq` へ正規化する。**`BuildingShadowReq` にも `place` 呼び出しにも `heightPx` が無い**
    ので、高度を出すには**フィールドを足して素通しする**(護衛/救援アライは `heightPx: v.shadowHeight`
    で既に渡している)(§2-2B)。
69. **`updateNpcDialogue` はキューの先頭を `npcDialogue` へ移してから `NPC_DIALOGUE_MS`(2800)表示する**
    (`cur = {...head, until: gameTime + NPC_DIALOGUE_MS}; queue = queue.slice(1);`)。
    → **`npcDialogueQueue.length === 0` は「最終行が表示され始めた瞬間」に真**(§2-8)。
70. **`resolveBountyMove(nx, ny, box)` が export 済み**で、store から ctx
    (`labTheme`/`farBackdrop`/`solidProps`(mine・uv-bar 除外)/`castleEvent`/`hospital`+`hospitalTaken`/
    `armory`+`armoryTaken`/`police`+`policeTaken`/`facilitiesHidden`)を組んで `resolveOutOfSolids` を呼ぶ。
    **ctx を書き写す必要は無い**。**`PLAYER_HITBOX` は 28 で export 済み**。
    **`EventQuestNpc` に width/height は無い**(§2-3)。
71. **`collectPickup` の着地点は `resolveOutOfSolids` の戻り値をそのまま採用する**(棄却も振り直しもしない。
    コメント「押し出しは一番浅い向きなので、木の根元に落ちた弾薬は幹のすぐ横に出る」)。
    → **最終フォールバックで押し戻し座標を採るのは、既存の作法に合わせる側**(§2-3)。
72. **囲いの if/else 連鎖の並びは `… else if (puzzleActiveNow) { 退屈補正 } else { 2分タイマー }`。**
    → **`puzzleActiveNow === true`(通常プレイの大半)の間、2分タイマーの枝には到達しない**
    =`nextArenaAtRef` の押し戻しは通常プレイでは no-op。効くのは `boredomArenaNextEligibleAtRef` だけ
    (退屈補正の囲いも中心=プレイヤー中心)。**#16「囲いの発火6条件のゲート」の材料の訂正**(§2-4)。
73. **レベルアップの演出とメニューは別の場所。** `gainExperience` が
    **`triggerTimeSlow(0.25, LEVELUP_INTRO_MS=850)`+グロー2枚+SE** を出し、`levelUpIntroUntil` の
    経過後に `useGameLoop` の別ブロックがメニューを開く。→ **メニュー側に `!deliveryLocked` を足しても
    演出は1バイトも変わらない**。会話は `newGameTime`(timeScale 込み)で進むので**遅くはなるが止まらない**(§2-8)。
74. **`updateReturnPhase` は通常ストーリー枝でも `justEntered` に設置物を撤去する**
    (`projectiles.filter(pr => !RETURN_CLEAR_WEAPON_TYPES.has(pr.weaponType))`、
    `RETURN_CLEAR_WEAPON_TYPES = new Set(['grenade','trap','turret','decoy'])`。
    コメント「入った瞬間(外→内)に設置中のトラップ/手榴弾/タレット/デコイを撤去(出入りハメ防止)」)。
    **クエスト専用枝でも引き継ぐ**(§2-8)。
75. **スケーターの慣性時定数は乗車中だけ別**(`inertiaTau = skaterRiding ? [PLAYER_INERTIA_TAU,1.2,0.8,0.5][skLv]
    : PLAYER_INERTIA_TAU`)で、**移動速度は乗車中 ×3**。急停止用の枝
    **`skaterStopping`(`skaterStopUntil` が未来の間、tau=50ms で残速度を減衰)は実在するが、
    現在この値をセットしている箇所がコードベースに1つも無い**(初期化と `resetGame` の 0 だけ)。
    降車は `dismountSkater`(1秒以上乗っていればスケボー投擲)(★未決 #22「スケーター乗車中の納品」)。
76. **城ボスの出現ブロックは `activeEvent` を見ていない**
    (`!danceTest && !indoor && !labTheme && !storyBoss && !isExStageRun() && !tutorialStage &&
    (!noSpawn || practiceWantsCastleBoss()) && !revisitRun && !corridorMode && !castle.bossSpawned &&
    castleBossReady && !castleSpawnLocked`)。→ **囲いに拘束されたまま城ボスの出現+アテンションが走りうる**
    (★未決 #20「レスキュー直後に足元で開くゲート囲い」)。
77. **ゲートの発火予約は時刻ではなく pending 駆動**(`gate1PendingRef` / `gate2PendingRef` が立っている限り、
    `shouldTriggerGate1/2` は `activeEvent` が空いた次のフレームに true になる)。
    → **プリエンプション保護で「待たせる」と、レスキューの囲いが終わった次フレームに必ず発火する**
    (押し戻す先が無い)(★未決 #20「レスキュー直後に足元で開くゲート囲い」)。

### 性能(CLAUDE.md「Performance review」)

**負荷スコア 1/10**(無視できる)。増えるのは①囲いイベント1回ぶんの敵(既存の囲いと同数帯)
②二人組スプライト1体の飛び去り+飛来アニメ(各数百ms)③サブクエ欄1行。
**強glow(=投影影を落とす光源)を増やさない**ので、実測で効く唯一の軸に触っていない。
per-frame の新規走査も追加しない(判定は時刻/フラグの比較のみ)。

---

## 2-15. ★未決事項(社長裁定へ)

> 書かれていないので**実装者が勝手に決めてしまう**箇所。すべて**ゲームの見え方・挙動・体験**に触れるため
> 設計チャットでは決めない。各項に **①問題 ②選べる案 ③推薦1つ** を書く(過去の裁定は事実として併記)。
>
> **全21件**(#1「レスキュー囲いの中身」・#2「赤い削岩型をS1/S3に出すか」・#4「レスキュー出現の告知と誘導」〜#22「スケーター乗車中の納品」。**#3 は本文へ降ろしたため欠番**——番号を詰めると本文からの参照が
> 全部ずれるので詰めない)。うち **#11「S1初対面の台詞が消える」〜#14「storyCanonテストの書き換え」・#16「囲いの発火6条件のゲート」〜#22「スケーター乗車中の納品」は「設計の穴」ではなく仕様判断**
> (物語の確定稿・拒否不能な必須経路・拘束時間・憲法テストの書き換え・**勝利不能へ倒れるゲート**・
> **S5の出現規則**・**S5のサブ任務カード**・**納品ロック中に失う宝箱の1回**・
> **レスキュー直後に足元で開くゲート囲い**・**発火枝の書き方**・**スケーター乗車中の納品**)なので、
> **設計チャットでは決めずに社長へ戻す**。
>
> **6巡目で追加した3件(#20「レスキュー直後に足元で開くゲート囲い」・#21「発火枝のゲートの置き場所」・#22「スケーター乗車中の納品」)は、どれも「本文だけで直すと仕様が動く」もの**
> (ゲートの発火を止める/既存の抽選の落ち方を変える/乗り物から降ろす)。
> **#21「発火枝のゲートの置き場所」は #16「囲いの発火6条件のゲート」の材料の前提なので、#16 より先に決める必要がある。**
>
> ### ★索引(番号だけでは引けないので、名札で引く)
> | # | 名札 | ひとことで言うと | 種別 |
> |---|---|---|---|
> | **#1** | レスキュー囲いの中身 | 「数体の敵」の数・種別・配置が未指定 | 穴 |
> | **#2** | 赤い削岩型をS1/S3に出すか | 初お目見えがS1になる+真っ赤な本体と赤い予告帯の可読性 | 穴 |
> | **#3** | (欠番) | 本文へ降ろした | — |
> | **#4** | レスキュー出現の告知と誘導 | 決めるのは見せ方だけ(バナー/アテンション/マーカー) | 穴 |
> | ~~#5~~ | 納品成立の契機 | ✅ **裁定済み**(2026-08-22「サークル3秒で。」=(A) 3秒滞在) | 解決 |
> | **#6** | レスキュー未訪問のまま城ボス時刻 | 二人組側の見え方 | 穴 |
> | **#7** | 報酬の100Gを撤去するか | 現行 `EVENT_QUEST_REWARD_GOLD`=100 の扱い | 穴 |
> | **#8** | 城ボス撃破時のゴール表示 | 「2人クエストの時は出さない」への回答 | 穴 |
> | **#9** | 納品先の誘導マーク | 「画面外の時だけ」では実質ほぼ出ない | 穴 |
> | **#10** | 囲い進入前の警告表示 | 要否(#12 の推薦と連動) | 穴 |
> | **#11** | S1初対面の台詞が消える | 物語の確定稿に触れる | 仕様判断 |
> | **#12** | 囲いを拒否不能のまま必須経路にするか | 拒否不能・不可逆 | 仕様判断 |
> | **#13** | 囲いの90秒上限 | 拘束時間 | 仕様判断 |
> | **#14** | storyCanonテストの書き換え | 憲法テストに触れる(#18 と連動) | 仕様判断 |
> | **#15** | デンジャーゾーン内に囲いが出た時の難度 | S1/S3/S4 で難度が跳ねる | 穴 |
> | ~~#16~~ | 囲いの発火6条件のゲート | ✅ **裁定済み**(2026-08-30: 6条件撤去=発火する / 裏ボス中だけ非表示 / 位置で他イベント範囲を避ける) | 解決 |
> | **#17** | S5の出現点の規則 | 「原点3000未満」がS5では毎回破れる | 仕様判断 |
> | **#18** | S5にサブ任務カードを出すか | 憲法テストに触れる(#14 と連動) | 仕様判断 |
> | **#19** | 納品ロック中に宝箱の1回を失うか | 装備選択が1回開かない | 仕様判断 |
> | **#20** | レスキュー直後に足元で開くゲート囲い | 城ボスの出現演出に重なる | 仕様判断 |
> | **#23** | **裏ボス非表示の窓に上限も合図も無い** | 裏ボス4体が居る間はレスキューが消える。上限が無く、理由も画面に出ない(★v3・監査A4で新設。推薦=(a)このまま) | 仕様判断 |
> | #21 | 発火枝のゲートの置き場所 | 条件側か枝の中か。**#16 の裁定(2026-08-30)でゲート自体が消えたため前提関係は解消**。本文の確定(枝の中)は構造の担保として有効=**実装を止めない** | 仕様判断 |
> | **#22** | スケーター乗車中の納品 | 受け入れ条件が2つ破れる | 仕様判断 |
>
> ### ★★実装を止めるブロッカーは**無い**(2026-08-30 で全て解消)
> | 件 | 状態 |
> |---|---|
> | ~~#16「囲いの発火6条件のゲート」~~ | ✅ **裁定済み(2026-08-30)**= 6条件を撤去して**発火する** / **裏ボス中だけ非表示**(`hidden`) / 出現位置で**他イベントの範囲を避ける**。本文 §2-4・§2-3・§2-2B に反映済み |
> | ~~#5「納品成立の契機」~~ | ✅ **裁定済み(2026-08-22)=(A) サークル内3秒滞在**。§2-8 は本文どおり実装してよい |
>
> **残る19件は実装を止めない**(本文の現行仕様のまま作り、裁定が出たら値/見せ方を差し替える)。
> ※3巡目までは §2-0 の表が「ブロッカー2つ」なのに §2-15 の注記が #16「囲いの発火6条件のゲート」しか書いておらず、
> **どこまで止まるのかも書いていなかった**。上の表が正(§2-0 と同じ内容)。

**#1 レスキューの囲いの中身(敵の数・種別・配置)**
- **①問題**: 「赤色の槍と、数体の敵台本」の**「数体」の中身が未指定**。数・種別・配置が決まっていない。
- **②選べる案**: (A) 赤い削岩型1体 + ゾンビ系4体(既存ミニボス囲い `ARENA_BOSS_ADDS`=4 と同数) /
  (B) もっと少なく2〜3体(4分時点の難度を抑える) / (C) もっと多く6体以上(見せ場として重くする)。
- **③推薦**: **(A) 赤い削岩型1体 + ゾンビ系4体**。サークル半径は `ARENA_EVENT_RADIUS`=240 を流用。
  配置は既存の囲いと同じ「円内・プレイヤーを避ける」。

**#2 赤い削岩型を S1/S3 に出してよいか / 赤い本体と赤い予告帯の可読性**
- **①問題**: (a) 削岩型は現在 **stage-4 以降**の敵として設計されている(`DRILLER_ALLOWED_STAGES`)。
  S1のレスキューに出すと**削岩型の初お目見えがステージ1**になる。加えて赤個体(`colorTier:'red'`)は
  HP/攻撃の色倍率が乗るため、S1では**難度が跳ねる**可能性がある。
  (b) 赤ティアは**本体スプライトを `0xff5544` で塗る**ので、CLAUDE.md の色の文法
  (「赤=カウンター/回避の対象・見たまんまが当たり判定」)に対して、**真っ赤な本体から赤い予告帯が出ると
  どこまでが判定かの一発読みが濁る**(=「赤いのに当たらない」領域が画面上に大量に出る)。
- **②選べる案**:
  (A) そのまま出す(色倍率も乗せる)。
  (B) 出すが**この個体だけ色倍率を乗せず、見た目の赤だけ使う**。
      ※事実として併記: **`spawnEnemyAtWithTier` は色と倍率が不可分**(`buildEnemy` に tier を渡す形)なので、
      この案は**新しい湧き経路が要る**(既存関数のままでは実現できない)。
  (C) S1/S3では**HPを固定値に据え置く**(色は赤のまま・強さだけ抑える)。
  (D) **予告帯の色/太さを個体色と差別化する**(例: 赤い予告帯に白縁を足す)。※(A)〜(C)と併用可。
- **③推薦**: **(A)+(D)**。社長指示どおり赤い削岩型を出し、**予告帯に白縁を足して「判定はこの帯だ」を
  読めるようにする**(難度が跳ねすぎたら (C) を後から足す)。
  ※事実として併記: §9 では削岩型は「ステージ4以降の新型雑魚」として設計されている。

> **#3 は欠番**(「囲いの最中にワクチン復活した時の扱い」=**既存挙動を1つも変えない**という選択だったため、
> 設計者が決めて **§2-4 の本文へ降ろした**)。**番号は詰めない**(本文からの参照を壊さないため)。

**#4 レスキュー出現(4:00)の告知と誘導 —— ★決めるのは「見せ方」だけ**
- **①問題**: **見失っても勝てなくならないこと**は必須要件なので **§2-9 の本文で確定済み**
  (未完了の間は必ず位置を提示し続ける)。**未決なのはその見せ方**——バナーの文言・アテンションを1回入れるか・
  提示の形(画面外の縁矢印か、画面内マーカーか)。
  ※§2-3 で「未完了の間は地平線フェードの対象外」を確定したので、**「画面内なのに透明」の死角は本文側で
  既に閉じている**。残るのは「画面外に出た時にどう指すか」と告知の作法。
- **②選べる案**(どれを選んでも「位置提示を必ず行う」は満たすこと):
  (A) 出現バナー(既存の囲い/レスキューと同じ枠。文言「救難信号受信」等)+**アテンション1回**(カメラ寄せ)+
      未完了の間ずっと、**画面外の時だけ縁矢印**(=既存の作法どおり)。
  (B) (A) から**アテンションを省く**(バナー+縁矢印だけ)。演出の割り込みを増やさない。
  (C) (A) に加えて、未完了の間は**画面内でもマーカーを出す**(常時表示)。
      ※事実として併記: **常時表示の画面内マーカーはこのゲームにまだ存在しない**(城ボスも商人も
      「画面外の時だけ」の縁矢印)=**新規UI部品を1つ作ることになる**(#9「納品先の誘導マーク」と同じ論点)。
- **③推薦**: **(A)**。既存の作法(バナー+縁矢印)で必須要件を満たせ、新規UI部品も増えない。
  アテンションは「4:00に何かが起きた」を1回だけ伝える役に立つ。
  ※事実として併記: v1では「二人組にattentionはしない」(裁定#7)と裁定されている。当時は「開始時から
  その辺に立っている受注NPC」だったため。**今は勝利の必須経路なので、推薦は誘導を付ける側**。
  ※**「誘導を足さない」は選択肢に置いていない**(選ぶと勝利不能な設計が確定するため)。

**#5 納品の3秒滞在と、既存の「帰還しますか?」確認ダイアログの関係**
- **★社長裁定 2026-08-22: 「サークル3秒で。」= (A) サークル内3秒滞在で納品成立。確定。**
  **ブロッカー解除**——§2-8 の「納品成立の契機」と「素通り防止」は、この裁定のとおり実装してよい。
- **①問題**: 現行の城ボス帰還は**離指→確認ダイアログ→はい**(`requestStoryReturnPrompt` /
  `answerStoryReturnPrompt`)。二人組クエスト時は「3秒滞在→納品成立→完了会話→リザルト」なので、
  **確認ダイアログを通すか通さないか**が未指定。
- **②選べる案**: (A) 二人組クエスト進行中は確認ダイアログを出さない(3秒滞在が帰る意思の確認を兼ねる) /
  (B) 従来どおり確認ダイアログを出し、「はい」で納品を始める / (C) 納品の後にもう一度確認を出す。
- **③推薦**: **(A)**。二重確認は社長指示の「追加の操作は要らない」に反するため。
  対象外ステージ・クリア済みランは**従来どおり確認を出す**(変更なし)。
- **★止まる範囲**: **§2-8 の「納品成立の契機」と「素通り防止(`requestStoryReturnPrompt`)」だけ**。
  納品ロックの中身(移動/攻撃/被弾/割り込みポーズ)・ワープの2段・`delivering` の円の扱い・
  §2-10 の永続記録は **(A)(B) どちらでも同じ**なので**先に実装してよい**。

**#6 レスキュー地点に「まだ行っていない」まま城ボス時刻を過ぎた時の、二人組側の見え方**
- **①問題**: 二人組はサークルの中で襲われ続けている(囲いは未発火)状態が延々続く。**その間の見た目**
  (敵に囲まれている絵を出すのか、二人が立っているだけなのか)が未指定。
- **②選べる案**: (A) 囲いの敵は進入まで湧かせない(=二人が立っているだけ) /
  (B) 少数の敵を先に置いて「襲われている絵」を作る / (C) 敵は置かないが、二人に怯えモーション等を足す。
- **③推薦**: **(A)**。理由=遠くの敵を常時走らせるのは負荷と事故(勝手に殺される/二人が死ぬ)の元。
  危機感は §2-13 の会話と誘導(#4「レスキュー出現の告知と誘導」)で伝える。

**#7 報酬の中身 —— 現行の 100G を撤去するのか据え置くのか**
- **①問題**: §2-8 は「当面は報酬なし、付与口だけ残す」と書いてあるが、**現行は
  `EVENT_QUEST_REWARD_GOLD` = 100 が実際に加算され、`+100G` のコールアウトも出ている**。
  社長の言葉は「**後で考えるわ。スキルかサブウェポンにするかも?**」であって「**今の100Gを消せ**」ではない。
  このまま実装すると**納品しても何も貰えない=体験の後退**になる。
- **②選べる案**: (A) **裁定が出るまで既存の100Gを据え置き**、後で差し替える /
  (B) 設計書どおり当面は報酬なし(付与口だけ) / (C) 今この場で新しい報酬(スキル/サブウェポン)を決める。
- **③推薦**: **(A) 100Gを据え置き**。裁定待ちの間だけ既存の体験を落とさないため。
  ※事実として併記: **v0.25.1686 で「報酬=ゴールド100(強制/サブ各)」と裁定されている**。

**#8 「城ボス倒した時のゴール表示は2人クエストの時は表示しない」への回答**
- **①問題**: 社長のこの指示に対し、v2は「納品場所を帰還サークルに統合したので**1つだけ出す**」という設計に
  なっている(=ゴール表示は**出る**)。§2-8 の「二重に作らない」は "2つ出さない" の話であって、
  "出さない" という指示には答えていない。バナー「**帰還サークル出現**」もそのまま出る。
- **②選べる案**:
  (A) **読み替える**: 納品場所=帰還サークルに統合した結果「同じ1つ」になったので、"出さない" ではなく
      **"1つだけ出す"** と解釈する。バナー文言は納品向けに差し替える(例:「**サンプルを届けろ**」)。
  (B) 指示どおり**ゴール表示(サークルの絵・バナー)を出さない**。二人組だけが城跡に現れ、
      そこへ行くと納品になる。
  (C) サークルは出すがバナーだけ出さない。
- **③推薦**: **(A)(バナー文言は納品向けへ差し替え)**。サークルの絵が無いと納品場所が分からなくなるため。
  **読み替えの是非は社長に確認する**(「出すなと言ったものが出ている」と受け取られうる箇所)。

**#9 納品先の誘導マーク —— 「画面外の時だけ」では実質ほぼ出ない**
- **①問題**: §2-9 は「帰還サークルの縁矢印がそのまま担う」で完結させているが、**実装は画面外の時だけ描く**。
  帰還サークルは**城ボスを倒したその場**に出るので、撃破直後はほぼ確実に画面内=**矢印は出ない**。
  社長の「納品の時、2人への誘導マークは表示してあげてね(城ボスとか商人と同じやつね)」が
  画面上では満たされない場面が多い。
  ※事実として併記: **「城ボスとか商人と同じやつ」も画面外の時だけ**の縁矢印であり、
  **常時表示の画面内マーカーはこのゲームにまだ存在しない**(新規に作ることになる)。
- **②選べる案**: (A) **納品待ちの間だけ、帰還サークルに画面内マーカーを新設**(常時表示の小さな印) /
  (B) 既存の縁矢印だけで足りるとする(実装ゼロ) / (C) 納品待ちの間だけ、画面内でも縁矢印を出す。
- **③推薦**: **(A)**。「マークを出せと言ったのに出ていない」を避けるため。
  ただし**新規のUI部品を1つ作ることになる**ので、(B) で足りるという判断も社長に選んでほしい。

**#10 囲いに入る前の警告表示の要否**
- **①問題**: §2-4 のとおり**サークルに触れた時点で戦闘確定・辞退不可**(円から出られない)。
  瀕死のまま偶然サークルに触れると、赤い削岩型と240pxの円に閉じ込められて逃げられない。
  **入る前に警告を出すか**が未指定。
- **②選べる案**: (A) 警告を出さない(既存の囲いと同じ=触れたら始まる) /
  (B) 円の縁に近づいた時だけ短い警告(「戦闘開始 —— 引き返せない」等)を1回出す /
  (C) 円の見た目自体を危険色にして、警告文は出さない。
- **③推薦**: **(B)**。他の囲いは「向こうから来る」のに対し、**これは自分から踏み込む**うえ
  **勝利の必須経路**なので、瀕死で偶然入って詰むのは避けたいため。

**#11 ★S1の初対面の台詞が消える(物語の仕様に触れるので設計チャットでは決めない)**
- **①問題**: v1では S1 は **「M1初遭遇 → 後で任意サブを受注」の2段**だった。初遭遇の原稿
  `EVENT_QUEST_LINES_FORCED`(実在・確定稿・`storyCanon.test.ts` が全文を固定)は
  ミラ「この国の人？　助かったよ！」/ グレン「俺はグレン。こいつはミラだ。礼を言う。では……」…の**5行**で、
  **ここで初めて名乗る**。
  v2は S1 に **受注原稿①(§2-13)** を割り当てているが、①の1行目は **グレン「またお前か。」**。
  → **初対面でいきなり「またお前か」と言う**。しかも `EVENT_QUEST_LINES_FORCED` は
  **黙って未使用になる**(=確定稿を1本捨てる)。**これは物語の仕様変更**なので設計チャットでは決めない。
- **②選べる案**:
  (A) **S1だけ「M1初遭遇の5行 → ①受注の3行」を連結して流す**(計8行=約26秒)。原稿はどちらも生きる。
  (B) 支給原稿①をそのまま使い、**M1初遭遇の5行(`EVENT_QUEST_LINES_FORCED`)は廃止**する。
  (C) ①の**1行目だけ差し替える**(「またお前か。」→初対面用の1行)。②③(S3/S4)の「また会ったな」
      「よく会うな」は**S1で会っている前提**なので、(B)を選ぶと辻褄が合わなくなる点に注意。
- **③推薦**: **(A)**。確定稿を捨てず、S3/S4の「また会ったな」「よく会うな」の前提も保てるため。
  8行=約26秒が長いなら、初遭遇分を先に流し、①は間を置いてから続ける形でもよい。
- **あわせて確認**: `EVENT_QUEST_ENCOUNTER_LINES`(M5・遭遇のみのミラ2行)は
  **v2では呼ばれなくなる=廃止**になる(§2-14 の `encounterOnly` 撤去)。これも確定稿なので裁定に含める。

**#12 ★囲いを「拒否不能・不可逆」のまま勝利の必須経路にしてよいか**
- **①問題**: §2-4 のとおり**サークルに触れた時点で戦闘確定・辞退不可**(`confinesPlayer` 既定 true で
  240px の円から出られない)。かつ**この囲いをクリアしないと城ボスが出ない=勝利できない**。
  つまり **「瀕死でも触れたら確定する不可逆イベント」を、勝利の必須経路に置く**ことになる。
  既存の囲いは「向こうから来る」ものか、勝利に必須でないものだけだった。
- **②選べる案**:
  (A) **そのまま**(拒否不能・不可逆。警告の要否は #10「囲い進入前の警告表示」で決める)。
  (B) **`confinesPlayer: false`(=出られる囲い)にする**。逃げても円は残るので入り直せる。
      ※前例として `confinesPlayer:false` はゲート1で使われていた(その後 v0.25.1523 で
      「やはり出れない囲いに」と裁定され、現在は全部 true)。
  (C) 拘束は維持するが、**HPが一定以下の時は進入で発火しない**(回復してから来い、の形)。
- **③推薦**: **(A) そのまま + #10「囲い進入前の警告表示」の警告(B)**。囲いの緊張感を保ちつつ、事故だけを潰せるため。
- ※これは**ゲームの挙動・体験に触れる**ので、監査役と2人では決めない。

**#13 ★囲いの上限を 90秒 にしてよいか**
- **①問題**: v2初稿の「10分」は、`confinesPlayer` 既定 true の 240px 円に**10分間閉じ込められる**ことを
  意味しており、「無期限にしない」という趣旨を満たしていなかった。本文は**90秒**へ直してあるが、
  **これは体験に触れる数字**なので事実として裁定に上げる。
- **②選べる案**: (A) **90秒**(既存 horde=40秒 / boss=60秒 の帯の少し上。取り残し保険として余裕を見た値) /
  (B) **60秒**(既存のボス囲いと完全に同じ) / (C) **40秒**(既存の horde と完全に同じ)。
- **③推薦**: **(A) 90秒**。編成が「赤い削岩型1体+数体」で既存hordeより重く、
  取り残しの保険が働く前に正攻法で倒し切れる余地を残したいため。
  ※事実として併記: 実在値は `ARENA_HORDE_DURATION_MS`=40000 / `ARENA_BOSS_DURATION_MS`=60000。
- **★4巡目までに無かった材料(90秒の意味が変わる2点・事実)**:
  1. **90秒は「最悪ここまで閉じ込められる時間」ではなくなった。**
     §2-4 の後始末を入れたので、**囲いが外から畳まれた場合はその瞬間に完了扱い**になる
     (=90秒を待たない)。90秒が効くのは「敵が1体取り残されて `cleared` が立たない」場合だけ。
  2. **90秒の間、通常囲いの次回タイマーは進み続ける**(#16「囲いの発火6条件のゲート」の材料と同じ事実)。
     レスキュー発火時に押し戻す(§2-4 で確定)ので**この囲いの直後には開かない**が、
     **90秒が長いほど「レスキュー中に他の側イベントが待たされる」時間も伸びる**。
  - → **#16「囲いの発火6条件のゲート」と連動**: #16「囲いの発火6条件のゲート」で「阻止条件を外す(A)」が選ばれると、レスキューの囲いが他イベントと
    重なる状況が増えるので、**閉じ込め時間は短い方が安全側**に寄る((B) 60秒が相対的に良くなる)。
    **#16「囲いの発火6条件のゲート」と同時に裁定してもらうのが早い。**

**#14 ★`src/data/storyCanon.test.ts` の書き換え(憲法テストに触れる)**
- **①問題**: §2-14 で `encounterOnly` を撤去すると、`storyCanon.test.ts` の
  「M5は遭遇のみ(encounterOnly)・ミラ2行」の節(`EVENT_QUEST_CONFIG['stage-5'].encounterOnly` を
  `true` に固定+`EVENT_QUEST_ENCOUNTER_LINES` の全文固定)が**必ず落ちてCIが赤になる**。
  CLAUDE.md「憲法テストを守る。**違反が必要なら★未決事項で社長裁定を得てからテストごと変更する**」に該当。
- **②選べる案**:
  (A) **その節を削除**し、代わりに「**S5は `encounterOnly` を持たない**(=納品できる)」を固定する新しい節を書く。
  (B) 節は残し、`encounterOnly` の期待値だけ `undefined` へ書き換える(`EVENT_QUEST_ENCOUNTER_LINES` の
      全文固定は残す=原稿は台帳に残るが未使用になる)。
  (C) `encounterOnly` を撤去せず、S5だけ別経路で納品できるようにする(フラグは残る)。
- **③推薦**: **(A)**。フラグを消すなら「消えていること」を固定するのが同じ強さの不変条件になるため。
  ※#11「S1初対面の台詞が消える」の裁定と連動する(`EVENT_QUEST_ENCOUNTER_LINES` を廃止するなら固定も消す)。

**#15 デンジャーゾーンの中に囲いが出てしまった時の難度の扱い(S1/S3/S4)**
- **①問題**: §2-3 で「出現点は原点から3000px未満」を確定したが、**空き地探索を振り切っても
  候補が1つも取れない場合**(プレイヤーが深部を走り続けている等)は、本文どおり
  **上限を諦めてプレイヤー近傍に出す**(**出さないと勝利不能になるため、出ないことは選べない**)。
  残る判断は **その時の難度をどう扱うか**。
  ※旧稿は「(=プレイヤー自身が3000px以遠に居る)」と等号で結んでいたが**誤り**だったので直した(§2-3)。
  ※**S5はこの条件が毎回ほぼ破れる**(#17「S5の出現点の規則」の別問題として切り出した)。この #15「デンジャーゾーン内に囲いが出た時の難度」は S1/S3/S4 の話。
- **②選べる案**: (A) **何もしない**(デンジャーゾーンの敵密度のまま囲いをやる) /
  (B) その場合だけ**編成を軽くする**(削岩型の色を落とす等) /
  (C) その場合だけ**囲いの円を大きくする**(逃げ場を作る)。
- **③推薦**: **(A) 何もしない**。深入りしたぶん重くなるのは既存の設計思想と一致し、
  例外分岐を1つも増やさないため。**難しすぎると実機で分かったら (B) を後から足す。**

**#16 ✅ 裁定済み(2026-08-30)** ★★囲いの発火6条件ANDが「原因の見えない勝利不能」へ倒れる

- **★社長裁定 2026-08-30**: 「**裏ボス中だけ非表示で。／ あとは発火する ／ ただし他のイベントの範囲とは
  重ならない様に注意**」= **6条件AND は撤去**し、**裏ボスだけ「非表示」**で扱い、衝突は**出現位置**で避ける。
  推薦だった (A)(賞金首と裏ボスを外す)より踏み込んだ形で、**「入っても何も起きない」を構造ごと消す**方向。
  本文への反映先は **§2-4(発火ゲート)/ §2-3(出現位置の確定②-b と諦める順序)/ §2-2B(`hidden` と遷移6・7)**。
  **以下は裁定前の検討の記録**(経緯が追えるように残す。結論として読まないこと)。

- **①問題**: §2-4 の6条件(`activeEvent` / `bossChasing` / 裏ボス存命 / ハンター追跡 / 紅き夜 / 賞金首存命)は、
  元は **「側イベントを重ねない」ためのペーシング都合**の条件で、**詰み防止のために設計されたものではない**。
  v2はそれを**勝利の必須経路(囲いの発火)の前提**へ流用している。条件が塞がっている間は
  **円に入っても何も起きず、拒否の合図も出ない**。プレイヤーに見えるのは
  「4:00に円が出た → 入った → 何も起きない → 城ボスがいつまでも出ない」だけで、**原因が画面上に一切無い**。
  - **事実(実在確認・思い込みで書かない)**: 6条件はいずれも**基本的には自己解消する**
    (紅き夜=20秒 / 賞金首=dormant のまま60秒+フェード / 裏ボス=帰巣で `despawn` / ハンター=立ち去り)。
    「賞金首が倒すまで居座る」わけではない。
  - **ただし1ケースだけ無限になる**: 賞金首の退場タイマーは **`dormant` の間しか進まない**。
    `dist <= aggroRange`(既定380)で起き、交戦中は毎フレームリセットされる。
    → **レスキューの円が賞金首の索敵圏に重なって出た場合、プレイヤーが円の中で待つ限り
    賞金首は起き続け、円は二度と発火しない。** 円は「進行方向500〜1000px先」に出るので普通に起こりうる。
  - **もう1つの実害は待ち時間そのもの**: 最悪ケースでは複数の条件が数珠つなぎになり、
    **合図が無いまま数十秒〜1分以上「入っても何も起きない」**が続く。
  - **★材料①: 阻止は「一度待てば解ける」ではなく、周期的に再発する(実在確認・★6巡目に訂正)。**
    囲いの次回時刻(`nextArenaAtRef` / `boredomArenaNextEligibleAtRef`)は**発火した時にだけ先送りされる**
    ので、塞がっている間もタイマーは進む。しかも**どちらも円の中心=プレイヤーの中心**。
    → **条件が空いた瞬間、プレイヤーの足元(=レスキューの円の中)で囲いが開き、
    その40〜60秒ぶん条件1(`activeEvent === null`)がまた塞がる。** これがランのあいだ繰り返される。
    - **★訂正(5巡目までの記述は不正確だった)**: 開くのは **「2分タイマーの囲い」ではなく
      「退屈補正の囲い」**。実在の並びは `… else if (puzzleActiveNow) { 退屈補正 } else { 2分タイマー }` で、
      **通常プレイ(`puzzleActiveNow=true`)の間、2分タイマーの枝には一度も到達しない**
      (=`nextArenaAtRef` は通常プレイでは効いていない)。**結論(周期的に再発する・円の中で開く)は
      変わらない**——退屈補正の囲いも中心はプレイヤーの中心。
    - (§2-4 で「レスキュー発火時に囲いのタイマーを押し戻す」を確定したが、**それは発火した後の話**で、
      **発火する前に塞がれ続ける問題には効かない**。)
    - **★この材料は ★未決 #21「発火枝のゲートの置き場所」の裁定に依存する**: 発火ゲートを**枝の中**に置く(本文の確定)なら、
      レスキューの円に入っている間は**後続の `else if` へ落ちない**ので**退屈補正の囲いも開かない**
      =**この再発は起きない**。**条件側**に置くと落ちるので**起きる**。**#21「発火枝のゲートの置き場所」を先に決める。**
  - **★4巡目に無かった材料②: 発火した後も、外から畳まれる経路が実在する**(§2-4「★★立っている自分の
    囲いを、ゲート囲いが外から畳む」)。**①のプリエンプション保護と②の後始末を入れる前提**で
    この6条件を読むこと。**後始末(=`activeEvent` が消えたのに未打刻なら強制クリア)を入れておけば、
    #16「囲いの発火6条件のゲート」がどちらに裁定されても「原因の見えない勝利不能」は後段で1回止まる**
    (ただし**円に入っても発火しない**という体験そのものは残るので、#16「囲いの発火6条件のゲート」の裁定は依然必要)。
- **②選べる案**:
  (A) **賞金首と裏ボスを発火の阻止条件から外す**(残るのは `activeEvent` / `bossChasing` / ハンター / 紅き夜)。
      どちらも `activeEvent` **ではない**ので、`endArenaEvent` の `fromEvent` 全消しで
      相手のイベント敵が消える事故は起きない(上書き事故の心配は `activeEvent` の1枠だけ)。
      ※事実として併記: 裏ボス側には「**囲い系イベント中は裏ボスを出さない(重なると逃走で詰み=
      終わらない・社長報告)**」という既往事故のコメントが実在する。**外すのは「囲い→裏ボス」ではなく
      「裏ボス→囲い」の向きだけ**なので、その事故の再発とは別の向きだが、**重なる状況は増える**。
  (B) **阻止は6条件のまま残し、逃げ道を必須にする**: ①発火しなかった時に
      **拒否のフィードバック**(バナー「交戦中は救援に入れない」等)を出す
      ②**ブロックが一定時間続いたら強制発火**する(上限を設けて必ず前に進める)。
  (C) **そのまま**(何もしない)。
- **③推薦**: **(A)**。理由 = ①この6条件はペーシング都合であって詰み防止ではない
  ②`activeEvent` の1枠さえ守れば「相手の終了処理が二重に走る/相手のイベント敵が消える」という
  **実害のある事故は起きない** ③(B) は「拒否の合図」と「強制発火の上限」という**新しい仕組みを2つ増やす**のに、
  結局は待たせる。**ただし②の懸念(裏ボスと囲いが重なる状況が増える)は残る**ので、
  (A) を採る場合は **裏ボスだけ阻止条件に残す (A′)** も選べる形にしておく。
- ※これは**ゲームの挙動・体験に触れる**ので、監査役と2人では決めない。
  **★止まるのは §2-4 の「発火ゲート(6条件)」の1箇所だけ**(§2-0 / §2-15 冒頭の表)。
  §2-4 の残り(編成・発火時の順序・`hordeSpawnRef` 潰し・id セットでの完了判定・90秒・時間切れの
  専用枝・プリエンプション保護・後始末)と §2-3 / §2-5〜§2-11 は**先に実装してよい**。

**#17 ★S5の出現点の規則(「原点3000未満」がS5では毎回破れる)**
- **①問題**: §2-3 は出現点に「原点からの距離が `AREA_THRESHOLDS[1]`=3000 未満」の上限を課している。
  だが **S5のレスキューは「拠点2か所確保」の後**に出る(§2-11)。
  **事実(実在確認)**: 拠点は `BASE_SITE_RADIUS`=**3200** の輪の上に置かれ、確保の在内判定は
  `BASE_CAPTURE_RADIUS`=**130**。→ **2か所目を確保した瞬間、プレイヤーは必ず原点から 3070〜3330px に居る
  =常にデンジャーゾーン(3000)の外**。
  → S5では上限を満たす候補が**進行方向が原点側を向いていた時しか**取れず、
  **ほぼ毎回フォールバック(上限を諦める)に倒れる**。
  **受け入れ条件「出現点の原点からの距離が3000未満」がS5では常に免除される=検証できない。**
- **②選べる案**:
  (A) **S5だけ、原点3000未満の制約を最初から課さない。代わりに「確保した拠点の近傍」に出す**
      (拠点=安全地帯が担保されている場所。例外分岐が1本で済む)。
  (B) **S5もS1/S3/S4と同じ規則のまま**(=毎回フォールバックする。上限は「たまたま通れば効く」扱い)。
  (C) **S5だけ上限を拠点の輪に合わせて緩める**(例: 原点から `BASE_SITE_RADIUS`+α 未満)。
- **③推薦**: **(A)**。理由 = ①S5のレスキューは「拠点を2つ取った直後」に起きる出来事なので、
  **物語としても取った拠点のそばに出るのが自然** ②例外分岐が「S5はこう」の1本で済み、
  (C) のような**新しいしきい値を発明しない**。
- **★推薦(A)の弱点も事実として併記する(4巡目は「安全地帯だから難度の担保になる」とだけ書いていた)**:
  1. **拠点は原点3200の輪の上=`AREA_THRESHOLDS[1]`(3000)より常に外**。つまり
     **「拠点の近傍」に出す=デンジャーゾーンの中に出す**ことを毎回確定させる、という意味になる。
     (A) は「上限を守る」案ではなく、**上限を最初から諦めて置き場所だけ決める**案。
  2. **拠点は失陥する**(§2-11・`status: 'captured' → 'open'`)。**確保直後は安全でも、
     レスキュー中〜城ボスまでの間に安全でなくなりうる**ので、「安全地帯だから難度の担保になる」は
     **成立しない**(この理由は取り下げた)。
  3. **★(A) は §2-11 のラッチと噛み合っていない。** レスキューの出現条件は
     「**これまでに2か所以上確保したことがあるか**」の**単調なラッチ**(`basesEverCaptured >= 2`)なので、
     **出現の瞬間に拠点が実際に確保されている保証は無い**——ラッチ済みのまま2か所とも失陥して
     `status: 'open'` に戻っていることがありうる。その時 **「確保した拠点の近傍」という置き場所は
     "敵に取り返された拠点の真上" になる**。
     - → (A) を採る場合、**「どの拠点の近傍か」の決め方も一緒に決める必要がある**。
       選べる形: (A-1) **ラッチした時点の2か所を覚えておき、その座標を使う**(以後の失陥は無視。
       ラッチと同じ「一度成立したら動かない」思想で揃う=**推薦**) /
       (A-2) **出現の瞬間に `captured` の拠点だけを候補にし、1つも無ければ S1/S3/S4 と同じ規則へ落とす**
       (置き場所は最新だが、**例外分岐が2本**になる)。
  - → **(A) の実質的な利点は「置き場所が説明できること」と「例外が1本で済むこと」**であって、
    難度の担保ではない。難度をどうするかは #15「デンジャーゾーン内に囲いが出た時の難度」と同じ扱い(実機で重ければ後から絞る)。
    ※(A-2) を選ぶと「例外が1本で済む」という利点も薄れる(**(A-1) を推薦する理由**)。
- ※これは**難度と体験**に触れるので社長裁定。本文の現行仕様=S1/S3/S4と同じ規則、は据え置く。

**#18 ★S5にサブ任務カードを出すか(憲法テストに触れる・#14「storyCanonテストの書き換え」と連動)**
- **①問題**: v2ではS5も **S1/S3/S4 とまったく同じクエスト**(レスキュー→城ボス→納品)を回す。
  だが **事実(実在確認)**: `src/data/campaign.ts` の `SUB_RESCUE_MISSION`(「身元不明民間人の救助」)は
  **stage-1 / stage-3 / stage-4 の `subs` にしか入っていない**。stage-5 は `subs: []`。
  MissionSelect のサブ進捗は `stage.subs.length > 0` を前提にしているので、
  **S5だけカードも n/N も CLEAR バッジも出ない**(納品しても一覧に何も残らない)。
  さらに `src/data/storyCanon.test.ts` が **`expect(st('stage-5').subs).toEqual([])`** を固定しているので、
  **カードを足すと憲法テストが落ちてCIが赤くなる**(CLAUDE.md「違反が必要なら★未決で社長裁定を得てから
  テストごと変更する」に該当)。
- **②選べる案**:
  (A) **S5にも `SUB_RESCUE_MISSION` を足す**(4本とも同じカードが並ぶ)。`storyCanon.test.ts` の
      `st('stage-5').subs` の固定を**S5もカードを持つ**側へ書き換える。
  (B) **足さない**(S5だけ一覧に出ない。ゲーム中は同じことをするが、記録上は「サブ任務ではない」)。
      テストは1バイトも触らない。
  (C) **S5専用の別カード**を新設する(文面を分ける)。
- **③推薦**: **(A)**。理由 = **プレイヤーがやることが4ステージとも同一**なのに、S5だけ達成が
  一覧に残らないのは「やったのに記録されない」に見えるため。
  ※事実として併記: **薬の付与条件は S1・S3・S4 の3本のまま**(§2-10)で、S5を足しても**解放条件の数は増えない**
  (`SUB_QUEST_STAGE_IDS` は書き換えない)。カードが並ぶのと解放条件は別物。
  ※#11「S1初対面の台詞が消える」(`EVENT_QUEST_ENCOUNTER_LINES` の廃止)と #14「storyCanonテストの書き換え」(`encounterOnly` の固定削除)と**同じテストファイル**を
  触るので、**3件まとめて裁定してもらうのが早い**。

**#19 納品ロック中に宝箱を拾った時の「装備選択1回」を失ってよいか(★ブロッカーではない)**
- **①問題**: 納品ロック中は `isPaused` を立てられない(立てると会話が止まって**永久フリーズ**・§2-8)。
  ところが **事実(実在確認)**: `collectPickup` の `case 'chest'`(ボスドロップの宝箱)は
  **`showUpgradeMenu: true, isPaused: true` を立てる**うえ、**入力が要らない**(磁力で拾えば起きる)。
  城ボスのドロップが磁力圏内にあると、**納品後の7〜10秒の間に開きうる**。
  レベルアップ(#1「レスキュー囲いの中身」の①)は `levelUpIntroUntil` が消えないので**保留できる**が、
  **宝箱には保留する受け皿が無い**(その場で `upgradeOptions` を作って開く形)。
  → 開かない=**その1回の選択が失われる**。
- **②選べる案**:
  (A) **開かない(失う)**。納品ロック中は `showUpgradeMenu` / `isPaused` を立てない。
  (B) **保留箱を作る**(「開くべきメニューが1件ある」を store に持たせ、ロック解除後に開く)。
      ※ただし解除後はすぐ `gameWon` へ行くので、**結局リザルト遷移と競合する**
      (=新しい仕組みを1つ増やして、実際に開く保証は得られない)。
  (C) **納品ロック中はピックアップの回収自体を止める**(足元に残す)。
      ※ロック中はプレイヤーが動けず、解除後はリザルトなので**結局拾えない**=(A)と同じ結果になる。
- **③推薦**: **(A)**。理由 = ①納品後は**そのままリザルトへ行く**ので、そこで選ぶ装備が効くのは
  「持ち帰り(`takeHomeEquipment`)」だけで、**得られるものが小さい** ②(B)(C)は**新しい仕組みを増やして
  結果は変わらない** ③会話が止まって `gameWon` に到達しない事故の方が桁違いに重い。
- ※**ブロッカーではない**(本文の現行仕様=(A)で実装してよい。裁定で (B) になったらその時に足す)。

**#20 ★レスキュー完了の直後に、ゲート囲いがプレイヤーの足元で開く(城ボスの出現演出に重なる)**
- **①問題**: §2-4 のプリエンプション保護①は、ゲート発火待ちの間**ゲートを「待たせる」**形にした。
  **事実(実在確認)**: ゲートの発火予約は**時刻ではなく pending 駆動**
  (`gate1PendingRef` / `gate2PendingRef` が立っている限り、`shouldTriggerGate1/2` は
  `activeEvent` が空いた**次のフレーム**に true になる)。**押し戻す先が無い**ので、
  **レスキューの囲いが終わった次フレームに必ず発火する**。
  → **受注会話 → 3.0秒後に城ボス出現 → その950ms後にアテンション(カメラ寄せ+カットイン)**、という
  並びの**真上に、半径240の拘束囲い(`confinesPlayer` 既定 true)が開く**。
  しかも **事実(実在確認): 城ボスの出現ブロックは `activeEvent` を見ていない**ので、
  **囲いに拘束されたままカメラ寄せ+カットインが走る**。
  ※§2-4 の「レスキュー発火時に通常囲いのタイマーを押し戻す」は**通常囲い/退屈補正にしか効かない**
  (ゲートは時刻で待っていないため)。
- **②選べる案**:
  (A) **レスキュー完了から城ボス出現までは、ゲート囲いを発火させない**(=`rescueClearedAt > 0` かつ
      城ボス未出現の間は `gateFireOk` を落とす)。ゲートの予約(`gate*PendingRef`)は消さないので、
      **城ボスが出た後に発火する**(=ゲートが消えるわけではない)。
  (B) **城ボスの出現条件に `!activeEvent` を足す**(囲いが終わってから出す)。
      ※これは**既存の城ボス出現ブロックの条件を1つ増やす**ことになり、
      **クエスト外のラン(退屈補正の囲い中など)でも城ボスの出現が遅れる**=影響範囲が広い。
  (C) **そのまま**(囲いの中でカットインが走る)。
- **③推薦**: **(A)**。理由 = ①効く範囲が「レスキュー完了〜城ボス出現」の数秒に限定され、
  **クエスト外のランに1バイトも影響しない** ②ゲートは予約が残るので**失われない**
  ③(B) は**全ランの城ボス出現タイミングに触る**ので、この案件の外へ影響が漏れる。
- ※**ゲートの発火タイミング(体験)に触る**ので設計チャットでは決めない。
  **ブロッカーではない**(裁定が出るまでは本文=保護①のまま実装してよい。事故は「演出が重なる」であって
  勝利不能ではない)。

**#21 ★発火枝のゲートを「条件側」に置くか「枝の中」に置くか(#16 より先に決める)**
- **①問題**: §2-4 は発火を既存の if/else 連鎖へ
  `else if (レスキューの円に入った && §2-4の発火ゲート) { … }` の形で入れる、と書きつつ、
  同じ節で「**枝の中は `if (!発火ゲート) {}` で抜けてよい**」とも書いていた。**この2つは挙動が違う**:
  - **ゲートを条件側に置く**: 6条件が塞がっているフレームは**次の `else if` へ落ちる**
    → **退屈補正の囲いが、レスキューの円の中(=プレイヤーの足元)で開く**。
  - **ゲートを枝の中に置く**: 落ちない → その間は**他の囲いも開かない**。
  → **★未決 #16「囲いの発火6条件のゲート」の材料②「周期的に再発する」は、前者でしか成立しない。**
    どちらに決めるかで **#16「囲いの発火6条件のゲート」に渡している事実そのものが変わる**ので、**#16「囲いの発火6条件のゲート」より先に決める**。
- **②選べる案**:
  (A) **枝の中に置く(落とさない)**=レスキューの円に入っている間は、他の囲いも開かない。
  (B) **条件側に置く(落とす)**=既存の抽選の落ち方をそのまま保つ(代わりに退屈囲いが円の中で開く)。
- **③推薦**: **(A) 枝の中に置く**。理由 = ①**「入っているのに何も起きず、代わりに別の囲いに
  閉じ込められる」が一番わかりにくい** ②#16「囲いの発火6条件のゲート」の実害(周期的な再発)が**そもそも起きなくなる**
  ③レスキューの円に立っている時間は長くない(入ったら発火するのが正常系)。
  - ※**本文は (A) で確定済み**(§2-4)なので**実装は止まらない**。(B) が選ばれた時に直すのは
    §2-4 の1箇所と #16「囲いの発火6条件のゲート」の材料②。
- ※これは**既存の囲いの抽選の落ち方**に触るので、監査役と2人では決めない。

**#22 ★スケーター乗車中に納品が成立した時(受け入れ条件が2つ破れる)**
- **①問題**: §2-8 は納品ロックを「`movePlayer` の中で `tx = ty = 0` にする」で作り、
  受け入れ条件6/6a-1 に「サークルを出られない」「その場で走行アニメが回り続けない」を置いている。
  **事実(実在確認)**: **スケーター乗車中は移動速度が ×3、慣性の時定数が Lv別に 1.2 / 0.8 / 0.5 秒**
  (`inertiaTau = skaterRiding ? [PLAYER_INERTIA_TAU, 1.2, 0.8, 0.5][skLv] : PLAYER_INERTIA_TAU`)。
  → **目標速度を0にしても、Lv1なら約1.2秒の時定数で滑り続ける**。
  帰還サークルの半径は **`RETURN_CIRCLE_RADIUS`=95** しかないので、
  **納品成立の直後に円から普通に出る**(=受け入れ条件6が破れる)。
  出た後もロック中なので入力は効かず、**「勝手に滑って出ていったまま会話が終わる」**絵になる。
- **②選べる案**:
  (A) **納品成立の瞬間に降車させる**(`dismountSkater` を呼ぶ)。
      ※事実として併記: `dismountSkater` は**1秒以上乗っていればスケボーを投擲する**
      (前方バッシュ=衝撃波+強制ノックバック)。**納品の場面で武器が飛ぶ**ことになるので、
      投擲を伴わない降車が要る(=**既存関数をそのまま呼べない**)。
  (B) **急停止の枝を使う**(`skaterStopUntil` を納品成立の瞬間に立てる)。
      ※事実として併記: この枝(`skaterStopping`: tau=50ms で残速度を減衰)は**実在するが、
      現在この値をセットしている箇所がコードベースに1つも無い**(初期化と `resetGame` の 0 だけ)=
      **使うのはこれが初**。乗車状態は続くので、絵は「乗ったまま急停止」になる。
  (C) **そのまま**(滑って円を出る)。受け入れ条件6/6a-1 を**スケーター乗車中だけ免除**する。
- **③推薦**: **(B) 急停止の枝を使う**。理由 = ①**実在する枝を初めて使うだけ**で、新しい停止処理を
  1つも書かない ②**tau=50ms=「ほんの少し慣性のある急停止」**なのでCLAUDE.md の慣性MUSTを満たす
  ③(A) は投擲の副作用を避けるために**降車関数を分岐させる**ことになり、スケーターの仕様に触る。
- **★あわせて: 受け入れ条件から「60ms」の断定は外した**(§2-8 の 6a-1)。時定数は乗り物で変わるため。
- ※**乗り物の挙動**に触るので設計チャットでは決めない。**ブロッカーではない**
  (本文=`tx=ty=0` のままで実装してよい。裁定で (B) になったら1行足す)。

---

## 2-16. ★v3(2026-08-30 着手前監査)の反映記録

**着手前の品質監査(Opus)で 18件**(A=12 / B=3 / C=3)。**うち5件は「08-30の裁定を本文にだけ入れて、
受け入れ条件・実装マップ・撤回表という『実装者が実際に読む3枚』を旧仕様のまま残していた」**もの。
以後、**裁定を反映する時はこの3枚を必ず一緒に直す**(この節はその教訓の記録でもある)。

### 直した(A・C)
| # | 指摘 | 直した場所 |
|---|---|---|
| A1 | 受け入れ条件9が「発火しない」のまま(裁定と正反対) | §2-4 受け入れ条件9 |
| A2 | 実装マップ/待機円の論拠/撤回表に6条件ゲートが生きていた | §2-14 / §2-3 / §2-12 |
| A3 | 発火枝が `if (!ae)` の中なので「入れば必ず発火」が成立せず、`kind:'rescue'` の手順が到達不能 | §2-4「★★発火のコードを置く場所」に**前置き1本**を確定 |
| A4 | 裏ボス非表示の窓に上限も合図も無く、§2-9 の必須要件と矛盾 | §2-9 の必須要件を「表示されている間」へ限定+**★未決 #23** を新設(推薦=(a)) |
| A5 | `rescue ⇄ hidden` の消え方・現れ方が未指定(慣性MUST) | §2-2B 遷移6/7 に既存の飛来・退場の型を指定 |
| A6 | 戦闘中(最大90秒 `rescue` のまま)にも非表示が成立しうる | 遷移6に「囲い未発火」を明記+既存ガードを事実併記 |
| A7 | `isHiddenBoss` は天使6体+idol+phillbossを含む=ゲート2戦でも消える | 述語を**裏ボス4体**に限定(専用述語1本) |
| A8 | `?castlenow=1` の確認ランが「帰る手段ゼロ」で詰む | §2-6 受け入れ条件3b(素通り防止の条件を納品経路限定へ+遷移3をエッジ→状態へ) |
| A9 | `gameWon` を納品フレームの1回しか評価しないと原稿ありステージが永久待機 | §2-14 の判定式を**毎フレーム評価**へ |
| A10 | 納品の実行主体が store とループの2箇所に割れていた | **`useGameLoop` の二人組ブロック1箇所**に確定 |
| A11 | `completeEventQuest` の分岐撤去で `syncQuestStageClear` の呼び出しごと消える | §2-10 に「呼び出し口は残す」を確定 |
| A12 | `warping` の投影影が実装マップの2行で正反対 | 「走査から外す」側へ統一(積み口に高さ引数が無いため) |
| C16 | `qGone` は3条件ではなく**4条件**(練習は既に除外済み) | §2-1 |
| C17 | `isInputLocked` は4項ではなく**5項**(`farBackdrop === 'ending'`) | §2-8 / §2-14 |
| C18 | `kind:'rescue'` の寿命は25秒ではなく**2分**(25秒はホールドゲージ) | §2-14 事実15 |

### 積んだ((B)・今回は直さない)
1. **「他イベントの範囲と重ならない」は出現の一瞬しか守っていない**(逆向きの回避が無い)。
   退屈補正/2分タイマーの囲いは**中心=プレイヤー中心**で開くので、円のそばに立てば重なる。
   緑卵の既存回避(`noEggCircles`)は `eventQuestNpc.radius`(64)しか見ず、新設の `triggerRadius`(240)を見ない。
   ※**ただし発火後は §2-4 の「枝に入ったフレームは後続の else if へ落ちない」で構造的に守られる**ので、
   残るのは**発火前に円のそばで別の囲いが開く**ケース。実機で気になったら緑卵側の半径を差し替える。
2. **出現位置判定に `facilitiesHidden` が混ざる**(`resolveBountyMove` の ctx)。ボス戦中は城/病院/武器庫/
   警察署の当たり判定が消えるので、4:00 がボス戦中に重なると建物の中が「立てる」と判定されうる。
3. **S5の拠点ラッチ(`basesEverCaptured`)だけ、置き場所・書き手・`resetGame` の行が未指定**
   (§2-14 の「このランの状態フィールド」4つに入っていない)。HUDが読む以上 store に要る。
