# 二人組(クエストNPC)クエスト設計 — 「特定変異種のサンプルが欲しい」/「とにかくサンプルを集めてきて」

社長指示(v0.25.1684直後のチャット)を設計に整理したもの。**会話内容は後で詰める(社長)=当面は仮テキスト。**
受領/納品のUI(サークル内3秒滞在=白メーター、受領後に一度出てから再滞在で納品)は v0.25.1684 実装済みの仕組みを使う。

## 確定事項(社長指示そのまま)

### 1) 強制クエスト「特定変異種のサンプルが欲しい」
- **クリアしないと城ボスが時間(7分)になっても出現しない。**
- 受けると、**二人がいるエリアと反対の研究エリア(=研究対象区域・距離1500-3000のリング)にネームドが出現**。討伐が条件。
- 対象ステージと内容(全ステージ共通で「反対側の研究対象区域にネームド→討伐」):
  | ステージ | 内容 |
  |---|---|
  | ステージ1 | 反対側の研究対象区域にネームド出現・討伐 |
  | ステージ3 | 同上 |
  | ステージ4 | 同上 |
  | ステージ5 | 同上 |

### 2) サブ扱い「とにかくサンプルを集めてきて」
- **強制クエストのクリア後に1度だけ受けられる。**
- 目標:
  | ステージ | 目標 |
  |---|---|
  | ステージ1 | 敵の討伐 10体 |
  | ステージ3 | レア敵の討伐 5体 |
  | ステージ4 | 紫敵の討伐 5体 |
  | ステージ5 | 赤敵の討伐 5体 |

### 3) 納品フロー(前回指示v0.25.1684・継続)
- 目標達成後、二人のサークルで再度3秒滞在=完了・報酬。
- 全クエスト完了後は「以後そのステージに二人は現れない」(そのプレイ中は立ち姿のまま・何も起きない)。

## 設計の解釈(コード事実に基づく・実装前提)
- **「研究エリア」= 既存区域名「研究対象区域」**(`AREA_ZONE_NAMES[1]`・原点距離1500-3000)。
  二人は距離460-950(軍備配置区域)に湧くので、「反対の研究対象区域」=二人の方位角+180°・距離は研究対象区域内。
- **ネームド=宿敵と同じ個体強化レシピを流用**(HP×2/与ダメ×2/サイズ×1.5・金tint・名前表示・上限カリング除外)。
  ただし宿敵システム(namedFoe・isNamed)とは**別フラグ(questTarget)**で持ち、宿敵の討伐処理/同時1体制約と混線させない。
- **城ボスゲート**: `useGameLoop`の城ボス湧き条件(7分・`castleBossReady`)に「強制クエスト納品済み(ステージ毎・永続)」をAND。
  納品した瞬間から(同run内でも)時間さえ満ちていれば出現する。
- **キル集計の合流点**: `damageEnemy`のkill分岐(銃/接触/爆発)と`grantMeleeKillRewards`(近接全経路)の2箇所でクエスト進捗を加算。
- **永続化**: v0.25.1684の`zombie.progress.eventQuestDone`を**段階制に拡張**
  (ステージ毎: 強制納品済み/サブ納品済み)。サブ納品済み=二人は以後不出現(`gone`)。
  強制納品済みのみ→次run以降はサブの受付から始まる。
- **状態機械(run内)**: available → (3秒滞在)accepted-forced → ネームド討伐=目標達成 → (再滞在3秒)強制納品=報酬
  → そのまま次の受付available-sub → (3秒滞在)accepted-sub → キル数達成 → (再滞在3秒)サブ納品=報酬 → 以後不出現。
- 負荷スコア: **1/10**(イベント駆動のみ。毎フレームコストは既存の滞在判定に分岐が数個増えるだけ。
  ネームド1体は既存スポーン経路の流用)。

## ★未決事項(社長裁定待ち — 推奨付き。「全部推奨どおり」の一言でも可)
1. **ネームドの敵種(ステージ毎)**: 未指定。推奨=各ステージの通常湧きの強敵型から1種固定(例: ステージ1=パンプキン)。
   社長が型を指名するならそれに従う(ステージ1/3/4/5の4つ)。
2. **「レア敵」(ステージ3)の定義**: 推奨=**色付き個体(青/紫/赤)のキル**を数える。
3. **サブ「1度だけ受けれる」の意味**: 推奨=**完了が1度きり**(受注して死亡した場合は次runで再受注可)。
   ※文字どおり「受注自体が生涯1回」(死亡したら永久消滅)なら明示ください。
4. **城ボスゲートの適用範囲**: 推奨=クエスト設定のあるステージ(1/3/4/5)のみ。ステージ6/7等は従来どおり
   (ゲート無しで7分出現)。フリー(周回)は同じstageIdなら本編と同じ扱い(未納品なら出ない)。
5. **報酬**: 引き続き叩き台150G(強制/サブ各)。内容・額の指定があれば差し替え。
6. **クエスト未設定ステージ(6/7等)の二人**: 推奨=**出現させない**(クエストが二人の存在理由のため)。
7. **ネームド位置の叩き台**: 二人の方位角+180°・距離2000〜2600のランダム+出現時に城ボスと同じ注意誘導(attention)。
8. **サブの進捗表示の叩き台**: 対象キル時にコールアウト「サンプル n/N」。

## 実装順(承認後)
1. `data/progress.ts`: eventQuestDone を段階制(`{forced:boolean, sub:boolean}`)へ移行(旧bool値の読み替え含む)。
2. ステージ毎クエスト設定テーブル(対象ステージ・ネームド型・サブ目標種別/数)を純関数モジュールに切り出し+ユニットテスト。
3. 受領/納品の状態機械拡張(useGameLoop+gameStore)・ネームド湧き(questTarget)・城ボスゲート・キル集計2箇所。
4. 仮テキスト(受領/納品/進捗)を EVENT_QUEST_LINES 方式で追加(会話は後日社長分を差し込み)。
