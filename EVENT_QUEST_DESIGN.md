# 二人組(クエストNPC)クエスト設計 — 「特定変異種のサンプルが欲しい」/「とにかくサンプルを集めてきて」

社長指示(v0.25.1684直後)+全裁定(v0.25.1686)を反映した**確定仕様・実装済み**。
**会話内容は後で詰める(社長)=当面は仮テキスト**(EVENT_QUEST_LINES / EVENT_QUEST_LINES_SUB)。
受領/納品のUIはサークル内3秒滞在(白メーター・v0.25.1684の仕組み)。「直前のやり取り以降に一度
サークルを出るまでメーターは進まない」ガード(leftSinceAccept)を受領/納品/次受付の全てに適用。

## 確定仕様(社長裁定済み・実装どおり)

### 1) 強制クエスト「特定変異種のサンプルが欲しい」
- **強制として課すのはステージ1のみ**(社長指示)。ステージ3/4/5はフラグ構造は持つが
  **最初からクリア済み扱い**(config.forced=false)=サブのみ発生。
- 受けると**二人と反対側(原点から方位角+180°)の研究対象区域(距離2000〜2600)にネームド出現**。討伐が条件。
- ネームドの型=**パンプキンか犬(werewolf)**(受領時にランダム1体・裁定#1)。宿敵と同じ個体強化
  (HP×2/与ダメ×2/サイズ×1.5・金tint・神話名プールから名前)だが**別フラグ questTarget**=宿敵システムと分離。
  休眠配置(aggroRange=320・叩き台)=近づくと起動。カリング/距離リサイクル/イベント一掃の対象外。
- **誘導**: 注意誘導(attention)はしない(裁定#7)。**拠点と同じ**「近く(500px以内)+画面外の時だけ縁矢印」
  (金色・pixiScene.syncArrows)。画面内は本体(金tint+名前)がマーク。
- **次ステージ解放 = 城ボスクリアフラグ && 強制クリアフラグ**(裁定#4。城ボスの出現ゲートは無し=7分出現は従来どおり)。
  - 城ボスフラグ: giantbat(イベント産fromEvent除く)討伐の瞬間に永続記録(年表と同じA方式)。
  - 強制フラグ: 納品の瞬間に永続記録。
  - どちらが後でも、揃った瞬間に markStageCleared(=次ステージ出現)。勝利(帰還)そのものでは解放しない。
  - クエスト設定の無いステージ(2/6/7/ex)は従来どおり勝利時に解放。

### 2) サブ「とにかくサンプルを集めてきて」
- **強制クリア後**に受注可(3/4/5は最初から受注可)。**完了は1度きり**(裁定#3=受注のみで死亡したら次runで再受注可)。
- 目標(裁定#2: レア=青、あとは紫・赤の記載どおり):
  | ステージ | 目標 |
  |---|---|
  | ステージ1 | 敵の討伐 10体(全キル) |
  | ステージ3 | レア敵(青個体) 5体 |
  | ステージ4 | 紫敵(紫個体) 5体 |
  | ステージ5 | 赤敵(赤個体) 5体 |
- **進捗表示**(裁定#8): 右上スクラップ下に短く「🧪 変異種討伐 0/1」「🧪 サンプル集め n/N」。達成で緑+「納品」。

### 3) 納品・報酬・完了後
- 目標達成後、二人のサークルで再度3秒滞在=納品。**報酬=ゴールド100**(裁定#5・強制/サブ各)。
- 強制納品後は二人は残り、そのままサブの受付へ(一度離れて再滞在3秒で受注)。
- **サブ納品後は以後そのステージに二人は出現しない**(永続)。そのプレイ中は立ち姿のまま(何も起きない)。
- **クエスト設定の無いステージには二人は出現しない**(裁定#6)。

## 実装マップ(v0.25.1686)
- `src/utils/eventQuest.ts`(+test): 設定テーブル(EVENT_QUEST_CONFIG)・ネームド位置/型抽選・キル進捗判定(純関数)。
- `src/data/progress.ts`: `zombie.progress.eventQuestMeta`({forced,sub}のステージ毎Map)・
  `zombie.progress.castleBoss`(城ボスフラグ)・`syncQuestStageClear`(両フラグ→markStageCleared)。
  旧v1684キー(eventQuestDone)は読まずに廃棄。resetProgressで全て消える。
- `src/store/gameStore.ts`: run内状態(eventQuestActive/Kills/GoalCount/GoalTier)・acceptEventQuest(種別決定+
  ネームド湧き)・completeEventQuest(報酬+永続化+分岐)・キル進捗フック(damageEnemy kill分岐/
  grantMeleeKillRewards)・城ボスフラグ(triggerDramaticDeath)・イベント一掃の除外・resetGameの出現ゲート。
- `src/hooks/useGameLoop.ts`: 滞在の状態機械(受領/納品・目標達成ゲート・台詞出し分け)。
- `src/pixi/pixiScene.ts`: questTargetの金tint+名前・縁矢印マーク。
- `src/components/GameHUD.tsx`: EventQuestPill(右上スクラップ下 n/N)。
- `src/App.tsx`: handleVictory=クエストステージはフラグ同期、他は従来どおり。
- `src/utils/enemyUtils.ts` / `src/utils/directorTick.ts`: questTargetの演出/カリング除外。

## 残タスク(社長)
- **会話テキスト**(受領/納品/サブ受領。現状は仮テキスト)。
- 叩き台の実機調整: ネームド位置(2000-2600)・起動距離(320)・HUD文言(「変異種討伐」「サンプル集め」)。

---

# v2(社長指示 2026-08-21): 二人組クエスト = レスキューイベント化 — 確定仕様

**この章がv2の正本**(発注可能な確定仕様1本)。上のv1章は **v2以前の実装の記録**として残してある——
v1章は「過去にこう作った」の説明であって v2 の仕様ではない。**食い違ったらこの章が正**。
案件の進行状態(どこまで実装されたか・誰のボールか)は **PROJECT_STATUS.md** を見る(設計書に状態は書かない)。

裁定は本文へ溶かしてある(「裁定が出るたび追記」はしない)。残る不明点は **§2-15 ★未決事項**に集約。

---

## 2-0. 1枚まとめ(この章で発注するもの)

未クリアランの本編動線を **「拠点確保(S5のみ)→ レスキューイベント → 城ボス → 納品して帰還」** の一本にする。
二人組は「道端に最初から立っている受注NPC」ではなく、**4分に襲われているところを助ける相手**になり、
クエストの中身は **「城ボス(搬送体)を倒してサンプルを持ち帰る」** 1本に統合される。

```
0:00 ──────── 4:00 ──────────────── 5:00 ─────────── 城ボス撃破 ──── 納品 ── リザルト
     二人組不在    レスキュー出現      城ボス出現        帰還サークル1つ    3秒滞在   会話終了で
     城ボスも不在   (制限時間なし)     ※レスキュー未完了 ＋二人がワープ    ＝完了会話  そのまま帰還
                   サークル進入=囲い    ならその分後ろへ  してくる          ＋報酬
                   赤い削岩型＋数体     押していく                         ＋クリア記録
                   殲滅 → 受注会話
```

---

## 2-1. 適用範囲(どのランで出るか)

- **対象ステージ = S1 / S3 / S4 / S5**(= `EVENT_QUEST_CONFIG` に設定のあるステージ)。
  設定の無いステージ(2/6/7/ex)には二人組もレスキューも出ない=**城ボスは従来どおり5:00**。
- **通常出撃のみ**。次のランでは1つも出さない(実在する除外経路で書く):
  練習ラン(`isPracticeRun`)/ ボスメーカー(`isBossMakerRun`)/ ガントレット(`isGauntletRun`)/
  ベンチ(`benchmarkRun`)/ 洋館通路(`corridorMode`)/ 訓練M0(`tutorialStage`)/ 屋内・ラボ(`indoorMode`・
  `stageTheme==='lab'`)/ EX(`isExStageRun`)/ 再訪(`revisitRun`)。
  ※v2の旧稿にあった「革命ラン」は**現行コードに実体が無い**(呼称のみ)。上の実在する除外で足りる。
- **納品済み(クリア済み)ステージ**: レスキューは出ず、二人も出ない(**消滅は永続**)。城ボスは通常の5:00。

**受け入れ条件**
1. 練習/ボスメーカー/ガントレット/ベンチ/通路/訓練/屋内/ラボ/EX/再訪のいずれでも、レスキュー地点も
   二人組も1度も出現しない。
2. stage-2/6/7/ex では城ボスの出現時刻・帰還サークル・解放条件が**この改修の前後で1つも変わらない**。
3. 納品済みステージへ再出撃すると、レスキュー地点が出ず城ボスが5:00に出る。

---

## 2-2. タイムライン(未クリアラン・S1/S3/S4)

| 時刻 / 契機 | 起きること |
|---|---|
| 0:00 | 二人組は場に居ない。城ボスも出ない(城は従来どおり最初から在る) |
| **4:00** | **レスキュー地点が出現**(プレイヤー近く・**制限時間なし**=消えない) |
| プレイヤーがサークルに入る | **囲いイベント開始**(赤い削岩型+数体)。全滅で終了 |
| 殲滅直後 | **受注時の会話**(通信原稿①〜③) |
| **5:00 と レスキュー完了 の遅い方** | **城ボス出現**(既存の出現演出・アテンション・カットインのまま) |
| 城ボス討伐 | **帰還サークルが1つ**(従来の場所=城跡付近)+**二人がそこへワープしてくる** |
| サークル内3秒滞在 | **納品** = 完了時の会話 + 報酬付与 + クリア記録 |
| 完了会話の終了 | **そのままリザルト(帰還)** — 追加の滞在も入り直しも不要 |

**S5だけ**: 「4:00」が **「4:00 と 拠点2か所確保 の遅い方」** になる(§2-10)。

**受け入れ条件**
1. 4:00 未満ではレスキュー地点も二人組も出ない。
2. レスキューを完了しない限り城ボスは出ない(5:00 を過ぎても出ない)。
3. レスキュー完了が 5:00 より前なら城ボスはちょうど 5:00 に出る。

---

## 2-3. レスキューイベントの出現(4:00)

- **4:00 に、プレイヤーの近くへ「レスキュー地点」が出る**。中身は **二人組(`quest-futari`)+ 会話サークル**。
- **制限時間なし**。放置しても消えない・失敗しない(=時間切れという概念を作らない)。
- **出現位置**(叩き台):
  - プレイヤーの**進行方向**(直近の移動方向。停止中は向き)へ伸ばした直線を**中心線**とする。
  - 中心線上、プレイヤーから **500〜1000px 先**(既存レスキューの距離帯 `RESCUE_SPAWN_DIST_MIN/MAX` を流用)を基準点にする。
  - 基準点から**中心線と直交する向きへ ±200px**(左右どちらかをランダム)ずらした点を出現位置にする。
  - 決めた点は必ず **`clampRectToPlayableArea`(`src/world/playableArea.ts`)を通す**。
    ※CLAUDE.md「Y方向に何かを動かす時の必須チェック」: 上へ寄ると①地平線フェードで透ける
    ②帯の外で減光 ③擬似遠近で小さくなる ④可視域外 ⑤移動可能帯の外(=追えない)。
    二人組は**戦う相手ではない**が、④⑤は致命(たどり着けない=詰み)なので clamp は必須。
  - 城・武器商人・拠点サークル・木/壁と重なる場合は、`beginReturnPhase` と同じ**空き地探索**
    (角度を振って半径を広げる)で押し出す。
- 出現の**告知と誘導**は §2-9。

**受け入れ条件**
1. 4:00 ちょうどに1回だけ出現し、以後そのランで再抽選されない。
2. 出現点が移動可能帯(`clampRectToPlayableArea`)の外に出ることが無い。
3. 出現点が城・商人・拠点・木/壁のいずれとも重ならない(重なったら押し出される)。
4. `?zoomlock=0.4`(最大引き)でも、出現点のサークル/二人組の描画が破綻しない。

---

## 2-4. 囲い戦闘(サークルに入る → 殲滅)

- **サークルに入った瞬間に囲いイベントが始まる**。**既存の囲い(`activeEvent`)の型をそのまま使う**。
  - `kind: 'horde'`(=**全滅で終了**する既存の型)。`confinesPlayer` は既定(=拘束する)、`permeable` は既定(=false)。
  - **既存の `kind:'rescue'`(25秒ホールドして守るNPC防衛)は流用しない**。名前が「レスキュー」なだけで
    中身が別物(あちらは `rescueSurvivors` を守る)。混同しないこと。
  - **制限時間なし** = `endsAt` は実質無期限(`POI_GUARD_DURATION_MS` と同じ流儀の巨大値)。終了条件は**全滅のみ**。
  - 半径は既存の `ARENA_EVENT_RADIUS`(=240)を流用(叩き台)。
- **編成**: **§9 の削岩型(`driller`)の赤個体1体** + 数体の敵(★数と種別は §2-15 #1)。
  - 赤個体 = `colorTier: 'red'`。`spawnEnemyAtWithTier` で色を強制指定する(抽選に任せない)。
  - 全員 `fromEvent: true` / `dormant: true` / `aggroRange = EVENT_SPAWN_AGGRO_RANGE` (既存の囲いと同じ作法)。
- **色の文法(CLAUDE.md)との整合**: ここでいう「赤」は**個体色(colorTier)**であって、
  「赤=カウンター/回避の対象」の予告色とは別物。削岩型の突きの**赤い予告帯は従来どおり判定と厳密一致**
  (§9-4)で、個体色の変更はこの一致に影響しない。
- 発火演出は既存の囲いに合わせる(リング+フラッシュ+シェイク+軽いスロー)。**新しい演出は作らない**。

**受け入れ条件**
1. サークルに入るまで囲いは始まらない(近づいただけでは始まらない)。
2. 囲い中は既存の囲いと同じくプレイヤーが円内に拘束される。
3. 敵を全滅させると囲いが終わる。時間経過だけでは終わらない(放置しても勝手に終了しない)。
4. 赤い削岩型が**必ず1体**出る(色抽選のブレで色なしになることが無い)。
5. 削岩型の赤い突き予告帯と実際の判定が一致している(§9-4の不変条件を壊していない)。

---

## 2-5. 受注時の会話(殲滅の直後)

- 全滅した瞬間に **「受注時」の通信**(§2-13 の①〜③)を左上の会話(`enqueueNpcDialogue`)へ流す。
- **この会話の終了をもって「レスキュー完了」**とする(=城ボスの押し出し条件の基準時刻・§2-6)。
- 会話の本文は §2-13 を**一言一句そのまま**。話者名は `グレン`/`ミラ`(`NpcDialogue` のバストアップ対応)。

**受け入れ条件**
1. 全滅の直後に受注時の会話が1回だけ流れる(2回流れない)。
2. 会話の本文・話者・順序が §2-13 と1文字も違わない。
3. 会話中に城ボスの出現演出が割り込まない。

---

## 2-6. 城ボス(5:00 / レスキュー未完了ならその分後ろへ)

- **出現時刻 = max(5:00, レスキュー完了時刻)**。
  - 5:00 = 既存 `CASTLE_BOSS_MIN_TIME_MS`(実在確認: **現行は5分**。v1章の「7分」は古い記述)。
  - レスキュー未完了の間は**何分経っても出ない**。完了した時点で(5:00を過ぎていれば)出る。
- **既存のゲートは1つも外さない**: ボス交戦中の先送り(`castleSpawnLocked` / `facilitiesLocked`)、
  通路/EX/ラボ/訓練/再訪の除外は従来どおり。今回足すのは「クエストステージ未クリアなら
  **レスキュー完了済み** を AND する」の1条件だけ。
- 出現演出(魔法陣・バナー「危険変異体出現」・アテンション・カットイン)は**従来どおり不変**。

**受け入れ条件**
1. レスキュー未完了のまま 5:00・6:00・7:00 を過ぎても城ボスが出ない。
2. レスキュー完了が 5:00 以降なら、完了(会話終了)の直後に城ボスが出る。
3. 対象外ステージ/クリア済みランでは 5:00 ちょうどに出る(変化なし)。
4. 別ボスと交戦中の先送りが従来どおり効く。

---

## 2-7. サブクエスト欄の討伐目標表示

- **城ボスの出現アテンションの後**、サブクエスト欄(右上・スクラップ下の縦積み)の**一番上**に
  **`[搬送体(変異)の討伐 0/1]`** を出す。
- **メインクエストらしく色を変える**: 黄色系(叩き台 `#facc15`)。通常サブクエ(白/達成で緑)と差別化する。
- 城ボス討伐で `1/1` になる。**この行を消すタイミングは ★未決(§2-15 #5)**。

**実装上の注意(実在確認)**: `SubquestHud.tsx` は色を `r.done` だけで決めており、**行データに色を持っていない**。
この行を黄色にするには、行データ側に色(または「メイン扱い」フラグ)を持たせる必要がある。
既存の自動サブクエ台帳(`src/data/subquests.ts`)へ混ぜず、**専用の1行**として先頭に差し込むこと
(自動補充の2枠を食わない)。

**受け入れ条件**
1. 城ボス出現アテンションより前には表示されない。
2. 常にサブクエスト欄の最上段に出る(自動サブクエより上)。
3. 通常サブクエと色が明確に違う。
4. この行の追加で自動サブクエの枠数(2)が減らない。
5. 表示は**進捗が動いた時にだけ**書き換わる(毎フレーム再描画しない=React再描画規律)。

---

## 2-8. 帰還サークル = 納品サークル(統合)・二人のワープ・納品→リザルト

社長の言葉(2026-08-21): 「**このサークルを二人組にしちゃえばいいか?**」「**つまりボス倒すとワープしてくるイメージだよ?**」

- 城ボス討伐 → **帰還サークルは従来どおりの場所(城跡付近)に1つだけ出る**(`beginReturnPhase` の出し方は変えない)。
- **そのサークルへ二人組がワープしてくる**(サンプルを受け取りに来る)。**プレイヤーが二人の元へ戻るのではない**。
- 二人組は**同時に2箇所に居てはいけない**。レスキュー地点の二人は消し、サークルの二人に**1体化**する。
- サークルに入って **3秒滞在 → 納品**:
  1. **完了時の会話**(§2-13 の「完了時」)
  2. **報酬付与**(中身は後日裁定。**付与口だけ用意する**=当面は何も付与しない)
  3. **クリア記録**(§2-10)
- **完了会話が終わったら、そのままリザルトへ**(=帰還)。**追加の滞在も、離れて入り直す操作も要らない。**
  - 理由(社長): 帰還サークルに入るかどうかは元々プレイヤーの任意なので、**入った時点で「帰る」と決めている**。
    拾い物や整理は入る前に済ませられる。
- **二重に作らない**: サークルは1つ、ゴール表示も1つ。納品用の別サークル・別マーカーを増やさない。

**ワープの出現演出(慣性は必須・CLAUDE.md)**
- 「パッと出て止まる」は禁止。**既存の型を流用し、新しい型を発明しない。**
- 流用先 = **救難信号アライ/守護霊の登場と同じ「飛来ジャンプ」**
  (水平は ease-out、垂直は sin 1山のホップ、着地で減速)。
  実在の定数: `RESCUE_ALLY_FLYIN_MS = 300` / `RESCUE_ALLY_HOP_PX = 48`(`gameStore.ts`)、
  描画側の実装は `pixiScene.ts` の救援アライ描画(飛来=放物線+`jumpStretch`)。
- 着地点は**サークルの縁寄り**で、プレイヤーと重ならない位置。着地後は既存の待機(呼吸)描画へ戻る。

**実装上の注意(★実在確認・仕様判断が要る)**: **現行の城ボス帰還は「3秒滞在」ではない。**
`updateReturnPhase` は `finaleDefeated && !corridorMode`(=通常ストーリー=城ボス撃破後)のとき
**滞在では完了せず、円内で指を離すと `requestStoryReturnPrompt` →「帰還しますか?」の確認ダイアログ**を出す。
3秒滞在(`RETURN_CIRCLE_HOLD_MS`)が効くのは屋内/イベント帰還の経路。
→ 二人組クエスト中の「3秒滞在で納品」をどう既存経路と噛み合わせるかは **★未決(§2-15 #6)**。

**受け入れ条件**
1. 城ボス討伐後、帰還サークルは**1つだけ**出る(納品用の2つ目のサークルが出ない)。
2. その直後に二人組がサークルへワープしてくる。**レスキュー地点の二人は同時に消えている**
   (画面のどこにも二人組が2組居ない)。
3. ワープの出現に加減速がある(等速直線で現れて瞬間停止しない)。
4. サークルに入って3秒で完了会話が始まり、**会話が終わるとリザルトへ遷移する**
   (再滞在・入り直し・追加操作を一切要求しない)。
5. 納品の瞬間に報酬付与口が呼ばれる(中身が空でも呼ばれていることをログ/テストで確認できる)。
6. 対象外ステージ・クリア済みランの帰還サークルの挙動が**1つも変わらない**。

---

## 2-9. 誘導マーク(縁矢印)

社長指示: 「**納品の時、2人への誘導マークは表示してあげてね(城ボスとか商人と同じやつね)**」

- **納品先の誘導は、既存の帰還サークルの縁矢印がそのまま担う**(実在確認: `pixiScene.syncArrows` 内で
  `returnCircle` を読み、**画面外の時だけ**エメラルド `0x34d399` の縁矢印を出している)。
  §2-8 で納品先=帰還サークルに統合したので、**新しい矢印を足す必要がない**。
- **二人組本体へ別の矢印を足さない**(同じ場所に2本出る=二重マーク)。
- 城ボス(`castle.bossSpawned` 時の赤)・武器商人(`0xfbbf24`)と同じ系統の縁矢印なので、
  「城ボスとか商人と同じやつ」という指示を満たす。
- **レスキュー地点(4:00)側の告知・誘導は ★未決(§2-15 #4)**。制限時間なしで見失うと勝利不能になるため、
  何らかの誘導は要る。

**受け入れ条件**
1. 納品前、帰還サークルが画面外にある時だけ縁矢印が出る(画面内では出ない=既存の作法)。
2. 二人組へ向く矢印が2本出ない。
3. 対象外ステージ・クリア済みランの矢印挙動が変わらない。

---

## 2-10. 永続記録・次ステージ解放・EX解放の鎖

### 記録
- **クリア記録 = 納品の瞬間**(城ボス討伐の瞬間ではない)。討伐だけして帰らなかったランは記録しない。
- **完了フラグ**: `eventQuestMeta`(`zombie.progress.eventQuestMeta`)に **新フィールドを1つ足す**
  (例: `delivered: boolean`)。旧 `forced` / `sub` は**読まない**(既存セーブの移行は考慮不要=社長裁定)。
- **城ボスフラグ**(`zombie.progress.castleBoss`)は従来どおり討伐の瞬間に記録する(不変)。
- **次ステージ解放**: `syncQuestStageClear` の判定を `delivered` ベースにする。新フローでは納品が必ず
  城ボス討伐より後なので、**納品の瞬間に揃って `markStageCleared` が走る**。
- **二人組の消滅は永続**: `delivered` のステージには以後レスキューが出ず、二人も出ない。

### EX解放の鎖(**1つも変えない**)
社長裁定(2026-08-21): 「こいつ倒してサンプル集めてきて。って城ボスの話にすれば、設定変えなくていいのでは?」→「おけ」

- 二人組クエストそのものを **「城ボス(搬送体)を倒してサンプルを持ち帰る」話として読み替える**ことで、
  現行の鎖(**サンプル3本 → グレンの薬 → 洋館再訪で使用 → EX解放**)は**構造を1つも変えなくてよい**。
- **薬の付与条件 = S1・S3・S4 の3本を納品**(通信原稿①血液試料/②特殊個体/③さらに希少な個体 と数が一致。
  **S5は4本目=解放条件には数えない**)。
- 実装は**読む先の差し替えだけ**: `src/utils/storyProgress.ts` の `subsAllCompletedFromMeta()` が
  `getEventQuestMeta(id).sub` を読んでいる箇所を `delivered` へ変える。
  `SUB_QUEST_STAGE_IDS = ['stage-1','stage-3','stage-4']` は**そのまま**。
  `medicineOwned` → `medicineUsed` → `canShowEx` の判定も**そのまま**。

**受け入れ条件**
1. 城ボスを倒しただけで死亡/撤退したランでは、次ステージが解放されない。
2. 納品した瞬間に次ステージが解放される(リザルトを見る前でも記録されている)。
3. S1・S3・S4 の3本を納品した状態でM7クリア→ED後に**グレンの薬**が付与される。
4. S5を納品しても薬の付与条件のカウントは増えない(3本の判定に影響しない)。
5. 練習ラン中は上記の永続書き込みが1つも起きない(`practiceGuard` の関所で塞がれている)。

---

## 2-11. ステージ5のみ: 拠点2か所確保が先行条件

- **第1段階「拠点を2か所以上確保」**: 既存の拠点システム(`baseSites` の `status === 'captured'` を数える)。
  **確保数 < 2 の間はレスキューが出ない = 城ボスも出ない**。
- **レスキュー出現 = 「4:00」と「2か所目を確保した瞬間」の遅い方**
  (4:00 時点で拠点2未満なら、2か所目を確保した瞬間に出る)。
- 掲示: 第1段階を**サブクエスト欄の最上段**に `[拠点確保 n/2]` として出す(§2-7 と同じ「メイン扱い」の色)。
  レスキュー出現とともにこの行は消え、以後は §2-7 の行に置き換わる。
- **S5の通信原稿は後日支給**(§2-13)。原稿が入るまでは差し込み口だけ用意し、S5では会話を流さない。

**受け入れ条件**
1. S5で拠点1か所以下のまま 4:00 を過ぎても、レスキューが出ない。
2. 2か所目を確保した瞬間(4:00 以降なら即座に)レスキューが出る。
3. `[拠点確保 n/2]` が拠点を取るたびに更新され、レスキュー出現で消える。
4. S1/S3/S4 には拠点の先行条件が付かない(4:00 で出る)。

---

## 2-12. v1 / v2旧稿から**廃止・撤回**した記述(残さないもの)

| 撤回した記述 | 現行(この章) |
|---|---|
| 「5分に二人組が出現→サークル3秒滞在で受注→通信→城ボス」 | **4:00 レスキュー出現→囲い殲滅→受注会話**(§2-2〜2-5) |
| 「納品の再滞在は不要(完了通信で完了)」 | **撤回**。納品(3秒滞在)を復活し、帰還サークルと統合(§2-8) |
| 「納品場所=レスキュー地点に残る二人。プレイヤーが戻る」 | **撤回**。**二人がサークルへワープしてくる**(§2-8) |
| 「納品後に一度離れて再滞在で帰還」(設計チャットの解釈) | **撤回**。完了会話の終了で**そのままリザルト**(§2-8) |
| v1「強制クエスト=ネームド(パンプキン/犬)討伐」 | 廃止。討伐対象は**城ボス(搬送体)**(§2-6) |
| v1「サブ『とにかくサンプルを集めてきて』」 | 廃止(サンプル系は右上の自動サブクエが担う)。二人組=城ボスクエスト専任 |
| v1「二人組は開始時から場に配置」 | 廃止。4:00 まで場に居ない(§2-2) |
| v1「次ステージ解放 = 城ボスフラグ && 強制フラグ」 | **納品の瞬間**に一本化(§2-10) |
| v1「城ボスは7分出現」 | **事実として現行は 5:00**(`CASTLE_BOSS_MIN_TIME_MS`)。v1章の記述が古い |
| 「革命ランでは出さない」 | 「革命ラン」は現行コードに実体なし。**実在する除外経路**で書く(§2-1) |

> 事実として併記(判断材料・制約ではない): 移行の考慮は不要(社長「いまは開発中なので移行のこととか
> 考えなくていいです」)、S5の既存「遭遇のみ」イベント(M5・確定台詞)は新仕様で置き換えてよい(社長「はい」)。

---

## 2-13. 通信原稿(社長支給 2026-08-21・**一言一句そのまま**使う)

**割り当て(社長裁定 2026-08-21): S1=① 血液試料回収 / S3=② 特殊個体 / S4=③ さらに希少な個体 / S5=後日支給。**
「受注時」= レスキュー完了直後(§2-5)。「完了時」= 納品(§2-8)。

### ① 血液試料回収(S1)
受注時
- グレン「またお前か。頼みがある。極秘のミッションでな。理由は聞くな」
- ミラ「そう！　血液サンプルを持ち帰って、独自開発した製剤の有用性を極秘で調べるんだよ！」
- グレン「……」

完了時
- ミラ「ありがと！　これ、報酬ね！」
- グレン「それ俺のだろ」

### ② 特殊個体の試料回収(S3)
受注時
- グレン「また会ったな。実は頼みがある。理由は聞くなよ」
- ミラ「秘密だからね！　通常個体とは違う血液が、こっちの試料にどう反応するか――」
- グレン「……あっ、後ろに変異体だ、ミラ！」
- ミラ「わっ！　どこ！？」

完了時
- グレン「報酬はそうだなぁ……」
- ミラ「もう渡したよ！」
- グレン「また俺の……」

### ③ さらに希少な個体の試料回収(S4)
受注時
- グレン「よく会うな。追っかけか？　まあいい、頼みがある」
- ミラ「うぐ……めっちゃ怒られるから、理由は言えないの……」
- グレン「頼んだぞ」

完了時
- グレン「今日は俺の物を渡すなよ」
- ミラ「もう渡したよ！」
- グレン「……」

> **実在確認(2026-08-22)**: この3本×2場面は **すでに `src/utils/eventQuest.ts` に実装済み**
> (`EVENT_QUEST_SUB_ACCEPT_LINES` / `EVENT_QUEST_SUB_COMPLETE_LINES`・stage-1/3/4)で、原稿と**完全に一致**する。
> v2実装では**本文を書き直す必要はなく、呼び出す場面(受注=レスキュー完了直後 / 完了=納品)を差し替えるだけ**。

---

## 2-14. 実装マップ(**実在確認済み・2026-08-22 時点のコード**)

### 触る場所

| 何を | どこ(実在) |
|---|---|
| レスキュー出現(4:00)・囲い開始・全滅判定 | `src/hooks/useGameLoop.ts` 囲い系イベントのブロック(`ARENA_EVENT_RADIUS`=615、抽選/発火=3419〜3538、終了判定はその直後) |
| 城ボスのゲート | `src/hooks/useGameLoop.ts:2740` `castleBossReady` に「クエストステージ未クリアなら**レスキュー完了済み**」を AND。出現ブロックは 2751、出現アテンションは 2786〜2791(いずれも**演出は不変**) |
| 帰還サークル出現 | `src/hooks/useGameLoop.ts:3831〜3843`(`finaleDefeated` → `beginReturnPhase(castleEvent.x, y)`) / 実体は `src/store/gameStore.ts:15347` |
| 納品の滞在・会話・報酬 | `src/hooks/useGameLoop.ts:9529〜9590`(二人組の滞在状態機械。`EVENT_QUEST_DWELL_MS`=3000・`leftSinceAccept` ガード) |
| 帰還の完了判定 | `src/store/gameStore.ts:15405` `updateReturnPhase`(★通常ストーリーは滞在ではなく**離指→確認ダイアログ**: `requestStoryReturnPrompt`=15449 / `answerStoryReturnPrompt`) |
| 二人組の状態・生成・出現ゲート | `src/store/gameStore.ts`: `createEventQuestNpc`=754、距離定数 `EVENT_NPC_MIN/MAX_DISTANCE`=435/436、`EVENT_NPC_INTERACT_RADIUS`=437、`resetGame` の出現ゲート≈17094 |
| 受注/完了のアクション | `src/store/gameStore.ts`: `acceptEventQuest`=10033 / `completeEventQuest`=10078(**報酬付与口はここ**。`EVENT_QUEST_REWARD_GOLD`=776 は当面使わない) |
| 囲いの開始/終了 | `src/store/gameStore.ts`: `beginArenaEvent`=13626 / `endArenaEvent`=13643 |
| 設定テーブル・会話原稿 | `src/utils/eventQuest.ts`: `EVENT_QUEST_CONFIG`=32(S5の `basesRequired: 2` を追加)、原稿=54〜89 |
| 永続フラグ | `src/data/progress.ts`: `EVENT_QUEST_META_KEY`=358、`getEventQuestMeta`=386 / `setEventQuestMeta`=391、`markCastleBossCleared`=422、`syncQuestStageClear`=435 |
| EX解放の鎖 | `src/utils/storyProgress.ts`: `SUB_QUEST_STAGE_IDS`=8、`subsAllCompletedFromMeta`=15(**ここの読み先だけ差し替え**) |
| 二人組の描画 | `src/pixi/pixiScene.ts`: `syncEventQuestNpc`=8499(status: available/accepted/completed/gone、`EVENT_NPC_FADE_MS`=1100) |
| 縁矢印 | `src/pixi/pixiScene.ts`: `syncArrows`=26136(帰還サークルの矢印は同関数内・`returnCircle` を直接読む=**追加配線不要**) |
| 帰還サークルの描画 | `src/pixi/pixiScene.ts`: `syncReturnCircle`=21043 |
| サブクエ欄 | `src/components/SubquestHud.tsx`(色は `r.done` のみ=**行データに色を持たせる拡張が要る**) / 縦積みの列は `src/components/GameHUD.tsx:245〜254` / 行の型は `src/utils/subquests.ts:29` |
| 二人組の進捗ピル(v1) | `src/components/GameHUD.tsx:19〜34` `EventQuestPill`(v1のサブ用。**v2では §2-7 の表示に置き換わるので撤去**) |

### 流用する既存部品(新規に作らない)

| 用途 | 流用先(実在) |
|---|---|
| 囲い(全滅で終わる) | `ActiveEvent { kind: 'horde' }`(`src/types/game.ts:1747〜`)。`ARENA_EVENT_RADIUS`=240 |
| 色を強制した敵の湧き | `src/utils/enemyUtils.ts:792` `spawnEnemyAtWithTier`(`forcedColorTier`) |
| 削岩型 | `src/utils/drillerAi.ts`(AI純関数)。型は `'driller'`(`src/types/game.ts:1186`) |
| ワープの出現演出 | 救難信号アライ/守護霊の飛来ジャンプ: `RESCUE_ALLY_FLYIN_MS`=300 / `RESCUE_ALLY_HOP_PX`=48(`gameStore.ts:1794/1802`)、描画は `pixiScene.ts` の救援アライ(放物線+ease-out+`jumpStretch`) |
| 出現位置の安全化 | `src/world/playableArea.ts` `clampRectToPlayableArea` / `beginReturnPhase` の空き地探索 |
| 拠点の確保数(S5) | `state.baseSites.filter(b => b.status === 'captured').length` |
| 通常プレイ判定 | `isPracticeRun` / `isBossMakerRun` / `isGauntletRun` / `benchmarkRun` / `corridorMode` / `isExStageRun` |

### 実在確認で判明した「旧マップの誤り・今の事実」

1. **城ボスは5:00**(`src/config/castleBoss.ts` `CASTLE_BOSS_MIN_TIME_MS = 5*60*1000`)。v1章の「7分出現」は**古い**。
2. **通信原稿3本は既に実装済み**で原稿と完全一致(§2-13 の注)。新規に書き起こす作業は無い。
3. **城ボス撃破後の帰還は「3秒滞在」ではない**。`finaleDefeated` 経路は**離指→「帰還しますか?」確認**(§2-8 の注)。
   3秒(`RETURN_CIRCLE_HOLD_MS`=3000)は屋内/イベント帰還の経路。
4. **帰還サークルの縁矢印は既にある**(`syncArrows` が `returnCircle` を直接読む・エメラルド `0x34d399`)。
   納品先を帰還サークルに統合したので、誘導マークの新規実装は要らない。
5. **`kind: 'rescue'` は既に別物として存在する**(`RESCUE_HOLD_NEED_MS`=25000 の防衛イベント・`src/world/rescue.ts`)。
   名前が同じでも流用しない(§2-4)。
6. **削岩型は現在 stage-4〜7 限定**(`DRILLER_ALLOWED_STAGES`・`allowDrillerForRun`)。
   ただしこのゲートが掛かるのは**通常湧きの pumpkin 枠の差し替え**だけで、`spawnEnemyAtWithTier` の
   直接湧きは通らない。**S1/S3 で赤い削岩型を出すと「削岩型の初出がS1」になる**(§2-15 #2)。
7. **`SubquestHud` は行ごとの色を持っていない**(`r.done` で緑/白のみ)。黄色の「メイン扱い」行には型の拡張が要る。
8. **練習ランの永続書き込みは `practiceGuard` が localStorage ごと塞ぐ**ので、新フラグを足しても既定で安全。

### 性能(CLAUDE.md「Performance review」)

**負荷スコア 1/10**(無視できる)。増えるのは①囲いイベント1回ぶんの敵(既存の囲いと同数帯)
②二人組スプライト1体の飛来アニメ(数百ms)③サブクエ欄1行。
**強glow(=投影影を落とす光源)を増やさない**ので、実測で効く唯一の軸に触っていない。
per-frame の新規走査も追加しない(判定は時刻/フラグの比較のみ)。

---

## 2-15. ★未決事項(社長裁定へ)

> 書かれていないので**実装者が勝手に決めてしまう**箇所。①〜⑦は**ゲームの見え方・挙動・体験**に触れるため
> 設計チャットでは決めない。各項に**推薦を1つ**添える(過去の裁定は事実として併記)。

**#1 レスキューの囲いの中身(敵の数・種別・配置)**
「赤色の槍と、数体の敵台本」の**「数体」の中身が未指定**。
推薦: **赤い削岩型1体 + ゾンビ系4体**(既存ミニボス囲い `ARENA_BOSS_ADDS`=4 と同数)。
サークル半径は既存 `ARENA_EVENT_RADIUS`=240 を流用。配置は既存の囲いと同じ「円内・プレイヤーを避ける」。

**#2 赤い削岩型を S1/S3 に出してよいか(§9 の解禁ステージとの衝突)**
削岩型は現在 **stage-4 以降**の敵として設計されている(`DRILLER_ALLOWED_STAGES`)。S1のレスキューに出すと
**削岩型の初お目見えがステージ1**になる。加えて赤個体(`colorTier:'red'`)はHP/攻撃の色倍率が乗り、
`bossPosture` の対象(赤=posture 60)にもなるため、S1では**難度が跳ねる**可能性がある。
推薦: **出す(社長指示のまま)**。ただし**この個体だけ色倍率を乗せず、見た目の赤だけを使う**か、
S1/S3では**HPを固定値に据え置く**かのどちらかで難度を抑える。
※事実として併記: §9 では「ステージ4以降の新型雑魚」として設計されている。

**#3 囲いで死んだ場合のやり直し方**
制限時間が無いので「失敗」は死亡だけ。**同一ラン中に再挑戦できるのか**が未指定
(死亡=ランが終わる想定なら問題ないが、蘇生・コンティニュー系がある場合の扱い)。
推薦: **死亡=そのランは終了**(社長「失敗したらやり直し」)。同一ラン中の再挑戦は作らない。

**#4 レスキュー出現(4:00)の告知と誘導**
v2旧稿には「アテンション[Help!]」があったがレスキュー化で宙に浮いた。**制限時間が無い＝見失うと
城ボスが出ず勝利不能**になるため、何らかの誘導は要る。
推薦: **①出現バナー(既存の囲い/レスキューと同じ枠。文言は「救難信号受信」等)+②アテンション1回
(カメラ寄せ・既存の素のattention)+③未完了の間ずっと、画面外の時だけ縁矢印**。
※事実として併記: v1では「二人組にattentionはしない」(裁定#7)と裁定されている。当時は「開始時から
その辺に立っている受注NPC」だったため。**今は勝利の必須経路なので、推薦は誘導を付ける側**。

**#5 `[搬送体(変異)の討伐 0/1]` を消すタイミング / 納品段階の表示**
討伐で `1/1` になった後、**いつ消すか**が未指定。また**納品が残っている**ことを何が伝えるか。
推薦: **討伐後は消さず、行を `[サンプルを届ける]` に差し替えて納品まで残す**(消すのは納品の瞬間)。
「倒したのに終わらない」の迷子を防ぐのがこの行の役目のため。

**#6 納品の3秒滞在と、既存の「帰還しますか?」確認ダイアログの関係**
現行の城ボス帰還は**離指→確認ダイアログ→はい**(§2-8 の注)。二人組クエスト時は「3秒滞在→完了会話→
そのままリザルト」なので、**確認ダイアログを通すか通さないか**が未指定。
推薦: **二人組クエスト進行中は確認ダイアログを出さない**(3秒滞在が「帰る意思の確認」を兼ねる。
二重確認は社長指示の「追加の操作は要らない」に反する)。対象外ステージ・クリア済みランは**従来どおり確認を出す**。

**#7 レスキュー地点に「まだ行っていない」まま城ボス時刻を過ぎた時の、二人組側の見え方**
二人組はサークルの中で襲われ続けている(囲いは未発火)状態が延々続く。**その間の見た目**
(敵に囲まれている絵を出すのか、二人が立っているだけなのか)が未指定。
推薦: **囲いの敵は進入まで湧かせない**(=二人が立っているだけ)。理由=遠くの敵を常時走らせるのは
負荷と事故(勝手に殺される)の元。危機感は §2-13 の会話と誘導(#4)で伝える。

**#8 報酬の中身(既知・後日裁定)**
社長「後で考えるわ。スキルかサブウェポンにするかも?」=**当面は報酬なし**、`completeEventQuest` に
**付与口だけ**残す(§2-8)。裁定が出たらここへ書く。

**#9 S5の通信原稿(既知・後日支給)**
S5は原稿が未支給。実装は差し込み口だけ用意し、原稿が入るまでS5では会話を流さない(§2-11)。
