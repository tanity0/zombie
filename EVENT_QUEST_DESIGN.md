# 二人組(クエストNPC)クエスト設計 — 「特定変異種のサンプルが欲しい」/「とにかくサンプルを集めてきて」

社長指示(v0.25.1684直後)+全裁定(v0.25.1686)を反映した**確定仕様・実装済み**。
**会話内容は後で詰める(社長)=当面は仮テキスト**(EVENT_QUEST_LINES / EVENT_QUEST_LINES_SUB)。
受領/納品のUIはサークル内3秒滞在(白メーター・v0.25.1684の仕組み)。「直前のやり取り以降に一度
サークルを出るまでメーターは進まない」ガード(leftSinceAccept)を受領/納品/次受付の全てに適用。

## 確定仕様(社長裁定済み・実装どおり)

### 1) 強制クエスト「特定変異種のサンプルが欲しい」
- **強制として課すのはステージ1のみ**(社長指示)。ステージ3/4/5はフラグ構造は持つが
  **最初からクリア済み扱い**(config.forced=false)=サブのみ発生。
- 受けると**二人と反対側(原点から方位角+180°)の研究対象区域(距離2000〜2600)にネームド出現**。討伐が条件。
- ネームドの型=**パンプキンか犬(werewolf)**(受領時にランダム1体・裁定#1)。宿敵と同じ個体強化
  (HP×2/与ダメ×2/サイズ×1.5・金tint・神話名プールから名前)だが**別フラグ questTarget**=宿敵システムと分離。
  休眠配置(aggroRange=320・叩き台)=近づくと起動。カリング/距離リサイクル/イベント一掃の対象外。
- **誘導**: 注意誘導(attention)はしない(裁定#7)。**拠点と同じ**「近く(500px以内)+画面外の時だけ縁矢印」
  (金色・pixiScene.syncArrows)。画面内は本体(金tint+名前)がマーク。
- **次ステージ解放 = 城ボスクリアフラグ && 強制クリアフラグ**(裁定#4。城ボスの出現ゲートは無し=7分出現は従来どおり)。
  - 城ボスフラグ: giantbat(イベント産fromEvent除く)討伐の瞬間に永続記録(年表と同じA方式)。
  - 強制フラグ: 納品の瞬間に永続記録。
  - どちらが後でも、揃った瞬間に markStageCleared(=次ステージ出現)。勝利(帰還)そのものでは解放しない。
  - クエスト設定の無いステージ(2/6/7/ex)は従来どおり勝利時に解放。

### 2) サブ「とにかくサンプルを集めてきて」
- **強制クリア後**に受注可(3/4/5は最初から受注可)。**完了は1度きり**(裁定#3=受注のみで死亡したら次runで再受注可)。
- 目標(裁定#2: レア=青、あとは紫・赤の記載どおり):
  | ステージ | 目標 |
  |---|---|
  | ステージ1 | 敵の討伐 10体(全キル) |
  | ステージ3 | レア敵(青個体) 5体 |
  | ステージ4 | 紫敵(紫個体) 5体 |
  | ステージ5 | 赤敵(赤個体) 5体 |
- **進捗表示**(裁定#8): 右上スクラップ下に短く「🧪 変異種討伐 0/1」「🧪 サンプル集め n/N」。達成で緑+「納品」。

### 3) 納品・報酬・完了後
- 目標達成後、二人のサークルで再度3秒滞在=納品。**報酬=ゴールド100**(裁定#5・強制/サブ各)。
- 強制納品後は二人は残り、そのままサブの受付へ(一度離れて再滞在3秒で受注)。
- **サブ納品後は以後そのステージに二人は出現しない**(永続)。そのプレイ中は立ち姿のまま(何も起きない)。
- **クエスト設定の無いステージには二人は出現しない**(裁定#6)。

## 実装マップ(v0.25.1686)
- `src/utils/eventQuest.ts`(+test): 設定テーブル(EVENT_QUEST_CONFIG)・ネームド位置/型抽選・キル進捗判定(純関数)。
- `src/data/progress.ts`: `zombie.progress.eventQuestMeta`({forced,sub}のステージ毎Map)・
  `zombie.progress.castleBoss`(城ボスフラグ)・`syncQuestStageClear`(両フラグ→markStageCleared)。
  旧v1684キー(eventQuestDone)は読まずに廃棄。resetProgressで全て消える。
- `src/store/gameStore.ts`: run内状態(eventQuestActive/Kills/GoalCount/GoalTier)・acceptEventQuest(種別決定+
  ネームド湧き)・completeEventQuest(報酬+永続化+分岐)・キル進捗フック(damageEnemy kill分岐/
  grantMeleeKillRewards)・城ボスフラグ(triggerDramaticDeath)・イベント一掃の除外・resetGameの出現ゲート。
- `src/hooks/useGameLoop.ts`: 滞在の状態機械(受領/納品・目標達成ゲート・台詞出し分け)。
- `src/pixi/pixiScene.ts`: questTargetの金tint+名前・縁矢印マーク。
- `src/components/GameHUD.tsx`: EventQuestPill(右上スクラップ下 n/N)。
- `src/App.tsx`: handleVictory=クエストステージはフラグ同期、他は従来どおり。
- `src/utils/enemyUtils.ts` / `src/utils/directorTick.ts`: questTargetの演出/カリング除外。

## 残タスク(社長)
- **会話テキスト**(受領/納品/サブ受領。現状は仮テキスト)。
- 叩き台の実機調整: ネームド位置(2000-2600)・起動距離(320)・HUD文言(「変異種討伐」「サンプル集め」)。

---

# v2(社長指示 2026-08-21): 城ボスゲート化 — 「二人と話さないと城ボスが出ない」

実装完了までは v1 仕様が現行。状態は PROJECT_STATUS.md を見る。

### ★社長検討中(2026-08-21・監査指摘への裁定)
1. **EXステージの解放条件の代替**(監査#1: サブ「サンプル集め」廃止で現行の解放線
   「サブ3本完了→エンディング後に薬→洋館再訪→EX」が切れる)——社長「必須になったからどうしようかな。
   考えます」=**検討中**。
2. **stage-1などのクリア記録の書き先=裁定確定(2026-08-21)**: 社長「二人組の解放は城ボス倒した時点で
   いいんじゃないの?」=**次ステージ解放の記録は城ボス討伐(ありがとう通信)の瞬間に一本化**。
   納品フラグ(meta.forced)の経路は廃止。

### 裁定済み(2026-08-21・追加)
3. **既存セーブ・旧仕様からの移行は考慮不要**(社長「いまは開発中なので移行のこととか考えなくて
   いいです」)——旧サブ納品済み端末の扱い(監査#4)等は対策を組み込まない。
4. **S5の既存「遭遇のみ」イベント(M5・確定台詞)は新仕様で置き換えてよい**(社長「はい」・監査#3)。

※監査18件の残り(設計の穴=設計者が埋める分)は、検討中2件の結論が出た時点でまとめてv2章を改稿し
再監査に掛ける(EX解放の結論次第で書き直しになるため部分改稿はしない)。
社長の言葉(要旨): 二人組出現ステージ(1/3/4/5)のみの特別仕様。城ボスが二人と話さないと出現しない。
5分経つと二人組が出現してアテンション[Help!]。受注すると通信会話→会話終了後に城からボス出現(通常
アテンション)。城ボスを倒すと帰還サークル出現+同時にその場で通信(ミラ「ありがとう!」的)。
一度クリアすると二人はそのステージから消え、以後は他ステージ同様5分で自動出現。
今までは最初から二人組がいたが、今後は5分経過しないと現れない。

## フロー(S1/3/4/5共通・未クリアラン)
1. **開始〜5分**: 二人組は場に居ない(v1の「最初から配置」を廃止)。城ボスも出ない。
2. **5分経過**: 二人組が出現+アテンション[Help!](カメラ寄せ)。
   - 出現位置: 中心線から約±200pxオフセット(#34の指示を統合)・プレイヤー進行方向の先(叩き台)。
   - #34「通常プレイ限定」も統合(練習/ボスメーカー/革命ラン等では出さない)。
3. **受注**(サークル3秒滞在=v1と同じUI)→ **通信会話**(既存の無線システム・本文は仮テキスト)
   → 会話終了後に**城からボスが出現**(既存の出現演出+アテンション+カットイン=従来どおり)。
4. **城ボス討伐** → 帰還サークル出現(従来どおり)+**同時にその場で通信**(ミラ「ありがとう!」的)。
   通信をもって**クエスト完了=永続記録**(納品の再滞在は不要=v1の納品滞在を廃止)。
5. **クリア済みラン(再訪)**: 二人はそのステージに出現しない(v1と同じ永続)。城ボスは
   **従来どおり5分で自動出現**(=現行コードのcastleBossReadyそのまま)。

## ステージ5のみ: 2段階の強制タスク
- **第1段階「拠点を2か所以上確保」**: 進軍NPCと拠点を奪還する流れ(既存の拠点システム=
  captured状態のカウントを流用)。**確保数<2の間は二人組が現れない=城ボスも出ない**。
- **第2段階**: 拠点2か所以上確保の状態で5分経過 → 二人組アテンション(以降は共通フロー)。
- 掲示: 第1段階をサブクエHUDの強制枠で「拠点確保 n/2」表示(叩き台・#38の任務詳細表示とも整合させる)。

## v1から変わるもの / 残るもの
- **廃止**: 強制クエスト「ネームド(パンプキン/犬)討伐」(城ボス討伐に置き換え)。納品の再滞在。
  二人組の開始時配置。
- **維持**: サークル3秒滞在UI・二人組の消滅永続(クリア後不出現)・クエスト無しステージ(2/6/7/ex)は
  従来どおり5分自動・次ステージ解放=城ボス討伐+クエスト完了(新フローでは同時に満ちる=実質一本化)。
- **注**: 受注しない限り城ボスが出ない=帰還サークルも出ない(勝利に受注が実質必須)。[Help!]の
  アテンションが誘導。死亡時はrun内状態リセット=次runでまた5分から。

## 裁定(2026-08-21・確定)
1. **サブ「サンプル集め」= 廃止**(サンプル系は右上の自動サブクエ40件が担う。二人組=城ボスクエスト専任)。
2. **報酬 = 後日裁定**(社長「後で考えるわ。スキルかサブウェポンにするかも?」)。
   **当面は報酬なしで実装**し、完了通信の瞬間に付与処理を差し込める口(completeEventQuest)だけ残す。
3. **S5の5分 = 開始からの5分と拠点2確保の遅い方**(5分時点で拠点2未満なら、2か所目を確保した瞬間に
   二人組出現)。

## ★v2改訂: レスキューイベント化(社長指示 2026-08-21・言葉のまま)
「二人イベントは確定版なんだけど、これはレスキューイベントとして扱うことにします。
4分にプレイヤー近くに発生。制限時間無し。中身はこのサークルに入ると囲いイベントとなり、赤色の槍と、
数体の敵台本が発生。倒し切ると、前述の会話後、5分経過したら城ボス発生。(まだ助けてなければその分
後ろに押していく)
つまり、メインは拠点確保、レスキューイベント、城ボス(失敗したらやり直し)
一度クリアしてた場合は通常の5分城ボス になる。」

### 解釈(設計チャット・改稿時にこの理解で書く)
- v2フローの「5分に二人組出現→サークル3秒滞在で受注→通信→城ボス」を**置き換える**:
  **4分にレスキューイベントがプレイヤー近くに出現**(制限時間なし)→サークルに入ると**囲いイベント**
  (赤色の槍+数体の敵台本)→**倒し切ると会話**→**5:00で城ボス出現**(レスキュー未完了ならその分
  後ろへ押す=城ボスはレスキュー完了より前に出ない)。
- 未クリアランのメイン動線=**拠点確保(S5)→レスキューイベント→城ボス**。失敗(死亡)したら次runでやり直し。
- クリア済みランは従来どおり**通常の5分城ボス**(レスキューは出ない)。
- 通信原稿(下記①〜③)の「受注時」は**レスキュー完了直後の会話**として使う想定。「完了時」=城ボス討伐時の
  通信(従来案どおり)。

### 改稿時に確認する点(★未決ではなく確認事項・改稿案に織り込んで社長へ)
- 「赤色の槍」=**§9の新敵・削岩型(槍持ち)の赤個体**(社長裁定 2026-08-21「赤色の削岩方です。合ってます」)。
- 4分レスキューはS1/3/4/5共通か(S5は拠点2確保が先行条件のままか)。
- ★検討中2件(EX解放条件の代替・stage-1クリア記録の書き先)は引き続き未決。

## 通信原稿(社長支給 2026-08-21・一言一句そのまま使う=v2実装時に配線)
**割り当て(社長裁定 2026-08-21): S1=①血液試料回収 / S3=②特殊個体 / S4=③さらに希少な個体 / S5=後日支給。**

### ① 血液試料回収(S1)
受注時
- グレン「またお前か。頼みがある。極秘のミッションでな。理由は聞くな」
- ミラ「そう！　血液サンプルを持ち帰って、独自開発した製剤の有用性を極秘で調べるんだよ！」
- グレン「……」

完了時
- ミラ「ありがと！　これ、報酬ね！」
- グレン「それ俺のだろ」

### ② 特殊個体の試料回収(S3)
受注時
- グレン「また会ったな。実は頼みがある。理由は聞くなよ」
- ミラ「秘密だからね！　通常個体とは違う血液が、こっちの試料にどう反応するか――」
- グレン「……あっ、後ろに変異体だ、ミラ！」
- ミラ「わっ！　どこ！？」

完了時
- グレン「報酬はそうだなぁ……」
- ミラ「もう渡したよ！」
- グレン「また俺の……」

### ③ さらに希少な個体の試料回収(S4)
受注時
- グレン「よく会うな。追っかけか？　まあいい、頼みがある」
- ミラ「うぐ……めっちゃ怒られるから、理由は言えないの……」
- グレン「頼んだぞ」

完了時
- グレン「今日は俺の物を渡すなよ」
- ミラ「もう渡したよ！」
- グレン「……」

## サブクエスト欄への討伐目標表示(社長指示 2026-08-21)
- **城ボスアテンション後**(=受注→通信→城ボス出現の後)、サブクエスト欄の**一番上**に
  **[搬送体(変異)の討伐 0/1]** のように表示する。
- **メインクエストらしく色を変える**: 黄色など視認性のある色(通常サブクエと差別化)。叩き台=黄色。
- 討伐で 1/1(以降の消し方=完了通信との前後関係はv2改稿時に確定)。

## 実装マップ叩き台(裁定後に確定・監査へ)
- `useGameLoop.ts` 城ボスゲート(≈2712): `castleBossReady` に「クエストステージ未クリアなら
  受注済み&&通信終了」を AND(クリア済み/対象外ステージは従来条件)。二人組の5分遅延出現+
  [Help!]アテンション+S5の拠点カウントゲート。
- `gameStore.ts`: acceptEventQuest=城ボス起動要求へ変更(ネームド湧き廃止)・完了フック=
  giantbat(fromEventでない)討伐時にクエストステージなら完了通信+永続記録。
- `eventQuest.ts`: 設定テーブルにS5の`basesRequired: 2`・目標種別を`castle-boss`へ。
- 通信: 既存のステージ台本無線に受注時/完了時の2本を追加(本文は仮テキスト=社長が後で)。
- サブ「サンプル集め」廃止(裁定1): eventQuestのsub系状態・HUD表示・受注分岐を撤去。
  進捗の永続キー(eventQuestMeta.sub)は読み捨て(既存端末で害なし)。
- 報酬(裁定2): completeEventQuestに付与口だけ残す(当面は何も付与しない)。
