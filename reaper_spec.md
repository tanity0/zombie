# 死神システム仕様 v1 (reaper_spec)

> **この文書は設計時の姿です。現行仕様の正はコード+DEVELOPMENT_LOG。案件の状態は PROJECT_STATUS.md を見る。**

作成: 2026-06-16 JST / 出典: 社長提供仕様

## 目的
死神は、無限マップにおける深奥リスクを表現する敵/演出システム。マップを物理的に制限せず、スタート地点・商人から遠くへ行きすぎたときに「この先は危険／戻れなくなる／欲張ると死ぬ」圧を与える。**ライトな深奥警告**として扱い、BGM変更は不要。絵は既存の死神(召喚レアと同じ)を流用。

## 基本コンセプト
奥へ行く → 死神がたまに画面を横切る → さらに奥で横切り頻度↑ → 深奥に長居で完全出現 → プレイヤーを追跡。
通常はほぼ勝てないが、極まったビルドなら討伐可能性を少しだけ残す。

## 出現段階
0. 未発生(スタート/商人付近)— 出さない。序盤戦闘・商人安全圏を邪魔しない。
1. 横切り警告 — 一定距離で画面を横切る。**攻撃なし/ダメージなし**。黒シルエット・高速・残像・短いSE。追跡しない。
2. 横切り頻発 — さらに奥で頻度↑。方向ランダム可。まだ追跡・攻撃なし。
3. 完全出現 — 深奥滞在/一定距離/横切り回数/内部リスク値で判定(推奨=内部リスク値)。本物の敵に。
4. 追跡 — 高速で追う。接触ダメージ非常に高い。完全出現直後は即接触させない(猶予)。

## 距離・危険度の目安(5分ステージ)
0〜45秒圏: なし / 45〜75: 低確率横切り / 75〜105: 頻度↑ / 105〜135: 頻発 / 135〜: 完全出現リスク↑ / さらに奥: 出やすい。
実装はピクセル距離可。設計は「商人から何秒ぶん離れているか」で考える。

## 内部リスク値
phase: none|warning_pass|frequent_pass|spawned|chasing。risk, lastPassAt, passCount, spawnedAt。
増加=遠い/深奥滞在(v1はこの2つだけでよい)。減少=中心へ戻る。

## 仮パラメータ
```
REAPER_CONFIG = {
  warningDepthSec: 45, frequentPassDepthSec: 75, spawnRiskDepthSec: 105, extremeDepthSec: 135,
  passIntervalNearSec: 35, passIntervalMidSec: 22, passIntervalDeepSec: 12, passIntervalExtremeSec: 8,
  riskMax: 100, riskGainPerSecDeep: 0.35, riskGainPerSecExtreme: 0.75, riskDecayPerSecReturning: 0.25,
  spawnRiskThreshold: 100, spawnGraceSec: 1.0,
  contactDamage: 9999, moveSpeedMultiplier: 1.2, canBeKilled: true,
}
```
数値は仮、実プレイ調整。

## 追跡速度
`reaperMoveSpeed = currentPlayerMoveSpeed * 1.2`。currentPlayerMoveSpeed は**成長・強化・移動速度UPを反映した現在値**。
ただし**一時的ダッシュ/ノックバック/強制移動は参照しない**(瞬間的な異常加速を避ける)。

## 強さ/報酬
普通は逃げる対象。倒すには特殊状況。討伐報酬は高スコア/レアトレジャー/称号/実績/見た目系など。恒久バランス崩壊報酬(弾無限・恒久火力大幅UP等)は避ける。

## UI/演出(v1)
採用: 黒シルエット・残像・短いSE・完全出現時のみ短い警告SE・横切り頻度で危険度を伝える。
不採用: BGM変更・長いカットイン・大きな画面停止・強い警告UI・過剰ホラー。

## 実装注意
1. 横切り段階では殺さない(警告)。2. 完全出現直後に即死させない(猶予)。3. マップ端ではなく深奥リスク。4. 通常攻略の必須要素にしない(やり込み)。5. BGM変更なし。

## v1実装範囲
距離によるフェーズ管理 / 横切り警告 / 横切り頻度上昇 / 完全出現 / 追跡 / 接触ダメージ / BGM変更なし / 簡易SE / 討伐可能フラグ。
後回し: 専用BGM・複雑な討伐報酬・専用UI・専用カットイン・トレジャー取得によるリスク増加・ボス接近によるリスク増加。
