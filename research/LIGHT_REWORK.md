# 光の描写を全面的に改善する(社長指示 2026-08-01)

> ゲーム全体を通して、これらの光描写を**全面的に改善できるならやりたい**

きっかけは松明の修正(v0.25.2633)。実機スクショの比較で「松明台の外に出てきてるやつ。
**明らかにベタ塗りじゃん**」→ 原因は**均一 `fill` の円**と**減衰カーブの違い**だった。
同じ病気が他にもあるはずなので、全面的に洗って直す。

---

## 1. 何が「綺麗な光」を作っているのか(v0.25.2633で確定した知見)

M6(洋館の壁灯)が綺麗な理由は **減衰カーブ**。照り返しでも焼き込みでもない
(**照り返しはM6でも既に廃止済み** = v0.25.2149 の負荷対策)。

| 中心からの距離 | 共有 `getGlowTexture` | M6の壁灯 / `getSoftGlowTexture` |
|---|---|---|
| 0%   | 1.00 | 1.00 |
| 25%  | —    | **0.55** |
| 45%  | **0.55** | — |
| 60%  | —    | **0.18** |
| 100% | 0 | 0 |

- **共有カーブは45%でまだ0.55** = 中心付近が広く均一に濃い ⇒ **塊に見える**。
- **M6カーブは早く落ちて尾を引く** = 芯があって外へ消える ⇒ **光に見える**。

### 3つの禁止事項(これを守れば「ベタ塗り」は起きない)
1. **光を `Graphics.fill()` の均一円/楕円で描かない。** `fill` は中心から縁まで同じ濃さで、
   **縁で突然0になる**。加算合成すると必ず「縁のある塊」になる。
2. **薄く広く塗らない。** α0.18のような薄さは、**面積のコストを払いながら光って見えない**
   =一番損。**濃く・狭く**が正解(M6は0.85〜0.9)。
3. **半径に上限を設ける。** 近距離で大玉になると加算オーバードローがベンチのG12 FAIL帯へ入る
   (v0.25.2149の実バグ)。M6は画面高の11%でキャップ。

---

## 2. 対象の棚卸し(v0.25.2633時点の実測)

| 種別 | 件数 | 備考 |
|---|---|---|
| `getGlowTexture()` を使うスプライト | **18箇所** | 共有カーブ=塊寄り。置き換え候補 |
| 暖色系の均一 `fill`(光っぽい描画) | **53箇所** | 全部が光ではない。要選別 |
| `blendMode = 'add'` のスプライト | 56箇所 | 加算=塗り面積が効く |

`getGlowTexture()` の主な利用先(11130/11152/13815/13843-4/2657/2903/14728 ほか):
城のグロー / 商人 / イベントNPC / **プレイヤーのライト** / **足元の光だまり** /
ステージ5の炎 / 松明(修正済み) / 火炎瓶 / エフェクト各種。

---

## 3. 進め方(1つずつ・実機で見ながら)

**一度に全部やらない。**53箇所には「光ではない塗り」も混ざっているし、
**変えると印象が変わる**ので、社長が実機で見て良し悪しを判断できる単位で刻む。

| # | 対象 | 状態 |
|---|---|---|
| 0 | **松明** | **完了 v0.25.2633** |
| 1 | プレイヤーのライト + 足元の光だまり | **完了 v0.25.2660**(社長の実機確認待ち) |
| 2 | 火炎瓶の地面の火 / 焚き火(ステージ4) / ステージ5の炎 | 未 |
| 3 | 城 / 商人 / イベントNPC のグロー | 未 |
| 4 | 拾い物・エフェクト系の小さい光 | 未 |
| 5 | 均一 `fill` 53箇所の選別と置き換え | 未 |

各回で: **変更前後をオフラインでレンダリングして比較 → push → 社長が実機確認**。
(描画コードなのでユニットテストは付けない=CLAUDE.md テスト方針)

---

### 3-1. #1 でやったこと(v0.25.2660)
- `playerLight` / `playerGroundPool` を **共有 `getGlowTexture` → ソフト `getSoftGlowTexture`** へ。
- ★**総光量を保つ補正 `SOFT_CURVE_ALPHA_GAIN = 1.69` を掛けた。**
  面積で重み付けした平均α = `2∫₀¹ a(u)·u du` は 共有=**0.3333**(実は直線 a=1-u)/ ソフト=**0.1976**。
  比 0.593 の逆数が 1.69。**補正しないと「綺麗になったけど暗くなった」になり、
  形の良し悪しと明るさの変化が混ざって判断できなくなる。**
- **塗り面積(半径)は一切変えていない**=負荷は完全に据え置き。**負荷 1/10**(テクスチャの差し替えとα1回の乗算)。
- 実機の生調整は既存の `?pool=`(濃さ)/ `?poolr=`(半径)がそのまま使える。
- ★**社長実機 v0.25.2661「プレイヤーの光強すぎるな」→ 本体の補助光だけ係数を分けた。**
  ソフトカーブは**総光量が同じでも芯が濃くなる**ので、1.69を掛けると中心が元の1.69倍まぶしい。
  **プレイヤーは画面のど真ん中に常に居る**ので、そこだけは「総光量を保つ」より
  **元のピークの明るさを保つ**方が正しい ⇒ `PLAYER_LIGHT_ALPHA_GAIN = 1.0`(形だけ変わる)。
  足元の光だまりは `SOFT_CURVE_ALPHA_GAIN = 1.69` のまま(指摘は本体の光について)。
  生調整は `?plight=` を新設。
  **教訓: 「総光量を保つ」は常に正解ではない。画面の主役の近くでは"ピークを保つ"方が合う。**

### 3-2. ★#1 に光源DOF(§4-3)は**足さない**(調べて分かったこと)
足そうとしたが、**プレイヤーの光には効かない**ので見送った。
- `lightDefocus01` は**静的な帯**(`TILT_SHIFT_BAND = 0.54`)からの距離で 0..1 を出す。
- 一方、**実際の tilt-shift のシャープ帯はプレイヤーに毎フレーム追従する**
  (`pixiScene.ts:4625-4649`・社長指示v0.25.1758「プレイヤーは外して」)。**プレイヤーは常にピント。**
- ⇒ プレイヤーの光にDOFを掛けると、**ピントが合っているのにボケる**という逆の絵になる。
- **★提案(未実施・社長裁定待ち)**: この2つの帯が食い違っている件そのもの。
  松明のボケ量も静的な0.54基準で出しているので、**プレイヤーが画面の上下に動くと、
  実際のピント面と松明のボケ具合がズレる**。`lightDefocus01` を動的な帯(`bandFocalY`)基準に
  揃えれば一致するが、**松明の見え方が変わる**(v0.25.2635で社長が実機で通した絵)。直すかは社長判断。
- ⇒ 光源DOFの横展開先は **#3(城/商人/イベントNPC)・#4(拾い物)・ステージ5の炎**など
  **プレイヤーから離れた光**。そちらは効く。

## 4. ★相談メモ(社長・2026-08-01・後で着手)

### 4-0. 水たまりの表現(社長メモ 2026-08-01・後で相談)
未着手。**光と密接**(水は「映すもの」=光源の反射・環境の映り込みで見せる)なので、
このファイルに置く。相談時に確認すること:
- どこの水たまり?(ステージ/屋内屋外/常設か一時的か)
- 何を映すか(松明などの光源だけ / プレイヤーや敵も / 空・環境)
- 動くか(波紋=踏んだ時、雨、常時のゆらぎ)
※このプロジェクトの地雷: 映り込みは**画面全体のパス**にすると加算オーバードローで即死ぬ。
M6の壁灯と同じく「**焼いた素材+スプライト**」で成立させられるかを最初に検討する。

### 4-1. 物体の動き(木や花などが風にたなびく) — **完了 v0.25.2648**
`src/utils/windGust.ts`(時刻だけから決まる純関数)。**炎・花・木が同じ1本の風**を読む。
- 揃え方: `WIND_PHASE_SPREAD=0`(種を渡しても同じ値)+ 描画側はフレーム先頭で1回引いて使い回す。
- 強さ: `setWorldWindScale` / 場面ごとの値は `worldWindScaleFor`(屋内=0・雪原=1.3・他=1.0)。
- しなり量: 花0.20rad / 炎0.30rad / **木0.035rad**(木は当たり判定を持つので絵だけ動かさない)。

**次に足すもの(煙・雨・草・旗など)も `this.windNow` を読むだけ。新しい揺れの式を作らないこと。**

### 4-2. 被写体深度を**個体ごと**に掛ける(オクトパストラベラー方式) — **★見送り(社長判断 2026-08-01)**

> あー被写体深度重いならいいやべつに

**やらない。** 理由は下記のとおり**対象ごとにブラー版の素材を焼き直す**必要があり、テクスチャ枚数が
2〜3倍=メモリと起動ロードが増えるため。**再開したくなった時のために調査結果は残す**が、
勝手に着手しないこと。
※**光源側のDOF(4-3)は別件で、こちらは継続**(素材の追加が不要=タダ。下記参照)。

> 今はグラデーションに画面に掛かるものを、**個体・オブジェクト毎に掛かるように**するには。

**現状**: 画面全体に掛かるグラデーション(=距離に関係なく画面の位置で決まる)。
**目標**: **そのオブジェクトの奥行きに応じて**ボケる(近すぎ/遠すぎがボケる)。

**★このプロジェクトには既に答えがある。M6(洋館)が個体ごとのDOFを実装済み。**
- 柱・燭台などに **`-blur`(近距離7px)/ `-farblur`(遠方26px)** の**事前ブラー版テクスチャ**を用意し、
  **奥行きに応じてクロスフェード**している(`corridorLayer` の `dofNear/dofFar`)。
- **毎フレームのぼかし処理はゼロ**。ブラーは**素材を焼く段階**で済ませてある(`make-blur.mjs`)。
- ⇒ **per-pixel のブラーフィルタを使わずに個体ごとDOFが成立する。**負荷はスプライト2枚のα操作のみ。

**横展開の条件**: 対象ごとに**ブラー版の素材を焼く**必要がある(枚数×2〜3倍のテクスチャ)。
メモリと読み込み時間が増えるので、**対象を絞る**のが前提(例: 木・プロップ・遠景の大物だけ)。

### 4-3. 光源にもDOFを掛けられるか(社長質問「これを光源側も改善できるの？」)
**掛けられる。しかも光源は一番安い。**

> **★状態(2026-08-01 走査で確定): 松明だけ実装済み(v0.25.2635)。部品は全部揃っている。**
> 残りは「同じ処方を他の光へ配る」だけなので、**§3 #1 に同梱する**(同じ場所を触るため分けると二度手間)。
>
> | 部品 | 場所 | 中身 |
> |---|---|---|
> | ピント用カーブ | `lighting.ts:39` `getSoftGlowTexture()` | 0→1 / 0.25→0.55 / 0.6→0.18 / 1→0 |
> | ボケ用カーブ | `lighting.ts:76` `getBokehGlowTexture()` | 0→1 / 0.25→**0.72** / 0.6→**0.34** / 1→0(平坦) |
> | ピント外れ量 | `pixiScene.ts:3488` `lightDefocus01(worldY)` | `|screenY - screenH*TILT_SHIFT_BAND| / TILT_SHIFT_GRADIENT` を0..1にクランプ |
>
> 松明の使い方(`pixiScene.ts:7174-7191`)= **これが横展開の雛形**:
> `light.alpha = haloA*(1-focusT)` / `lightBokeh.alpha = haloA*focusT / R_MULT²`、
> ボケ側は半径 `×TORCH_BOKEH_R_MULT(1.6)`。**αを面積比で割って総光量を保つ**のが肝
> (割らないとボケた光が明るくなる=加算オーバードローの本丸へ直行)。
>
> **注意(走査で判明)**: 本編の光(プレイヤーのライト/足元の光だまり/松明/エフェクト)は
> `filteredWorld` の**内側**=既に画面全体の tilt-shift も掛かっている。松明は現に
> **画面フィルタ+自前のボケ切替の二重**。基準の帯は同じ(0.54)なので方向は矛盾しない。
> **松明が社長承認の絵なので、他の光にも同じ処方を配る**(揃わない方が事故る)。
> 一方 **`filteredWorld` の外**にある光は別扱いが要る: **ステージ5の炎グロー / M6の廊下 / ダンスUI**。

理由: **グローは既に放射グラデーション**なので、**ボケ = カーブを緩くして半径を広げる**だけで表現できる。
**専用のブラー素材を焼く必要が無い**(通常の素材と違い、光には「輪郭」が無いため)。

具体的には、奥行き `t` に応じて:
- **ピント位置**: `getSoftGlowTexture` のカーブ(25%で0.55 / 60%で0.18)+ 半径そのまま
- **ボケ位置**: **より平坦なカーブ**のテクスチャ(例: 40%で0.6 / 80%で0.25)+ **半径を1.3〜1.8倍**、
  **αは下げる**(総光量を保つ=ボケても明るさが増えない)

⇒ **焼くテクスチャは2枚だけ**(ピント用・ボケ用)。全光源で使い回せる。M6の柱のように
**素材ごとにブラー版を用意する必要が無い**。

**注意**: 半径を広げると**塗り面積が増える**(=加算オーバードローの本丸)。
**ボケ側は必ずαを下げ、半径キャップも掛ける**こと。ここを守らないと v0.25.2149 の再来になる。

**やる順序の提案**: **4-3(光源のDOF)を先に**やる方が良い。素材の追加が要らず、
**効果が一番大きい**(暗いステージでは光源が一番目立つ)。4-2(物体のDOF)は素材の焼き直しが要るので後。
