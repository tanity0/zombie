# トール技台本の改稿(社長指示 2026-08-20 + 裁定3件 2026-08-20)v2

> **この文書には未実装の設計が含まれます。実装済み部分の現行仕様の正はコード+DEVELOPMENT_LOG。案件の状態は PROJECT_STATUS.md を見る。**

★優先規則: **本書(v2)が正**。初版(v0.25.3675)の読み——「無の境地=一閃と同じ範囲の紫**ライン**」
「必中の引き金は**赤**予告中の近接」——は 2026-08-20 の裁定3件で置き換わっている。本書は
その裁定と設計監査11件を織り込んだ**発注前の確定仕様**で、旧記述は残していない。

---

## 0. ゴール(社長の言葉のまま)

**原指示(2026-08-20)**
> 「トールの技台本を変更。一閃は必ず以下台本に。
> 無の境地 300ms予告 何もしない。紫で一閃と同じ範囲のラインを書く。(しかし何も起きない)
> 一閃 500ms予告 赤ラインの今まで通りの一閃+この予告中に近接攻撃すると、即一閃が発動。つまり必中。
> 以下、突き の仕様変更
> 予告 1100ms 帯の長さ 300px 帯の幅 30px」

**追加指示(同日)**: 払いの予告を 600ms へ。連携に**「突き×3 → 払い×1」**を追加。
**追加指示(2026-08-21)**: トールに**突進(ミゲル型)を新設**し、その起点に**「突進→突き→突き」**を付ける
(→ §4。既存の起点台本は1本も上書きしない)。

**裁定3件(2026-08-20・設計監査の差し戻しに対する社長回答)**
1. **逆転**: 必中の引き金は**無の境地(紫)中の近接**。紫の間に近接を振ると**即一閃**が出て**必中**。
   **通常の一閃(赤)は従来どおりカウンターできる**(引き金は赤には置かない)。
2. **無の境地は円形判定・近距離**(紫のラインではなく**円**。半径は叩き台=実機で詰める)。
3. **突きの帯(300×30)を赤い予告として出す**(従来の「溜め中は予告ラインなし」を改める)。

**「ではない」条件(この設計が引き受けないもの)**
- ボスメーカーの見た目・UXは対象外(開発用であってプレイヤー用ではない)。
- トールの**既存の動き**(旋回距離216 / 接近後退43.5 / バックステップ / 旋回ステップ / ゆっくり歩き /
  ジャンプの発火条件)は本件の対象外=**1つも触らない**。
- 報酬・ドロップ・HP・ダメージ量の調整は対象外。

**経緯(事実として併記・制約ではない)**
- v0.25.1626頃の裁定「覚えられる上限=コンボ2組のみ・**突き起点の連携は作らない**」(thorScript.ts:16-17)。
  → 2026-08-21の裁定で**新技「突進」の起点**に置く形になったため、**突き起点は上書きしない**(§4)。
- v0.25.3461の裁定「一閃、もう少し発動早くていい」で溜め 3000→2400ms。
  → 本指示で**300ms(紫)+500ms(赤)=800ms**へ。反応時間は§8-1で**裁定済み**(500msのまま実機)。
- v0.25.1621の社長指示「突きの溜め中は方向が未確定=**予告ラインなし**」。→ 裁定3で改める。

---

## 1. 一閃の台本(2段固定)

一閃は**毎回この2段**で出る。段の間に空き時間を置かない(連続)。

```
[段1] 無の境地 300ms  紫の円(近距離)・何もしない・カウンター不可
        └─ 円の中でプレイヤーが近接を振った ⇒ 即・段2の実行へ(必中一閃)
[段2] 一閃     500ms  赤の帯(310×160)・従来どおりカウンター可
        └─ 予告満了 ⇒ 従来のダッシュ(dashMs 280)→ issen-recover
```

### 1-1. 段1「無の境地」(紫・円・近距離)

| 項目 | 値 | 種別 |
|---|---|---|
| 州名(bossState) | `issen-nihil` | 確定(§5-1に理由) |
| 予告時間 | **300ms** | 社長指定=確定 |
| 判定の形 | **円**(中心=トールの当たり判定矩形の中心) | 裁定2=確定 |
| 半径 | **200px** | **叩き台** |
| 色 | 紫(既存のミーミル・レーザーと同じ紫系: 塗り `0x9333ea` / 縁 `0xc084fc`) | 確定(文法) |
| ダメージ | **なし**(この州はプレイヤーに一切ダメージを与えない) | 確定 |
| カウンター | **不可**(紫の文法。体が重なっていても成立させない) | 確定 |

- 半径200pxの根拠(叩き台): ナイフの間合い `MELEE_RADIUS=74` + トールの体の半幅70 ≒ 144、
  鞭の射程150 + 半幅70 ≒ 220。**「殴れる位置=紫の中」**にしたいので、その間を取った丸い数字。
  実機で「近づいたら振れない」圧が出る値へ寄せる。
- **円は「振るな」の合図**であって立ち入り禁止ではない。中に居るだけでは何も起きない。
- **赤いフラッシュを出さない(監査⑧)**: `thorFlashTint`(`pixiScene.ts`)は
  「ダメージの瞬間の**400ms前**から体を赤く鋭く点滅」させる演出。紫の段は**300ms**しかないので、
  同じ式を通すと**紫の間じゅう体が赤く光る**=「赤=危険/カウンター対象」の文法と真っ向から食い違う。
  よって **`issen-nihil` では `thorFlashTint` を呼ばない**。体のティントは**紫**(`0xc084fc` 寄り)を
  使い、赤フラッシュは**段2(赤予告)の残り400ms**からだけ出す(=現行の意味「ダメージの瞬間の前」を保つ)。
- **慣性(MUST)**: 円は出現時に半径 0→1 を ease-out(叩き台120ms)で開き、消える時は ease-in で
  縮めながらフェードする。**パッと出てパッと消えるのは禁止**(CLAUDE.md「動きの絶対ルール」)。
  トール本体は静止ではなく**息を詰める**予兆(既存 `windupTremorPx` の震えを紫段でも使う=
  型を発明しない)。
- **動きは大きく**: 円の縁は既存の帯予告(`drawTelegraphBand`)と同じ濃さ以上で描く。薄い紫は
  「出ていない」のと同じ。

**受け入れ条件(段1)**
1. `issen-nihil` 中に**プレイヤーがどのような接触をしても**ダメージが発生しない(体の重なりも含む)。
2. `issen-nihil` 中に**カウンターが成立しない**(近接を振っても青いCounter!演出・反撃ダメージ・
   counter-leap のいずれも出ない)。→ 出る場合は§5-2の配線漏れ。
3. 描いてある紫の円の半径と、必中の引き金に使う円の半径が**同じ1つの定数**から来ている
   (`HIDDEN_THOR_TUNING.issen.nihilRadius`)。片方だけ動く実装は不可。
4. 300ms 経過で必ず段2(`issen-windup`)へ進む。ここで `chase` へ戻る経路が存在しない。
5. 円の出現・消滅に加減速がある(1フレームで最大サイズにならない)。
6. **紫の300msの間、体が赤く光らない**(`thorFlashTint` が呼ばれていない)。赤フラッシュは
   段2の残り400msからのみ出る。

### 1-2. 段2「一閃」(赤・500ms・従来のダッシュ)

| 項目 | 値 | 種別 |
|---|---|---|
| 州名 | `issen-windup` → `issen-dash` → `issen-recover`(既存のまま) | 確定 |
| 予告時間 | **500ms**(旧2400) | 社長指定=確定 |
| ダッシュ | `dashMs 280` / `range 310` / `halfWidth 80` / `recover 900` | **不変**(触らない) |
| 予告の絵 | 従来どおり赤の帯+流星の描き→消し(`meteorPhase`+`drawTelegraphBand`) | 確定 |
| カウンター | **従来どおり可**(裁定1) | 確定 |

- 狙いのロックは**段1の開始時**(`beginThorMove('issen')` と同じ処理)に1回だけ行い、段2で
  ロックし直さない。段1の300msも含めて「もう振り向かない」=居合の型を保つ。
- **段2に入った瞬間、赤の帯が0からではなく既に張り詰めた状態で出る**のは避ける。既存の
  流星文法(`prog` 0→1)をそのまま500msへ縮めて使う=絵の入りに慣性が残る。

**受け入れ条件(段2)**
1. 赤い帯の図形と `issen-dash` の当たり判定(`distToBandRect` 310×80)が**一致**する
   (現行の一致を壊していない)。
2. 予告500ms・ダッシュ280ms・硬直の値が台帳(`HIDDEN_THOR_TUNING`)から読まれている
   (pixiScene 側に数値の複製が増えていない)。
3. `issen-windup` 中のカウンター成立が**赤帯で**起きる(§8-2の裁定=帯へ揃える。他ボスと同じ宣言表に乗せる)。

### 1-3. 必中一閃(紫の円の中で近接を振った時)

**引き金**: 州が `issen-nihil` の間に、**プレイヤーが近接攻撃を振り**、その瞬間に
**プレイヤーの中心がトールの紫円の内側**にあること。

**結果**: 残りの紫予告と**段2の赤予告500msを丸ごと捨てて**、その場で `issen-dash` へ入る。
狙いは**引き金の瞬間のプレイヤー中心**へ**取り直す**(段1でロックした向きは使わない。
振ってきた相手を斬るため)。ダッシュ自体(280ms・310px・半幅80)は通常の一閃と同じ。

- **カウンター不可(★配線を明示する。宣言だけでは破れる)**: 即発動の `issen-dash` は紫の文法を
  引き継ぐ=この一撃はカウンターできない。プレイヤーは**近接の振り硬直中に斬られる**(=「つまり必中」)。
  **ところが現行のままでは逆に「ほぼ確実にカウンターされる」**——実在確認した事実:
  - `issen-dash` の被弾解決(`useGameLoop.ts` 6311-6317)は**帯に触れた時点で
    `Date.now() <= cp.counterWindowEnd` を見て、開いていればカウンター成立**にする
    (windup州だけにカウンター経路がある、ではない。**実行中の帯にも経路がある**)。
  - 引き金である近接スイングの確定は、**同じ tick で `counterWindowEnd` を開く**
    (gameStore 6775=ナイフ / 6304=刀 / 6330=鞭)。
  - つまり「振った瞬間に即発動」させると、**その振りが開けた窓で必ず弾かれる**=必中の逆。
  - **確定仕様**: 即発動時に `Enemy.issenGuaranteedUntil`(= `issen-dash` の終了 gameTime)を立て、
    立っている間は **6313の `counterWindowEnd` 分岐をスキップして `damagePlayer` へ直行**する。
    ゴースト/守護霊側の帯ヒット(`applyGhostAllyCapsuleHit`)は現行どおり(対プレイヤーの
    カウンター経路だけを閉じる)。通常の一閃(赤経由)では**このフラグを立てない**
    =従来どおりカウンターできる(裁定1)。
  - フラグは `issen-dash` を抜ける時(recover/chase/カウンター以外の中断)に**必ず落とす**
    (立ちっぱなしで次の一閃までカウンター不能にしない)。
- **「必中」の実装は判定を消すことではない**。狙いを振った相手へ取り直すので、近接の間合い
  (紫円の内側)に居る相手は帯の中に入る=**幾何的に当たる**。当たり判定は従来の帯のまま
  (見たまま=帯の外の第三者には当たらない)。
- **慣性(MUST)**: 即発動でもダッシュの加減速カーブは通常の一閃と同一。ワープに見せない。
- **1回だけ**: 1つの `issen-nihil` から即発動できるのは1回(2重発火の防止)。
- 演出: 紫の円は即発動と同時に**縮めて消す**(ease-in)。斬撃そのものの絵は既存の一閃と同じ
  (`latchFx` 経由で最後まで描き切る)。

**「近接を振った」の定義(監査⑦への回答=確定)**
- 判定に使うのは**その tick でプレイヤーが近接スイングを確定させた事実**であり、
  `player.meleeSwingAt` を**そのまま読んではいけない**。理由(実在確認済み):
  - `markMeleeSwingFx()` が `meleeSwingAt` を書く経路は**9箇所**(+ useGameLoop 2413 の
    アダプタ受け渡し1件)。内訳: **カウンター成立=7箇所**(useGameLoop 5502=トール /
    5566=裏ボス3体 / combatTick 312・748・1189 / angelBossTick 231=天使 / bountyTick 395=賞金首 /
    idolTick 311=アイドル ※この7つは全部「青いCounter!演出」の一式)、
    **追撃=1箇所**(gameStore 4139=スラッシャー追撃)。
    そのまま読むと「**カウンターしたら必中一閃が出る**」という誤発火になる。
  - `counterWindowEnd` の打刻も引き金に使えない。**武器庫サークルでショップを開いた時**にも
    同じフィールドが開く(gameStore 6278)=近接を振っていないのに引き金が引ける。
- **採る形(規則)**: **プレイヤーの近接スイングが確定する箇所すべて**に、**専用の打刻**
  (`player.meleeSwingCommitAt`)を1本立てる。**行番号で列挙した4件は現時点の実例であって
  仕様ではない**——将来ナイフ系の武器や新しい振り方が増えたら、**そこにも打つのが規則**
  (打ち忘れは「新しい武器だけ必中一閃が出ない」という無音の穴になる)。
  - 現行の実例4件: ナイフ=`gameStore` 6775 / 刀=6304 / 鞭=6330 / スラッシャー追撃=4139経路。
  - **打たない**: ショップ経路(6278)/ カウンター成立の演出(上の7箇所)/
    守護霊・分身・幻影の攻撃(社長の言葉は「近接攻撃すると」=プレイヤーの操作)。
  - **経路数を固定する純関数テスト**を同コミットで置く: 打刻を書く関数を1本の純関数
    (例 `stampMeleeSwingCommit`)にまとめ、**それを呼ぶ箇所の数**をテストで固定する
    (ソース走査 or 呼び出し口の集約 + 件数アサート)。増減したらテストが落ちて
    「新しい近接に打刻を足したか?」を人に問う=`ghostTelegraph` の完全性検査と同じ型。
- **時計を混ぜない(実バグ級)**: 打刻は `Date.now()` 系、州の残り時間(`bossStateUntil`)は
  `gameTime` 系。**同じ式で引き算しない**。引き金は「`meleeSwingCommitAt` が前フレームから
  進んだか」のエッジで見る(絶対時刻の比較をしない)。同型の事故が v0.25.3721(フィル+トール)で
  出ている。

**受け入れ条件(必中一閃)**
1. 紫円の内側でナイフ/刀/鞭/スラッシャー追撃を振ると、**その tick で** `issen-dash` に入る。
2. 紫円の**外側**で振っても発動しない(300ms満了後に通常の赤予告へ進む)。
3. **カウンター成立(青演出)では発動しない**(`markMeleeSwingFx` 経由の打刻を読んでいない証明)。
4. **武器庫サークルでショップを開いても発動しない**。
5. 即発動した一閃は**カウンターできない**。根拠は「windup を経ていないから」**ではない**
   (`issen-dash` の実行中の帯にも `counterWindowEnd` を見るカウンター経路がある=6311-6317)。
   **`issenGuaranteedUntil` が立っている間、その分岐をスキップして `damagePlayer` へ直行する**
   ことで閉じる。テスト: 紫円の中で振った直後にカウンター窓が開いていても、
   青いCounter!演出・反撃ダメージ・counter-leap が出ずに**プレイヤーが被弾する**。
9. 通常の一閃(赤予告を経たもの)は `issenGuaranteedUntil` が立たず、**実行中の帯でも
   従来どおりカウンターできる**(裁定1の後半を壊していない)。
10. `issen-dash` を抜けた後に `issenGuaranteedUntil` が残っていない(次の技がカウンター不能に
   ならない)。
6. 引き金の瞬間にプレイヤー方向へ狙いが取り直され、帯がプレイヤーを含む。
7. 1つの `issen-nihil` で発動は**1回だけ**(同フレームに2回 `issen-dash` を書かない)。
8. ダッシュの速度カーブ・所要時間は通常の一閃と**同一**。

---

## 2. 突き(tsuki)

| 項目 | 現行 | 新 | 種別 |
|---|---|---|---|
| 予告 | 1000ms | **1100ms** | 社長指定=確定 |
| 帯の長さ(range) | 240 | **300px** | 社長指定=確定 |
| 帯の幅 | 全幅30(halfWidth 15) | **全幅30(halfWidth 15=現行のまま)** | 確定 |
| 実行(ms)/追従(trackFrac)/硬直 | 180 / 0.5 / 600 | **不変** | 触らない |
| 溜め中の予告 | **なし** | **赤の帯 300×30 を出す**(裁定3) | 確定 |

**赤帯の描き方(裁定3の実装)**
- 帯は**その瞬間の狙い**から組む: 始点=トールの中心、終点=`(中心) + (遅延追従中の狙い点への
  単位ベクトル) × range(300)`。**狙い点そのものを終点にしない**(狙い点はプレイヤー位置=
  射程より近い/遠いことがあり、実行時に作られる帯とズレる)。
- 溜め中は `trackFrac 0.5` で狙いが**遅れて追う**ので、帯も**毎フレーム描き直す**
  (=見たままが「今の狙い」)。実行(`tsuki`)に入った瞬間に方向が確定し、以降は固定。
- 既存の一閃/払いと同じ**流星の描き→消し**(`meteorPhase`+`drawTelegraphBand`)で統一する。
  新しい意匠を発明しない。
- 400ms前の体の赤フラッシュ(`thorFlashTint`)は現行どおり残す。

**受け入れ条件(突き)**
1. 溜め中に描かれる帯の矩形と、実行(`tsuki`)の当たり判定の矩形が**同じ式**で作られている
   (長さ=`tsuki.range`、半幅=`tsuki.halfWidth`、始点=ボス中心)。
2. 溜め中に狙いを動かす(プレイヤーが横へ走る)と、帯も遅れて追う。**追従が止まった瞬間=実行**。
3. 帯の長さ300・全幅30が台帳の値からのみ来ている(pixiScene に 300/15 の直書きが無い)。
4. ボスメーカーで `tsuki.range` を動かすと、**予告の絵も判定も同時に**変わる。
5. 予告1100ms・実行180ms・硬直の時間は変わっていない(実行と硬直は本件の対象外)。

---

## 3. 払い(harai)

| 項目 | 現行 | 新 |
|---|---|---|
| 予告 | 1000ms | **600ms** |
| 実行220 / range 310 / halfWidth 40 / 硬直700 | — | **不変** |

**受け入れ条件**: 予告が600msになっていること。他の値・絵・カウンター条件が変わっていないこと。

---

## 4. 新技「突進」と、その起点の連携「突進 → 突き → 突き」

**社長裁定(2026-08-21)**: 「突進持ってないんだっけ? 突進足して。ミゲルと同じ様なやつ。で、突 突 を付けて。」

**この裁定で§8-3の問題は消滅した**。当初案は「突き起点の台本を新しくする」形だったが、トールの起点は
**既に4本すべて埋まっている**(実在確認 `bossChoreography.ts:45-48`: `issen`/`tsuki`/`harai`/`jump`)ため、
新台本を入れるにはどれかを潰す必要があった。**新技=新起点を足せば、既存4本を1本も触らずに済む**
——社長が避けていたのはこの上書きである。

### 4-1. 新技「突進」(dash・ミゲル型)
- **形**: ミゲルの突進と同じ(溜め → 直進で突っ込む → 打撃 → 硬直)。
- **予告**: **赤の流星ライン**(突進系の既定文法=裏ボスダッシュ/ミゲル踏み込みと同じ作り)。**カウンター可**。
- **カウンター成立時**: **来た方向へ弾き返す**。既存の `dashCounterPushbackPx: 150`(`angelScript.ts:54`・
  ミゲル/ウリと共有の合流点)を**そのまま使う**=新しい機構を作らない。
- **数値(叩き台=ミゲルと同値・`angelScript.ts:94`)**: windup 700 / moveMs 230 / strikeMs 110 /
  recover 800(withRecoverFloor) / cdMs 6000。**トールは体格も間合いも違うので実機で調整する前提**。

### 4-2. 起点台本
**追加**: `SCRIPTS.thor.dash = ['dash', 'tsuki', 'tsuki']`
**既存4本(issen / tsuki / harai / jump 起点)は一切変更しない。**

**3手なのでキュー上限の変更は不要**: `trimForPhase` は `moves.slice(0, phase<=1 ? 2 : 3)` で
Phase1=2手・Phase2以降=3手。この台本は**枠内に収まる**ので、`FIXED_SCRIPTS` のような
「長さを削らない宣言」の仕組みも、フェーズ条件も**足さない**(当初案の4手前提は撤回)。

**確定**: 連携の台本は **`bossChoreography.planBossChoreography` が正本**(監査②)。
`thorScript.ts` の `pickThorCombo` / `thorComboChance` / `THOR_COMBO_NEAR_MAX` は
**どこからも呼ばれていない死にコード**(実在確認済み: import しているのは `pickThorMove` と
`thorPhaseForHealth` だけ)。**この3つと対応するテストを削除**し、連携表を1箇所に保つ。

**受け入れ条件(突進+連携)**
1. 突進が**単発でも台本でも**出る(`pickThorMove` の抽選に加わる)。
2. 突進の**赤ラインが判定と厳密に一致**する(CLAUDE.md 攻撃ヴィジュアルの2分類①)。
3. 突進をカウンターすると**来た方向へ弾き返る**(既存 `dashCounterPushbackPx` 経由・新定数を作らない)。
4. `planBossChoreography('thor','dash',phase)` が **`['dash','tsuki','tsuki']`**(Phase1では先頭2手)を返す。
5. **既存4起点と他ボスの台本が1文字も変わっていない**(既存テストが無改修で通る)。
6. **同じ技(tsuki)が連続する台本**をキュー処理・`trimForPhase` が正しく扱う(重複を潰す実装に
   なっていないかを**実在確認**してから実装する)。
7. 連携中の突きも**それぞれ1100msの予告(赤帯つき)**で出る。テンポを変えない。
8. 連携中の硬直は既存の `choreographyRecoverMs`(次があるなら最大300ms)に従う=新しい値を作らない。
9. `pickThorCombo` 系の死にコードとそのテストが消えている(残したまま新表を足さない)。
10. 必中一閃(`issenGuaranteedUntil`)とは独立=突進は通常どおりカウンター可。

---

## 5. 配線表(★ここを抜かすと「動いているのに見えない/効かない」)

新しい州 `issen-nihil` を足すと**触れる場所は1つではない**。監査で挙がった経路を全部並べる。
**全部やって1バッチ**(1つでも抜けると静かに壊れる)。

### 5-1. 州名の決定(確定)

**`issen-nihil`(接尾辞 `-windup` を付けない)**。理由:
- `-windup` を付けると `isBossCounterableNowApprox`(bossScript.ts:197・**語尾で判定**)が
  **「カウンターできる」と誤答**する。これを読んでいるのは `playerTraits`(プレイヤーの機会計測)と
  守護霊/ゴーストの疑似反応。紫の州を「機会」と数えさせない。
- `botSkill.ts:321` の帯脅威も **`bs.endsWith('-windup')`** で拾う。紫は帯ではなく円なので
  誤った帯を避けようとする。
- `moveCancelGuard.parseMovePhase` では接尾辞なし=`active` 扱いになるため、
  `issen-nihil → issen` の遷移は**申告が要る**(§5-7)。

### 5-2. カウンター(監査⑤・実バグ級)

- **トールは `counterReach.ts` の宣言表を通らない**。`hiddenBossCounterableNow` が
  `boss.type !== 'thor'` で除外しており(useGameLoop 5814)、トールは各州のハンドラが
  `thorBodyOverlapNow()` を直接呼ぶ形。**だから `COUNTER_REACH_DECL` に 'none' と書いても効かない。**
- **やること①(紫の州)**: `issen-nihil` のハンドラに**カウンター判定を書かない**
  (`thorBodyOverlapNow` を呼ばない)。加えて `COUNTER_REACH_DECL` に
  `'hidden:issen-nihil': 'none'` を**記録として**足す(テストの完全性検査と、後任が表を見て
  判断できるようにするため)。
- **やること②(必中ダッシュ・★致命の穴)**: **カウンター経路は windup 州だけではない**。
  `issen-dash` の被弾解決(useGameLoop **6311-6317**)が帯ヒット時に `counterWindowEnd` を見て
  成立させるため、**引き金の振りが開けた窓で必中一閃が弾かれる**(§1-3)。
  `Enemy.issenGuaranteedUntil` を立て、**立っている間だけ 6313 の分岐をスキップして
  `damagePlayer` へ直行**する。閉じるのは**対プレイヤーのカウンターのみ**
  (`applyGhostAllyCapsuleHit` は現行どおり)。通常の一閃では立てない。
  ダッシュを抜ける時に必ず落とす。
- **★やること③(突進の走り・v0.25.3785 で追加)**: `thor-dash-move` を**宣言表と実行中州の一覧へ載せる**
  (`'hidden:thor-dash-move': 'body'` / `HIDDEN_COUNTER_ACTIVE_STATES`)。裏ボスの突進は実行中州
  (`'dash'`)が載っているのに、トールの走りだけが表の外で生の `rectsOverlap` で解決されていた
  =§5-2の狙いから外れる唯一の州だった。宣言は裏ボスと同じ `'body'` なので**成立域は1pxも変わらない**。
  成立時の反応だけ、突進専用の層(`thorDashCounterHit`=弾き返しを足す)を通す。
  - **併記の事実(挙動が1つ増える)**: 表へ載せたことで、**守護霊(ゴースト)も走行中の突進を
    カウンターできる**ようになる(旧実装はハンドラ内の判定=プレイヤー専用だった)。
    裏ボスの突進(`'dash'`)とミゲル(どの州でも取れる)は既にそうなっているので、**他ボスへ揃う**方向。
  - **併記の事実(`?bosscounter=0` の扱い)**: 走行中のカウンターも共通ブロックの
    `BOSS_COUNTER_ENABLED` ゲートに入る(旧実装のハンドラ内判定はこのデバッグフラグを無視していた)。
- **★やること④(受け流しに横取りさせない・v0.25.3785)**: `combatTick` の**ボス接触受け流し**
  (`BOSS_CONTACT_PARRY_ENABLED && counterActiveNow && isHiddenBoss(...)`)に
  **`thor-dash-move` を落とさない**除外を足す。理由=**競合順序が固定で不利**だから:
  ボスtickは**フレーム頭の座標**で重なりを見て、その後に座標を進める/`applyContactDamage` は
  **進めた後の座標**で見る ⇒「突進が到達したフレーム」は必ず受け流しが先に取る。受け流しは
  `rootUntil=900ms` を立て、`frozen` がそれを拾って `bossState='chase'` にするので、**突進の
  カウンター(Counter!/クリ反撃/counter-leap/弾き返し/専用CD)が全部出ない**。
  走行中は**接触ダメージだけを通し**、カウンターの解決はトール側の1本に残す。
  ※「ボスtick側の重なり判定を patch 適用後の座標へ切り替える」案もあるが、**フレーム内の順序に
  依存しない**この案を採る。
- **★弾き返しの効かせ方(v0.25.3785・重大D)**: 流用元のミゲルは弾き返し後 `bossState='chase'` へ戻す
  =**押された位置がそのまま最終位置**になる。トールは全技共通の反応(`counter-leap`)を積むので、
  **弾き返し位置を `aiTarget`(=跳びの到達点)へ入れる**ことで最終位置をミゲルに揃える
  (旧実装は `aiFrom` だけを差し替えていたため、**到達点が弾き返しの有無で1pxも変わらなかった**)。
  座標を直接書くと150pxの1フレームテレポート=**慣性MUST違反**になるので、**patch.x/y は書かない**
  (counter-leap の補間が運ぶ)。
  - **★未決へ移した(v0.25.3799・検収監査6巡目 重大3)**: `counter-leap` の補間は現状**等速**
    (トールの全技共有)。1フレームのテレポートは解消したが、**加減速は無い**=慣性MUSTを
    満たしていない。**本バッチの目玉である150pxの弾き返しは、この等速補間で運ばれる。**
    → **裁定は §9-10**(案と推薦はそちら)。
    ※v0.25.3793 まではここに「この案件の中では動かさない」と書いていたが、それは
    CLAUDE.md「★過去の裁定は『制約』ではなく『事実』」が禁じる**再検討の芽を潰す書き方**だったので
    撤回した。**社長が選べる形(§9-10 の(a)/(b)/(c)+推薦)で一覧へ出す。**
- 受け入れ条件:
  1. 紫の300ms中にカウンター窓を開けて体当たりしても、青い成立演出が出ない。
  2. 紫円の中で振って即発動した一閃は、**窓が開いていてもカウンターされずプレイヤーが被弾する**。
  3. 赤予告を経た通常の一閃は、実行中の帯で**従来どおりカウンターできる**。
  4. **★やること③④(走行中の突進)の受け入れ条件**——走行中(`thor-dash-move`)に
     カウンター窓を開けてトールの体を当てると:
     - (1) **青い Counter! 演出+クリティカル反撃**が出る(受け流しの淡い青リングだけで終わらない)。
     - (2) **ボスが来た方向へ下がる**(`counter-leap` の到達点が弾き返し位置=`dashCounterPushbackPx` 150px ぶん)。
     - (3) **`thorDashReadyAt` が立つ**=突進の専用CD(`dash.cdMs`)が入り、**連発できない**。
     - (4) **受け流し(体幹削り+`rootUntil` 900ms)は起きない**(=`bossPosture` が減らず、
       `bossState` が受け流し経由で `chase` へ落ちない)。**(1)〜(3)が出るのは(4)が起きないことの結果**なので、
       4つは1セットで確認する。
     - **※注記1(既知・裁定待ち)**: **守護霊(ゴースト)が取った場合の(2)の行き先は §9-6c の裁定待ち**。
       現状はプレイヤー中心基準なので、守護霊がプレイヤーから離れた場所で取ると
       「来た方向へ下がる」ではなく**プレイヤーの手前150pxへ寄る(位置によっては前進する)**。
       **これは不合格ではない**——(2)は**プレイヤーが取った場合**で確認する。
     - **※注記2(既知・裁定待ち)**: **成立の1フレーム前に接触ダメージを1発もらうのは §9-6b の裁定待ち**
       (フレーム内の順序=ボスtickはフレーム頭の座標で重なりを見る)。現状は**入る**。
       **これも不合格ではない**——(1)〜(4)の確認は被弾の有無と切り離して見る。
     - `?bosscounter=0` では **(1)〜(3)が出ない**(共通ブロックの `BOSS_COUNTER_ENABLED` ゲートに入ったため)。
       この時に走行中の体当たりが接触ダメージになることは仕様どおり。
       - **★併記の事実(v0.25.3798・低7)**: `combatTick` の `thorDashRunNow` 除外は
         **`BOSS_COUNTER_ENABLED` を見ていない**(`combatTick.ts:1110-1111`)。よって `?bosscounter=0` の時、
         **`thor-dash-move` はカウンターも受け流しも無い唯一の州**になる(他の裏ボスの `'dash'` は
         共通カウンターだけが消え、**受け流しは残る**)。`?bosscounter=0` は**デバッグ用途のみで
         実プレイには出ない**ため、**コードは触っていない**(事実の記録)。

### 5-3. ゴースト/ボットの予告台帳(`ghostTelegraph.ts`)

- 台帳には**完全性テスト**がある(ソースを走査し、未分類の州があると落ちる)。
  `issen-nihil` を**円(または「予告ではない」)として登録**する。帯として登録しない。
- 受け入れ条件: `ghostTelegraph.test.ts` が緑。かつ守護霊が紫の円を「避けるべき帯」と誤認しない。

### 5-4. ボットの回避(`botSkill.ts`)

- 紫の円は**立っていても安全**なので、**回避脅威には足さない**(円に入ること自体は避けさせない)。
- **裁定済み(§8-4・社長「マスターとスキルドは覚える」)**: `master` / `skilled` の2段だけ
  「**紫の円の中では近接を振らない**」を学習する。`novice` / `casual` は従来どおり踏んで食らう。
  ダイヤルは `BotSkillProfile` に1つ足す(`seesBossCounterPhases` と同じ段別の真偽ダイヤル)。
  **詳細と受け入れ条件は§8-4**。
- **必中ダッシュは既にボットに見えている(妥当・変更不要)**: `botSkill.ts:321` は
  `bs === 'issen-dash'` を**明示リストで**帯脅威に入れているため、即発動の一閃も
  (語尾ルールではなく名指しで)帯として拾われる。実際に判定を持つ帯なので**そのままで正しい**。
- 受け入れ条件:
  1. `issen-nihil` によって既存の回避ベクトルが変化しない(**円が帯として拾われていない**)。
  2. 即発動の `issen-dash` は従来どおり帯脅威として拾われる(`botSkill.ts:321` の明示リストを
     壊していない)。

### 5-5. 技への反応表(`moveReaction.ts`・G4a計測)

- 掟は「**その技の全フェーズを同じキーへ寄せる**」。`THOR_STATE_TO_MOVE` に
  `'issen-nihil': 'thor-issen'` を足す(足さないとエピソードが途中で閉じ、**1つの一閃が
  2回に数えられる**)。
- 受け入れ条件: 紫→赤→ダッシュ→硬直が**1エピソード**として数えられる。

### 5-6. 型と描画

- `types/game.ts` の `bossState` ユニオンへ `'issen-nihil'` を追加(コメントの技一覧も更新)。
  併せて `Enemy` に **`issenGuaranteedUntil?: number`**(§5-2やること②・単位=`gameTime`)を追加する。
- `Player` に **`meleeSwingCommitAt`**(§1-3・単位=`Date.now()`)を追加。**単位をフィールド名の
  コメントに書く**(時計混在の再発防止=v0.25.3721と同型の事故対策)。
- `pixiScene.ts` に紫円の描画を追加。**色は既存の紫**(`0x9333ea`/`0xc084fc`)、
  意匠は既存の円予告(城ボスの着地円/ミーミルの噛みつき円)に揃える。
- 受け入れ条件: `?zoomlock=0.4`(最大引き)でも円が欠けない・消えない。

### 5-7. 技キャンセル規則(`moveCancelGuard.ts`)

- `ALLOWED_MOVE_CHAINS` に **`'issen-nihil -> issen'`** を理由つきで申告する
  (「社長指示: 一閃は必ず2段の台本。段1は攻撃ではなく、300msを使い切った時にだけ段2へ渡す」)。
- **必中一閃(段1→`issen-dash`)は申告不要**(`issen-nihil` は `active` 扱い、`issen-dash` は
  `-windup` ではないので違反にならない)。ただし**実装後に観測器で確認する**。
- **★命名規約の唯一の例外を残した(v0.25.3793・検収監査 低9)**: 突進の実行中の州名は `thor-dash-move` で、
  `parseMovePhase` はこれを `move='thor-dash-move'` / `phase='active'` と解釈する(`-move` は既知の
  接尾辞ではない)。**溜め `thor-dash-windup` / 硬直 `thor-dash-recover` は `move='thor-dash'` なので
  技名が割れる**——他の全実装は実行中を `<move>`(接尾辞なし)で書いており、**この州だけが例外**。
  今のところ**誤検知も見逃しも出ない**(違反判定は「次が windup か」だけを見るので、
  windup→active / active→recover は割れていても引っかからない)。**州名を変える方が参照箇所が多く
  危険**(store/描画/カウンター宣言表/botSkill/moveReaction 等)なので、**規約の例外として残す**方を採り、
  `moveCancelGuard.parseMovePhase` のコメントに注記した。**新しい技でこの形を真似しない。**
- 受け入れ条件: `moveCancelGuard` の観測でトールの違反が0。

### 5-8. `?thorscript=0` 経路(監査⑩)

- `THOR_SCRIPT_ENABLED=false` の時、現行は硬直を作らず即 `chase` へ戻る(退避経路)。
- **決め**: **紫の段は `?thorscript=0` でも出す**(技の見た目そのものなので、台本フラグとは別)。
  必中一閃も出す。台本フラグが切るのは**硬直と連携キュー**だけ、という現行の切り分けを保つ。
- 受け入れ条件: `?thorscript=0` で紫→赤→ダッシュが出て、硬直に入らず `chase` へ戻る。

### 5-9. ボスメーカー(監査⑪)

- **▸の粒度は「一閃=1本」のまま**(`th-issen` を押したら**紫から**再生される)。
  紫だけを単独再生するボタンは作らない(実戦に存在しない状態を作らないため)。
- 数値欄に **`issen.nihilMs`(300)と `issen.nihilRadius`(200)** を追加する(セクションは
  既存の「一閃(th-issen)」の中)。`HIDDEN_THOR_TUNING_DEFAULTS` も自動で追随する
  (`deepCloneTuning` 済みなので追加のみ)。
- 受け入れ条件: ボスメーカーで半径を動かすと、**紫円の絵と必中の引き金域が同時に**変わる。

### 5-10. 予告SE

- 現行は `beginThorMove` の頭で `BOSS_ALERT_SFX_KEY` を1回鳴らす。2段になっても
  **鳴るのは1回=紫の開始時のみ**(赤の開始で二度鳴らさない)。
- `issen-dash` 突入時の `katana-dash`(v0.25.3700)は**通常・必中とも鳴らす**。
- 受け入れ条件: 一閃1回につき警告音1回・ダッシュ音1回。

---

## 6. 数値表(叩き台と不変の区別)

| 技 | 項目 | 値 | 種別 |
|---|---|---|---|
| 一閃 | `issen.nihilMs` | 300 | 社長指定=確定 |
| 一閃 | `issen.nihilRadius` | **200px** | **叩き台**(実機で詰める) |
| 一閃 | `issen.windup` | 500(旧2400) | 社長指定=確定 |
| 一閃 | `dashMs / range / halfWidth / recover` | 280 / 310 / 80 / 900 | 不変 |
| 突き | `tsuki.windup` | 1100(旧1000) | 社長指定=確定 |
| 突き | `tsuki.range` | 300(旧240) | 社長指定=確定 |
| 突き | `tsuki.halfWidth` | 15(=全幅30・現行のまま) | 確定 |
| 突き | `ms / trackFrac / recover` | 180 / 0.5 / 600 | 不変 |
| 払い | `harai.windup` | 600(旧1000) | 社長指定=確定 |
| 払い | `active / range / halfWidth / recover` | 220 / 310 / 40 / 700 | 不変 |
| 連携 | **突進起点**の台本 | `['dash','tsuki','tsuki']` | 確定(既存4起点は不変・§4) |
| 突進 | `windup / moveMs / strikeMs / recover / cdMs` | 700 / 230 / 110 / 800 / 6000(ミゲル同値) | **叩き台** |
| 演出 | 紫円の開き/閉じ | ease 120ms | **叩き台** |

**負荷スコア: 1/10**(描画=円1つ+帯1本の追加。どちらも既存の `Graphics` 予告と同じ描き方で、
投影影を落とす強glowは増えない=計測済みの高コスト要因に触れない。シミュ側は州が1つ増えるだけ。
安全弁: 紫円はトール1体しか出さない技=同時に1個以上存在しない。)

---

## 7. 横断の受け入れ条件(バッチ全体)

1. `npm run typecheck` / `npm run lint` がエラー0。
2. 純関数の新規テスト: ①`planBossChoreography('thor','dash',phase)` が `['dash','tsuki','tsuki']`(Phase1は先頭2手)
   ②他ボスの台本長が不変 ③必中の引き金の純関数(「紫州か」「円の内側か」「スイングのエッジか」)を
   切り出して `src/utils/` でテスト(useGameLoop に判定を直書きしない=実装精度の規律4)
   ④近接スイングの打刻を書く経路の**件数を固定する**テスト(§1-3)。
3. `counterReach.test.ts` / `ghostTelegraph.test.ts` / `constitution.test.ts` が緑。
4. **既存の一閃・突き・払いの当たり判定の寸法を1つも変えていない**(溜め中のカウンター成立域だけは
   §8-2の裁定で**帯へ揃える**=これは意図した変更)。
5. **既存の起点台本(issen / tsuki / harai / jump)を1文字も書き換えていない**。追加するのは
   **突進(dash)起点の1本だけ**(§4)。
6. 自己点検1行(DEVELOPMENT_LOG に明記): 憲法第4条(初心者ゾーン)・第5条(緩を荒らさない)へ
   抵触しないか——トールはステージ5の裏ボスであり初心者ゾーン外。
7. **実在確認**: 実装後、紫の円が**実機で見えているか**(発火・素材・可視性・表示時間)まで確認する。
   「コードに書いたから出ているはず」で完了にしない。

---

## 8. 裁定(8-1〜8-5 の範囲。★未決は §9 に11件)

> **この見出しは 8-1〜8-5 についてだけの話**です(文書全体の主張ではない)。
> **8-1〜8-5 は全件裁定済み=確定仕様**(2026-08-20〜21)。当初の発注に必要な裁定はここで揃っており、
> **実装バッチを発注できる状態**になった、という意味。
> **★未決は §9 に11件残っています**(実装バッチ v0.25.3780〜3799 で当たったもの)。
> **ここで「未決なし」と読まないこと。**

### 8-1. 一閃の反応時間(赤500ms)= **裁定済み**(社長「一旦それで」)

- **確定**: **500msのまま実機で見る**。帯の半幅(80)も触らない。
- 事実として: 台帳コメントに「半幅80+自機14=94px → **必要900ms**」という反応時間の見積もりが
  ある(v0.25.3461で3000→2400へ縮めた時の根拠)。500msでは**帯の外へ走って逃げるのはほぼ不可能**で、
  答えは「カウンターで返す」へ寄る。紫300msが「来る」の予告として先に立つので**実質の読み時間は800ms**。
- **実機で見る点**: 一閃が「避ける技」ではなく「カウンターする技」になって手応えが成立しているか。
  ここが崩れていたら、半幅か500msのどちらを動かすかを改めて裁定にかける(**先回りして絞らない**)。

### 8-2. 赤い予告中のカウンター成立域(帯か体か)= **裁定済み**(社長「赤いのにカウンターできないは聞くまでもなく直すでしょ」)
- **事実**: 確立済みの文法は「**赤い予告の図形=カウンターの成立域**」(v0.25.3591の是正)。
  ところが**トールだけは宣言表を通らず**、`issen-windup` / `tsuki-windup` / `harai-windup` は
  **体の重なりのみ**で成立する(実行中だけ帯)。実在確認: `hiddenBossCounterableNow`(useGameLoop:5814)が
  `boss.type !== 'thor'` でトールを除外している。
- **確定: 本バッチで揃える**。トールの溜め中のカウンター成立域を、他ボスと同じ**赤い予告帯でも成立**
  する形へ直す。CLAUDE.md「**赤い予告そのものは判定と厳密に一致させる(この一致だけは妥協しない)**」の適用。
  「別バッチにしたい」という当初の推薦は撤回(社長裁定が優先)。§7-5の「意図して残す不一致」の宣言も**削除**。
- **実機で見る点**: 成立域が広がる=**トール戦の難度は下がる方向**。下がりすぎていないかを実機で見る。
- **受け入れ条件**:
  1. `issen-windup` / `tsuki-windup` / `harai-windup` / **`dash-windup`(新技)** の各赤帯で
     カウンターが成立する(帯の図形=成立域)。
     - ★**`dash-windup`(=実装名 `thor-dash-windup`)だけは保留**: 実装は `'body'`(=既存の全突進と同じ)で
       着地しており、その差分は**§9-3の裁定待ち**。帯へ揃えると全突進(裏ボス3体・ミゲル・馬乗り)に
       及ぶため、トール1体では決められない。**この行だけを読んで「通った」と判断しないこと。**
  2. **必中一閃(`issenGuaranteedUntil` 中)は引き続きカウンター不可**(§1-3の裁定と衝突しない)。
  3. **他ボスの挙動が1つも変わらない**(除外条件の撤去がトール以外へ波及していない)。
  4. `isBossCounterableNowApprox`(語尾判定)・`ghostTelegraph`・`botSkill` の帯脅威と整合する。

### 8-3. 新技「突進」の追加で解決 = **裁定済み**(社長「突進足して。ミゲルと同じ様なやつ。で、突 突 を付けて」)
- **事実**: トールの起点は既に4本すべて埋まっている(`issen`/`tsuki`/`harai`/`jump`)。当初案の
  「突き起点に新台本」は**既存の `['tsuki','issen','harai']` を潰す**ことになり、社長はそれを避けていた。
- **確定**: **新技「突進」(ミゲル型)を足し、その新起点に `['dash','tsuki','tsuki']` を置く**
  =**既存4本は1本も触らない**。3手なのでキュー上限(Phase1=2/Phase2+=3)の枠内に収まり、
  `FIXED_SCRIPTS` のような長さの特例もフェーズ条件も**不要**になった。詳細と受け入れ条件は**§4**。

### 8-4. ボットは紫の円で近接を止めるか = **裁定済み**(社長「マスターとスキルドは覚える」)

- **確定**: **`master` / `skilled` の2段だけ**「**紫の円の中では近接を振らない**」を学習する。
  `novice` / `casual` は**従来どおり=踏んで食らう**(段の差として残すのが狙い)。
- **実装の形**: `BotSkillProfile` にダイヤルを1つ足す(例 `respectsNihilCircle: boolean`)。
  `master`/`skilled`=true、`novice`/`casual`=false。**既存の `seesBossCounterPhases` と同じ型**
  (段ごとの真偽ダイヤル)を踏襲する=新しい仕組みを発明しない。
- **止める対象は「近接を振ること」だけ**。紫の円は**回避脅威に足さない**(立っているだけなら安全
  =§5-4の既定は変えない)。円の中に居ても、殴らずに待つ/離れるのは自由。
- 判定は**必中一閃の引き金と同じ円**(`issen.nihilRadius`)を読む。ボット側に半径を複製しない。
- **受け入れ条件**:
  1. `master` / `skilled` のボットは、`issen-nihil` 中に紫円の内側にいる間**近接を振らない**
     (紫が明けた/円の外へ出た後は従来どおり振る)。
  2. `novice` / `casual` の挙動は**1つも変わらない**(既存のボットランの結果が同傾向で出る)。
  3. ダイヤルの**段の単調性**(上位ほど≧)を壊さない(`botSkill.test.ts` の不変条件6と同じ流儀で
     新ダイヤルにも単調性テストを付ける)。
  4. 紫の円は回避ベクトルに影響しない(=帯としても円としても脅威に足していない)。
  5. 半径がボスメーカーで変わると、ボットの「振らない」範囲も同時に変わる(複製していない証明)。

### 8-5. 紫円の半径(200px)= **裁定済み**(社長「そのままで一旦様子見」)

- **確定**: **200pxのまま実機**。鞭(射程150)が円の外から殴れる**非対称も現状は許容**する
  (武器ごとに間合いが違うのは既存の設計思想と整合。刀・鞭は間合いで有利を取る武器)。
- 半径の値は引き続き**叩き台**(§6の表のまま)。実機の手応えを見て動かす余地は残す。

---

## 9. ★未決事項(実装バッチ v0.25.3780〜3799 で当たったもの)= **11件**

> **実装チャットの記録**。CLAUDE.md「実装精度の規律1」に従い、設計書に無い値は勝手に決めず**ここに書いて止める**。
> 多くは「発注仕様のどこにも書かれていないが、実装を成立させるには何かを入れざるを得なかった」もの。
> **入れた値は全て『既にある物の流用』**(新しい数字を発明していない)が、**採否は社長裁定**。
>
> **★未決の内訳(11件)**: 9-1 / 9-2 / 9-3 / 9-5 / 9-6 / 9-6b / 9-6c / 9-7 / 9-9 / **9-10** / **9-11**。
> (9-4 は裁定済みの叩き台の確認事項、9-8 / 9-8b は裁定不要の事実メモ・申し送りなので**この11件に含まない**。)

### 9-1. 突進の抽選重み(§4に指定なし)
- **問題**: §4は「突進が単発でも台本でも出る(`pickThorMove` の抽選に加わる)」を受け入れ条件にしているが、
  距離帯ごとの**重み**が書かれていない。重みを入れないと抽選に参加できない。
- **入れた値(叩き台)**: `dash: { melee: 0, near: 0, mid: 20, far: 55 }`
  = **スカジの突進の行をそのまま流用**(`skadiScript.SKADI_MOVE_WEIGHTS.dash` と同値)。
- **これで何が変わるか(事実)**: 遠距離の抽選に突進が入るぶん、**一閃(far:70)と突き(far:30)の相対頻度が下がる**
  (far の総重みが 100 → 155)。近距離(melee/near)の読み合い=一閃/突き/払いの配分は**1つも変わらない**。
- **裁定が要る点**: 「遠距離で一閃が減る」のは狙いどおりか。**一閃は決闘の看板**なので、
  減らしたくないなら突進の far を下げる(例 30)か、突進を mid 専用にする案がある。
- **選べる案**(far の総重み=一閃70+突き30+突進X。カッコ内は**遠距離で一閃が出る割合**。
  ※phase1/2の値。phase3は一閃と払いだけ×1.2されるので、どの案でも一閃の割合はこれより上がる):
  - **(a) 現状のまま `far: 55`**(スカジ流用) … 一閃 **45%**。突進が遠距離の主役級に出る。
  - **(b) `far: 30` へ下げる** … 一閃 **54%**。突進は遠距離にも出る(§4の受け入れ条件を満たす)が、
    一閃の看板性が(a)より残る。突き(30)と同格の扱い。
  - **(c) mid 専用(`far: 0`)** … 一閃 **70%**(=突進を足す前と同じ)。遠距離の読み合いは1つも変わらないが、
    **最も遠い間合いから突っ込んでくる**という突進らしい絵は出なくなる。
  - **※いずれも「突進のCDが明けている瞬間」の値**(上限)。`pickThorMove` は `dashReady` が false の間
    **突進を候補から落とす**ので(`cdMs`=6000 + recover)、突進が far の抽選に入っているのは時間の一部だけ。
    **CD中は far=一閃70/突き30=一閃70%** なので、**実測の平均はどの案でもこの表より高い**。
- **★推薦は(b) `far: 30`**。理由: 突進は「遠くから間合いを詰める」技なので far から消す(c)は技の意味を削る一方、
  (a)は**一閃が遠距離の半分以下**まで落ちる。(b)は突き(far:30)と同格=「遠距離は 一閃 > 突き = 突進」という
  読める順序になり、看板の一閃を過半に保ったまま突進を遠距離に残せる。
  **なお現在のコードは(a)**(叩き台のまま)なので、(b)(c)なら1行の数値変更が要る。

### 9-2. 突進の斬り抜けカプセルの寸法(§4に指定なし)
- **問題**: §4-1は windup/moveMs/strikeMs/recover/cdMs しか与えていない。到達時の**斬り抜けの帯**の
  長さと半幅が無い。
- **入れた値**: **トールの払い(`harai.range`=310 / `harai.halfWidth`=40)を流用**。
  根拠は「ミゲル型」の型そのもの——ミゲルの踏み込みも `MG_T.harai.range/halfWidth` を流用している
  (`angelBossTick.ts` の mdash-move)。**別欄は作っていない**(ボスメーカーで払いを動かすと突進も動く)。
- **裁定が要る点**: ミゲルの払いは range 190、トールの払いは range 310。**同じ流用ルールでも
  トールの斬り抜けの方が1.6倍長い**。長すぎるなら専用欄(`dash.range/halfWidth`)を新設する。
- **選べる案**:
  - **(a) 現状のまま(払いを流用・range 310 / halfWidth 40)** … 「ミゲル型の流用ルール」をそのまま守る。
    ボスメーカーで払いを動かすと突進も動く(欄が1つ=食い違わない)。**斬り抜けはミゲルの1.6倍長い。**
  - **(b) 専用欄 `dash.range/halfWidth` を新設して 190 相当へ短くする** … ミゲルの**実寸**に揃う。
    ただし**新しい欄=新しい数字**が増え、払いを調整しても突進が付いてこなくなる(2箇所管理)。
  - **(c) 専用欄を新設し、値は当面 310 のまま**(将来の調整の口だけ開ける) … 見た目・判定は今と同じ。
- **★推薦は(a) 現状のまま**。理由: ①§4-1の指示は「ミゲル**型**」であり、ミゲルの実装が
  `MG_T.harai` を流用しているという**作法**をトールも守っている(**その技の払いの寸法を使う**)。
  ②長さが問題になるのは「**赤い線より先に判定がある**」からで、それは §9-5 の1件
  (**全突進に共通**)として別に裁定を待っている——§9-5①(赤い線を斬り抜けの終端まで延ばす)を採れば
  310px でも「赤くないのに当たる」は消える。**長さそのものより、線と判定が揃っているかが本質**。
  ③(b)は当たり判定を縮める=**バランス変更**なので、実機の手応えを見る前に動かす理由が無い。

### 9-3. 突進の溜め中のカウンター成立域(§8-2の受け入れ条件1と§4-1の記述が読み分かれる)
- **事実**: §4-1は「予告=**赤の流星ライン**(突進系の既定文法=**裏ボスダッシュ/ミゲル踏み込みと同じ作り**)」。
  §8-2の受け入れ条件1は「issen-windup / tsuki-windup / harai-windup / **dash-windup(新技)** の
  **各赤帯**でカウンターが成立する」。
- **入れた値**: **`'hidden:thor-dash-windup': 'body'`**(=既存の全突進と同じ)。理由は§4-1が名指しした
  2つの前例がどちらも 'body' だから(`'hidden:dash-windup': 'body'` / ミゲルは `bodyOverlapNow`)。
  流星ラインは**判定を持つ図形ではない**(判定は突進の体そのもの)ので、帯にすると
  「赤くないのに当たる/赤いのに当たらない」ではなく**「予告線の上に立っただけでカウンターが取れる」**
  という別の非対称が生まれる。
- **裁定が要る点**: §8-2の受け入れ条件1を「dash-windup も帯で取れる」と読むなら、
  **全突進(裏ボス3体・ミゲル・馬乗り)を一緒に変える話**になる(トールだけ帯にすると文法が割れる)。
  → **これはトール1体の話ではないので、ここで止めて社長へ**。
- **選べる案**:
  - **(a) 現状のまま `'body'`(全突進と同じ)** … 溜め中は**トールの体に当てる**とカウンター成立。
    予告の流星ラインは判定を持たない絵のまま。**全突進で文法が揃う。**
  - **(b) トールの `thor-dash-windup` だけ帯('band')にする** … §8-2の受け入れ条件1を字面どおり満たすが、
    **同じ"突進"という動詞の中で、トールだけ予告線の上に立つとカウンターが取れる**=文法が割れる。
  - **(c) 全突進(裏ボス3体・ミゲル・馬乗り・トール)を一緒に帯にする** … 文法は揃うが、
    **5体ぶんの成立域が広がる**=難度が下がる方向の**バランス変更**。トール案件の外へ出る。
- **★推薦は(a) 現状(`'body'`)据え置き**。理由: 流星ラインは**判定を持つ図形ではない**(判定は突進の体そのもの)ので、
  帯にすると「赤いのに当たらない/赤くないのに当たる」ではなく**「予告線の上に立っただけでカウンターが取れる」**
  という別の非対称が生まれる。§8-2の禁則(赤い予告=判定の一致)は**この州では既に満たされている**
  (赤い線は"来る方向"の予告であって攻撃判定の告知ではない)。
  (b)は文法割れ、(c)はトール1体を超えるバランス変更なので、**動かすなら §9-5 の一括裁定と同じ回で**。

### 9-4. 一閃の反応時間・紫円の半径・突進のミゲル同値
- §8-1(赤500ms)・§8-5(半径200px)・§4-1(ミゲル同値)は**裁定済みの叩き台**として指定どおり入れた。
  実機の手応え待ち(この節は未決ではなく確認事項)。

### 9-5. 突進の「赤い線」と判定のズレ(★全突進に共通・トールだけの話ではない)
- **全突進に共通のズレ: 赤い線の終点より先に斬り抜けの判定がある。ミゲルもトールも同じ。直すなら
  全突進に及ぶので社長裁定。**
- 事実(実在確認・v0.25.3784の検収で確認):
  - 予告の赤い流星ラインは**ボス中心 → 突進の終点**まで。ところが到達時の斬り抜けカプセルは
    **終点からさらに `harai.range` 先**まで伸びる(ミゲル=190px / トール=310px)。
    = 「赤くない所に判定がある」。**この構造はミゲルの `mdash-move` そのままの流用**なので、
    トール側だけを直すと突進の文法が割れる。
  - **走行中の接触**は、ミゲルは当たる(除外を持たないので共通の接触ダメージ経路が出す)。
    トールは `combatTick` の `enemy.type === 'thor'` 除外で**丸ごと止まっていた**ので、
    v0.25.3784 で **`thor-dash-move` だけ除外から外してミゲルに揃えた**(他3技+ジャンプの除外は不変)。
- **裁定が要る点**: 斬り抜けの判定を赤い線に合わせる(=線を斬り抜けのぶん延ばす/カプセルを終点で
  止める)なら、**裏ボス3体・ミゲル・馬乗り・トールの全突進を一緒に変える**話になる。
- **選べる案**: ①**赤い線を斬り抜けの終端まで延ばす**(線=`ボス中心 → 終点 + 進行方向×harai.range`)。
  当たる範囲は1pxも変わらず、「赤くない所に判定がある」だけが消える。
  ②**カプセルを終点で止める**(赤い線はそのまま)。突進の**リーチが 190〜310px 短くなる**=当たらなく
  なる位置ができるので、難度が下がる方向へ動く。 ③このまま許容する(全突進で揃ってはいる)。
- **★推薦は①(全突進一括で赤い線を斬り抜けの終端まで延ばす)**。CLAUDE.md の
  「**赤くないのに当たる**」だけは妥協しない禁則で、①は**判定・難度を1つも動かさずに**それを解消できる
  (②は当たり判定を縮める=バランス変更になる)。**ただし全突進に及ぶ仕様変更なので実装は裁定後**。
- 併記の事実(ミゲル側の別件・**ミゲルは触っていない**): ミゲルは `mdash-windup` を
  カウンターされた時に `mDashReadyAt`(突進の専用CD)を立てていない(`mdash-recover` と走行中だけ)。
  トールは v0.25.3784 で全成立経路(溜め/走行/硬直)へ揃えた=**ここだけトールがミゲルより厳しい**。
- ★追記(v0.25.3785・実物確認): **斬り抜けの「絵」も、ミゲルとトールで同じズレを持っている。**
  ミゲルの `mdash-move` の斬り抜けは `drawMiguelSlash(e.aiFromX, e.aiFromY, e.aiTargetX, e.aiTargetY, …)`
  =**突進が走った経路(始点→終点)**を焼いている(`pixiScene.ts` の mdash 分岐)。**「終点→さらに
  `harai.range` 先」ではない。** よってミゲルも「絵は走ってきた後ろ、判定は終点の先310px(ミゲルは190px)」で、
  **絵と判定が別の場所にある**。トールの2つのラッチ(`thorswing` kind===3 / `thor-dash-complete`)も
  同じく `aiFrom→aiTarget` を焼いているので、**現状はミゲルと同じ**=v0.25.3785 では**動かしていない**
  (社長指示「ミゲルに揃える」を、ミゲルの実物に照らして「揃っている」と判定した)。
  → 直すなら**赤い線・判定・絵の3つを全突進で一緒に**動かす話になる(この節の裁定と同じ1件)。

### 9-6. ★未決: 突進の**走行中の体当たり**でダメージを与えてよいか(社長裁定待ち)
- **今の実装**: 当たる(v0.25.3784 で `combatTick` の除外から `thor-dash-move` を外し、ミゲルに揃えた)。
  **v0.25.3785 でも動かしていない**=裁定が出るまで現状維持。
- **問題(文法が割れる)**: 予告の赤い流星ラインは**走行中に始点から蒸発していく細い線**なのに、
  実際にダメージを持つのは**巨体のAABB**。線よりはるかに広い体が230msのあいだ動き回る
  =**「赤くないのに当たる」**(CLAUDE.md「攻撃ヴィジュアルの2分類」で唯一妥協しない禁則)。
  トールの他3技(一閃/突き/払い)は「ライン上のみ」を維持しているので、**トール1体の中で文法が割れる**。
- **併記の事実**: **ボットには見えていない**(`botSkill.ts` の帯脅威は `bs.endsWith('-windup')` と
  明示リストで拾うが、`thor-dash-move` はどちらにも入っていない)。
- **選べる案は2つ**——**(a) 当てる(+下の1セットを同時に入れる)** / **(b) 当てない(除外へ戻す)**。
  それぞれ何が要るかは次の2項に書いてある。
- **当てる裁定になるなら1セットで必要**: ①走行中も**体幅ぶんの赤い帯**を描く(絵=判定)
  ②`botSkill.ts` の帯脅威に `thor-dash-move` を足す。**この2つまでやって初めて「当たる」が成立する。**
- **当てない裁定になるなら**: `combatTick` の除外へ `thor-dash-move` を戻すだけ(1行)。
  ただし**流用元のミゲルは当たる**ので、トールだけ当たらない=そちらの文法が割れる。
- **★推薦は「当てる+1セットを同時に入れる」**(=上の①②を足して初めて確定にする)。理由:
  流用元のミゲルが当たる以上、トールだけ無害にすると**突進という動詞の文法が割れる**(ボスごとに
  「走ってきた体に当たるかどうか」が変わるのは、プレイヤーが一目で判断できない差)。**当てるなら
  「赤くないのに当たる」を残さないために、赤い帯と bot への通知まで1セットで入れるのが条件**。
  1セットを入れないまま当てるくらいなら、当てない(除外へ戻す)方がまだ良い。

#### ★9-6b(v0.25.3793・検収監査 重大2で追加): 走行中は「カウンターしても必ず1発もらう」
- **事実(フレーム内の競合が残っている)**: v0.25.3785 で受け流しの横取りを止めた結果、走行中に
  カウンター窓を開けて体が重なると `applyContactDamage` は**受け流しを素通りして `damagePlayer` へ落ちる**。
  ところが同じ重なりを見る**共通カウンターブロックは"フレーム頭"の座標**で判定するので:
  - **フレームN(重なり始め)**: ボスtick側=まだ重なっていない→カウンター不成立 /
    接触側=進めた後の座標で重なっている→**被弾**。
  - **フレームN+1**: ボスtick側がようやく重なりを見てカウンター成立。
  = **走り(230ms)をカウンターすると、成立の1フレーム前に接触ダメージが必ず1発入る。**
    (**例外は到達フレームだけ**——斬り抜けのカプセル分岐は移動後の座標で同フレームに取るため。)
- **なぜ問題か**: トールの他3技(一閃/突き/払い)には無い挙動で、**トール1体の中で手触りが割れる**
  (「カウンターできたのに必ず削られる技」が1つだけある)。
- **選べる案**:
  - **(a) 走行中の接触ダメージを、カウンター窓が開いている間だけ免除する**(`combatTick` の
    トール除外に「`thor-dash-move` かつ `counterActiveNow` なら落とす」を足す=1行)。
    窓を開けていない時は従来どおり当たる(=9-6の「当たる」を壊さない)。
  - **(b) 共通カウンターブロックの重なり判定を、patch 適用後の座標へ寄せる**。順序依存が
    そのまま残る(=別のフレームで同じ型の食い違いが出る)ので**非推薦**。
- **★推薦は(a)**。「カウンターに成功したのに1発もらう」を消すのが目的で、(a)は**窓が開いている
  瞬間だけ**に効くため9-6の裁定(当てる/当てない)とも独立に成立する。
- **v0.25.3793 のコードは現状維持**(裁定が出るまで動かさない)。

#### ★9-6c(v0.25.3793・検収監査 中4で追加): 弾き返しの行き先が「プレイヤーの手前150px」になっている
- **事実**: `thorDashCounterHit` の行き先は **プレイヤー中心を基準**に組んでいる
  (`プレイヤー中心 − 突進の進行方向 × dashCounterPushbackPx`)。これは流用元のミゲルの式そのまま。
  ミゲルの走行カウンターは**プレイヤー専用**だったので「プレイヤーの手前=来た方向」で一致していた。
- **何が壊れたか**: v0.25.3785 で走行中の突進を宣言表へ載せたことで**守護霊(ゴースト)も走行中の
  突進を取れる**ようになった。守護霊が**プレイヤーから離れた場所で**取ると、ボスは「来た方向へ
  弾き返る」のではなく**プレイヤーの手前150pxへ引き寄せられる**(位置関係によっては**前進**する)。
  → §4-1の受け入れ条件3「カウンターすると**来た方向へ弾き返る**」が、**新しく開いた経路で成立しない**。
- **選べる案**:
  - **(前者) 起点を"ボスの現在位置"にする**: 行き先 = `ボスの現在中心 − 進行方向 × 150px`。
    誰が取っても必ず「来た方向へ150px下がる」。プレイヤーが取った場合の見え方も現行とほぼ同じ
    (走行中はボスがプレイヤーへ向かっているので、両者の差は詰まった距離ぶん)。
  - **(後者) プレイヤー中心基準のままと明記する**: 「弾き返しは**プレイヤーから見た退け**であって
    ボスの進行の打ち消しではない」と設計書に書いて、守護霊経由では前進しうることを許容する。
- **★推薦は前者(起点をボスの現在位置にする)**。社長の言葉が「**来た方向へ弾き返す**」であり、
  それを誰が取っても満たすのは前者だけ。ミゲルは「プレイヤー専用」という前提があったから同じ式で
  一致していたのであって、前提が変わった今は式の方を合わせ直すのが筋。
- **v0.25.3793 のコードは現状維持**(仕様の見え方が変わるので裁定後に動かす)。

### 9-7. ★未決: 刀のスワイプ一閃を必中の引き金に加えたことの帰結(社長裁定待ち)
- **事実**: `triggerKatanaDash`(刀/村雨のスワイプ一閃)は同じ `set` で**実効180msの無敵**と
  **154pxの移動**を書く。`damagePlayer` は無敵で即落ちるので、**必中一閃(280ms)の先頭180ms=64%の
  被弾が丸ごと消える**。帯の半幅は80pxなので、横へスワイプすれば154pxで帯の外へ抜けられる。
  → **「紫の中で振ったら必ず斬られる」が、刀装備だけ効かない。**
- **選べる案**: ①引き金から `triggerKatanaDash` を外す(=刀のスワイプでは必中を誘発しない)
  ②i-frame中でも必中だけは通す専用の口を作る ③このまま許容する。
- **推薦は③+設計書へ明記**(§8-5「刀・鞭は間合いで有利を取る武器」の思想に沿う。
  「必中を誘発できるが、そのぶん初動を抜けられる」は武器差として読める)。
- **v0.25.3785 のコードは現状維持**(裁定が出るまで動かさない)。

### 9-9. ★未決: ノックバックで押された位置が、解除の瞬間に消える(**中3の直しは時間軸だけ**・社長裁定待ち)

> 番号は**追加順**(§9-10 / §9-11 が続く。§9-8・§9-8b は裁定不要の事実メモ/申し送り)。

- **★まず事実として明記しておくこと(v0.25.3793 中3の直しの範囲)**:
  v0.25.3793 で直したのは **時間軸だけ**(`kbOnlyStop` の枝で `bossStateUntil` / `bossNextActionAt` /
  `aiStartedAt` を凍結時間ぶん繰り下げた)。**位置(XY)のワープは直っていない=そのまま残っている。**
  設計書はこれまでこの残りに一言も触れていなかったので、ここに書き起こす。
- **問題(中3と同じ分類で、軸が時間からXYへ移っただけ)**:
  1. 凍結中(`frozen`)は `thor-dash-move` の状態機械が**丸ごとスキップ**されるので `patch.x/y` は書かれない。
  2. 一方 `gameStore.updateEnemies` の押し出しブロック(`gameStore.ts:10818-10839`)は
     `canShoveEnemy`(=`knockbackShoveUntil` が立っている時)なら**ボスを実際に横へ滑らせる**。
     このブロックは `isHiddenBoss` の早期return(同 10851)より**手前**にあるので、トールにも効く。
  3. 解除の瞬間、突進ハンドラは `aiFrom→aiTarget` の**絶対位置**(`airHopEase01` の補間)を書き戻すので、
     **滑った分が1フレームで消える=元の直線へスナップバックする**(=**1フレームのワープ**・慣性MUST違反)。
- **効く道具と滑る量(実測ではなく定数からの算術)**: 押し道具のうち `kbOnlyStop`(=root/気絶/浮き/
  ワープを伴わない純粋なノックバック)になるのは **シールドバッシュ**(速度714)と
  **鞭**(600・**クリ・非クリを問わず**)。
  - **★訂正(v0.25.3799・検収監査6巡目 中6)**: 旧記述は「鞭(600・**非クリ**)」と書いていたが**過小**。
    トールは `usesBossCrit=true` なので、近接の停止パッチ(`bossCritStopPatch`)は
    **`bossSlowUntil`(移動半減)しか書かない**——`stunUntil` を書くのは `usesBossCrit=false` の敵だけ。
    よって**クリの鞭でも `stunnedNow` は立たず、`kbOnlyStop` が成立する**。
    滑る道具は「**バッシュ + 鞭(クリ・非クリを問わず)**」。
  - 減衰は線形(1→0・`KNOCKBACK_DURATION`=280ms)なので滑走距離は **速度×0.28/2 ≒ 100px / 84px**。
    突進の `moveMs` は **230ms** なので、**1回の押しで走りの残り全部を覆える**=戻り幅も最大約100px。
  - **※`接触受け流し` はここに含まれない**。理由は州によって違い、**走行中は先に効く方が別**:
    - **`thor-dash-move`(走行中)**: そもそも**接触受け流しの対象から丸ごと外れている**
      (`combatTick.ts` の `thorDashRunNow` 除外・§5-2 やること④)。受け流しが成立しない以上
      `rootUntil` も立たない=**走行中は二重に成立しない**。これが先に効く理由。
    - **windup / recover 側**: 受け流しは成立するが、同時に `rootUntil` 900ms を立てるので
      `kbOnlyStop` が成立せず、`else` 側=`bossState='chase'` で**技ごと中断**される。
    - どちらの経路でもスナップバックは起きない。
- **★この症状は突進だけではない(事実)**: 凍結中に状態機械を丸ごと飛ばす形はトールの全技(および
  裏ボス/天使の移動技)で共通で、`aiFrom→aiTarget` の**絶対位置**で補間する技は同じスナップバックを持つ
  (`useGameLoop.ts` の 6355/6441/6518/6553/6602/6673/6696/6710 …)。**直す範囲がそのまま裁定の中身になる。**
- **選べる案**:
  - **(a) 現状のまま**(位置のワープを許容する) … コード変更ゼロ。押されても「走った線」は不変=
    突進の到達点が押し道具でズレない、という読み方はできる。ただし**最大約100pxの1フレームワープ**が残る。
  - **(b) 凍結中に滑った差分を `aiFromX/aiFromY` と `aiTargetX/aiTargetY` の両方へ加算して、
    ★線ごと平行移動させる**(=押された位置から続きを走る) … 走る向き・残り距離・イージングは1つも
    変わらず、**線が横へずれるだけ**。ワープが消え、押し道具が「軌道をずらす」道具として読める。
    - 実装の要点(裁定後): ①差分は「凍結中に実際に動いた量」= その枝で `boss.x/y` と前フレームの
      補間値の差 ②平行移動後の到達点は**必ず `clampRectToPlayableArea` を通す**(CLAUDE.md Y方向の掟。
      押し出しで帯の外へ出た線をそのまま走らせない) ③対象を**トールの突進だけ**にするか
      **`aiFrom→aiTarget` の全技**にするかを決める(揃えないと技ごとに手触りが割れる)。
    - ★**Y方向の5つの副作用の確認表**(CLAUDE.md「Y方向に何かを動かす時の必須チェック」・
      v0.25.2615〜2617 で同じ週に3つとも踏んだ事故の再発防止)。この押しは**上下にも掛かる**
      (バッシュ/鞭の向きはプレイヤー→ボスなので、真上に押されれば**最大約100px上へ動く**)。
      **5つ全部に答える**:
      | # | 副作用 | この案での判定 |
      |---|---|---|
      | 1 | **地平線フェード**(`pixiScene.horizonActorAlpha`・上へ行くほど alpha→0) | **該当する**。約100px上へずれた線をそのまま走ると、斬り抜けの瞬間にボスが**薄くなる**。裁定後の実装では、平行移動後の `aiTarget` が**フェード帯へ掛かっていないか**を実機(`?zoomlock=0.4` 併用)で見る。フェード帯は**引くほど画面px幅で広がる**ので、引きの倍率でも確認する。 |
      | 2 | **帯の外の減光**(`LAB_CORRIDOR_Y_LIMIT_PX`) | **該当しない**。これは `lab` テーマ(廊下)専用の減光で、**トールの戦場(ステージ5)は lab ではない**ため発火しない。 |
      | 3 | **擬似遠近スケール**(`pixiScene.depthScaleEnemy`・上へ行くほど絵が縮む) | **該当する**。約100px上へずれると**斬り抜けの瞬間だけ絵が小さくなる**(判定=カプセルの寸法は変わらないので、**絵と判定の食い違い**として出る)。実機で目視確認する。 |
      | 4 | **可視域**(`gameBounds` × `contextZoomTarget`) | **該当する(ただし低リスク)**。押しの量は最大約100px、突進の走りは1回だけで、走り終わりに `chase` へ戻る=**居続けない**。とはいえ画面上端付近で押された場合に**画面外へ出ないか**を、絶対最大引き `ZOOM_MIN_ABS`=0.40(`?zoomlock=0.4`)で確認する。 |
      | 5 | **移動可能帯**(`world/playableArea.clampRectToPlayableArea`) | **該当する**。上の②がこの1つ(=プレイヤーが追える帯の外へ線を出さない)。**判定は world/store 側に置く**(`pixiScene` には置かない)。 |
    - **※上の表は「(b)を採ったら何を確認するか」であって、(b)を推す根拠ではない。**
      1・3・4のどれかが実機で破綻したら、その時点で(b)は**破綻した案として社長へ戻す**(勝手に補正を足さない)。
  - **(c) 押し道具で突進を中断する**(`kbOnlyStop` の対象から突進を外し、`else` 側=`chase` へ落とす)
    … ワープは消えるが、**押し道具が突進のキャンセル手段になる**=v0.25.3497 の社長裁定
    「ノックバックもだけど、技だけキャンセルされなければええで」に**逆行する**ので非推薦。
- **★推薦は(b) 線ごと平行移動**(対象は**まず `thor-dash-move` だけ**)。理由: 社長裁定
  「技だけキャンセルされなければええで」を守ったまま、**現実の物理**(押されたら押された場所から
  続きを走る)に一致する唯一の案で、判定・ダメージ・所要時間・イージングを1つも動かさない。
  他技への横展開は**同じ直し方で足せる**ので、まず突進で見てから広げるのが安全。
- **v0.25.3798 のコードは現状維持**(=(a)。位置は動かしていない)。

### 9-10. ★未決: `counter-leap` の補間が**等速**=慣性MUST違反(本バッチの弾き返しがこれで運ばれる)

- **問題**: `counter-leap`(useGameLoop `st === 'counter-leap'` の枝)は
  `t = 1 - (bossStateUntil - now) / counterLeapMs` を**そのまま**位置に掛けている(イージングなし)。
  = **カクッと出てカクッと止まる 260ms**。CLAUDE.md「★動きの絶対ルール: 慣性」の
  「等速で始まり瞬間停止する」禁止に**真正面から当たる**。
- **なぜ今この案件で出すのか(=本バッチの目玉に直結する)**: 本バッチが入れた
  **150pxの弾き返し**(§5-2「★弾き返しの効かせ方」/§4-1 受け入れ条件3)は、
  **この等速補間で運ばれる**。1フレームのテレポートは v0.25.3785 で解消したが、
  **弾き返しの動き自体は加減速を持っていない**=「弾き返された」という物理が絵に出ていない。
  ※これまで §5-2 に**事実メモとしてしか書いていなかった**(=社長の目に入る一覧に無かった)。
  v0.25.3799 の検収監査で「判断の機会が消えている」と指摘され、ここへ立て直した。
- **及ぶ範囲(事実)**: `counter-leap` は**トールの全技共通のカウンター反応**
  (一閃/突き/払い/ジャンプ/突進)。裏ボス3体は counter-leap を使わない(即 `chase` 復帰)ので**無関係**。
  同じ形の等速線形補間は `backstep`(180ms)・`orbit-step`(160ms)にもある(トールの移動演出)。
- **選べる案**:
  - **(a) 現状のまま(等速)** … コード変更ゼロ。慣性MUST違反が残る。
  - **(b) `counter-leap` だけ既存のイージング関数(`airHopEase01`=smootherstep)へ通す** …
    突進の走り(`thor-dash-move`)が既に使っている**同じ関数**を、`t` に掛けるだけの1行。
    **判定・ダメージ・所要時間(260ms)・到達点は1pxも変わらない**(この関数は t=0で始点・t=1で終点に
    必ず一致する=「移動の見た目の配分」だけを変える)。**新しい定数を発明しない。**
  - **(c) (b)に加えて `backstep`(180ms)・`orbit-step`(160ms)= トールの線形補間**すべて**へ広げる** …
    トールの移動の手触りが全部揃う。ただし**カウンター以外の演目の見え方も変わる**ので、
    実機の印象を確かめる面積が広がる。
- **★推薦は(b)**。理由: ①本バッチの成果物(150pxの弾き返し)が**今この瞬間、慣性MUST違反の
  動きで運ばれている**ので、直す価値が最も高いのがここ ②**既にこのファイルの中で使っている
  関数**(突進の走りと同じ `airHopEase01`)を掛けるだけで、判定・時間・到達点が1つも動かない
  =バランス変更にならない ③(c)は同じ直し方でいつでも足せるので、**まず一番見る場所で見てから**
  広げるのが安全。**(a)を選ぶ余地もある**——260msは短く、実機で等速が気にならないなら触らない、
  という判断は成立する(**実機で見てから決める**選択肢として残す)。
- **v0.25.3799 のコードは現状維持**(=(a)。裁定が出るまで動かさない)。

### 9-11. ★未決: 凍結中に繰り下げていない**4本目の時計**(`bossBurstNextAt`)= 裏ボス3体の連射

> **トールには出ない**(トールの技はどれも `bossBurstNextAt` を読まない)。
> **本バッチ由来の回帰でもない**(v0.25.3793 の中3の直しより前から在る)。
> トール案件の走査で見つかった**裏ボス3体の話**なので、ここに置いて社長裁定へ上げる。

- **事実**: `kbOnlyStop` の枝(`useGameLoop.ts` 5424〜)は `boss.type === 'thor'` の中ではなく
  **裏ボス4体(mimir / jormungand / skadi / thor)が共有する枝**。ここで凍結時間ぶん繰り下げているのは
  `bossStateUntil` / `bossNextActionAt` / `aiStartedAt` の**3本**。
  ところが同じ枝を通る州のうち **`burst` / `radial` / `skadi-ice` / `skadi-blade`** は
  **`bossBurstNextAt`(絶対時刻)** を読んで次弾を撃つのに、**繰り下げていない**。
- **何が起きるか**: 鞭かバッシュで**ミーミル/ヨルムンガルド/スカジの連射中**を押すと、
  凍結が明けた瞬間に**1発が間隔(`burstGapMs` 等)を無視して飛ぶ**
  (=中3で直したのと**同じ型**の「解除の瞬間に何かが飛ぶ」。軸が位置ではなく発射間隔)。
- **選べる案**:
  - **(a) 現状のまま** … コード変更ゼロ。裏ボス3体の連射中に押すと1発が詰まる挙動が残る。
  - **(b) 同じ枝で `bossBurstNextAt` も `kbDtMs` ぶん繰り下げる**(1行) …
    3本セットが4本セットになる。**押している間は連射も止まる**=「ノックバックで技は中断しないが
    時計は進まない」という v0.25.3497 の社長裁定の形に、連射も揃う。
    **ただし裏ボス3体の弾幕の刻みが変わる**(押した時だけ1発ぶん後ろへずれる)=**挙動の変更**。
  - **(c) 「詰まる1発」を仕様として明記して残す** … 押しに対する見返り(=押すと1発余分に来る)と
    読むこともできる。
- **★推薦は(b)**。理由: 中3で `aiStartedAt` を足した時と**同じ理屈**(凍結中に絶対時刻の時計だけが
  進むと、解除の瞬間に飛ぶ)がそのまま当てはまり、直し方も**同じ1行**。ただし
  **裏ボス3体の挙動が変わる仕様変更**なので、**トール案件では触らない**(社長裁定後に別バッチ)。
- **v0.25.3799 のコードは現状維持**(=(a)。**1文字も触っていない**)。
  テスト側(`thorNihil.test.ts`)は、旧コメントの「時計は**3本セット**」という**閉じた集合の宣言**を
  「**トールの突進が読む時計は3本**」へ限定し、4本目の存在とこの節への参照を書き足した
  (=後任が「3本で全部」と読んで4本目を探さなくなるのを防ぐ)。

### 9-8. 事実メモ(裁定不要・記録)
- **`clampRectToPlayableArea` はトールの戦場では実質 no-op**(tutorial / lab / corridor / EX結界の
  どの条件にも当たらないので、渡した座標がそのまま返る)。**木や壁の解決(`resolveAabb`)はしていない。**
  → DEVELOPMENT_LOG v0.25.3784 の「着地点は `clampRectToPlayableArea` を通した」は
  **「壁判定が閉じた」という意味ではない**。正しい事実は「**トールのボスは障害物と当たらない設計なので、
  弾き先の壁判定は不要**。クランプは『行ける帯』の定義を1本に保つための作法として通している」。
- **他案件の台帳を触った(v0.25.3784→3785)**: v0.25.3784 で `ghostTelegraph.ts` のラフィ/フィルの
  分類をトール案件のバッチが決めた(緑=`quickblades-recover` の追加と `phill-judgment-windup` の削除)。
  v0.25.3785 で残りの `phill-judgment-active` / `-recover` も確認した——**到達不能だが台帳からは外せない**
  (`beginJudgment()` は v0.25.3740 から `beginCage()` を呼ぶだけで judgment 州へ入る経路が無い一方、
  この2件は `patch.bossState = '…'` の代入としてソースに残っており**完全性テストの走査が拾う**ため、
  外すと「走査で見つかる状態が台帳から漏れている」で落ちる)。理由をコメントに残して**残置**した。
  併せて `angelPlayback.test.ts` の入口表を実装に合わせた(`ph-judgment` → `phill-cage-windup`)。

#### ★9-8b(v0.25.3793・検収監査 低11): **フィル案件への申し送り**(トール側で直す話ではない)
> この項目だけは**トールの仕様ではなく、フィル(phillboss)のプレイヤー体験の話**です。
> トール案件では**触りません**。フィル案件が拾ってください。

- **実態(実在確認済み)**: ボスメーカーの **▸`ph-judgment`(裁きの光)を押すと、再生されるのは
  檻(`phill-cage-windup`)**。`beginJudgment()` が **v0.25.3740 の社長指示「裁きの光の中身を、
  羽根の檻に差し替え」**で `beginCage()` を呼ぶだけになったため。**これは事故ではなく裁定どおり**で、
  メーカーの欄名も既に `裁きの光=羽根の檻★必須(ph-judgment)` と書き換わっている。
- **残っているもの**: 中身を差し替えても **`phill-judgment-windup / -active / -recover` のハンドラ
  (`angelBossTick.ts` 3179-3201)と描画(`pixiScene.ts` 17336)と `ghostTelegraph` の2件は生きたソースのまま**
  =**到達不能なのに消せない**(完全性テストがソース走査で拾うので、台帳から外すと落ちる)。
  つまり「裁きの光」という**旧・技の実体だけが宙に浮いている**。
- **トール案件が触った範囲**: `angelPlayback.test.ts` の入口表を**実装に合わせた**だけ
  (`ph-judgment` → `phill-cage-windup`)。**赤いテストを実装の実態へ追随させた**のであって、
  「裁きの光が出ない」ことを直したわけではない。
- **フィル案件で決めること**: ①旧「裁きの光」(光の柱)を**別枠の技として復活**させるか、
  ②**技ごと畳む**(3州のハンドラ+描画+`ghostTelegraph` の2件+▸の再生対象を一緒に消す。
  そこまでやれば完全性テストの走査からも消えるので台帳の残置も要らなくなる)か。
  **どちらもフィルの技構成=仕様の話**なので、トール案件では選ばない。
