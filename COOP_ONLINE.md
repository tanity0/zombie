# 乱入型 協力プレイ(CO-ON)— **オンライン基盤の依頼書(Codex向け)**

社長裁定 2026-08-01。前身は `GHOST_ONLINE.md` §6(2026-07-31の考察)と
**`DEVELOPMENT_LOG` v0.25.2585 の申し送り**(2026-07-31・「夜中に依頼するかも。一旦覚えておいて」=未発注)。
**このファイルが、その申し送りで言う「継ぎ目の契約1枚」**にあたる=**これで発注できる状態**。
ゲーム側の遊びの仕様は**別**(§9)。

### 2026-07-31 に合意済みだった前提(そのまま生きている)
1. **通信の土台はゲーム非依存として切り出し、Codex へ発注可**(新規ディレクトリのみ=**衝突ゼロ**)。
2. こちらは**継ぎ目の契約(`createNetLink` 相当)を1枚書き**、**ループバックの偽トランスポート**で
   状態同期を**先行開発**する(§1-6)。
3. **一晩の現実的な到達点は「ゲストがホストのボス戦を観戦できる」まで**(A+B・§6-0)。

---

## 1. 何を作るのか(社長の言葉のまま)

> 普通のプレイ中に、**乱入する形でボス戦を手伝う**ってやつ。
> **つながらなければ AI がそのままシームレスに引き継ぐ**。
> **ボス倒すか死んだら終わり。**
> オンラインならではの仕様は多少必要だろうけど、それはゲーム側だから、
> 一旦**オンラインのインフラを整える** Codex への依頼を。

### この依頼書の範囲
| | 中身 | 担当 |
|---|---|---|
| **やる(この依頼書)** | 匿名ID / **シグナリングサーバ** / 募集と待ち合わせ / P2P接続(オファー・アンサー・ICE・STUN/TURN) / **DataChannel 2本** / 再接続・切断検知・**RTT/loss の統計** / 中継フォールバック / バージョン照合 / 接続の一生 / 安全弁 / **偽トランスポート(ループバック)** / **ゲーム抜きの疎通テストと単体ページ** | **Codex** |
| **やらない(別途・ゲーム側)** | 何をスナップショットに載せるか、入力の解釈、乱入されるかどうかの条件、UI、演出、遅延補正 | 設計チャット→実装 |

**Codex はゲームのコードを読まなくてよい。** 触るのは `src/online/` 配下だけ。
オンライン層は **`Uint8Array` を運ぶ土管**であって、中身を一切解釈しない。

### 1-2. ★救助ボタン — 遊びの流れ(社長設計 2026-07-31・会話ログから復元 2026-08-01)
**この節は社長が昨日まとめた設計。文書化が漏れていたので復元して正本化する。**

> ホスト依存のP2P型で、**ホストは救助ON/OFFボタンをプレイ画面のはじっこにあるやつONにしてあり、
> ボス戦に突入**。すると、**ほかの人がプレイ中にランダムに何人か**…**救助を押すとプレイしながら裏で
> マッチング、そのワールドは一時保存されて、ボス戦に2Pで突入。撃破したら、解散。元の世界に戻る**。

| 段 | 誰が | 何が起きる |
|---|---|---|
| ① | ホスト | 画面の端の **救助 ON/OFF ボタン**を ON にしておく(常時表示・押しっぱなしの設定) |
| ② | ホスト | ボス戦に突入 → **募集が立つ**(= `advertise()`) |
| ③ | ゲスト | **プレイ中の何人かに「救助」の声がかかる**(ロビーで待たせない=MHのSOSフレアの利点だけ取る) |
| ④ | ゲスト | 救助を押す → **プレイしながら裏でマッチング** |
| ⑤ | ゲスト | 成立 → **自分のワールドを一時保存**して、ホストのボス戦へ2Pで突入 |
| ⑥ | 両者 | **撃破(or 死亡)で解散** → ゲストは**元の世界に戻る** |

### 1-3. 参加資格(社長の条件リスト + 設計側の追加1つ)
| 条件 | 誰が判定するか |
|---|---|
| **バージョンが一緒** | **オンライン層**(§5-4) |
| 救助できる状態(ボス戦中でない・イベント中でない) | ゲーム側 |
| **通信遅延が高すぎない** | **オンライン層**(`rttMs()`) |
| ほかの救助に参加していない / 救助されていない | **オンライン層**(1セッションのみ) |
| **★ランク/ステージ帯が合う**(設計側からの追加提案・2026-07-31) | ゲーム側 |

★ランク帯を足す理由: **Lv低の人が裏ボス戦に呼ばれても一瞬で死んで何もできない=双方に不快**。
守護霊はビルド写し+ボスHP×1.6で釣り合いを取っているが、**生身の人間には「そもそも呼ばない」が
一番きれい**。オンライン層には**不透明な `matchKey` の一部**として渡す(意味を知らせない)。

### 1-4. ★最初に決めるべき論点 = ネットコードの権限モデル
**このゲームの核はカウンターで、窓は400ms。「誰が当たったと決めるか」で手触りが決まる。**

- **推奨=ハイブリッド**: **自分の攻撃・カウンター判定は各自のクライアントが宣言**、
  **ボスの状態(HP・技の選択・遷移)はホストが正**。Souls系と同じ妥協。400msの窓なら遅延100msでも成立する。
- **「全部ホスト判定」にすると、ゲストのカウンターだけ理不尽に落ちて即クソゲー化する。**

⇒ **これはゲーム側の設計だが、遊べるかどうかがほぼこれで決まる**ので、基盤より先に裁定が要る(§10)。

### 1-5. 設計上あらかじめ決めてある分岐(2026-07-31)
- **ホスト切断は"エラー"ではなく"設計された道"。** P2Pホスト依存の宿命。
  **ゲストは必ず自分の世界へ無傷で帰る**を第一級の分岐として作る(例外処理扱いにすると必ず事故る)。
- **一時保存は"ディスク保存"じゃなくていい。** 同一セッション内で戻るだけなら**メモリ上で凍結**+
  クラッシュ保険に localStorage スナップショット1枚で足りる。
  **完全なワールドシリアライズを作り込むと工数が跳ねる。ここは軽く。**
- **募集表示の競合**: 5人に出して1人しか入れないと4人が「満員でした」を食らう。
  **表示は多く・確定は先着1名で即時解決**し、**負けた側には通知を残さず消す**
  (=「押したのに無駄だった」を見せない)。
- **ゲストの動機**(自分のランを中断してまで行く理由): 推奨は
  **①同行枠の討伐記録(実装済み・「誰と倒したか」が残る)+②ゴールド/スクラップの少額固定**。
  **変動報酬にしない**——ホストが正のP2Pである以上、**改造ホストからの配布で経済が壊れる**。

### 1-6. ★偽トランスポート(ループバック)= 並行開発の鍵
**Codexが本物の通信を作るのと、こちらがゲーム側の同期を作るのを、完全並行にするための仕掛け。**
§4のインターフェースを満たす**同一プロセス内のループバック実装**を用意すれば、
**通信ゼロで2P同期を開発・検証できる**(同じタブに2つの世界を持って繋ぐ)。
- 最後に偽物→本物の差し替えは**1箇所**。
- しかも**一番危ない部分(状態同期)をネットワーク無しで検証済みにできる**。
- ★**この偽トランスポートも Codex の成果物に含める**(`src/online/loopback.ts`)。
  §4の型を満たし、遅延とパケットロスを**任意に注入できる**こと(遅延100ms/ロス5%等を再現して
  ゲーム側の補正を検証するため)。

---

## 2. なぜ「乱入型」だと設計が楽になるのか(★ここが今回の肝)

**ゲストは"2人目のプレイヤー"ではなく、既に居る守護霊(AI)の入力元を差し替えるだけ。**

この形にすると、オンラインで普通に一番面倒な部分が**丸ごと消える**:

| 普通の協力プレイで必要なもの | 乱入型では |
|---|---|
| 待ち合わせロビー / 「相手を待っています」画面 | **不要**。ホストは1人で戦い始めている |
| 参加者が揃うまでゲームを止める | **不要**。止めない |
| 参加/離脱の演出・状態遷移 | **不要**。霊の中身が入れ替わるだけ |
| 切断時の救済(片方が消えたゲームをどう続けるか) | **不要**。**AIが即座に引き継ぐ**=元の1人プレイに戻るだけ |
| セッションの終わり方の設計 | **不要**。**ボス撃破 or 死亡**で必ず終わる(ゲーム側が既に持つ終端) |

⇒ **オンライン層の責務は「バイト列を運ぶ」「切れたら黙って諦める」の2つだけ**になる。

### ★訂正: 「ボス戦だけなら通信量も軽い」は**間違い**(走査 2026-08-01 で判明)
当初「ボス戦だけにすれば敵60+弾130の問題が消える」と書いたが、**弾は消えない**。

| | 通常ラン | ボス戦 | 出典 |
|---|---:|---:|---|
| 敵の同時数(目標) | 10(ランク7成長で最大20) | **4**(`bossRelax` で `round(cap×0.4)`。R7最大でも8) | `scriptPuzzle.ts:182` / `directorTick.ts:477` |
| 敵の同時数(ボス専用ステージ) | — | **1体だけ**(`storyBossOnly` が全湧きを停止) | `campaign.ts:448,505` / `useGameLoop.ts:2410,10375` |
| **弾の同時数** | 上限なし | **上限なし。最悪128発** | 下記 |

**弾には上限が無い**(`projectiles.length` の参照は3箇所とも読み取りだけ。cap もプールも無い)。
境界は寿命 `ENEMY_PROJECTILE_DURATION = 4000ms` だけ。
そして**最悪ケースはボスの技そのもの**——ヨルムンガルドの螺旋全方位が **16発 × 8波 = 128発**を
2.1秒で撒くので、**128発が同時に生存しうる**(`useGameLoop.ts:840-841`)。
⇒ **「ボス戦だけ」で得られるのは通信量ではなく、上表の"セッションの作り"の単純化**である。
帯域はゲーム側の差分設計で解く問題として残る(§9)。**この依頼書(基盤)の見積もりを甘くしないこと。**

### 絶対に守る原則
1. **オフラインファースト。** 通信が全部死んでも**ゲームは完全に1人で遊べる**。
   このゲームはストア配布の単機アプリ(`CLAUDE.md` 配布方針)。通信を前提にした瞬間に体験が壊れる。
2. **ゲームはオンラインの成否を待たない。** すべて非同期・**例外を投げない**・失敗は `null` で返す。
3. **失敗を人に見せない。** エラーダイアログを出さない。繋がらなければ**無音で1人プレイのまま**。
4. **`?online=0` で全停止**(`GHOST_ONLINE.md` §2-11 と同じ安全弁)。

---

## 3. 境界(ここを破ったら作り直し)

### 3-1. ディレクトリと依存の掟
★**Codex の成果物は新規ディレクトリのみに閉じ込める。既存ファイルを1つも触らない**
(=同じブランチで並行しても**コンフリクトが起きない**。2026-07-31 の合意・`DEVELOPMENT_LOG` v0.25.2585)。

| 置き場 | 中身 |
|---|---|
| **`src/online/`** | クライアント側(§4の実装 / WebRTC / 偽トランスポート / 境界テスト) |
| **`server/`** | **シグナリングサーバ**(小さい WebSocket 1本。Cloudflare Worker 等) |
| **`tools/netcheck/`**(名前は任意) | **接続確認用の単体ページ**(ゲームとは別の開発ツール・§7) |

- クライアント実装は **`src/online/` 配下だけ**。
- **`src/online/` から `src/store` / `src/pixi` / `src/utils` / `src/world` / `src/components` / `src/hooks` を import してはいけない。**
- 逆向き(ゲーム側 → `src/online`)は、**§4のインターフェース経由のみ**。
- ★**この掟をテストで機械化すること**(`src/online/boundary.test.ts`)。
  `src/online/**/*.ts` を読み、禁止 import が1つも無いことを assert する。
  ※このリポジトリには同種の「掟をテストで縛る」前例がある(`src/utils/constitution.test.ts`)。同じ作法で。

### 3-2. オンライン層が知ってよいこと / 知ってはいけないこと
| 知ってよい | 知ってはいけない |
|---|---|
| ビルドのバージョン文字列 | 敵・弾・プレイヤーの型 |
| 匿名ID(UUID) | ボスの名前が何を意味するか |
| 「部屋のラベル」= **不透明な文字列**(ゲーム側が作る) | スナップショットの中身 |
| バイト列の長さ | バイト列の意味 |

**部屋の突き合わせに使う値も、オンライン層にとっては「一致するかどうか」だけの不透明な文字列**にすること
(ステージ名・ボス名の意味を知らせない)。

---

### 3-3. 足元の事実(走査 2026-08-01・Codexはこれを前提にしてよい)

| 事項 | 事実 | 出典 |
|---|---|---|
| **外部通信** | **1つも無い。** `fetch` は2箇所だけ(①ベンチの自サイトRTT計測 `BenchmarkOverlay.tsx:521` ②ローカル音声 `audioManager.ts:792`)。`WebSocket`/`XMLHttpRequest`/`sendBeacon`/`navigator.onLine` は**全て0件** | `src/` 全走査 |
| **依存** | dependencies は6つだけ(`react`, `react-dom`, `zustand`, `pixi.js`, `pixi-filters`, `lucide-react`)。**supabase/firebase 等は0件** | `package.json` |
| **Playwright** | `devDependencies` に `playwright ^1.61.1` が**既にある** → §7の疎通テストは依存追加なしで書ける | `package.json` |
| **`?online=0`** | **未実装。** `GHOST_ONLINE.md` §2-11 に仕様として書かれているだけ。**CO-1 で作る** | `src/` に該当識別子なし |
| **匿名UUID** | **未実装。** `GHOST_ONLINE.md` §2-2 は裁定だけ。**CO-1 で新規に作る**(既存キーの流用先は無い) | localStorage キー全走査 |
| **バージョン** | `__APP_VERSION__` = `package.json` の version を `vite.config.ts:57` で埋め込み。実行時にそのまま読める | `vite-env.d.ts:3` |
| **ネイティブラッパ** | **無い**(capacitor/tauri/electron の記述なし)。現状は素のWebビルド | `package.json`, `vite.config.ts` |
| **CI** | push毎に lint→typecheck→test→build(`ci.yml`)。`pages.yml` がこのブランチもデプロイする | `.github/workflows/` |

### ★時計が2系統ある(遅延補正に効くので基盤側も知っておくこと)
| 系統 | 何に使われているか | slow-mo の影響 |
|---|---|---|
| **`Date.now()`** | **カウンター窓(`COUNTER_WINDOW = 400ms`)**・被弾i-frame(`INVULN_MS = 700ms`)・刀/ワイヤーのダッシュ・守護霊の全クールダウン | **受けない** |
| **`gameTime`** | 敵の `aiPhaseUntil` / ボスの `bossStateUntil` / 予告・硬直・CD / サブウェポンCD | **受ける(伸縮する)** |

- ヒットストップ中は `timeScale = 0` で**ループが早期returnする**(`HITSTOP_MS = 100`)。
  **この間もオンライン層は動き続ける**こと(送受信を止めない=切断と誤認させない)。
- `rttMs()` は **`Date.now()` 基準**で測ること(ゲーム側のカウンター窓と同じ時計に揃える)。

---

## 4. ゲーム側との唯一の口(TypeScript・これが成果物の中心)

`src/online/types.ts` に置く。**Codex はこの型を満たすものを作る。** ゲーム側はこれしか触らない。

```ts
export type CoopRole = 'host' | 'guest';

/** 接続の状態。ゲーム側はこれを毎フレーム読んでよい(同期・軽い・例外を投げない)。 */
export type CoopStatus =
  | 'off'          // 無効(?online=0 / 基盤未設定 / 初期化失敗)
  | 'advertising'  // 募集中(まだ誰も来ていない)
  | 'connecting'   // 相手が来た。接続を確立中
  | 'connected'    // 疎通した。バイト列を運べる
  | 'closed';      // 終了(理由は onClosed で通知済み)

export type CoopCloseReason =
  | 'peer-left'      // 相手が抜けた/落ちた
  | 'timeout'        // 確立できなかった
  | 'version'        // ビルドのバージョンが違う
  | 'local'          // こちらから閉じた(ボス撃破・死亡・撤退)
  | 'error';         // それ以外(内部で握りつぶした結果)

/** 募集の内容。**中身の意味をオンライン層は知らない**(一致判定にしか使わない)。 */
export interface CoopAd {
  /** ビルドの識別子。違えば繋がない(毎日pushするプロジェクトなので必須)。 */
  buildVersion: string;
  /** 突き合わせ用の不透明なラベル(ゲーム側が作る。例: ステージとボスを表す短い文字列)。 */
  matchKey: string;
  /** 一覧に出す表示用の短い文字列(オンライン層は中身を検証だけして解釈しない)。 */
  label: string;
}

export interface CoopOpenRoom {
  roomId: string;
  label: string;
  matchKey: string;
  /** 募集が立ってからの経過(ms)。古い部屋を避けるのに使う。 */
  ageMs: number;
}

export interface CoopSession {
  readonly role: CoopRole;
  status(): CoopStatus;

  /** 状態や入力を送る。**到達保証なし・順序保証なし**(毎フレーム投げる用)。 */
  sendUnreliable(data: Uint8Array): void;
  /** 制御を送る。**到達保証あり・順序保証あり**(開始・終了・合意など)。 */
  sendReliable(data: Uint8Array): void;

  /** 受信。戻り値は購読解除。 */
  onMessage(cb: (data: Uint8Array, channel: 'unreliable' | 'reliable') => void): () => void;
  /** 相手が入って疎通した瞬間(status が 'connected' になった時)。 */
  onPeerJoined(cb: () => void): () => void;
  /** 終了。**必ず1回だけ呼ばれる**。ゲーム側はここでAIへ戻す(何もしなくてよい設計にする)。 */
  onClosed(cb: (reason: CoopCloseReason) => void): () => void;

  /** 往復遅延の実測(ms)。まだ測れていなければ -1。 */
  rttMs(): number;

  close(reason: CoopCloseReason): void;
}

export interface CoopApi {
  /** 使える状態か(?online=0・未設定・初期化失敗なら false)。**同期・例外なし**。 */
  enabled(): boolean;

  /**
   * ホスト: 手伝いを募集する。**部屋を立てた時点で resolve**(相手を待たない)。
   * 失敗したら null(ゲームは何事もなく続く)。
   */
  advertise(ad: CoopAd): Promise<CoopSession | null>;

  /** ゲスト: 入れる部屋を探す。失敗したら空配列。 */
  listOpen(matchKey: string, buildVersion: string): Promise<CoopOpenRoom[]>;

  /** ゲスト: 入る。失敗したら null。 */
  join(roomId: string): Promise<CoopSession | null>;
}
```

### 4-1. この口の約束(守れないなら設計が間違っている)
- **すべての関数が例外を投げない。** 内部の失敗は握りつぶして `null` / 空配列 / `'off'` にする。
- **`status()` と `rttMs()` は同期・軽い。** 毎フレーム呼ばれても平気なこと(通信しない・確保しない)。
- **`onClosed` は必ず1回だけ呼ばれる。** ゲーム側の「AIへ戻す」処理を確実に走らせるため。
- **`sendUnreliable` は詰まらない。** 送れない時は**黙って捨てる**(キューを溜めない=遅延の蓄積を作らない)。
- バイト列の**中身を見ない・書き換えない・ログに出さない**。

---

## 5. 中の作り(Codexの裁量。ただし選択理由を書き残すこと)

### 5-1. 方式(社長と合意済み・`GHOST_ONLINE.md` §6)
**ホスト権威 + WebRTC P2P。ゲームサーバーは作らない。**
- ホストが従来どおりシムを回す。ゲストは入力を送り、状態を受け取って描くだけ。
- **ロックステップ(両者が同じシムを回す)は採らない。**
  理由: `Math.random()` が非シードで全域に散在=決定性が無い。シード化は大改修。
- **サーバーでシムを回す案も採らない**(常駐費+移植コスト)。

### 5-2. 部品
| 部品 | 用途 | 備考 |
|---|---|---|
| 匿名UUID(localStorage) | 身元 | `GHOST_ONLINE.md` §2-2 の裁定と**同じIDを使い回す**(2つ作らない)。キー名は既存を調べて合わせる |
| **自前のシグナリング(社長裁定v0.25.2659=②で確定)** | **募集の掲示と待ち合わせだけ** | **小さい WebSocket 1本**(Cloudflare Worker 等)。SDP/ICE の交換に使う。**ゲームのデータは1バイトも通さない**。※公開ブローカー(PeerJS等)とSupabase Realtimeは**不採用**——テスト用に作った物を捨てることになるため、**最初から製品に持っていける形で作る** |
| WebRTC DataChannel × 2 | 本番の通信 | ①`unreliable`(`ordered:false, maxRetransmits:0`)=状態と入力 ②`reliable`=制御 |
| TURN(中継) | NAT越えに失敗した時だけ | Cloudflare を第一候補。**使用量を必ず計測してログに出す**(課金の崖はここにしか無い) |

### 5-3. 部屋のデータ(シグナリング側に置く最小限)
最小限にすること。**ゲームの中身を持たせない。**
★**永続データベースは要らない。** 部屋は**揮発でよい**(下の掃除のとおり90秒で消える)。
サーバー内のメモリ(Cloudflare なら Durable Object 等)で足りる。**選択と理由は Codex が書き残すこと。**

| 列 | 型 | 意味 |
|---|---|---|
| `room_id` | uuid | 主キー |
| `host_id` | uuid | 匿名UUID |
| `build_version` | text | 違えば一覧に出さない |
| `match_key` | text | 不透明な突き合わせ用 |
| `label` | text | 表示用(長さ上限+フィルタを通してから保存) |
| `created_at` | timestamptz | 古い部屋の掃除に使う |
| `state` | text | `open` / `taken` |

- **書ける相手を絞る**: 自分が立てた部屋しか更新・削除できないこと(`host_id` を持つ接続だけ)。
- **掃除**: 一定時間(叩き台=**90秒**)を過ぎた `open` の行は一覧から外し、削除する。
  ボス戦は長くても数分なので、部屋が残り続けることは無い。

### 5-4. バージョン照合(★必須)
このプロジェクトは**毎pushでバージョンを上げる**(`CLAUDE.md` Versioning)。
`__APP_VERSION__`(`vite.config.ts:57` で `package.json` の version を注入)を使い、
- **一覧の段階で弾く**(違うビルドの部屋を見せない)
- **接続直後の `reliable` ハンドシェイクでも突き合わせる**(掲示と実体がズレた場合の保険)
- 不一致なら `close('version')`。**人にエラーを見せない**。

### 5-5. 接続の一生
```
[off] --enabled()--> [advertising] --相手が来た--> [connecting] --疎通--> [connected]
   \                      |                            |                     |
    `---- 失敗は全部 ---> [closed] <---- timeout ------'                     |
                            ^                                                |
                            `------ peer-left / version / local -------------'
```
- **各段の失敗は必ず `closed` へ落ちる。** 宙ぶらりんの状態を作らない。
- `connecting` の**タイムアウト**(叩き台=**8秒**)を必ず持つ。
- **ホストは `advertising` のまま普通に戦っている。** 誰も来なくても何も起きない。

---

## 6. 作る順番と受け入れ条件

**一度に全部作らない。** 各段は**それ単体で動作確認できる**こと。

### 6-0. 全体の中でのこの依頼書の位置(2026-07-31 の見積もり)
co-op 全体は **A〜D の4段・合計5〜8バッチ**。**この依頼書は A を丸ごと Codex へ出すもの**。

| | 内容 | バッチ数 | リスク | 担当 |
|---|---|---:|---|---|
| **A. 通信の土台** | シグナリング + WebRTC DataChannel、2端末が繋がるだけ | 1 | 低 | **Codex(この依頼書=CO-1〜CO-4)** |
| **B. 状態同期の骨** | ホストが毎tickスナップショット送信 / ゲストは**シミュせず描画だけ**(=観戦モード) | **2〜3** | **高** | ゲーム側 |
| **C. ゲストが戦う** | ゲスト入力→ホストへ。**守護霊の椅子に遠隔プレイヤーを座らせる** | 1〜2 | 中 | ゲーム側 |
| **D. 救助フロー** | ON/OFFボタン・勧誘表示・世界の凍結/復帰・解散 | 1〜2 | 中 | ゲーム側 |

- **B が重い理由**: `useGameLoop` が唯一の書き手として敵・弾・エフェクトを毎tick全書き換えする作りなので、
  **「シミュせず受信状態を適用するモード」**を作るのが侵襲的。**ここさえ通れば残りは既存資産に乗る。**
- **順序の掟**: 最初の目標は **A+B の「観戦まで」**(ゲストの画面にホストのボス戦が映る)。
  **「まず救助UIから」は、一番簡単な所を作って一番危ない所を残すので順序として最悪。**
- A が Codex へ出るので、こちら側は実質 4〜6 バッチ。**しかも §1-6 の偽トランスポートで並行できる。**

| 段 | 中身 | 受け入れ条件(これが通れば次へ) | 社長の手 |
|---|---|---|---|
| **CO-1** ✅**検収合格 v0.25.2667** | `src/online/` の骨組み + §4の型 + 匿名UUID + `?online=0` + `enabled()` + **境界テスト** + ★**偽トランスポート `loopback.ts`**(遅延・ロスを任意に注入できる) | 境界テストが通る。`loopback` 越しに**同一プロセスで2者が双方向にバイト列を送受できる**(遅延100ms/ロス5%を注入しても壊れない)。**ゲーム側は1行も変わっていない** | **不要** |
| **CO-2** | **自前シグナリングサーバ(`server/`)をローカルで動かす** + `advertise` / `listOpen` / `join` | ローカル起動(`wrangler dev` 等)で、**同一PCの2タブ**が部屋を立てる→一覧に出る→入室まで到達する。**バージョンが違う部屋は出ない**。**まだデプロイしない** | **不要** |
| **CO-3** | **WebRTC DataChannel 2本** / `sendUnreliable` / `sendReliable` / `onMessage` / `rttMs` / `close` + **接続確認用の単体ページ** | ローカルサーバー越しに2タブが `connected` に到達し双方向に届く。**毎秒20回×数KBを流し続けて落ちない**。§7の疎通テストが通る | **不要** |
| **CO-4** | **デプロイして実機で繋ぐ** | **スマホ↔PC**で単体ページを開いて繋がる。RTTが表示される | **★要**(Cloudflare のアカウント) |
| **CO-5** | **TURN フォールバック** + 失敗時の無音ローカル落ち + **使用量の計測とログ** | P2P直結を意図的に塞いだ状態でも繋がる。**TURN経由かどうかがログで分かる** | **★要**(TURN の設定) |

★**CO-3 までは社長の手が一切要らない = 今すぐ着手できる。** アカウント作業が要るのは CO-4 から。
★**CO-1 の偽トランスポートが出来た時点で、こちら(ゲーム側)の状態同期の開発が並行で始められる**(§1-6)。

★**CO-5 までは、ゲーム側のコードを1行も変更しない。** ゲームとの接続は別の便で行う。

---

## 7. ゲーム抜きの疎通テスト(★これが無いと受け取れない)

**Codex は自分の成果物を、ゲームを起動せずに検証できること。**
このリポジトリには **Playwright が既に devDependency にある**(`package.json`)。これを使う。

- 2つのブラウザコンテキストを立ち上げ、片方が `advertise`、もう片方が `listOpen` → `join`。
- `connected` に到達し、双方向に既知のバイト列を送って**受信できること**を assert。
- 追加で確認すること:
  - 相手を強制的に閉じた時に **`onClosed('peer-left')` が1回だけ**呼ばれる
  - `buildVersion` を変えると **一覧に出ない**
  - `?online=0` で `enabled()` が false、`advertise` が `null`
  - `sendUnreliable` を送れない状態で連打しても**メモリが増え続けない**(捨てていること)
- ★**接続確認用の単体ページも作る**(ゲームとは別の開発ツール)。2端末で開いて
  「繋がった / RTT / ロス」が目で見える1枚。実機(スマホ↔PC)での確認はこれで行う。
- **このテストは既定の `npm test` に入れない**(通信を伴い遅く不安定なため)。
  `npm run test:online` のような別コマンドにする。

---

## 8. コストと制限(★着手前に Codex が現行値を確認して報告すること)

`GHOST_ONLINE.md` §4 の試算(非同期の守護霊オンライン)では**現実的な規模まで0円**だった。
乱入型は**シグナリングにしかサーバーを使わない**ので、サーバー費用はさらに小さい。
ただし**中継(TURN)だけは通信量に比例する**ので、ここだけ注意する。

- Supabase Realtime / Database の**現行の無料枠**(同時接続数・メッセージ数・容量)
- Cloudflare TURN(または代替)の**現行の無料枠と課金の単位**
- **1試合あたりの想定通信量**(ゲーム側の設計が決まってから確定。この依頼書では**計測できる形にしておく**ことだけ求める)

※**数字は必ず着手時点で確認し直すこと。** この依頼書に古い数字を書かない。

---

## 9. ゲーム側(この依頼書の対象外・後で設計する)

社長「オンラインならではの仕様は多少必要だろうけど、**それはゲーム側**」。
基盤ができてから設計する。**Codexは触らない。** ここには走査で判明した材料と論点を置く。

### 9-1. ★差し込み口は既に実在する(走査 2026-08-01)
**守護霊のAI判断は純関数1本に集約されており、全リポジトリで呼び出しは1箇所しかない。**

| 事項 | 実体 | 出典 |
|---|---|---|
| 判断の純関数 | `decideGhost(input: GhostDriverInput): GhostDecision`「毎tick1回呼ぶ純関数」 | `src/utils/ghostDriver.ts:591` |
| **呼び出し箇所** | **1箇所だけ** | `src/hooks/useGameLoop.ts:7388` |
| 戻り値 = ほぼ「入力」 | `moveX(-1..1)` / `moveY(-1..1)` / `action: 'shoot'\|'melee'\|'none'` / `targetId` / `facing` + 持ち越し状態 | `ghostDriver.ts:570-589` |
| 乱数 | `input.rand ?? Math.random` = **既に注入口が空いている** | `ghostDriver.ts:568, 593` |
| 同種の前例 | `?bot=<persona>` が**プレイヤーの入力をローカルで差し替えている**(「storeへは書かない=タッチUI非干渉」と明記) | `useGameLoop.ts:2038` / `:1936-1937` |

⇒ **ゲスト = `decideGhost` の戻り値を、ネット越しの相手の入力に差し替えるだけ**。
`useGameLoop` が唯一の書き手という原則は**壊れない**(差し替わるのは"判断"であって"書き込み"ではない)。

### 9-2. セッションの開始条件も既に実在する
守護霊は**ボス交戦の立ち上がりの瞬間にだけ**召喚される。**その瞬間が `advertise()` を呼ぶ瞬間**。

| 事項 | 実値 | 出典 |
|---|---|---|
| 召喚の契機 | 交戦の**立ち上がり(rising)**のみ・**900px以内**・`isEngageableBoss` | `directorTick.ts:678-683` |
| 交戦の距離 | ENTER **900px** / EXIT **1400px**(ヒステリシス) | `bossEngagement.ts:38-39` |
| 対象ボス | **12型**(`giantbat` / 裏ボス4 / 天使6 / `idol`)。`reaper`/`hunter`/`pumpkin` は意図的に除外 | `bossEngagement.ts:24-30` |
| 同時数 | 守護霊は**1体まで** | `directorTick.ts:667-676` |
| 終端 | ボスが `enemies` から消える or 守護霊HP0 | `directorTick.ts:669-674` |

### 9-3. 設計ドクトリンが最初からこれを想定していた(引用)
> **守護霊は独立した2人目のプレイヤーである。**共有帳簿・専用枠・ゴースト特有の仕組みを新設しない(恒久)。
> **将来オンラインで実プレイヤーに置き換わっても成立する形だけを作る。**
> — `BOT_AND_GHOST.md:670-680`(同文が `types/game.ts:778-782` にも)

守護霊の到達状況(走査で確認): 被弾する / ノックバックする / ヘイトを持つ / **プレイヤーと同じ
`COUNTER_WINDOW` でカウンターできる** / 弾を反射する / ビルド丸ごと持つ / 刀・ワイヤーは
**プレイヤーと同じ型の状態機械**。**意図的に違うのは2群だけ**(カメラ/時間演出、弾薬非消費など)。
※ `research/GHOST_PARITY_LEDGER.md` は 2026-07-30 のスナップショットで**現状と一致しない**
(以後 v0.25.2514〜2583 で消し込みが進んだ)。現在の到達を一覧にした台帳は**無い**。

### 9-4. 残る論点(ゲーム側で設計する)
- スナップショットに何を載せるか / 頻度 / 差分。
  **型が大きい**: `Enemy` **119**フィールド / `Projectile` **57** / `Player` **107** / `Summon` **59**、
  ストアの非関数フィールド **225**(うち配列35)。**丸ごと送るのは論外=白名簿方式が要る**。
- **弾の最悪ケース128発**(§2訂正)への差分設計。
- **スナップショットの取り込み口が存在しない**: `applySnapshot`/`restoreState`/`loadState`/`hydrate`/
  `replaceState` は `src/` に**0件**。ゲスト側の流し込み経路は新規に作る必要がある。
- **遅延とカウンター窓**: `COUNTER_WINDOW=400ms`(`Date.now()`基準)をゲストの往復遅延が圧迫する。
  ゲスト側だけ窓を甘くする補正が要るか。
- **人間が乗った時に倍率をどうするか**: 現在、守護霊が出たランは**ボスHP ×1.6**
  (`GHOST_BOSS_HP_MULT`)・**スコア ×0.5**(`GHOST_SCORE_MULT`)。人間が操作しても同じでよいか。
- **ゲストの世界の一時保存と復帰**(§1-2 ⑤⑥)。**メモリ上で凍結+クラッシュ保険にlocalStorage 1枚**で足りる
  (完全なワールドシリアライズは作らない=§1-5)。**ホスト切断でも必ず無傷で帰す**分岐を第一級で作る。
- 乱入される側の可否 / 片方がバックグラウンドへ行った時 / `close('local')` を誰が呼ぶか。

---

## 10. ★未決事項(社長裁定が要る)

1. ~~**アカウント作業**~~ → **裁定済み(v0.25.2659)**: シグナリングは**②自前**で確定したので、
   必要なアカウントは **Cloudflare 1つだけ**(シグナリングもTURNも同じ提供元で足りる)。
   **必要になるのは CO-4 から。CO-1〜CO-3 は社長の手なしで進む。**
2. **誰でも乱入できてよいか。** 公開(誰でも) / 合言葉 / 知り合いのみ、で作りが変わる。
   叩き台は**公開**(一番軽い)。
3. **同時に手伝えるのは1人でよいか。** 1人固定なら設計がずっと簡単(守護霊の枠が1つ=そのまま乗る)。
4. **乱入を受け付けない設定をゲーム側に置くか**(§9の論点だが、基盤の一覧の作りに影響するので先に知りたい)。
5. **守護霊のパリティ完走を前提条件にするか**(`GHOST_ONLINE.md` §6 では「事実上の前提条件」とされていた)。
   乱入型は「霊の入力元の差し替え」なので、**霊がプレイヤーとどこまで同じ土俵に立てているか**が
   そのまま体験の上限になる。
   ※走査の結果、**土俵はかなり揃っている**(被弾・ノックバック・ヘイト・カウンター・弾反射・
   ビルド丸ごと・刀/ワイヤーは同じ型の状態機械)。ただし**現在の到達を一覧にした台帳が無い**
   (`GHOST_PARITY_LEDGER.md` は2026-07-30のスナップショットで現状と一致しない)。
   前提条件にするなら**台帳の作り直しが先**になる。
6. **人間が操作している時の倍率**(§9-4): 守護霊が出たランは今 **ボスHP×1.6 / スコア×0.5**。
   人間が手伝った時も同じでよいか(「手伝ってもらったら報酬半分」でよいか)。
7. ★**ネットコードの権限モデル**(§1-4)。**基盤より先に決める**。推奨=ハイブリッド
   (自分の攻撃・カウンターは各自が宣言 / ボスの状態はホストが正)。**ここで手触りが決まる。**
8. ~~**シグナリングをどうするか**~~ → **裁定済み(社長 v0.25.2659)**: **②自前の小さいWebSocket 1本**。
   「基本的には2で、ただ、段階的にはやっていきたい」→ **最初から②を作り、段階を刻む**(§6)。
   公開ブローカーで一度作って捨てる遠回りをしない。
9. **ゲストの報酬**(§1-5): ①同行枠の討伐記録 + ②ゴールド/スクラップの少額固定、でよいか。
10. **`?online=0` 相当のUI**: 基盤側は `?online=0` を作るが、**プレイヤーが設定画面から切れる**必要があるか
    (あるならゲーム側の作業)。

---

## 11. 発注文(Codexへコピペする用)

### 11-1. CO-1(発注済み・v0.25.2667 検収合格)

**リポジトリ**: `https://github.com/tanity0/zombie`
**この依頼書のあるブランチ**: `claude/chat-context-continuity-saxlH`(※`main` にはまだ無い)

```
リポジトリ: https://github.com/tanity0/zombie

まず `claude/chat-context-continuity-saxlH` ブランチの `COOP_ONLINE.md` を最初から最後まで読んでください。
それがこの仕事の仕様書です。今回お願いするのは、その §6 の表の CO-1 だけです。

作業ブランチ: 上記ブランチから `codex/coop-online` を切って、そこへ push してください。
（PRは立てても立てなくてもよい。マージはこちらで行います）

■ CO-1 の成果物
1. `src/online/types.ts` — 仕様書 §4 の型をそのまま置く（CoopApi / CoopSession / CoopAd /
   CoopOpenRoom / CoopStatus / CoopCloseReason / CoopRole）。中身の実装はまだ無くてよい。
2. `src/online/id.ts` — 端末ごとの匿名UUID（localStorage）。無ければ生成、あれば再利用。
3. `src/online/config.ts` — `?online=0` で全部を無効にする安全弁。`enabled()` の判定。
4. `src/online/loopback.ts` — ★偽トランスポート。§4 の型を満たす「同一プロセス内の2者」を作り、
   遅延（ms）とパケットロス（%）を任意に注入できること。これがこの段の主役です。
5. `src/online/boundary.test.ts` — `src/online/**` が `src/store` `src/pixi` `src/utils` `src/world`
   `src/components` `src/hooks` を一切 import していないことを機械的に検証するテスト。

■ 受け入れ条件（これが通れば完了）
- 境界テストが通る。
- loopback 越しに、同一プロセスの2者が双方向にバイト列を送受できる。
  遅延100ms・ロス5%を注入しても壊れない（unreliable は落ちてよい／reliable は必ず届く）。
- `?online=0` で `enabled()` が false。
- `npm run typecheck` と `npm run lint` がエラー0。
- ★**既存ファイルを1つも変更していないこと**（新規ファイルのみ。`package.json` も触らない）。

■ 掟（仕様書 §3・§4-1 に詳細。特に重要な3つ）
- オンライン層はゲームを一切知らない。運ぶのは `Uint8Array` だけで、中身を解釈しない。
- すべての関数が例外を投げない。失敗は null / 空配列 / 'off' で返す。
- `status()` と `rttMs()` は同期・軽い（毎フレーム呼ばれても平気なこと）。

CO-2 以降（自前シグナリングサーバ、WebRTC、デプロイ、TURN）は、CO-1 を確認してから順に出します。
仕様書に書かれていない判断が必要になったら、勝手に決めずに質問してください。
```

### 11-2b. ★CO-2 検収結果(v0.25.2700 = **合格**・`codex/coop-online-2` を取り込み済み)

**成果物**: `server/`(worker.js / room-store.js / wrangler.jsonc / node:test 6本)+
`src/online/signaling.ts`(+4テスト)。`config.ts` / `id.ts` に追記あり(どちらも `src/online/` 内)。

| 検収項目 | 結果 |
|---|---|
| **境界**(新規ディレクトリのみ・既存ファイル不可侵) | **合格**。差分は `server/` と `src/online/` だけ。**ゲーム側から `src/online` を import している箇所は0**(=まだ1行も走らない) |
| **バージョン隔離** | **合格**。`RoomStore.list()` が `buildVersion` 不一致を**サーバ側で**弾く。テスト①が担保 |
| **匿名IDの流用** | **合格**。サーバは UUID 形式のみ受理。`id.ts` は古いWebView向けに `getRandomValues`→`Math.random` の代替経路を追加(「認証トークンではない」と明記あり) |
| 安全側の作り | **良**。1接続=1部屋 / 部屋数128 / メッセージ16KB / 接続256 / 開室TTL90秒 / SDP・ICEの形式検証 / Origin は localhost 系のみ(=**まだデプロイできない**=発注どおり) |
| **2タブの往復**(実走) | **未実施**。ストアのユニットでは通っているが、`wrangler dev` を起動しての実走は社長のPCが要る |

**検証ログ**: `npm run typecheck` / `npm run lint`(エラー0)/ `src/online` 9テスト /
`server` 6テスト、いずれも合格。

#### ★CO-3 へ持ち越す申し送り(3件・今は実害なし)
1. **`DEFAULT_SIGNAL_URL = 'ws://localhost:8787'` を出荷時の既定にしない。**
   いまは `src/online` をゲーム側が読んでいないので無害だが、**CO-3以降で配線した瞬間、
   製品版が「プレイヤー自身のPC」へ繋ぎに行く**。配信ビルドでは未設定(=`enabled()` が false)が既定であるべき。
   (なお https ページからの `ws://` は mixed content で塞がれるので、事故っても失敗側に倒れる。)
2. **自分の部屋に自分で入れる**。`join()` は host と guest の**接続**は区別するが、
   同じ端末IDが別接続で入るのを禁じていない。乱入型では「自分の卓に自分が乱入」は起こしたくない。
3. サーバの UUID 判定が **v1〜v5** を受理(`id.ts` が作るのは v4)。緩いだけで害は無いが、揃えると綺麗。

### 11-3. CO-3 の発注文(v0.25.2707・★品質監査の指摘を反映した版)

```
リポジトリ: https://github.com/tanity0/zombie
ブランチ: claude/chat-context-continuity-saxlH の `COOP_ONLINE.md` が仕様書です。
CO-2 は検収合格して取り込み済みです（あなたの `server/` と `src/online/` はそのまま生きています）。
今回は §6 の表の CO-3 だけをお願いします。

作業ブランチ: 上記ブランチから `codex/coop-online-3` を切ってください。

■ 触ってよい範囲（★CO-1/CO-2 から条件が変わっています。よく読んでください）
- **`src/online/` `server/` `tools/netcheck/` の中は、下の指示の範囲で既存ファイルを変更してよい。**
  既存のテスト（`src/online/signaling.test.ts` など）も、仕様変更に合わせて**更新してください**。
- **この3つのディレクトリの外は1ファイルも変更しないこと。**
  特にリポジトリ直下の `package.json` / `vite.config.ts` / `index.html` / `tsconfig*` は不可侵。
- 版数の bump・`src/data/changelog.ts`・`DEVELOPMENT_LOG.md` は**取り込む側でやります。触らないでください。**

■ CO-3 の成果物 = 「2タブが本当にP2Pで繋がって、双方向にバイト列が流れ続ける」まで

1. WebRTC で §4 の `CoopSession` を完成させる。
   - **DataChannel は2本。両方 `negotiated: true` で開き、`id` を固定**してください
     （`id: 0` = reliable / `id: 1` = unreliable）。`ondatachannel` は使わない
     ＝**どちらのチャネルか取り違える余地を無くす**ためです。
     - `unreliable` … `{ ordered: false, maxRetransmits: 0, negotiated: true, id: 1 }`
     - `reliable`   … `{ ordered: true, negotiated: true, id: 0 }`
   - **オファーは必ずホスト側から**（このゲームはホスト権威です）。ゲストは answer を返すだけ。
     perfect negotiation は要りません。
   - **`status()` が 'connected' になるのは「2本とも `open`」になった時だけ。**
     片方でも閉じたら即 `close`（半分だけ繋がった状態をゲームに見せない）。
   - **ICE候補は remote description を設定するまでキューに溜めて、設定後にまとめて流す。**
     （trickle なので候補が SDP より先に着くのは普通です。localhost では表面化せず、
     CO-4 の実機で初めて壊れる種類のバグなので、必ず対処してください。）
   - **ICE の確立タイムアウトは 20 秒**（モバイル回線を想定。CO-2 の WS 接続用 8 秒とは別物です）。
   - **STUN は `src/online/` 内の定数**（開発用に公開STUN 1つで可）。**TURN は CO-5 なので入れない。**
     ※本番でどこの STUN/TURN を使うかは CO-5 で決めます。ここでは開発用と分かるようコメントを。

2. 送受信の契約（★数値まで決めてあります。勝手に変えないでください）
   - **`binaryType = 'arraybuffer'` に固定**（既定はブラウザ差があります）。
   - **`onMessage` が渡す `Uint8Array` は、コールバックを抜けた時点で無効**とする
     （保持したい側がコピーする）。この寿命を README とコメントに明記してください。
   - **1メッセージの上限は 16KB**。超える送信は**黙って捨てる**（例外を投げない）。
   - **`sendUnreliable` は `bufferedAmount > 64KB` なら捨てる**（キューに溜めない）。
   - **`sendReliable` は `bufferedAmount > 1MB` になったら `close('error')`**
     （詰まったまま無制限に溜めない。ここが無いとメモリが増え続けます）。

3. 切断の検出（★どこで検出するかを固定します）
   - **シグナリングの WebSocket は、P2P 確立後は閉じてよい**（維持しない）。
   - **相手の離脱は DataChannel の close/error で検出**し、reason は **'peer-left' に正規化**する。
     **シグナリング経由のイベントに依存しない。**
   - **確立後に WS が切れても、それだけでセッションを 'error' にしない。**
   - **無通信ウォッチドッグ**: `connected` 後、**3 秒**どちらのチャネルでも受信が無ければ
     `close('peer-left')`。（ICE の `failed` は10〜30秒かかるため、これが無いと
     「繋がらなければAIが引き継ぐ」が10秒以上の棒立ちになります。数値は後で実機調整します。）

4. `rttMs()`（★同期・軽いことが絶対条件）
   - **`getStats()` の candidate-pair の `currentRoundTripTime` を 1 秒間隔で取得してキャッシュ**し、
     `rttMs()` はキャッシュを返すだけにする。**独自の ping フレームは作らないでください**
     （ゲーム用のチャネルに制御バイトが混ざると「運ぶのは `Uint8Array` だけ・中身を解釈しない」の
     掟が壊れ、ゲーム側に正体不明のバイトが届きます）。
   - 未測定の間は 0 ではなく **-1**（0 だと「とても速い」と誤読されるため）。意味をコメントに。
   - あわせて **`stats()` を足してください**（キャッシュ済み・同期・確保しない）。
     中身は `{ rttMs, packetsLost, bytesSent, bytesReceived }` 程度で構いません。
     ゲーム側が「回線が悪いのでAIに戻す」を判断する口になります。**後から足すと全経路の作り直しに
     なるので今入れます。**

5. 匿名IDと「自分の部屋に自分で入れない」の両立（★ここは矛盾しやすいので明示します）
   - `join()` で **`hostId === guestId` の入室を弾く**ようにしてください（自分の卓に自分が乱入しない）。
   - ただし **匿名IDは localStorage（オリジン単位）に保存**されているため、
     **同じオリジンの2タブは同じIDになり、このままでは2タブ確認が永久に成立しません。**
     → **`?anonid=<uuid>` で匿名IDを上書きできる**ようにしてください（開発・テスト専用）。
       2タブ確認とテストは、片方に `?anonid=` を付けて別IDにして行います。
   - サーバの UUID 判定は **v4 のみ**に揃える（`id.ts` が作るのは v4。いまは v1〜v5 を受理）。

6. 接続先の指定（★CO-2 の既定を変えます）
   - **`DEFAULT_SIGNAL_URL='ws://localhost:8787'` をやめ、既定は「未設定」に。**
     `?signal=` が無ければ `enabled()` は false（＝何もしない）。
     ゲームに配線した瞬間に製品版がプレイヤー自身のPCへ繋ぎに行くのを防ぐためです。
   - **`createSignalingApi({ signalUrl })` のように、URLを引数でも渡せるように**してください
     （netcheck の入力欄から指定できるように。CO-4 でスマホから毎回URLを手打ちしないため）。
   - ★**`loopback.ts`（CO-1 の偽トランスポート）はこの制限の対象外**にしてください。
     サーバ不要で動くのが存在意義なので、**loopback は従来どおり `?online=0` だけで判定**します。
   - 既存テストの期待値（`search:''` で `enabled()===true` 等）は、この変更に合わせて更新してください。

7. `tools/netcheck/` に接続確認用の単体ページ（ゲームとは別の開発ツール・§7）
   - 1枚のページで「接続先URLの入力欄 / 部屋を立てる / 一覧から入る / 繋がった / RTT /
     送受信レート / ロス」が**目で見える**こと。
   - `src/online/` の**実物を import** して使ってください（別実装を書かない）。
   - ★**ロス計測の連番は netcheck 側のペイロードの中に入れてください。**
     転送層（`src/online/`）に連番やロス集計を持ち込まないこと（「中身を解釈しない」掟）。
   - ★**自己完結させること**。`tools/netcheck/` が自分の起動手段（自分の package.json 等）を持ち、
     **リポジトリ直下のファイルを1つも変更しない**。起動手順は `tools/netcheck/README.md` に。
   - これは **CO-4 で社長がスマホ↔PC の実機確認に使うもの**です。1枚で完結させてください。

8. `server/` の Origin 許可を LAN まで広げる
   - いまは `localhost` / `127.0.0.1` / `[::1]` のみです。**CO-4 でスマホから繋ぐと 403 で弾かれます。**
   - **プライベートIP（`192.168.*` / `10.*` / `172.16〜31.*`）のオリジンも許可**してください。
     公開ホストを許可するのは CO-4 以降です。

9. 疎通テストを `npm run test:online` で回せるように（§7）
   - **実行系は Playwright**（このリポジトリの devDependency に既にあります）。
     `node-datachannel` 等の重い依存を新しく足さないでください。
   - ★**テストファイル名・置き場所は、リポジトリ直下の `vitest` が既定で拾わないもの**にしてください
     （直下の `vite.config.ts` は改変禁止なので、後から除外設定を足せません。
     `server/test/*.node.js` と同じ発想で、`*.test.ts` を避ける等）。
   - 見るもの: 双方向にバイト列が届く / 相手を強制的に閉じたら `onClosed('peer-left')` が**1回だけ** /
     `buildVersion` が違うと一覧に出ない / `?signal=` 無しで何も起きない / `?online=0` で全部止まる /
     **`sendUnreliable` を送れない状態で連打してもメモリが増え続けない** /
     **`sendReliable` が詰まったら `close('error')` になる**。

■ 受け入れ条件
- ローカルでサーバを起動し、`?signal=ws://localhost:8787` を付けた**同じPCの2つのタブ**
  （片方は `?anonid=` で別IDにする）が **`connected` に到達し、双方向に既知のバイト列が届く**。
- **1メッセージ 8KB を毎秒20回、双方向に3分以上流し続けて落ちない**
  （切れない・メモリが増え続けない）。※ 8KB は SCTP の断片化を跨ぐ実サイズとして指定しています。
- 相手を閉じると `onClosed('peer-left')` が**1回だけ**呼ばれる。
- **`?signal=` が無ければ何も起きない**（`enabled()` が false）。`?online=0` でも全部止まる。
- **`loopback.ts` は `?signal=` 無しでも従来どおり動く。**
- 繋がらない時は**例外を投げず**、`status()` が 'off' 側に落ちる。
- `npm run typecheck` と `npm run lint` がエラー0。CO-1 の境界テストが引き続き通る。
- **`npm test`（既定）に通信テストが混ざらないこと。**
- ★**触ってよい3ディレクトリの外を1ファイルも変更していないこと。**
- **まだデプロイしない。**

■ 性能について（このリポジトリの決まり）
- 実装と一緒に**負荷スコア（1/10〜10/10）とその理由**を報告してください。
- **受信経路で毎回アロケートしない**（20回/秒×2方向でGCが走ります）。
- **接続確立の処理（SDP生成・setLocalDescription 等）がフレームを止めないこと**
  （モバイルで「乱入した瞬間にカクつく」を作らないため）。

■ 掟（CO-1・CO-2 と同じ。特に重要な4つ）
- オンライン層はゲームを一切知らない。運ぶのは `Uint8Array` だけで、中身を解釈しない。
- すべての関数が例外を投げない。失敗は null / 空配列 / 'off' で返す。
- `status()` と `rttMs()` と `stats()` は同期・軽い（毎フレーム呼ばれても平気。通信しない・確保しない）。
- `sendUnreliable` は詰まらない。送れない時は**黙って捨てる**。

■ ゲーム側への申し送りを `src/online/README.md`（無ければ新規）に1節書いてください
- **`unreliable` は unordered なので古いスナップショットが後から届く。**
  受け手（ゲーム側）に**連番と棄却が必須**であること。
- `onMessage` の `Uint8Array` の寿命（コールバックを抜けたら無効）。

CO-4（デプロイして実機で繋ぐ）は、CO-3 を確認してから出します。**Cloudflare のアカウント作業は CO-4 からです。**
仕様書に書かれていない判断が必要になったら、勝手に決めずに質問してください。
```

### 11-2. CO-2 の発注文(v0.25.2669)

```
リポジトリ: https://github.com/tanity0/zombie
ブランチ: claude/chat-context-continuity-saxlH の `COOP_ONLINE.md` が仕様書です。
CO-1 は検収合格して取り込み済みです（あなたの `src/online/` はそのまま生きています）。
今回は §6 の表の CO-2 だけをお願いします。

作業ブランチ: 上記ブランチから `codex/coop-online-2` を切ってください。

■ CO-2 の成果物 = 「自前のシグナリングをローカルで動かして、2タブが待ち合わせできる」まで
1. `server/` … 小さい WebSocket のシグナリングサーバ 1本（Cloudflare Worker 想定）。
   - 役割は「部屋の掲示」と「SDP/ICE の橋渡し」だけ。**ゲームのデータは1バイトも通しません。**
   - 部屋は揮発でよい（永続DBを作らない）。90秒を過ぎた open の部屋は一覧から外して消す。
   - 自分が立てた部屋しか更新・削除できないこと。
   - ★**`server/` は自分の `package.json` を持ち、その中で完結させてください。**
     リポジトリ直下の `package.json` を含め、**既存ファイルは1つも変更しないこと**（CO-1と同じ掟）。
2. `src/online/` に、その サーバへ繋ぐ `CoopApi` の実装（`advertise` / `listOpen` / `join`）。
   - 接続先は `src/online/` 内の定数 + `?signal=ws://...` の上書きで決めてください。
     **Vite の設定やルートの env に手を入れないこと。** 既定はローカル（例 `ws://localhost:8787`）。
   - **WebRTC はまだ不要**です。この段は「部屋が見えて、入室が成立する」まで。
3. CO-1 の申し送り2点をここで直してください。
   - `config.ts` の `enabled()` が **「接続先が設定されていなければ false」** も見るようにする。
   - `id.ts` が `crypto.randomUUID()` を持たない環境でも ID を作れるようにする
     （取れない時に null のままにするなら、その扱いを README かコメントに明記）。

■ 受け入れ条件
- ローカルでサーバを起動した状態で、**同じPCの2つのタブ**が
  「片方が部屋を立てる → もう片方の一覧に出る → 入室が成立する」まで到達する。
- **buildVersion が違う部屋は一覧に出ない。**
- **`?online=0` で全部止まる**（CO-1 と同じ安全弁）。
- サーバに繋げない時は **例外を投げず**、`advertise` は null / `listOpen` は空配列。
- **まだデプロイしない。**
- `npm run typecheck` と `npm run lint` がエラー0。境界テストが引き続き通る。
- ★**既存ファイルを1つも変更していないこと。**

■ 掟（CO-1と同じ。特に重要な3つ）
- オンライン層はゲームを一切知らない。運ぶのは `Uint8Array` だけで、中身を解釈しない。
- すべての関数が例外を投げない。失敗は null / 空配列 / 'off' で返す。
- `status()` と `rttMs()` は同期・軽い（毎フレーム呼ばれても平気なこと）。

■ 起動手順を `server/README.md` に短く書いてください（こちらで再現するため）。

CO-3（WebRTC の DataChannel 2本 + 接続確認の単体ページ）は、CO-2 を確認してから出します。
仕様書に書かれていない判断が必要になったら、勝手に決めずに質問してください。
```

### 発注のしかた(段階を守る)
- **1回に1段だけ出す。** CO-1 が返ってきて確認できたら CO-2、という順で進める(社長指示「段階的にやっていきたい」)。
- **CO-3 までは社長のアカウント作業が要らない。** CO-4(デプロイ)の直前に Cloudflare のアカウントを用意する。
- **`main` にはこの依頼書がまだ無い。** Codex には必ず**ブランチ名を伝える**こと。
