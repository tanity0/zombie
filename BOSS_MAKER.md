# ボスメーカー(格ゲーのトレーニングルーム型・数字を画面で回す道具)

社長指示 2026-08-01:
> これはおそらくボスを作るのに大きな変革が必要で一個ずつやってられないので**覚悟を決めて取り組みます**。
> まず**ボスメーカー**を準備します。格ゲーのトレーニングみたいな部屋を用意。**広さ無限**。
> プレイヤーのHPは一旦**無敵**。**セレクト欄**でボスを選択すると画面に現れる。
> **右上に各技のパラメータを直接数字入れれるフォーム** ダメ、CD、頻度、遠近、速度など。
> **左上に行動パターンの同じくいじれる数字一覧**。
> これは技ごとに数値を画面上で打って、**リアルタイムで変更できる**もの。(**?パラメータは回りくどい**)
> **台本はここで作ってもらい、数字の調整はこのボスメーカーでやる。**
> 数字を合わせたら**コピーを押すと全数字がコピーされ、ここに貼り付けるだけで伝わる**。

## 0. なぜ要るか(この道具が解く問題)

現状のループは **「実機で遊ぶ → 社長が報告 → 調査 → コード修正 → push → 再度実機」**。
1周が長く、1日に数体しか進まない。しかも**数値の良し悪しは実機でしか分からない**ので、
このループを回さない限りバランスが決まらない。

ボスメーカーは**このループを画面内で完結**させる。数字を打った瞬間に反映されるので、
社長が「速い/遅い/届かない」をその場で詰められる。**設計チャットは台本(技の構成・ロジック)に専念し、
数字は社長が直接決める**、という分業になる。

---

## 1. 画面仕様

### 1-1. 部屋
- **広さ無限**: 壁なし・障害物なし・`clampRectToPlayableArea` の制限なし・画面外リサイクルなし。
- 地面は方眼(距離が目で分かること)。**100pxごとに薄い線、500pxごとに濃い線**+数字ラベル。
  → 「主戦帯300px」が目で見える状態にする(数字を詰める道具なので距離が読めないと話にならない)。
- カメラは通常どおりプレイヤー追従。ズームは通常と同じ規則。
- **敵は選んだボス1体だけ**。雑魚・イベント・湧きは一切出さない。

### 1-2. プレイヤー
- **無敵**(ダメージを受けるが死なない・HPは減らして表示だけする → 後述の受け入れ条件参照)。
  ※「一旦無敵」なので**トグルで切れる**ようにする(当たるかどうかの確認には被弾が要るため)。
- 武器・スキルは通常どおり選べる(最低限、初期装備で動けばよい)。

### 1-3. セレクト欄(ボス選択)
- 画面上部中央。**全ボス**(giantbat/thor/mimir/jormungand/skadi/miguel/jibril/rafi/uri/suriel/acrasiel/idol)。
- 選ぶと**その場で入れ替わる**(前の個体は消える)。HP満タン・Phase1から。
- **リセットボタン**: HPとフェーズと位置を初期化(フェーズ2を見たい時のために**フェーズ直接指定**も置く)。

### 1-4. 右上: 技ごとのパラメータ
選択中ボスの**全技**を縦に並べ、技ごとに数値入力欄を出す。最低限これらを揃える:

| 項目 | 意味 |
|---|---|
| ダメージ | その技の与ダメージ |
| 予告(windup) | 予告が出てから判定までの時間(ms) |
| 判定(active) | 判定が出ている時間(ms) |
| 硬直(recover) | 技後の硬直(ms)=プレイヤーの反撃窓 |
| CD | その技の再使用までの時間(ms) |
| 頻度(重み) | 抽選の重み。距離帯ごとに持つ技はゾーン別に出す |
| 距離(遠近) | 発動する距離帯の下限/上限 |
| 弾速 | 弾を出す技のみ |
| 判定サイズ | 半径 or 帯の長さ/半幅 |

### 1-5. 左上: 行動パターンのパラメータ
ボス全体の挙動。最低限:

| 項目 | 意味 |
|---|---|
| 移動速度 | 中立の移動速度(語彙ごとの倍率も) |
| 主戦帯 | 維持したい距離の下限/上限 |
| 休符 | ストリング終端の休み(ms) |
| ストリング段数 | Phase別 |
| 懲罰の閾値 | 遠距離◯秒 / 密着◯秒 / 同角度◯秒 |
| フェーズ閾値 | HP何%で移行するか |
| HP / 接触ダメージ | ボスの基礎値 |

### 1-6. コピー
- **「コピー」ボタン1つ**で、**現在の全数値**をテキストとしてクリップボードへ。
- 形式は**そのまま設計チャットに貼れば伝わるもの**。人が読めて、かつ機械が読み戻せること。
  推奨: 見出し+`key = value` の平文(JSONは読みにくいので**平文を主**、末尾にJSONを併記)。
- **差分が分かること**: 既定値から変えた項目に印を付ける(`*` など)。
  → 貼られた側が「どこを触ったか」を一目で追える。
- **貼り戻し**(ペーストして反映)も付ける。社長が前回の数字に戻せるように。

---

## 2. アーキテクチャ(これが「大きな変革」の中身)

### 2-1. 問題
ボスの数字は**モジュール定数**(`export const IDOL_TIMING = {...}`, `const MIMIR_BITE_CD_MS = 6000`)。
**定数は実行中に書き換えられない**ので、このままでは画面から触れない。

### 2-2. 方針: 「台本はコード / 数字はテーブル」
ボスごとに**可変の数値テーブル1個**へ寄せる。ロジック(台本)は今までどおりコードに置き、
**数値だけをテーブルから読む**。

```ts
// 例: src/utils/idolScript.ts
export const IDOL_TUNING = {
  timing: { aim: { windup: 700, active: 0, recover: 900 }, ... },
  rollDist: 140, punchRange: 90, punchHalfWidth: 30,
  neutralBand: { min: 200, max: 340 },
  ...
};
```

- **読み方は変えない。** 既存コードは既に使用時参照(`IDOL_TIMING.aim.windup` を tick 中に読む)なので、
  **オブジェクトへ寄せるだけで使用箇所の書き換えはほぼ発生しない**。
  素の数値定数(`const IDOL_ROLL_DIST = 140`)だけがテーブルのプロパティになる。
- **`as const` を外す**(実行時に書き換えるため)。型は明示的な interface で守る。
- **既定値は別に保持**(`IDOL_TUNING_DEFAULTS`)。リセットとコピーの差分表示に使う。

### 2-3. レジストリ
```ts
// src/utils/bossTuning.ts
registerBossTuning('idol', IDOL_TUNING, IDOL_TUNING_DEFAULTS, SCHEMA);
```
- `SCHEMA` = 画面に出すためのメタ情報(表示名・単位・最小/最大・所属グループ=技/行動パターン)。
  **UIはスキーマを読んでフォームを自動生成する**(ボスを足すたびにUIを書かない)。
- **1ボス対応 = テーブル + スキーマを1つ書くだけ**にする。ここを守らないと14体で破綻する。

### 2-4. やってはいけないこと
- **`?パラメータ` は使わない**(社長明示「回りくどい」)。全て画面から。
- **ゲーム本編の挙動を変えない。** テーブル化は**既定値が現行値と1つも変わらないこと**が絶対条件
  (=リファクタとして無変更であることをテストで担保する)。
- **判定と描画の一致を壊さない**(予告の figures は判定と同じ値を読み続けること)。
- ★**技ごとに違ってよい値は、必ず技ごとに持つ。**ボス共通の値からの流用は、
  **その技の性格を数字で作り分けられない**ことを意味する(=メーカーに欄が出ない)。
  **同じ間違いを2回している**:
  - **弾速**(v0.25.2627): `enemyUtils` の11ボス共通の1行から流用していた
  - **ダメージ**(v0.25.2629): 殴り/狙撃線が**接触ダメージ `stats.damage` を流用**していた
  **新しい技を足す時は、その技が使う値がどこから来ているかを必ず確認する。**
  同じ弾を撃つ技どうしでも共通化しない(社長指示v0.25.2628)。
  実例: aim と fan は同じ弾を撃つが、共通1組にすると「狙い撃ち=速い1発 / 連射扇=遅いが数で押す」が
  作れない。`bullet: { aim: {...}, fan: {...} }` のように**技名で割る**。
  ※`enemyUtils.getEnemyFireProfile` は**型ごと**の解決なので技差を表現できない。
  `registerEnemyFireProfile(type, ...)` は**型の既定**として残し、実際の値は
  **`createEnemyProjectile` の `profileOverride` 引数**で発射地点から渡す
  (`size` は x/y の逆算に使うので**生成後に上書きすると弾の位置がズレる**=生成時に渡すこと)。

### 2-5. 部屋の掃除(§1-1の履行・社長指示v0.25.2628「npcじゃま」)
**部屋にはプレイヤーとボスだけ。**「消し忘れが1つでもあると、調整中にそれが原因かどうか毎回疑う羽目になる」
のが一番のコスト。ボスを足す前に、部屋に何が居るかを**列挙して**潰す。現在の消し方(v0.25.2628時点):

| 消すもの | 方法 |
|---|---|
| 護衛NPC | `BOSS_TEST_RUN` に `bossmaker` を含める(resetGameが `escorts: []` にする) |
| 商人 / 二人組クエストNPC | resetGame で不在座標(1e9)/`status:'gone'` |
| 木・松明・緑卵・飾りの花 | world層の `set*Disabled(true)`(描画/判定/配置回避が一括で消える) |
| 寄り道POI(病院/武器庫/警察署)・裏ボス深層域 | resetGame の `detourVisible` / `hiddenBoss` を false/null |
| 雑魚湧き・イベント | `?nospawn=1`(部屋のURLに既定で入っている) |
| ゲームHUD | `bossMaker.hideHud`(既定ON・👁トグルで戻せる) |

---

## 3. 入口と配布上の扱い

- **入口は既存の「ボス戦テスト」メニュー**(`BossTestMenu.tsx`)に1項目足す形。
- 本編のプレイ導線からは入れない(誤爆防止)。
- ※本作は**アプリ配布**が最終形(CLAUDE.md 配布方針)。製品ビルドで隠すかは**★未決**として残す
  (開発中は出しておく方が便利。社長裁定)。

---

## 4. 受け入れ条件

1. **数字を打った瞬間に反映される**(リロード不要・ボス再召喚不要)。
2. **既定値が現行の実装値と完全一致**(テーブル化で挙動が1つも変わっていないこと。テストで担保)。
3. **ボスを切り替えても数値が混ざらない**(ボスごとに独立)。
4. **コピー → 貼り付け → 反映** が往復できる(貼り戻しで同じ状態に戻る)。
5. **無敵のON/OFF**が切り替えられる。
6. **距離が目で分かる**(方眼+ラベル)。
7. **React再描画の規律を守る**(CLAUDE.md): フォームは毎フレーム再描画しない。
   ボスの生値を出す表示(HP・距離・現在の状態)が要る場合は**専用の小コンポーネントに隔離**する。
8. `npm run typecheck` / `npm run lint` エラー0。

## 5. 負荷スコア

**1/10。** 追加コストは「定数直読み → オブジェクトのプロパティ読み」1段だけで、
毎フレームの実行回数は変わらない。UIはボスメーカー画面でのみマウントされ、本編には出ない。
方眼の描画はメーカー部屋限定。**本編の負荷は変化しない。**

---

## 6. フェーズ(1体で型を証明してから広げる)

| # | 内容 | 完了の判定 |
|---|---|---|
| **1** | **部屋+セレクト+アイドル1体**。テーブル化・スキーマ・フォーム・コピー | 社長がアイドルの数字を画面で詰められる |
| **1+** | **見出しの折りたたみ + 技/動きの個別再生(▶)**(社長要望v0.25.2625) | 畳んだ状態で開き、見たい技だけを即座に出せる |
| **2** | 残り11体のテーブル化(1体=テーブル+スキーマ) | 全ボスが同じUIで触れる |
| **3** | 貼り戻し・プリセット保存(localStorage)・既定との差分表示 | 数字の試行錯誤が往復できる |

**フェーズ1が動くまで他のボス作業は止める。**型が間違っていたら11体ぶん書き直しになるため。

**状況(v0.25.2625時点)**: フェーズ1 **完了**(v0.25.2621)+ 追加の使い勝手2件 **完了**(v0.25.2625)。
フェーズ2は未着手。**個別再生もスキーマ駆動**なので、ボスを足す側が `playables` / `onPlay` / `playState`
の3点を宣言すれば▶はそのまま動く(パネルの手書きは不要)。

---

## 7. ★未決(社長裁定)

1. **製品ビルドで隠すか**(開発中は出す前提。CLAUDE.md 配布方針=アプリ配布)。
2. **無敵の既定**: ON(仰せのとおり)/ ただしトグルは必須(被弾を確認したい場面があるため)。
3. **弾の当たり判定の可視化**を入れるか(`?bosshitbox=1` 相当を常時ONにできると数字合わせが速い)。
