# ボスメーカー(格ゲーのトレーニングルーム型・数字を画面で回す道具)

社長指示 2026-08-01:
> これはおそらくボスを作るのに大きな変革が必要で一個ずつやってられないので**覚悟を決めて取り組みます**。
> まず**ボスメーカー**を準備します。格ゲーのトレーニングみたいな部屋を用意。**広さ無限**。
> プレイヤーのHPは一旦**無敵**。**セレクト欄**でボスを選択すると画面に現れる。
> **右上に各技のパラメータを直接数字入れれるフォーム** ダメ、CD、頻度、遠近、速度など。
> **左上に行動パターンの同じくいじれる数字一覧**。
> これは技ごとに数値を画面上で打って、**リアルタイムで変更できる**もの。(**?パラメータは回りくどい**)
> **台本はここで作ってもらい、数字の調整はこのボスメーカーでやる。**
> 数字を合わせたら**コピーを押すと全数字がコピーされ、ここに貼り付けるだけで伝わる**。

## 0. なぜ要るか(この道具が解く問題)

現状のループは **「実機で遊ぶ → 社長が報告 → 調査 → コード修正 → push → 再度実機」**。
1周が長く、1日に数体しか進まない。しかも**数値の良し悪しは実機でしか分からない**ので、
このループを回さない限りバランスが決まらない。

ボスメーカーは**このループを画面内で完結**させる。数字を打った瞬間に反映されるので、
社長が「速い/遅い/届かない」をその場で詰められる。**設計チャットは台本(技の構成・ロジック)に専念し、
数字は社長が直接決める**、という分業になる。

---

## 1. 画面仕様

### 1-1. 部屋
- **広さ無限**: 壁なし・障害物なし・`clampRectToPlayableArea` の制限なし・画面外リサイクルなし。
- 地面は方眼(距離が目で分かること)。**100pxごとに薄い線、500pxごとに濃い線**+数字ラベル。
  → 「主戦帯300px」が目で見える状態にする(数字を詰める道具なので距離が読めないと話にならない)。
- カメラは通常どおりプレイヤー追従。ズームは通常と同じ規則。
- **敵は選んだボス1体だけ**。雑魚・イベント・湧きは一切出さない。

### 1-2. プレイヤー
- **無敵**(ダメージを受けるが死なない・HPは減らして表示だけする → 後述の受け入れ条件参照)。
  ※「一旦無敵」なので**トグルで切れる**ようにする(当たるかどうかの確認には被弾が要るため)。
- 武器・スキルは通常どおり選べる(最低限、初期装備で動けばよい)。

### 1-3. セレクト欄(ボス選択)
- 画面上部中央。**全ボス**(giantbat/thor/mimir/jormungand/skadi/miguel/jibril/rafi/uri/suriel/acrasiel/idol)。
- 選ぶと**その場で入れ替わる**(前の個体は消える)。HP満タン・Phase1から。
- **リセットボタン**: HPとフェーズと位置を初期化(フェーズ2を見たい時のために**フェーズ直接指定**も置く)。

### 1-4. 右上: 技ごとのパラメータ
選択中ボスの**全技**を縦に並べ、技ごとに数値入力欄を出す。最低限これらを揃える:

| 項目 | 意味 |
|---|---|
| ダメージ | その技の与ダメージ |
| 予告(windup) | 予告が出てから判定までの時間(ms) |
| 判定(active) | 判定が出ている時間(ms) |
| 硬直(recover) | 技後の硬直(ms)=プレイヤーの反撃窓 |
| CD | その技の再使用までの時間(ms) |
| 頻度(重み) | 抽選の重み。距離帯ごとに持つ技はゾーン別に出す |
| 距離(遠近) | 発動する距離帯の下限/上限 |
| 弾速 | 弾を出す技のみ |
| 判定サイズ | 半径 or 帯の長さ/半幅 |

### 1-5. 左上: 行動パターンのパラメータ
ボス全体の挙動。最低限:

| 項目 | 意味 |
|---|---|
| 移動速度 | 中立の移動速度(語彙ごとの倍率も) |
| 主戦帯 | 維持したい距離の下限/上限 |
| 休符 | ストリング終端の休み(ms) |
| ストリング段数 | Phase別 |
| 懲罰の閾値 | 遠距離◯秒 / 密着◯秒 / 同角度◯秒 |
| フェーズ閾値 | HP何%で移行するか |
| HP / 接触ダメージ | ボスの基礎値 |

### 1-6. コピー
- **「コピー」ボタン1つ**で、**現在の全数値**をテキストとしてクリップボードへ。
- 形式は**そのまま設計チャットに貼れば伝わるもの**。人が読めて、かつ機械が読み戻せること。
  推奨: 見出し+`key = value` の平文(JSONは読みにくいので**平文を主**、末尾にJSONを併記)。
- **差分が分かること**: 既定値から変えた項目に印を付ける(`*` など)。
  → 貼られた側が「どこを触ったか」を一目で追える。
- **貼り戻し**(ペーストして反映)も付ける。社長が前回の数字に戻せるように。

---

## 2. アーキテクチャ(これが「大きな変革」の中身)

### 2-1. 問題
ボスの数字は**モジュール定数**(`export const IDOL_TIMING = {...}`, `const MIMIR_BITE_CD_MS = 6000`)。
**定数は実行中に書き換えられない**ので、このままでは画面から触れない。

### 2-2. 方針: 「台本はコード / 数字はテーブル」
ボスごとに**可変の数値テーブル1個**へ寄せる。ロジック(台本)は今までどおりコードに置き、
**数値だけをテーブルから読む**。

```ts
// 例: src/utils/idolScript.ts
export const IDOL_TUNING = {
  timing: { aim: { windup: 700, active: 0, recover: 900 }, ... },
  rollDist: 140, punchRange: 90, punchHalfWidth: 30,
  neutralBand: { min: 200, max: 340 },
  ...
};
```

- **読み方は変えない。** 既存コードは既に使用時参照(`IDOL_TIMING.aim.windup` を tick 中に読む)なので、
  **オブジェクトへ寄せるだけで使用箇所の書き換えはほぼ発生しない**。
  素の数値定数(`const IDOL_ROLL_DIST = 140`)だけがテーブルのプロパティになる。
- **`as const` を外す**(実行時に書き換えるため)。型は明示的な interface で守る。
- **既定値は別に保持**(`IDOL_TUNING_DEFAULTS`)。リセットとコピーの差分表示に使う。

### 2-3. レジストリ
```ts
// src/utils/bossTuning.ts
registerBossTuning('idol', IDOL_TUNING, IDOL_TUNING_DEFAULTS, SCHEMA);
```
- `SCHEMA` = 画面に出すためのメタ情報(表示名・単位・最小/最大・所属グループ=技/行動パターン)。
  **UIはスキーマを読んでフォームを自動生成する**(ボスを足すたびにUIを書かない)。
- **1ボス対応 = テーブル + スキーマを1つ書くだけ**にする。ここを守らないと14体で破綻する。

### 2-4. やってはいけないこと
- **`?パラメータ` は使わない**(社長明示「回りくどい」)。全て画面から。
- **ゲーム本編の挙動を変えない。** テーブル化は**既定値が現行値と1つも変わらないこと**が絶対条件
  (=リファクタとして無変更であることをテストで担保する)。
- **判定と描画の一致を壊さない**(予告の figures は判定と同じ値を読み続けること)。
- ★**技ごとに違ってよい値は、必ず技ごとに持つ。**ボス共通の値からの流用は、
  **その技の性格を数字で作り分けられない**ことを意味する(=メーカーに欄が出ない)。
  **同じ間違いを2回している**:
  - **弾速**(v0.25.2627): `enemyUtils` の11ボス共通の1行から流用していた
  - **ダメージ**(v0.25.2629): 殴り/狙撃線が**接触ダメージ `stats.damage` を流用**していた
  **新しい技を足す時は、その技が使う値がどこから来ているかを必ず確認する。**
  同じ弾を撃つ技どうしでも共通化しない(社長指示v0.25.2628)。
  実例: aim と fan は同じ弾を撃つが、共通1組にすると「狙い撃ち=速い1発 / 連射扇=遅いが数で押す」が
  作れない。`bullet: { aim: {...}, fan: {...} }` のように**技名で割る**。
  ※`enemyUtils.getEnemyFireProfile` は**型ごと**の解決なので技差を表現できない。
  `registerEnemyFireProfile(type, ...)` は**型の既定**として残し、実際の値は
  **`createEnemyProjectile` の `profileOverride` 引数**で発射地点から渡す
  (`size` は x/y の逆算に使うので**生成後に上書きすると弾の位置がズレる**=生成時に渡すこと)。

### 2-6. 自動保存(社長要望v0.25.2631「毎回やり直すのは無理」)
- 触った瞬間に端末へ**差分だけ**保存(`bossmaker.tuning.v1.<boss>`)。まとめ書き(400ms)でスクラブ中は書かない。
- ★**復元は「部屋の中だけ」**。数値テーブルは**本編と同じ実体**なので、無条件復元は**調整値が本編のボスに乗る**。
  - **入る時** = `applySavedTuning`(必ず既定へ戻してから保存ぶんを適用)
  - **出る時** = `resetTuning`(必ず既定へ)
  - この対をテストで固定してある(`bossTuning.test.ts`「部屋の中だけ復元し、出る時は必ず既定へ戻る」)。
- 保存は**差分**なので、項目を増減しても古い保存が壊れない(知らない項目は無視・範囲外は丸める)。
- 「リセット」ボタンは**保存も消す**(次に入っても既定から)。

### 2-5. 部屋の掃除(§1-1の履行・社長指示v0.25.2628「npcじゃま」)
**部屋にはプレイヤーとボスだけ。**「消し忘れが1つでもあると、調整中にそれが原因かどうか毎回疑う羽目になる」
のが一番のコスト。ボスを足す前に、部屋に何が居るかを**列挙して**潰す。現在の消し方(v0.25.2628時点):

| 消すもの | 方法 |
|---|---|
| 護衛NPC | `BOSS_TEST_RUN` に `bossmaker` を含める(resetGameが `escorts: []` にする) |
| 商人 / 二人組クエストNPC | resetGame で不在座標(1e9)/`status:'gone'` |
| 木・松明・緑卵・飾りの花 | world層の `set*Disabled(true)`(描画/判定/配置回避が一括で消える) |
| 寄り道POI(病院/武器庫/警察署)・裏ボス深層域 | resetGame の `detourVisible` / `hiddenBoss` を false/null |
| 雑魚湧き・イベント | `?nospawn=1`(部屋のURLに既定で入っている) |
| ゲームHUD | `bossMaker.hideHud`(既定ON・👁トグルで戻せる) |

---

## 3. 入口と配布上の扱い

- **入口は既存の「ボス戦テスト」メニュー**(`BossTestMenu.tsx`)に1項目足す形。
- 本編のプレイ導線からは入れない(誤爆防止)。
- ※本作は**アプリ配布**が最終形(CLAUDE.md 配布方針)。製品ビルドで隠すかは**★未決**として残す
  (開発中は出しておく方が便利。社長裁定)。

---

## 4. 受け入れ条件

1. **数字を打った瞬間に反映される**(リロード不要・ボス再召喚不要)。
2. **既定値が現行の実装値と完全一致**(テーブル化で挙動が1つも変わっていないこと。テストで担保)。
3. **ボスを切り替えても数値が混ざらない**(ボスごとに独立)。
4. **コピー → 貼り付け → 反映** が往復できる(貼り戻しで同じ状態に戻る)。
5. **無敵のON/OFF**が切り替えられる。
6. **距離が目で分かる**(方眼+ラベル)。
7. **React再描画の規律を守る**(CLAUDE.md): フォームは毎フレーム再描画しない。
   ボスの生値を出す表示(HP・距離・現在の状態)が要る場合は**専用の小コンポーネントに隔離**する。
8. `npm run typecheck` / `npm run lint` エラー0。

## 5. 負荷スコア

**1/10。** 追加コストは「定数直読み → オブジェクトのプロパティ読み」1段だけで、
毎フレームの実行回数は変わらない。UIはボスメーカー画面でのみマウントされ、本編には出ない。
方眼の描画はメーカー部屋限定。**本編の負荷は変化しない。**

---

## 6. フェーズ(1体で型を証明してから広げる)

| # | 内容 | 完了の判定 |
|---|---|---|
| **1** | **部屋+セレクト+アイドル1体**。テーブル化・スキーマ・フォーム・コピー | 社長がアイドルの数字を画面で詰められる |
| **1+** | **見出しの折りたたみ + 技/動きの個別再生(▶)**(社長要望v0.25.2625) | 畳んだ状態で開き、見たい技だけを即座に出せる |
| **2** | **射撃部品(§10)**=画面から新しい弾撃ち技を足せる(8枠) | 社長が「通常弾/連射の弾幕」を画面だけで作れる |
| **3** | **台本エディタ(§11)**=段の並べ替え・台本の増減 | 「何かの後に撃つジャブ」が作れる |
| **3+** | **ノックバック距離(§13)** ✅ v0.25.2653 / 手動スロット(未) | 「押しやる殴り」が作れる |
| **4** | 残り11体のテーブル化(1体=テーブル+スキーマ) | 全ボスが同じUIで触れる |

**フェーズ1が動くまで他のボス作業は止める。**型が間違っていたら11体ぶん書き直しになるため。

**状況(v0.25.2642時点)**: フェーズ **1 / 1+ / 2 / 3 まで完了**。残りは **3+**(ノックバック距離・手動スロット)と
**4**(残り11体のテーブル化)。
**UIは全部スキーマ駆動**なので、ボスを足す側は `fields` / `playables` / `onPlay` / `playState` /
`scripts`(台本API)を宣言するだけで、パネルを一切書かずに同じ画面が生える。

---

## 7. ★未決(社長裁定)

1. **製品ビルドで隠すか**(開発中は出す前提。CLAUDE.md 配布方針=アプリ配布)。
2. **無敵の既定**: ON(仰せのとおり)/ ただしトグルは必須(被弾を確認したい場面があるため)。
   **→ 解決済み(v0.25.2630・社長裁定「おねがい」)**: メーカーの無敵は **HPを減らさず、ダメージは満額通す**。
   数字・フラッシュ・シェイク・ノックバックが正しく出る=「当たったことが分かる」。
   訓練(M0)の「HP1で踏みとどまる」とは**別物として分離**した(意図が違うため)。
   ※旧実装は両方を同じ `noDeath` に載せていたため、HPが1に達した瞬間から `amount=0` になり、
   **爆風経路(SEを無条件で鳴らす)は気づけるのに、狙撃線(`damagePlayer` 直呼び)は本当に無反応**という
   経路差が出ていた。これが社長報告「殴り、狙撃線は何も食らってない」の後半の正体。
3. **弾の当たり判定の可視化**を入れるか(`?bosshitbox=1` 相当を常時ONにできると数字合わせが速い)。
4. **距離帯の"数"そのもの**の増減(4つ=密着/主戦/遠/超遠)。**保留**(境界値は `zoneEdges.*` で既に可変)。

---

## 8. 社長の技の要望(控え)
> **通常弾 / 連射の弾幕 / 何かの後に撃つジャブ / ローリング / ダッシュ詰め / 押しやる殴り**

**検算(v0.25.2638・射撃部品を入れた時点)**:

| 要望 | 作れるか | 作り方 |
|---|---|---|
| **通常弾** | ✅ | 射撃枠: 弾数1・斉射1 |
| **連射の弾幕** | ✅ | 射撃枠: 弾数5・斉射4・間隔120・斉射ごとの回転15° |
| **何かの後に撃つジャブ** | ⚠️ **半分** | 「短い予告の弾」は射撃枠で作れる。**「何かの後に」=台本の2段目に置くこと**は3便目(台本エディタ)待ち |
| **ローリング** | ✅ | 既存 roll(離脱距離を調整) |
| **ダッシュ詰め** | ❌ | **移動しながらの攻撃**という部品が無い(射撃部品は撃つ間その場に留まる) |
| **押しやる殴り** | ⚠️ **半分** | 既存 punch で殴れるが、**押しやる距離が全ゲーム共通**(§9-2)。3便目 |

⇒ **足りない部品は2つ**: ①台本の組み立て(段の並べ替え)②移動を伴う技(ダッシュ詰め)。
どちらも3便目以降の対象。

---

## 10. 射撃部品(v0.25.2638・2便目)

### 10-1. 考え方
弾を撃つ技は aim / fan / orb と3つ書いてきたが、**違いは数字だけ**だった(1発 / 扇3本 / 追尾)。
ならば「**弾数・斉射・広がり・速度・誘導・狙い方をパラメータにした1つの技**」を空枠で置いておけば、
社長が画面で新しい射撃技を作れる。**コードを書き足さずに技が増える。**

- **枠は8つ**(`s1`〜`s8`)。上限の理由: 技が多すぎるとプレイヤーが読めない(ERのボスでも実質10前後)。
  加えて**型・州名・スキーマを静的に持てる**上限にしておくと、動的な型を持ち込まずに済む。
- **既定は8枠すべて無効**。=**本編のアイドルの挙動は1ミリも変わらない**(テストで固定)。
- 画面の「**＋技**」で1枠ずつ現れる。足していない枠は**見出しごと出ない**(120行の欄が常時並ぶと道具として使えない)。
- **名前を付けられる**(「ジャブ」等)。見出しにも反映する。
  ※ただし**記録のキーは枠の記号(`idol-s3`)で固定**する——名前を変えた瞬間に記録が分断されるため。

### 10-2. 持っている数値(全部が数字=フォームがそのまま生える)
| 項目 | 意味 |
|---|---|
| 予告 / 硬直 | 中核6技と同じ |
| 弾数(1斉射) | 扇の本数 |
| 広がり | 弾1本あたりの開き角(度) |
| 斉射の回数 / 間隔 | 2以上で連射になる |
| 斉射ごとの回転 | 回る弾幕が作れる |
| 弾速 / ダメージ / 大きさ | 弾の三点セット(技ごと=§2-4の掟) |
| 誘導 | 旋回速度(度/秒)。0=直進 |
| 狙い方 | 0=撃つ瞬間 / 1=予告開始で固定 / 2=偏差(移動先を読む) |
| 出す距離帯 / 頻度 | 0密着〜3超遠 / 重み。0=台本に出ない(▶では出せる) |

### 10-3. 守っていること
- **予告は数字から引く。**弾数/広がり/狙い方を変えると**赤い線もその場で変わる**
  (§9の教訓「描画用に同じ数字を書いておく」は、テーブル化した瞬間に嘘になる)。
  同時に、**連射扇(fan)の描画に残っていた `0.14` のベタ書きも潰した**(同じ病気)。
- **公平性の検算に載る。**足した技も `idolFairnessP1/P2` に入る。予告を人の反応より短くすると
  テストが落ちる。誘導があってプレイヤー(104.4px/s)より速い弾は自動でC分類(=詰めるのが答え)。
- **州名は技の一覧から機械的に組む。**手書きの配列だと枠を足した時に
  「予告なのに赤くならない」「硬直の青白tintが出ない」が静かに漏れる(v0.25.2426の教訓の同型)。
- **狙い方=1(予告で固定)の時だけロック線を描く。**撃つ向きが決まっているのに線が追従すると嘘になる。

### 10-4. まだ無いもの(★未決)
1. ~~台本の組み立て~~ **→ §11 で解決(v0.25.2642)。** 射撃枠は台本エディタの段に直接置ける。
2. **移動を伴う技**(ダッシュ詰め)。射撃部品は撃つ間その場に留まる。
3. **予告SEが全技共通**(§9-1)。技を8つ足しても**音では聞き分けられない**。
4. **枠の並べ替え/複製**。いまは先頭の空き枠から順に埋まるだけ。

---

## 9. メーカーで触れない値の一覧(共有値の洗い出し・v0.25.2631)

**「技ごとに違ってよいのに、共通の1箇所から来ている値」**の一覧。社長が優先順位を決めるための材料。
※**弾速(v0.25.2627)・ダメージ(v0.25.2629)は同じ形の問題で、すでに技ごとへ分離済み。**

| # | 値 | いまの出どころ | 何が困るか | 直す重さ |
|---|---|---|---|---|
| 1 | **予告SE** | `BOSS_ALERT_SFX_KEY` 1種を全技で共有(`sfx.alert()`) | **音で技を聞き分けられない**。ソウル系は音で読ませるので効き目が大きい | 中(技ごとのキー+素材) |
| 2 | **ノックバック距離** | `PLAYER_KNOCKBACK_SPEED=460` / `PLAYER_KNOCKBACK_MS=260`(全ゲーム共通) | 「押しやる殴り」が作れない。技ごとの押し量が持てない | 小〜中(3便目で着手予定) |
| 3 | **ヒットストップ/シェイク** | カウンター時は `COUNTER_HITSTOP_MS` 等、被弾は `SHAKE_MS=280`/`SHAKE_MAG=16` | 大技と小技の「重さ」を作り分けられない | 中 |
| 4 | **多段ヒットの可否** | プレイヤーの被弾i-frameのみが間引く(技側に指定が無い) | 狙撃線は判定200msで**2ヒット**する。1発にしたいのにできない | 小(技ごとに「1回だけ」フラグ) |
| 5 | **判定の持続** | `timing[m].active`(**触れる**)。ただし**判定が毎フレーム出る技と1回だけの技が混在** | 同上。持続と多段はセットで設計が要る | 小 |
| 6 | **予告の色** | 天使系の共通ビーム/カプセル描画に固定 | 技の種類を色で区別できない | 中 |
| 7 | **予告の形** | 技ごとに固定の描画関数(線/カプセル/円) | 新しい技を足す時に「どの形か」を選べない | 大(射撃部品と一緒に設計するのが自然) |
| 8 | **連射扇の描画長さ** | `IDOL_FAN_VIS_RANGE=700`(pixi側の定数) | 判定を持たない"派手さの絵"なので実害は小さい | 小 |
| 9 | **カウンター半径** | `MELEE_RADIUS`(全ボス共通) | ボスごとの反撃距離が作れない | 中(全ボスに影響) |
| 10 | **起床距離** | `aggroRange`(=`LAB_VISION_RANGE` 200px) | 部屋では常時起きているので実害なし。本編用 | 小 |

### ★この洗い出しで見つけた実害(修正済み・v0.25.2631)
**予告の描画が判定値を"手で写した定数"を使っていた**(`pixiScene.ts`)。
`IDOL_AIM_WINDUP_MS_VIS=700` / `IDOL_FAN_WINDUP_MS_VIS=900` / `IDOL_PUNCH_WINDUP_MS_VIS=600` /
`IDOL_PUNCH_RANGE_VIS=90` / `IDOL_PUNCH_HALF_WIDTH_VIS=30` / `IDOL_BULLET_VIS_HALFWIDTH=8`。
⇒ **メーカーで判定側を変えても赤い予告は古い形のまま**=CLAUDE.md の禁止事項
「**赤いのに当たらない/赤くないのに当たる**」を踏む。**テーブルを直接読む関数へ差し替え済み。**
**教訓: 「描画用に同じ数字を書いておく」は、テーブル化した瞬間に必ず嘘になる。**

---

## 11. 台本エディタ(v0.25.2642・3便目)

### 11-1. 社長の言い方(確定)
> これ技を**パズルピースみたいに**できる? / で、**遠近中の枠にはめていく**。
> **台本の数の+-** / **技の段は入れ放題**

⇒ **+/− が増減させるのは「台本(ストリング)の本数」**。段(技)は行の中で好きなだけ積める。

### 11-2. 画面
**1本の台本 = 1行のチップの並び。**
- 行の頭に **距離帯(選択)** と **重み(±)**、行末に **✕(台本を消す)**。
- 段のチップを押すと小さなメニュー: **← → で並べ替え / 差し替え / 後ろに足す / 消す**。
- **フェーズ上限で切られる段は薄く表示**(P2でも届かない段は取り消し線)。
  社長要望「書いたのに出ない」を目で分かるようにするため。
- 段に置ける技 = **中核6技 + 有効な射撃枠**。⇒ **2便目と3便目が噛み合う**
  (射撃枠で「ジャブ」を作り、台本エディタで殴りの後ろに置く、が画面だけで完結する)。
- 警告は**止めずに出すだけ**(「この距離帯に重み>0の台本が無い」等)。作業途中を弾かない。

### 11-3. 守っていること
- **編集は必ず純関数を通す**(`src/utils/bossScriptEdit.ts`)。UIが配列を直接触ると検証を素通りする。
- **最後の1本 / 最後の1段は消せない。**台本ゼロ・段ゼロ=ボスが何もしなくなるため。
- **配列の参照を保つ。**ゲームは `IDOL_TUNING.strings` を毎フレーム読むので、
  新しい配列に差し替えると読んでいる側が古いものを見続ける。
- **部屋を出たら既定へ戻る**(`resetTuning` が台本もリセット)。数値と同じ扱い。
- **保存・コピー・貼り戻しに乗る。**形式は `zone|weight|move,move;…` の1行。
  読み戻す時、**知らない技名は落とす**——射撃枠を消した後に古い保存を読んでもボスが固まらない。
- ★**重みの欄を数値スキーマから外した。**旧実装は `strings.${i}.weight` と**添字でパスを作っていた**ので、
  台本を1本消すと**以降の重みが1つずつ手前の台本に化ける**。台本が可変になった以上、添字パスは持てない。
  (テストで固定済み: `strings.` で始まるパスがスキーマに存在しないこと)

### 11-4. まだ無いもの
1. **ノックバック距離**(§9-2)=「押しやる殴り」。次(3+便目)。
2. **移動を伴う技**(ダッシュ詰め)。
3. **技ごとの予告SE**(§9-1)。技を8つ足しても**音では聞き分けられない**。

---

## 12. エフェクトの速さ(v0.25.2651)

### 12-1. きっかけ
> **エフェクトの速さもいじりたい。拳とかダメージ判定早いのにエフェクト遅いので**(社長)

調べた結果、**判定と絵はズレていなかった**——拳は溜め(600ms)を**まるまる使って等速で伸び**、
判定の瞬間にちょうど届く実装だった。**ズレていないのに遅く見える**のは、
**0.6秒かけて等速で伸びる動きが「突き」に見えない**から(拳は本来ためて一気に出る)。

⇒ 直すべきは**着弾の瞬間ではなく、そこへ至る動き方**。よって「速さ」を数値にした。

### 12-2. 欄(至近の殴りの節に並ぶ)
| 欄 | 意味 |
|---|---|
| **拳が動く区間** | 溜めの**後ろ何割**で伸ばすか。1.0=溜め全体(旧・遅い) / **0.35=最後の35%で突き出す(既定)** |
| **拳の伸び方** | カーブ。1=等速 / 大=溜めてから伸びる / 小=出だしが速い |
| **拳が残る時間** | 当たってから消えるまで(ms) |

### 12-3. 掟(この節を増やす時に必ず守ること)
★**「速さ」を触っても、絵が完成する瞬間は判定の瞬間から動かさない。**
`idolFistReach(u, lead, ease)` は **u=1 で必ず 1 を返す**(どのパラメータでも)。
これを崩すと「絵はまだ届いていないのにダメージが入る」「絵が当たったのにダメージが無い」に
なる=CLAUDE.md の禁止事項そのもの。**テストで全パラメータ組合せを機械検査してある。**

⇒ 他の技にエフェクト欄を足す時も、**変えてよいのは「動き出す時刻」と「途中のカーブ」だけ**。
**終点(判定の瞬間)は動かさない。**

### 12-4. 未対応
拳以外の絵(斬撃の弧・砂埃・リング等)はまだ欄が無い。ただし**それらは「派手さの絵」(分類②)**なので、
判定と揃える義務がなく、優先度は低い。**分類①(危険を伝える絵)の動きが遅い**という報告が来たら足す。

---

## 13. 技ごとの押し出し(v0.25.2653)

### 13-1. きっかけ
社長要望「**殴りは一気に中距離まで押しやる技にしたい**」(§8)。
押し量は**全ゲーム共通の1組**(`PLAYER_KNOCKBACK_SPEED=460 / MS=260`)だったので、
**技の性格を数字で作れなかった**。弾速(v0.25.2627)・ダメージ(v0.25.2629)と**まったく同じ形の問題**。

### 13-2. 欄(至近の殴りの節)
| 欄 | 既定 | 意味 |
|---|---|---|
| 押し出す距離 | **60px** | 進む距離。共通の被弾ノックバックと同じ値=**既定では挙動不変** |
| 押し出す時間 | 260ms | 短いほど鋭く飛ぶ |

★**距離(px)で入れる。**「中距離まで押しやる」は**距離の話**であって速度×時間の話ではない。
速度は `knockbackSpeedFor(距離, 時間)` が逆算する
(吹き飛びは1→0へ直線減衰するので `距離 = 初速 × 秒 ÷ 2` ⇒ `初速 = 距離 × 2 ÷ 秒`)。

### 13-3. 配管(他のボス/技へ広げる時の道)
`hitCapsule(..., knockback)` → `PumpkinBlast.kbSpeed/kbMs` → `combatTick.applyPumpkinBlastDamage`。
**未指定なら従来の共通値**なので、既存の全ボス・全技は1つも変わらない。
爆風パイプラインに相乗りしている技(城ボスの踏み鳴らし・トールのジャンプ等)は、
積む所で `kbSpeed/kbMs` を渡すだけで技ごとの押し量を持てる。

### 13-4. ★踏んだ地雷(同型の再発防止)
減衰は `(残り時間) ÷ 持続時間` で計算するが、**割る側が共通定数のままだった**。
技ごとに持続時間を変えた瞬間、**長い吹き飛びで減衰率が1を超え、初速が跳ね上がる**。
⇒ `Player.knockbackMs` を持たせ、**その吹き飛び自身の持続時間で割る**ようにした。
**押し量を書く場所では、必ず持続時間も一緒に書く**(前の技の持続時間が残ると初速が合わなくなる)。

---

## 14. ヘルプ(用語の説明)【v0.25.2654 仕様・実装はSonnet】

### 14-1. きっかけと、本当の原因
> **ヘルプ必要だわ、懲罰とか色々意味わからないのある**(社長)

★**原因は「説明が無い」ではなく「説明が見えない」。**
欄には既に `hint` があるが、描画は `title` 属性=**PCのマウスホバーでしか出ない**。
このゲームは**スマホ縦持ちで触る道具**なので、hint は事実上**1つも読めていなかった**。
(同型の事故: 「作ったのに見えていない」= CLAUDE.md 攻撃ヴィジュアル節の4連続事故と同じ形)

### 14-2. 仕様
1. **上部アイコン列に「？」トグル**を足す(他のトグルと同じ作法・端末に記憶)。
2. **ON の間だけ**、
   - 各欄の下に `hint` を**本文として**表示(`title` 頼みをやめる)。
   - 各**見出しの直下**にその節の説明を表示(下の `SECTION_HELP`)。
3. **OFF が既定**(普段は邪魔なので)。ONにした時だけ画面が説明で埋まる。
4. レジストリに `sectionHelp?: Record<string, string>` を足し、**ボス側が宣言**する
   (UIはボスを知らないまま出す=スキーマ駆動の作法を崩さない)。

### 14-3. 節の説明(そのまま実装に使う文言)
```
基礎値            このボスの土台。HP・移動速度と、体が触れた時のダメージ。
                  技ごとの威力は各技の欄にある(ここではない)。
間合い            ボスが保ちたい距離と、距離帯の境目。距離帯ごとに出す技が変わる。
中立の移動        技を出していない間の動き方。詰める/離れる/並走の速さと、次の技までの間。
ストリングと休符  技を何回続けて出すか(段数)と、その後に必ず入る休み。
                  この休みがプレイヤーの攻撃チャンス。0にすると反撃する暇が無くなる。
懲罰              プレイヤーが同じことを続けた時に、ボスが決まった返し技を出す仕組み。
                  例: 遠くに居続けたら狙撃線 / 密着し続けたら離脱。「見られている」感を作る。
台本              どの距離帯で、どの技を、どの順に出すか。ボスの手そのもの。
狙い撃ち(aim)     単発の弾。遠距離の圧の基礎。
連射扇(fan)       扇状に複数発。主戦帯の主力。
離脱ローリング    判定を持たない移動だけの技。密着から間合いを作り直す。
至近の殴り        目の前の帯にダメージ。密着に居座らせないための技。
狙撃線(snipe)     細長い帯。まっすぐ下がるだけでは避けられない=横に動かせる技。
追尾弾(orb)       追いかけてくる弾。走っても振り切れないので、詰めて内側に入るのが答え。
射撃枠            画面から足した弾撃ち技。数字を変えると赤い予告の線も一緒に変わる。
動き(単独再生)    技を出さず、移動だけを見るための再生ボタン。
```

### 14-4. 用語の説明(欄の hint。既にある物は上書きせず、無い欄に足す)
```
予告      攻撃が来ることを知らせる赤い figure が出てから、実際に当たるまでの時間。長いほど避けやすい。
判定      当たり判定が出ている時間。0=出た瞬間だけ。
硬直      技の後に動けない時間。ここがプレイヤーの反撃チャンス(820ms=近接1発ぶん)。
重み      抽選のくじの枚数。同じ距離帯の台本どうしで比べる。0=出ない。
主戦帯    ボスが保ちたい距離。これより遠いと詰め、近いと離れる。
密着帯の上限/中帯の上限/遠帯の上限
          距離帯の境目。密着/主戦/遠/超遠の4つに分かれ、どの台本を使うかが決まる。
段数      1回のストリングで出す技の数。フェーズ2で1段増える。
第二波    フェーズ2で、同じ技がもう一度追いかけてくる仕組み。避けが2回必要になる。
遠距離の長居  この時間だけ遠くに居続けると、懲罰の技が飛ぶ。
密着の居座り  この時間だけ密着し続けると、ボスが離脱する。
同角度の長居  同じ方向に居続けると、並走の向きが反転する(回り込みを読まれる)。
フェーズ2の閾値  HPがこの割合を下回ると第2形態になる。
接触ダメージ  体が触れた時のダメージ。技の威力とは別軸。
```

### 14-5. やらないこと
- **チュートリアル的な導線は作らない**(この道具は社長専用で、毎回読むものではない)。
- **説明を常時表示しない**(画面が説明で埋まると調整の道具として使えない)。

---

## 15. 台本の on/off【v0.25.2654 仕様・実装はSonnet】

### 15-1. きっかけ
> **台本のon off欲しい**(社長)

いまは「出さない」手段が**重み0**か**削除**しかない。
- 重み0 = **元の重みが失われる**(戻す時に何だったか分からない)
- 削除 = **段の並びごと消える**(作り直しになる)

⇒ **数字を保ったまま、一時的に外す**手段が要る。A/B比較の基本操作。

### 15-2. 仕様
1. `StringScript` に **`off?: boolean`** を足す(未指定=有効。既存の保存/台本は無変更で読める)。
2. `idolStrings()` が **off の台本を落とす**(=抽選に出ない)。**重みは保持したまま**。
3. 台本エディタの行頭に**トグル**を1つ。off の行は**行ごと薄く**表示(消さない)。
4. 編集操作 `toggleScript` を `bossScriptEdit.ts` へ追加(純関数・テスト付き)。
5. **直列化に載せる**: `zone|weight|moves` の zone 側へ接頭辞 `-` を付ける形
   (例 `-near|40|fan,orb`)。**接頭辞が無ければ従来どおり有効**=古い保存がそのまま読める。
6. ★**全部 off にはできない**(=最後の1本と同じ理由。ボスが何もしなくなる)。
   `scriptWarnings` は「この距離帯に生きた台本が無い」を **off も考慮して**出す。

### 15-3. 受け入れ条件
- off にしても**重みと段の並びが消えない**(onに戻すと元どおり)。
- off の台本が**抽選に出ない**(`pickStringScript` に渡る配列に含まれない)。
- 保存・コピー・貼り戻しで **on/off が往復する**。
- **古い保存(接頭辞なし)がそのまま読める**。
- 全部 off は弾く(最後の1本は off にできない)。

## 16. 画面外のボスマーク / 距離表示(v0.25.2657)

社長指示:
> (ゲーム中も)ボス交戦中は画面外のボスマーク表示
> ボスメーカー中は常にボスとの距離を表示

### 16-1. なぜ
アイドルは上下左右に大きく動くので**すぐ画面外へ出る**。出た瞬間「どっちに居るか」も
「どれくらい離れたか」も分からなくなり、距離帯(melee/near/mid/far)を詰める作業が止まる。
ゲーム本編でも同じで、裏ボス/天使/グレンは画面外へ出ると位置を見失う。

### 16-2. 仕様
- **マーク(方角)= ゲーム中も出す**。ボスが画面外に居る間だけ、画面の縁にドクロ+矢印(赤)。
  画面内は出さない(本体が見えているので不要)。
- **★出すのは「交戦中」だけ**(社長指示v0.25.2658)。「ボスが生きている」では出さない——裏ボスは
  逃げても巣で復活待機したまま生き続けるので、そのままだと**巣を指す矢印が一生残る**。
  交戦中の定義(`isEngagedBoss`)は次のどちらか:
  1. **帰巣/離脱中ではない**(`bossState !== 'return'`)= まだこちらを見ている。
     `'return'` を使うのは裏ボス4体のコントローラだけ(天使/idol/グレンは使わない=常に交戦中扱い。
     グレンはリーシュしない仕様なので正しい)。
  2. **直近6秒以内に殴った**(`lastHit`)。★この2番目が要る理由: 裏ボスは**画面外へ出た瞬間に
     `'return'` へ落ちる**ので、「マークを出したい状況」と「帰巣状態」が完全に重なる。
     殴り合いで繋がないと、画面外へ出た瞬間にマークまで消えて道具にならない。
- **距離(数字)= ボスメーカーの部屋の中だけ、常に**。画面外ならマークの下、画面内ならボスの頭上に
  `340px`。**部屋の外(本編)では数字を出さない**——計測値をゲーム画面へ持ち込まない。
- 距離は**プレイヤー中心↔ボス中心のワールド距離**。`zoneForDistance`(台本の距離帯)と**同じ測り方**に
  揃える(揃えないと「表示は melee なのに台本は near を引く」道具になる)。ズーム倍率は掛けない。

### 16-3. 誰が「ボス」か(`utils/bossMarker.ts` の `isMarkedBoss` 1箇所)
- **出す**: `isHiddenBoss`(裏ボス4体+天使6体+idol)/ `isStoryBoss`(グレン・未確認変異体)。
- **出さない**: 城ボス(giantbat)=城マーカー(`bossSpawned`=赤)が既に同じ方角を指している /
  hunter =「検知された時だけ」の赤矢印が既にある / pumpkin 等=同時に複数湧いて画面端が渋滞する。
  足したくなったら**この関数1本**を変える(全経路に効く)。

### 16-4. ★ズーム引き考慮(CLAUDE.md 必須項目)
ボス戦は常時 `BOSS_ZOOM_MIN`(既定0.7)まで引く=**可視域が画面の約1.43倍**ある。
`world - camera` を画面座標として扱うと、**まだはっきり見えている位置でマークが出る**。
よってワールド→画面は Pixi の実値(`worldGroup.scale` / `worldGroup.position` / `world.position`)から
`screen = origin + world × zoom` を組んで渡す(式を2箇所に持たない=カメラシェイクや寄せも自動で乗る)。
矢印の起点も同じ写像で出したプレイヤーの実画面位置を使う。
- ※**既存の方角マーク(弾/拠点/POI/イベント/護衛/ハンター等)は従来どおり等倍前提のまま**。
  引いている間だけ数十px ずれるが、今回の指示の対象外なので触っていない(直すなら社長判断)。

### 16-5. 負荷 **1/10**
描画は既存の `arrowGfx`(1枚のGraphics・既に毎フレーム再構築している)へ数十本のプリミティブを足す
だけで、同時に生きるボスは通常1体。距離ラベルは**焼いたビットマップフォント(`dmg-num`)のプール**
なので `Text` 生成(グリフのラスタライズ+GPUアップロード=ベンチで最重だった経路)は一切起きない。
新規の強glow・加算合成の大面積は増やさない。

---

## 17. 手動スロット(A/B比較の保存枠)【★棚上げ・未着手】

> ★**2026-08-03 社長指示により棚上げ。** 「行動の数字が細かすぎて使いづらい」(§18)が先。
> 下の仕様はそのまま残す(着手する時はここから)。
>
> **着手前監査は1回済み(結果は不合格)。** 着手する時は以下を先に直すこと:
> ①読込1タップで未保存の現在値が復旧不能に消える(退避が無い) ②`stats.*` は `BossMakerPanel.onChange` だけが
> 生きている個体へ反映しているので、枠の読込・貼り戻し・自動保存では**床のボスに効かない**(既存の穴)
> ③一致印は payload の生成を1本化(`buildTuningMachine`)し、**適用後に再生成した正規形**で比べないと常に消灯する
> ④保存/消すモードの解除条件が未定義 ⑤下端行は横スクロール無し+シートを閉じると消えるので置き場所を指定する。

### 17-1. きっかけ(何が困っているか)
フェーズ3+の残件(§6の表・DEVELOPMENT_LOG v0.25.2657「次の申し送り」)。

いま数字の記憶は**自動保存の1枠だけ**(`bossmaker.tuning.v1.<boss>`・§2-6)。よって
**「さっきの数字」と「今の数字」を並べて比べられない**。実際の調整はこうなる:

> 予告を700→500に下げてみる → 速すぎる気がする → 700に戻す → やっぱり600? → 500はどうだったか

**戻す手段が「手で打ち直す」しかない**ので、3つ以上の案を行き来した時点で元の値が分からなくなる。
これはバランス調整で最も回数の多い操作(A/B比較)がこの道具に無い、ということ。

※コピー/貼り戻し(§1-6)は**外(設計チャット)へ渡すため**の口で、A/B比較には使えない
(クリップボードは1本しか持てないので、A案をコピーした時点でB案が消える)。

### 17-2. 仕様

**枠は4つ**(`0`〜`3`)。A/B比較が主目的なので2つでは足りず(「元・案1・案2」で3つ要る)、
多すぎると1行に収まらない。**静的な上限**にしておくと型・保存形式が動的にならない。

#### 中身(1枠に入るもの)
**コピーの機械行と同じ payload をそのまま持つ**。=
```ts
{ boss: string; changed: Record<string, number>; labels?: Record<string,string>; scripts?: string }
```
★**新しい保存形式を発明しない**。既に `formatTuningText` が吐き `parseTuningText` が読み戻せる形で、
**往復がテストで担保済み**(受け入れ条件4)。枠がこれと同じなら「枠へ保存 → 読込」は
**コピー → 貼り戻しと同一の経路**になり、片方だけ壊れることが起きない。
- ⇒ 実装は `parseTuningText` の**適用側だけを純関数 `applyTuningMachine(entry, machine)` へ切り出し**、
  `parseTuningText`(文字を剥がす係)と枠の読込の**両方がそれを呼ぶ**。適用の道は1本だけ。

保存単位はさらに `{ name, savedAt, machine }`。`name` は空でよい(空なら `A`/`B`/`C`/`D` と保存時刻を出す)。

#### 保存場所
`bossmaker.slots.v1.<boss>`(JSON配列・**ボスごと**)。既存の3キー(数値/名前/台本)とは**別キー**なので、
古い保存は1バイトも壊れない(§2-6と同じ作法)。

#### 操作(1行・チップ4つ)
| 操作 | 手数 | 挙動 |
|---|---|---|
| **読込** | チップを**1タップ** | その枠を現在値へ適用。**A/B比較の主操作なので最短にする** |
| **保存** | `保存▾` → チップ | 押すとチップ列が保存モード(黄)になり、次に押した枠へ書く。上書きも同じ |
| **消す** | `消す▾` → チップ | 同上(赤モード) |

★**隠しジェスチャ(長押し等)を使わない。** 見えない操作は開発用の道具で最も事故る
(モードは色で見えている・1回押せば必ず元へ戻る)。
- 空の枠を**読込**しようとした時は**何もしない**(「空です」とだけ出す)。
- 名前は**保存した瞬間に `window.prompt` で聞く**(空でOK)。★専用の入力UIは作らない——
  この道具は開発用で、名前を付ける頻度は「枠を作る時」の4回だけ(§17-5「かけない労力」)。

#### いま自分がどの枠に居るか(★これが無いと道具にならない)
チップに**一致印**を出す。**現在の状態を同じ payload に直列化して、枠のものと文字列比較**する。
- 一致 = チップが**点灯**(いまの数字はこの枠そのもの)
- 不一致 = 消灯(読込んだ後に1つでも触った)
⇒ 「戻したつもりで戻っていない」が目で分かる。**比較のたびに数え直さない**
(`changed 12件` の表示だけでは、A案とB案が両方12件の時に区別できない)。

### 17-3. 守ること(既存の掟を1つも崩さない)
1. ★**部屋の外へ持ち出さない。** 枠は端末の保存であって、テーブルへ載るのは**読込を押した時だけ**。
   部屋を出る時の `resetTuning`(§2-6)は**一切変えない**。⇒ 本編のボスに調整値は乗らない。
2. **読込は必ず「既定へ戻してから当てる」**(`applyTuningMachine` が `resetTuning` を先に呼ぶ)。
   差分を積み上げると、前の案で触った項目が残って**枠に書いていない値が混ざる**。
3. **読込んだら自動保存(§2-6)も更新する**。部屋を出て入り直しても、読込んだ状態が続く
   (自動保存と枠が食い違ったまま残るのが一番混乱する)。
4. **別のボスの payload は弾く**(`machine.boss !== entry.bossType` なら適用しない=貼り戻しと同じ判定)。
5. **壊れた保存で落ちない。** 読めない/形が違う/長さが違う JSON は**その枠だけ空**として扱い、
   他の枠と本体は無傷(`normalizeSlots` が必ず長さ4の配列を返す)。
6. **知らない項目は落として続行**(射撃枠を消した後に古い枠を読んでも固まらない=§11-3と同じ)。
7. **編集は純関数を通す**(`src/utils/bossSlots.ts`)。UIが `localStorage` を直接触らない。
8. **React再描画の規律**: このUIは store を購読しない。既存の `bump` で再描画する。

### 17-4. 受け入れ条件
1. 枠へ保存 → 数字を触る → その枠を読込 → **保存した時と完全に同じ状態**(数値・技の名前・台本)。
2. 4つの枠が**独立**(片方へ保存しても他が変わらない)。**ボスが違えば枠も別**。
3. **一致印が正しい**: 読込直後は点灯、1つ触ったら消灯、元へ戻したらまた点灯。
4. **空枠の読込で何も壊れない**(現在値が変わらない)。
5. **リセット(既定へ戻す)は枠を消さない**(§2-6のリセットは自動保存を消すが、枠は社長の資産)。
   ★消えると「戻れると思って触ったら全部消えた」になる。**枠を消すのは `消す▾` だけ**。
6. 壊れた/古い保存を入れても**例外を投げず**、他の枠が生き残る。
7. **部屋を出た後、本編のボスの数値が既定のまま**(既存テストが引き続き通ること)。
8. `npm run typecheck` / `npm run lint` エラー0。ユニットテストを同コミットで足す。

### 17-5. これは何**ではない**か(かけない労力)
- **プレイヤーが触る画面ではない**(開発用)。⇒ 見た目・アニメーション・空状態のイラスト等は**対象外**。
- **プリセット配布の仕組みではない**。枠は社長の端末にしか無く、共有はコピー(§1-6)の仕事。
- **やり直し(undo)履歴ではない**。自動で溜まる履歴は作らない(押した時だけ・4枠固定)。
- **ゲームの仕様・挙動・バランス・演出には一切触れない**(道具の中だけで閉じる)。

### 17-6. 負荷 **1/10**
`localStorage` の読み書きは**押した時だけ**(毎フレームの処理はゼロ)。一致印の直列化も
再描画時の1回で、対象は既定から変わった項目だけ(数十個)。本編の負荷は変化しない。

---

## 18. 「行動」を主要な数字だけにする(選ぶ + 裏マスター)【v0.25.2819 仕様・監査2回反映済み】

### 18-1. 社長指示(2026-08-03・原文)
> ここではボスメーカーの改良をお願いしたい。**ゲームのシステムに触れる部分ではない**ので、切り離して改良する
>
> いま技以外の、**行動などについて数字が細かすぎて使いづらい**ので、**主要な数字だけ入れる仕様**にしたい。
> たとえば、**距離の取り方も 近 中 遠 の3種類**にして、**数字は全体を通して固定帯**。(**多少のランダム性あり**)
> とか。**細かく指定しづらいものはそれに合わせてセットで規定値を設定しておき、表示しない。**
> **そこを変える必要がでてきたら、裏側のマスターで変更する。**

### 18-2. いま何が起きているか(数えた)
「行動パターン」タブには**数値欄が23個**並んでいる(`idolTuning.ts` `behaviorFields()` の実測)。

| 節 | 欄 | 数 |
|---|---|---|
| 基礎値 | HP / 接触ダメージ / 移動速度 / フェーズ2の閾値 | 4 |
| 間合い | 主戦帯 下限・上限 / 密着帯の上限 / 中帯の上限 / 遠帯の上限 | 5 |
| 中立の移動 | 詰める・離れる・並走の倍率 / 中立の最短・最長 | 5 |
| ストリングと休符 | 段数P1/P2 / 休符P1/P2 / 第二波の遅れ | 5 |
| 懲罰 | 遠距離の長居 / 密着の居座り / 同角度の長居 / 同角度の幅 | 4 |

★**問題は「多い」ことではなく、23個の大半が「単独では決められない」こと。**
現行値は実際に**束で辻褄が合っている**:
`主戦帯 上限340 = 中帯の上限340` / `主戦帯 下限200 = rollDist 140 + 余白60`
(=「密着から離脱ローリング1回でちょうど主戦帯の下端へ戻る」`idolScript.ts:199-201`)/
`遠帯の上限700 < 狙撃線の射程900`(=「超遠まで届く=逃げ撃ちを消す」`idolScript.ts:225`)。
⇒ **1つずつ数字で触らせる形が、そもそも仕様と噛み合っていない。**

### 18-3. 考え方: 「選ぶ」を主・「数字」を副
- **束で意味を持つ値は、束のまま選ばせる**(近/中/遠)。中身の px は**画面に出さない**。
- 束の中身は**裏側のマスター**(`src/utils/bossPresets.ts` 1ファイル)。変えたくなったらそこを直す。
- ★**隠すのであって、消すのではない。** 値は今までどおりテーブルの同じ場所に同じ形で存在し、
  ゲームロジック・保存・コピー・既存テストは**1行も変わらない**。
  ⇒ 社長の条件「**ゲームのシステムに触れる部分ではない**」を満たす作りは、これしかない。

### 18-4. 画面(改良後)
**行動パターンタブ = 数字3個 + 選択5個**(23欄 → 8個。**隠すのは20欄**)。

| 節 | 画面に出す | マスターへ隠す |
|---|---|---|
| 基礎値 | **HP** / **接触ダメージ** / **移動速度**(数字のまま) | フェーズ2の閾値(1) |
| 間合い | **間合い = 近 / 中 / 遠** | 5欄 |
| 中立の移動 | **動きの速さ = のろい / ふつう / すばやい** | 5欄 |
| ストリングと休符 | **手数 = 少 / 並 / 多** ／ **休み = 短 / 並 / 長** | 5欄 |
| 懲罰 | **懲罰 = 緩 / 並 / 厳** | 4欄 |

- 選択は**チップ3つ**(押すだけ)。摘まむ操作より速く、**辻褄の合わない組み合わせを作れない**。
- ★**既定はすべて「まん中」**(中 / ふつう / 並 / 並 / 並)。
- 「詳細」トグルを上部アイコン列へ1つ足す(**既定OFF**・端末に記憶。既存の `？` と同じ作法)。
  ONにすると隠した20欄がこれまでどおり全部出る。★**理由**: 貼り戻し・古い保存で
  **どの束とも一致しない中間値**に入りうる。逃げ道が無いとその状態で**触れなくなる**(=道具が壊れる)。

#### ★節が消えない仕掛け(監査指摘3)
`BossMakerPanel.tsx:221-232` は**節を `entry.fields` から作る**ので、「欄はあるが全部隠れている」節は
**見出しごと消える**(=チップの置き場が無くなる)。よって:
- 節の一覧を作る時、**チップ群(`choices`)が属する節も必ず生やす**(欄0本でも出す)。
- 畳んだ見出しの要点(`summarize`)は、簡易表示では**選択名を出す**(「間合い 中 / 動き ふつう」)。
  値から作ると空欄になり、**今より情報が減る**。

### 18-5. ★マスター(裏側の固定帯・`src/utils/bossPresets.ts`)
★**「まん中」= 現行のアイドルの値そのもの**。⇒ **既定の挙動は1つも変わらない**
(§2-4「既定値が現行の実装値と完全一致」を崩さない唯一の組み方)。

**間合い**(px)
| | 密着帯の上限 | 中帯の上限 | 遠帯の上限 | 主戦帯 下限 | 主戦帯 上限 |
|---|---|---|---|---|---|
| 近 | 100 | 240 | 520 | **200** | 240 |
| **中(既定=現行)** | **140** | **340** | **700** | **200** | **340** |
| 遠 | 180 | 460 | 880 | **200** | 460 |

★**主戦帯の下限は3段とも200で固定**(監査指摘1への答え)。理由: 下限は `shape.rollDist`(140・**技タブの値**)と
`下限 = rollDist + 余白60` で結ばれている。**下限だけ動かすと離脱ローリングが帯の下端を通り越す/届かない**。
技タブの値は今回の対象外(社長「技以外の」)なので、**下限を動かさないことで束の意味を守る**。
⇒ 間合いで動くのは**帯の境目と主戦帯の上限**。
★**遠帯の上限は3段とも `shape.snipeRange`(900)未満**(近520 / 中700 / 遠880)。超えると
**超遠帯の始まりより狙撃線が短くなり**「遠距離に居させない」という本ボスの主題が壊れる。
- **この2つは不変条件としてテストで固定する**(`下限 ≥ rollDist + 40` / `遠帯の上限 < snipeRange`)。

**動きの速さ**
| | 詰める | 離れる | 並走 | 中立の最短 | 中立の最長 |
|---|---|---|---|---|---|
| のろい | 0.85 | 0.35 | 0.35 | 900 | 1700 |
| **ふつう(既定=現行)** | **1** | **0.45** | **0.45** | **700** | **1300** |
| すばやい | 1.2 | 0.6 | 0.6 | 450 | 900 |

★**最短 < 最長**(3段とも)。`idolTick.ts:553` が `min + rand×(max−min)` なので、逆転すると**幅が負**になる。

**手数**(段数) / **休み**(ms)
| 手数 | P1 | P2 | | 休み | 休符P1 | 休符P2 | 第二波の遅れ |
|---|---|---|---|---|---|---|---|
| 少 | 2 | 3 | | 短 | 600 | 600 | 500 |
| **並(既定=現行)** | **3** | **4** | | **並(既定=現行)** | **900** | **900** | **650** |
| 多 | 4 | 5 | | 長 | 1300 | 1300 | 850 |

★**手数は必ず `P2 = P1 + 1`**(「フェーズ2で1段伸びる」現行仕様)。★**休符は0にしない**
(§14-3「この休みがプレイヤーの攻撃チャンス」)。
★**「多」は台本を伸ばさないと効かない(監査指摘2)。** `bossSkeleton.ts:86` は
`len = min(段数の上限, 台本の段数)` で、既定の台本9本は**全て4段ちょうど**。よって P2=5 は
`min(5,4)=4` に切り詰められ、**並と1ミリも変わらない**。⇒ **勝手に台本を伸ばさない**(仕様変更になる)。
代わりに**気づかせる**: 台本エディタの警告(`scriptWarnings`・§11-2「止めずに出すだけ」)へ
**「段数の指定(P2=◯)に対し台本が△段しかありません(△段までしか出ません)」**を追加する。
段を足すのは社長が台本エディタでできる=**道具の中で完結する**。

**懲罰**(ms / °)
| | 遠距離の長居 | 密着の居座り | 同角度の長居 | 同角度の幅 |
|---|---|---|---|---|
| 緩 | 3000 | 4500 | 6000 | 45 |
| **並(既定=現行)** | **2000** | **3000** | **4000** | **30** |
| 厳 | 1300 | 2000 | 2600 | 20 |

★**`遠距離の長居 < 密着の居座り < 同角度の長居`**(3段とも)。

**単独の隠し値**: `フェーズ2の閾値 = 0.5`(マスターに1つだけ。選択肢は作らない)。

### 18-6. 「いま何が選ばれているか」は**逆引きで出す**(★状態を二重に持たない)
選択を別に保存**しない**。**いまテーブルに入っている数値が、どの束と一致するか**を毎回引き直す。
- どれとも一致しない = **「カスタム」**(チップはどれも点いていない)。
- ⇒ 数値を直接いじれる経路(詳細トグル/貼り戻し/保存の復元/古い保存)が**何本あっても、
  表示が実態とズレることが原理的に起きない**。
- ★**保存・コピー・貼り戻しの形式を1つも増やさない**(選択は「複数のパスへ数値を書く」だけ)。
- 比較は**刻み幅で丸めてから**(`phaseHpThreshold` は 0.05 刻みで `0.35000000000000003` が出る。生の `===` は使わない)。

### 18-7. 守ること
1. **ゲームの挙動を変えない。** 既定はすべて「まん中」= 現行値と完全一致。既存テストがそのまま通ること。
2. **隠すのは画面だけ。** コピーの平文は従来どおり全部出す
   (`fieldVisible` = **データ由来**(欄が存在するか)/ 詳細トグル = **見る側の好み**。軸が違うので
   `fieldVisible` は触らず `simpleViewHides(f)` を新設する)。
   ★**さらに、平文の見出し行へ選択名を1行足す**(監査指摘10)。社長は今後「間合いは近」と喋るのに、
   貼られた側が px 5個を読む羽目になるのは道具の目的(「貼るだけで伝わる」)に反する。**派生値なので保存形式は増えない。**
3. ★**チップはスキーマに載せる**(監査指摘4)。`BossTuningEntry` へ
   `choices?: readonly TuningChoiceField[]` を足し、**ボス側が宣言する**。UIはボスを知らないまま並べる
   (§2-3「1ボス対応=テーブル+スキーマを1つ書くだけ」を崩さない)。
   ★**そのボスに存在しないパスを含む束は出さない。** `setAtPath` は無い場所へ黙って `false` を返す
   (`bossTuning.ts:178-192`)ので、出すと**押しても効かず永久にカスタム表示**になる。
4. ★**束の適用は既存の保存経路を通す**(監査指摘5)。`saveSoon()` を呼ぶのは
   `BossMakerPanel.onChange`(451-466行)だけで、退室時は無条件 `resetTuning`(436行)。
   チップが `setAtPath` を直接叩くと**保存されないまま消え、次に入ると全部「中」に戻る**。
   ⇒ 純関数 `choiceValues(field, key)` は**パス→値の対を返すだけ**にし、書き込みは
   **UIが既存の onChange と同じ道**(setAtPath → saveSoon → bump)でまとめて行う。
5. **隠す欄の集合はマスターから派生させる**(監査指摘7)。手書きリストを別に持つと**二重管理**になり、
   束を1つ足した日にズレる。⇒ 「**全束が持つパスの和集合 ∪ {`phaseHpThreshold`}**」。
6. ★**ピン留めは詳細トグルに従う**(監査指摘8)。`pinFields`(543行)は `fieldVisible` しか通していないので、
   端末に残った古いピン(`bossmaker.pins.v1.idol`)が**詳細OFFでも生数字を出し続ける**。
   ⇒ 詳細OFFの間は隠した欄のピンを**出さない**(ピン自体は消さない=詳細ONで戻る)。
7. **毎フレーム走らせない**。逆引きは再描画時の1回(`NumberScrub` は pointermove ごとに親を再描画する)。
8. **React再描画の規律**: チップは store を購読しない(既存の `bump` に乗る)。

### 18-8. ヘルプの文言(§14の後退を起こさない・監査指摘9)
詳細OFFでは既存欄の `hint` も一緒に消える。**新しい5語には必ず説明を持たせる**。
節の説明(`sectionHelp`)も、px の話をやめて選択の話に書き換える。
```
間合い      ボスが保ちたい距離と、距離帯の境目をまとめて選ぶ。近/中/遠で
            「どのくらい離れて戦うボスか」が決まる。細かい px は裏で決まっている。
動きの速さ  技を出していない間の動き方。詰める/離れる/並走の速さと、次の技までの間。
手数        1回のストリングで続けて出す技の数。多いほど攻めが途切れない。
            ※台本の段が足りないと、指定しても段数までしか出ない(台本の警告に出る)。
休み        ストリングの後に必ず入る休み。ここがプレイヤーの攻撃チャンス。短いほど厳しい。
懲罰        プレイヤーが同じことを続けた時に、ボスが決まった返し技を出す仕組み。
            厳しいほど早く反応する(遠くに居続ける/密着し続ける/同じ方向に居続ける)。
```

### 18-9. 受け入れ条件
1. **既定の挙動が1つも変わらない**(全部まん中 = 現行値。`changedPaths` が空)。既存テストが全通過。
2. 行動パターンタブの既定表示が**数字3個 + 選択5個**(20欄が隠れている)。**節は5つとも残っている**。
3. 選択を押すと**束の全パスが同時に変わる**(片方だけ変わらない)。
4. **選択が保存される**: 選ぶ → 部屋を出る → もう一度入る、で**選択が残っている**。
5. 「詳細」ONで隠した20欄が出て、そこで数字を1つ変えると**選択が「カスタム」**になる。
   元へ戻すと**元の束が再び点灯**する。
6. **マスターの全パスが実在する**(`IDOL_TUNING_FIELDS` に存在するテスト。typoの無言故障を潰す)。
7. 束の不変条件が**3段すべてで成立**: 主戦帯上限=中帯上限 / 帯の単調性 / 下限 ≥ rollDist+40 /
   遠帯の上限 < snipeRange / P2=P1+1 / 休符>0 / 中立の最短<最長 / 懲罰の3値が昇順。
8. 段数の上限が台本の段数を超えている時、**台本エディタに警告が出る**(止めない)。
9. コピー → 貼り戻しが従来どおり往復する(**保存形式を増やしていない**)。平文に選択名が出る。
10. 部屋を出たら既定へ戻る(`resetTuning`)= 調整値が本編へ漏れない。
11. `npm run typecheck` / `npm run lint` エラー0。純関数のユニットテストを同コミットで足す。

### 18-10. これは何**ではない**か(かけない労力)
- **プレイヤーが触る画面ではない**(開発用)。見栄え・アニメーション・アクセシビリティは対象外。
- **ゲームのバランスを決める作業ではない。**この便で決めるのは**「どの数字を画面から外すか」**だけ。
  近/遠 は**選ばなければ一切効かない**(既定は中)。
- **技(move)タブ・射撃枠は対象外**(社長「技以外の、行動などについて」)。
  ⇒ だから §18-5 は `rollDist` / `snipeRange` を**動かさない**側に倒してある。
- **距離帯の"数"は4つのまま**(密着/主戦/遠/超遠)。社長の「近 中 遠」は**入力の選択肢が3つ**の意味で、
  ゲーム内部の帯の数の話ではない(§7-4で帯の増減は保留のまま)。
- **台本を自動で伸ばさない**(「多」を活かすのは社長が台本エディタでやる。警告だけ出す)。

### 18-11. ★未決(社長裁定)
1. **近/遠 の実数値**(§18-5)。中=現行を軸に置いた叩き台。既定は中なので**選ばない限り何も起きない**。
2. **「多少のランダム性あり」の解釈。**現状すでに `主戦帯 200〜340`(帯)と `中立の滞在 700〜1300ms`(乱数)で
   ランダム性は入っている。**追加のランダム化はゲーム挙動の変更**なので、この便では**入れない**。
3. **主戦帯の下限を動かせるようにするか。**いまは3段とも200固定(理由は§18-5)。動かすなら
   **間合いの束に `shape.rollDist` を含める**ことになり、**間合いチップが技タブの値を動かす**。
   社長の「技以外の」に触れるので、**やらない側に倒してある**。

### 18-12. 検収監査で見つかって直したこと(v0.25.2820)
| # | 何が起きていたか | 直し |
|---|---|---|
| ① | **コピー平文に節が二重に出る**(既定でも4個・射撃枠を足すとその見出しも重複=**元からの穴**)。`openSection` が「最後に開いた節」としか比べておらず、末尾の取りこぼし回収が既に出した見出しを開き直していた | 開いた節を `Set` で覚える。**見出しが二重に出ないテスト**を追加 |
| ② | **チップを押した直後に部屋を出ると保存が全部消える**。退室時のクリーンアップが `resetTuning` → 書き残しフラッシュの順で、**既定へ戻した後に差分ゼロを書き `localStorage` のキーごと削除**していた(**元からの穴**。まとめ書き400ms) | 「**流してから戻す**」順へ修正(`flushSave`)。加えてチップは1タップで決まるので**まとめ書きにせず即保存** |
| ③ | **隠れたピンが枠(3個)を食い、簡易表示から外せず詰む**。端末に残った古いピンでピン行は空なのに「ピンは3個まで」 | 枠は**いま画面に出ているピン**で数える |
| ④ | **段数の警告が2経路で取りこぼす**。全ゾーン横断の `Math.max` なので (a)1本だけ伸ばすと警告が消えるのに他の帯は切り詰められたまま (b)`weight=0`/`off` の**出ない台本が警告を黙らせる** | **距離帯ごと**・**抽選に出る台本だけ**で判定 |
| ⑥ | マスターの値が欄の min/max を外れると `clampField` が黙って切り、**そのチップが永久に点かない**(潜伏) | 全値が**範囲内かつ刻みに乗っている**ことをテストで固定 |
| ★ | (自分で気づいた)台本エディタの距離帯の見出しが `密着(0〜140)` と**px をベタ書き**しており、間合いを変えると**嘘の見出し**になる(§9の教訓の同型) | テーブルから引く関数へ |

**★社長裁定に上げる(監査⑤⑦・勝手に決めない)**
- ⑦ **コピー平文には隠した20個の生数字が従来どおり全部並ぶ**(選択名の1行が増えただけ)。
  画面の「細かすぎる」は解けたが、**貼られた側の読み物では解けていない**。
  ⇒ 平文も「選択名だけ」に絞るか、今のまま全部出すか。**私の推薦は今のまま**(貼る相手=設計チャットは
  実数値が要る。社長が読むのは画面であって平文ではない)。
- ⑤ `phaseHpThreshold` は束を持たないので、簡易表示では**「カスタム」も出ず気づけない**
  (手掛かりは右上の「変更 N」だけ)。⇒ 束にする(選択肢を作る)か、このままか。
  **私の推薦はこのまま**(全ボス0.5で動かす場面が無く、選択肢を作る方が画面を増やす)。

### 18-13. 負荷 **1/10**
選択の適用は押した時だけ(`setAtPath` を数回)。逆引きは再描画時に1回・比較対象は20値。
毎フレームの処理はゼロ。**本編の負荷は変化しない**(ゲーム側が読む値の形は今までと同一)。

---

## 19. 切り分け(独立ツール化・v0.25.2848〜)

### 19-1. 社長の言葉(ゴール)
> このボスメーカーを切り分けてこのゲーム専用のツールとして独立させたい。
> で、ゲーム内のボスメーカーはボスラッシュモードとしてシンプルにしてメニュー化させます。
> ボスメーカーでは、[本番化]ボタンを置き、押すとゲーム側に反映される仕組みだと嬉しいが、
> 重くなるならスクリプト化して渡せるだけでもいいです。
> まずは切り分けからやってみよう

**この節が扱うのは「まずは切り分け」だけ。** ボスラッシュ(§20)と本番化(§21)は別便。

### 19-2. 「ではない」条件(労力をかけなくてよい所)
- ボスメーカーは **開発用であってプレイヤー用ではない**。⇒ **UIの見栄え・演出・導線に労力をかけない。**
  今のボトムシートUIをそのまま移すだけでよい(作り直さない)。
- **製品(アプリ配布)に載る物ではない**。⇒ ロード時間・初回DL量・オフライン対応は対象外。
- **切り分けはゲームの遊びを変える作業ではない**。⇒ ボスの挙動・数値・バランスの指摘は対象外。
  既定値が1つも変わらないことだけが条件(§19-7)。
- 本番化の作り込み(§21)は**後の段**。この便では「今ある平文コピーで渡せる」以上を作らない。

### 19-3. 何が問題か(なぜ切り分けたいか、コード側の事実)
ボスメーカーのUIは**ゲーム本体のバンドルに同梱**されている(`App.tsx` が `BossMakerPanel` を
直接 import)。よって:
- 製品ビルドに**開発用の道具が約2,400行載る**(パネル3本 1,060行 = `BossMakerPanel` 845 + `Live` 59
  + `ScriptEditor` 156、加えて `bossTuning` 578 + `idolTuning` 387 + `bossScriptEdit` 219 + `bossPresets` 167)。
- ゲームのメニュー(ボス戦テスト)から入る作りなので、**ゲームを触ると道具も動く**(逆も同じ)。
- 道具を育てるほど本編のバンドルが太る、という**利害が逆向きの関係**になっている。

### 19-4. 切る場所(実コードで裏を取った依存)

| モジュール | 行数 | いま誰が読むか | 切り分け後 |
|---|---|---|---|
| `components/BossMakerPanel.tsx` | 845 | 道具のみ | → `src/tools/bossmaker/BossMakerPanel.tsx` |
| `components/BossMakerLive.tsx` | 59 | 道具のみ | → `src/tools/bossmaker/BossMakerLive.tsx` |
| `components/BossScriptEditor.tsx` | 156 | 道具のみ | → `src/tools/bossmaker/BossScriptEditor.tsx` |
| `utils/bossTuning.ts`(欄の台帳・保存・平文) | 578 | 下記3箇所 | → `src/tools/bossmaker/bossTuning.ts` |
| `utils/bossPresets.ts`(束マスター) | 167 | 道具のみ | → `src/tools/bossmaker/bossPresets.ts` |
| `utils/idolTuning.ts`(欄の登録) | 387 | 道具のみ | → `src/tools/bossmaker/idolTuning.ts` |
| `utils/bossScriptEdit.ts`(台本の編集操作) | 219 | `idolTuning.ts` のみ | → `src/tools/bossmaker/bossScriptEdit.ts` |
| **`utils/idolScript.ts`(数値テーブルの実体)** | — | **ゲームのボスが読む** | **ゲームに残す**(道具はこれを書き換える) |
| **`utils/bossScript.ts`(台本の実行器)** | — | `useGameLoop` / `pixiScene` | **ゲームに残す** |

移すテスト(道具専用): `bossTuning.test.ts` / `bossPresets.test.ts` / `bossScriptEdit.test.ts` を
同じ `src/tools/bossmaker/` へ。**vitest の設定追加は不要**(`vitest.config.*` は存在せず default
include が `**/*.test.*` なので `src/tools/` 配下もそのまま拾う)。`tsconfig.app.json` の
`include: ["src"]` と eslint の ignore(`dist` のみ)も**変更不要**。

**`bossTuning.ts` の実 import は3箇所**(「ゲーム側は `deepCloneTuning` だけ」を正確に言い直す):
- `utils/idolScript.ts:21` — `deepCloneTuning`(**ゲーム側**)
- `utils/idolScript.test.ts:10` — `deepCloneTuning`(**ゲーム側のテスト。ここも直す**)
- `components/BossScriptEditor.tsx:17` — `type BossScriptApi`(道具側)

⇒ `deepCloneTuning` を **`src/utils/deepClone.ts` へ切り出し**、上の2箇所をそこへ向ける。
これで `bossTuning.ts` は100%道具側になる。

**★設計の肝**: 道具は「戦いながら摘まむ」物(社長v0.25.2621)なので、**エンジンを共有しなければ
価値の大半が消える**。よって別リポジトリ/別アプリにはしない——エンジンを丸ごと複製することになり、
二重管理で必ず腐る。**同じリポジトリの中で、ビルドの入口だけを分ける**。

### 19-5. 形(マルチページビルド)
```
index.html      → src/main.tsx                  → <App />                    ゲーム本編
bossmaker.html  → src/tools/bossmaker/main.tsx  → <App playingOverlay={…} /> 道具
                                                   └ 同じエンジン(store/useGameLoop/pixi)を import
```
- `App.tsx` から `BossMakerPanel` の import を**消す**。代わりに `playingOverlay?: ReactNode` を
  1つ受け、`gameState === 'playing'` の時に描くだけにする(**2行の口**)。
  ⇒ ゲームのバンドルから道具が**木ごと落ちる**(他に import 元は無く、動的 import も barrel も無い
  ことを確認済み)。※**CSSは落ちない** → §19-6-c。
- `vite.config.ts` に `build.rollupOptions.input = { main: index.html, bossmaker: bossmaker.html }`。

#### ★19-5-a. 起動クエリの注入は **`bossmaker.html` のインライン classic script でやる**(絶対)
部屋の判定は**モジュールスコープ定数**である:
- `hooks/useGameLoop.ts:1028` `const BOSS_MAKER = evParam('bossmaker') === '1';`
- `hooks/useGameLoop.ts:638` `const NOSPAWN = evParam('nospawn') === '1';`

一方 store 側(`gameStore.ts:13082` の `isBossMakerRun()`)は**実行時関数**。よって

> **`src/tools/bossmaker/main.tsx` の中で `history.replaceState` してはいけない。**

ESM は静的 import が全部評価されてからモジュール本体が走るので、その時点で `BOSS_MAKER` は
既に `false` で焼き付いている。結果は「**木や松明は消えるのに雑魚が湧き続け、相手のボスが
1体も出ない**」という半端な部屋(`useGameLoop.ts:5515` の分岐が一生 false)。

**正しい形**: `bossmaker.html` の `<head>` に**インライン classic `<script>`**(`type="module"` を
付けない)を置き、そこで query を注入する。`type="module"` は defer 相当なので、classic script は
**必ず全モジュール評価より前に走る**。
- 注入するのは `bossMakerQuery`(`utils/bossTest.ts:135`)と**同じ組**: `smoke=1 / stage / nospawn=1 /
  bossmaker=1 / class / retry=1`。
- **既に付いているクエリは残してマージする**(`?class=rogue` で開けるように・§19-5-b)。

#### 19-5-b. 職の選び方(勝手に決めない)
- ゲーム側のボス戦テストメニューのボタンは `bossmaker.html?class=<選んだ職>` へリンクする
  (**今と同じく職を選んでから飛ぶ**=導線は不変)。
- 道具ページを直接開いた場合の既定は **`warrior`**。
- **道具ページ内に職の選択UIは作らない**(§19-2 の「UIに労力をかけない」)。
- リンクの組み立ては **`import.meta.env.BASE_URL + 'bossmaker.html'`**。`/zombie/` 直書き禁止
  ——`pages.yml` は `npm run build -- --base=/zombie/v2/` の**v2線も合成デプロイ**しており、
  直書きすると v2 で 404 になる(前例: `OpeningScene.tsx:32` / `MissionSelect.tsx:210`)。

#### 19-5-c. `bossmaker.html` と道具側 `main.tsx` の中身(未指定にしない)
- **`bossmaker.html` は `index.html` をコピーし、`<title>` と `<script src>` だけ変える。**
  index.html は safe-area 変数・`touch-action`・スクロールバー抑止・`--game-font`・`#root` の
  100% を**約60行の直書き `<style>` で持っている**。これを共有CSSへ移す案は採らない
  (本編の初期表示のタイミングを変える=受け入れ条件1に触る)。**二重管理になる旨をファイル冒頭に明記する。**
- **`src/main.tsx` の起動処理は `src/bootstrap.ts` へ括り出し、両エントリが呼ぶ**
  (`--game-font` 注入・contextmenu/gesture/ダブルタップ抑止・`document.fonts.load`)。
  これを再現し損ねると**フォントが違う状態で PixiJS がダメージ数字アトラスを焼く**。
  こちらはJSなので共有して安全(実行内容は不変=純粋な切り出し)。

### 19-6. ゲーム側に残る物(消さない)
`store.bossMaker`(active/invincible/paused/showHitbox/hideHud)・`?bossmaker=1` の部屋作り
(木/松明/地雷/花/NPCを消す・相手を1体出す)・無敵判定は**エンジンの機能**なので残す。
道具ページがこれを起動する。**本編では `active=false` のまま**なので毎フレームの負荷は変わらない。

#### ★19-6-a. `registerEnemyFireProfile('idol', …)` をゲーム側へ移す(**致命の穴**)
`BossMakerPanel.tsx:39` は**モジュール最上位**で `registerIdolTuning()` を呼んでおり、その中の
`idolTuning.ts:362` が `registerEnemyFireProfile('idol', IDOL_TUNING.bullet.aim)` を実行している。
**App がパネルを eager import しているため、この登録は今「本編のロード時に必ず走っている」。**
パネルを落とすと**この副作用も一緒に消える**。

現状は登録値と fallback(`enemyUtils.ts:635` の `{interval:99999, range:99999, speed:320, damage:20,
size:16}`)が**たまたま完全一致**しているので挙動は変わらない。つまり受け入れ条件1は
**偶然守られているだけ**で、どちらかの既定値を触った瞬間に本編と道具が静かに食い違う。

⇒ **`registerEnemyFireProfile('idol', IDOL_TUNING.bullet.aim)` を `utils/idolScript.ts` へ移す**
(本編でも必ず登録される)。`enemyUtils` は `idolScript` を import していないので**循環しない**(確認済み)。
道具側の `registerIdolTuning()` からはこの1行を外す(欄の登録だけを残す)。

#### 19-6-b. 道具ページの「メニューに戻る」= **部屋の入り直し**(仕様として決める)
`App.tsx:288` は smoke セッションで `window.location.replace(window.location.pathname)` する。
道具ページの pathname は `bossmaker.html` なので、リロード → インライン script が再注入 → **また部屋**。
**これを「部屋のリセット」として正とする**(道具に「ゲームのメニュー」は要らない)。
ゲームへ戻るのは `index.html` を開く。App 側は**改造しない**。

#### 19-6-c. 落ちないもの・残るもの(把握しておく)
- **CSSは落ちない**: `tailwind.config.js` の content が `./src/**/*` なので、道具のクラスは
  本編のCSSに残る。「木ごと落ちる」のは**JSだけ**。数KBなので許容(§19-2)。
- 分離後の本編 `index.html?bossmaker=1` は**部屋だけ立ってUIが無い**状態になる(`isBossMakerRun()` は
  残るため)。害は無いが、見かけても事故ではない。
- `localStorage` は origin 単位なので、`index.html` と `bossmaker.html` は
  **`bossmaker.tuning.v1` を最初から共有する**。⇒ §19-9 の「(a) この端末のゲームに今すぐ効かせる」は
  分離した時点で半分できている(§21 の判断材料。**この便では作らない**)。

### 19-7. 受け入れ条件(機械的に判定できる形)
1. **ゲームの挙動が1つも変わらない**。`npm test` の既存テストが全て通る(`npm run typecheck` /
   `npm run lint` エラー0)。
2. `dist/` に **`index.html` と `bossmaker.html` の2つ**が出る。
3. **本編のJSに道具が含まれない**。判定は minify で消えない文字列で行う:
   `dist/index.html` が読み込むチャンク群に **`bossmaker.tuning.v1`**(`bossTuning.ts:341` の
   `SAVE_KEY`)が**含まれないこと**。※識別子 `BossMakerPanel` での検索は minify で消えるので**不可**。
4. `bossmaker.html` を開くと**部屋が本当に立つ**: 木/松明/地雷/花が消え、**相手が1体だけ出て、
   雑魚が湧かない**。⇒ §19-5-a の再発検知器。条件1〜3では絶対に捕まらない。**社長の実機確認**。
5. 今までと同じ調整部屋がそのまま出る(UIの作り直しはしない)。数字を触るとボスに**即反映**され、
   保存・平文コピー・貼り戻しが従来どおり動く。**社長の実機確認**。
6. ゲーム側メニューのボタンから道具ページへ到達できる(**`npm run dev` と Pages の両方**)。

### 19-8. 負荷スコア **1/10**
本編は**軽くなる**。バンドルから落ちるのは
パネル3本 1,060行 + `bossTuning` 578 + `bossPresets` 167 + `idolTuning` 387 + `bossScriptEdit` 219
= **約2,400行**とその依存。増えるのはビルド出力1ファイルだけで、**実行時のコストはゼロ**
(import の付け替えと入口の追加のみ)。道具ページはゲーム1本と同じ重さで動くが開発用なので対象外(§19-2)。

### 19-9. ★未決(社長裁定・§21で聞く)
- **[本番化]の意味**: (a)この端末のゲームに今すぐ効かせる(localStorage・実機で完結)/
  (b)リポジトリのファイルに書き戻して恒久化(`npm run dev` 中のみ可能)/(c)平文を書き出して渡すだけ。
  ⇒ 実機(GitHub Pages)では (b) が原理的に不可能なので、**(a)+(c) の組み合わせが現実的**。
  (c)は**すでに実装済み**(平文コピー/貼り戻し)、(a)は §19-6-c のとおり**分離した時点で半分できている**。

### 19-9-b. 実装結果(v0.25.2849・**完了**)
| 受け入れ条件 | 結果 |
|---|---|
| ① 挙動が1つも変わらない / typecheck・lint 0 | **`npm test` 2,755件 全通過・0失敗**。typecheck 0 / lint エラー0(warning 8=既存) |
| ② `dist/` に2つのHTML | **達成**(`dist/index.html` + `dist/bossmaker.html`) |
| ③ 本編のJSに道具が入らない | **達成**。`index.html` から到達する9チャンクに `bossmaker.tuning.v1` は**無し**。`bossmaker.html` 側の `assets/bossmaker-*.js`(44.5kB)にのみ存在 |
| ④ 部屋が本当に立つ | **社長の実機確認待ち**(木/松明/地雷/花が消え、相手1体・雑魚なし) |
| ⑤ 今までと同じ調整部屋・即反映・保存/コピー | **社長の実機確認待ち** |
| ⑥ メニューのボタンから到達(dev と Pages 両方) | **社長の実機確認待ち** |

実際に足した/動かした物: `bossmaker.html`(新) / `src/tools/bossmaker/`(移設10本+`main.tsx`+
`bossmakerEntry.test.ts`) / `src/utils/deepClone.ts`(新) / `src/bootstrap.ts`(新) /
`App.tsx`(`playingOverlay`) / `main.tsx`(スリム化) / `idolScript.ts`(弾プロファイル登録の移設) /
`BossTestMenu.tsx`(別ページへのリンク) / `vite.config.ts`(マルチページ入力)。

### 19-11. 余計なものの排除(v0.25.2851・社長指示「タイトルや他の要素もいらない」)
道具ページは `App` をそのまま使っていたので、**ゲームの画面が全部生きていた**(起動時にタイトルが
見え、素材ロードもタイトル画面が担っていた)。`App` に **`bare`** プロップを1つ足し、道具ページだけが渡す。

| 消したもの | | 残したもの | |
|---|---|---|---|
| タイトル画面 | ミッション選択(作戦室) | ゲーム本体 | 差し込み口(調整UI) |
| 起動ローディング(大タイトル付き) | エンディング | 出撃時の暗転 | 縦持ちガード |
| リザルト画面 | オープニング / 洋館通路プレビュー | 素材待ちの小スピナー(`compact`) | |

- **決着したら部屋に入り直す**(リザルトを描かないので、そのままだと固まる)。
- ★**進行に一切触らない**: `bare` の勝利はクリア解放・エンディング予約・スコアを**通さない**。
  道具でボスを倒したせいで本編のステージが解放される副作用を作らないため。
- ゲーム本編は `bare` を渡さない(`false`)ので、描画条件の評価は**今までと完全に同一**。

### 19-10. 品質監査の結果(v0.25.2848・CLAUDE.md 必須手順)
初稿に監査を1回。**致命2件・重要8件・軽微4件**の指摘。全て実コードで裏を取り、**全件を上へ反映した**
(握り潰した指摘は無い)。特に効いた2件:
- **致命1**: 起動クエリを `main.tsx` で `replaceState` する初稿の案は、**ESMの評価順で必ず間に合わない**。
  部屋が半分しか立たない(§19-5-a で「やってはいけない」と書き切った)。
- **致命2**: `registerEnemyFireProfile('idol', …)` の**モジュール副作用が本編から消える**ことに
  初稿は気づいていなかった。今は登録値と fallback が偶然一致して助かっているだけ(§19-6-a)。

いずれも**設計の穴**であり、ゲームの見え方・挙動・体験は変えていない(=社長裁定は不要と判断)。

---

## 20. ボスラッシュ(作戦室の正式メニュー・v0.25.2853〜)

### 20-1. 社長の言葉(ゴール)
> ボスラッシュを正式にメニュー化してください。作戦室にならぶ形です。
> UIは任せますが、守護霊メニューのボスごとのデータみたいなノリで、
> 情報がボス絵と関するデータ 攻略ヒント みたいなのりかなぁと
> それか、守護霊メニューに統合した方がよさそうであれば、それでも
> **ラッシュは連戦じゃない。1体**
> **仕様としては一度ステージで出会ったことがあれば解放される。練習モードの位置づけ**

### 20-2. 「ではない」条件(労力をかけなくてよい所)
- **連戦ではない。** 通し・引き継ぎ・回復・段位・タイムアタックの設計は**この便の対象外**。
- **報酬や進行の仕組みではない。** 練習モードなので、クリア解放・スコア・エンディング・
  実績の設計は対象外(むしろ**触らないこと**が要件・§20-6)。
- **新しいボスや新しい部屋を作る話ではない。** 既存の強制出現(`bossnow`/`idolnow`/`gateboss`/
  `castlenow`)を**正式な入口に格上げするだけ**。ボスの挙動・HP・技には一切触らない。
- **オンライン守護霊の仕様変更ではない。** 霊の共有・いいね・討伐記録の作りへの指摘は対象外。

### 20-3. 置き場所: **守護霊メニューには統合せず、作戦室に独立させる**
社長の選択肢のうち「統合」は**採らない**。理由:
- 守護霊メニューは **「討伐済みのボスの記録を見る場所」=過去形**(未討伐は `?` 表示)。
- ボスラッシュは **「これから戦う入口」=未来形**。しかも解放条件が**遭遇**なので、
  **討伐していないボスほど並ぶ**(=あちらでは `?` のまま埋まらない枠)。
- 同じ画面に混ぜると「記録を見に来ただけなのに出撃してしまう」動線になる。

⇒ 拠点(ホーム)に **`HubButton`「ボスラッシュ / 練習」を1項目**足す。位置は「作戦準備」の下、
「装備」の上(戦う系をまとめる)。**見た目の作りは守護霊メニューに揃える**(社長の「ノリ」の履行)。

### 20-4. 出撃カタログ(★監査で全面訂正・v0.25.2856)
並ぶボスの**台帳**は `utils/ghostDossier.ts` の `GHOST_DOSSIER_SLOTS`(18枠)を共用する
(2画面が別表を持つと必ず片方だけ増える)。ただし**「枠→出撃」の変換は初稿が実装と食い違っていた**。
実コードを読んで訂正した表が下記。

| 枠 | 出撃のさせ方 | 実際に成立するか |
|---|---|---|
| `giantbat@stage-1/3/4/5` | `castlenow` | **成立** |
| `giantbat@stage-7` / `giantbat@stage-ex1` | **`castlenow` は効かない。** 両ステージは `storyBossOnly:true` で城ボスの湧きゲート(`useGameLoop.ts:2413` の `!storyBoss`)の外。ボスは**storyBoss経路で勝手に出る**ので、パラメータ無しで出撃するだけでよい | **成立(別経路)** |
| `giantbat@stage-2` | **どう出撃しても出ない。** stage-2 は `theme:'lab'` で同ゲートの `!labTheme` の外。**本編にも城ボスが居ない** | **不成立=永久ロック**(★§20-10) |
| `miguel@stage-1` / `jibril@stage-3` / `rafi@stage-4` / `uri@stage-5` | `gateboss` | **成立** |
| `suriel@stage-6` | `gateboss` | 出撃は可。ただし**本編では遭遇できない**(★§20-10) |
| `acrasiel@stage-ex1` | `gateboss` | ★`canForceGateBossNow` は `storyBoss` を見ていないので出撃自体は通るが、**同時に未確認変異体(giantbat)も湧く**=「1体」が崩れる(§20-7-b)。本編では遭遇できない(★§20-10) |
| `mimir@stage-1` / `jormungand@stage-3` / `skadi@stage-4` / `thor@stage-5` | `bossnow` | **成立**(`campaign.hiddenBoss` と一致) |
| `idol@stage-2` | `idolnow` | **成立** |

- **逆引きは実表(`campaign.hiddenBoss` / `GATE2_BOSS_TYPE_BY_STAGE`)から作り、写経しない。**
  ズレたら落ちるテストを同コミットで書く。
- ★**`BOSS_TEST_ENTRIES`(`utils/bossTest.ts`・12件)が第2の対応表として残る。**内容も食い違っている
  (城ボスは stage-1 の1件だけ)。**`bossPractice` の台帳を正本にし、`BOSS_TEST_ENTRIES` はそこから
  導出する**(タイトル左下の開発用ボス戦テストは入口として残す)。表を2本持たない。
- ★**死神(`reaper`)・凶悪ハンター(`hunter`)は台帳に入れない**(社長裁定§20-9-2)。
  `ENGAGEABLE_BOSS_TYPES`(`utils/bossEngagement.ts:25`)も**ちょうどこの12型**で一致している。
  **台帳に death/hunter が居ないことをテストで固定**する。

### 20-5. 解放条件: **一度ステージで出会ったことがあるか**(★監査でフック地点を訂正)
既存の記録に「遭遇」は無い(あるのは討伐記録だけ)ので新設する。
- `src/utils/bossEncounter.ts`: localStorage 1キー(`boss.encountered.v1`)。
  `markBossEncountered(slotKey)` / `loadEncounteredBosses(): Set<string>`。
  枠キーは `GHOST_DOSSIER_SLOTS.slotKey` と同一。

★**初稿の「出現した時に記録」は誤り。採らない。** 理由:
- **idol は `addEnemy` を通らない**。`resetGame` が `enemies` 配列へ直接置く休眠敵で、
  **ラン開始時点で盤上に居る**。「出現で記録」だと **stage-2 に出撃しただけで解放**され、
  「探しに行った人だけが会う」という配置意図を壊す。
- **城ボスも `dormant:true` で城に湧くだけ**。近づかなくても記録されてしまう。
- **`giantbat` はイベント産(`fromEvent`)でも出る**。型だけ見ると誤解放する。
- そもそも出現経路が5つに割れており、**このプロジェクトが繰り返している「1経路だけ書いて取りこぼす」
  事故**(CLAUDE.md v0.25.2426の教訓)にそのまま突っ込む。

★**正しい合流点が既にある**: `utils/bossEngagement.ts` の **`engagedBossSlotKeys()`**。
- **dormant を除外**し、**距離のヒステリシス(ENTER/EXIT)付き**で、
  **`GHOST_DOSSIER_SLOTS` と同形式の slotKey 集合**を返す。
- 対象 `ENGAGEABLE_BOSS_TYPES` は**まさにこの12型**(死神・ハンターを含まない)。
- 既に `utils/directorTick.ts:666` で**毎tick呼ばれている**(`NOSPAWN` や盤面状態に関係なく走る)。

⇒ **ここで返ってきたキーを `markBossEncountered` に流す。1箇所で全ボスを拾える唯一の場所。**
意味も社長の言葉に合う——**「出撃したら盤上に居た」ではなく「実際に近づいて戦い始めた」= 出会った**。
- ★**練習・ボス戦テストのランでは記録しない**(§20-6 の練習フラグが真なら書かない)。
  練習で出会ったことを解放条件に使うと、入口が自分自身を解放する輪になる。

### 20-6. 練習モードの掟(★監査で全面書き換え。ここが最大の作業)

#### 20-6-a. 判定は専用フラグ `?practice=1` にする(`BOSS_TEST_RUN` に相乗りしない)
初稿は `BOSS_TEST_RUN` を流用する前提だったが、**`giantbat@stage-7/ex1` はパラメータ無しで出撃する**
(§20-4)ため `BOSS_TEST_RUN` が立たない枠が出る。**安全弁が枠によって効いたり効かなかったりする**のは
最悪なので、**練習出撃には必ず `?practice=1` を付け、遮断はこの1つで判定する**。
- `isPracticeRun()` を `utils/bossPractice.ts` に置く(モジュールロード時定数=他のフラグと同じ作法)。
- チュートリアル抑止・護衛NPC抑止は `BOSS_TEST_RUN` 側で既に効く(v0.25.2852)。練習も
  `BOSS_TEST_RUN` に含める(= `FORCE_PARAMS` に `practice` を足す)。

#### 20-6-b. 止める書き込みの**全リスト**(初稿は `handleVictory` 1箇所だけだった=全然足りない)
★**最重要**: 城ボスは**勝利画面を経由せず、死んだ瞬間にステージ解放が確定する**。
`gameStore.triggerDramaticDeath` が `markCastleBossCleared` → `syncQuestStageClear` → `markStageCleared`
まで到達する。stage-3/4/5 は `forced:false` なので**無条件で通る**。

| # | 書き込み | 場所 | 練習で起きる害 |
|---|---|---|---|
| 1 | `markCastleBossCleared` + `syncQuestStageClear` | `gameStore.ts:2405-2409` | **ステージ解放が確定** |
| 2 | `recordChronicle`(boss/hunter) | `gameStore.ts:2396` | 年表に討伐が残る |
| 3 | `markKogarasuUnlocked` / `maybeUnlockMurasame` | `gameStore.ts:2414-2418` | **トール練習で小烏丸が恒久解禁** |
| 4 | `notifyBossClear` / `recordDuoBossClear` | `gameStore.ts:2572` と `gameStore.ts:8189`(**2箇所**) | 討伐記録に載る(社長裁定§20-9-1 で禁止) |
| 5 | `submitStageHighScore` | `GameOverScreen.tsx:328` | ハイスコアを汚す |
| 6 | `addGold` + 装備スクラップ換金 | `GameOverScreen.tsx:276-282` | **練習で永続ゴールドが増える** |
| 7 | `pushRunCore` | `GameOverScreen.tsx:291-299` | 掘削記録に練習ランが並ぶ |
| 8 | `unlockRecordsForStage` | `GameOverScreen.tsx:348-355` | **資料室が解放される** |
| 9 | `settlePendingTraits` + `uploadGhostSlots` + `sendGhostFeedback` | `GameOverScreen.tsx:232-243` | 守護霊プロファイルへ反映+**オンライン送信** |
| 10 | `setStartRankFromFinal` | `useGameLoop.ts:1562, 3275` | **次ランの開始ランクが変わる**=本編の難易度が動く |
| 11 | `setWallMeta` / `markSelfDeepest` / `recordChronicle(zone/POI)` / `setEventQuestMeta` | `useGameLoop.ts:626,632,3016,3022,4009` ほか | 自己最深・区域到達・拠点解放・クエスト進捗 |
| 12 | `markStageCleared` / エンディング予約 | `App.tsx:292`(`handleVictory`) | ステージ解放・エンディング |

★**実装方針**: 12箇所に `if (isPracticeRun()) return;` を散らすと必ず取りこぼす。
**`utils/bossPractice.ts` に `guardPersist(fn)` 的な1関数を置き、上の書き込み側をそこへ通す**
(=判定を1箇所に集約する。社長裁定§20-9-1「後で1行で切り替えられる形」の履行)。

#### 20-6-c. 受け入れ条件は**localStorage の差分**で機械判定する
「進行が動かない」を目視で確認するのは無理。**練習ランの前後で `localStorage` を丸ごと比較し、
差分が `boss.encountered.v1` 以外ゼロ**であることをテストで固定する。
監視すべきキーの一覧は `data/progress.ts` の `resetProgress()`(703行〜)が**そのまま使える**。

### 20-7. 出撃まわりの穴(★監査で追加)
#### 20-7-a. URL差し替えの副作用3つ
1. `?smoke` ハンドラが **`setSelectedStageId(params.get('stage'))`** を呼ぶ(`App.tsx:261`)=
   **プレイヤーが作戦準備で選んでいたステージを永続的に上書きする**。練習後に選択が変わっている。
   ⇒ 練習出撃の**前に現在値を退避し、戻り時に復元する**(または練習は選択を書かない経路にする)。
2. **`getSelectedMission()` / `getSelectedFreeMode()` はURLでリセットされない。**
   `selectedMission='revisit'` が残った状態で `suriel@stage-6` を練習すると **`revisitRun` が成立して
   洋館再訪ラン**になる(内容もクリア処理も別物)。⇒ 練習出撃時に **`main` / `free=false` へ寄せる**。
3. 戻り導線は `returnToMenu` の `location.replace(pathname)` = **タイトルへ全再読込**。
   別のボスを選ぶたびに起動ローディングを通る。**同じボスの反復は「もう一度プレイ」で再読込なし**。
   これは既存挙動なので**そのまま採用し、設計書に明記する**(直さない)。

#### 20-7-b. 「1体」が崩れる組み合わせ
- stage-1/3/4/5 で GATE/DEEP を練習中、**5分経過すると城ボスが湧く**
  (`castleBossReady = … || newGameTime >= CASTLE_BOSS_MIN_TIME_MS`)。
- `acrasiel@stage-ex1` を `gateboss` で出すと、**未確認変異体(giantbat)も同時に湧く**。
  しかもそれを倒すと勝利経路に乗る。
⇒ **練習ランでは城ボス/ストーリーボスの自動湧きを止める**(狙った1体以外を出さない)。
`?nospawn=1` は雑魚だけなので**別途、城ボス抑止が要る**。社長の「ラッシュは1体」の履行。

#### 20-7-c. 練習の「終わり方」が未定義
GATE/DEEP のボスは**倒してもクリアにならない**(帰還サークルは城ボス撃破が条件)。
トールを倒した後、プレイヤーは死ぬか商人で撤退するまでラン継続する。**練習で一番使う導線**。
⇒ **練習ランは「狙ったボスを倒したら即リザルト(練習用の簡易表示)→ボス選択へ戻る」**とする。
リザルトは既存の `GameOverScreen` を**使わない**(§20-6-b の #5〜#9 が全部そこに居るため)。

### 20-8. データ表示(★監査で訂正)
- HPの正本は **`src/config/bossHealth.ts`**(`src/utils/` ではない)。しかも**3テーブルに割れており、
  slotKey→HP を引く関数が無い**。⇒ `bossPractice.ts` に **`practiceBossHealth(slot)`** を作る。
- ★**storyBoss(`giantbat@stage-7` / `@stage-ex1`)は `stageBossHealthFor` を通らない**。
  HP代入が無く `enemyUtils` の base **`health: 500`** のまま戦っている。
  表の `'stage-7': 6000` を出すと**12倍の嘘**になる。⇒ この2枠は**HP表示を出さない**(「—」)。
  ※ボス側のHPを変えるのは**バランス変更なのでこの便ではやらない**(§20-2)。

### 20-8-b. 受け入れ条件(機械判定できる形)
1. 拠点に「ボスラッシュ」が並び、**遭遇済みの枠だけ**が押せる。未遭遇は `?` でロック(押しても無反応)。
2. 台帳が `GHOST_DOSSIER_SLOTS` を参照している。**死神・ハンターが含まれないことをテストで固定**。
3. 18枠の**枠→出撃**変換が §20-4 の表どおり(テストで固定)。**実表(`campaign.hiddenBoss` /
   `GATE2_BOSS_TYPE_BY_STAGE`)と突き合わせる**。
4. **練習ラン前後の `localStorage` 差分が `boss.encountered.v1` 以外ゼロ**(§20-6-c・テストで固定)。
5. 遭遇の記録が `engagedBossSlotKeys` 経由でのみ付き、**練習ランでは付かない**(テストで固定)。
6. 練習中に**狙った1体以外のボスが湧かない**(城ボスの5分湧き・ex1の同時湧きを抑止)。
7. 攻略ヒントの本文に**半角数字が入っていない**(テストで固定。全角数字・漢数字は対象外=文意で使う分には可)。
8. `npm run typecheck` / `npm run lint` エラー0。React再描画の規律(CLAUDE.md)を守る。

### 20-8-c. 実装結果(v0.25.2857〜2859・**完了**)
| 受け入れ条件 | 結果 |
|---|---|
| ① 拠点に並び、遭遇済みだけ押せる | **達成**(未遭遇は錠前+「?」・`disabled`) |
| ② 台帳が守護霊メニューと同じ1本 / 死神・ハンター不在 | **達成**(`bossPractice.test.ts` で固定) |
| ③ 18枠の枠→出撃変換が表どおり | **達成**(実表と突き合わせ・lab/storyBossで castlenow を使わないことも固定) |
| ④ 練習ラン前後の localStorage 差分ゼロ | **達成**(`practiceGuard.test.ts`・監査が挙げた実キー14本を1つも通さない) |
| ⑤ 遭遇は `engagedBossSlotKeys` 経由のみ / 練習では付かない | **達成**(`bossEncounter.test.ts`) |
| ⑥ 狙った1体以外が湧かない | **達成**(`?nospawn=1` + 城ボス/storyBossだけ上書き) |
| ⑦ ヒント本文に半角数字なし | **達成**(`bossHints.test.ts`) |
| ⑧ typecheck / lint 0 | **達成**(`npm test` 2,846件 全通過) |

新規: `utils/bossEncounter.ts` / `utils/bossPractice.ts` / `utils/practiceGuard.ts` /
`config/gateBoss.ts` / `data/bossHints.ts` / `components/BossRush.tsx`(+テスト4本)。
改修: `directorTick`(遭遇の記録) / `gameStore`(BOSS_TEST_RUN・練習の決着) / `useGameLoop`(1体保証) /
`App`(練習の勝利は進行に触らない) / `bootstrap`(関所の設置) / `ghostOnline`(送信停止) /
`progress`(遭遇記録のリセット) / `MissionSelect`(入口・前回選択の打ち消し)。

### 20-9. 社長裁定(確定・v0.25.2854)
1. **練習の勝利は「討伐記録」(守護霊メニューの図鑑)に残さない。**
   ⇒ 練習ランでは討伐記録の書き込み経路を1つも通さない(§20-6-3 は確定=★未決ではない)。
   実装は**書くか書かないかの判定を1箇所**に置く(後で方針が変わっても1行で切り替わる形)。
2. **死神(`reaper`)と凶悪ハンター(`hunter`系)は「ボス」に含めない。** 一覧に出さない。
   ⇒ `GHOST_DOSSIER_SLOTS` は元からこの2種を含まないので**現状の台帳のまま**でよいが、
   **将来ここに足さないこと**を掟として明記する(台帳を共用しているので、守護霊メニュー側に
   足された瞬間ボスラッシュにも出てしまう)。**台帳に死神/ハンターが無いことをテストで固定する。**
3. **ステージボス(城ボス `giantbat`)は全員含める。** = §20-4 の「7枠すべて出す」で確定。

- **攻略ヒントの文章**は私が実装から起こした**叩き台**。文体・内容は社長が上書きする前提。


### 20-10. ★社長裁定が要る(監査で判明・設計者と監査で決めない)
**遭遇で解放する仕様のままだと、本編で一生遭遇できない=永久ロックの枠が3つある。**

| 枠 | なぜ本編で会えないか |
|---|---|
| `giantbat@stage-2` | stage-2 は研究施設(`theme:'lab'`)で**城ボスの湧きゲートの外**。**本編に城ボスが存在しない** |
| `suriel@stage-6` | stage-6 は洋館通路(`corridorMode`)で**ゲート2の予約ごと丸ごとスキップ**される |
| `acrasiel@stage-ex1` | stage-ex1 は `storyBossOnly` で**ゲート2の発火条件(`!storyBoss`)を満たさない**。コード側に「通常経路からは当面到達できない」と注記済み |

**社長裁定(確定・v0.25.2857): C =「?」のまま並べる。**
> ？で。後からどっかに置く予定

⇒ **3枠は台帳から外さない。** 遭遇できない間は `?` のロック表示のまま置いておき、
**将来この3体が本編のどこかに置かれたら、遭遇の仕組みがそのまま働いて自動的に解放される**
(解放条件は §20-5 の1本だけなので、置いた側は何もしなくてよい)。
- よって**「遭遇できないから外す」特別扱いのコードを書かない**。ロック枠は普通に `?` で並ぶ。
- **この3枠だけを別扱いする分岐を作らないこと**(将来ボスが置かれた時に外し忘れて詰む)。

**もう1つ**: §20-7-c で「練習は狙ったボスを倒したら即リザルト→ボス選択へ戻る」と決めましたが、
これは**既存のボス戦テストには無い挙動**です(あちらは倒した後もラン継続)。
練習モードとしてはこの形が自然だと考えますが、**ゲームの体験に関わるので一応お伝えします**。
