## ★進軍NPC(護衛軍人)の前進仕様の改良(社長指示 2026-08-02)

### 社長の指示(原文)
> * 敵1体：射撃しながら70%速度で前進
> * 敵2〜3体：射撃しながら30〜40%速度で前進
> * 敵4体以上、または強敵が至近距離：完全停止して救援要請
> * プレイヤーが周囲の敵を減らす：救助成立
> * 救助後5秒程度：通常速度で前進
> * NPCの後方にいる敵や、進路と関係ない敵は無視

### 現状(コードを読んで確認したもの)
- **移動は二値**: 検知範囲(`MELEE_RADIUS 74 × ESCORT_DETECT_MULT 2.25 = 166.5px`)に敵が
  **1体でも居れば完全停止して射撃**、居なければ `ESCORT_SPEED` で担当拠点へ前進
  (`gameStore.ts:12367-12402`)。
- ★**`SURROUND_RADIUS=200 / SURROUND_COUNT=3 / RESCUED_FREE=1` は既にあるが、
  セリフ(`surrounded`/`rescued`)を出すためだけに使われていて、移動には一切効いていない**
  (`gameStore.ts:12331-12348`)。**今回はここを移動へ繋ぐ改良。**
- ★**護衛NPCはHPを持たない**(`gameStore.ts:3282` のコメント「HPなし」)。
  ⇒ **敵を無視しても護衛が死ぬ不利益は無い**ので、「後方の敵は無視」を素直に実装してよい。

### 新仕様
| 脅威の数 | 動き |
|---|---|
| **0体** | 通常速度で前進(現状どおり) |
| **1体** | **射撃しながら 70% 速度で前進** |
| **2体** | **射撃しながら 40% 速度** |
| **3体** | **射撃しながら 30% 速度** |
| **4体以上**、または**強敵が至近距離** | **完全停止 + 救援要請** |
| **救援成立から5秒** | ★**脅威の数に関わらず通常速度**(押し返しの勢い) |

- 「2〜3体で30〜40%」は **2体=40% / 3体=30%** と解釈(範囲の両端を素直に割り当てる)。
- **射撃はどの段でも継続**する(停止中も撃つ=現状と同じ)。

### ★脅威に数える敵の条件(今回の核心)
**次の3つを全部満たす敵だけを数える。**
1. ★**前方の扇の中**: 進行方向(護衛 → 担当拠点)に対して **±60°** 以内。
   ⇒ **後方の敵**も**進路と関係ない横の敵**も数えない。
2. **脅威半径 `SURROUND_RADIUS`(200px) 以内**。
3. **空中(`aiPhase === 'jump'`)は除外**(現状どおり=無敵中は狙わない)。

★**射撃対象も同じ条件で絞る**(「無視」=撃ちもしない)。**HPが無いので不利益が無い。**
★**担当拠点が制圧済み(`captured`)の時は進行方向が無い**ので、**扇を使わず全方位**で数える
(巡回中に背後から来た敵を無視すると、撃たずに棒立ちになる)。

### 強敵の定義(★新しい分類を作らない)
- **強敵** = `isScoreElite(type)`(pumpkin) **または** `isScoreBoss(type)`
  (giantbat / mimir / jormungand / skadi / thor / hunter) — **`gameStore.ts:1876-1877` の既存定義をそのまま使う。**
- **至近距離** = **`MELEE_RADIUS × 1.5 = 111px`**(新定数 `ESCORT_STRONG_NEAR_PX`)。
  ⇒ **強敵が1体でもこの距離に入ったら、他が0体でも完全停止。**

### 救援要請と成立
- **要請(停止に入った瞬間)**: 既存の `surrounded` セリフを出す。
- **成立**: 脅威数が **`RESCUED_FREE`(1) 以下**に減った瞬間 **かつ 強敵が至近距離に居ない**
  ⇒ 既存の `rescued` セリフ + **5秒の通常速度ウィンドウ**を開始。
- ★**既存のセリフしきい値 `SURROUND_COUNT = 3` を、停止条件と同じ 4 に揃える。**
  理由: 社長指示が「**完全停止して救援要請**」と1つの事象にしているため、
  **停止していないのに「囲まれた」と言うのは嘘になる**。
  ★**帰結**: 「囲まれた」セリフの発生頻度が下がる(3体→4体、かつ扇で絞るのでさらに減る)。
  **既存挙動の変更なので、実機で頻度を見て社長に再確認する。**

### 実装の置き場所(CLAUDE.md 実装精度の規律4)
★**判定は `src/utils/` の純関数に切り出し、同コミットでテストを書く。**
```
escortThreat(esc, base, enemies) => {
  count: number;        // 扇+半径で絞った脅威数
  strongNear: boolean;  // 強敵が至近距離に居るか
  halted: boolean;      // 完全停止か
  speedMult: number;    // 1 / 0.7 / 0.4 / 0.3 / 0
  target: Enemy | null; // 射撃対象(同じ条件で絞った最寄り)
}
```
**store はこの結果を使うだけ**にする(判定を `gameStore` に直書きしない)。
救援後の5秒は `esc.rescuedUntil`(gameTime基準)で持つ。

### 受け入れ条件
- **純関数テスト**: 脅威 0/1/2/3/4 体で `speedMult` が **1 / 0.7 / 0.4 / 0.3 / 0**。
- **純関数テスト**: **後方(進行方向の逆)の敵は `count` に入らない**。真横(±90°)も入らない。
- **純関数テスト**: **強敵が至近距離に1体居れば、他が0体でも `halted`**。
- **純関数テスト**: 空中の敵は数えない。
- **救援成立から5秒間は、脅威が4体以上でも通常速度**。5秒経過で再び段階速度へ戻る。
- **画面外では従来どおり停止**(既存の `onScreen` ガードを壊さない)。
- **チュートリアル(M0)の随行NPCは影響を受けない**(別経路 `gameStore.ts:12139-12260`)。
- **拠点制圧の10秒滞在(`dwellMs`)が従来どおり動く**(前進しながらでも円内なら進む)。
- `npm run typecheck` / `npm run lint` エラー0。

### ★未決
**無し**(強敵の定義・至近距離・扇の角度・2体/3体の割り当ては設計チャットで確定した)。
ただし**セリフ頻度の変更(3体→4体)だけは実機で社長に確認する**。

---

## ★品質監査1回目(v0.25.2761)= **未達**。設計者の見落としを記録する。

### ★★社長裁定が要る3件(設計チャットで決めない)

**Q1. 「後方の敵は無視」がミッションの尺を変える(★私が完全に見落としていた)**
★**敵は護衛を狙わない**(`resolveEnemyTarget` は player と summons のみ・`utils/enemyUtils.ts:383-431`)。
⇒ **敵の分布はプレイヤーの立ち位置で決まる**。**プレイヤーが前に出れば、敵は護衛の後方に溜まる**。
⇒ 新ルールでは**それを全部無視するので、護衛が止まらずに拠点を取り続ける**。
⇒ ★**全拠点制圧はクリア条件**なので、これは**ランの長さそのものの変更**。
| 案 | 中身 |
|---|---|
| **A(推薦)** | **扇 + 「担当拠点の周辺(半径R)に居る敵」は角度に関わらず数える**。拠点は目的地なので、そこに張り付く敵は無視しない。⇒ **後方の無駄な足止めは消え、拠点前の抵抗は残る** |
| B | 指示どおり後方は完全無視(ランが短くなるのを受け入れる) |
| C | 「無視」は**撃たないだけ**にして、数には入れる(足は止まる=尺は変わらない) |

**Q2. 射撃レンジが変わる**
現行の射撃/検知半径は `huntingMeleeRadius(player) × ESCORT_DETECT_MULT`
= **プレイヤー連動の可変値(166.5〜243px)**。`gameStore.ts:412` のコメントに
**「社長指示で 1.5→×1.5=2.25」**とある=社長が明示的に決めた値。
「射撃対象も扇+200pxで絞る」と、**固定200pxへ変わる**。
⇒ **推薦: 撃つ範囲は従来どおり(プレイヤー連動)のまま、扇だけ適用する。**
  (数える範囲=扇+`SURROUND_RADIUS`、撃つ範囲=扇+従来の可変半径。**役割を分ける**。)

**Q3. 「囲まれた」セリフのしきい値 3→4**
設計書は「実装してから実機で確認」としたが**順序が逆**(仕様変更は事前承認)。
かつ**「4体」と「扇で絞る」を同時に掛ける**ので、**発火頻度が乗算で下がり、後から分離できない**。
⇒ **推薦: セリフのしきい値は 3 のまま据え置き**(停止は4)。
  「3体で"囲まれた"と言いながら30%で進み、4体で止まる」は、**焦りの表現として自然**。

### 設計チャットが直す(社長裁定不要)
- **B-1 致命**: 拠点到達後(占拠の10秒間)は前進ベクトルが**長さ2px未満の残差**になり、
  **扇の向きがほぼ任意の方角に固定/0除算でNaN**。しかも新仕様では**この状態が常態**になる。
  ⇒ **扇の基準は「その時の進行方向」ではなく「護衛→担当拠点の向き」を、
    到達前の最後の有効な向きとして保持する**。距離が閾値未満なら**全方位**へ切り替える。
- **B-2 致命**: 「強敵」に `isScoreElite`/`isScoreBoss` を使うと、**裏ボス(miguel/jibril/rafi/uri/
  suriel/acrasiel/idol)と reaper を取りこぼす**。あれは**スコア集計用**の分類だった。
  ⇒ **`utils/enemyUtils.ts:116` の `isHiddenBoss` を使い、`reaper` と pumpkin/giantbat/hunter 等を
    合わせた「脅威用の分類」を新規に純関数で定義する**(スコア用と混ぜない)。
- **B-3 致命**: `rescuedUntil` を `gameStore.ts:12412` の `escortsChanged` 判定式に足さないと、
  **停止中(x,y不変・fireAt更新なし・dwell 0)のフレームで計算結果ごと捨てられる**
  ⇒ **セリフだけ出て5秒ウィンドウが消える**(症状が「たまに効かない」になり原因が見えない)。
- **B-4**: 純関数の引数に **`now`(gameTime)** と **`player`**(可変半径のため)が要る。
- **C-1 発振**: `strongNear` の111pxに**ヒステリシス幅が無い**ので境界上で毎フレーム反転し、
  **そのたびに5秒ウィンドウが再武装** ⇒ 強敵が居座るだけで**恒久的に100%速度**。
  ⇒ **入る111px / 出る150px** のように**幅を持たせる**。
- **C-2 発振**: `pumpkin`/`giantbat`/`hunter` は**跳ぶたびに `aiPhase==='jump'` で脅威集合から消える**
  ⇒ **攻撃周期そのままで「停止→救助成立→フル速度→停止」を繰り返す**。
  ⇒ ★**jump除外は「射撃対象」だけに適用し、「脅威の数」には空中の敵も数える**。
- **D-1 経路漏れ**: 護衛の実装経路は**3つ**(M0 / **洋館通路 corridorMode** / 通常拠点)。
  corridorMode(`gameStore.ts:12212-12277`)も**同じ「1体でも居れば完全停止」**をしている。
  ⇒ **対象に含める**(CLAUDE.md「同じ"動作"を持つ全員に付ける」)。
- **E-1〜E-6**: 5秒ウィンドウは `strongNear` も上書きするか(**しない**=強敵が至近なら止まる)/
  巡回時の速度段(**従来どおり停止**)/ 救助成立に `wasSurrounded` ラッチを要求する(**要求する**)/
  `face` の決定順(**撃つ方を優先**)/ 画面外でウィンドウが経過するのは**許容**/
  壁抜けは**既存踏襲**(今回は触らない)。**全部設計書に明記する。**
- **F**: 「検知半径166.5px固定」は誤り。**ハンティング溜め中は最大243pxまで伸びる**可変値。
