# HANDOFF_CODEX.md — Codexへの引き継ぎブリーフ

## 最新追補 — v0.25.2847 / 2026-08-05（Codex作業分のみ）

この追補が現在の入口。ここでは今回Codexが担当した `v0.25.2844`〜`v0.25.2846` だけを記録する。
それ以前の設計史は、下の旧ブリーフではなく `DEVELOPMENT_LOG.md` と `PACING_PUZZLE.md` を正本として確認すること。

### 現在地

- 開発ブランチ: `claude/chat-context-continuity-saxlH`
- 最終ゲーム実装: `5be642c1`（v0.25.2846）
- 公開先: `https://tanity0.github.io/zombie/`
- v0.25.2846のGitHub Actions CI / Pagesは成功済み。公開ページでもバージョンと「フィールドガイド」を確認済み。
- v0.25.2847はこの引き継ぎ文書の追加のみ。ゲーム内容の変更はない。

### 今回実装した内容

1. **全助っ人へのいいね・オンライン集計（v0.25.2844 / `f76d1d74`）**
   - 固定AI20人とオンライン守護霊の両方を、リザルトのいいね対象にした。自分の守護霊への自己投票は除外。
   - 守護霊部屋へ公開状態、新着、最終更新、ボス別の同行回数・いいね数を表示。
   - 固定AI集計はD1へ冪等保存。本番migration `0002_fixed_guardian_feedback.sql` 適用済み。
   - 公開Worker Version ID: `2f07cb8f-e23f-4b2d-aae9-47cfa44ac7c2`。

2. **守護霊部屋のボス図鑑化・未開放拠点の安全化（v0.25.2845 / `8dd3485d`）**
   - 左に18体のボスアイコン、右に選択ボスの討伐ドシエを置く構成へ刷新。未討伐は `?`。
   - 討伐時キャラ、ステータス、武器、近接、装備、サブ、スキル、討伐時間、評点、いいね、同行回数を集約。
   - 同行ベストと同行者ビルド、固定AI上位4人の評点・同行・いいねも同じ画面へ統合。
   - ハンターは拠点の開放前後を問わず、150px以内では出現せず、索敵・追跡中も撤退する。

3. **ステージ1の一度だけの3枚チュートリアル（v0.25.2846 / `5be642c1`）**
   - 「武器商人」「拠点」「目的」の横スライド式フィールドガイドを、ステージ1初出撃時に一度だけ表示。
   - 左右スワイプ、戻る・次へ、ページ番号、ドット、最終ページの「はじめる」を実装。
   - 新ID `stage1-guide` のため、旧チュートリアル既読端末にも新版が一度だけ出る。
   - M0訓練は従来どおり毎回表示。資料室も複数ページへ対応。

### 次に引き継ぐこと

- **チュートリアル動画待ち**: 社長から3ページ分の素材を受け取ったら、`src/data/tutorials.ts` の
  `STAGE1_GUIDE_SLIDES` 各ページへ `img` を指定し、`mediaPending` を外す。表示部品と資料室は対応済み。
- **守護霊部屋の実機目視**: 実装とテストは完了しているが、v0.25.2845はローカルブラウザ接続が使えず、
  390×844での最終目視を未実施。iPhoneで左レール、未討伐 `?`、右ドシエの読みやすさを確認する。
- 現時点で既知のコードブロッカーはない。

### 主なファイル

- 守護霊図鑑: `src/components/GhostBossDossier.tsx`, `src/utils/ghostDossier.ts`, `src/components/MissionSelect.tsx`
- いいね/オンライン: `src/utils/ghostOnline.ts`, `src/components/GameOverScreen.tsx`, `server/src/ghost-api.js`
- ハンター安全地帯: `src/utils/viciousHunter.ts`, `src/hooks/useGameLoop.ts`
- フィールドガイド: `src/data/tutorials.ts`, `src/components/TutorialPopup.tsx`, `src/utils/stage1GuideTutorial.ts`
- 詳細な検証記録: `DEVELOPMENT_LOG.md` の v0.25.2844〜v0.25.2846

---

## 旧ブリーフ — v0.25.1358時点（歴史資料）

このファイルは、開発をCodex(または任意の新しいエージェント)へ引き継ぐための入口。
**最初にこのファイルを読み、次に§3の順でファイルを読むこと。**

## 1. 何をしたいか(目的)

**大目的**: トップダウンHD-2Dサバイバル(React+Zustand+PixiJS/モバイルSafari実機・60fps死守)の
ゲーム体験を「ジェットコースター(最初からMAX)」から「**心電図**(緩急が呼吸する)」へ再設計し、
コアループ(①秒=刈る手触り/②分=関所と深入りの意思決定/③ラン=ゴールド→強化)を閉じること。

**直近の具体テーマ(2026-07-03時点)**:
1. **台本ローテーション化(★会議中・社長採否待ち)**: 統合テスト(v0.25.1357)の判定が
   「あまりにも単調・ゾンビまでしか出ない」。原因診断済み=Tank化で問題児が配役専用になったが
   pressureの登り(τ8s)が関所40秒に対して遅い+台本選択がrank寄せで優しい台本ばかり回る。
   対策の叩き台(60秒コマ交互・未見優先シャッフル・直前台本禁止・時間解禁の階段・上げτ5s)が
   PACING_REDESIGN.mdの★未決事項/DEVELOPMENT_LOG最新エントリにある。**社長の採否・修正が出たら
   それを実装するのが次の大仕事。**
2. ゴールド経済の接続(CORE_LOOP.md§6の価格叩き台・社長採否待ち)。
3. ダンスビートB方式は実装済み(v0.25.1349)・実機確認待ち。

## 2. いまどういう状況か

- **実装済み**: 緩急再設計バッチ1/1.5/2/2.5/3最小版/3完成版(Tank化)/3.5(配合+盤面在庫)/
  4(緩の演目)/5(台本選択)/5追補(イベント関所=囲い昇格)/6(stageAggro)/7(イベントゲート)+
  診断グラフ(リザルトに関所帯・イベント帯・pressure・在庫・up+N)+監査修正一式+ダンスB。
- **品質ガード**: `src/utils/constitution.test.ts`(憲法=横断不変条件7件)が生きている。
  これを破る変更はCIで落ちる。破りたい時は★未決事項で社長裁定。
- **直近の実機判定**: 統合テストは「単調」(上記1の理由)。数値バランスは全体に「実機調整前提」の
  叩き台のまま。
- **並行体制**: これまで「設計チャット(Fable)=会議・診断・仕様化」「実装チャット(Sonnet)=バッチ実装」の
  2チャット体制。受け渡しは**全部ファイル経由**(チャットにしか無い情報は存在しないのと同じ)。
  Codexがどちらの役を担うかは社長の指示に従うこと。

## 3. 読むファイル(この順で)

| 順 | ファイル | 何が書いてあるか |
|---|---|---|
| 1 | **CLAUDE.md** | 絶対規律。仕様変更ルール(勝手に変えない・提案→社長採否)/実装精度の規律/2チャット分業/トークン節約/Branch lock/version bumpルール |
| 2 | **PACING_REDESIGN.md** 冒頭 | ★未決事項と実装キュー=現在地。以降の本文は緩急システムの全仕様 |
| 3 | **DEVELOPMENT_LOG.md** 先頭〜10エントリ | 直近の変更履歴(これが正史)。作業したら必ず追記 |
| 4 | **ENGINEERING_NOTES.md** | 診断の型・実バグ化した地雷(Pixi v8 filterArea/iOS音声/集計の生参照等)・逆引き表。**描画/音声/集計/スポーンを触る前に必読** |
| 5 | **CORE_LOOP.md** | コアループ棚卸しとゴールド経済の叩き台 |
| 6 | **GAME_AUDIT.xlsx** | 抜け漏れ54件+バグ/仕様疑義16件の台帳(対応状況つき) |
| 7 | DESIGN_CHAT_GUIDE.md | 設計チャット役をやる場合の運転マニュアル(社長との流儀) |
| 8 | HANDOFF_DANCE_AUDIO.md | 音声を触る場合のみ(iOS Safariの壁の経緯とB方式仕様) |

※旧CHAT_HANDOFF.mdは古い(非推奨ヘッダあり)。読まなくてよい。

## 4. 作業ルールの要点(詳細はCLAUDE.md)

- ブランチは **`claude/chat-context-continuity-saxlH`** のみ(Branch lock)。push前に必ず`git fetch`
  (並行エージェントがいる。version番号が衝突したら自分の分を繰り上げる)。
- **毎pushで`package.json`のversionをbump**し、**全返信の末尾に現バージョンを明記**。
- 変更ごとにDEVELOPMENT_LOG.mdへ追記(何を・なぜ・検証結果・負荷スコア1/10形式・
  憲法4条/5条の自己点検1行)。
- **push前検証は`typecheck`のみが常時フロア(約9秒)。テスト/ビルドは社長が指示した時だけ回す**
  (v0.25.1528改訂・詳細はCLAUDE.md「Testing policy」)。自己判断で毎push `npm test`/build を回さない
  (M9ボットスモークが約38秒=無駄が大きい)。文書のみ=typecheck不要。CIは毎pushフル=安全網。
- **仕様・数値・挙動を勝手に変えない**。未決に当たったらPACING_REDESIGN.mdの★未決事項に書いて
  止まる(コードコメントに質問を書かない)。
- 挙動を変える機能には復帰フラグ(`?xxx=0`)を付ける。既存フラグ: ?ladder/?events/?mix/?debt/
  ?setpiece/?beat/?upswing/?gateprogram/?program/?zoomlock。
- シミュレーション(src/store, src/utils, src/world)はレンダラ非依存を維持。PixiJSは読むだけ。
