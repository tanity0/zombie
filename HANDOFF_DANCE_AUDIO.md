# 引き継ぎ: ダンスタイムBGMが「軽く」かつ「鳴る」を両立できない問題

> Codex への依頼用プロンプト。zombie（React + Zustand + PixiJS のトップダウン survival）で、
> **四神舞(ダンスタイム)中に専用BGMを鳴らしつつ実機で60fpsを維持したい**が未達。
> 長時間の試行錯誤の経緯・**確定事実**・**憶測**・**次の一手**を整理した。続きをお願いします。

## 0. ゴール
四神舞(shijin サブウェポンで停止すると入る「ダンスタイム/リズムモード」)の間だけ、
**戦闘BGMの代わりにダンス専用ループBGM**(四神舞レベルでBPMが変わる: Lv1=100 / Lv2=120 / Lv3=140)を鳴らす。
**実機(モバイル。挙動からおそらく iOS Safari)で 60fps を維持**すること。
※ダンスの**見た目の演出(リズムのサークル/パルスグリッド等のVFX)は完成済みで軽い**。問題は**音声だけ**。

## 1. 現在の状態（このブランチ）
- バージョン: **v0.25.282**（`package.json`）
- 方式: **単一BGM `HTMLAudioElement` の `src` 差し替え + スタート操作時の `unlockDanceAudio()`**。
- 実機結果: **v0.25.280で成功候補**。ユーザー確認は「うん、上手くいってる。微調整は必要だけど。」
- つまり「鳴るが重い」状態は脱出した可能性が高い。次はこの方式を維持した微調整。

## 2. 確定事実（低電力モードOFFで再現確認済み）
低電力モード(iOSの省電力モード)は**途中までずっと計測を汚染していた**(30fps固定の原因)。
以下は**低電力OFFを確認した上での確定**:

1. **HTMLAudioElement での音声再生＝軽い**。戦闘BGM(MP3を要素で再生)で通常プレイ60fps。
2. **ダンスVFXだけ＝軽い**。`?danceaudio=0`(ダンス中も戦闘BGMを流しダンス専用音声を出さない)で**60fps**。
   → リズムのサークル等の描画は安い。**重さの原因は「ダンス専用音声を出すこと」**。
3. **Web Audio の AudioBufferSourceNode を“連続ループ再生”すると重い（ダンス中~20fps）**。
   SFX(短い一発)は同じ AudioBufferSource でも平気＝**「連続再生」がこの端末のWeb Audioで高コスト**。
   （v0.25.275 / v0.25.278 の両方で 20fps、音は鳴る、を確認）
4. **HTMLAudioElement の src をゲーム中に差し替えると、差し替え先は“無音”**。
   要素は「最初に(スタートのタップで)再生した src」しか鳴らせず、**src を変えると鳴らない**。
   - 戦闘曲↔ダンス曲、どちら向きの差し替えでも、差し替え後は無音。
   - `canplay`/`loadeddata`/`canplaythrough` を待って再生し直す堅牢化(v0.25.276)でも**無音のまま**。
   - 差し替え方式が「軽い(~57fps)」のは、結局**何も鳴っていない**から。
5. **ダンス曲ファイル自体は正常**。要素の“最初のsrc”にダンス1を設定したら起動時にちゃんと鳴った(v0.25.273)。
   → 無音は**ファイル不良ではなく「差し替え」固有**。
6. **毎フレームの音楽同期(resync)は撤去済み**。ダンスのサークルは固定 gameTime グリッドで動く
   (v0.25.250 で resync 廃止、v0.25.277 で死にコード `resyncRhythm`/`getMusicTimeMs`/`lastMusicMs` を完全削除)。
   **フレーム処理系に音楽同期の残骸は無い**。毎フレーム走るのは `tickRhythm`(固定グリッド上の拍取りこぼし判定のみ)。

## 3. 憶測 / 未確定（クリーンに測れていない）
- **【最有力・未検証】「専用の2つ目の HTMLAudioElement を用意し、各レベルが自分のsrcを鳴らす」方式**
  （差し替えしない＝無音にならない／要素再生＝軽いはず）。
  - これを「重い(9〜10fps)」と判断した **v0.25.259(素再生) と v0.25.267(WebAudioルーティング) は、
    低電力モードを切る前のテスト**だった。**数値は低電力モードに汚染されている疑いが濃い**。
  - **低電力OFFでの再評価が未実施**。要素再生自体は軽い(確定2/1)ので、**2要素方式は低電力OFFなら軽い可能性が高い**。
  - これが本命。コードは git 履歴にある（`99510f3` = v0.25.267 ルーティング版 / 素再生版は v0.25.259 系）。
- **差し替え無音の機序＝自動再生ポリシー(per-resource のジェスチャ解錠)** と推測。
  「最初にタップで鳴らした src だけ許可される」挙動と整合。未確定。
  - もし正しければ対策案: **スタートボタンのタップ(ジェスチャ)中に、各ダンス曲を一瞬だけ要素で鳴らして“解錠”**しておく。
- **createMediaElementSource(ルーティング)自体は軽い**と推測（戦闘BGMはルーティング済みで60fps）。
  重いのは **AudioBufferSourceNode の連続再生**であって、MediaElementSource 経由ではない。
- ハリケーン鳴動(`hurricane.wav`)は loop する AudioBufferSource だが体感問題なし＝**短い/小さいバッファは平気**。
  ダンス曲は 13〜19秒の大きいバッファ。ただし**ループ再生のCPUはバッファ長に依存しない**はずで、
  なぜ重いのかは未解明（端末のWeb Audio実装が連続出力に弱い、が今のところの結論）。

## 4. 試した方式と結果（時系列の要点）
| 版 | 方式 | 結果 | 低電力 |
|---|---|---|---|
| `?danceaudio=0` | ダンス中も戦闘BGM継続(専用音声なし) | **60fps** / 専用曲なし | OFF |
| 258 | Web Audio バッファ ループ | 重い | ON(汚染) |
| 259 | 専用ダンス要素×複数・素再生(element.volume) | 10fps・音出る | **ON(汚染)** |
| 263 | 単一要素・ダンス中も戦闘BGM(差し替えなし) | 60fps / 専用曲なし | OFF |
| 265/266 | 単一要素・src差し替え | **無音**・軽い | 一部OFF |
| 267 | 専用ダンス要素×複数・WebAudioルーティング | 9fps・音出る | **ON(汚染)** |
| 269(=266) | 単一要素・src差し替え | 通常60/ダンス57・**無音** | **OFF** |
| 271/272 | 単一要素・src差し替え(+canplay堅牢化, ダンス曲MP3化) | **無音**・軽い | OFF |
| 273 | 要素の“最初のsrc”をダンス1に | 起動時に**鳴る**(ファイル無罪) | OFF |
| 274/275 | Web Audio バッファ(MP3デコード)・連打防止 | 音出る・**ダンス20fps** | OFF |
| 276 | 単一要素 src差し替え + 多イベント堅牢再生 | **無音**・ダンス30fps | OFF |
| 277 | 音楽同期の死にコード撤去 | 挙動不変 | OFF |
| 278 | Web Audio バッファ | 音出る・ダンス20fps | OFF |
| 279 | レベル別固定HTMLAudioElement + unlock | 音出る・ダンス20fps | OFF |
| **280/281/282(現在)** | **単一BGM要素src差し替え + unlockDanceAudio** | **成功候補。音あり・軽い方向。微調整必要** | OFF |

## 5. コアのジレンマ
ダンス曲を鳴らすには次の2択しか見つかっておらず、両方とも欠点がある:
- **(a) Web Audio バッファ** → 鳴る。でも**重い(20fps)**。
- **(b) HTMLAudioElement の src差し替え** → 軽い。でも**無音(差し替え先が鳴らない)**。

`v0.25.279` で第3の道（固定複数HTMLAudioElement）は、音は鳴るがダンス20fpsで不採用。
`v0.25.280` で単一BGM要素 `src` 差し替え + gesture unlock が成功候補になった。
以後はこの方式を維持して微調整する。

## 6. 関連ファイル / コードアンカー
- `src/audio/audioManager.ts` … 音声の中心。現在は Web Audio バッファ方式(278)。
  - `DANCE_LOOP_TRACKS`（`public/audio/dance-100/120/140-loop.mp3`）
  - `danceBuffers` / `loadDanceBuffer` / `startDanceBuffer` / `stopDanceBuffer` / `applyDanceBuffer`
  - `setDanceMode(active, level)`（export。ダンス中は `applyBgm` で戦闘要素を pause、`applyDanceBuffer` でバッファ再生）
  - `applyBgm`（戦闘要素。`danceActive` の間は pause）
  - **連打(ダダダ)対策**: `danceSourceLevel` + `danceStopTimer`(300msディレイ)。
    理由: `rhythm.active` が一瞬 false に揺れると `setDanceMode(false)→(true)` が往復し、バッファが頭出し連打される。
- `src/hooks/useGameLoop.ts` … `setDanceMode(true, lvl)` / `setDanceMode(false)` を **rhythm.active の変化エッジ**でのみ呼ぶ(～L650-663)。毎フレームではない。
- `src/store/gameStore.ts` … rhythm 状態と `tickRhythm`(毎フレーム・固定グリッド・音声なし)。
- `src/config/shijin.ts` … `RHYTHM_ENTER_IDLE_MS`(600) / `RHYTHM_EXIT_MOVE_MS`(320) / `rhythmIntervalForLevel`。
- 音源: `public/audio/dance-100/120/140-loop.wav`(各~3MB) と `.mp3`(各~400KB)。各BPM曲の盛り上がり8小節を継ぎ目クロスフェードでシームレスループ化したもの。

## 7. 推奨する次の一手（優先順）
1. **現行 `v0.25.280+` 方式を維持して微調整**。
   - 単一BGM要素 `src` 差し替え。
   - スタート操作時の `unlockDanceAudio()`。
   - ダンス終了時の300ms遅延復帰。
   - これはWeb/iOS Safari向け対策。ネイティブアプリ移行時は削除する。
2. 音の継ぎ目、復帰タイミング、ダンス開始/終了時のフェード感が気になる場合だけ微調整。
3. もし今後また無音化する端末が出たら、同じBGM要素そのものを使ったgesture unlockへ強化する。

## 8. 注意・落とし穴
- **低電力モードを必ず切って計測**（電池が減ると自動再ONされFPSが30固定に汚染される。充電しながら推奨）。
- **`package.json` の version を毎push必ずインクリメント**（左下/タイトルに表示。実機で読み込んだビルドの確認用）。
- 毎pushで `DEVELOPMENT_LOG.md` に追記する運用。
- React は毎フレーム再描画させない（CLAUDE.md の「React re-render discipline」厳守）。
- 既知の小問題: 要素 loop / バッファ loop どちらも継ぎ目に微小ギャップが出うる。iOSは素再生で音量スライダーが効かない。
- 開発ブランチ: `claude/zombie-sprite-optimization-cld954`（ミラー: `claude/chat-context-continuity-saxlH`）。

## 9. 全実験の羅列（バージョン別・ダンスBGM関連 v0.25.233〜278）

> 「方式の組み合わせ」を時系列で全部列挙。低電力=計測時に低電力モードがONだった疑い(=数値汚染)。

### A. 初期実装フェーズ（演出+最初の音声）
- **233**: リズムをBGM(120BPM)の拍に位相同期 + `pulse-grid.mp3` をBGMに追加。
- **234**: pulse-grid はダンス中だけ。メインBGMはダック(音量0で再生継続)。終了でフェードイン。拍を再同期。
- **235**: 失敗してもダンス継続。サークルは120BPMで流れ続ける。
- **236**: ダンスBGMが鳴らない問題を「常時再生でアンロック」で修正。
- **238**: ダンスBGM混在/音量修正・素材プリロード。
- **247**: レベルでBPMが変わる(Lv1=100/Lv2=120/Lv3=140)。トラックも切替。
- **248**: レベル別ダンス曲を実配置(100/120/140) + URLキャッシュバスト。

### B. 「ダンス中だけ重い」原因切り分けフェーズ
- **249/250/251**: 演出側の軽量化（resync を音楽進行時のみ→**毎フレームresync廃止**→頭上矢印を毎フレーム描き直さない）。＝VFXは軽くした。
- **252**: ダンス曲を**再エンコード**(320k/48kHz→128k/44.1kHz, 約26MB→11MB)。→ 改善せず＝ビットレート無関係。
- **253**: 診断トグル `?danceaudio=0`(音声OFF) / `?dancevfx=0`(描画OFF) を追加。→ **音声OFFでのみ60fps回復＝原因は音声**。

### C. 再生エンジン/要素トポロジを変える本格フェーズ
- **254**: ダンス曲を**WebAudio非経由のネイティブ再生**(element.volume/muted)に。メインはダック維持。→ まだ重い。
- **255**: ダンス中は**メインBGMを pause**(同時2ストリーム回避、常に1本)。→ まだ重い。「2本同時」ではなく**2つ目の要素の存在自体**が怪しいと判明。
- **256**: **1音声要素のsrc入れ替え**方式(2つ目の`<audio>`を作らない。戦闘曲↔ダンス曲を同一要素で差し替え)。→ まだ重い/無音。
- **257**: 「サンプルレート不一致(44.1k→48kリサンプル)」を疑い解消。→ 改善せず。
- **258**: ダンス曲を**WebAudio の PCMバッファ(AudioBufferSourceNode)ループ再生**に。→ 重い+ブツ切れ（※当時は低電力疑いだったが後に**バッファ連続再生が重い**と確定）。
- **259**: **専用ダンス要素×複数・素再生(element.volume)**。→ 10fps（**低電力汚染**の疑い）。音は出る。
- **260**: ダンス専用音声を**既定OFF**(戦闘BGM継続=60fps)に一旦退避。

### D. 単一要素 src差し替え vs 戦闘BGM継続の切り分け
- **261**: 診断: **戦闘BGMの素を起動時からダンス1**に。→ 起動時は軽く鳴る。
- **262**: **単一要素の src差し替え**方式(2系統目を作らない)。→ ダンスだけ重い。
- **263**: 診断: **ダンス中も戦闘BGMのまま**(src差し替えを起こさない)。→ **軽い(60fps)**＝差し替え/読み込みが重さの源。
- **264**: 診断: 戦闘曲を起動時からダンス1に。→ 軽い。
- **265**: 診断: **通常=ダンス1 / ダンス中=戦闘MP3へ差し替え**。→ ダンス中**無音**・軽い。
- **266**: 診断: **BGMをWebAudio非経由の素再生(element.volume)**に(`?bgmroute=on`で従来)。→ 通常/ダンス両方~30fps(後に**低電力**と判明)・無音。
- **267**: **専用ダンス要素×複数・WebAudioルーティング**。→ 9fps（**低電力汚染**の疑い）。音は出る。
- **268**: ダンス専用BGMを一旦断念し263構成へ戻す（発熱/低電力を疑い始める）。

### E. 低電力モードを切ってからの再評価
- **269(=266)**: 低電力OFF確定。→ **通常60 / ダンス57fps**・ただし**無音**(差し替え)。
- **270(=267)**: 低電力OFF。→ ダンス9fps（2要素ルーティング。要再評価）。
- **271**: 単一要素 src差し替え + **canplay待ち**で無音解消を試行。→ **無音**のまま。
- **272**: ダンス曲を**MP3化**(差し替え先がMP3なら鳴る仮説)。→ **無音**のまま・軽い。
- **273**: 診断: 起動時の通常曲を**ダンス1ファイル**に。→ **起動時は鳴る**＝ファイルは無罪、無音は差し替え固有。
- **274**: **Web Audio バッファ(MP3デコード)でループ再生**。→ 音は鳴る。ただし「ダダダ」連打。
- **275**: 連打(ダダダ)を**同レベル再起動なし＋停止300msディレイ**で防止。→ 普通にループ。**ダンス20fps**。
- **276**: 単一要素 src差し替え + **多イベント堅牢再生**(即時+loadeddata/canplay/canplaythrough)。→ **無音**・ダンス30fps(堅牢再生のチャーンで余計重い)。
- **277**: 音楽同期の死にコード(`resyncRhythm`/`getMusicTimeMs`/`lastMusicMs`)を完全撤去。挙動不変。
- **278(現在)**: **Web Audio バッファ方式**(=275相当)に確定。→ **音は鳴る・ダンス20fps**。

### F. 直交する「方式の組み合わせ」マトリクス（要点）
| 再生エンジン \ 要素トポロジ | 2要素(メイン+ダンス) | 1要素 src差し替え | 1要素 差し替えなし(ダンス=戦闘BGM) |
|---|---|---|---|
| **要素 素再生(element.volume)** | 254/255/259（重い/無音。低電力汚染あり） | 256/265/266/271/272/276（**無音**・軽い） | 263/268（**60fps**・専用曲なし） |
| **要素 WebAudioルーティング** | 267/270（重い。低電力汚染の疑い・**音は出る**） | 261/262/264（重い or 無音） | 269（**通常60/ダンス57**・無音） |
| **WebAudio バッファ(AudioBufferSource)** | — | — | 258/274/275/**278**（**音は出る・ダンス20fps＝重い**） |

→ **唯一クリーンに「音が出る」のは WebAudioバッファだが重い(20fps)。要素src差し替えは軽いが無音。
2要素方式は“音は出る”が低電力下でしか測れておらず未確定**（＝最優先の再評価対象）。
