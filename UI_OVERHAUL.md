# UI刷新(社長裁定2026-08-28)— 設計書

> 状態は PROJECT_STATUS.md。ビジュアルの正=アーティファクト「zombie 新UI提案」v2
> (https://claude.ai/code/artifact/688d9c78-696a-474d-8808-a3922091c0cd)。
> 本書はその実装仕様。**先行バッチ=プレイ中HUD(§1戦況ライン+§2ワインHP)**(社長指示
> 「取り急ぎ、プレイ中のUIの方を先行して修正。HPとメーター」)。作戦室(§3)・吸血収束(§2b)は後続。

## 裁定台帳(2026-08-28・全て社長の言葉から)
- メニュー再編成=採用/方向=案A(司令部)を「**現在の雰囲気**」で(「モックの内容だとダサい。
  変に角を削るのとかもダサい」=コーナーカット等の装飾は全廃)。
- HP=「もっとリアルな液体の様に…減ると赤い水が減って、波を縦ながら落ち着く感じ。(増えてもそう。
  増減幅で波の大きさが変わる)」「HPの波のイメージは、ワイングラスにワインが注がれてるようなイメージ」
  「**液体の動きだけ。器は今の丸サークルです。(経験値の見せ方もあるし)**」。
- メーター=「いま上部にピークとかランクを表示してるLINEがあるけど、あれも変えたい。上から0pxに
  1px幅で横いっぱいにライン引いて、色でピーク表示はいままで通り、ランクは真ん中から伸びていき、
  ランク7で横幅いっぱいになるイメージ。その際、左右はフェードアウトで透過していくラインで、
  ランク7は透過無しのいっぱいいっぱいの表示になるイメージ。」
- 吸血スキルの血の粒はワイン(HP)へ収束(後続バッチ§2b)。TREASURE!統一=済(v4016)。
- 未裁定: ワインの色(赤 or 現行紫)→**本バッチは現行紫のまま「動きだけ」**(裁定が出たら定数1箇所で差し替え)。

## §1 戦況ライン(DirectorLine の置き換え)
対象=`src/components/DirectorLine.tsx`(現行: 上部30px下・幅72px・左→右で満ちる・刻み7つ)。
1. **位置/形**: `top: 0`(画面最上端・safe-area より上=文字通り0px)・**高さ1px**・中央基準。
2. **長さ=ランク**: 幅 = `rank/7 × 100vw`(`left:50%; translateX(-50%)`)。変化はCSS transition(現行の
   width 400ms・慣性カーブ)。**★幅を持つ本体要素は再マウントしない**(現行DirectorLineの
   `key={hud.rank}` を写経すると transition が一度も走らない=着手前監査A-1)。ランク変化フラッシュは
   **兄弟オーバーレイ要素**に key を持たせるか WAAPI(`el.animate()`)で再生する。
3. **両端フェード**: CSS `mask-image`+`WebkitMaskImage` 併記(インラインstyleはautoprefixer対象外・
   リポ内前例 TitleScreen.tsx:163)。**フェード幅はランクの関数で連続的に細らせ、ランク7で自然に0**
   (=端までベタ)。7になった瞬間の mask:none スナップは慣性MUST違反のため不可(監査A-6・
   叩き台: 透過帯% = 22%×(1 − (rank−1)/6))。
4. **色=コマ(現行踏襲)**: relax青緑(静止)/normal白(弱明滅)/harvest金(ゆらぎ)/peak赤(速い脈動・
   1pxのため太さ変化は廃止し脈動=opacityのみ・**現行の boxShadow 発光(0 0 6px)は残す**=低α×細線の
   視認補強)。**色の優先順位: ①紅き夜=紫が全体を乗っ取り ②コマ色**(監査A-8)。
5. **ランク変化フラッシュ**: brightness フラッシュを**兄弟オーバーレイ**で再生(§1.2のとおり本体は
   再マウントしない)。
6. **廃止/翻訳**: 下地の刻み7つ(1pxでは読めない)は廃止。ボス戦の金縁 → ランク1〜6=**両端フェード
   部分を金に差し替え**・ランク7(フェード0)=**両端の最外2%だけ金**(叩き台。読めなければ実機後調整)。
6b. **top:0の注記(監査A-7)**: 社長の言葉どおり**文字通り0px**(safe-areaの上)。アプリ全画面では
   ノッチ帯/丸角に端が掛かる端末がある——開発チャネル(Safari)では再現しないので実機で欠けが
   気になった時のために `?linetop=1` で `env(safe-area-inset-top)` へ下げる切替ツマミを付ける
   (既定=0px)。
7. **再レンダー規律(現行踏襲・MUST)**: 購読は `hudDirector` ミラー(変化時のみ書かれる)+
   紅き夜/ボスのboolean派生のみ。脈動はCSSアニメ=毎フレームJSなし。

## §2 ワインHP(VitalsOrb の液体だけ差し替え)
対象=`src/components/VitalsOrb.tsx`。**変えないもの(MUST)**: 器(76px円)・EXPリング・中央のHP数字
(即時反映のまま)・Lvバッジ・被弾パンチ/白フラッシュ・装備アイコン・色(現行紫の2段階
hpHi/hpLo・裁定待ち)。
1. **液体の実装**: 現行のSVG矩形塗り(280ms ease)→ **オーブ内専用の小canvas**(76px・DPR対応)に
   置き換え。円クリップ内で液面を描く。
   - **DOM構造(監査A-2/A-3で確定)**: 暗い土台円+液体=canvas(`opacity:0.7`=現行の
     `<g opacity={0.7}>` v0.25.1994指示の継承)。ガラス艶・縁・EXPリング・HP数字・白フラッシュ=
     既存SVGを**絶対配置でcanvasの上に重ねる**。canvasは**被弾パンチのラッパー内**に置くが、
     パンチの再生は `key={hitKey}` の再マウントをやめ**クラス付け外し+`el.animate()`**に変える
     (再マウントするとcanvasの波・shownHpが被弾のたびに消える=波が最大になるべき瞬間に飛ぶ)。
   - **液面**: 2層サイン波(位相逆行・端は振幅減衰)。
   - **★モックで踏んだバグ(社長報告2026-08-28「HP増えると細くなる中身が」)**: 塗りの多角形を
     「液面の高さでの円の弦幅」で作ると、液面が円の上部(弦が狭い)にある時に胴体まで細い帯になる。
     **胴体は常に円の全幅(±R)で塗り、円クリップに削らせる。弦幅を使うのは液面の波線だけ**。`shownHp` は実HPへ指数追従(注ぎ/流出の速度感・
     叩き台3.2/s)。液面ハイライト線は現行の白35%を波線で踏襲。
   - **波=増減幅**: HP変化時 `振幅 += k×|ΔHP比|`(cap付き・叩き台)。減衰は指数(ワイン=粘る・
     叩き台1.9/s)。**慣性MUST**(パッと出ない・スッと消えない)。
   - **回復=注がれる**: 上から細い注ぎ(オーブ上端→液面)+着水点の波紋(~0.9秒・叩き台)。
   - **危険演出は足さない**(現行の色段階のみ継承。「液体の動きだけ」の裁定に忠実=濁り/脈動は入れない)。
2. **駆動と負荷(MUST)**: 波のアニメは**rAFローカル**(Reactのstate/storeを毎フレーム触らない)。
   HP/EXPの購読は現行のままイベント駆動。
   - **rAFの規律(監査A-4/A-5)**: ハンドルはrefで1本(多重起動ガード)・unmountでcancel・
     `dt=min(dt,0.05)`クランプ(タブ復帰/ポーズ明けの巨大dtで液面が飛ばない)。
     **停止条件は数値で**: `|shownHp−hp|<0.001 かつ 振幅<0.02` になったら shownHp を実HPへ
     スナップして停止(指数追従は漸近=これが無いと永久に止まらない)。
     ポーズ中は実時間で減衰が進み自然に静まる(=許容・実害なし)。
   - **初期マウント**: shownHp=現HPへスナップ(出撃直後に注ぎ演出はしない)。
   - 負荷 2/10: 支払うのはrendering(76px canvas 1枚・活動時のみrAF)。安全弁=上記の停止+
     `?wineorb=0`。simulation/audio/memoryへの影響ゼロ。
3. **ツマミ**: `?wineorb=0` で旧静的塗りへ復帰。実装層はリポ前例(config/devtools.ts の
   モジュール定数・LowHpVignette.tsx:5と同型)。
4. **§2b(後続・本バッチではやらない)**: 吸血(vampire)の血の粒をこのオーブへ収束させ、
   到着で液面が跳ねる(ワールド→HUD座標変換が要るためバッチ分離)。

## 受け入れ条件(先行バッチ)
1. typecheck 0 / lint エラー0。
2. **再レンダー規律**: DirectorLine後継・VitalsOrbとも、プレイ中に毎フレームのReact再レンダーを
   発生させない(hudDirectorミラー/HP変化時のみ。波はrAFローカル)。
3. rAFは波・注ぎが収まったら止まる(アイドルでCPUを食わない)。
4. ランク7=全幅・フェード無し/ランク1〜6=中央から伸び両端フェード、が実機で見えること
   (これは社長の実機確認に委ねる。push前はコードの配線確認まで)。
5. `?wineorb=0`・`?speedramp` 等既存ツマミと非干渉。DirectorLine置き換えで購読が増えないこと。
6. 憲法テストgreen(シミュレーション層は1bitも触らない)。※DirectorLine/VitalsOrbを参照する既存
   テストは0件(監査C-2の実測)=このバッチの検証はtypecheck+lint+コード配線確認+社長実機。
